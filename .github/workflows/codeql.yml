# CodeQL static analysis for C#. The job "Analyze C#" always completes successfully on every PR
# (doc-only PRs skip init/build/analyze but still report green) so required checks stay satisfied.
#
# Trigger model (see CI_POLICY.md § Repository Evidence):
#   - pull_request       — path-filtered; skips init/build/analyze when no code files change
#   - workflow_dispatch  — manual re-run
#   - schedule (weekly)  — full security-and-quality scan
#
# push-to-main was removed: the squash-merged commit was already analyzed seconds earlier
# on its PR, and CodeQL re-running on every main push was pure duplicate work.
# SARIF results upload to the repository Security tab.

name: CodeQL

on:
  pull_request:
  workflow_dispatch:
  schedule:
    - cron: "27 5 * * 1"

concurrency:
  group: codeql-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: false

permissions:
  contents: read
  # Required so github/codeql-action/analyze can upload SARIF to the Security tab.
  security-events: write

jobs:
  analyze:
    name: Analyze C#
    runs-on: ubuntu-latest
    timeout-minutes: 120

    steps:
      - name: Check out repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Detect code / project changes (PR only)
        id: filter
        if: github.event_name == 'pull_request'
        uses: dorny/paths-filter@v3
        with:
          filters: |
            code:
              - "**/*.cs"
              - "**/*.fs"
              - "**/*.csproj"
              - "**/*.vbproj"
              - "**/*.sqlproj"
              - "**/*.sln"
              - "**/*.slnx"
              - "**/Directory.Build.props"
              - "**/Directory.Build.targets"
              - "**/Directory.Packages.props"
              - "**/global.json"
              - "**/*.razor"
              - "**/*.cshtml"
              - "**/*.xaml"
              - "**/*.resx"
              - "**/NuGet.config"
              - "**/nuget.config"

      - name: CodeQL skipped (no matching paths in this PR)
        if: github.event_name == 'pull_request' && steps.filter.outputs.code != 'true'
        run: |
          echo "CodeQL analysis skipped — no matching code or project files in this pull request."
          exit 0

      - name: Set up .NET
        if: github.event_name != 'pull_request' || steps.filter.outputs.code == 'true'
        uses: actions/setup-dotnet@v5
        with:
          dotnet-version: 10.0.x

      - name: Initialize CodeQL
        if: github.event_name != 'pull_request' || steps.filter.outputs.code == 'true'
        uses: github/codeql-action/init@v3
        with:
          languages: csharp
          build-mode: manual
          queries: "${{ github.event_name == 'pull_request' && 'security-extended' || 'security-and-quality' }}"

      - name: Restore and build
        if: github.event_name != 'pull_request' || steps.filter.outputs.code == 'true'
        run: dotnet restore RoslynMcp.slnx && dotnet build RoslynMcp.slnx -c Release --no-restore

      - name: Perform CodeQL Analysis
        if: github.event_name != 'pull_request' || steps.filter.outputs.code == 'true'
        uses: github/codeql-action/analyze@v3
