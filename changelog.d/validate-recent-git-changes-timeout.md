---
category: Fixed
---

- **Fixed:** `validate_recent_git_changes` now returns a structured retryable timeout result with the git-derived changed-file set when an internal validation phase exceeds its budget (`validate-recent-git-changes-timeout`).
