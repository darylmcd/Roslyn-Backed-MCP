---
category: Fixed
---

- **Fixed:** release drift, assembled-release, edit-guard, bump, release-cut, and publication contracts now treat the plugin's exact `dnx` package pin as the seventh version source; stale or missing pins fail validation. Closes `plugin-dnx-pin-release-sync`.
