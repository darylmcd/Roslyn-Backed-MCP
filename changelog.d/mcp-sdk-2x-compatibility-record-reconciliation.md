---
category: Changed — BREAKING
---

- **Changed — BREAKING:** Reconciled the direct MCP C# SDK 1.4.1→2.1.0 adoption and its dual-protocol wire contract. Schema-declaring tools now build explicit producer-owned dual-channel results from typed DTOs instead of returning JSON strings, response projection preserves producer-owned result/content metadata, and 2026-07-28 cache/result fields are omitted from legacy sessions. The `workspace_drift_check` description now also uses the advertised `filesDrifted` casing. Clients must consume `structuredContent` according to the advertised schema and treat `resultType`/`ttlMs`/`cacheScope` as negotiated fields. Release tooling now refuses patch/minor consumption of a pending breaking fragment, rejects a consumed breaking section that does not advance the major version, stops before restore when any child validation script exits nonzero, and safely retries only transient removal failures for its guarded per-run test directory while preserving simultaneous test and cleanup failures. The NuGet workflow and manual publisher also refuse every unconsumed fragment before package selection or push, while typed Boolean workflow conditions make a requested dry run non-publishing. ADR 0003 records the evaluated preview/RC lineage, consumer migration, and still-tracked resource-error, logging, MRTR, sampling, completeness, disclosure, and notification corrections without claiming they shipped; `protocol-version-result-shape-wire-contract` remains open as the synthetic non-object dual-era compatibility guard. The upgrade matrix now points to GitHub's default CodeQL setup instead of the deleted advanced workflow, and publish preflight uses the canonical six-file version count. Closes `mcp-sdk-2x-compatibility-record-reconciliation`, `schema-result-ownership-single-source`, `structured-projector-lossless-envelope-preservation`, `schema-tool-native-structured-results`, `cache-hints-protocol-version-gating`, `workspace-drift-description-schema-casing`, `breaking-fragment-major-version-gate`, `publish-path-breaking-contract-enforcement`, `publish-workflow-dry-run-boolean-gate`, `verify-release-child-script-exit-propagation`, `release-test-temp-cleanup-retry`, `upgrade-matrix-codeql-default-setup-drift`, and `publish-preflight-version-count-drift`.
