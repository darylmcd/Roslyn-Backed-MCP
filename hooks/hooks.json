{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "mcp__roslyn__(rename_apply|code_fix_apply|fix_all_apply|extract_interface_apply|extract_type_apply|move_type_to_file_apply|remove_dead_code_apply|apply_composite_preview|apply_project_mutation|apply_code_action|bulk_replace_type_apply|format_document_apply|organize_usings_apply|scaffold_type_apply|scaffold_test_apply|create_file_apply|delete_file_apply|move_file_apply|format_range_apply)",
        "hooks": [
          {
            "type": "prompt",
            "prompt": "The agent is about to apply a mutation to the Roslyn workspace. Verify that valid preview evidence exists earlier in this conversation for the same file(s) and symbol(s) being applied now.\n\nAccept (respond 'proceed') when EITHER of these is true:\n(a) A direct matching `*_preview` call appears in the recent conversation turns covering the same file/symbol as the current apply (e.g. `move_type_to_file_preview` for `Foo` targeting `Bar.cs`, followed by `move_type_to_file_apply` for the same `Foo` / `Bar.cs`). The preview may return either a patch/diff or a preview token — both count as evidence.\n(b) An `apply_composite_preview` call appears in the recent turns that bundled/redeemed one or more individual previews covering the same file/symbol. Composite-preview redemption is equivalent to an individual preview for the purposes of this check — do NOT block a follow-up apply just because the preview token was consumed by a composite apply.\n\nBlock (deny and instruct the agent to call the matching `*_preview` first) when:\n- No preview call appears anywhere in the conversation for this file/symbol.\n- The only preview present is stale: it targets a different file, a different symbol, or is separated from the current apply by many unrelated turns such that it is implausibly still the intended change.\n\nExamples:\n1. Three turns ago the agent called `move_type_to_file_preview(type='Foo', targetFile='Bar.cs')` and reviewed the diff; now it calls `move_type_to_file_apply(type='Foo', targetFile='Bar.cs')`. Respond 'proceed'.\n2. Earlier the agent called `apply_composite_preview` which redeemed a bundle that included a `move_type_to_file_preview` for `Foo` → `Bar.cs`; now it calls `move_type_to_file_apply` for the same type/target. Respond 'proceed' — composite-preview redemption counts as preview evidence.\n3. The agent calls `move_type_to_file_apply(type='Foo', targetFile='Bar.cs')` with no `move_type_to_file_preview`, no `apply_composite_preview`, and no other preview evidence for `Foo` anywhere in the conversation. Block it and instruct the agent to call `move_type_to_file_preview` first.\n\nRespond with 'proceed' if valid preview evidence exists under (a) or (b), or explain concisely why it should be blocked."
          }
        ]
      }
    ],
    "PostToolUse": [
      {
        "matcher": "mcp__roslyn__server_info",
        "hooks": [
          {
            "type": "prompt",
            "prompt": "Check the server_info response for the 'update' field. If 'update.updateAvailable' is true, briefly tell the user that a newer version of the Roslyn MCP plugin is available (mention the current and latest versions) and suggest running /roslyn-mcp:update. If update is null or updateAvailable is false, respond with 'ok' and do not mention updates."
          }
        ]
      },
      {
        "matcher": "mcp__roslyn__(rename_apply|extract_interface_apply|extract_type_apply|move_type_to_file_apply|bulk_replace_type_apply|remove_dead_code_apply|apply_composite_preview|move_file_apply|create_file_apply)",
        "hooks": [
          {
            "type": "prompt",
            "prompt": "A Roslyn refactoring or file mutation was just applied. Remind the agent to run compile_check or build_workspace to verify no compilation errors were introduced, unless it already plans to do so in its next step. Respond with a brief one-line reminder, or 'ok' if a verification step is already planned."
          }
        ]
      }
    ]
  }
}
