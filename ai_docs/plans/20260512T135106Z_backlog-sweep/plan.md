# Backlog sweep plan — 20260512T135106Z

**Generated:** 2026-05-12T13:51:06Z
**Backlog snapshot:** 2026-05-12T13:51:06Z (`ai_docs/backlog.md` updated_at)
**Mode:** `/backlog-sweep:prepare count=25`
**Pipeline phase:** skeleton (Phase A complete; Phases B-G pending)
**Cycle:** 0 / 2
**Addenda:** loaded (`ai_docs/prompts/backlog-sweep-addenda.md`)
**Selected:** 22 initiatives (max eligible after excluding 7 Reserved-for-contributor rows; user requested count=25)

## Selection notes

- Source backlog has 29 actionable rows (2 High + 5 Medium + 22 Low − 7 Reserved-for-contributor). All 22 non-Reserved rows are selected.
- Reserved rows excluded per planner Step 1 hard-skip: `add-project-reference-self-reference-not-rejected`, `compile-check-project-filter-miss-no-error-envelope`, `find-duplicated-methods-mcp-wrapper-false-positive`, `get-prompt-text-multi-step-required-param-errors`, `goto-type-definition-builtins-invalidoperation`, `test-run-unfiltered-no-failure-envelope`, `workspace-load-prewarm-double-nullable`.
- Defer rows excluded: 5 (per backlog `## Defer` section).
- Prior plan `20260511T184004Z_backlog-sweep` completed cleanly (status: completed, 19 merged + 4 obsolete + 2 deferred); no in-flight initiatives. Duplicate-check ran during /backlog-intake earlier today and dropped `get-syntax-tree-byte-cap-not-enforced` as a duplicate of obsolete `get-syntax-tree-maxtotalbytes-not-enforced` (pre-shipped per 2026-05-12 re-vet).
- Ranking order: correctness risk (server-impacting bugs > skill-side coverage > docs-only) → blast radius → feasibility.
- Rule 1 bundle candidates probed and rejected: (workspace-close-with-drain, parallel-mode-workspace-cap) share `WorkspaceManager.cs` but address different code paths (close vs load cap); (parameter-naming-canonicalization, skill-prompts-deprecated-workspace-load-param-name-cleanup) split server-side vs skill-side discipline; (audit-and-refactor-skills-roslyn-self-check, routine-flows-wrap-csharp-work-with-roslyn-bookends) target different skill prompt files. All marked `bundleCandidatePeers: []`.

## Initiatives

### 1. parallel-mode-workspace-cap-lru-or-raise

| Field | Content |
|---|---|
| Status | merged (PR #681, 2026-05-12) |
| Backlog rows closed | `parallel-mode-workspace-cap-lru-or-raise` |
| Diagnosis | Root cause confirmed at `src/RoslynMcp.Roslyn/Services/WorkspaceManager.cs:192-195`: `_workspaceSlots.WaitAsync(0, ct)` times out immediately when all 8 semaphore slots are consumed, throwing `InvalidOperationException("The server is already tracking 8 workspaces...")` with no recovery hint. `WorkspaceSession.LastAccessedUtc` (line 2093) and `WorkspaceSession.TouchAccess()` (line 2098) already exist and are maintained by the existing `FindSessionByLoadedPath` dedup path (line 183). The cap default of 8 lives in `src/RoslynMcp.Roslyn/Services/WorkspaceManagerOptions.cs:12`. The `workspace_load` tool surface is at `src/RoslynMcp.Host.Stdio/Tools/WorkspaceTools.cs:18` with the `LoadWorkspace` method at line 21. Two fix paths were evaluated: (A) raise `MaxConcurrentWorkspaces` default to 16 in `WorkspaceManagerOptions.cs:12` — cheap, directly resolves the 3 observed saturation events in session 758e3c35 where a 16-initiative parallel sweep hit the 8-workspace limit; (B) add `evictPolicy="lru"` parameter to `workspace_load` which, when the cap is hit, evicts the least-recently-used workspace (using the already-tracked `LastAccessedUtc` on each `WorkspaceSession`) instead of throwing. Both are implemented together: raising the default reduces the common-case friction while LRU provides a robust recovery path when sweeps run deeper. The enriched strict-mode error envelope (`activeWorkspaces` + `lruCandidate`) allows callers to self-recover with one extra round-trip, matching the `WorkspaceEvicted` hint pattern established in `WorkspaceManagerEvictionTests.cs`. |
| Approach | 1. `src/RoslynMcp.Roslyn/Services/WorkspaceManagerOptions.cs`: Change `MaxConcurrentWorkspaces` default from `8` to `16` at line 12. 2. `src/RoslynMcp.Roslyn/Services/WorkspaceManager.cs`: In `LoadAsync()` at lines 192-196, accept an `EvictPolicy evictPolicy = EvictPolicy.Strict` parameter. When `_workspaceSlots.WaitAsync(0, ct)` returns false AND `evictPolicy == EvictPolicy.Lru`: enumerate `_sessions.Values`, pick the entry with the smallest `LastAccessedUtc`, call `Close(lruId)` (releasing its semaphore slot), then retry `WaitAsync`. When `evictPolicy == EvictPolicy.Strict` (existing path): enrich the thrown `InvalidOperationException` message to include a JSON-serializable `activeWorkspaces: [{id, loadedAtUtc, path}, ...]` + `lruCandidate: <id>` field, mirroring the `WorkspaceEvictedException` shape established in the eviction tests. Add `enum EvictPolicy { Strict, Lru }` in the same file or a sibling. 3. `src/RoslynMcp.Host.Stdio/Tools/WorkspaceTools.cs`: Add `evictPolicy` parameter to `LoadWorkspace` (line 21) with `[Description(...)]` and wire it through to `workspace.LoadAsync(path, evictPolicy, c)`. The `workspace_close` method at line 98 and the existing `Close()` implementation are unchanged. Mirror the `expandSanctionedRoots` optional parameter pattern for the new `evictPolicy` parameter. |
| Scope | Production files touched: 3 — `src/RoslynMcp.Roslyn/Services/WorkspaceManagerOptions.cs`, `src/RoslynMcp.Roslyn/Services/WorkspaceManager.cs`, `src/RoslynMcp.Host.Stdio/Tools/WorkspaceTools.cs`. Test files added: 1 — new `tests/RoslynMcp.Tests/WorkspaceCapLruEvictionTests.cs` (2 test methods: LRU-evict succeeds on 9th load; strict-mode error envelope contains `activeWorkspaces` + `lruCandidate`). `WorkspaceManager.cs` is a listed addenda hotspot; this initiative must not share a parallel wave with another hotspot-touching initiative. `IWorkspaceManager` interface may require an `evictPolicy` overload — if the interface is in `src/RoslynMcp.Core/Services/IWorkspaceManager.cs`, that counts as a 4th production file; executor must confirm at vet time and split if Rule 3 is exceeded. |
| Tool policy | edit-only |
| Estimated context cost | 45000 |
| Risks | (1) `IWorkspaceManager` interface: if `LoadAsync` is declared in `src/RoslynMcp.Core/Services/IWorkspaceManager.cs` with an overload that must be updated, production file count rises to 4 — still within Rule 3 but requires confirmation during re-vet. (2) LRU eviction of a workspace that is concurrently in use: `Close()` acquires no read-lock on the evicted session; the executor must check whether evicting a session while a concurrent read is in flight is safe (the `WorkspaceExecutionGate` owns the per-session lock at the tool layer, not the manager layer — eviction from within `LoadAsync` bypasses the gate). A safe implementation would skip sessions that are currently locked by the gate. (3) Raising the default cap from 8 to 16 increases peak RSS by up to 2× for 16-project solutions loaded simultaneously; acceptable for maintainer workstations but worth noting. (4) `WorkspaceManager.cs` is a hotspot file; schedule this initiative in its own wave. |
| Validation | (1) Build: `dotnet build RoslynMcp.slnx -c Release -p:TreatWarningsAsErrors=true` must pass. (2) New test `WorkspaceCapLruEvictionTests.WorkspaceManager_LruEviction_NinthLoadSucceeds`: construct `WorkspaceManager` with `MaxConcurrentWorkspaces=8`, load 8 distinct mock workspaces, call `LoadAsync(path9, EvictPolicy.Lru, ct)`, assert return is non-null success and `_sessions.Count == 8`. (3) New test `WorkspaceCapLruEvictionTests.WorkspaceManager_StrictMode_ErrorEnvelopeContainsLruCandidate`: construct with cap=1, load 1 workspace, call `LoadAsync(path2, EvictPolicy.Strict, ct)`, catch `InvalidOperationException`, assert message contains `"lruCandidate"` and `"activeWorkspaces"`. (4) Run `mcp__roslyn__compile_check` after each edit. (5) Run `mcp__roslyn__test_run --filter "WorkspaceCapLruEviction"` to exercise both new test methods before PR. |
| Performance review | N/A — correctness fix, no hot-path changes. The cap-check is a semaphore wait that already short-circuits on failure; the LRU scan over a bounded dictionary (≤16 entries) is O(N) on a tiny N. |
| CHANGELOG category | Fixed |
| CHANGELOG entry (draft) | Fixed parallel-mode workspace saturation: raised the default `MaxConcurrentWorkspaces` from 8 to 16 and added `evictPolicy="lru"` to `workspace_load` so callers can opt into silent LRU eviction of idle workspaces instead of receiving a hard error. Strict-mode errors now include `activeWorkspaces` and `lruCandidate` fields for one-round-trip self-recovery. |
| Backlog sync | Close rows: [`parallel-mode-workspace-cap-lru-or-raise`]. |

<details>
<summary>Sonnet handoff notes</summary>

- **IWorkspaceManager location:** the interface is at `src/RoslynMcp.Roslyn/Contracts/IWorkspaceManager.cs:43` (`LoadAsync(string path, CancellationToken ct)`), NOT `src/RoslynMcp.Core/Services/...` as the planner hypothesized. Production file count IS 4 (still within Rule 3). The implementation has a no-globals shim at `WorkspaceManager.cs:157` that forwards to the 3-arg overload at `WorkspaceManager.cs:160` — preserve that shim shape when adding `evictPolicy`.
- **EvictPolicy placement:** commit to `src/RoslynMcp.Roslyn/Contracts/EvictPolicy.cs` (new file, NOT counted as a 5th production file because it's a one-line enum addendum to the interface contract layer; cite as part of the interface unit). Defining it in `WorkspaceManager.cs` creates a Roslyn→Contracts back-reference for the interface.
- **Test target:** add `WorkspaceCapLruEvictionTests.cs` as a new file in `tests/RoslynMcp.Tests/`; mirror `WorkspaceManagerEvictionTests.cs:339-346` (`CreateManager()` factory) for the manager construction pattern and `:51-52` (`[DoNotParallelize] [TestClass]`) for the parallelism attributes. Sample-solution fixture pattern is at `:58-67`.
- **Hotspot seam:** the LoadAsync edit goes in the 3-arg overload at `WorkspaceManager.cs:160-266`, specifically the slot-wait block at `:192-196` and the `finally` at `:259-265`. Do NOT modify `LoadIntoSessionAsync` (`:202`) or the dedup paths at `:178-189` / `:209-223` — those operate on already-acquired slots.
- **Edge cases:** (1) eviction of a session whose write-lock is held by a concurrent `RunWriteAsync` / `RunLoadGateAsync` caller — either skip locked sessions during LRU candidate scan or document that the manager-layer eviction bypasses the gate intentionally; (2) idempotent-load (dedup hit at `:180`) must NOT count as cap consumption — the `TouchAccess()` at `:183` already covers it; (3) `Close(lruId)` releases the semaphore at `WorkspaceManager.cs:301` — do NOT also release in the retry path or the slot count will drift.
- **Negative space:** do NOT touch `ReloadAsync` (`:268`) or the race-winner dedup at `:209-223` — those are existing-session paths. Do NOT change `WorkspaceManager.cs:192` to anything other than `WaitAsync(0, ct)` on the strict path — a non-zero timeout would change semantics for non-LRU callers. Also: the `Description(...)` on `LoadWorkspace` at `WorkspaceTools.cs:18` mentions "default 8" — update to "default 16" when raising the cap.

</details>

<!-- deepener-notes: judgmentHeavy=true; IWorkspaceManager interface file count unconfirmed and may push production files to 4 at re-vet time. Executor must confirm the interface situation before proceeding. -->


### 2. routine-flows-wrap-csharp-work-with-roslyn-bookends

| Field | Content |
|---|---|
| Status | merged (PR #678, 2026-05-12) |
| Backlog rows closed | `routine-flows-wrap-csharp-work-with-roslyn-bookends` |
| Diagnosis | Root cause confirmed: none of the four targeted prompt/skill files contain a workspace-bookend prelude. `ai_docs/prompts/backlog-sweep-plan.md` (repo-local copy) has no `workspace_load` or `.csproj`/`.sln` probe anywhere in its text. `ai_docs/prompts/backlog-sweep-execute.md` references `workspace_load` only in concrete code-path examples (lines 342, 359, 728 are implementation-context snippets, not an entry-time probe block). `skills/refactor/SKILL.md:18` says "If a workspace is not already loaded, ask the user for the solution path and load it first" — conditional-ask only, no auto-probe on entry. `skills/refactor-loop/SKILL.md:17` is identical in shape. `skills/workspace-health/SKILL.md` does NOT contain a CWD classifier or `applicable:` field today — the backlog row's reference to this skill as a "CWD probe pattern" is forward-looking (workspace-health row #4 in this plan adds that feature). The executor will need to invent the probe pattern (Glob for `*.csproj`/`*.sln`/`*.slnx` in CWD) rather than copy from workspace-health. `validate_workspace` at `src/RoslynMcp.Host.Stdio/Tools/ValidationBundleTools.cs:21` is confirmed live and is the exit-gate anchor. Evidence confirms the gap: 9 of 16 inspected sessions made zero Roslyn semantic calls during C# work where semantic tools were directly applicable. |
| Approach | Add a **workspace-bookend prelude** block to each of the four targeted prompt/skill files. The prelude pattern for each file: (1) Glob CWD for `*.csproj`, `*.sln`, `*.slnx` — if zero hits, skip and note `applicable: false`; (2) on positive hit, call `workspace_load` on the found solution/project path (prefer `.slnx` or `.sln` over bare `.csproj`); (3) inject a "recommended-tool block" into the session context naming the top 5 semantic primitives: `find_references`, `find_consumers`, `find_implementations`, `compile_check`, `validate_workspace`. Exit-gate: call `validate_workspace` (at `src/RoslynMcp.Host.Stdio/Tools/ValidationBundleTools.cs:21`) once before flow exit as the post-mutation gate. Specific changes per file: (a) `ai_docs/prompts/backlog-sweep-plan.md` — add a new "Step 0b — Workspace bookend" subsection under Step 0, before candidate selection; (b) `ai_docs/prompts/backlog-sweep-execute.md` — add a "Workspace prelude" block at the top of Step 3 (the per-initiative pre-work phase), where the subagent environment is established; (c) `skills/refactor/SKILL.md` — replace the conditional-ask at line 18 ("If a workspace is not already loaded, ask the user…") with an auto-probe that globs first and loads on positive hit, with a non-C# skip path; (d) `skills/refactor-loop/SKILL.md` — same replacement at the equivalent conditional-ask line. Test file to add: `tests/RoslynMcp.Tests/Skills/RoslynBookendPromptTests.cs` with two lockstep content-assertion methods: one asserting that each targeted prompt file contains the `workspace_load` probe language and the `validate_workspace` exit-gate reference; one asserting the non-C# skip path language (`applicable: false` or equivalent) is present. |
| Scope | Production files touched: 4 — `ai_docs/prompts/backlog-sweep-plan.md`, `ai_docs/prompts/backlog-sweep-execute.md`, `skills/refactor/SKILL.md`, `skills/refactor-loop/SKILL.md`. Test files added: 1 — `tests/RoslynMcp.Tests/Skills/RoslynBookendPromptTests.cs`. No files deleted. Note: `skills/refactor/SKILL.md` and `skills/refactor-loop/SKILL.md` are also touched by initiative 13 (`audit-and-refactor-skills-roslyn-self-check`), which adds an exit-time self-check block to those same files. The edits are in different sections (prelude vs exit block) so content conflicts are minimal, but parallel execution would cause a merge conflict in the same files. Executor must schedule these in different waves or serial order with initiative 13 running after this one. |
| Tool policy | edit-only |
| Estimated context cost | 40000 |
| Risks | (1) The workspace-health CWD probe pattern cited in the backlog row as a model does not yet exist in `skills/workspace-health/SKILL.md` — initiative 4 (`workspace-health-csharp-applicability-classifier`) adds it. The executor for initiative 2 must invent the probe pattern independently; if initiative 4 lands first, re-check for consistency. (2) `skills/refactor/SKILL.md` and `skills/refactor-loop/SKILL.md` are shared with initiative 13 (`audit-and-refactor-skills-roslyn-self-check`). Do not parallel-execute initiatives 2 and 13 in the same wave. (3) The backlog-sweep-plan.md and backlog-sweep-execute.md edited here are repo-local copies under `ai_docs/prompts/`; the global canonical copies live under `~/.claude/prompts/`. The executor must confirm which copies the skills actually load at runtime (the repo-local addenda contract suggests `ai_docs/prompts/` is what the local plan/execute commands read). If the global copies are authoritative, this initiative's production-file list changes. (4) Probe scope: the CWD glob for `*.csproj`/`*.sln`/`*.slnx` could be fooled by sample or test projects in subdirectories. The prelude language should specify top-level-first search or limit to `depth=1` to avoid loading the wrong project. |
| Validation | (1) Read each of the four modified prompt files and confirm the `workspace_load` prelude block is present and syntactically correct markdown. (2) Read each modified file and confirm the non-C# skip path (`applicable: false` or equivalent text) is present. (3) Run `mcp__roslyn__compile_check` on the solution to confirm no `.cs` files were accidentally touched. (4) Run the new `RoslynBookendPromptTests` test class: `mcp__roslyn__test_run --filter RoslynBookendPromptTests` — both assertions must pass. (5) Manually verify: open `skills/refactor/SKILL.md` and confirm the conditional-ask ("If a workspace is not already loaded, ask the user…") is replaced by the auto-probe block, not just augmented alongside it. (6) Run `./eng/verify-ai-docs.ps1` to confirm no broken markdown links were introduced by the new prelude sections. |
| Performance review | N/A — correctness fix, no hot-path changes. Prompt-file edits have no runtime performance impact on the MCP server. |
| CHANGELOG category | Maintenance |
| CHANGELOG entry (draft) | **Improve C# session coverage: add Roslyn workspace-bookend prelude to routine-flow prompts and refactor skills** (`routine-flows-wrap-csharp-work-with-roslyn-bookends`). The backlog-sweep plan/execute prompts and the `refactor`/`refactor-loop` skills now probe CWD for `*.csproj`/`*.sln`/`*.slnx` on entry; positive detection triggers `workspace_load` and surfaces the top 5 Roslyn semantic primitives (`find_references`, `find_consumers`, `find_implementations`, `compile_check`, `validate_workspace`) as a recommended-tool block for every dispatched subagent. Non-C# repos skip cleanly. `validate_workspace` call added as post-mutation exit gate. Addresses a 9-of-16-session missed-opportunity rate observed in multi-session retro. |
| Backlog sync | Close rows: [`routine-flows-wrap-csharp-work-with-roslyn-bookends`]. Update related: [`workspace-health-csharp-applicability-classifier` — review CWD probe consistency when both land; `audit-and-refactor-skills-roslyn-self-check` — must not parallel-execute with this initiative due to shared files]. |

<details>
<summary>Sonnet handoff notes</summary>

- **Anchor line corrections (verify before editing):** Diagnosis cites `skills/refactor/SKILL.md:18` and `skills/refactor-loop/SKILL.md:17` for the conditional-ask line — actual locations are `skills/refactor/SKILL.md:20` ("If a workspace is not already loaded, ask the user for the solution path and load it first.") and `skills/refactor-loop/SKILL.md:16` ("If no workspace is loaded, ask for the solution path and call `workspace_load` first."). Replace the whole line with the auto-probe block, do not augment alongside.
- **Pattern coordinates:** exit-gate anchor is `src/RoslynMcp.Host.Stdio/Tools/ValidationBundleTools.cs:21-24` (`validate_workspace` tool — `ReadOnly = true`, takes `workspaceId` + optional `runTests`). Reference this exact tool name in the prelude exit-gate language.
- **`backlog-sweep-plan.md` insertion point:** there is NO existing "Step 0" in this file — earliest section header is `## Step 1 — Candidate selection (row-level)` at line 219. Insert the new "Step 0b — Workspace bookend" as a new `## Step 0` section immediately before line 219.
- **`backlog-sweep-execute.md` insertion point:** Step 3 begins at line 232 (`## Step 3 — Set up worktree (or branch)`). Insert the "Workspace prelude" at the top of Step 3.
- **Test target:** add `tests/RoslynMcp.Tests/Skills/RoslynBookendPromptTests.cs`. Mirror structure of `tests/RoslynMcp.Tests/Skills/AuditPhaseRunnerHandoffTests.cs` — `[TestClass] sealed`, `File.ReadAllText` + `StringAssert.Contains` shape, repo-root via `TestFixtureFileSystem.FindRepositoryRoot()`.
- **Edge cases to cover:** (1) CWD has multiple loaders — prefer `.slnx` > `.sln` > bare `.csproj`; (2) CWD has only `Directory.Build.props` and no `.csproj`/`.sln`/`.slnx` — skip (not sufficient signal per initiative 4 risk note); (3) glob depth: limit to CWD top-level (depth=1); (4) workspace already loaded from a prior turn — prelude must check loaded state first, not re-load blindly.
- **Negative space:** do NOT edit `~/.claude/prompts/backlog-sweep-{plan,execute}.md` (global copies); do NOT add a CWD probe to `skills/workspace-health/SKILL.md` — that file is initiative 4's exclusive target; do NOT touch the exit-block / self-check sections of `skills/refactor/SKILL.md` and `skills/refactor-loop/SKILL.md` — those are initiative 13's territory. Confine edits to the conditional-ask line replacement only.

</details>

<!-- deepener-notes: judgmentHeavy=true — (1) workspace-health CWD probe pattern doesn't exist yet (initiative 4 adds it); executor must invent; (2) skills/refactor/SKILL.md and skills/refactor-loop/SKILL.md shared with initiative 13. Hard conflict-graph edge with #13. -->


### 3. workspace-close-with-drain-processes-for-teardown

| Field | Content |
|---|---|
| Status | merged (PR #679, 2026-05-12) |
| Backlog rows closed | `workspace-close-with-drain-processes-for-teardown` |
| Diagnosis | `workspace_close` at `src/RoslynMcp.Host.Stdio/Tools/WorkspaceTools.cs:98-127` calls `workspace.Close(workspaceId)` (line 118), which disposes the `MSBuildWorkspace` session (`src/RoslynMcp.Roslyn/Services/WorkspaceManager.cs:285-304` — calls `session.Dispose()` at line 300). `session.Dispose()` disposes only the in-process `MSBuildWorkspace` object; it does NOT shut down the out-of-process `VBCSCompiler.exe` or `MSBuild.exe` server nodes spawned during workspace evaluation. Those processes hold file-system locks on `.worktrees/<initiative>/` directories (specifically `bin/` and `obj/` paths). The `git worktree remove --force` call in sweep teardown fails with `Permission denied` because those processes are still alive. Evidence: 4 cross-repo sessions (b65dddea firing `dotnet build-server shutdown` 5× at L199/283/406/612/817; 8eaad4ff L639; 56bb8a22 L173; 5878f8db L88) used `dotnet build-server shutdown` as a manual workaround. The root code path is `CloseWorkspace` — specifically, it does not run an equivalent drain after `workspace.Close()`. Note: `IWorkspaceManager.Close` interface and `WorkspaceManager.Close` do NOT need to change; the drain is an out-of-process OS concern and belongs at the tool-layer where `IDotnetCommandRunner` is already injected (mirroring `ReloadWorkspace` at `WorkspaceTools.cs:77`). `WorkspaceManager.cs` is NOT touched — this avoids the hotspot-conflict entirely. |
| Approach | 1. In `src/RoslynMcp.Host.Stdio/Tools/WorkspaceTools.cs`: add `IDotnetCommandRunner commandRunner` parameter to `CloseWorkspace` (mirror `ReloadWorkspace`'s injection at line 77) and add `bool drainProcesses = false` optional parameter. After the existing `gate.RunLoadGateAsync` / `workspace.Close` / `gate.RemoveGate` sequence, when `drainProcesses == true` AND `closed == true`, invoke `commandRunner.RunAsync(workingDirectory: ..., targetPath: string.Empty, arguments: ["build-server", "shutdown"], ct)` — use the loaded path's directory as working directory (resolved from the session before close, or the process working directory if unavailable since the session is already removed). Update the `[Description(...)]` attribute to document the new `drainProcesses` flag. 2. In `.claude/skills/release-cut/SKILL.md`: add a teardown step after `workspace_close` calls: `workspace_close(workspaceId: ..., drainProcesses: true)` before `git worktree remove`. 3. In `.claude/skills/reconcile-backlog-sweep-plan/SKILL.md`: same teardown note in the worktree-cleanup phase. No changes to `IWorkspaceManager`, `WorkspaceManager.cs`, or `ServerSurfaceCatalog.Workspace.cs`. |
| Scope | Production files: 1 — `src/RoslynMcp.Host.Stdio/Tools/WorkspaceTools.cs`. Skill files (not in Rule 3 budget): 2 — `.claude/skills/release-cut/SKILL.md`, `.claude/skills/reconcile-backlog-sweep-plan/SKILL.md`. Test files: 1 new — `tests/RoslynMcp.Tests/WorkspaceCloseDrainTests.cs`. `WorkspaceManager.cs` (listed in addenda hotspot table) is explicitly NOT touched; the drain is handled at the tool layer. |
| Tool policy | edit-only |
| Estimated context cost | 30000 |
| Risks | (1) Working directory for the `dotnet build-server shutdown` call: the session is already removed from the registry when we invoke drain, so the loaded path must be captured before `workspace.Close()` is called and passed as working directory. Failing to capture it would produce an ambiguous shutdown targeting the wrong SDK version. (2) `dotnet build-server shutdown` is a best-effort operation — it prints one informational line and may return non-zero on some SDK versions if no server nodes are running. The tool wrapper must treat any non-zero exit as a warning, not an error, and still return the original close result. (3) The backlog row mentions wiring this into `parallel-mode /backlog-sweep:execute` skill teardown — that skill lives in a global `~/.claude/` prompt, not in this repo. Skill wiring in the two cited in-repo skills (`release-cut`, `reconcile-backlog-sweep-plan`) is in scope; global execute-prompt wiring is out of scope for this initiative. (4) `WorkspaceManager.cs` is a declared hotspot; the approach deliberately avoids touching it. |
| Validation | (1) New test `WorkspaceCloseDrainTests.cs`: load a workspace, call `CloseWorkspace(drainProcesses: true)`, verify `commandRunner.RunAsync` was called with arguments `["build-server", "shutdown"]` and the response includes `success: true`. (2) Negative test: `CloseWorkspace(drainProcesses: false)` should NOT invoke `commandRunner`. (3) Verify `workspace_close(workspaceId: <unknown>, drainProcesses: true)` still returns `success: false` without erroring on the drain path. (4) `mcp__roslyn__compile_check` after editing `WorkspaceTools.cs`. (5) Confirm the two skill files are readable by running `./eng/verify-ai-docs.ps1`. |
| Performance review | N/A — correctness fix, no hot-path changes. `workspace_close` is a teardown operation; `dotnet build-server shutdown` adds ~50-200ms on a warm node, which is acceptable at teardown time. |
| CHANGELOG category | Fixed |
| CHANGELOG entry (draft) | Fixed `workspace_close` not releasing MSBuild build-server process locks on Windows after session disposal. Add a `drainProcesses` parameter (default `false`) that, when `true`, runs `dotnet build-server shutdown` after the workspace session is removed — eliminating the `Permission denied` error during `git worktree remove` in parallel-sweep teardown. Wire the flag into the `release-cut` and `reconcile-backlog-sweep-plan` skill teardown sequences. |
| Backlog sync | Close rows: [`workspace-close-with-drain-processes-for-teardown`]. |

<details>
<summary>Sonnet handoff notes</summary>

- **Pattern coordinates:** mirror `src/RoslynMcp.Host.Stdio/Tools/WorkspaceTools.cs:77-96` (`ReloadWorkspace`) for `IDotnetCommandRunner` injection signature; mirror `WorkspaceTools.cs:284-315` (`RestoreAndReloadIfRequiredAsync`) for `commandRunner.RunAsync(workingDirectory, status.LoadedPath, [...], ct)` pattern — note `workingDirectory = Path.GetDirectoryName(status.LoadedPath)`.
- **Capture-before-close ordering:** `workspace.Close(workspaceId)` at line 118 REMOVES the session from `_sessions` (see `WorkspaceManager.cs:285-305`). Resolve `LoadedPath` via `workspace.GetStatus(workspaceId).LoadedPath` BEFORE entering the `RunWriteAsync` close block, or capture it inside the block before calling `workspace.Close`. Do NOT call `GetStatus` after close — the session is gone and the lookup will surface `WorkspaceEvictedException`.
- **Test target:** create new `tests/RoslynMcp.Tests/WorkspaceCloseDrainTests.cs` (sibling to `WorkspaceToolsIntegrationTests.cs`); reuse the `IDotnetCommandRunner` fake pattern at `tests/RoslynMcp.Tests/Workspace/WorkspaceCachePrewarmTests.cs:266-274` (`ThrowingDotnetCommandRunner`) or `tests/RoslynMcp.Tests/HardeningBehaviorTests.cs:92-103` (`HangingDotnetCommandRunner`) — build a recording variant that captures `arguments` for assertion.
- **Drain-failure swallow:** Risk #2 requires treating non-zero exit from `dotnet build-server shutdown` as a warning, not a throw — do NOT mirror `RestoreAndReloadIfRequiredAsync`'s `throw new InvalidOperationException` on `!execution.Succeeded` (line 311). Catch/log/continue; preserve the original `{success, workspaceId}` close payload.
- **Negative space:** do NOT touch `src/RoslynMcp.Roslyn/Services/WorkspaceManager.cs` (declared hotspot; tool-layer placement is the architectural decision); do NOT touch `IWorkspaceManager` contract; do NOT modify `ServerSurfaceCatalog.Workspace.cs:11` entry — adding a parameter to the existing tool does not change the catalog row.

</details>

<!-- deepener-notes: judgmentHeavy=true — two reasonable approach paths (drain inside IWorkspaceManager.Close vs drain at tool layer). Tool-layer selected as architecturally cleaner; avoids WorkspaceManager.cs hotspot. -->


### 4. workspace-health-csharp-applicability-classifier

| Field | Content |
|---|---|
| Status | merged (PR #676, 2026-05-12) |
| Backlog rows closed | `workspace-health-csharp-applicability-classifier` |
| Diagnosis | `skills/workspace-health/SKILL.md` currently has no CWD-language probe. Step 3 ("Enumerate Workspaces") at line 43-50 records "no workspaces loaded" and implicitly suggests `workspace_load`, but does not first assess whether the CWD repo is even a C# repo. Session fcde52df ran the skill against SysLog-Server (a Rust repo); `workspace_list` returned `workspaceCount: 0` (correct), but with no `applicable` flag the skill then surfaced the generic "consider loading a workspace" hint, leading Claude to guess "this is a python repo" from globally-visible `python-refactor` MCP registrations. Root cause: the skill emits workspace hints without first classifying the CWD stack. The secondary anchor at `src/RoslynMcp.Host.Stdio/Tools/WorkspaceTools.cs:175` (`workspace_health` tool) is confirmed present and untouched by this initiative — the backlog row defers any server-side extension to a follow-on row, and this initiative takes the bounded skill-only scope per Rule 3. |
| Approach | Edit `skills/workspace-health/SKILL.md` only. Insert a new **Step 0: CWD Applicability Probe** between the existing "Connectivity precheck" section and "Step 1: Server Identity". The probe: (1) glob CWD for `*.csproj`, `*.sln`, `*.slnx`, and `Directory.Build.props`; (2) if any hit → `applicable: true`, `detectedStack: "csharp"`; else → `applicable: false`, `detectedStack: "unknown"`. Emit `applicable` and `detectedStack` as the first two lines of the output report (before the Server section). In Step 3's zero-workspaces branch, add a conditional: if `applicable: false`, suppress the "consider calling `workspace_load`" hint and instead emit: "CWD does not appear to be a C# repo (`detectedStack: unknown`). The workspace-health skill is most useful when a `.sln` or `.csproj` is present." If `applicable: true` and zero workspaces are loaded, retain the existing "no workspaces loaded — consider `workspace_load`" hint unchanged. No changes to Steps 1–6 logic, output format, or the server-side tool. |
| Scope | Production files touched: 1 — `skills/workspace-health/SKILL.md`. Test files added: 0 (skill-level behavioral test; no server unit-test infrastructure exists for skill `.md` files; validation is manual invocation per the Validation field below). Rule 3: 1 file, well within the ≤4 cap. Rule 3 exemption not required. |
| Tool policy | edit-only |
| Estimated context cost | 20000 |
| Risks | (1) The CWD glob runs in the skill's conversational turn — relies on the `Glob` tool being available to the skill agent; this is universally true for Claude Code sessions but worth noting for constrained environments. (2) `Directory.Build.props` at the CWD root is a valid C# signal but also appears in non-C# monorepos with C# sub-projects; the skill should require at least one of `*.csproj`/`*.sln`/`*.slnx` before setting `detectedStack: csharp` — `Directory.Build.props` alone should not be sufficient. Deepener recommendation: make the glob require `*.csproj` OR `*.sln` OR `*.slnx` as the primary signal; `Directory.Build.props` can be listed as a supporting secondary only. (3) If `applicable: false` suppresses the workspace load hint but the user has already loaded a workspace from a different CWD (e.g. a C# workspace loaded earlier in the session), the skill's Step 3 will still enumerate it — the suppression should only apply to the zero-workspaces hint path, not to the workspace list itself. This is what the Approach specifies; executor should take care not to suppress Step 4/5 per-workspace output. (4) Fanout probe skipped — surgical edit to one skill `.md` file, no cross-cutting symbol changes. |
| Validation | (1) Manual: invoke `workspace-health` skill from a non-C# repo CWD (e.g. `C:/Code-Repo/Jedi-Py-MCP`); assert the report header shows `applicable: false` + `detectedStack: unknown` and the zero-workspaces hint is suppressed. (2) Manual: invoke `workspace-health` from `C:/Code-Repo/Roslyn-Backed-MCP` with no loaded workspaces; assert `applicable: true` + `detectedStack: csharp` + the standard "consider `workspace_load`" hint is present. (3) Manual: invoke `workspace-health` from `C:/Code-Repo/Roslyn-Backed-MCP` with a loaded workspace; assert `applicable: true` appears and Step 4/5 workspace detail is unaffected. (4) Doc check: `./eng/verify-ai-docs.ps1` — confirms no broken links introduced by the edit. |
| Performance review | N/A — correctness fix, no hot-path changes. The added Glob call is a single CWD-only pattern match; negligible overhead. |
| CHANGELOG category | Fixed |
| CHANGELOG entry (draft) | Fixed `workspace-health` skill misclassifying non-C# repos: the skill now probes CWD for `.csproj`/`.sln`/`.slnx` at startup and emits `applicable: bool` + `detectedStack` in the report header. When `applicable: false`, the "consider loading a workspace" hint is suppressed, preventing the misleading stack-guess that occurred when globally-registered MCP servers for other languages were visible in the tool catalog. |
| Backlog sync | Close rows: [`workspace-health-csharp-applicability-classifier`]. |

<!-- deepener-notes: Fanout probe skipped — surgical edit to one skill .md file. Risk note: recommend restricting CWD applicability signal to *.csproj/*.sln/*.slnx only (not Directory.Build.props alone) to avoid false-positive on non-C# monorepos. -->


### 5. parameter-naming-canonicalization-experimental-surface

| Field | Content |
|---|---|
| Status | merged (PR #677, 2026-05-12) |
| Backlog rows closed | `parameter-naming-canonicalization-experimental-surface` |
| Diagnosis | The 2026-05-02 experimental-promotion audit (session 396e0724) drove 51 experimental tools and documented two overlapping inconsistency families across 13 tools + 8 prompts on the experimental surface: (1) NuGet tools use `packageId` on `add_package_reference_preview` / `remove_package_reference_preview` / `add_central_package_version_preview` / `remove_central_package_version_preview` (verified at `src/RoslynMcp.Host.Stdio/Tools/ProjectMutationTools.cs:34,55,197,217`) yet the description for `get_nuget_dependencies` (stable) references `packageId` in its output schema — the naming is internally consistent for NuGet tools but was flagged by the audit as conflicting with a hypothetical `packageName` alias. (2) Locator tools mix LSP-style `character` (column 0-based) vs server-style `column` (column 1-based) across the experimental surface — `find_shared_members` retro evidence (retro §3 P6: *"this server uses parameter name 'column' (1-based), not the LSP-style 'character'"*). (3) One error path on the experimental surface still emits bare *"An error occurred"* with no parameter hint (retro §3 P6). The audit's retrospective rated this *"the most impactful finding — high severity"* of the experimental-promotion exercise. No design note exists yet at `ai_docs/items/parameter-naming-canonicalization-design.md`. The follow-on migration row `skill-prompts-deprecated-workspace-load-param-name-cleanup` explicitly depends on this design landing first. The full migration across 13 tools + 8 prompts is heroic-class — this initiative is scoped to the design note only, following the `parameter-object-preview-design.md` precedent (shipped 2026-05-07). |
| Approach | Produce one new file: `ai_docs/items/parameter-naming-canonicalization-design.md`. No `src/` mutations. The design note must: (1) Enumerate ALL affected tools and prompts on the experimental surface by name, citing their current parameter names in a table (executor reads `src/RoslynMcp.Host.Stdio/Catalog/ServerSurfaceCatalog.*.cs` for experimental tier entries, then reads the corresponding `[McpServerTool]` method signatures in `src/RoslynMcp.Host.Stdio/Tools/*.cs` to extract current parameter names). (2) Decide the canonical naming convention — the backlog row mandates "camelCase, lower-first, no LSP aliases — match the existing `path` precedent from `workspace_load`"; the design note should enshrine this as a binding decision with a one-paragraph rationale. (3) Produce a per-tool migration table: `tool | param | current name | canonical name | breaking?` — mark each rename as breaking if it changes the MCP tool schema. (4) Specify the regression test shape for the follow-on migration initiative: a lockstep test asserting every `[McpServerTool]` parameter name in `src/RoslynMcp.Host.Stdio/Tools/*.cs` matches the canonical convention. **Anchor note:** the backlog row cited `IssueTemplateAndLabelSeedTests.cs` as the lockstep model; that file was shipped in v1.35.1 per CHANGELOG. Executor should verify the file exists at `tests/RoslynMcp.Tests/Skills/IssueTemplateAndLabelSeedTests.cs` and use it as the structural template; if not found, fall back to `ReadmeSurfaceCountTests.cs`. (5) Document a verdict (`go` / `no-go` / `defer`) with next deliverable: file one new backlog row `parameter-naming-canonicalization-migration` after the design lands. The design note should NOT propose any `src/` edits; implementation belongs to the follow-on row. |
| Scope | Production files touched: 0 (the target `ai_docs/items/parameter-naming-canonicalization-design.md` is a doc file, not a `src/` production file — excluded from Rules 3/4 budget per the same convention as `ai_docs/items/parameter-object-preview-design.md`). Test files added: 0 (no regression test in the design-note initiative itself; the test shape is specified in the note for the follow-on migration row). No Rule 3 exemption required — this is doc-only. Rule 5 shape: doc-only / design note. |
| Tool policy | edit-only |
| Estimated context cost | 25000 |
| Risks | (1) Convention decision may be contested at review time — the note should enumerate the tradeoffs explicitly so the reviewer can override. (2) The migration that follows will be BREAKING (MCP tool parameter renames are visible to all callers); the design note must call this out with a CHANGELOG-category recommendation (`Changed — BREAKING`) and suggest a deprecation window or alias strategy if the team wants non-breaking migration. (3) Anchor verification needed: deepener could not locate `tests/RoslynMcp.Tests/Skills/IssueTemplateAndLabelSeedTests.cs` via its initial search; v1.35.1 CHANGELOG says it shipped. Executor should confirm during plan execution. Fall back to `ReadmeSurfaceCountTests.cs` if not present. (4) Fanout probe skipped — surgical doc-only edit, no cross-cutting symbol changes. (5) The design note must not undercount the affected surface: the 396e0724 audit found 13 tools + 8 prompts, but the catalog has grown since 2026-05-02; executor should re-enumerate live experimental entries rather than trusting the audit count. |
| Validation | (1) `./eng/verify-ai-docs.ps1` passes (no broken markdown links). (2) `ai_docs/items/parameter-naming-canonicalization-design.md` exists, has a `Verdict` section, and has a per-tool migration table with at least the tools known from the 396e0724 audit. (3) `dotnet build RoslynMcp.slnx -c Release -p:TreatWarningsAsErrors=true` still passes (no src/ changes, but confirms no accidental file mutation). No new tests ship in this initiative — test shape is specified in the doc for the follow-on row. |
| Performance review | N/A — correctness fix, no hot-path changes. |
| CHANGELOG category | Maintenance |
| CHANGELOG entry (draft) | `Maintenance: add design note for parameter-naming canonicalization across the experimental MCP surface — enumerates affected tools and prompts, establishes canonical camelCase convention (no LSP aliases), and specifies migration contract for the follow-on breaking-rename initiative.` |
| Backlog sync | Close rows: [`parameter-naming-canonicalization-experimental-surface`]. Update related: [`skill-prompts-deprecated-workspace-load-param-name-cleanup` — its `do` cell references "ai_docs/items/parameter-naming-canonicalization-design.md once that design note row's design lands"; after this initiative ships, remove that qualifier in that row]. |

<details>
<summary>Sonnet handoff notes</summary>

- **Anchor confirmed:** `tests/RoslynMcp.Tests/Skills/IssueTemplateAndLabelSeedTests.cs` exists (295 LOC). Mirror its three-surface lockstep shape — `AllAreaValues`/`PubliclyFileableAreaValues` arrays at lines 33-50 + per-surface assertion methods. Specify the migration regression as: assert every `[McpServerTool]` parameter name in `src/RoslynMcp.Host.Stdio/Tools/*.cs` matches canonical camelCase. Fallback to `ReadmeSurfaceCountTests.cs` is unneeded — anchor was located.
- **Precedent coordinates:** model the new note on `ai_docs/items/parameter-object-preview-design.md` (153 LOC) — section structure: `# Title`, `## (a) Use case`, `## (b)..(g)` body, `## Verdict` (line 130), `### Next deliverable` (line 134), `## Refs` table (line 140). Match this exact heading hierarchy.
- **Experimental surface enumeration:** the tier classifier lives in `src/RoslynMcp.Host.Stdio/Catalog/ServerSurfaceCatalog.cs` (5 `Experimental` references). Re-enumerate live experimental tools/prompts from the `ServerSurfaceCatalog.*.cs` partials rather than trusting the audit's count of "13 tools + 8 prompts" — catalog has grown since 2026-05-02.
- **Verdict commitment:** the note must end with a binding `go` / `no-go` / `defer` (precedent: line 132 `**Go for v1.**`) and name the follow-on backlog row `parameter-naming-canonicalization-migration` with `Changed — BREAKING` CHANGELOG category.
- **Negative space:** do NOT edit any `src/` file, any `[McpServerTool]` signature, or `ServerSurfaceCatalog.*.cs`. Do NOT file the follow-on backlog row in this PR — that is the next initiative. Do NOT add a regression test file in this initiative; the test shape is *specified in prose* inside the design note for the migration row to implement.

</details>

<!-- deepener-notes: judgmentHeavy=true, anchorStale (partial — IssueTemplateAndLabelSeedTests.cs cited but deepener's search was incomplete; v1.35.1 CHANGELOG confirms it ships; executor verifies at run-time). productionFilesTouched=0 is deliberate (design note is not a src/ file). -->


### 6. promote-tier-supports-prompts-and-resources

| Field | Content |
|---|---|
| Status | pending |
| Backlog rows closed | `promote-tier-supports-prompts-and-resources` |
| Diagnosis | The `/promote-tier` skill already includes catalog-only resolver paths for resources and prompts (`SKILL.md` Step 2 — "Resources" at lines 106–109 and "Prompts" at lines 112–113). Neither `[McpServerResource]` nor `[McpServerPrompt]` carries a tier metadata attribute today — confirmed by reading `src/RoslynMcp.Host.Stdio/Resources/ServerResources.cs` (only `[McpServerResource(UriTemplate=..., Name=...)]`) and `src/RoslynMcp.Host.Stdio/Prompts/RoslynPrompts.cs` (only `[McpServerPrompt(Name=...)]`). Only `[McpToolMetadata]` exists (`src/RoslynMcp.Host.Stdio/Catalog/McpToolMetadataAttribute.cs`). The real gap is a pending design decision: (a) accept catalog-only promotion as sufficient for resources/prompts — single-write contract, no dual-write parity enforcement — OR (b) introduce `[McpServerPromptMetadata]` / `[McpServerResourceMetadata]` attributes to mirror the `[McpToolMetadata]` dual-write pattern enforced by `SurfaceCatalogTests.McpToolMetadata_RequiredOnEveryTool_MatchesCatalogEntry`. Option (b) would let `/promote-tier` extend naturally but requires new attribute types, catalog test extensions, and annotation of all 20 prompts and 13 resources. The backlog row mandates a design note settling this decision before any code change. No implementation exists yet; the design note file `ai_docs/items/promote-tier-prompts-and-resources-design.md` does not exist. |
| Approach | Create `ai_docs/items/promote-tier-prompts-and-resources-design.md`. The note must: (1) Document the current state — catalog-only resolver paths for resources/prompts in `/promote-tier` are already wired (SKILL.md Step 2); the gap is only the attribute-side parity enforcement that tools enjoy. (2) Evaluate option (a) — catalog-only promotion: single-write, `/promote-tier` works today for resources/prompts against the catalog partial files; no new attributes needed; the tradeoff is no compile-time or test-time guard against catalog/implementation drift. (3) Evaluate option (b) — introduce `[McpServerPromptMetadata]` / `[McpServerResourceMetadata]`: mirrors `McpToolMetadataAttribute.cs`; requires annotating all 20 existing prompts in `src/RoslynMcp.Host.Stdio/Prompts/RoslynPrompts.*.cs` and all 13 resources in `src/RoslynMcp.Host.Stdio/Resources/ServerResources.cs` and `WorkspaceResources.cs`; requires extending `SurfaceCatalogTests.cs` with parity checks; `/promote-tier` SKILL.md Steps 2 and 3 would then apply to prompts/resources identically to tools. (4) State the recommended path with rationale — the design note is the decision artifact. (5) Identify the follow-on implementation row that the executor for the decided path should reference. The design note is the entire deliverable; no source code changes in this initiative. |
| Scope | Production files touched: 1 (`ai_docs/items/promote-tier-prompts-and-resources-design.md` — new design-note file). Test files added: 0. No Rule 3 exemption required — design-note-only initiative, single file. |
| Tool policy | edit-only |
| Estimated context cost | 20000 |
| Risks | (1) The design note must be opinionated enough to unblock the follow-on implementation row — a wishy-washy "both options are valid" document would leave the executor without a clear path. (2) Option (b) has significant annotation surface (20 prompts + 13 resources + 2 test extensions); the design note should include a realistic file-count estimate so the follow-on initiative can be sized correctly against Rule 3. (3) The `ai_docs/items/` path is not a hotspot file per addenda but design notes that reference catalog partial files should avoid linking them with bracket-paren syntax (doc-link verifier rule). Fanout probe skipped. |
| Validation | (1) `ai_docs/items/promote-tier-prompts-and-resources-design.md` exists after the commit. (2) `./eng/verify-ai-docs.ps1` passes. (3) The design note references `.claude/skills/promote-tier/SKILL.md` and both catalog partials by name and contains a named recommendation for either option (a) or option (b) with rationale. (4) The design note ends with a "Follow-on implementation" section that can be directly consumed as the `do` cell for a new backlog row. |
| Performance review | N/A — correctness fix, no hot-path changes. |
| CHANGELOG category | Maintenance |
| CHANGELOG entry (draft) | Add design note for `promote-tier` prompt and resource promotion paths (`ai_docs/items/promote-tier-prompts-and-resources-design.md`) — settles the attribute-or-no-attribute decision that gates the follow-on implementation. |
| Backlog sync | Close rows: [`promote-tier-supports-prompts-and-resources`]. |


### 7. test-related-column-required-schema-mismatch

| Field | Content |
|---|---|
| Status | pending |
| Backlog rows closed | `test-related-column-required-schema-mismatch` |
| Diagnosis | The prior plan's diagnosis was incorrect. Re-reading `src/RoslynMcp.Host.Stdio/Tools/SymbolLocatorFactory.cs:41-82` confirms column is NOT unconditionally required — it is conditional-required-as-a-group with `filePath` and `line`. The three locator modes are: (1) `symbolHandle` alone, (2) `metadataName` alone, (3) `filePath` + `line` + `column` all together. Mode 3 silently accepts a partial call (filePath+line, no column) and then throws `ArgumentException` from `SymbolLocatorFactory.cs:62-78` with a message explaining the missing field. The actual bug: `src/RoslynMcp.Host.Stdio/Tools/ValidationTools.cs:181-185` marks all three source-location parameters as `"Optional: …"` in their `[Description]` attributes — the schema the MCP framework derives from these attributes presents filePath, line, and column as independently optional. An MCP client reading that schema cannot discover the "required-together" constraint for mode 3. Callers following the published schema (omitting column, supplying filePath+line) receive a thrown exception rather than a successful call. The fix is purely a description rewrite on the three source-location parameters in `ValidationTools.cs:181-183` to communicate the conditional-required-as-a-group relationship. No logic change needed; `SymbolLocatorFactory.cs` is correct as-is. |
| Approach | Edit `src/RoslynMcp.Host.Stdio/Tools/ValidationTools.cs` method `FindRelatedTests` (line 177): rewrite the `[Description]` attributes on the three source-location parameters (filePath, line, column) to communicate the three mutually-exclusive locator modes explicitly. New text should follow the pattern established by `symbol_info` and `find_references` on the same parameters. Example rewrite: `filePath` → `"Source-location mode: absolute path to the source file. Must be provided together with line and column. Omit all three to use symbolHandle or metadataName instead."` — likewise for `line` and `column`. Optionally add a sentence to the tool's top-level `[Description]` on line 174 listing the three modes explicitly. No change to `SymbolLocatorFactory.cs`, `ServerSurfaceCatalog.Analysis.cs`, or any service layer. Mirror the existing description style used by `symbol_info`'s locator parameters in `src/RoslynMcp.Host.Stdio/Tools/SymbolTools.cs` for consistency. |
| Scope | Production files touched: 1 — `src/RoslynMcp.Host.Stdio/Tools/ValidationTools.cs`. Test files: extend `tests/RoslynMcp.Tests/ValidationToolsIntegrationTests.cs` with two methods (1 verifying mode-3 partial-call still throws with diagnostic message; 1 verifying mode-2 metadataName round-trip succeeds). Rule 3 exemption applicable: tool-surface-only, description-only change. |
| Tool policy | edit-only |
| Estimated context cost | 22000 |
| Risks | (1) Other tools sharing `SymbolLocatorFactory.Create` — `symbol_info`, `find_references`, `find_type_usages`, `goto_definition`, `goto_implementation`, `goto_type_definition`, `test_related` — have the same conditional-required pattern in their own parameter descriptions. This initiative touches only `test_related` (gh #618). Follow-on sweep would pick up the others as separate rows. (2) If the rewritten description diverges in style from sibling tools, it creates a consistency issue — cross-check against `symbol_info`'s parameter descriptions before finalizing. (3) Fanout probe skipped — surgical description edit. |
| Validation | (1) `mcp__roslyn__compile_check` after editing `ValidationTools.cs`. (2) Run `mcp__roslyn__test_run --filter "ValidationToolsIntegrationTests"`. (3) Manually verify the MCP schema emitted by the server for `test_related` (via raw MCP `tools/list` call) shows the updated description. (4) Confirm `test_related(workspaceId, filePath, line)` without `column` throws with diagnostic message. (5) Confirm `test_related(workspaceId, metadataName=...)` succeeds. |
| Performance review | N/A — correctness fix, no hot-path changes. |
| CHANGELOG category | Fixed |
| CHANGELOG entry (draft) | Fixed `test_related` schema to document the conditional-required-as-a-group relationship between `filePath`, `line`, and `column`. Callers using source-location mode must supply all three together; callers using `symbolHandle` or `metadataName` mode omit all three. Fixes gh #618. |
| Backlog sync | Close rows: [`test-related-column-required-schema-mismatch`]. |

<details>
<summary>Sonnet handoff notes</summary>

- **Edit target (exact lines):** rewrite the `[Description]` text on `filePath`, `line`, `column` at `src/RoslynMcp.Host.Stdio/Tools/ValidationTools.cs:181-183` inside `FindRelatedTests` (signature at `:177`). Tool-level `[Description]` to optionally augment is at `:174`. Do NOT alter the parameter list, default values, types, or the `SymbolLocatorFactory.Create` call at `:191`.
- **Pattern coordinates caveat:** the planner's instruction to "mirror `symbol_info`'s locator parameter style" is misleading — `SymbolTools.cs:125-127` (`GetSymbolInfo`) currently has the SAME generic "Optional: …" text this initiative is fixing. There is no improved sibling to mirror; you are *establishing* the new style. Use `SymbolLocatorFactory.cs:54-78` (the runtime error message text and BUG-001 comment) as the semantic source-of-truth for what the descriptions must convey: three modes (symbolHandle / metadataName / filePath+line+column-together), partial mode-3 throws.
- **Test target:** add 2 methods to the existing `ValidationToolsIntegrationTests` class in `tests/RoslynMcp.Tests/ValidationToolsIntegrationTests.cs` — mirror `TestRelated_UnknownSymbol_ReturnsEmptyList` at `:136-149` for the metadataName-success case and `TestRelated_Returns_Json_For_File` at `:98-113` for the partial-call structure (but assert `ArgumentException` is thrown via `Assert.ThrowsExceptionAsync`). Do NOT create a new test file.
- **Negative space:** other tools sharing this 3-param-locator shape — `SymbolTools.cs`, `AnalysisTools.cs`, `ConsumerAnalysisTools.cs`, `RefactoringTools.cs`, `CohesionAnalysisTools.cs` — also have the bug. Risks #1 in the stanza calls this out: leave them alone, this row covers only `test_related` (gh #618). Follow-on rows pick up the rest.

</details>

<!-- deepener-notes: judgmentHeavy=true — prior plan deferred on diagnosis mismatch; deepener re-framed problem (schema annotation gap, not unconditional-required bug). Description rewrite is the clear correct fix. -->


### 8. move-type-to-project-preview-leaks-projectid-tokens

| Field | Content |
|---|---|
| Status | pending |
| Backlog rows closed | `move-type-to-project-preview-leaks-projectid-tokens` |
| Diagnosis | Root cause confirmed at `src/RoslynMcp.Roslyn/Services/CrossProjectRefactoringService.cs:681-692` — the private `EnsureProjectReference` method calls `solution.AddProjectReference(sourceProjectId, new ProjectReference(targetProjectId))` with no cycle pre-check. When the target project already (transitively) references the source project, Roslyn's workspace internals throw an `InvalidOperationException` whose message contains raw `(ProjectId, #<guid> - <abs-path>)` tuple strings — e.g., session 4868dff2 L599: *"Adding project reference from '(ProjectId, #e87edc0a-...)' to '(ProjectId, #1bdedfd8-...)' would create a circular dependency"*. The `Project` objects are fully available via `solution.GetProject(projectId)` — their `.Name` property gives the human-readable identifier the caller passed in. Contrast with `src/RoslynMcp.Roslyn/Services/ProjectMutationService.cs:566-611`, which has the correct pattern: `ValidateProjectReferenceCanBeAdded` runs a `WouldCreateProjectReferenceCycle` BFS walk using `project.Name` in its message, catching the cycle before Roslyn's internals produce the leaky error. `CrossProjectRefactoringService` has no analogous guard. |
| Approach | In `src/RoslynMcp.Roslyn/Services/CrossProjectRefactoringService.cs`: (1) Rewrite `EnsureProjectReference` (line 681) to resolve `Project` objects from the `Solution` before calling `AddProjectReference` — `solution.GetProject(sourceProjectId)` and `solution.GetProject(targetProjectId)` are non-null at this point since the projects were resolved earlier in the preview flow. (2) Add a private `static bool WouldCreateCrossProjectCycle(Project source, Project target)` method mirroring the BFS walk at `src/RoslynMcp.Roslyn/Services/ProjectMutationService.cs:581-611` — copy the same `HashSet<ProjectId>` + `Stack<Project>` pattern. (3) Before `solution.AddProjectReference`, throw `InvalidOperationException($"Adding project reference from '{source.Name}' to '{target.Name}' would create a circular dependency.")` if the cycle check returns true. No changes to `CrossProjectRefactoringTools.cs`, `ProjectMutationService.cs`, or the interface layer. |
| Scope | Production files touched: 1 — `src/RoslynMcp.Roslyn/Services/CrossProjectRefactoringService.cs`. Test files modified: 1 — `tests/RoslynMcp.Tests/CrossProjectRefactoringIntegrationTests.cs` (add 1 new test method: set up 2-project fixture with ProjectA transitively referencing ProjectB, call `PreviewMoveTypeToProjectAsync` source=ProjectB target=ProjectA; assert thrown `InvalidOperationException` message contains project names not `ProjectId` tuples). Satisfies Rule 3 (1 prod file ≤ 4) and Rule 4 (1 test file ≤ 3). |
| Tool policy | edit-only |
| Estimated context cost | 28000 |
| Risks | (1) `EnsureProjectReference` is called from 3 call sites in the same file (lines 87, 181, 745 — `PreviewMoveTypeToProjectAsync`, `PreviewExtractInterfaceAsync`, `CreateInterfaceExtractionSolutionAsync`); all three benefit from the guard but verify each can reach the cycle condition. (2) BFS cycle check walks the Roslyn `Solution` graph; ensure it handles solutions with many projects without perf regression on the happy path (O(V+E), typically short-circuits quickly). (3) The `solution` passed to `EnsureProjectReference` is the mutated `updatedSolution` (after `AddDocument`), not the original — verify `solution.GetProject(sourceProjectId)` and `solution.GetProject(targetProjectId)` still resolve correctly on the post-document-add state. (4) Fanout probe skipped — surgical edit. |
| Validation | (1) Run the new test asserting the cycle rejection message contains project names not `ProjectId, #<guid>` tuples. (2) Run existing `CrossProjectRefactoringIntegrationTests.cs` suite to confirm no regressions. (3) Run `mcp__roslyn__compile_check` on `CrossProjectRefactoringService.cs` after edit. (4) Manual repro: call `move_type_to_project_preview` with a source project the target already transitively references; confirm the error message uses project names. |
| Performance review | N/A — correctness fix, no hot-path changes. The BFS cycle check is O(V+E) on the project-reference graph; common-path (no cycle) exits after the topological walk. |
| CHANGELOG category | Fixed |
| CHANGELOG entry (draft) | Fixed `move_type_to_project_preview` (and the related `extract_interface_cross_project_preview` / `dependency_inversion_preview`) emitting raw Roslyn `ProjectId` tuple strings in circular-dependency error messages. The error message now reads "Adding project reference from 'ProjectA' to 'ProjectB' would create a circular dependency." using the human-readable project names the caller supplied. |
| Backlog sync | Close rows: [`move-type-to-project-preview-leaks-projectid-tokens`]. |


### 9. document-symbols-accepts-symbol-handle

| Field | Content |
|---|---|
| Status | pending |
| Backlog rows closed | `document-symbols-accepts-symbol-handle` |
| Diagnosis | Confirmed at `src/RoslynMcp.Host.Stdio/Tools/SymbolTools.cs:271-279`: `document_symbols` (and its alias `get_symbol_outline` at line 309-325) accept only `string filePath` — no `symbolHandle` or `metadataName` path. Both delegate to the internal `GetDocumentSymbolsCore` at line 286, which passes `filePath` directly to `symbolSearchService.GetDocumentSymbolsAsync(workspaceId, filePath, c)` (line 298). The service implementation at `src/RoslynMcp.Roslyn/Services/SymbolSearchService.cs:236-248` calls `SymbolResolver.FindDocument(solution, filePath)` — it has no locator-aware path. The `ISymbolSearchService` interface at `src/RoslynMcp.Core/Services/ISymbolSearchService.cs:29` declares `GetDocumentSymbolsAsync(string workspaceId, string filePath, CancellationToken ct)` with no locator overload. By contrast, `symbol_info` (line 118-146 of SymbolTools.cs) accepts all three locator strategies (`filePath`+`line`+`column`, `symbolHandle`, `metadataName`) via `SymbolLocatorFactory.Create`, and `SymbolResolver.ResolveAsync` at `src/RoslynMcp.Roslyn/Helpers/SymbolResolver.cs:33-48` resolves a handle by delegating to `SymbolHandleSerializer.ResolveHandleAsync`. The resolved `ISymbol` carries source location data (`symbol.Locations.FirstOrDefault(l => l.IsInSource)?.GetLineSpan().Path`) from which the file path needed by `GetDocumentSymbolsCore` can be extracted. The fix is additive: add a new overload `GetDocumentSymbolsAsync(workspaceId, SymbolLocator, CancellationToken)` to the service layer, resolve the locator to its source file path, then delegate to the existing `FindDocument` + `CollectSymbols` path. |
| Approach | 1. `src/RoslynMcp.Core/Services/ISymbolSearchService.cs`: Add new overload `Task<IReadOnlyList<DocumentSymbolDto>> GetDocumentSymbolsAsync(string workspaceId, SymbolLocator locator, CancellationToken ct)` alongside the existing `filePath`-based signature. The existing signature stays for backward compatibility with the 4 call sites in `RoslynPrompts.cs`. 2. `src/RoslynMcp.Roslyn/Services/SymbolSearchService.cs`: Implement the new overload — call `SymbolResolver.ResolveAsync(solution, locator, ct)` to get `ISymbol`; extract the source file path via `symbol.Locations.FirstOrDefault(l => l.IsInSource)?.GetLineSpan().Path`; throw `KeyNotFoundException` (matching the `ResolveOrThrowAsync` pattern at line 59-73 of `SymbolResolver.cs`) if resolution fails or no source location exists; then call the existing `filePath`-based implementation. Mirror the locator validation pattern at `SymbolResolver.cs:35` (`locator.Validate()`). 3. `src/RoslynMcp.Host.Stdio/Tools/SymbolTools.cs`: Add `string? symbolHandle = null` and `string? metadataName = null` parameters to `GetDocumentSymbols` (line 271), `GetSymbolOutline` (line 309), and the shared `GetDocumentSymbolsCore` at line 286. In `GetDocumentSymbolsCore`, when any locator field is non-null alongside or instead of `filePath`, use `SymbolLocatorFactory.Create(filePath, null, null, symbolHandle, metadataName)` to build a `SymbolLocator`. If the locator is handle- or metadataName-driven, call the new `GetDocumentSymbolsAsync(workspaceId, locator, ct)` overload; the resolved path is used for the subsequent `ClientRootPathValidator.ValidatePathAgainstRootsAsync` call. If `filePath` is provided without any locator, call the existing overload as today. Mirror the optional-parameter pattern of `symbol_info` at line 124-130. |
| Scope | Production files touched: 3 — `src/RoslynMcp.Host.Stdio/Tools/SymbolTools.cs`, `src/RoslynMcp.Core/Services/ISymbolSearchService.cs`, `src/RoslynMcp.Roslyn/Services/SymbolSearchService.cs`. Test files added: 1 — new `tests/RoslynMcp.Tests/DocumentSymbolsSymbolHandleTests.cs` (1 test method: call `symbol_info` to obtain a handle for a known type in SampleSolution, then call `document_symbols(symbolHandle=...)` and assert the response shape and `count` match the equivalent `filePath`-driven invocation). Rule 3 satisfied (3 ≤ 4). Rule 4 satisfied (1 ≤ 3). No hotspot files touched. |
| Tool policy | edit-only |
| Estimated context cost | 35000 |
| Risks | (1) `ClientRootPathValidator` call: the validator currently receives `filePath` directly; when the path is resolved from a symbol handle, the executor must ensure the resolved path is passed to the validator (not the raw handle string). (2) `metadataName` locator: for non-type symbols (e.g. method `SampleLib.AnimalService.GetAllAnimals`), the resolved symbol's source location gives the file containing the method — the outline returned will be the full file outline, not just the method. Consistent with filePath-based behavior; tool description should say so. (3) Symbols defined in metadata only (BCL types, NuGet packages with no source) have no source location — `GetLineSpan().Path` will be null/empty. Implementation must return a clear `KeyNotFoundException` rather than throwing a null-dereference. Add a guard in the new overload. (4) The existing `filePath`-based overload on `ISymbolSearchService` is retained, so the 4 call sites in `RoslynPrompts.cs` are unaffected. Fanout probe skipped — additive parameter additions only. |
| Validation | (1) Build: `dotnet build RoslynMcp.slnx -c Release -p:TreatWarningsAsErrors=true` must pass. (2) New test `DocumentSymbolsSymbolHandleTests.DocumentSymbols_AcceptsSymbolHandle_ReturnsMatchingOutline`: use `SharedWorkspaceTestBase`; call `SymbolTools.GetSymbolInfo(...)` to get a `symbolHandle` for the `AnimalService` class in SampleSolution; then call `SymbolTools.GetDocumentSymbols(workspaceId, filePath: null, symbolHandle: handle)`; deserialize response and assert `count > 0` and the outline matches the one returned by the `filePath`-driven call on the same file. (3) Run `mcp__roslyn__compile_check` after each edit. (4) Run `mcp__roslyn__test_run --filter "DocumentSymbolsSymbolHandle"`. (5) Manual verification: confirm `get_symbol_outline(workspaceId, symbolHandle=<handle>)` also works. |
| Performance review | N/A — correctness fix, no hot-path changes. |
| CHANGELOG category | Fixed |
| CHANGELOG entry (draft) | Fixed `document_symbols` and its alias `get_symbol_outline` to accept either `filePath` OR `symbolHandle` (or `metadataName`), mirroring the locator flexibility of `symbol_info`. Callers can now pivot directly from a `symbol_info` handle to a document outline without an intermediate file-path roundtrip. |
| Backlog sync | Close rows: [`document-symbols-accepts-symbol-handle`]. |


### 10. editorconfig-write-no-auto-invalidation

| Field | Content |
|---|---|
| Status | pending |
| Backlog rows closed | `editorconfig-write-no-auto-invalidation` |
| Diagnosis | Root cause confirmed at `src/RoslynMcp.Roslyn/Services/EditorConfigService.cs:43-70`. `GetOptionsAsync` first collects known-key values from Roslyn's `AnalyzerConfigOptionsProvider` (lines 43-50) into an `options` list, then supplements with disk-parsed keys at lines 57-70 — but the supplement guard at line 64 (`!existingKeys.Contains(key)`) only adds keys **absent** from Roslyn's cache. When `set_editorconfig_option` writes a **new value for a known key** (one Roslyn already holds in its cached workspace snapshot), the supplement guard blocks the updated disk value and the stale cached value wins. A prior fix (`dr-get-editorconfig-options-incomplete-after-set`, shipped PR #126) addressed **new/unknown keys** only — keys for unloaded analyzer IDs that Roslyn's provider never enumerated. The residual gap: known keys (those in `GetKnownOptionKeys()` at line 173, e.g. `indent_size`, `csharp_style_var_for_built_in_types`) written via `set_editorconfig_option` return the old Roslyn-cached value on immediate re-read until `workspace_reload`. The fix is to extend the disk-supplement logic to also **override** Roslyn-cached values with disk-parsed values for keys that already exist in the options list — the disk is authoritative because `SetOptionAsync` (line 299) writes directly to it. |
| Approach | Edit `src/RoslynMcp.Roslyn/Services/EditorConfigService.cs` only. In `GetOptionsAsync`, after the existing disk-supplement block (lines 57-70), add a second pass: for each `(key, value)` from `ParseEditorconfigCsKeys(editorconfigPath)` where the key **is already present** in `options`, replace the existing entry's value with the disk-parsed value (mark source as `"disk"`). Concretely: after building the `existingKeys` set and adding missing keys, iterate the same `ParseEditorconfigCsKeys` result and for keys already in `existingKeys`, find the matching `EditorConfigEntryDto` in `options` and replace it with a new `EditorConfigEntryDto(key, diskValue, "disk")`. No changes to `EditorConfigEntryDto`, `EditorConfigOptionsDto`, `EditorConfigTools.cs`, or any other file. The two-pass approach is preferable to a single-pass merge because it keeps the Roslyn-cache read as a first-class fallback (for environments where the editorconfig file is absent) and then corrects stale values when the file is present. |
| Scope | Production files touched: 1 — `src/RoslynMcp.Roslyn/Services/EditorConfigService.cs`. Test files modified: 1 — `tests/RoslynMcp.Tests/EditorConfigServiceTests.cs` (add new method `SetThenGet_KnownKey_ReturnsNewValueWithoutReload`). Rule 3: 1 production file (≤4). Rule 4: extends 1 existing test file (≤3). |
| Tool policy | edit-only |
| Estimated context cost | 28000 |
| Risks | (1) The two-pass disk override runs on every `GetOptionsAsync` call when a `.editorconfig` file is present, adding a second `ParseEditorconfigCsKeys` iteration. For typical files (<500 lines) the cost is negligible. (2) If the disk file is deleted between a `set_editorconfig_option` call and the subsequent `GetOptionsAsync` call (e.g., during `revert_last_apply`), the override pass is skipped (the `File.Exists` guard at line 60 stays intact). This is correct behavior. (3) The source label on overridden entries changes from `"editorconfig"` (Roslyn-sourced) to `"disk"` — verify no test asserts a specific source label on known keys. (4) Fanout probe skipped — surgical edit to one private method inside a single service class. |
| Validation | (1) Build: `dotnet build RoslynMcp.slnx -c Release -p:TreatWarningsAsErrors=true` must pass. (2) New test `EditorConfigServiceTests.SetThenGet_KnownKey_ReturnsNewValueWithoutReload`: write `indent_size = 4` via `SetOptionAsync`, immediately call `GetOptionsAsync`, assert the returned entry for `indent_size` has value `"4"` — no `workspace_reload` call between write and read. (3) Existing test `SetThenGet_UnloadedAnalyzerId_IsReturned` must still pass unchanged. (4) Existing `SectionMatchesCSharp_RecognizesCommonGlobs` must still pass. (5) Run `mcp__roslyn__compile_check` after editing. (6) Run `mcp__roslyn__test_run --filter "EditorConfigServiceTests"`. |
| Performance review | N/A — correctness fix, no hot-path changes. `GetOptionsAsync` is called by agents after a write to verify the new value; frequency is low. The added disk parse is identical in cost to the already-present disk-supplement pass. |
| CHANGELOG category | Fixed |
| CHANGELOG entry (draft) | Fixed `get_editorconfig_options` returning stale cached values for known keys (e.g. `indent_size`, `csharp_style_var_for_built_in_types`) immediately after `set_editorconfig_option` writes a new value. The disk-parsed value is now authoritative when the `.editorconfig` file is present, overriding the Roslyn workspace snapshot for any key the snapshot had cached before the write. |
| Backlog sync | Close rows: [`editorconfig-write-no-auto-invalidation`]. |

<details>
<summary>Sonnet handoff notes</summary>

- **Pattern coordinates:** mirror the existing disk-supplement loop at `src/RoslynMcp.Roslyn/Services/EditorConfigService.cs:57-71` (inside `GetOptionsAsync`). The override pass goes immediately after line 70 (still inside the `if (editorconfigPath is not null && File.Exists(editorconfigPath))` block at line 60) — re-iterate `ParseEditorconfigCsKeys(editorconfigPath)` and for keys whose `existingKeys.Contains(key)` is true, replace the matching `EditorConfigEntryDto` in `options` with `new EditorConfigEntryDto(key, value, "disk")`. `EditorConfigEntryDto` is declared at `src/RoslynMcp.Core/Models/EditorConfigDto.cs:14` (record — construct fresh, don't mutate).
- **Test target:** add `SetThenGet_KnownKey_ReturnsNewValueWithoutReload` to the existing class `EditorConfigServiceTests` in `tests/RoslynMcp.Tests/EditorConfigServiceTests.cs` (class at line 14); mirror the sibling fixture `SetThenGet_UnloadedAnalyzerId_IsReturned` at lines 22-48. Use a known key from `GetKnownOptionKeys()` at line 173 — `indent_size = "4"` is the canonical pick. Do NOT create a new test file.
- **Edge cases to cover:** the new test must NOT call `workspace_reload` between `SetOptionAsync` and `GetOptionsAsync` — that is the bug surface. The existing `SetThenGet_UnloadedAnalyzerId_IsReturned` must still pass unchanged.
- **Negative space:** do NOT touch `SetOptionAsync` (line 299), `UpsertKeyInSection` (line 357), or `SectionMatchesCSharp` (line 128) — those are the write path and the section matcher; this is a read-path fix only. No new fields on `EditorConfigEntryDto`. No changes to `EditorConfigTools.cs`.

</details>

<!-- deepener-notes: judgmentHeavy=true — two fix approaches evaluated (auto-invalidate disk values win vs add response note); auto-invalidate selected as architecturally correct per project's correct-over-cheap memory. -->


### 11. set-project-property-preview-directory-build-props-blindness

| Field | Content |
|---|---|
| Status | pending |
| Backlog rows closed | `set-project-property-preview-directory-build-props-blindness` |
| Diagnosis | Root cause is in `src/RoslynMcp.Roslyn/Services/ProjectMutationService.cs:172–211` (`PreviewSetProjectPropertyAsync`). The method inspects only the raw `.csproj` XDocument (`propertyGroup.Element(request.PropertyName)` at line 188) for a same-value no-op check. It does NOT call `_msbuildEvaluation.EvaluatePropertyAsync(...)` to inspect the evaluated MSBuild property graph, so a value inherited from `Directory.Build.props` is invisible. The method then emits an XDocument mutation that adds a redundant `<PropertyName>value</PropertyName>` element to the `.csproj`. The `_msbuildEvaluation` field (type `IMsBuildEvaluationService`) is already injected and used by sibling methods — `PreviewAddTargetFrameworkAsync` calls it at lines 223–225, and `PreviewAddPackageReferenceAsync` calls `_msbuildEvaluation.EvaluateItemsAsync` at lines 60–70 for the CPM duplicate-detection guard. The gap is that `PreviewSetProjectPropertyAsync` lacks the analogous evaluated-property check. Fix is a warn-not-reject: when the evaluated effective value already matches the requested value, append a warning to the response rather than throwing, so callers can decide whether to proceed. |
| Approach | Modify `src/RoslynMcp.Roslyn/Services/ProjectMutationService.cs` method `PreviewSetProjectPropertyAsync` (line 172) to make it async and call `_msbuildEvaluation.EvaluatePropertyAsync(workspaceId, request.ProjectName, request.PropertyName, ct)` before the XDocument mutation. If `evaluatedResult.EvaluatedValue` is non-empty and equals `request.Value` (case-insensitive, consistent with the XDocument check at line 189), and the value is NOT declared in the `.csproj` itself (i.e., `existingElement` is null in the XDocument scan), then add a warning string such as `"Property 'Nullable' is already set to 'enable' via the inherited MSBuild property graph (e.g. Directory.Build.props). Applying this preview will create a redundant entry in the .csproj."` to the `warnings` list. Do not throw — proceed with the preview so the caller can inspect the diff and decide. Mirror the warning-list plumbing already present in `PreviewProjectMutationAsync` (the `IReadOnlyList<string>? warnings` overload at line 498). Update the `[Description]` attribute on `PreviewSetProjectProperty` in `src/RoslynMcp.Host.Stdio/Tools/ProjectMutationTools.cs` (line 109) to note: "When the property value is already inherited from Directory.Build.props, the response includes a warning annotation." |
| Scope | Production files touched: 2. (1) `src/RoslynMcp.Roslyn/Services/ProjectMutationService.cs` — add evaluated-property guard. (2) `src/RoslynMcp.Host.Stdio/Tools/ProjectMutationTools.cs` — update `[Description]`. Test files modified: 1 (extend `tests/RoslynMcp.Tests/ProjectMutationIntegrationTests.cs` with 1 new test method). |
| Tool policy | edit-only |
| Estimated context cost | 28000 |
| Risks | (1) The warn-not-reject design means callers who do not inspect `warnings` will still apply the redundant property. Per backlog spec ("warn or reject"); the less-breaking choice. (2) `EvaluatePropertyAsync` runs MSBuild evaluation which adds latency to the preview call; acceptable for a preview-path tool. (3) Fanout probe skipped — surgical addition of an async call within one method. (4) `PreviewSetConditionalPropertyAsync` (line 371) has the same blindness for conditional property groups but is out of scope here — flag as a follow-on row. |
| Validation | 1. New test method in `ProjectMutationIntegrationTests.cs`: call `PreviewSetProjectPropertyAsync("Nullable", "enable")` on a project whose workspace's `Directory.Build.props` sets `<Nullable>enable</Nullable>`; assert the `RefactoringPreviewDto.Warnings` collection contains a substring "already" and the diff still shows the property being added (warn-not-reject). 2. Existing test `Set_Project_Property_Preview_And_Apply_Updates_Isolated_Project_File` must still pass. 3. `mcp__roslyn__compile_check` after edit. 4. `mcp__roslyn__test_run --filter "ProjectMutationIntegrationTests"`. |
| Performance review | N/A — correctness fix, no hot-path changes. The added `EvaluatePropertyAsync` call is on the preview code path only. |
| CHANGELOG category | Fixed |
| CHANGELOG entry (draft) | Fixed `set_project_property_preview` silently generating redundant property entries when the target property is already inherited from `Directory.Build.props`. The tool now inspects the evaluated MSBuild property graph and includes a `warnings` annotation when the requested value is already globally effective. |
| Backlog sync | Close rows: [`set-project-property-preview-directory-build-props-blindness`]. Follow-on note: `PreviewSetConditionalPropertyAsync` has the same blindness; file separate row if needed. |


### 12. eternal-experimental-age-audit

| Field | Content |
|---|---|
| Status | pending |
| Backlog rows closed | `eternal-experimental-age-audit` |
| Diagnosis | No automated tooling exists to flag experimental-tier entries that have aged past 180 days without promotion or deprecation. The `ServerSurfaceCatalog.*` partial files each call factory methods `Tool(name, category, "experimental", ...)`, `Resource(...)`, or `Prompt(...)`. The `SurfaceEntry` record (declared at `src/RoslynMcp.Host.Stdio/Catalog/ServerSurfaceCatalog.cs:186`) exposes `SupportTier` as a string field. The catalog currently carries dozens of experimental entries (all 20 prompts in `ServerSurfaceCatalog.Prompts.cs` are experimental, plus several tools and resources across the analysis, refactoring, editing, and orchestration partials). There is no time-based audit today — the staleness contract in `full.md` line 878 covers scorecard JSON artifacts only, not the experimental surface tier itself. The `eng/` directory contains no `audit-experimental-age.ps1`. The `publish-preflight` Step 8 (at `.claude/skills/publish-preflight/SKILL.md` line 99) currently focuses on promotion-scorecard aggregation but has no age-based gate. |
| Approach | 1. Create `eng/audit-experimental-age.ps1`. The script: (a) globs all `src/RoslynMcp.Host.Stdio/Catalog/ServerSurfaceCatalog.*.cs` files; (b) uses a regex to extract lines matching the `Tool(..., "experimental", ...)` / `Resource(...)` / `Prompt(...)` factory call pattern, capturing name, kind, category, and file path; (c) for each experimental entry, runs `git blame -L <line>,<line> <file> --porcelain` (from repo root) to read the commit timestamp; (d) computes age in days from today; (e) collects entries where age > 180 days; (f) emits a markdown table with columns: Kind, Name, Category, File, Line, Age (days), Blame Commit, Blame Date. The script accepts an optional `-ThresholdDays` parameter (default 180) and a `-RepoRoot` parameter (defaults to `$PSScriptRoot/..`). It exits 0 always — advisory only, never auto-promotes or auto-deprecates. 2. (Optional hook) Add a Step 8.5 advisory note to `.claude/skills/publish-preflight/SKILL.md` after the existing Step 8 body, directing the maintainer to run `pwsh -NoProfile -File eng/audit-experimental-age.ps1` and review entries > 180 days old before each release. Mirror the existing Step 8 advisory-not-blocking framing. Style reference: mirror `eng/aggregate-promotion-scorecards.ps1` (comment-based help, `$ErrorActionPreference = "Stop"`, `param()` block, stdout output). |
| Scope | Production files touched: 2. (1) NEW `eng/audit-experimental-age.ps1` — primary deliverable; (2) `.claude/skills/publish-preflight/SKILL.md` — optional Step 8.5 hook. Test files added: 1. NEW `tests/RoslynMcp.Tests/ExperimentalAgeAuditScriptTests.cs` — 1 fixture creating a synthetic catalog partial with a tier-line git-blame age > 180 days; assert the script emits it in the output table with the correct age (stub blame output to avoid live git dependency). No Rule 3 exemption needed. |
| Tool policy | edit-only |
| Estimated context cost | 35000 |
| Risks | (1) `git blame` on the catalog lines requires the lines to be committed — synthetic test fixtures need a real git commit or a mock strategy; recommend stubbing the blame output. (2) The regex must handle multi-line factory calls and future factory-method name changes. Anchor on the string literal `"experimental"` in the third argument position rather than the method name. (3) Age computation depends on the system clock; use UTC dates consistently. (4) `git blame` may return a committer-date inflated by branch imports — acceptable for an advisory script but worth noting in comment-based help. (5) Fanout probe skipped — surgical new-file addition. |
| Validation | (1) Run `pwsh -NoProfile -File eng/audit-experimental-age.ps1` from repo root — emits table of experimental entries with age > 180 days. (2) Run with `-ThresholdDays 0` — should emit all experimental entries. (3) Run `dotnet test --filter ExperimentalAgeAuditScriptTests`. (4) `pwsh -NoProfile -File eng/verify-ai-docs.ps1` — no broken links from SKILL.md update. (5) Inspect `eng/audit-experimental-age.ps1 -?` for comment-based help. |
| Performance review | N/A — correctness fix, no hot-path changes. |
| CHANGELOG category | Added |
| CHANGELOG entry (draft) | Added `eng/audit-experimental-age.ps1` — advisory script that walks `ServerSurfaceCatalog.*` for experimental-tier entries, runs `git blame` on each catalog line, computes age, emits a table of entries older than 180 days. Does not auto-promote or auto-deprecate. Optional advisory hook added to `/publish-preflight` Step 8.5. |
| Backlog sync | Close rows: [`eternal-experimental-age-audit`]. |


### 13. audit-and-refactor-skills-roslyn-self-check

| Field | Content |
|---|---|
| Status | pending |
| Backlog rows closed | `audit-and-refactor-skills-roslyn-self-check` |
| Diagnosis | All four anchor files confirmed present. `skills/refactor/SKILL.md` (117 lines) ends with Step 6 "Report" + "Error Recovery" sections; no self-check block exists. `skills/refactor-loop/SKILL.md` (106 lines) ends with Stage 5 "Output" section. `skills/architecture-review/SKILL.md` (144 lines) ends with "Output Format" and "Refusal conditions" sections; no self-check. `.claude/skills/mcp-server-stress/SKILL.md` (50 lines) is a thin alias that delegates entirely to `/mcp-server-surface-test --output-mode=fragments`; it contains no per-session summary or exit phase at all. Root cause: skill prompts dispatch subagents without seeding Roslyn-tool affordances into the subagent context, and have no exit-phase instrumentation that surfaces zero-semantic-call sessions as actionable feedback. Confirmed in two concrete sessions: 8405ce90 (5 subagents, 0 Roslyn calls) and c2a4c5b7 (1 `workspace_load` + 0 semantic follow-ups). Conflict with initiative 2 confirmed: that initiative edits `skills/refactor/SKILL.md` lines ~17–18 (entry-time conditional-ask) and `skills/refactor-loop/SKILL.md` lines ~16–17 (same pattern). This initiative's self-check block goes at the EXIT of each file. Different line ranges, same files — parallel execution causes merge conflict. |
| Approach | Add a **session-end self-check block** to each of the four skill files. The block emits `summary: { semanticCalls: N, classificationApplied: <repo-stack> }` on exit; when `classificationApplied` is `csharp` and `N == 0`, renders an explicit warning. Specific changes per file: (a) `skills/refactor/SKILL.md` — append a new "Step 7: Session Self-Check" section after the existing "Error Recovery" section (end of file). (b) `skills/refactor-loop/SKILL.md` — append the same self-check after Stage 5 "Output" (end of file) as "Stage 6: Session Self-Check". (c) `skills/architecture-review/SKILL.md` — append as "Step 8: Session Self-Check" after the existing "Refusal conditions" section. Threshold: for an architecture-review session, `N == 0` is a stronger signal because Steps 2–6 each call specific Roslyn tools by name; warning should cite which Steps were expected to use Roslyn but didn't. (d) `.claude/skills/mcp-server-stress/SKILL.md` — add a one-paragraph "Exit annotation" section noting the delegated `/mcp-server-surface-test` skill is responsible for emitting the semantic-call summary; this alias-level file should instruct the operator to check the surface-test audit report's tool-invocation section. Do NOT add a redundant self-check block here. Note: initiative 13 must execute AFTER initiative 2 to avoid merge conflicts. |
| Scope | Production files touched: 4 — `skills/refactor/SKILL.md`, `skills/refactor-loop/SKILL.md`, `skills/architecture-review/SKILL.md`, `.claude/skills/mcp-server-stress/SKILL.md`. Test files added: 0 (skill-level instrumentation; no server unit-test infrastructure for `.md` behavioral assertions per backlog row). Rule 3: 4 files, at the cap; no exemption required. Rule 4: 0 new test files. |
| Tool policy | edit-only |
| Estimated context cost | 35000 |
| Risks | (1) **Schedule conflict with initiative 2 (order=2):** must schedule initiative 13 strictly AFTER initiative 2 completes. `scheduleHint: "must-not-parallel-with: routine-flows-wrap-csharp-work-with-roslyn-bookends"`. (2) **`mcp-server-stress/SKILL.md` is a thin alias** — judgment call: alias-level exit annotation only (not full self-check) to keep the alias light. Alternative: skip this file. Executor re-vets. (3) "Tagging" a session as C# relies on CWD glob; if initiative 4 has not yet landed, executor must invent the pattern independently. (4) Semantic call counting is heuristic (LLM-behavioral, not instrumented); the warning is best-effort. (5) **Weaker evidence per backlog row** — structural pattern observed across only 2 concrete sessions. |
| Validation | (1) Read each modified skill file and confirm the self-check section is present at the end with correct markdown structure. (2) Run `./eng/verify-ai-docs.ps1`. (3) Manual invocation: invoke `skills/refactor` against a synthetic C# repo stub, perform zero Roslyn semantic calls, confirm final output contains `summary: { semanticCalls: 0, classificationApplied: "csharp" }` and the warning. (4) Manual smoke: invoke against a non-C# directory stub, confirm `classificationApplied: "unknown"` with no warning. (5) Confirm `skills/refactor/SKILL.md` and `skills/refactor-loop/SKILL.md` do not conflict with initiative 2's prelude edits if both have landed. |
| Performance review | N/A — correctness fix, no hot-path changes. |
| CHANGELOG category | Maintenance |
| CHANGELOG entry (draft) | **Add session-end Roslyn self-check to audit and refactor skill prompts** (`audit-and-refactor-skills-roslyn-self-check`). The `refactor`, `refactor-loop`, and `architecture-review` skills now append a self-check block on exit: when the CWD is classified as a C# repo but zero Roslyn semantic tool calls were made during the session, the skill emits `summary: { semanticCalls: 0, classificationApplied: "csharp" }` and a warning recommending a Roslyn-first rerun. Companion to `routine-flows-wrap-csharp-work-with-roslyn-bookends`. |
| Backlog sync | Close rows: [`audit-and-refactor-skills-roslyn-self-check`]. Update related: [`routine-flows-wrap-csharp-work-with-roslyn-bookends` — must not parallel-execute]. |

<details>
<summary>Sonnet handoff notes</summary>

- **Insertion coordinates (append at EOF, after the cited terminal section):**
  - `skills/refactor/SKILL.md` — append "Step 7: Session Self-Check" AFTER "Error Recovery" (file ends line 116; current "Error Recovery" runs lines 110-116).
  - `skills/refactor-loop/SKILL.md` — append "Stage 6: Session Self-Check" AFTER "Output" (file ends line 105; "Output" runs lines 97-105).
  - `skills/architecture-review/SKILL.md` — append "Step 8: Session Self-Check" AFTER "Refusal conditions" (file ends line 143; "Refusal conditions" runs lines 138-143).
  - `.claude/skills/mcp-server-stress/SKILL.md` — append a single "Exit annotation" paragraph AFTER "Hard rules" (file ends line 49). Do NOT add a full self-check block; this alias delegates to `/mcp-server-surface-test`.
- **Architecture-review warning specificity:** the stanza requires citing which Steps were expected to use Roslyn but didn't. Concrete Roslyn tools by name in `skills/architecture-review/SKILL.md`: Step 2 (`project_graph`), Step 3 (`get_namespace_dependencies`), Step 4 (`symbol_relationships`, `find_references`), Step 5 (`get_di_registrations`, `type_hierarchy`), Step 6 (`find_reflection_usages`). Enumerate these in the zero-call warning.
- **Negative space — initiative 2 seam:** initiative 2 edits `skills/refactor/SKILL.md:20` and `skills/refactor-loop/SKILL.md:16` (the conditional-ask "If a workspace is not already loaded..."). Your edits land at EOF — do NOT touch the entry-time prelude region, do NOT modify Step/Stage bodies, do NOT alter the "Connectivity precheck" block in `architecture-review/SKILL.md` (lines 23-31). This initiative is strictly EOF-append-only on all four files.
- **Self-check schema contract:** emit literal `summary: { semanticCalls: N, classificationApplied: <repo-stack> }`. Trigger warning only when `classificationApplied == "csharp"` AND `N == 0`. Non-C# CWDs (`classificationApplied: "unknown"`) emit summary but NO warning.

</details>

<!-- deepener-notes: judgmentHeavy=true. CONFLICT-GRAPH EDGE with initiative 2 confirmed (shared skills/refactor/SKILL.md + skills/refactor-loop/SKILL.md). schedule order: 13 strictly after 2. -->


### 14. bump-skill-double-version-string-detect

| Field | Content |
|---|---|
| Status | pending |
| Backlog rows closed | `bump-skill-double-version-string-detect` |
| Diagnosis | Two-part gap. (1) `.claude-plugin/server.json` was not tracked as a bump target — drifted to v1.33.2 while canonical was v1.35.1. PR #597 (commit 3260036, 2026-05-09) fixed this by adding `server.json` as version file #5 and including an explicit `replace_all: true` instruction in Step 3 of `.claude/skills/bump/SKILL.md:71`. (2) Skill still has no proactive preflight step that pre-greps all target files for occurrence count before issuing any Edit. The session 564d708d (2026-05-12) failure on v1.36.0 release cut would have been prevented by (1) alone — confirming PR #597 resolved the acute regression — but the general preflight called for by the backlog row was not added. Without it, any future file that accumulates a second occurrence of the version string will produce the same Edit error. Confirmed: `.claude-plugin/server.json` at lines 9 and 16 contains two `"version": "X.Y.Z"` fields (top-level and `packages[0].version`) — the exact structure that triggered the failure. |
| Approach | Edit `.claude/skills/bump/SKILL.md` only. Insert a new "Step 2.5 — Preflight: occurrence count" section between Step 2 (Compute New Version) and Step 3 (Edit Version Files). The preflight: for each of the five target files, grep for the OLD version string and count occurrences. Emit a preflight table `file | occurrences | edit-strategy` before any Edit call. When occurrences > 1: use `replace_all: true` for that file and annotate row with "replace_all". When occurrences == 0: abort with a structured error — *"Preflight failed: OLD version not found in FILE — file may have already been bumped or is tracking a different version. Verify before proceeding."* When occurrences == 1: standard single-replacement Edit. The server.json row in Step 3 already documents `replace_all: true`; the preflight supplements it by making the occurrence check explicit and general. The zero-occurrence guard catches mid-release partial application. No changes to `docs/release-policy.md`, `eng/verify-version-drift.ps1`, or any server code. |
| Scope | Production files touched: 1 — `.claude/skills/bump/SKILL.md`. Test files added: 0. The backlog row describes a regression test fixture but no test infrastructure in this repo exercises skill prompt content at unit-test level; the validation is manual invocation of `/bump` and verification via `eng/verify-version-drift.ps1`. Rule 3/4 satisfied. |
| Tool policy | edit-only |
| Estimated context cost | 18000 |
| Risks | (1) Zero-occurrence guard could cause spurious abort if `Directory.Build.props` is parsed differently from how the grep matches (e.g. `<Version>1.36.0</Version>` vs `"1.36.0"`). Use file-format-aware grep patterns. (2) The preflight adds a Bash grep call per file before each Edit; negligible cost. (3) The skill now has two layers of `replace_all` guidance (preflight table and per-file Step 3 instruction for server.json); keep both. (4) Fanout probe skipped — surgical skill-prompt edit. |
| Validation | (1) Manually invoke `/bump patch` against a worktree where server.json has been temporarily given a second version field; assert both occurrences are updated and `eng/verify-version-drift.ps1` exits 0. (2) Inspect the preflight output for each file to confirm occurrence counts. (3) Introduce a deliberate zero-occurrence file (rename a version key) and confirm the preflight aborts with the structured error. (4) Run `eng/verify-version-drift.ps1` after a real `/bump patch` to confirm all six locations agree. |
| Performance review | N/A — correctness fix, no hot-path changes. |
| CHANGELOG category | Maintenance |
| CHANGELOG entry (draft) | **Maintenance:** `/bump` skill now runs a per-file occurrence preflight before editing version files; files with >1 occurrence of the old version string use `replace_all: true` automatically, and files where the version string is absent abort with an explicit error rather than silently failing. Prevents the "Found 2 matches of the string to replace" Edit error against `.claude-plugin/server.json`. |
| Backlog sync | Close rows: [`bump-skill-double-version-string-detect`]. |

<!-- deepener-notes: PR #597 (commit 3260036, 2026-05-09) already fixed the acute server.json regression. This initiative ships the general preflight that was never added. NOT obsolete. -->


### 15. roslyn-skill-prompts-exceed-read-token-cap

| Field | Content |
|---|---|
| Status | pending |
| Backlog rows closed | `roslyn-skill-prompts-exceed-read-token-cap` |
| Diagnosis | The backlog row cites two anchors: `.claude/skills/mcp-server-stress/prompts/maintainer-overlay.md` (**stale** — this file was deleted in a prior refactor; the mcp-server-stress skill is now a thin alias documented in `.claude/skills/mcp-server-stress/SKILL.md` lines 17-28) and `skills/mcp-server-surface-test/prompts/full.md` (confirmed live at 96,352 bytes). The second anchor is the live problem: `full.md` at 96 KB exceeds the Read tool's 25K-token cap (byte-to-token ratio places it at approximately 24,000–28,000 tokens; the original error was triggered at 31,976 tokens on the smaller deleted file, so `full.md` at 96 KB almost certainly triggers the same cap). The `quick.md` sibling at 15,195 bytes (~3,800 tokens) is well under the cap and needs no split. The root cause is that `full.md` has grown to 1,076 lines across 20+ phases (Phase -1 through Phase 19 plus Final surface closure, Output Format, and Appendix sections) as new audit phases and apply-tool coverage were added iteratively. A consuming agent that needs the whole file for whole-prompt comprehension cannot Read it in one call. |
| Approach | Split `skills/mcp-server-surface-test/prompts/full.md` into one orchestrator entry file (< 5K tokens, containing the preamble, cross-cutting principles, execution strategy, and phase-order directive) plus three phase-group sub-files, keeping total new/modified file count at 4 to satisfy Rule 3: (1) rename or replace the current `full.md` with a slim orchestrator that includes phase-group sub-file references the consuming agent can follow on demand; (2) `skills/mcp-server-surface-test/prompts/phases/setup-and-analysis.md` — phases -1, 0, 0.5, 1, 2, 3, 4, 5; (3) `skills/mcp-server-surface-test/prompts/phases/apply-and-test.md` — phases 6 (all 6a–6z sub-phases), 7, 8, 8b, 9, 10; (4) `skills/mcp-server-surface-test/prompts/phases/output-and-close.md` — phases 11–19, Final surface closure, Output Format section, Promotion scorecard schema, Appendix. The orchestrator's phase-order directive and cross-cutting audit principles (currently lines 33–68 of `full.md`) are preserved verbatim in the orchestrator; sub-files contain only their phase bodies. SKILL.md at `skills/mcp-server-surface-test/SKILL.md` currently references `prompts/full.md` directly (lines 63-79 of `McpServerSurfaceTestSkillTests.cs` enforce this); **retain the `full.md` filename for the orchestrator** to avoid touching SKILL.md or the test path. Extend `tests/RoslynMcp.Tests/Skills/McpServerSurfaceTestSkillTests.cs` with one new test method `Skill_PromptFiles_BelowReadTokenCap` that asserts every file under `skills/*/prompts/` has byte length ≤ 100,000 bytes (~25K-token proxy with margin). |
| Scope | Production files modified: 4 — `skills/mcp-server-surface-test/prompts/full.md` (replaced with slim orchestrator) + 3 new sub-files under `skills/mcp-server-surface-test/prompts/phases/` (`setup-and-analysis.md`, `apply-and-test.md`, `output-and-close.md`). Exactly meets Rule 3's 4-file ceiling. Test files: 1 existing test file extended (`tests/RoslynMcp.Tests/Skills/McpServerSurfaceTestSkillTests.cs`). |
| Tool policy | edit-only |
| Estimated context cost | 30000 |
| Risks | (1) The consuming agent for `/mcp-server-surface-test` and `/mcp-server-stress` must be instructed to follow sub-file references — orchestrator must include explicit agent-readable instructions (e.g., "Read `prompts/phases/setup-and-analysis.md` now") rather than passive Markdown link references. (2) `McpServerSurfaceTestSkillTests.Skill_PromptFiles_ExistAtDocumentedPaths` asserts `prompts/full.md` exists; orchestrator retains the `full.md` filename to avoid breaking this test. (3) The `surface-test-skill-self-correction-observability` backlog row (initiative 16) references `prompts/full.md` as an anchor for adding a Phase-N self-check — if that row executes after this split, executor places the self-check in `output-and-close.md` rather than the monolith. Conflict-graph edge with #16. (4) **Stale anchor**: the first anchor cited in the backlog row (`.claude/skills/mcp-server-stress/prompts/maintainer-overlay.md`) no longer exists and is not part of scope. (5) **judgmentHeavy** — the 3-sub-file phase grouping is a planner judgment call; 4 sub-files is equally defensible but would push to 5 production files (Rule 3 violation). |
| Validation | (1) `Read skills/mcp-server-surface-test/prompts/full.md` (orchestrator) completes without a token-cap error — should be under 5K tokens. (2) `Read skills/mcp-server-surface-test/prompts/phases/apply-and-test.md` completes without error (highest-risk sub-file). (3) `dotnet test --filter McpServerSurfaceTestSkillTests` — all existing tests pass; new `Skill_PromptFiles_BelowReadTokenCap` passes. (4) `./eng/verify-skills-are-generic.ps1` — no banned markers in any new or modified file. (5) `./eng/verify-ai-docs.ps1`. (6) Manually confirm `apply-and-test.md` is < 100,000 bytes after split. |
| Performance review | N/A — correctness fix, no hot-path changes. |
| CHANGELOG category | Maintenance |
| CHANGELOG entry (draft) | Split `skills/mcp-server-surface-test/prompts/full.md` into a slim orchestrator and three phase-group sub-files to bring each file under the Read tool's 25K-token cap; adds a lockstep test asserting all skill prompt files remain within the byte-size limit. |
| Backlog sync | Close rows: [`roslyn-skill-prompts-exceed-read-token-cap`]. Update related: [`surface-test-skill-self-correction-observability`] — executor for that row targets the new `prompts/phases/output-and-close.md` sub-file rather than the monolith. |

<details>
<summary>Sonnet handoff notes</summary>

- **Phase-group line boundaries** (verified in `skills/mcp-server-surface-test/prompts/full.md`, 1076 lines): orchestrator carries lines 1-68 verbatim (preamble + phase-order directive at line 33 + cross-cutting principles ending at line 68). `setup-and-analysis.md` carries phases -1 (line 71) through 5 (ending line 258). `apply-and-test.md` carries phases 6 (line 259) through 9 (ending line 507) — **note the audit-prescribed run order is 6 → 7 → 8 → 8b → 10 → 9** (Phase 10 at line 474 precedes Phase 9 at line 493 in source order; preserve this when carving). `output-and-close.md` carries phases 11 (line 508) through 19, plus Final surface closure (line 753), Output Format, Promotion scorecard JSON, Markdown template, Concurrency probe set, Historical notes (ending line 1076).
- **Test target:** add `Skill_PromptFiles_BelowReadTokenCap` to the existing class `McpServerSurfaceTestSkillTests` in `tests/RoslynMcp.Tests/Skills/McpServerSurfaceTestSkillTests.cs` (sole class in file, ends ~line 227). Mirror the directory-walk style of `Skill_PromptFiles_ExistAtDocumentedPaths` at lines 94-105. Do NOT create a new test file. Glob both `skills/*/prompts/**/*.md` AND `.claude/skills/*/prompts/**/*.md` for completeness.
- **Hotspot seam — DO NOT change the orchestrator's filename or path.** The existing test `Skill_PromptFiles_ExistAtDocumentedPaths` at lines 94-105 asserts `prompts/full.md` and `prompts/quick.md` exist; SKILL.md lines 78-79 and 104-105 reference `prompts/full.md`. The orchestrator MUST retain the `full.md` filename and stay at the same path.
- **Orchestrator instruction style:** sub-file references must be agent-readable imperatives (e.g., `"Read \`prompts/phases/setup-and-analysis.md\` now before proceeding to Phase -1"`), NOT passive Markdown links. Risk (1) of the stanza is load-bearing on this.
- **Negative space:** do NOT touch `prompts/quick.md` (15,195 bytes, well under cap). Do NOT modify `SKILL.md` (Steps 4/5 reference `prompts/full.md` at lines 104-105 and the test at lines 94-105 locks that contract). Do NOT modify the deleted `.claude/skills/mcp-server-stress/prompts/maintainer-overlay.md` anchor — it does not exist (stale anchor).

</details>

<!-- deepener-notes: judgmentHeavy=true, anchorStale=true. maintainer-overlay.md anchor is dead (file deleted in prior refactor). 3-sub-file grouping is judgment call. CONFLICT-GRAPH EDGE with #16 (shared full.md). -->


### 16. surface-test-skill-self-correction-observability

| Field | Content |
|---|---|
| Status | pending |
| Backlog rows closed | `surface-test-skill-self-correction-observability` |
| Diagnosis | Root cause is in `skills/mcp-server-surface-test/prompts/full.md` at two locations. (1) The coverage ledger status enum (portability contract item 4, line 40) lists six valid terminal statuses — `exercised`, `exercised-apply`, `exercised-preview-only`, `skipped-repo-shape`, `skipped-safety`, `blocked` — but has no value for a tool that was assigned to a phase and planned for exercise but never actually called due to context exhaustion or session interruption. Session 4868dff2 demonstrated this: `format_range_preview` (Phase 6e, line 294) was planned, context exhaustion hit before phase executed, agent reported `exercised` because no honest status existed. (2) The "Final surface closure" section (~line 753) instructs agent to finalize statuses but has no cross-validation step against the draft's actual tool-call evidence. The second cited anchor, `.claude/skills/mcp-server-stress/prompts/maintainer-overlay.md`, **does not exist** (anchor stale; same finding as #15). The `skills/mcp-server-surface-test/prompts/phases/output-and-close.md` sub-file also does not exist yet — #15 has not landed; fix targets `full.md` today and executor redirects to `phases/output-and-close.md` if #15 has merged before execution. |
| Approach | Edit `skills/mcp-server-surface-test/prompts/full.md` at two locations. (1) Portability contract item 4 (line ~40): extend the status enum to add `scoped-but-skipped` as a seventh valid terminal status, defined as "the tool was assigned to a phase in the coverage ledger but no tool-call result for it appears in the draft." Add a note that the agent MUST NOT assign `exercised` (or any `exercised-*` variant) unless at least one tool-call result for that name is recorded in the draft. (2) Final surface closure (~line 753), after existing step 2, add a new numbered step — "Self-check: for each entry currently marked `exercised`, `exercised-apply`, or `exercised-preview-only` in the ledger, confirm the draft contains a tool-call result (or inline evidence line) for that tool name. Any entry without call evidence MUST be downgraded to `scoped-but-skipped` with a note citing the missed phase and reason. Entries marked `scoped-but-skipped` score `needs-more-evidence` in the experimental promotion scorecard — identical to `blocked`. This step runs before scorecard computation." If initiative 15 has landed before execution, the same edits target `phases/output-and-close.md` for the Final surface closure change, and the slim orchestrator stub in `full.md` for the coverage-ledger status-enum change. Executor verifies layout at re-vet. |
| Scope | Production files touched: 1 (pre-#15 state) — `skills/mcp-server-surface-test/prompts/full.md`. If #15 has landed: 2 files — `full.md` (enum only) and `phases/output-and-close.md` (self-check step). Test files added: 0 — no infrastructure for prompt behavioral assertions (consistent with #13/#14 precedent). Rule 3/4 satisfied. |
| Tool policy | edit-only |
| Estimated context cost | 22000 |
| Risks | (1) **Conflict-graph dependency with #15**: must execute AFTER #15; serial ordering enforced. Executor verifies layout at re-vet. (2) The `mcp__roslyn__server_heartbeat` invocation-count mechanism mentioned in the backlog row is NOT implemented in this fix — the self-check relies on draft-text evidence scanning. Architecturally correct (the draft IS the tool-call record); future initiative could add a heartbeat-based counter if the behavioral approach proves insufficient. (3) The stale `maintainer-overlay.md` anchor is omitted from scope. (4) If executor applies this change to `full.md` and #15 subsequently lands, the sub-file split must carry the `scoped-but-skipped` definition into `output-and-close.md`. |
| Validation | (1) Read `full.md` (or `phases/output-and-close.md` if #15 landed): confirm portability contract item 4 lists `scoped-but-skipped` with definition. (2) Confirm Final surface closure contains the self-check step. (3) Confirm scoring note: `scoped-but-skipped` → `needs-more-evidence`. (4) Manual simulation: imagine Phase 0 seeding `format_range_preview` as `exercised`, context exhaustion hits before Phase 6e — confirm the new self-check would cause a compliant agent to downgrade. (5) `eng/verify-ai-docs.ps1`. |
| Performance review | N/A — correctness fix, no hot-path changes. |
| CHANGELOG category | Fixed |
| CHANGELOG entry (draft) | Fixed false `exercised` status in mcp-server-surface-test coverage ledger: added `scoped-but-skipped` as a valid terminal status for tools assigned to a phase but never actually invoked, and added a self-check step in Final surface closure requiring agents to downgrade any unsubstantiated `exercised` claim before computing the experimental promotion scorecard. |
| Backlog sync | Close rows: [`surface-test-skill-self-correction-observability`]. |

<details>
<summary>Sonnet handoff notes</summary>

- **Pre-flight layout check (anchorStale):** before editing, `ls skills/mcp-server-surface-test/prompts/` to determine post-#15 state. If `phases/output-and-close.md` exists, the enum edit goes in the slim `full.md` orchestrator and the self-check step goes in `phases/output-and-close.md`. If it does NOT exist (#15 unmerged), both edits land in `full.md` at the current coordinates below.
- **Pattern coordinates (current `full.md`, pre-#15):** coverage-ledger enum at `full.md:40` (Portability contract item 4 — single-line list of six statuses); Final surface closure header at `full.md:753`, with existing steps 1-2 at `753-756`, then a numbered `3.` followed by `3a.` HARD GATE at `758`. Insert the new self-check step as **step 3** (renumbering the existing `3.` to `4.` and so on); the `3a.` sub-step stays attached to its parent. Do NOT insert before step 1 (Compare ledger) or after step 2 (Call/assign unaccounted) — the self-check must precede the run-end git clean-check and scorecard computation.
- **Negative space:** do NOT add a `server_heartbeat` invocation-counter mechanism (the backlog row mentions it; risk #2 explicitly rejects it — self-check is draft-evidence-scanning only). Do NOT touch `quick.md` (sibling prompt, out of scope). Do NOT touch `skills/mcp-server-stress/` (the `maintainer-overlay.md` anchor is dead).
- **Validation simulation:** the manual simulation in Validation step 4 (Phase 0 seeds `format_range_preview` as `exercised`, context exhaustion hits before Phase 6e — `full.md:294`) is the canonical regression case from session 4868dff2; ensure the self-check rubric you write would in fact catch it.

</details>

<!-- deepener-notes: anchorStale=true (maintainer-overlay.md doesn't exist; phases/ doesn't exist yet). judgmentHeavy=true. CONFLICT-GRAPH edge with #15. Must execute AFTER #15. -->


### 17. skill-namespace-and-semantic-search-discoverability

| Field | Content |
|---|---|
| Status | pending |
| Backlog rows closed | `skill-namespace-and-semantic-search-discoverability` (partial — establishes the `installed_as:` contract via lockstep test; bulk frontmatter edits across the 46 SKILL.md files deferred to a new follow-on backlog row, spun off at executor Step 7) |
| Diagnosis | Two distinct discoverability failures confirmed in the source backlog row evidence. (a) Natural-language skill lookup: Claude routed to the wrong skill when asked about a "stress test" skill (session a26fdd10); root cause is that no canonical `installed_as:` frontmatter field exists in any SKILL.md — neither in `skills/` (32 files) nor `.claude/skills/` (14 files). Current frontmatter: only `name`, `description`, `user-invocable`, `argument-hint`. (b) Plugin-prefix confusion (session 36084a03 L767): `release-cut` is installed bare (not `roslyn-mcp:release-cut`); no frontmatter row documents this. **Remediation split rationale (cycle 1):** original plan touched 47 files (1 new script + 46 SKILL.md edits), violating Rule 3's 4-file cap with no applicable exemption (no docs-only exemption defined in addenda). Per Rule 3 + Rule 5b, this initiative is **split**: this stanza ships the **infrastructure + contract** (the `eng/list-skills.ps1` enumerator + a lockstep test that asserts every SKILL.md carries `installed_as:`). The lockstep test will fail on first run, encoding the contract; the bulk frontmatter edits required to make it pass become a new backlog row (to be spun off by the executor) the maintainer can plan honestly at a later sweep. |
| Approach | NEW `eng/list-skills.ps1` — walk `.claude/skills/*/SKILL.md` and `skills/*/SKILL.md` (handling the `mcp-server-surface-test` subdirectory shape), parse YAML frontmatter, emit a table with columns `name | installed_as | path` where missing `installed_as:` renders as `[missing]`. Mirror the directory-walk pattern from `eng/verify-skills-are-generic.ps1`. NEW `tests/RoslynMcp.Tests/Skills/SkillFrontmatterInstalledAsTests.cs` — lockstep test that walks both directories and asserts every SKILL.md frontmatter contains an `installed_as:` key matching either `^[a-z][a-z0-9-]+$` (bare) or `^roslyn-mcp:[a-z][a-z0-9-]+$` (namespaced). Frontmatter parser pattern: mirror `tests/RoslynMcp.Tests/Skills/McpServerStressSkillFrontmatterTests.cs`. Mark test with `[Skip]` attribute OR ship as a known-failing test (project preference TBD by executor — but the test MUST exist and encode the contract). **Do NOT edit any SKILL.md files in this initiative.** The lockstep test failing is the desired post-state — it documents the contract and lets a follow-on row close the gap row-by-row. |
| Scope | Production files: 1 NEW (`eng/list-skills.ps1`). Test files: 1 NEW (`tests/RoslynMcp.Tests/Skills/SkillFrontmatterInstalledAsTests.cs`). `productionFilesTouched: 2` (counting the eng script as production per project convention; tests not counted toward production budget). Within Rule 3 cap. **No SKILL.md files touched this initiative** — the 46-file bulk frontmatter edit is the subject of a new follow-on backlog row, spun off at executor Step 7 sync. |
| Tool policy | edit-only |
| Estimated context cost | 30000 |
| Risks | (1) Test ship-state policy: if shipping a known-failing test breaks the green-CI baseline, executor may use `[Fact(Skip = "Pending bulk frontmatter migration — see backlog row <new-id>")]` instead. The contract is documented either way. (2) `eng/verify-skills-are-generic.ps1` bans `ai_docs/` and `eng/` strings in shipped `skills/` files — `installed_as:` values are slug-format and safe; the test file references both directories from the test project (not from skill bodies) and is unaffected. (3) `mcp-server-surface-test` is the only `skills/` entry with a subdirectory shape; the script must handle both `skills/<name>/SKILL.md` and `skills/<name>/<subdir>/SKILL.md` paths. (4) **Mandatory follow-on:** executor MUST spin off a new backlog row in Step 7 sync titled approximately `skill-namespace-installed-as-bulk-frontmatter-migration` covering the 46-file frontmatter edit. Without that row, the deferred bulk work is lost. |
| Validation | (1) Run `eng/list-skills.ps1` from repo root: verify one row per skill (46 total), `installed_as:` column reads `[missing]` for all 46 pre-edit. (2) `mcp__roslyn__test_run --filter "SkillFrontmatterInstalledAsTests"` — confirms the test loads and either fails (if shipped active) or is skipped (if shipped `[Skip]`-marked). (3) `mcp__roslyn__compile_check` after test file add. (4) Run `eng/verify-skills-are-generic.ps1`. (5) Build: `dotnet build RoslynMcp.slnx -c Release -p:TreatWarningsAsErrors=true`. |
| Performance review | N/A — infrastructure addition, no hot-path changes. |
| CHANGELOG category | Added |
| CHANGELOG entry (draft) | Added `eng/list-skills.ps1` skill enumerator and `SkillFrontmatterInstalledAsTests` lockstep test establishing the `installed_as:` frontmatter contract for SKILL.md files (bare vs `roslyn-mcp:` namespaced). Bulk frontmatter migration to follow in a separate row. |
| Backlog sync | Close rows: [`skill-namespace-and-semantic-search-discoverability`] (partial close — infrastructure portion). **Spin off new row** `skill-namespace-installed-as-bulk-frontmatter-migration` covering the 46-file SKILL.md frontmatter edit. |

<details>
<summary>Sonnet handoff notes</summary>

- **Pattern coordinates:**
  - PowerShell directory-walk pattern: `eng/verify-skills-are-generic.ps1:24-48` (Get-ChildItem `-Recurse -File -Filter 'SKILL.md'` + per-file line walk). Note that script only scans `skills/`; your new `list-skills.ps1` must walk BOTH roots: `skills/` (32 files) AND `.claude/skills/` (14 files) for 46 total.
  - Frontmatter parser: `tests/RoslynMcp.Tests/Skills/McpServerStressSkillFrontmatterTests.cs:177-202` (`ExtractFrontmatter` — `^---\s*\r?\n(?<body>.*?)\r?\n---\s*\r?\n` regex, splits on `\n`, strips surrounding double quotes). Lift verbatim.
  - Repo-root resolution in tests: `TestFixtureFileSystem.FindRepositoryRoot()` at `tests/RoslynMcp.Tests/TestInfrastructure/TestFixtureFileSystem.cs` — canonical helper used by every existing Skill test.
- **Test target:** add NEW file `tests/RoslynMcp.Tests/Skills/SkillFrontmatterInstalledAsTests.cs` (NEW class, do not extend `McpServerStressSkillFrontmatterTests`). Mirror class structure of `McpServerStressSkillFrontmatterTests` (lines 34-46 for class declaration + first test). Namespace: `RoslynMcp.Tests.Skills`. Test framework: MSTest (`[TestClass]` / `[TestMethod]`).
- **Edge cases to cover:**
  1. Walk both `skills/<name>/SKILL.md` AND `.claude/skills/<name>/SKILL.md` — pass both roots through the helper.
  2. Validate `installed_as:` regex: `^[a-z][a-z0-9-]+$` (bare) OR `^roslyn-mcp:[a-z][a-z0-9-]+$` (namespaced).
  3. Assert discovered file count > 0 BEFORE asserting per-file presence — a zero-file walk must fail loud, not vacuously pass.
  4. Stanza Risk #3 claims `mcp-server-surface-test` has subdir-shape SKILL.md — VERIFIED FALSE on disk. All 46 SKILL.md files live at depth-2 (`<root>/<skill>/SKILL.md`); `Get-ChildItem -Recurse` handles uniformly. No special-case branch needed.
- **Ship-state decision:** prefer `[Ignore("Pending bulk frontmatter migration — see backlog row <new-id>")]` over a known-failing test. The repo's CI gate (`eng/verify-release.ps1`) treats red tests as release-blockers; a shipped-failing test would block every PR until the follow-on row lands. `[Ignore]` keeps the contract encoded without breaking green.
- **Negative space:** do NOT edit any of the 46 SKILL.md files; do NOT extend `eng/verify-skills-are-generic.ps1` (that's a banned-string scanner; the new enumerator is a distinct utility); do NOT touch `McpServerStressSkillFrontmatterTests.cs` (it covers a different contract).
- **Mandatory Step 7 follow-on:** spin off backlog row `skill-namespace-installed-as-bulk-frontmatter-migration` covering the 46-file edit. Without this, the deferred work is lost — call out in Step 7 sync output explicitly with the exact row id.

</details>

<!-- cycle-1-remediation: scope-narrowed from 47-file fanoutOversize to 2-file infrastructure shape per Rule 3. Bulk migration deferred to follow-on row. Conflict-graph edges 2-17, 3-17, 4-17, 12-17, 13-17, 14-17 dropped — remediated #17 no longer touches any SKILL.md. -->


### 18. skill-prompts-deprecated-workspace-load-param-name-cleanup

| Field | Content |
|---|---|
| Status | **obsolete** (grep confirms 0 occurrences of `solutionOrProjectPath` in `.claude/skills/**/*.md`, `skills/**/*.md`, or `ai_docs/prompts/*.md` — target locations already clean) |
| Backlog rows closed | `skill-prompts-deprecated-workspace-load-param-name-cleanup` |
| Diagnosis | Grep across all skill-prompt and ai_docs/prompt locations returns 0 hits for `solutionOrProjectPath`. The three file matches found are: (1) `ai_docs/backlog.md` line 83 — the backlog row text itself, quoting the deprecated name as the thing to fix; (2) `review-inbox/archive/20260512T134225Z/20260512T130723Z_roslyn-backed-mcp_roslyn-mcp-multisession-retro.md` — historical evidence; (3) `src/RoslynMcp.Roslyn/Services/NuGetDependencyService.cs:268` — a private C# method parameter name (`ComputeLockfileHash(string solutionOrProjectPath, ...)`), a local identifier unrelated to MCP tool invocations in prompts. The backlog row itself mandates: *"If 0 occurrences exist (already clean), this initiative is obsolete — flag."* That condition holds. |
| Approach | None required — target files already clean. The sole remaining `solutionOrProjectPath` reference in production source is a private method parameter in `NuGetDependencyService.cs:268`, a C# local identifier with no connection to MCP tool parameter names in prompts. |
| Scope | 0 production files, 0 test files. No changes required. |
| Tool policy | edit-only |
| Estimated context cost | 5000 |
| Risks | No risks — no changes made. Retro and backlog.md retain the deprecated name as historical evidence; those are not actionable targets. |
| Validation | Re-run grep for `solutionOrProjectPath` scoped to `skills/**/*.md`, `.claude/skills/**/*.md`, and `ai_docs/prompts/**/*.md` — expect 0 hits. |
| Performance review | N/A. |
| CHANGELOG category | Maintenance |
| CHANGELOG entry (draft) | Confirmed `workspace_load` parameter canonicalization already complete in all skill prompts and ai_docs prompt files; backlog row `skill-prompts-deprecated-workspace-load-param-name-cleanup` closed as obsolete. |
| Backlog sync | Close rows: [`skill-prompts-deprecated-workspace-load-param-name-cleanup`]. Mark obsolete. |


### 19. filepaths-array-vs-stringified-tool-description-clarification

| Field | Content |
|---|---|
| Status | pending |
| Backlog rows closed | `filepaths-array-vs-stringified-tool-description-clarification` |
| Diagnosis | Grep for `string[]\s*\?` in `src/RoslynMcp.Host.Stdio/Tools/` reveals **9 distinct tool-surface parameters** across 9 files with array-typed inputs that lack an explicit "Pass as a native JSON array" guard phrase in their `[Description]` text. Two existing parameters — `usings` in `src/RoslynMcp.Host.Stdio/Tools/SnippetAnalysisTools.cs:19` and `imports` in `src/RoslynMcp.Host.Stdio/Tools/ScriptingTools.cs:21` — already show inline examples with escaped-JSON notation (`[\"System.IO\", \"System.Net.Http\"]`) which invites mis-reading. The remaining 7 have no example at all. Full enumeration of the 9-file problem space: (1) `src/RoslynMcp.Host.Stdio/Tools/SnippetAnalysisTools.cs:19` — `string[]? usings`; (2) `src/RoslynMcp.Host.Stdio/Tools/ScriptingTools.cs:21` — `string[]? imports`; (3) `src/RoslynMcp.Host.Stdio/Tools/WorkspaceWarmTools.cs:34` — `string[]? projects`; (4) `src/RoslynMcp.Host.Stdio/Tools/ValidationBundleTools.cs:29` — `string[]? changedFilePaths`; (5) `src/RoslynMcp.Host.Stdio/Tools/AdvancedAnalysisTools.cs:150` — `IReadOnlyList<string>? filePaths`; (6) `src/RoslynMcp.Host.Stdio/Tools/MSBuildTools.cs:60` — `string[]? includedNames`; (7) `src/RoslynMcp.Host.Stdio/Tools/InterfaceExtractionTools.cs:30` — `string[]? memberNames`; (8) `src/RoslynMcp.Host.Stdio/Tools/ScaffoldingTools.cs:35` — `string[]? interfaces`; (9) `src/RoslynMcp.Host.Stdio/Tools/ParameterObjectTools.cs:37` — `string[]? dtoFolders`. **The 9-file fix space exceeds Rule 3's 4-file cap**, so this initiative is scoped to the 4 highest-frequency parameters (items 1–4). Items 5–9 become a follow-on backlog row `filepaths-array-vs-stringified-tool-description-clarification-batch-2`, to be spun off at executor Step 7 backlog sync — NOT in this plan. **Per-initiative blast radius is therefore 4 files (`fanoutEstimate: 4`); the 9-file value previously recorded reflected the full problem space, which is now captured in this Diagnosis rather than the per-initiative fanout** (cycle 1 remediation per Rule 5b). |
| Approach | Edit **4 production files** — the `[Description]` attribute text on the array-typed parameter in each: (a) `src/RoslynMcp.Host.Stdio/Tools/SnippetAnalysisTools.cs` line 19 — `usings`: replace current description with one that opens "Pass as a native JSON array, not a JSON-encoded string. Example: [\"System.IO\", \"System.Net.Http\"]." (b) `src/RoslynMcp.Host.Stdio/Tools/ScriptingTools.cs` line 21 — `imports`: same pattern. (c) `src/RoslynMcp.Host.Stdio/Tools/WorkspaceWarmTools.cs` line 34 — `projects`: "Pass as a native JSON array, not a JSON-encoded string. Example: [\"MyProject.Core\", \"MyProject.Tests\"]." (d) `src/RoslynMcp.Host.Stdio/Tools/ValidationBundleTools.cs` line 29 — `changedFilePaths`: "Pass as a native JSON array of absolute file paths, not a JSON-encoded string. Example: [\"/abs/path/a.cs\", \"/abs/path/b.cs\"]." The canonical phrase that must appear in every updated description is "native JSON array". Add **1 new test method** to `tests/RoslynMcp.Tests/SurfaceCatalogTests.cs` — `AllArrayTypedToolParameters_DescriptionContainsNativeJsonArrayPhrase` — using reflection over `McpServerToolAttribute`-decorated methods, filtering parameters by array-assignable type, asserting each parameter's `[Description]` text contains the phrase "native JSON array". Scope assert to the 4 fixed tool names (pass a `HashSet<string>` of `toolName:paramName` pairs to test) since the remaining 5 are out of scope. |
| Scope | Production files touched: 4 — the four `Tools/*.cs` files above. Test files modified: 1 (extend `tests/RoslynMcp.Tests/SurfaceCatalogTests.cs` with 1 new method). Does NOT claim tool-surface-only exemption — 4 files hits the Rule 3 fix/refactor ceiling. **Executor must spin off a follow-on backlog row for the remaining 5 array parameters** (`AdvancedAnalysisTools.cs:filePaths`, `MSBuildTools.cs:includedNames`, `InterfaceExtractionTools.cs:memberNames`, `ScaffoldingTools.cs:interfaces`, `ParameterObjectTools.cs:dtoFolders`). |
| Tool policy | edit-only |
| Estimated context cost | 28000 |
| Risks | (1) Description text changes are not semantically validated by the compiler — regression test is the only guard. The test must reflect over the `Host.Stdio` assembly at test time; if the assembly is not loaded, walk returns zero methods and test passes vacuously. Executor must assert result count > 0. (2) The 5 unfixed parameters remain incorrect after this wave — the follow-on backlog row must be created at executor Step 7 sync or the fix is incomplete. (3) `SnippetAnalysisTools.cs` and `ScriptingTools.cs` already have inline examples using escaped-JSON notation — technically correct but ambiguous. Update should clarify that the outer `[...]` is a JSON array, not a string containing a bracket. (4) Fanout probe confirmed: 9 distinct files. Per-initiative fanout=4 (within Rule 3); fanoutEstimate=9 reflects full problem space. |
| Validation | (1) `mcp__roslyn__compile_check` after each description edit. (2) Run `mcp__roslyn__test_run --filter "AllArrayTypedToolParameters_DescriptionContainsNativeJsonArrayPhrase"` — must pass. (3) Run full `SurfaceCatalogTests`. (4) Manual spot-check: call `evaluate_csharp` with `imports: "[\"System.IO\"]"` (mis-encoded form) — confirm tool description in the MCP manifest now contains "native JSON array". |
| Performance review | N/A — correctness fix, no hot-path changes. Description attributes are read once at server startup and cached. |
| CHANGELOG category | Fixed |
| CHANGELOG entry (draft) | Fixed: `[Description]` text on `string[]`-typed tool parameters (`usings`, `imports`, `projects`, `changedFilePaths`) now explicitly states "Pass as a native JSON array, not a JSON-encoded string" with a concrete example, preventing LLM clients from mis-encoding array values as stringified JSON. |
| Backlog sync | Close rows: [`filepaths-array-vs-stringified-tool-description-clarification`]. **Spin off new row** `filepaths-array-vs-stringified-tool-description-clarification-batch-2` for the remaining 5 array-typed parameters (`AdvancedAnalysisTools.cs:filePaths`, `MSBuildTools.cs:includedNames`, `InterfaceExtractionTools.cs:memberNames`, `ScaffoldingTools.cs:interfaces`, `ParameterObjectTools.cs:dtoFolders`). |

<details>
<summary>Sonnet handoff notes</summary>

- **Edit coordinates (4 production files):** `SnippetAnalysisTools.cs:19` (`usings`), `ScriptingTools.cs:21` (`imports`), `WorkspaceWarmTools.cs:34` (`projects`), `ValidationBundleTools.cs:29` (`changedFilePaths`). Each is a single `[Description("...")]` line on the parameter declaration — replace the string literal in place; do not touch the parameter signature, default value, or surrounding `[Description]` attributes on adjacent parameters.
- **Test target:** add `AllArrayTypedToolParameters_DescriptionContainsNativeJsonArrayPhrase` to `tests/RoslynMcp.Tests/SurfaceCatalogTests.cs` (existing class, line 15). Mirror `McpToolMetadata_RequiredOnEveryTool_MatchesCatalogEntry` at line 83 — same assembly walk via `typeof(ServerTools).Assembly` + `GetMethods(BindingFlags.Public | BindingFlags.NonPublic | BindingFlags.Static)` + `GetCustomAttribute<McpServerToolAttribute>()`. Do NOT create a new test file.
- **Test reflection predicate gotcha:** scope assert to a `HashSet<(string tool, string param)>` containing exactly the 4 in-scope pairs (`analyze_snippet:usings`, `evaluate_csharp:imports`, `workspace_warm:projects`, `validate_workspace:changedFilePaths`). The deferred `AdvancedAnalysisTools.cs:150` parameter is typed `IReadOnlyList<string>?` (not `string[]?`) — a naive "array-assignable" predicate would catch it; the explicit allowlist avoids that confusion. Assert allowlist count > 0 to prevent vacuous pass when assembly fails to load.
- **Canonical phrase:** every updated `[Description]` must contain the exact substring `native JSON array` (case-sensitive — the test asserts this literal). The two pre-existing inline examples (`[\"System.IO\", ...]`) must be preserved or rewritten to keep the outer `[...]` unambiguous as a JSON array.
- **Negative space:** do NOT edit `AdvancedAnalysisTools.cs:150`, `MSBuildTools.cs:60`, `InterfaceExtractionTools.cs:30`, `ScaffoldingTools.cs:35`, or `ParameterObjectTools.cs:37` — those 5 are the `-batch-2` spin-off row (Backlog sync field is mandatory at Step 7).

</details>

<!-- cycle-1-remediation: fanoutEstimate reset from 9 (full problem space) to 4 (per-initiative blast radius, matching productionFilesTouched) per Rule 5b. Spin-off row id `filepaths-array-vs-stringified-tool-description-clarification-batch-2` made explicit in Backlog sync. -->


### 20. list-analyzers-totalrules-variance

| Field | Content |
|---|---|
| Status | **deferred** (variance is workspace-session-level, not a determinism bug; no actionable code change identified) |
| Backlog rows closed | `list-analyzers-totalrules-variance` |
| Diagnosis | `totalRules` in `src/RoslynMcp.Host.Stdio/Tools/AnalyzerInfoTools.cs:33` is computed as `results.Sum(a => a.Rules.Count)` over ALL results returned from `ListAnalyzersAsync` BEFORE the `Skip(offset).Take(limit)` pagination slice on lines 34-38. The field is a full-walk total, not a paged count — the rename-to-`returnedRuleCount` approach proposed in the backlog row is inapplicable. The tool description already states: "totalRules = sum of rules across all assemblies — use this for total rule count without paging the full list." The 87-rule variance (495 vs 408) is explained by normal workspace-session variation: `src/RoslynMcp.Roslyn/Services/AnalyzerInfoService.cs:24` calls `_workspace.GetCurrentSolution(workspaceId)` — different workspace load sessions produce different MSBuild compilation snapshots with different sets of `project.AnalyzerReferences`. The two audit calls 28 minutes apart were almost certainly issued against different workspace IDs. Within a single stable workspace ID, two back-to-back calls would already return identical `totalRules`. There is no reproducible determinism bug. |
| Approach | No code change required. Mark deferred. If a future session reproduces the variance using the SAME workspace ID (verified by passing the identical `workspaceId` token in both calls), investigation should look at whether the workspace's `GetCurrentSolution` snapshot mutates between calls — a different and much deeper issue in `WorkspaceManager.cs`. |
| Scope | 0 production files, 0 test files. |
| Tool policy | edit-only |
| Estimated context cost | 15000 |
| Risks | If a future session DOES reproduce identical-workspace-ID variance, the root cause shifts to `WorkspaceManager.GetCurrentSolution` snapshot mutation — a deeper issue outside this row's scope. The proposed regression test ("2 back-to-back calls asserting identical `totalRules`") would already pass today and cannot distinguish same-workspace-ID stability from cross-session variation. |
| Validation | N/A — no change shipped. To reopen: reproduce the variance using the SAME workspace ID value in both calls; record the workspace ID token, elapsed time, and any `workspace_reload`. If confirmed same-ID, file a new backlog row targeting `WorkspaceManager.GetCurrentSolution`. |
| Performance review | N/A. |
| CHANGELOG category | Maintenance |
| CHANGELOG entry (draft) | (none — no code or documentation change shipped) |
| Backlog sync | Close rows: [`list-analyzers-totalrules-variance`]. Closure note: variance explained by workspace-session-level analyzer evolution, not a determinism defect. Reopen only if reproduced same-workspaceId. |


### 21. compile-check-not-connected-raw-transport-error-envelope

| Field | Content |
|---|---|
| Status | **deferred** (no server-side interception surface for the raw transport-level "Not connected" error; WorkspaceEvictedException path shipped 1.34.2 covers the post-recycle workspace-lookup case; defer until a second independent occurrence confirms a reproducible server-side gap) |
| Backlog rows closed | `compile-check-not-connected-raw-transport-error-envelope` |
| Diagnosis | The raw "Not connected" string observed in session 4d410565 subagent a843af1d at L131 originates from the MCP C# SDK's stdio transport layer — not from any tool handler or the `StructuredCallToolFilter`. A grep of `src/` for "Not connected" returns zero matches, confirming this repo emits no such string. The `StructuredCallToolFilter` at `src/RoslynMcp.Host.Stdio/Middleware/StructuredCallToolFilter.cs:115-154` wraps `next(context, cancellationToken)` and catches all C# exceptions, routing them through `ToolErrorHandler.ClassifyAndFormat`. However, a transport-level pipe break (the stdio channel drops while the call is in flight) is emitted by the SDK transport BELOW the filter boundary — it arrives at the client as a raw string before `CallToolResult` is ever constructed. The `WorkspaceEvictedException` shape (shipped 1.34.2 per `ToolErrorHandler.cs:34-50`) covers the structurally different post-recycle case: a workspace lookup miss on the first tool call to a newly-started host process. That path is already handled and returns `category: "WorkspaceEvicted"` with a `workspace_load(path: ...)` recovery hint. The "Not connected" transport error is a distinct failure mode that occurs BEFORE any tool dispatch runs; it cannot be intercepted and reformatted by any production file in this repo without modifying the MCP SDK itself. The backlog row's own defer instruction applies: "defer until a second independent occurrence confirms the recurrence shape." |
| Approach | Deferred — no actionable server-side code path exists to intercept the raw transport "Not connected" string. If a second independent occurrence is reported, the investigation should focus on whether the MCP C# SDK's `StdioServerTransport` can be subclassed or wrapped to provide a structured error envelope for mid-call pipe failures, or whether a keep-alive / reconnect negotiation at the SDK level is the correct fix surface. |
| Scope | Deferred. No files touched this initiative. |
| Tool policy | edit-only |
| Estimated context cost | 15000 |
| Risks | (1) If "Not connected" recurs on `compile_check` or other tools in a second independent session, it may indicate a reproducible SDK transport bug that this server CAN work around (e.g. by catching `IOException`/`ObjectDisposedException` at the filter boundary). Re-examine `StructuredCallToolFilter`'s catch clause at that time. (2) The `WorkspaceEvictedException` path (1.34.2) is confirmed to cover the post-recycle workspace-lookup scenario — note this when closing the backlog row so it isn't re-raised by a future audit as still-open. |
| Validation | N/A — deferred. If un-deferred: (1) simulate a pipe break during an in-flight `compile_check` and confirm whether the SDK delivers the failure as a C# exception or a raw string to the filter; (2) if exception: extend `StructuredCallToolFilter` catch clause to map `IOException`/`ObjectDisposedException` to `category: "Disconnected"` with `recovery: workspace_reload(workspaceId: ...)` hint; (3) if raw string: file an upstream MCP SDK issue. |
| Performance review | N/A. |
| CHANGELOG category | Maintenance |
| CHANGELOG entry (draft) | Deferred `compile-check-not-connected-raw-transport-error-envelope`: raw "Not connected" transport error is below the server's catch boundary; existing `WorkspaceEvicted` envelope (1.34.2) already covers the post-recycle workspace-lookup case. Row parked until a second independent session confirms a reproducible server-side gap. |
| Backlog sync | Close rows: [`compile-check-not-connected-raw-transport-error-envelope`]. Defer rationale noted. Reopen only when a second independent occurrence reproduces the raw "Not connected" string. |


### 22. scaffolding-service-split-by-scaffold-type

| Field | Content |
|---|---|
| Status | **deferred** (pure organizational refactor with no concrete bug motivation; backlog row and deepener instruction both require deferral until a concrete bug motivates the split) |
| Backlog rows closed | `scaffolding-service-split-by-scaffold-type` |
| Diagnosis | `src/RoslynMcp.Roslyn/Services/ScaffoldingService.cs` is confirmed at 2,776 LOC (the backlog row cited 2,521 — the file has grown by ~255 lines since the row was written, with 6 additional commits: fixes for missing usings, nullable ctor args, internal-target accessibility, static-type detection, dotted-FQN stripping, and sibling-inference overbreadth — all landing IN the monolithic file but **none caused BY the monolithic structure**). All nine cited anchors are live. The file owns 4 scaffold types × 3 test frameworks plus sibling-pattern inference, interface resolution, and constructor-arg generation. The recent bug history demonstrates the file is actively modified, but all 6 post-row bug-fixes were localized per-scaffold-type logic errors that would have been equally locatable in partials — file organization did not contribute to any regression. `tests/RoslynMcp.Tests/ScaffoldingIntegrationTests.cs` exists and is the regression anchor. No concrete motivating bug has emerged. The backlog row's own text and the deepener prompt both require deferral under these conditions. |
| Approach | Deferred — no implementation planned. If a future bug is demonstrably harder to find or fix due to the monolithic structure (e.g., merge conflict from concurrent edits to different scaffold types, or a regression that takes >1 session to locate because the relevant method is buried in 2,776 LOC), re-raise with that concrete evidence and scope the split to the affected partial only. The proposed split shape (`ScaffoldingService.TypePreview.cs`, `.TestPreview.cs`, `.TestBatchPreview.cs`, `.FirstTestFilePreview.cs`) would touch 5 production files (original + 4 partials), exceeding Rule 3's 4-file cap. Would need planning as a 2-initiative sequence (e.g., public-method partials first, private-helper partials second). |
| Scope | Deferred. Estimated if executed: 5 production files, 0 new test files. **Exceeds Rule 3 (4-file cap)** without a structural-unit exemption — would require planning as 2 initiatives. |
| Tool policy | edit-only |
| Estimated context cost | 55000 |
| Risks | File is actively modified (6 commits since row creation); a split mid-active-development increases rebase friction. The addenda case study "heroic-last → defer-at-closeout" applies: pure organizational refactors often go obsolete or are deprioritized by the time they run. Phase 2 row (TestFrameworkScaffolder strategy extraction) explicitly depends on this split completing first — deferring this row parks Phase 2 as well. |
| Validation | If re-raised: `tests/RoslynMcp.Tests/ScaffoldingIntegrationTests.cs` must pass unchanged after the split. `mcp__roslyn__compile_check` must report zero errors post-split. `./eng/verify-release.ps1 -Configuration Release` is the full CI gate. |
| Performance review | N/A — correctness fix, no hot-path changes. |
| CHANGELOG category | Maintenance |
| CHANGELOG entry (draft) | Deferred — no entry. If re-raised and shipped: `Maintenance: Split ScaffoldingService.cs into per-scaffold-type partial files (TypePreview, TestPreview, TestBatchPreview, FirstTestFilePreview) — no behavior change.` |
| Backlog sync | Mark deferred in backlog with note: no concrete motivating bug as of 2026-05-12; re-raise if a merge conflict or regression is directly attributable to the monolithic file structure. |

