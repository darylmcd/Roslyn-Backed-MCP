# Backlog sweep plan — 20260517T025647Z

**Generated:** 2026-05-17T02:56:47Z
**Backlog snapshot:** 2026-05-17T00:55:00Z
**Mode:** `/backlog-sweep:prepare count=5`
**Initiative count:** 4 selected (only 4 actionable rows in backlog; count=5 caps higher)
**Deepener status:** 3 ok, 1 error (track-only row)

## Plan summary

Sweep follows the 20260516T200033Z mega-sweep (15 initiatives shipped). All three spin-off rows from that sweep are included here. The 4th initiative (`tool-surface-pagination-or-tool-sets`) is a self-described track-only row — deepener returned `status: error` because neither of its act-on triggers has fired (surface count 168 < ~200 threshold; no small-model friction reported).

## Bundle considerations (Rule 1)

No bundle candidates. Each initiative targets a distinct file set:

- Init 1 (`compact-semantic-grep-pagination`) — `AnalysisTools.cs` only (tool-surface-only exemption)
- Init 2 (`test-suite-fixture-reuse-cohesion-and-validate-git`) — 3 existing test files modified, 0 production
- Init 3 (`test-suite-expanded-surface-class-split`) — 1 existing test file modified + 3 new test files (at Rule 4 cap)
- Init 4 — deepener errored; not plannable this cycle

## Initiatives

### 1. compact-semantic-grep-pagination

| Field | Content |
|---|---|
| Status | pending |
| Backlog rows closed | `compact-semantic-grep-pagination` |
| Diagnosis | Confirmed at `src/RoslynMcp.Host.Stdio/Tools/AnalysisTools.cs:436–438`. The `SemanticGrep` tool wrapper calls `semanticGrepService.SearchAsync(workspaceId, pattern, scope, projectFilter, limit, c)` and serializes `new { count = results.Count, items = results }`. No `offset`, `totalCount`, or `hasMore` field is emitted. Callers who receive a full 500-item page have no signal that more results exist and no mechanism to advance the window. The `ISemanticGrepService.SearchAsync` interface and `SemanticGrepService` implementation do not need to change — the service already hard-caps collection at `limit` (default 500); the wrapper can do `Skip(offset).Take(pageLimit)` on the returned list, mirroring the collect-all-then-paginate pattern used by `find_type_usages` (`AnalysisTools.cs:403`) and `find_reflection_usages` (`AdvancedAnalysisTools.cs:246`). The service's existing `limit` parameter continues to act as the total-hit collection cap; the wrapper's new `offset` + renamed-conceptual `limit` slice the window. Description attribute also requires updating to reflect the new response shape fields (`offset`, `totalCount`, `hasMore`). |
| Approach | Single production file edit — `src/RoslynMcp.Host.Stdio/Tools/AnalysisTools.cs` only. Steps: (1) Add `[Description("...")] int offset = 0` parameter to `SemanticGrep` before `CancellationToken ct`. (2) Call `ParameterValidation.ValidatePagination(offset, limit)` at the top of the lambda (mirrors `FindTypeUsages` at line 400 and `FindReflectionUsages` at line 212). (3) Replace the current single-line serialize with: `var paged = results.Skip(offset).Take(limit).ToList(); var hasMore = offset + paged.Count < results.Count; return JsonSerializer.Serialize(new { count = paged.Count, totalCount = results.Count, hasMore, offset, limit, items = paged }, JsonDefaults.Indented);`. (4) Update the `[Description(...)]` attribute text to replace the current "Response shape: { count, items: … }" sentence with "Paginated via offset/limit … callers should iterate until hasMore=false. totalCount counts all hits up to the hard cap; the paged items slice contains only the [offset, offset+limit) window." Test: extend `tests/RoslynMcp.Tests/SemanticGrepServiceTests.cs` with two new `[TestMethod]` entries: (a) assert `hasMore: true` when `offset=0` and `limit < actual_hit_count` (use the `.` all-identifier pattern against the shared workspace — known to produce >> 5 hits; set `limit: 5`); (b) assert `offset` slides the window (`offset=0` and `offset=5` return disjoint first items). Both tests call `SemanticGrep` via `AnalysisTools.SemanticGrep(gate, service, workspaceId, ...)` to exercise the wrapper, not the service directly. |
| Scope | Production files touched: 1 — `src/RoslynMcp.Host.Stdio/Tools/AnalysisTools.cs`. Rule 3 exemption: tool-surface-only, 1 file (envelope/parameter/description change on an already-registered tool; `ISemanticGrepService` and `SemanticGrepService` are unchanged). Test files modified: 1 — `tests/RoslynMcp.Tests/SemanticGrepServiceTests.cs` (extend with 2 new test methods; no new test file needed). |
| Tool policy | edit-only |
| Estimated context cost | 25000 |
| Risks | (1) The `limit` parameter's semantic shifts subtly: before this fix it was both the collection cap and the page size; after, it is both (no service change, so callers who pass `limit=5` will still receive at most 5 items — `offset` is new and defaults to 0, so existing callers are unaffected). Verify existing `SemanticGrep_Limit_CapsResultCount` test still passes. (2) The `totalCount` field now reflects hits collected up to the hard cap (default 500), not the true universe — this is the same tradeoff `find_reflection_usages` makes; document this in the description attribute update. (3) Fanout probe skipped — surgical edit, no cross-cutting symbol changes. Only the wrapper method is modified; `ISemanticGrepService` and its implementation are untouched. |
| Validation | (1) `mcp__roslyn__compile_check` after edit — zero new CS errors. (2) `mcp__roslyn__test_run --filter SemanticGrepServiceTests` — all existing tests pass; new pagination tests pass. (3) Manual: call `semantic_grep` with `pattern="."`, `scope="identifiers"`, `limit=5`, `offset=0` — verify response contains `hasMore: true`, `totalCount > 5`, `count: 5`, `offset: 0`. (4) Manual: repeat with `offset=5` — verify `items[0]` differs from previous call's `items[0]`. (5) Manual: call with `limit=500`, `offset=0` on small workspace — verify `hasMore: false`. (6) `mcp__roslyn__compile_check` post-test to confirm no analyzer regressions (RMCP001/RMCP002 catalog gates). |
| Performance review | N/A — correctness fix, no hot-path changes. The `Skip/Take` on an already-collected in-memory list is O(n) with n ≤ 500; negligible. |
| CHANGELOG category | Fixed |
| CHANGELOG entry (draft) | Fixed `semantic_grep` response envelope to include `offset`, `totalCount`, and `hasMore` fields, enabling callers to page past the first result window on broad queries. Mirrors the pagination contract established by `find_reflection_usages` and `find_type_usages` (split from gh #760, companion to PR #780). |
| Backlog sync | Close rows: [`compact-semantic-grep-pagination`]. |

### 2. test-suite-fixture-reuse-cohesion-and-validate-git

| Field | Content |
|---|---|
| Status | pending |
| Backlog rows closed | `test-suite-fixture-reuse-cohesion-and-validate-git` |
| Diagnosis | All three test files confirmed present and in the described state. `CohesionAnalysisTests.cs` currently extends `SharedWorkspaceTestBase` (not `TestBase` directly); 5 of its 11 tests use the shared workspace via `LoadSharedSampleWorkspaceAsync`, while 6 tests call `CreateSampleSolutionCopy()` directly with manual `try/finally` + `WorkspaceManager.Close()` + `DeleteDirectoryIfExists()` cleanup. `ValidateRecentGitChangesTests.cs` extends `TestBase` and has 5 tests; 4 call `CreateSampleSolutionCopy()` with the manual-cleanup pattern, and the git-mutation tests additionally call `InitializeGitRepo()` / `StageAndCommitAll()` before `WorkspaceManager.LoadAsync()`. `ChangeSignaturePreviewMetadataNameShapeTests.cs` extends `TestBase` and has 2 tests that both call `CreateSampleSolutionCopy()` with manual `try/finally`. The already-converted sibling `ChangeSignaturePreviewTests.cs` (`tests/RoslynMcp.Tests/ChangeSignaturePreviewTests.cs:21`) uses `await using var workspace = CreateIsolatedWorkspaceCopy()` which auto-closes and auto-deletes on scope exit. `IsolatedWorkspaceTestBase` (`tests/RoslynMcp.Tests/IsolatedWorkspaceTestBase.cs`) inherits from `TestBase` and exposes `CreateIsolatedWorkspaceCopy()` (returns `IsolatedWorkspaceScope`, does not load) and `CreateIsolatedWorkspaceAsync()` (creates + loads). `GetOrLoadWorkspaceIdAsync(SampleSolutionPath, ct)` is defined on `TestBase` (`tests/RoslynMcp.Tests/TestBase.cs:257`) and is the equivalent of `LoadSharedSampleWorkspaceAsync` from `SharedWorkspaceTestBase`. Diagnosis note: the backlog row's characterization of `CohesionAnalysisTests` as a class "with 6 isolated tests that write custom fixture files" is accurate — those 6 tests write files to the copy and load separate workspaces. The remaining 5 tests in the same class use the shared workspace and will be unaffected. |
| Approach | Three test-file-only conversions, no production code changes. **`ChangeSignaturePreviewMetadataNameShapeTests.cs`** — simplest conversion: change base class from `TestBase` to `IsolatedWorkspaceTestBase`. Replace both `CreateSampleSolutionCopy()` + manual `WorkspaceManager.LoadAsync()` + `WorkspaceManager.Close()` in `try/finally` blocks with `await using var workspace = CreateIsolatedWorkspaceCopy()` followed by `await workspace.LoadAsync()`, then reference `workspace.WorkspaceId`. Mirror the pattern in `tests/RoslynMcp.Tests/ChangeSignaturePreviewTests.cs:39-70` exactly. **`ValidateRecentGitChangesTests.cs`** — change base class from `TestBase` to `IsolatedWorkspaceTestBase`. For each test: replace `CreateSampleSolutionCopy()` + `Path.GetDirectoryName()` with `var workspace = CreateIsolatedWorkspaceCopy()` (gives `workspace.SolutionPath` and `workspace.RootPath`). For tests that run `InitializeGitRepo()` before loading, call `workspace.LoadAsync()` explicitly after the git mutations. Replace the `finally { WorkspaceManager.Close(workspaceId); }` block with `await using var workspace = ...` or a `try/finally { await workspace.DisposeAsync(); }` (since the git tests need `finally` to ensure cleanup even when git fails). `RemoveAnyAncestorGitDir()` and all git helper methods remain unchanged. **`CohesionAnalysisTests.cs`** — change base class from `SharedWorkspaceTestBase` to `IsolatedWorkspaceTestBase`. The class `ClassInit` currently calls `LoadSharedSampleWorkspaceAsync(CancellationToken.None)` which is `GetOrLoadWorkspaceIdAsync(SampleSolutionPath, ct)` on `TestBase`; replace with `GetOrLoadWorkspaceIdAsync(SampleSolutionPath, CancellationToken.None)` which is inherited from `TestBase` via `IsolatedWorkspaceTestBase`. For the 6 isolated tests, replace the `CreateSampleSolutionCopy()` + manual `LoadAsync` + `WorkspaceManager.Close` + `DeleteDirectoryIfExists` `try/finally` pattern with `await using var workspace = CreateIsolatedWorkspaceCopy()` then `await workspace.LoadAsync()`. The `workspace.GetPath(...)` helper replaces the manual `Path.Combine(copiedRoot, ...)` calls throughout all 6 tests. |
| Scope | Production files touched: 0. Test files modified: 3 — `tests/RoslynMcp.Tests/CohesionAnalysisTests.cs`, `tests/RoslynMcp.Tests/ValidateRecentGitChangesTests.cs`, `tests/RoslynMcp.Tests/ChangeSignaturePreviewMetadataNameShapeTests.cs`. No new test files added. No production files touched; this is a test-infrastructure-only change. Rule 4: 3 existing test files modified, 0 new test files (within cap of ≤ 3). Rule 3: 0 production files (well within cap). |
| Tool policy | edit-only |
| Estimated context cost | 30000 |
| Risks | (1) `CohesionAnalysisTests` base-class switch: the 5 shared-workspace tests rely on `LoadSharedSampleWorkspaceAsync` which is defined only on `SharedWorkspaceTestBase`. Changing to `IsolatedWorkspaceTestBase` requires substituting `GetOrLoadWorkspaceIdAsync(SampleSolutionPath, CancellationToken.None)` (defined on `TestBase`, so still accessible). The executor must verify no method-resolution error. (2) `ValidateRecentGitChangesTests` git-mutation ordering: `IsolatedWorkspaceScope.LoadAsync()` must be called only after `InitializeGitRepo()` / `StageAndCommitAll()` mutations. The scope supports deferred loading (`CreateIsolatedWorkspaceCopy()` does not auto-load), so this is safe — but the executor must not accidentally use `CreateIsolatedWorkspaceAsync()` (which auto-loads) for the git tests. (3) `SlowCompileCheckService` inner class in `ValidateRecentGitChangesTests` is unaffected by the refactor; verify it remains valid after class restructuring. (4) `[DoNotParallelize]` attribute on `ValidateRecentGitChangesTests` must be preserved — git repo manipulation tests are order-sensitive. (5) The `RemoveAnyAncestorGitDir` test for the no-git-repo fallback calls `CreateSampleSolutionCopy()` without loading a git repo — `IsolatedWorkspaceScope.Dispose()` handles cleanup the same way `DeleteDirectoryIfExists` does, so correctness is preserved. Fanout probe skipped — surgical test-only edit, no cross-cutting symbol changes. |
| Validation | 1. `dotnet build RoslynMcp.slnx -c Release -p:TreatWarningsAsErrors=true` must succeed (no new compile errors from base-class changes). 2. `mcp__roslyn__compile_check` on the three modified test files after each edit. 3. Run `dotnet test --filter "ClassName=RoslynMcp.Tests.CohesionAnalysisTests"` — all 11 tests must pass. 4. Run `dotnet test --filter "ClassName=RoslynMcp.Tests.ValidateRecentGitChangesTests"` — all 5 tests must pass (git-dependent tests may mark `Inconclusive` if git is not on PATH, which is acceptable and matches pre-conversion behaviour). 5. Run `dotnet test --filter "ClassName=RoslynMcp.Tests.ChangeSignaturePreviewMetadataNameShapeTests"` — both tests must pass. 6. Confirm no `CreateSampleSolutionCopy()` direct call remains in any of the three converted files (grep for the raw method name). |
| Performance review | N/A — correctness fix, no hot-path changes. |
| CHANGELOG category | Maintenance |
| CHANGELOG entry (draft) | Converted `CohesionAnalysisTests`, `ValidateRecentGitChangesTests`, and `ChangeSignaturePreviewMetadataNameShapeTests` to `IsolatedWorkspaceTestBase`, replacing manual `CreateSampleSolutionCopy()` + `try/finally` cleanup with the `IsolatedWorkspaceScope` RAII pattern introduced in PR #795. |
| Backlog sync | Close rows: [`test-suite-fixture-reuse-cohesion-and-validate-git`]. |

### 3. test-suite-expanded-surface-class-split

| Field | Content |
|---|---|
| Status | pending |
| Backlog rows closed | `test-suite-expanded-surface-class-split` |
| Diagnosis | `tests/RoslynMcp.Tests/ExpandedSurfaceIntegrationTests.cs` (683 lines, 17 test methods) is a single `[TestClass]` mixing four distinct concerns: (1) general shared-workspace tool-contract tests (completions, syntax, advanced analysis, pagination, diagnostics, symbol/usage, bulk-refs, relationships/cohesion, code-actions, edit-tools — lines 29–554); (2) a repo-solution reflection/DI test already marked `[TestCategory("RepoSolution")]` at line 393; (3) resource and prompt contract tests (lines 421–468); (4) coverage-tool process tests marked `[TestCategory("Process")]` at line 557 (lines 556–624). The `IntegrationTests_*` sibling files shipped in PR #793 (`IntegrationTests.SymbolNavigation.cs`, `IntegrationTests.WorkspaceCore.cs`, `IntegrationTests.EditApply.cs`) established the pattern: one focused `[TestClass]` per concern, each with its own `[ClassInitialize]`/`[ClassCleanup]` calling `InitializeServices()`/`DisposeServices()` and `GetOrLoadWorkspaceIdAsync(SampleSolutionPath, ...)`. The existing `[TestCategory("RepoSolution")]` on `Reflection_And_Di_Analysis_Run_On_Repo_Solution` and `[TestCategory("Process")]` on `TestCoverageTool_Returns_Structured_Response` already signal where the class-level category attributes should land. The `FakeCodeActionService` nested class and `GetPromptText` helper are only used by tool-contract tests; `FindDocumentPath` and `LoadWorkspaceCopyAsync` are used across the general and edit-tools groups. No production code is involved; no product behavior changes. |
| Approach | Delete `tests/RoslynMcp.Tests/ExpandedSurfaceIntegrationTests.cs` and replace with three focused files following the `IntegrationTests_*` pattern: **(1)** `tests/RoslynMcp.Tests/ExpandedSurfaceIntegrationTests.ToolContract.cs` — class `ExpandedSurfaceIntegrationTests_ToolContract : SharedWorkspaceTestBase`; moves `Resources_Return_Machine_Readable_Contracts`, `Prompts_Return_Actionable_Context`, plus the `FakeCodeActionService` nested class and the `GetPromptText` private helper. No class-level `[TestCategory]` — these are sample-workspace tool-surface tests, same category as the general integration tests. **(2)** `tests/RoslynMcp.Tests/ExpandedSurfaceIntegrationTests.RepoSolutionAnalysis.cs` — class `ExpandedSurfaceIntegrationTests_RepoSolutionAnalysis : SharedWorkspaceTestBase`; add `[TestCategory("RepoSolution")]` at class level (PR #792 established this convention, mirrored in `SecurityDiagnosticIntegrationTests.cs:10`); moves `Reflection_And_Di_Analysis_Run_On_Repo_Solution` (remove the redundant per-method `[TestCategory("RepoSolution")]`). **(3)** `tests/RoslynMcp.Tests/ExpandedSurfaceIntegrationTests.CoverageProcess.cs` — class `ExpandedSurfaceIntegrationTests_CoverageProcess : SharedWorkspaceTestBase`; add `[TestCategory("Process")]` at class level; moves `TestCoverageTool_Returns_Structured_Response` and `TestCoverageTool_WhenCoverletMissing_ReturnsStructuredErrorBeforeRunning` (remove the per-method `[TestCategory("Process")]`). The remaining 12 tests (completions, syntax, advanced analysis, pagination, diagnostics, symbol/usage, bulk-references, relationships/cohesion, code-actions, edit-tools) stay in a renamed `tests/RoslynMcp.Tests/ExpandedSurfaceIntegrationTests.cs` with class `ExpandedSurfaceIntegrationTests : SharedWorkspaceTestBase` — this file retains the `FindDocumentPath`, `LoadWorkspaceCopyAsync` helpers (both `private static`) and the general test body. Each new file gets the identical `[ClassInitialize]`/`[ClassCleanup]` boilerplate that calls `InitializeServices`/`DisposeServices` and `GetOrLoadWorkspaceIdAsync(SampleSolutionPath, ...)`, mirroring `IntegrationTests.SymbolNavigation.cs:6-22`. |
| Scope | Production files touched: 0. Test files modified: 1 (`tests/RoslynMcp.Tests/ExpandedSurfaceIntegrationTests.cs` — shrunk to the general-tools class, helpers retained). Test files added: 3 (`ExpandedSurfaceIntegrationTests.RepoSolutionAnalysis.cs`, `ExpandedSurfaceIntegrationTests.ToolContract.cs`, `ExpandedSurfaceIntegrationTests.CoverageProcess.cs`). Total test file changes: 4 (1 modified + 3 added). Rule 4 note: the rule caps "adds ≤3 new test files" — this initiative adds exactly 3 new files and modifies 1 existing, which is within the spirit and letter of Rule 4. No CHANGELOG fragment is needed (test-only refactor with no user-visible behavior change), but a `changelog.d/test-suite-expanded-surface-class-split.md` fragment under category `Maintenance` should be emitted to satisfy the repo's fragment convention. |
| Tool policy | edit-only |
| Estimated context cost | 40000 |
| Risks | (1) `ClassInitialize`/`ClassCleanup` must be correctly duplicated in each new file — missing `InitializeServices()` will cause null-reference failures at test discovery time. Mirror `IntegrationTests.SymbolNavigation.cs:13-21` exactly. (2) `[DoNotParallelize]` must be present on all three new classes — the shared sample workspace is a static resource; removing this attribute allows parallel dispatch which corrupts state. (3) `FakeCodeActionService` is a `private sealed class` nested inside the monolithic class; it must move into `ExpandedSurfaceIntegrationTests_ToolContract` alongside `PreviewCodeAction_Serializes_Service_Response`. If left in the original file, the `ToolContract` class will fail to compile. (4) `GetPromptText` uses `(message.Content as TextContentBlock)` from `ModelContextProtocol.Protocol` — ensure the using directive is present in the `ToolContract` file. (5) The backlog row says sequenced AFTER PR #792 (`test-suite-heavy-lane-categorization`); PR #792 appears already shipped given `[TestCategory("RepoSolution")]` and `[TestCategory("Process")]` are already in the current file at lines 393 and 557. Executor should verify these category attributes are test-runner-honored before moving them to class level. (6) The existing `TestCoverageTool_WhenCoverletMissing_ReturnsStructuredErrorBeforeRunning` test is NOT decorated `[TestCategory("Process")]` at the method level, even though it belongs semantically with the coverage-process group — confirm whether moving it to `CoverageProcess` class is the correct grouping or if it stays in the general class. |
| Validation | (1) `dotnet test --filter "FullyQualifiedName~ExpandedSurfaceIntegrationTests"` must continue to collect and pass all 17 test methods across the 4 resulting classes. (2) `dotnet test --filter "TestCategory=RepoSolution"` must include `Reflection_And_Di_Analysis_Run_On_Repo_Solution` from `ExpandedSurfaceIntegrationTests_RepoSolutionAnalysis`. (3) `dotnet test --filter "TestCategory=Process"` must include both coverage tests from `ExpandedSurfaceIntegrationTests_CoverageProcess`. (4) `mcp__roslyn__compile_check` on the test project must return zero errors after all edits. (5) No test previously passing must become a new failure — this is a pure move, no assertion changes. (6) `./eng/verify-ai-docs.ps1` must pass (no new markdown link hrefs introduced). |
| Performance review | N/A — correctness fix, no hot-path changes. Test-only refactor with no product code modifications. |
| CHANGELOG category | Maintenance |
| CHANGELOG entry (draft) | Refactor: split `ExpandedSurfaceIntegrationTests` into three focused test classes (`ExpandedSurfaceIntegrationTests_RepoSolutionAnalysis`, `ExpandedSurfaceIntegrationTests_ToolContract`, `ExpandedSurfaceIntegrationTests_CoverageProcess`) mirroring the `IntegrationTests_*` pattern; class-level `[TestCategory]` attributes now correctly scope `RepoSolution` and `Process` lane filters without per-method decoration. No product behavior changes. |
| Backlog sync | Close rows: [`test-suite-expanded-surface-class-split`]. Mark obsolete: none. Update related: none. |

### 4. tool-surface-pagination-or-tool-sets

_Deepener returned `status: error`: row is self-described as track-only; neither trigger condition has fired (current surface count is 168 vs ~200 threshold; no small-model client friction reported externally). Additionally, catalog-level pagination is already implemented in `src/RoslynMcp.Host.Stdio/Resources/ServerResources.cs` via `roslyn://server/catalog/tools/{offset}/{limit}` and `roslyn://server/catalog/prompts/{offset}/{limit}` resources, further reducing urgency. Recommend: leave the backlog row in place as a tracking entry; do not execute. The reviewer may recommend moving the row to the `Defer` section formally._
