# Backlog sweep plan — 20260519T145945Z

**Generated:** 2026-05-19T14:59:46Z
**Backlog snapshot:** 2026-05-19T13:48:28Z (`ai_docs/backlog.md` updated_at)
**Schema version:** 3 (prepare-extended)
**Initiative count:** 6 (all Low / P3-shaped from 2026-05-16 self-audit)

## Selection notes

count=15 cap requested; only 6 concrete rows are sweep-actionable today.

Excluded:
- 2 aggregator rows (`firewallanalyzer-p2-polish-aggregate-20260516` Medium, `firewallanalyzer-p3-polish-aggregate-20260516` Low) — backlog standing rule says replace umbrella rows with concrete follow-ons **before** planning against them.
- 1 track-only row (`tool-surface-pagination-or-tool-sets`) — weak-evidence; act-on triggers haven't fired.
- 4 `Reserved` good-first-issue rows — skipped per planner Step 1.
- 6 `Defer` rows — explicitly parked with re-evaluate conditions.

No Rule 1 bundle candidates — each row has its own distinct code path / service file. Cross-checked against the two most recent state.json files (20260518T221744Z, 20260517T235058Z) — no overlap with shipped initiatives.

All 6 selected rows are P3-priority single-bug fixes from the 2026-05-16 self-audit on roslyn-mcp 1.38.1. **Anchor staleness was widespread:** 5 of 6 backlog rows cite source paths that no longer exist (services were renamed/consolidated since the audit). Deepeners located the real fix sites; see per-initiative `Diagnosis` notes.

## Initiatives

### 1. workspace-changes-atomic-batch-split-without-batchid

| Field | Content |
|---|---|
| Status | pending |
| Backlog rows closed | `workspace-changes-atomic-batch-split-without-batchid` |
| Diagnosis | Root cause confirmed in `src/RoslynMcp.Roslyn/Services/EditService.cs:577-579`. `ApplyTextEditsCoreAsync` (private) calls `_changeTracker?.RecordChange(workspaceId, $"Apply text edit to {Path.GetFileName(filePath)}", [filePath], toolName)` once per file. `ApplyMultiFileTextEditsAsync` (the multi-file batch path, lines 111-198) calls `ApplyTextEditsCoreAsync` in a per-file `foreach` loop (lines 159-184) without suppressing the per-file `RecordChange`, so a 2-file batch produces 2 separate ledger entries. This matches the gh #740 symptom exactly. The undo path correctly uses a single batch-level snapshot (line 152-156 captures all files at once), but the change-tracker path did not mirror this. Contrast with `CompositeApplyOrchestrator.ApplyCompositeAsync` at `src/RoslynMcp.Roslyn/Services/CompositeApplyOrchestrator.cs:83`, which already records one entry covering all `distinctFiles` after its loop — that is the pattern to mirror here. **Stale anchor:** the backlog row cited `src/RoslynMcp.Roslyn/Services/WorkspaceChangeLedger.cs`, but that file does not exist; the actual implementation lives in `src/RoslynMcp.Roslyn/Services/ChangeTracker.cs` and `src/RoslynMcp.Roslyn/Services/EditService.cs`. Both located and verified via symbol search. |
| Approach | **Fix (option a — merge into one entry, mirrors `CompositeApplyOrchestrator`):** In `src/RoslynMcp.Roslyn/Services/EditService.cs`, add a `bool suppressChangeTrackerRecord = false` parameter to the private `ApplyTextEditsCoreAsync` method. When `ApplyMultiFileTextEditsAsync` calls it from the per-file loop, pass `suppressChangeTrackerRecord: true` so each individual file apply does not emit a ledger entry. After the per-file loop completes (line 184), before returning `MultiFileEditResultDto`, add a single `_changeTracker?.RecordChange(workspaceId, $"Apply text edits to {fileEdits.Count} file(s)", results.Select(r => r.FilePath).ToList(), toolName)` call — mirroring `CompositeApplyOrchestrator.cs:83`. The single-file path through `ApplyTextEditsAsync` continues calling `ApplyTextEditsCoreAsync` with `suppressChangeTrackerRecord: false` (default), so its behavior is unchanged. **Test:** add one test method to `tests/RoslynMcp.Tests/ChangeTrackerTests.cs` — `ApplyMultiFileEdit_TwoFileBatch_ProducesOneChangeTrackerEntry` — following the pattern of existing `ChangeTrackerTests` fixtures: create isolated workspace, clear tracker, apply a 2-file batch via `EditService.ApplyMultiFileTextEditsAsync`, assert `GetChanges(wsId).Count == 1`, assert `AffectedFiles.Count == 2` on the single entry, assert `ToolName == "apply_multi_file_edit"`. |
| Scope | Production files: 1 (`src/RoslynMcp.Roslyn/Services/EditService.cs`). Test files: 1 (extend `tests/RoslynMcp.Tests/ChangeTrackerTests.cs` with 1 new method). No deletions. |
| Tool policy | edit-only |
| Estimated context cost | 28000 |
| Risks | (1) The description in the single merged entry changes from the per-file `"Apply text edit to X.cs"` format to `"Apply text edits to N file(s)"` — callers parsing description text would see a different string. Low risk: description is human-readable, not a machine-parseable contract field. (2) The `ChangeRecorded` event fires once instead of twice for a 2-file batch; `IUndoService.CommitPendingCapture` is called once (correct, since there is one undo snapshot). This is intentional and correct. (3) The stale anchor `WorkspaceChangeLedger.cs` in the backlog row does not exist; the actual implementation is `ChangeTracker.cs` and `EditService.cs`. No executor confusion risk since Approach names the real files. (4) Verify single-file `apply_text_edit` path (goes through `ApplyTextEditsAsync` → `ApplyTextEditsCoreAsync` with default `suppressChangeTrackerRecord: false`) is unaffected and still emits one entry per call. |
| Validation | (1) Run the new `ChangeTrackerTests.ApplyMultiFileEdit_TwoFileBatch_ProducesOneChangeTrackerEntry` — must produce exactly 1 ledger entry with `AffectedFiles.Count == 2` and `ToolName == "apply_multi_file_edit"`. (2) Run existing `ChangeTrackerTests` suite — all existing tests must pass unchanged (single-file `apply_text_edit` path still emits 1 entry per call). (3) Run `mcp__roslyn__compile_check` after the edit to confirm no new diagnostics. (4) Build: `dotnet build RoslynMcp.slnx -c Release -p:TreatWarningsAsErrors=true`. |
| Performance review | N/A — correctness fix, no hot-path changes. |
| CHANGELOG category | Fixed |
| CHANGELOG entry (draft) | Fixed `workspace_changes` splitting an atomic `apply_multi_file_edit` two-file batch into separate ledger entries: the multi-file apply path now records one consolidated change-tracker entry covering all affected files (matching the `apply_composite_preview` behavior), so `revert_apply_by_sequence` and callers reading `workspace_changes` see the batch as a single atomic unit. Fixes gh #740. |
| Backlog sync | Close rows: [`workspace-changes-atomic-batch-split-without-batchid`]. |

### 2. validate-workspace-changetracker-no-disk-reconcile-after-git-checkout

| Field | Content |
|---|---|
| Status | pending |
| Backlog rows closed | `validate-workspace-changetracker-no-disk-reconcile-after-git-checkout` |
| Diagnosis | Root cause is in `src/RoslynMcp.Roslyn/Services/WorkspaceValidationService.cs:344–352`, method `ResolveChangedFiles`. When `caller` is null/empty the method falls back to `_changeTracker.GetChanges(workspaceId)` and returns the union of all `AffectedFiles` from every recorded mutation entry. `ChangeTracker` (`src/RoslynMcp.Roslyn/Services/ChangeTracker.cs:33–63`) is a pure append-only log — it records every `RecordChange` call but has no mechanism to remove entries when a file is subsequently reverted via an out-of-band operation (e.g. `git checkout -- .editorconfig`). After the revert the file is provably clean (`git status --porcelain` returns nothing), but the ChangeTracker still holds the original mutation entry, so `ResolveChangedFiles` includes `.editorconfig` in `changedFiles` — causing `validate_workspace(changedFilePaths=null)` to surface it in `ChangedFilePaths`. The fix is to reconcile the ChangeTracker-derived list against `git status --porcelain` (the same mechanism already present in `ValidateRecentGitChangesAsync` via `CollectGitChangedFilesAsync` at line 111) and exclude any file that git reports as clean. Backlog row's "OR document the intent" alternative is not the right fix: callers pass `null` expecting "what is actually dirty now", and silently returning stale entries is a correctness defect. The fix does not require touching `ChangeTracker` itself — the reconciliation belongs in the consumer (`ResolveChangedFiles`) because the tracker's append-only log is correct for its other consumers (undo/revert history via `IUndoService`). |
| Approach | Modify `src/RoslynMcp.Roslyn/Services/WorkspaceValidationService.cs` only. In `ResolveChangedFiles` (line 291), after collecting the ChangeTracker-derived file list (lines 347–351), add a git-status reconciliation step: call `CollectGitChangedFilesAsync` (already present at line 111, extracted from `ValidateRecentGitChangesAsync`) with the solution directory to get the set of currently-dirty files, then intersect the ChangeTracker list against that set. Files in the ChangeTracker list that are NOT reported dirty by git are excluded (they were reverted). If `CollectGitChangedFilesAsync` returns warnings (git unavailable / non-git repo), fall back to the existing unfiltered behavior — same fallback semantics already used in `ValidateRecentGitChangesAsync`. This keeps the no-git graceful degradation path intact. No changes to `ChangeTracker.cs`, `IChangeTracker.cs`, or any DTO. |
| Scope | Production files: 1 — `src/RoslynMcp.Roslyn/Services/WorkspaceValidationService.cs`. Test files: 1 — new `tests/RoslynMcp.Tests/ValidateWorkspaceChangeTrackerReconcileTests.cs` using `IsolatedWorkspaceTestBase` (same pattern as `ChangeTrackerTests.cs`). Fixture: apply a text edit to `Dog.cs` (records it in ChangeTracker), then revert the file on disk via `File.WriteAllText` to restore original content (simulating `git checkout --`), then call `ValidateAsync(changedFilePaths: null)` and assert `ChangedFilePaths` does not contain the reverted file. (Integration-level git revert can also be tested if the isolated workspace is a git repo; if not, the file-content-hash variant confirms the same path.) |
| Tool policy | edit-only |
| Estimated context cost | 30000 |
| Risks | (1) `CollectGitChangedFilesAsync` is async and currently called only from the async `ValidateRecentGitChangesAsync` path; adding it to `ResolveChangedFiles` (currently synchronous) will require making `ResolveChangedFiles` async or extracting the git call to the call site in `ValidateInternalAsync`. The safer approach is to pass the git-dirty set in from `ValidateInternalAsync` rather than calling it inside the synchronous helper — executor should verify the call chain. (2) If the git invocation is slow (non-SSD, large repo), the extra `git status` call adds latency to every `ValidateAsync(null)` call. Mitigate: reuse the existing `_gitStatusTimeout` field (already set to 10 s); the call is bounded. (3) Files mutated through non-git paths (e.g. in-process edits not yet flushed to disk) would be excluded if git does not see them — this is the correct behavior (the file is dirty in-memory but git agrees it's clean on disk if the on-disk copy matches HEAD). Verify this scenario does not regress the existing `ValidateAsync_NullChangedFilePaths_NoUnknownSurfaced` test. (4) No peer bundle candidates; `bundleCandidatePeers` was empty. |
| Validation | (1) New test in `ValidateWorkspaceChangeTrackerReconcileTests.cs`: apply edit → revert file to original content → `ValidateAsync(null)` → assert reverted file absent from `ChangedFilePaths`. (2) Existing `ValidateWorkspaceSummaryTests` must all pass (especially `ValidateAsync_NullChangedFilePaths_NoUnknownSurfaced` which asserts change-tracker fallback path). (3) `ChangeTrackerTests` must all pass (ChangeTracker.cs is not modified). (4) `dotnet build RoslynMcp.slnx -c Release -p:TreatWarningsAsErrors=true` green. (5) Manual repro from gh #738: load workspace, `git checkout -- .editorconfig`, call `validate_workspace(changedFilePaths=null)`, confirm `.editorconfig` is absent from `changedFilePaths` in response. |
| Performance review | N/A — correctness fix, no hot-path changes. The added `git status` call is bounded by the existing `_gitStatusTimeout` (10 s) and runs only on the `changedFilePaths=null` branch. |
| CHANGELOG category | Fixed |
| CHANGELOG entry (draft) | Fixed `validate_workspace(changedFilePaths=null)` returning stale file paths in `changedFilePaths` after an out-of-band revert (e.g. `git checkout -- <file>`). The ChangeTracker auto-scope list is now reconciled against `git status` before being used as the validation scope; reverted files are excluded. Fixes gh #738. |
| Backlog sync | Close rows: [`validate-workspace-changetracker-no-disk-reconcile-after-git-checkout`]. |

### 3. find-type-mutations-single-scope-misses-compound-io

| Field | Content |
|---|---|
| Status | pending |
| Backlog rows closed | `find-type-mutations-single-scope-misses-compound-io` |
| Diagnosis | Root cause confirmed in `src/RoslynMcp.Roslyn/Services/MutationAnalysisService.cs` at `ClassifyMethodMutationScope` (line 729–732): when `TryClassifyMethodSideEffects` returns a non-null scope (e.g. `IO`), the method returns immediately without checking `HasInstanceFieldAssignment` or `HasMutatingCollectionCall`. This means any method performing both a high-severity side effect (IO/Process/Network/Database) and a lower-severity one (CollectionWrite/FieldWrite) only reports the highest severity. The `MutationScope` field on `MutatingMemberDto` is a `string` (not a Flags enum — the backlog's enum framing is aspirational; no enum exists in the codebase). The `SideEffectClassifier.Scopes` class holds the string constants. **Stale anchors:** the backlog cited `src/RoslynMcp.Roslyn/Services/MutationAnalysisService.cs:87` — line 87 is string interpolation inside `AnalyzeImpactAsync`, not the classifier entry point. True entry points are `ClassifyMutationScope` (line 679) and `ClassifyMethodMutationScope` (line 713). The "MutationScope enum definition" anchor is also stale: no enum exists, only a `string` property default-valued `"FieldWrite"` on the positional record at `src/RoslynMcp.Core/Models/TypeMutationDto.cs:19`. Fix: change `MutatingMemberDto.MutationScope` from `string` to `IReadOnlyList<string>`, collect all applicable scopes (don't short-circuit), and update `ClassifyMethodMutationScope` to gather every dimension independently before returning. |
| Approach | **1. `src/RoslynMcp.Core/Models/TypeMutationDto.cs`** — Change `MutatingMemberDto.MutationScope` parameter from `string` to `IReadOnlyList<string>`, rename to `MutationScopes` (or keep `MutationScope` as a `string` joined value — pick the list shape for precision). Update the XML doc to describe the multi-scope behavior. **2. `src/RoslynMcp.Roslyn/Services/MutationAnalysisService.cs`** — Rewrite `ClassifyMethodMutationScope` to gather all dimensions instead of short-circuiting. Pattern: collect into a local `List<string> scopes`. Call `TryClassifyMethodSideEffects` and if non-null, add to `scopes`. Then independently check `HasInstanceFieldAssignment` (add `FieldWrite`) and `HasMutatingCollectionCall` (add `CollectionWrite`). Update `MutationCandidate` record's `Scope` field from `string?` to `IReadOnlyList<string>?` (or `string[]`). Mirror the pattern of `SideEffectClassifier.ClassifyMethodSideEffects` (lines 36–70) which already accumulates across both invocations and type-constructions — apply the same accumulation model here across the three scope dimensions. Update `BuildMutatingMemberAsync` at line 448 which passes `candidate.Scope!` to `MutationScope:` to pass the list. **3. `tests/RoslynMcp.Tests/MutationAnalysisSideEffectsTests.cs`** — Update the existing test `FindTypeMutations_FileIOClass_FlagsWriteMethodsAsIO` (line 148) to assert `MutationScope` contains `IO` rather than equals it. Add one new test method covering the compound-scope repro from gh #741: a fixture class whose `LoadAsync` does both `TryAdd` (CollectionWrite) and `File.OpenRead` (IO) — assert both scopes appear on the member. |
| Scope | Production files touched (2): `src/RoslynMcp.Core/Models/TypeMutationDto.cs`, `src/RoslynMcp.Roslyn/Services/MutationAnalysisService.cs`. Test files modified (1): `tests/RoslynMcp.Tests/MutationAnalysisSideEffectsTests.cs` (extend existing file with 1 new test method + update 2 existing assertions). Satisfies Rule 3 (2 prod files) and Rule 4 (0 new test files, 1 existing extended). |
| Tool policy | edit-only |
| Estimated context cost | 35000 |
| Risks | **Breaking change to `MutatingMemberDto.MutationScope`:** changing from `string` to `IReadOnlyList<string>` (or renaming to `MutationScopes`) is a wire-format breaking change for any caller deserializing `find_type_mutations` responses. The CHANGELOG category should be `Changed — BREAKING`. Callers that compare `MutationScope == "IO"` must switch to `MutationScopes.Contains("IO")`. **Existing test updates required:** `FindTypeMutations_FileIOClass_FlagsWriteMethodsAsIO` at line 148 uses `Assert.AreEqual(SideEffectClassifier.Scopes.IO, writeManifest.MutationScope)` — this becomes a list-contains check. **Hotspot adjacency:** `WorkspaceManager.cs` is listed as a hotspot, but this initiative does not touch it (the fix is in the classifier service and DTO layer). Safe to schedule in the same wave as other initiatives. **Anchor staleness noted:** the cited `MutationAnalysisService.cs:87` and the "MutationScope enum definition" anchor are both stale; executor should navigate to `ClassifyMethodMutationScope` at line 713 and the `MutatingMemberDto` record at `TypeMutationDto.cs:12–19` instead. |
| Validation | 1. Run `mcp__roslyn__compile_check` after edits — zero errors. 2. Run `mcp__roslyn__test_run --filter MutationAnalysisSideEffectsTests` — all existing tests pass with updated assertions, new compound-scope test passes. 3. Manually verify: a fixture class with a method that calls both `File.WriteAllText` (IO) and `_dict.TryAdd(key, val)` (CollectionWrite) returns `MutationScopes` containing both `"IO"` and `"CollectionWrite"`. 4. Verify a pure-IO method (only `File.WriteAllText`) returns `MutationScopes = ["IO"]` only (no spurious CollectionWrite). 5. Verify a pure-CollectionWrite method (only `_sessions.TryAdd`) returns `MutationScopes = ["CollectionWrite"]` only (no IO). |
| Performance review | N/A — correctness fix, no hot-path changes. The change from single highest-severity to multi-scope accumulation adds at most 2 extra boolean checks per mutating member candidate; negligible. |
| CHANGELOG category | Changed — BREAKING |
| CHANGELOG entry (draft) | `find_type_mutations`: `MutatingMemberDto.MutationScope` (string, single highest-severity scope) is replaced by `MutationScopes` (`string[]`, all detected scopes). Methods performing compound mutations — e.g. both IO and CollectionWrite — now report every applicable scope rather than only the highest severity. Callers must update from `member.MutationScope == "IO"` to `member.MutationScopes.Contains("IO")`. Fixes gh #741. |
| Backlog sync | Close rows: [`find-type-mutations-single-scope-misses-compound-io`]. |

### 4. analyze-control-flow-partial-slice-warning-on-full-method

| Field | Content |
|---|---|
| Status | pending |
| Backlog rows closed | `analyze-control-flow-partial-slice-warning-on-full-method` |
| Diagnosis | Root cause is in `src/RoslynMcp.Roslyn/Services/FlowAnalysisService.cs` at lines 117–121. The warning guard reads: `if (!result.Succeeded \|\| (entryPoints.Count == 0 && exitPoints.Count == 0 && returnStatements.Count == 0))`. For a complete void method body (e.g. `public void Foo() { int x = 1; }`) whose range is passed in full, Roslyn returns `Succeeded = true` with zero entry, exit, and return points — a structurally correct result for a void block with no explicit control-flow exits. The second branch of the OR fires and emits "Control-flow results may be incomplete for this line range. Prefer a range that covers full statement blocks within a single method body (not a partial slice of a method)." — but the analysis succeeded and is not incomplete. The backlog description is accurate: the misleading text is the partial-slice branch firing on a succeeded zero-count result. Confirmed live: `ControlFlowAnalysis.Succeeded` is confirmed live in `ControlFlowAnalysisDto` (symbol resolved at `src/RoslynMcp.Core/Models/ControlFlowAnalysisDto.cs:6`); `ResolveAnalysisRegionAsync` confirmed at `FlowAnalysisService.cs:153`. No anchor staleness. |
| Approach | Modify `AnalyzeControlFlowAsync` in `src/RoslynMcp.Roslyn/Services/FlowAnalysisService.cs` lines 116–128. Split the unified `if` into two distinct cases: (1) `!result.Succeeded` — emit the partial-slice warning (analysis genuinely failed); (2) `result.Succeeded && entryPoints.Count == 0 && exitPoints.Count == 0 && returnStatements.Count == 0` — suppress the warning entirely (the analysis succeeded; zero counts are valid for a void complete-body block with no explicit control-flow exits). The `EndPointIsReachable` branch at lines 123–129 is unchanged. No DTO changes, no interface changes, no callers to update. |
| Scope | Production files: 1 — `src/RoslynMcp.Roslyn/Services/FlowAnalysisService.cs`. Test files: 1 (extend existing) — `tests/RoslynMcp.Tests/FlowAnalysisServiceTests.cs` (add 1 new `[TestMethod]` covering the repro from gh #743: a void method body whose complete range previously triggered the spurious warning; assert `Warning == null` post-fix). No new files created. Rules 3 and 4 satisfied: 1 production file (≤ 4), 1 test extension (≤ 3). |
| Tool policy | edit-only |
| Estimated context cost | 28000 |
| Risks | The second warning branch (`Succeeded && all-counts-zero`) is currently used by any legitimate zero-count partial slice that happened to succeed. Verify that the suppression is safe: a partial slice that covers only declarations (e.g. `int x; int y;`) will also yield zero entry/exit/return AND `Succeeded=true`. The fix should confirm the condition is truly benign for that case — Roslyn still returns correct data (zero control-flow constructs), and suppressing the warning is accurate. Adjacent behavior to verify: the `!result.Succeeded` branch still emits the warning for genuinely failed analyses (multi-block spans that the block-group fallback couldn't resolve). The `ExpressionBody` synthesis path (lines 66–82) is unaffected. The `EndPointIsReachable` warning branch (lines 123–128) is unaffected. |
| Validation | 1. Add `[TestMethod] AnalyzeControlFlow_FullMethodRange_NoSpuriousPartialSliceWarning` to `FlowAnalysisServiceTests.cs`: write a void method fixture (e.g. `public void VoidMethod() { int x = 1; }`) into the copied solution file; call `AnalyzeControlFlowAsync` with startLine=signature_line, endLine=closing_brace_line; assert `result.Succeeded == true`; assert `result.Warning == null`. 2. Verify existing tests still pass (`AnalyzeControlFlow_StatementBodiedMethod_StillUsesStatementPath`, `AnalyzeControlFlow_ExpressionBodiedMethod_ReturnsSyntheticImplicitReturn`). 3. `mcp__roslyn__compile_check` after edit. 4. `mcp__roslyn__test_run --filter FlowAnalysisServiceTests`. |
| Performance review | N/A — correctness fix, no hot-path changes. |
| CHANGELOG category | Fixed |
| CHANGELOG entry (draft) | Fixed `analyze_control_flow` spuriously emitting a "partial-slice" warning when the supplied line range covers an entire method body and the analysis succeeds with zero explicit control-flow exits (void method or complete block with no returns). The warning is now suppressed when `Succeeded = true` and entry/exit/return counts are all zero. Fixes gh #743. |
| Backlog sync | Close rows: [`analyze-control-flow-partial-slice-warning-on-full-method`]. |

### 5. format-document-preview-empty-diff-instead-of-noop

| Field | Content |
|---|---|
| Status | pending |
| Backlog rows closed | `format-document-preview-empty-diff-instead-of-noop` |
| Diagnosis | Root cause is in `src/RoslynMcp.Roslyn/Helpers/DiffGenerator.cs:38-40`. `GenerateUnifiedDiff` unconditionally emits `--- a/{filePath}` and `+++ b/{filePath}` header lines before iterating hunks. When all diff lines are `Unchanged` (the Roslyn formatter made no net edits), `hunksWritten` stays 0 and the method returns a string containing only the two header lines — not `string.Empty`. The call chain is: `RefactoringService.PreviewFormatDocumentAsync` (at `src/RoslynMcp.Roslyn/Services/RefactoringService.cs:450`) calls `SolutionDiffHelper.ComputeChangesAsync` which calls `DiffGenerator.GenerateUnifiedDiff`. `SolutionDiffHelper` guards against `oldText == newText` at line 71, but a Roslyn `WithDocumentText` on an already-canonical document can produce a "changed" document where the extracted strings are equal yet the guard is not reached (different SourceText instances). In that case `GenerateUnifiedDiff` is called and returns a header-only string, which becomes a `FileChangeDto` entry with a non-empty `UnifiedDiff` containing no `@@` hunks. Callers that check `changes.length > 0` misread the no-op as pending changes. **Stale anchor:** the backlog cited `src/RoslynMcp.Roslyn/Services/FormatService.cs` — that file does not exist; the actual implementation is `src/RoslynMcp.Roslyn/Services/RefactoringService.cs:450`. |
| Approach | Two-file surgical fix. (1) In `src/RoslynMcp.Roslyn/Helpers/DiffGenerator.cs`, add an early-return at the end of `GenerateUnifiedDiff`: when `hunksWritten == 0 && !truncated`, return `string.Empty` instead of the header-only string. The docstring already says "or an empty string if there are no differences" (line 32) — the implementation does not honour this contract in the Unchanged-only case. (2) In `src/RoslynMcp.Roslyn/Helpers/SolutionDiffHelper.cs`, add a one-line belt-and-suspenders filter in the `TryAdd` lambda or after calling `DiffGenerator.GenerateUnifiedDiff`: skip any entry where the generated diff string is `string.Empty`. This mirrors the existing `if (oldText == newText) { continue; }` guard at line 71 and is defensive against any future caller of `DiffGenerator` that similarly forgets to check for empty returns. No changes to `RefactoringService`, `RefactoringTools`, or `RefactoringPreviewDto` are required — the fix is entirely within the diff-computation helpers. The `noOpResult: true` envelope option (backlog alternative B) is not pursued; filtering at the diff level is cleaner and consistent with how the helper is documented. |
| Scope | Production files touched (2): `src/RoslynMcp.Roslyn/Helpers/DiffGenerator.cs`, `src/RoslynMcp.Roslyn/Helpers/SolutionDiffHelper.cs`. Test files modified (1): `tests/RoslynMcp.Tests/DiffGeneratorTests.cs` — add two new test methods: (a) `GenerateUnifiedDiff_IdenticalContent_ReturnsEmptyString` asserting `GenerateUnifiedDiff(text, text, "file.cs") == string.Empty`, (b) `GenerateUnifiedDiff_NoOpFormat_ProducesNoChangesEntry` covering the gh #739 repro scenario (Roslyn-formatted identical text → `changes` count is 0). No Rule 3 exemption needed (2 production files, well within cap). |
| Tool policy | edit-only |
| Estimated context cost | 28000 |
| Risks | (1) `DiffGenerator.GenerateUnifiedDiff` returning `string.Empty` changes a publicly visible contract (`public static` method); callers outside this repo that relied on non-empty output for header-only diffs would be affected — however the docstring explicitly states `string.Empty` is the intended return for no-differences, so this is a contract correction, not a break. (2) The `SolutionDiffHelper` filter is belt-and-suspenders; confirm it does not accidentally suppress the truncation-sentinel entries (`TruncatedSentinelFilePath`), which intentionally have non-hunk content. The truncation sentinel's `UnifiedDiff` starts with `# FLAG-6A:` — not headers — and is added only when `truncatedFiles.Count > 0`, so it will never be an empty string and is safe. (3) The existing `IdenticalText_ReturnsEmptyList` test in `SolutionDiffHelperTests.cs:81` already covers the same-SourceText-identity path; verify the new tests cover the distinct-SourceText-identity path that the existing test misses. (4) Fanout probe skipped — surgical edit, no cross-cutting symbol changes. |
| Validation | (1) `dotnet test --filter "FullyQualifiedName~DiffGeneratorTests"` — new tests must pass. (2) `dotnet test --filter "FullyQualifiedName~SolutionDiffHelperTests"` — existing `IdenticalText_ReturnsEmptyList` and `NoChanges_ReturnsEmptyList` must continue to pass. (3) `dotnet test --filter "FullyQualifiedName~RefactoringToolsIntegrationTests.FormatDocument_Preview_Returns_PreviewToken"` — must still return a valid `previewToken` for a file that has actual formatting changes (regression guard). (4) Manual repro: call `format_document_preview` on an already-formatted file; assert the JSON response has `changes: []` (empty array) rather than `changes: [{filePath: "...", unifiedDiff: "--- a/...\n+++ b/..."}]`. (5) `mcp__roslyn__compile_check` after edits — both changed files must produce zero errors. |
| Performance review | N/A — correctness fix, no hot-path changes. The `string.Empty` early-return in `DiffGenerator` slightly reduces allocations on no-op paths. |
| CHANGELOG category | Fixed |
| CHANGELOG entry (draft) | `format_document_preview` now returns `changes: []` for a no-op format pass (already-formatted document). Previously `DiffGenerator` emitted a header-only unified diff string (`--- a/…` / `+++ b/…` with no `@@` hunks) when the formatted document text was identical to the original, causing callers that check `changes.length > 0` to misidentify the result as pending changes. Fixes gh #739. |
| Backlog sync | Close rows: [`format-document-preview-empty-diff-instead-of-noop`]. |

### 6. callers-callees-previewtext-asymmetry

| Field | Content |
|---|---|
| Status | pending |
| Backlog rows closed | `callers-callees-previewtext-asymmetry` |
| Diagnosis | **Stale anchor:** the backlog cited `src/RoslynMcp.Roslyn/Services/CallersCalleesService.cs` — that file does not exist in the current tree; logic was consolidated into `src/RoslynMcp.Roslyn/Services/SymbolRelationshipService.cs`. The asymmetry still reproduces. In `SymbolRelationshipService.cs`, `CollectCallersAsync` (line 320–322) acquires preview text via `SymbolResolver.GetPreviewTextAsync(refLocation.Document, refLocation.Location, ct)` and passes it as the third argument to `SymbolMapper.ToLocationDto`. `CollectCalleesAsync` (line 358) calls `SymbolMapper.ToLocationDto(invokedLoc, invokedSymbol)` with no `previewText` argument, so `PreviewText` is always `null` on every callee entry. `ToLocationDto` at `src/RoslynMcp.Roslyn/Helpers/SymbolMapper.cs:123` accepts an optional `string? previewText = null` parameter — the omission is the sole cause of the null output. The `Document` needed to call `GetPreviewTextAsync` is already available in the callee loop: the outer `doc` covers the `invocation.GetLocation()` fallback path; for the `invokedSymbol.Locations.FirstOrDefault(l => l.IsInSource)` path, the document is retrieved via `solution.GetDocument(invokedLoc.SourceTree)`. |
| Approach | 1. In `src/RoslynMcp.Roslyn/Services/SymbolRelationshipService.cs`, inside `CollectCalleesAsync` (line 347–358), after resolving `invokedLoc`, determine the correct `Document`: if `invokedLoc` is a source location (i.e., not the `invocation.GetLocation()` fallback), look up `solution.GetDocument(invokedLoc.SourceTree)`; otherwise use the iteration's `doc`. 2. `await SymbolResolver.GetPreviewTextAsync(calleeDoc, invokedLoc, ct)` to obtain `previewText`. 3. Pass `previewText` as the third argument to `SymbolMapper.ToLocationDto(invokedLoc, invokedSymbol, previewText)`. No changes required to `SymbolMapper.cs` or the tool wrapper (`AnalysisTools.cs`) — `ToLocationDto` already accepts and propagates the optional parameter. |
| Scope | Production files touched: 1 — `src/RoslynMcp.Roslyn/Services/SymbolRelationshipService.cs`. Test files modified: 1 — extend `tests/RoslynMcp.Tests/ServiceCoverageTests.cs` with a test method asserting `result.Callees` are all non-null `PreviewText` (mirrors the existing `GetCallersCallees_Finds_Speak_Callers` fixture at line 161). Rule 3 satisfied (1 production file, well under the 4-file cap). Rule 4 satisfied (0 new test files; 1 existing file extended). |
| Tool policy | edit-only |
| Estimated context cost | 28000 |
| Risks | (1) `invokedLoc` can be a metadata (non-source) location when the callee is an external library method — in that case `GetPreviewTextAsync` will receive a null `SourceTree`, and the helper already guards against invalid `lineSpan` (returns `null`). The preview field will stay null for external-symbol callees, which is the correct behavior. (2) The `invocation.GetLocation()` fallback location lives in the same `doc` as the outer loop iteration; using `doc` directly in that branch is safe. (3) Existing test `Callers_Callees_For_MakeThemSpeak` in `IntegrationTests.SymbolNavigation.cs` checks `Callees.Count > 0` but does not assert `PreviewText`; it will continue to pass. The new test exercises the regression scenario from gh #742. Stale anchor note: the backlog cited `src/RoslynMcp.Roslyn/Services/CallersCalleesService.cs` which does not exist; actual file is `src/RoslynMcp.Roslyn/Services/SymbolRelationshipService.cs`. |
| Validation | 1. Run `mcp__roslyn__compile_check` against `RoslynMcp.slnx` after the edit — expect zero new errors. 2. Run the new test method in `ServiceCoverageTests.cs` (assert all callee entries for `MakeThemSpeak` have non-null `PreviewText`). 3. Run the existing `Callers_Callees_For_MakeThemSpeak` test to confirm no regression. 4. Manually invoke `callers_callees` on a method with in-source callees (e.g., `AnimalService.MakeThemSpeak`) and confirm every item in the `callees` array includes a non-empty `previewText` field in the JSON response. |
| Performance review | N/A — correctness fix, no hot-path changes. The added `GetPreviewTextAsync` calls are identical in cost to those already made for callers in the same service method. |
| CHANGELOG category | Fixed |
| CHANGELOG entry (draft) | Fixed `callers_callees` returning `null` `previewText` on all callee entries while populating it correctly for callers; callees now use the same per-location source-extract path as callers. Fixes gh #742. |
| Backlog sync | Close rows: [`callers-callees-previewtext-asymmetry`]. |

## Conflict graph

**No production-file overlaps across the 6 initiatives.** All 6 are zero-degree (safely parallelizable).

| # | id | Production files |
|---|----|------------------|
| 1 | workspace-changes-atomic-batch-split-without-batchid | `src/RoslynMcp.Roslyn/Services/EditService.cs` |
| 2 | validate-workspace-changetracker-no-disk-reconcile-after-git-checkout | `src/RoslynMcp.Roslyn/Services/WorkspaceValidationService.cs` |
| 3 | find-type-mutations-single-scope-misses-compound-io | `src/RoslynMcp.Core/Models/TypeMutationDto.cs`, `src/RoslynMcp.Roslyn/Services/MutationAnalysisService.cs` |
| 4 | analyze-control-flow-partial-slice-warning-on-full-method | `src/RoslynMcp.Roslyn/Services/FlowAnalysisService.cs` |
| 5 | format-document-preview-empty-diff-instead-of-noop | `src/RoslynMcp.Roslyn/Helpers/DiffGenerator.cs`, `src/RoslynMcp.Roslyn/Helpers/SolutionDiffHelper.cs` |
| 6 | callers-callees-previewtext-asymmetry | `src/RoslynMcp.Roslyn/Services/SymbolRelationshipService.cs` |

Hotspot check: none of the 6 touch `ServerSurfaceCatalog.cs`, `ServiceCollectionExtensions.cs`, or `WorkspaceManager.cs`. The wave rule (≤1 hotspot per wave) does not bind.

Edges: 0. Zero-degree initiatives: [1, 2, 3, 4, 5, 6].

## Review

**Cycle 0 outcome: passed-with-warnings** (0 block, 1 warn, 6 info). See `review-cycle-0.md` (or `review.md`) for the full reviewer artifact.

Key signal:
- **Warn (Rule 5b):** Initiative 3 (`find-type-mutations-single-scope-misses-compound-io`) skipped the fanout probe on a BREAKING DTO-shape change (`MutationScope` → `MutationScopes`). Real-world fanout is bounded (~1 test consumer per reviewer spot check). Executor should re-verify consumer surface before applying.
- **Info × 4:** Anchor-stale findings on initiatives 1, 3, 5, 6 — all already self-acknowledged by the plan stanzas.
- **Info × 3:** Counting-convention inconsistency where `testFilesAdded: 0` in state.json paired with "Test files modified: 1" in Scope (initiatives 3, 5, 6). Rule 4 cap not threatened.

No remediation cycle required (`block: 0`).
