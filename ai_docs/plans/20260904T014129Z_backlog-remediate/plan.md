# Backlog sweep plan — 20260904T014129Z_backlog-remediate

Generated by `/backlog-remediate`. 25 initiative(s) selected off `ai_docs/backlog.md` (snapshot 2026-09-04T00:30:35Z).

<!-- BSWEEP:STATUS-TABLE BEGIN — generated from state.json; do not edit by hand -->
## Status (generated)

| # | id | status | PR | rows closed |
|---|----|--------|----|-------------|
| 1 | collector-diagnostics-bypass-error-formatter | pending | — | collector-diagnostics-bypass-error-formatter |
| 2 | actionlint-chmod-failure-diagnostic | pending | — | actionlint-chmod-failure-diagnostic |
| 3 | actionlint-unsupported-platform-contract | pending | — | actionlint-unsupported-platform-contract |
| 4 | actionlint-unpinned-rid-contract | pending | — | actionlint-unpinned-rid-contract |
| 5 | actionlint-extraction-failure-contract | pending | — | actionlint-extraction-failure-contract |
| 6 | actionlint-missing-binary-contract | pending | — | actionlint-missing-binary-contract |
| 7 | scaffold-target-type-ambiguity-before-sampling | pending | — | scaffold-target-type-ambiguity-before-sampling |
| 8 | test-service-container-duplicate-service-instances | pending | — | test-service-container-duplicate-service-instances |
| 9 | interface-extraction-catch-chain-dead-rethrow | pending | — | interface-extraction-catch-chain-dead-rethrow |
| 10 | sibling-test-name-discovery-warning | pending | — | sibling-test-name-discovery-warning |
| 11 | tool-consolidation-policy-foundation | pending | — | tool-consolidation-policy-foundation |
| 12 | desc-budget-harness-param-family | pending | — | desc-budget-harness-param-family |
| 13 | workspace-id-optional-adoption-evidence | pending | — | workspace-id-optional-adoption-evidence |
| 14 | workspace-id-named-args-validation | pending | — | workspace-id-named-args-validation |
| 15 | tasks-extension-compatibility-decision | pending | — | tasks-extension-compatibility-decision |
| 16 | structured-tool-dispatch-adapter | pending | — | structured-tool-dispatch-adapter |
| 17 | workspace-auto-load-on-demand-design | pending | — | workspace-auto-load-on-demand-design |
| 18 | refactoring-format-range-preview-decomposition | pending | — | refactoring-format-range-preview-decomposition |
| 19 | test-service-container-two-phase-construction-cycle | pending | — | test-service-container-two-phase-construction-cycle |
| 20 | test-assembly-fixture-disposal-observable-postcondition | pending | — | test-assembly-fixture-disposal-observable-postcondition |
| 21 | formatter-baseline-generator-double-restore-cost | pending | — | formatter-baseline-generator-double-restore-cost |
| 22 | desc-budget-harness-method-adopt-wave-2 | pending | — | desc-budget-harness-method-adopt-wave-2 |
| 23 | desc-budget-harness-method-adopt-wave-3 | pending | — | desc-budget-harness-method-adopt-wave-3 |
| 24 | method-diet-undo-workspace-lifecycle | pending | — | method-diet-undo-workspace-lifecycle |
| 25 | method-diet-fixall-bulk-operations | pending | — | method-diet-fixall-bulk-operations |
<!-- BSWEEP:STATUS-TABLE END -->

## Initiatives

### 1. collector-diagnostics-bypass-error-formatter

| Field | Content |
|---|---|
| Route | direct |
| Diagnosis | `eng/collect-hosted-shard-timings.ps1` and `eng/summarize-test-results.ps1` allow uncaught throws to pass through PowerShell's width-dependent formatter, so fail-closed operator diagnostics are multi-line and can echo source text. |
| Approach | A script-scope `trap` writes `$_.Exception.Message` to stderr and exits 1, so no ConciseView source echo, squiggle, or console-width-dependent wrap appears in the output.<br>`TestResultsSummaryContractTests` asserts the exact one-line diagnostic without applying `NormalizeConsoleDiagnostic` to the expected side.<br>Whether `eng/summarize-test-results.ps1` adopts the same shape is decided explicitly, not left implicit. |
| Scope | Production: 2 (`eng/collect-hosted-shard-timings.ps1`, `eng/summarize-test-results.ps1`). Tests: 1 (`tests/RoslynMcp.Tests/TestResultsSummaryContractTests.cs`). Deletions: none. |
| Tool policy | edit-only |
| Estimated context cost | 40000 |
| Risks | Preserve successful stdout reports and cancellation/exit codes; exact stderr assertions must not become path- or console-width-sensitive. No refactor-shaped fanout. |
| Validation | Focused `TestResultsSummaryContractTests`; direct negative-path script probes; `./eng/verify-release.ps1 -Configuration Release`. |
| Performance review | N/A — correctness fix, no hot-path changes. |
| CHANGELOG category | Fixed |
| CHANGELOG entry (draft) | Emit deterministic one-line shard collector diagnostics on stderr. |
| Backlog sync | Close rows: [collector-diagnostics-bypass-error-formatter]. |

### 2. actionlint-chmod-failure-diagnostic

| Field | Content |
|---|---|
| Route | split-child of verify-actionlint-chmod-and-throw-branch-coverage |
| Diagnosis | `eng/verify-actionlint.ps1` contains the unverified fail-closed branch named by this child; the parent combined five distinct branch outcomes. |
| Approach | Check `$LASTEXITCODE` immediately after chmod and throw the bounded executable-marking diagnostic; add the exact contract regression. |
| Scope | Production: 1 (`eng/verify-actionlint.ps1`). Tests: 1 (`tests/RoslynMcp.Tests/ActionlintGateContractTests.cs`). Deletions: none. |
| Tool policy | edit-only |
| Estimated context cost | 25000 |
| Risks | Keep the tests hermetic and offline; do not weaken checksum or RID pinning. No refactor-shaped fanout. |
| Validation | Focused `ActionlintGateContractTests`; `pwsh -NoProfile -File ./eng/verify-actionlint.ps1`; `./eng/verify-release.ps1 -Configuration Release`. |
| Performance review | N/A — correctness fix, no hot-path changes. |
| CHANGELOG category | Fixed |
| CHANGELOG entry (draft) | Report actionlint executable permission failures precisely. |
| Backlog sync | Close rows: [actionlint-chmod-failure-diagnostic]. |

### 3. actionlint-unsupported-platform-contract

| Field | Content |
|---|---|
| Route | split-child of verify-actionlint-chmod-and-throw-branch-coverage |
| Diagnosis | `eng/verify-actionlint.ps1` contains the unverified fail-closed branch named by this child; the parent combined five distinct branch outcomes. |
| Approach | Add a hidden `-PlatformForTest` parameter with a closed `windows`/`macos`/`linux`/`unsupported` value set and no default override. Route only explicitly bound test values through `Get-CurrentRid`; keep ordinary invocations on the runtime OS probe. Run the script with `-PlatformForTest unsupported` in the hermetic contract harness and assert the exact bounded diagnostic before any network or filesystem mutation. |
| Scope | Production: 1 (`eng/verify-actionlint.ps1`). Tests: 1 (`tests/RoslynMcp.Tests/ActionlintGateContractTests.cs`). Deletions: none. |
| Tool policy | edit-only |
| Estimated context cost | 25000 |
| Risks | Keep the tests hermetic and offline; do not weaken checksum or RID pinning. No refactor-shaped fanout. |
| Validation | Focused `ActionlintGateContractTests`; `pwsh -NoProfile -File ./eng/verify-actionlint.ps1`; `./eng/verify-release.ps1 -Configuration Release`. |
| Performance review | N/A — correctness fix, no hot-path changes. |
| CHANGELOG category | Maintenance |
| CHANGELOG entry (draft) | Lock the unsupported-platform actionlint gate contract. |
| Backlog sync | Close rows: [actionlint-unsupported-platform-contract]. |

### 4. actionlint-unpinned-rid-contract

| Field | Content |
|---|---|
| Route | split-child of verify-actionlint-chmod-and-throw-branch-coverage |
| Diagnosis | `eng/verify-actionlint.ps1` contains the unverified fail-closed branch named by this child; the parent combined five distinct branch outcomes. |
| Approach | Drive the existing unpinned-RID refusal through `PwshScriptRunner` and assert its exact no-network failure. |
| Scope | Production: 1 (`eng/verify-actionlint.ps1`). Tests: 1 (`tests/RoslynMcp.Tests/ActionlintGateContractTests.cs`). Deletions: none. |
| Tool policy | edit-only |
| Estimated context cost | 25000 |
| Risks | Keep the tests hermetic and offline; do not weaken checksum or RID pinning. No refactor-shaped fanout. |
| Validation | Focused `ActionlintGateContractTests`; `pwsh -NoProfile -File ./eng/verify-actionlint.ps1`; `./eng/verify-release.ps1 -Configuration Release`. |
| Performance review | N/A — correctness fix, no hot-path changes. |
| CHANGELOG category | Maintenance |
| CHANGELOG entry (draft) | Lock the unpinned-RID actionlint gate contract. |
| Backlog sync | Close rows: [actionlint-unpinned-rid-contract]. |

### 5. actionlint-extraction-failure-contract

| Field | Content |
|---|---|
| Route | split-child of verify-actionlint-chmod-and-throw-branch-coverage |
| Diagnosis | `eng/verify-actionlint.ps1` contains the unverified fail-closed branch named by this child; the parent combined five distinct branch outcomes. |
| Approach | Inject a failing tar extraction command through the existing test seam and assert the bounded failure before execution. |
| Scope | Production: 1 (`eng/verify-actionlint.ps1`). Tests: 1 (`tests/RoslynMcp.Tests/ActionlintGateContractTests.cs`). Deletions: none. |
| Tool policy | edit-only |
| Estimated context cost | 25000 |
| Risks | Keep the tests hermetic and offline; do not weaken checksum or RID pinning. No refactor-shaped fanout. |
| Validation | Focused `ActionlintGateContractTests`; `pwsh -NoProfile -File ./eng/verify-actionlint.ps1`; `./eng/verify-release.ps1 -Configuration Release`. |
| Performance review | N/A — correctness fix, no hot-path changes. |
| CHANGELOG category | Maintenance |
| CHANGELOG entry (draft) | Lock the actionlint archive-extraction failure contract. |
| Backlog sync | Close rows: [actionlint-extraction-failure-contract]. |

### 6. actionlint-missing-binary-contract

| Field | Content |
|---|---|
| Route | split-child of verify-actionlint-chmod-and-throw-branch-coverage |
| Diagnosis | `eng/verify-actionlint.ps1` contains the unverified fail-closed branch named by this child; the parent combined five distinct branch outcomes. |
| Approach | Provide a successful extraction fixture without the expected binary and assert the bounded post-extraction refusal. |
| Scope | Production: 1 (`eng/verify-actionlint.ps1`). Tests: 1 (`tests/RoslynMcp.Tests/ActionlintGateContractTests.cs`). Deletions: none. |
| Tool policy | edit-only |
| Estimated context cost | 25000 |
| Risks | Keep the tests hermetic and offline; do not weaken checksum or RID pinning. No refactor-shaped fanout. |
| Validation | Focused `ActionlintGateContractTests`; `pwsh -NoProfile -File ./eng/verify-actionlint.ps1`; `./eng/verify-release.ps1 -Configuration Release`. |
| Performance review | N/A — correctness fix, no hot-path changes. |
| CHANGELOG category | Maintenance |
| CHANGELOG entry (draft) | Lock the actionlint missing-binary failure contract. |
| Backlog sync | Close rows: [actionlint-missing-binary-contract]. |

### 7. scaffold-target-type-ambiguity-before-sampling

| Field | Content |
|---|---|
| Route | direct |
| Diagnosis | `src/RoslynMcp.Roslyn/Services/SingleTestScaffolder.cs:64-74` samples before semantic target resolution; a known-ambiguous type therefore triggers a client round trip before the deterministic ambiguity refusal. |
| Approach | A `scaffold_test_preview` whose `targetTypeName` matches types in multiple namespaces returns the ambiguity error without any sampling request reaching the client.<br>A regression asserts the provider's `SuggestTestNameAsync` call count is 0 for the ambiguous-target case. |
| Scope | Production: 1 (`src/RoslynMcp.Roslyn/Services/SingleTestScaffolder.cs`). Tests: 1 (`tests/RoslynMcp.Tests/SamplingMrtrWireTests.cs`). Deletions: none. |
| Tool policy | edit-only |
| Estimated context cost | 35000 |
| Risks | Keep the successful MRTR sampling hoist intact; the preflight must be cheap and must not duplicate full compilation work. No refactor-shaped fanout. |
| Validation | Focused `SamplingMrtrWireTests`; Roslyn `compile_check`; `./eng/verify-release.ps1 -Configuration Release`. |
| Performance review | Review the new ambiguity preflight to ensure it removes a guaranteed network round trip without adding a second full semantic resolution. |
| CHANGELOG category | Fixed |
| CHANGELOG entry (draft) | Refuse ambiguous scaffold targets before requesting sampled test names. |
| Backlog sync | Close rows: [scaffold-target-type-ambiguity-before-sampling]. |

### 8. test-service-container-duplicate-service-instances

| Field | Content |
|---|---|
| Route | direct |
| Diagnosis | `tests/RoslynMcp.Tests/TestInfrastructure/TestServiceContainer.cs:295-299` constructs fresh metrics, cohesion, and unused-code collaborators instead of the instances exposed at `:199-203`, so the fixture graph diverges from production singleton DI. |
| Approach | `RefactoringSuggestionService` is constructed from the container's own `CodeMetricsService`, `CohesionAnalysisService` and `UnusedCodeAnalyzer` instances, not fresh ones.<br>A regression asserts reference identity between what the container exposes and what `RefactoringSuggestionService` was given. |
| Scope | Production: 0. Tests: 1 (`tests/RoslynMcp.Tests/TestInfrastructure/TestServiceContainer.cs`). Deletions: none. |
| Tool policy | edit-only |
| Estimated context cost | 25000 |
| Risks | Avoid a new construction-order dependency; use explicit locals before the final initializer. No refactor-shaped fanout. |
| Validation | Focused fixture/container tests; Roslyn `compile_check`; `./eng/verify-release.ps1 -Configuration Release`. |
| Performance review | N/A — test fixture correctness fix. |
| CHANGELOG category | Maintenance |
| CHANGELOG entry (draft) | Align the test service container's suggestion collaborators with production DI identity. |
| Backlog sync | Close rows: [test-service-container-duplicate-service-instances]. |

### 9. interface-extraction-catch-chain-dead-rethrow

| Field | Content |
|---|---|
| Route | direct |
| Diagnosis | `src/RoslynMcp.Roslyn/Services/InterfaceExtractionService.cs:411-423` rethrows `InvalidOperationException` immediately before an IO-only exception filter that can never catch it, leaving a dead catch and no regression for unexpected cache failures. |
| Approach | Remove the now-dead `catch (InvalidOperationException) { throw; }` at `InterfaceExtractionService.cs:415` — verify the conflict-throw test still passes, proving the deliberate `InvalidOperationException` propagates past the narrowed filter unaided.<br>Add a test that injects an `ICompilationCache` whose `GetCompilationAsync` throws a non-IO exception (e.g. `KeyNotFoundException`) and asserts `PreviewExtractInterfaceAsync` propagates it rather than skipping the conflict check, so the narrowing cannot silently regress to bare `Exception`. |
| Scope | Production: 1 (`src/RoslynMcp.Roslyn/Services/InterfaceExtractionService.cs`). Tests: 1 (`tests/RoslynMcp.Tests/ExtractInterfaceSemanticUsingsTests.cs`). Deletions: none. |
| Tool policy | edit-only |
| Estimated context cost | 30000 |
| Risks | Preserve the narrow IOException/UnauthorizedAccessException degradation and cancellation propagation. No refactor-shaped fanout. |
| Validation | Focused `ExtractInterfaceSemanticUsingsTests`; Roslyn `compile_check`; `./eng/verify-release.ps1 -Configuration Release`. |
| Performance review | N/A — correctness fix, no hot-path changes. |
| CHANGELOG category | Fixed |
| CHANGELOG entry (draft) | Remove a dead interface-extraction catch and lock unexpected cache failure propagation. |
| Backlog sync | Close rows: [interface-extraction-catch-chain-dead-rethrow]. |

### 10. sibling-test-name-discovery-warning

| Field | Content |
|---|---|
| Route | direct |
| Diagnosis | `src/RoslynMcp.Roslyn/Services/SingleTestScaffolder.cs:352-375` returns only names; its per-file IO/authorization catch silently omits unreadable siblings, hiding collision risk from the sampled-name result. |
| Approach | Return collected names plus one stable incomplete-discovery warning when a sibling test file cannot be read for an expected IO/authorization reason; do not silently `continue`.<br>Append that warning to the sampling result while retaining usable names and the deterministic placeholder fallback.<br>Exclude exception type, raw message, full path, and secret-bearing values by reusing the scaffolding warning policy; unexpected exceptions and cancellation are not swallowed.<br>One deterministic per-file read-failure regression proves the warning is emitted once and a successful sibling file still contributes its method names. |
| Scope | Production: 1 (`src/RoslynMcp.Roslyn/Services/SingleTestScaffolder.cs`). Tests: 1 (`tests/RoslynMcp.Tests/ScaffoldingIntegrationTests.cs`). Deletions: none. |
| Tool policy | edit-only |
| Estimated context cost | 40000 |
| Risks | Keep diagnostics redacted and bounded while retaining partial usable results; never catch cancellation or unexpected failures. No refactor-shaped fanout. |
| Validation | Focused `ScaffoldingIntegrationTests`; Roslyn `compile_check`; `./eng/verify-release.ps1 -Configuration Release`. |
| Performance review | N/A — correctness fix, no hot-path changes. |
| CHANGELOG category | Fixed |
| CHANGELOG entry (draft) | Warn when sibling test-name discovery is incomplete without leaking file details. |
| Backlog sync | Close rows: [sibling-test-name-discovery-warning]. |

### 11. tool-consolidation-policy-foundation

| Field | Content |
|---|---|
| Route | split-child of tool-consolidation-adr-and-alias-machinery |
| Diagnosis | The repository has no recorded risk-bucket consolidation policy. `src/RoslynMcp.Host.Stdio/Tools/ToolAliasDeprecation.cs:3-23` only formats response notices, and `src/RoslynMcp.Host.Stdio/Catalog/ServerSurfaceCatalog.cs:453-489` has no alias lifecycle. The original row's 27-file live fanout exceeds its M boundary, so this child isolates the decision prerequisite. |
| Approach | Record formatting, text-edit, code-transform, file-lifecycle, and project-file risk buckets; prohibit cross-bucket apply merges. Require deprecated aliases to remain callable for at least one minor release and permit removal only at the next major. Link ADR 0009 from the release policy and add the published-surface migration-policy changelog fragment. Extend AI-doc validation to prove the decision and cross-reference remain valid. |
| Scope | Production files: 3 — `docs/decisions/0009-tool-surface-policy.md`, `docs/release-policy.md`, `eng/verify-ai-docs.ps1`. Test files: 0. Deletions: 0. |
| Tool policy | edit-only |
| Estimated context cost | 20000 |
| Risks | This public-surface policy must not imply that aliases can disappear in a minor release. It must prohibit cross-risk-bucket merges without blocking consolidation within one bucket. |
| Validation | Run `pwsh -NoProfile -File ./eng/verify-ai-docs.ps1`, then `pwsh -NoProfile -File ./eng/verify-release.ps1 -Configuration Release`. Verify the ADR and release-policy links resolve. |
| Performance review | N/A — documentation decision, no hot-path changes. |
| CHANGELOG category | Maintenance |
| CHANGELOG entry (draft) | Documented risk-bounded tool consolidation and the public deprecated-alias lifecycle. |
| Backlog sync | Close rows: [tool-consolidation-policy-foundation]. |

### 12. desc-budget-harness-param-family

| Field | Content |
|---|---|
| Route | direct |
| Diagnosis | `tests/RoslynMcp.Tests/ParameterDescriptionCanonicalizationTests.cs` and `ParamDescriptionCanonicalFormCodeActionSuppressionTests.cs` duplicate parameter reflection enumeration and canonical-string assertions, multiplying boilerplate across ten dependent slices. |
| Approach | One shared helper owns the parameter-level `[Description]` reflection enumeration and the canonical-form assertions; each slice declares only its tool-type set and its canonical-form expectations.<br>The helper asserts exact canonical strings for boilerplate parameters rather than shape, and leaves semantically load-bearing parameter descriptions unconstrained — the cold-review advisory both shipped param slices carried.<br>Both existing param slice classes adopt it with unchanged pass/fail behaviour. |
| Scope | Production: 0. Tests: 3 (`tests/RoslynMcp.Tests/ParameterDescriptionBudgetHarness.cs`, `tests/RoslynMcp.Tests/ParamDescriptionCanonicalFormCodeActionSuppressionTests.cs`, `tests/RoslynMcp.Tests/ParameterDescriptionCanonicalizationTests.cs`). Deletions: none. |
| Tool policy | edit-only |
| Estimated context cost | 35000 |
| Risks | Preserve exact strings and negative-path coverage; do not merge method-description and parameter-description enumeration semantics. No production fanout. |
| Validation | Focused two adopter classes plus new helper self-tests; Roslyn `compile_check`; `./eng/verify-release.ps1 -Configuration Release`. |
| Performance review | N/A — test refactor. |
| CHANGELOG category | Maintenance |
| CHANGELOG entry (draft) | Share exact parameter-description budget assertions across tool slices. |
| Backlog sync | Close rows: [desc-budget-harness-param-family]. |

### 13. workspace-id-optional-adoption-evidence

| Field | Content |
|---|---|
| Route | deepen |
| Diagnosis | The original pilot remains live: `go_to_definition`, `find_references`, and `document_symbols` advertise optional `workspaceId` at `src/RoslynMcp.Host.Stdio/Tools/SymbolTools.cs:194`, `:264`, and `:380`; `compile_check` later joined the optional cohort at `src/RoslynMcp.Host.Stdio/Tools/CompileCheckTools.cs:33`. The middleware distinguishes explicit from omitted resolution at `src/RoslynMcp.Host.Stdio/Middleware/StructuredCallToolFilter.cs:206-237`, but `src/RoslynMcp.Core/Services/AmbientGateMetrics.cs:115-124` retains that signal only for the current request. The existing inventory therefore proves surface availability, not adoption; historical adoption must be derived from real invocation/result records. |
| Approach | Perform a read-only, post-pilot transcript census using the prefix-agnostic and false-positive-resistant extraction pattern in `ai_docs/prompts/roslyn-mcp-multisession-retro.md`: scan Claude Code and both live and archived Codex transcript stores; select actual Roslyn MCP invocation/result records; correlate call/result ids; restrict the primary cohort to the three PR #959 pilot tools; report later optional tools separately; exclude schema dumps, prompt echoes, tests, manual probes, duplicate nested-output copies, and sessions without real calls. Tabulate eligible calls by tool, harness, consumer repository, session, and `_meta.autoResolution` value, with explicit denominator, cutoff, exclusions, and limitations. Add `docs/decisions/0008-workspace-id-optional-adoption.md`, mirroring the Context/Decision/Consequences/Validation shape of `docs/decisions/0006-modelcontextprotocol-2-2-servicing.md`, and index it in `docs/README.md:23-28`. The decision records go only when omission occurs organically outside self-tests/manual probes with no material fast-fail signal; otherwise it records no-go plus the concrete recheck trigger and prevents the eleven dependent flip batches from becoming accidentally actionable. Correct the unsupported “so agents proactively omit it” causality wording in `tests/RoslynMcp.Tests/WorkspaceIdOptionalSurfaceTests.cs:8-10` and `:42-43` to describe schema capability rather than observed behavior. |
| Scope | Production files (2): `docs/decisions/0008-workspace-id-optional-adoption.md` (new), `docs/README.md`. Test files with new or extended methods (0); one existing test file receives comment/assertion-message-only correction: `tests/RoslynMcp.Tests/WorkspaceIdOptionalSurfaceTests.cs`. Deletions: none. Transcript sources are read-only evidence inputs and are not committed. |
| Tool policy | edit-only |
| Estimated context cost | 25000 |
| Risks | Transcript formats differ by harness and may serialize the same result more than once; raw string matching would count tool schemas and prompt text as usage. Current bootstrap guidance still tells clients to retain `workspaceId`, so a no-go may reflect steering rather than middleware utility; the ADR must distinguish those explanations. Raw transcripts may contain sensitive source or paths, so commit aggregate counts and bounded redacted methodology only. On no-go, closing this gate would otherwise make its absent dependency appear satisfied; reconciliation must mark `workspace-id-flip-batch-01` through `workspace-id-flip-batch-11` obsolete or replace their dependency with the recorded recheck trigger. Not refactor-shaped; no source fanout probe applies. |
| Validation | Run the transcript extractor twice and require identical aggregate output; manually verify at least one underlying actual tool-result record for every nonzero resolution bucket and confirm excluded schema/prompt records do not enter the denominator. Verify the ADR totals reconcile across tool, harness, repository, and session dimensions. Run `mcp__roslyn__test_run --filter "WorkspaceIdOptionalSurfaceTests"`, `./eng/verify-ai-docs.ps1`, and the addenda CI-equivalent `./eng/verify-release.ps1 -Configuration Release`. |
| Performance review | N/A — evidence and documentation decision; no hot-path changes. |
| CHANGELOG category | Maintenance |
| CHANGELOG entry (draft) | Measured real-world workspaceId omission adoption and recorded the decision gate for the remaining read-only tool surface. |
| Backlog sync | Close rows: [workspace-id-optional-adoption-evidence]. Mark obsolete: [] on go; on no-go mark obsolete: [workspace-id-flip-batch-01, workspace-id-flip-batch-02, workspace-id-flip-batch-03, workspace-id-flip-batch-04, workspace-id-flip-batch-05, workspace-id-flip-batch-06, workspace-id-flip-batch-07, workspace-id-flip-batch-08, workspace-id-flip-batch-09, workspace-id-flip-batch-10, workspace-id-flip-batch-11]. |

### 14. workspace-id-named-args-validation

| Field | Content |
|---|---|
| Route | split-child of workspace-id-optional-named-argument-prerequisite |
| Diagnosis | The live census found affected positional invocations in `TestRunFailureEnvelopeTests.cs` and `ValidationToolsIntegrationTests.cs`; their tool declarations have required parameters after `workspaceId`. |
| Approach | Convert every affected direct read-only tool invocation in the two files to fully named arguments. Preserve argument expressions, evaluation order, explicit optional values, and assertions. Make no production-signature or runtime-behavior change; require compile check and both targeted test classes to pass. |
| Scope | Production files: 0. Test files: 2 — `tests/RoslynMcp.Tests/TestRunFailureEnvelopeTests.cs`, `tests/RoslynMcp.Tests/ValidationToolsIntegrationTests.cs`. |
| Tool policy | edit-only |
| Estimated context cost | 20000 |
| Risks | Fanout probe: two affected positional invocations in the two scoped files. Preserve failure-envelope and validation precedence assertions. |
| Validation | Run Roslyn `compile_check`; run both edited test classes; after all siblings land, repeat the census and run the release gate. |
| Performance review | N/A — mechanical test-call refactor. |
| CHANGELOG category | Maintenance |
| CHANGELOG entry (draft) | Converted validation tool tests to named arguments for safe workspaceId reordering. |
| Backlog sync | Close rows: [workspace-id-named-args-validation]. |

### 15. tasks-extension-compatibility-decision

| Field | Content |
|---|---|
| Route | deepen |
| Diagnosis | `Directory.Packages.props:6` and `src/RoslynMcp.Host.Stdio/RoslynMcp.Host.Stdio.csproj:51` pin and reference only the base `ModelContextProtocol` 2.2.0 package; `src/RoslynMcp.Host.Stdio/Program.cs:75-116` configures the base server, transport, surface, and request filters without `WithTasks`, so the Tasks capability and `tasks/get`, `tasks/update`, and `tasks/cancel` handlers are absent. `docs/decisions/0003-sdk-2x-wire-compatibility.md:69-71` deliberately deferred the separately packaged extension to a product decision, while `docs/decisions/0003-sdk-2x-wire-compatibility.md:96` still names the retired umbrella row. |
| Approach | Add `docs/decisions/0007-mcp-tasks-extension-compatibility.md`, mirroring the context, decision, classification, migration, consequences, validation, and references shape in `docs/decisions/0006-modelcontextprotocol-2-2-servicing.md:9-76`. Record an adopt decision: the separately published Tasks 2.2.0 package supports `net10.0` and has an exact dependency on base SDK 2.2.0, so its artifact is separate but its compatible release cadence is lockstep with the core pin. Require per-call client opt-in and configure the future `WithTasks` `ExecutionModeSelector` as `Optional` only for the named slow-operation tools and `Synchronous` otherwise. Record that non-opted and down-level clients retain ordinary synchronous results, while down-level direct `tasks/*` calls receive method-not-found. Choose a process-scoped in-memory store for the local stdio boundary and explicitly document that host restart invalidates task handles. Index the decision in `docs/README.md`, add it to `docs/release-policy.md` beside ADRs 0003 and 0006, and replace the retired Tasks planning handle in ADR 0003 with the new decision plus live execution children. |
| Scope | Production files: 4 — `docs/decisions/0007-mcp-tasks-extension-compatibility.md`, `docs/decisions/0003-sdk-2x-wire-compatibility.md`, `docs/README.md`, `docs/release-policy.md`. Test files: 0. Deletions: 0. The changelog fragment and transactional backlog close are excluded shared workflow files. |
| Tool policy | edit-only |
| Estimated context cost | 25000 |
| Risks | The decision must not claim the Tasks package is already referenced or the capability already ships. The default `WithTasks` behavior makes every tool task-capable, so the ADR must preserve the bounded rollout by requiring the stable per-call `ExecutionModeSelector`. A process-scoped store cannot survive host recycle; that limitation and unknown-task recovery must be explicit rather than described as durable across processes. The old Tasks umbrella reference in ADR 0003 is stale and is repaired within this initiative. Not refactor-shaped; no symbol fanout probe is required. |
| Validation | Re-fetch the official Tasks 2.2.0 flat-container index and nuspec, confirm the exact base-package dependency and `net10.0` asset group, and check the SDK 2.2.0 Tasks source for `ExecutionModeSelector`, protocol gating, and non-opted synchronous fallback. Run `pwsh -NoProfile -File ./eng/verify-ai-docs.ps1`, then `pwsh -NoProfile -File ./eng/verify-release.ps1 -Configuration Release`. Verify every new decision link resolves and ADR 0003 names only live planning handles. |
| Performance review | N/A — correctness fix, no hot-path changes. |
| CHANGELOG category | Maintenance |
| CHANGELOG entry (draft) | Document the opt-in MCP Tasks extension compatibility posture for the pinned SDK 2.2.0 line. |
| Backlog sync | Close rows: [tasks-extension-compatibility-decision]. |

### 16. structured-tool-dispatch-adapter

| Field | Content |
|---|---|
| Route | direct |
| Diagnosis | `src/RoslynMcp.Host.Stdio/Tools/ToolDispatch.cs:442-451` always serializes `TDto` to `Task<string>`, while `WorkspaceDriftTool.cs:30-39` duplicates gate/service/result projection to return `CallToolResult`. |
| Approach | Add a generic/result-projecting read-by-workspace dispatch path that preserves gate and cancellation semantics.<br>Migrate `workspace_drift_check` off its one-off gate/service lambda without forwarding wrappers or serialization in `ToolDispatch`.<br>Correct `ToolDispatch` ownership comments so every pure read path follows the declared adapter contract.<br>One dispatch regression proves the structured result, workspace id, and cancellation token pass through unchanged. |
| Scope | Production: 2 (`src/RoslynMcp.Host.Stdio/Tools/ToolDispatch.cs`, `src/RoslynMcp.Host.Stdio/Tools/WorkspaceDriftTool.cs`). Tests: 1 (`tests/RoslynMcp.Tests/Services/WorkspaceDriftServiceTests.cs`). Deletions: none. |
| Tool policy | edit-only |
| Estimated context cost | 40000 |
| Risks | Preserve generated string dispatch and structured-content metadata; cancellation must remain gate-provided. No refactor-shaped fanout beyond the one migrated caller. |
| Validation | Focused workspace-drift/dispatch tests; Roslyn `compile_check`; `./eng/verify-release.ps1 -Configuration Release`. |
| Performance review | Confirm no extra serialization or gate hop is introduced. |
| CHANGELOG category | Maintenance |
| CHANGELOG entry (draft) | Reuse the standard read dispatch path for structured workspace results. |
| Backlog sync | Close rows: [structured-tool-dispatch-adapter]. |

### 17. workspace-auto-load-on-demand-design

| Field | Content |
|---|---|
| Route | deepen |
| Diagnosis | The detail still claims the feature is unimplemented at `ai_docs/items/workspace-auto-load-on-demand-design.md:6`, but the host now resolves omitted IDs at the middleware chokepoint (`src/RoslynMcp.Host.Stdio/Middleware/StructuredCallToolFilter.cs:148`), dispatches unique zero-workspace discoveries through registered `workspace_load` (`src/RoslynMcp.Host.Stdio/Middleware/StructuredCallToolFilter.cs:616`), and records the designed auto-resolution metrics (`src/RoslynMcp.Host.Stdio/Middleware/StructuredCallToolFilter.cs:657`). The query-root question is resolved by bounded sanctioned-root discovery in `src/RoslynMcp.Host.Stdio/Middleware/SolutionDiscoveryHelper.cs:9`. The prerequisite plan has been garbage-collected, and residual adoption work is independently tracked at `ai_docs/backlog.md:209` and `ai_docs/backlog.md:212`. The design spec is obsolete; its retirement row remains actionable. |
| Approach | Close `workspace-auto-load-on-demand-design` with the canonical backlog helper, atomically removing its slim row and `ai_docs/items/workspace-auto-load-on-demand-design.md`. Do not copy its implementation sections elsewhere: current behavior is represented by source and `CHANGELOG.md:488`, while adoption evidence, named-argument preparation, alias parity, and the remaining schema flips already have dedicated backlog rows. Make no host, bootstrap, or runtime behavior changes. |
| Scope | Production/docs: 1 deletion (`ai_docs/items/workspace-auto-load-on-demand-design.md`). Tests: 0. The helper-owned `ai_docs/backlog.md` mutation is excluded from the production-file count. No CHANGELOG fragment is required for internal `ai_docs/**` planning cleanup. |
| Tool policy | edit-only |
| Estimated context cost | 15000 |
| Risks | Do not interpret deletion as completion of the workspace-ID adoption rollout: `ai_docs/runtime.md:167` still gives explicit-load bootstrap guidance, and the evidence/prerequisite/flip rows remain open. The legacy anchors are stale: `WorkspaceTools.cs:268` moved to `src/RoslynMcp.Host.Stdio/Tools/WorkspaceTools.cs:690`, `WorkspaceManager.cs:167` moved to `src/RoslynMcp.Roslyn/Services/WorkspaceManager.cs:220`, and the middleware ranges drifted. Closing this dependency should leave `structured-call-tool-filter-pipeline-decomposition` otherwise unchanged. No refactor-shaped fanout applies to this one-file documentation deletion. |
| Validation | Confirm the canonical backlog close removes both the row and detail without dangling document references; run `pwsh -NoProfile -File ./eng/verify-ai-docs.ps1`; run `pwsh -NoProfile -File ./eng/verify-release.ps1 -Configuration Release`. |
| Performance review | N/A — documentation cleanup, no runtime path changes. |
| CHANGELOG category | Maintenance |
| CHANGELOG entry (draft) | Retired the obsolete workspace auto-load design spec after verifying the feature shipped and remaining adoption work is separately tracked. |
| Backlog sync | Close rows: [workspace-auto-load-on-demand-design]. Mark obsolete: []. |

### 18. refactoring-format-range-preview-decomposition

| Field | Content |
|---|---|
| Route | direct |
| Diagnosis | `src/RoslynMcp.Roslyn/Services/RefactoringService.cs` keeps range resolution, formatting, splice validation, and preview assembly in `PreviewFormatRangeAsync`/`SpliceFormattedRange`, both measured at cyclomatic complexity 11. |
| Approach | Extract range resolution, formatting, splice-boundary validation, and preview assembly into named helpers.<br>Keep each extracted method below cyclomatic complexity 10 and 80 logical lines.<br>Preserve full-line, partial-line, trivia, newline, and invalid-range regressions. |
| Scope | Production: 1 (`src/RoslynMcp.Roslyn/Services/RefactoringService.cs`). Tests: 1 (`tests/RoslynMcp.Tests/FormatRangeServiceTests.cs`). Deletions: none. |
| Tool policy | edit-only |
| Estimated context cost | 40000 |
| Risks | Preserve byte/trivia boundaries and preview token semantics; rejected peer `refactoring-code-fix-preview-decomposition` remains dependency-blocked. No cross-file fanout. |
| Validation | Focused `FormatRangeServiceTests`; complexity check; Roslyn `compile_check`; `./eng/verify-release.ps1 -Configuration Release`. |
| Performance review | Keep allocation and formatting passes equivalent; extraction must not duplicate formatting. |
| CHANGELOG category | Maintenance |
| CHANGELOG entry (draft) | Decompose format-range preview orchestration without changing edit semantics. |
| Backlog sync | Close rows: [refactoring-format-range-preview-decomposition]. |

### 19. test-service-container-two-phase-construction-cycle

| Field | Content |
|---|---|
| Route | direct |
| Diagnosis | `tests/RoslynMcp.Tests/TestInfrastructure/TestServiceContainer.cs:77-87` captures a nullable local in `Lazy<IWorkspaceExecutionGate>` and assigns it only after constructing `WorkspaceManager`; correctness depends on statement order. |
| Approach | The `WorkspaceExecutionGate` / `WorkspaceManager` cycle is resolved without a mutable local captured by a `Lazy<>` that is assigned after the constructor call, OR the ordering requirement is enforced so reordering fails loudly at compile time rather than at runtime.<br>The throwing "resolved before initialization" guard is either unreachable by construction or covered by a regression. |
| Scope | Production: 0. Tests: 1 (`tests/RoslynMcp.Tests/TestInfrastructure/TestServiceContainer.cs`). Deletions: none. |
| Tool policy | edit-only |
| Estimated context cost | 30000 |
| Risks | Preserve the production-like eviction gate and avoid introducing another mutable global. No production fanout. |
| Validation | Focused fixture/container tests; Roslyn `compile_check`; `./eng/verify-release.ps1 -Configuration Release`. |
| Performance review | N/A — test fixture refactor. |
| CHANGELOG category | Maintenance |
| CHANGELOG entry (draft) | Remove order-dependent two-phase construction from the test service container. |
| Backlog sync | Close rows: [test-service-container-two-phase-construction-cycle]. |

### 20. test-assembly-fixture-disposal-observable-postcondition

| Field | Content |
|---|---|
| Route | direct |
| Diagnosis | `tests/RoslynMcp.Tests/TestAssemblyFixtureTests.cs:93` observes disposal entry, but `TestAssemblyFixture.cs:50-51` separates entry from owned-resource cleanup; deleting the release block can leave the test green. |
| Approach | `DisposeAsync_ReleasesOwnedResourcesExactlyOnce` asserts an observable post-condition proving the release block RAN, not merely that it was entered — deleting the block's body must fail the test.<br>The assertion is deterministic: it passes and fails identically in isolation and under the full suite, with no dependence on state other tests accumulate.<br>`WorkspaceIdCache` exposes whatever minimal read surface the assertion needs (e.g. a count or contains check), used only by the regression. |
| Scope | Production: 0. Tests: 3 (`tests/RoslynMcp.Tests/TestAssemblyFixtureTests.cs`, `tests/RoslynMcp.Tests/TestInfrastructure/TestAssemblyFixture.cs`, `tests/RoslynMcp.Tests/TestInfrastructure/WorkspaceIdCache.cs`). Deletions: none. |
| Tool policy | edit-only |
| Estimated context cost | 35000 |
| Risks | Do not reuse the known state-sensitive disposed-manager assertion; isolate cache state deterministically. No production fanout. |
| Validation | Focused disposal regression in isolation and repeated with fixture-sharing tests; Roslyn `compile_check`; `./eng/verify-release.ps1 -Configuration Release`. |
| Performance review | N/A — test fixture correctness. |
| CHANGELOG category | Maintenance |
| CHANGELOG entry (draft) | Prove assembly-fixture resource cleanup with an observable post-condition. |
| Backlog sync | Close rows: [test-assembly-fixture-disposal-observable-postcondition]. |

### 21. formatter-baseline-generator-double-restore-cost

| Field | Content |
|---|---|
| Route | deepen |
| Diagnosis | The row remains valid: `Generator_IsDeterministicAndTheTrackedInventoryCoversTheLiveRunAsync` invokes `RunGeneratorCheckAsync` twice at `tests/RoslynMcp.Tests/FormatterBaselineContractTests.cs:360-361`, while the helper adds `-Check` but never `-NoRestore` at `tests/RoslynMcp.Tests/FormatterBaselineContractTests.cs:582-589`. Consequently, both invocations execute `dotnet restore` through `eng/generate-format-baseline.ps1:115-124`. The script already defines `-NoRestore` for a previously restored working tree at `eng/generate-format-baseline.ps1:46-53`, and its independent fail-closed truncation rejection remains active at `eng/generate-format-baseline.ps1:141-144`; therefore the second restore is not required to preserve that contract. The detail's cited truncation anchor `eng/generate-format-baseline.ps1:103-106` is stale and now points to path validation. |
| Approach | Keep the row but narrow it to the second deterministic pass. Add a defaulted `noRestore` parameter to `RunGeneratorCheckAsync`; retain the restoring first call and invoke the second as `RunGeneratorCheckAsync(noRestore: true)`, appending the existing script switch only for that call. Extend `Generator_IsDeterministicAndTheTrackedInventoryCoversTheLiveRunAsync` to assert that the first stderr sequence contains restore-start, restore-end, format-start, and format-end, while the second contains only format-start and format-end. Preserve the existing byte-equality assertion and script-side truncation failure. Do not modify `eng/generate-format-baseline.ps1`. |
| Scope | Production files: 0. Test files: 1 (`tests/RoslynMcp.Tests/FormatterBaselineContractTests.cs`). Deletions: none. The orchestrator-owned `changelog.d/formatter-baseline-generator-double-restore-cost.md` fragment and backlog mutation are excluded from the production-file count. |
| Tool policy | edit-only |
| Estimated context cost | 25000 |
| Risks | Keep the first restore because it establishes the prerequisite that makes the second call's `-NoRestore` safe. Accept the redesign only when the second invocation emits format-only markers, produces byte-identical stdout, and does not trigger the script's truncation or exit-code failures. If those checks fail, retain both restores and document the measured necessity at the call site; removal as obsolete is warranted only if the live helper already avoids the second restore, which it currently does not. Fanout probe: `RunGeneratorCheckAsync` has 2 use sites, both in the same determinism test at lines 360-361. |
| Validation | Run Roslyn `compile_check`; run `dotnet test tests/RoslynMcp.Tests/RoslynMcp.Tests.csproj -c Release --filter "FullyQualifiedName~FormatterBaselineContractTests.Generator_IsDeterministicAndTheTrackedInventoryCoversTheLiveRunAsync"` and inspect captured phase markers to prove exactly one restore across the pair; run `pwsh -NoProfile -File ./eng/verify-release.ps1 -Configuration Release`. |
| Performance review | Expected improvement is one fewer full `dotnet restore` per determinism test. Compare the existing per-run `totalMs` and phase markers, while preserving the five-minute process bound and fail-closed truncation behavior. |
| CHANGELOG category | Maintenance |
| CHANGELOG entry (draft) | Reuse the formatter baseline test's first successful restore for its second deterministic generator pass while retaining fail-closed truncation checks. |
| Backlog sync | Close rows: [formatter-baseline-generator-double-restore-cost]. Mark obsolete: []. |

### 22. desc-budget-harness-method-adopt-wave-2

| Field | Content |
|---|---|
| Route | direct |
| Diagnosis | Three method-description slice classes duplicate reflection/assertion bodies instead of calling the already-shipped `ToolDescriptionBudgetHarness`; its discriminating-trigger branch still has no real caller. |
| Approach | All three slice classes call the shared harness instead of carrying their own reflection enumeration and assertion bodies.<br>Each declares its own ceilings, tool-type set and discriminating-trigger substrings; no ceiling is loosened during the migration.<br>Behaviour is unchanged — the same tools pass and the same violations would fail, proven by the suite staying green with no constant edits.<br>`ToolDescriptionBudgetHarness.AssertDiscriminatingTriggers` gets at least one real caller here (or this row adds a self-test) that exercises both its pass and fail branches — cold review of `desc-budget-harness-method-family` found the method ships with zero callers, so the trigger-ratchet branching is currently unverified. |
| Scope | Production: 0. Tests: 3 (`tests/RoslynMcp.Tests/MethodDescriptionDietDiagnosticsSecurityTests.cs`, `tests/RoslynMcp.Tests/MethodDescriptionDietEditingMsBuildTests.cs`, `tests/RoslynMcp.Tests/MethodDescriptionDietScaffoldingMutationTests.cs`). Deletions: duplicated local helpers only. |
| Tool policy | edit-only |
| Estimated context cost | 35000 |
| Risks | Preserve every current ceiling and trigger; do not weaken exact assertions to simplify adoption. No production fanout. |
| Validation | Focused three slice classes and shared harness tests; Roslyn `compile_check`; `./eng/verify-release.ps1 -Configuration Release`. |
| Performance review | N/A — test refactor. |
| CHANGELOG category | Maintenance |
| CHANGELOG entry (draft) | Migrate the second method-description test wave to the shared harness. |
| Backlog sync | Close rows: [desc-budget-harness-method-adopt-wave-2]. |

### 23. desc-budget-harness-method-adopt-wave-3

| Field | Content |
|---|---|
| Route | direct |
| Diagnosis | Three remaining method-description slice classes, including the one-type refactoring outlier, still duplicate reflection/assertion logic instead of using `ToolDescriptionBudgetHarness`. |
| Approach | All three slice classes call the shared harness instead of carrying their own reflection enumeration and assertion bodies.<br>Each declares its own ceilings, tool-type set and discriminating-trigger substrings; no ceiling is loosened during the migration.<br>`RefactoringCoreDescriptionBudgetTests` — the one-type, one-test outlier — either adopts the helper or the row records why the shape does not fit. |
| Scope | Production: 0. Tests: 3 (`tests/RoslynMcp.Tests/MethodDescriptionDietServerSurfaceTests.cs`, `tests/RoslynMcp.Tests/MethodDescriptionDietTestToolingTests.cs`, `tests/RoslynMcp.Tests/RefactoringCoreDescriptionBudgetTests.cs`). Deletions: duplicated local helpers only. |
| Tool policy | edit-only |
| Estimated context cost | 35000 |
| Risks | Preserve the one-type outlier's stricter semantics; no ceiling relaxation. No production fanout. |
| Validation | Focused three slice classes and shared harness tests; Roslyn `compile_check`; `./eng/verify-release.ps1 -Configuration Release`. |
| Performance review | N/A — test refactor. |
| CHANGELOG category | Maintenance |
| CHANGELOG entry (draft) | Migrate the final method-description test wave to the shared harness. |
| Backlog sync | Close rows: [desc-budget-harness-method-adopt-wave-3]. |

### 24. method-diet-undo-workspace-lifecycle

| Field | Content |
|---|---|
| Route | direct |
| Diagnosis | The four tool files named by the row contain five `[McpServerTool]` descriptions up to 1,142 characters, mixing capability discovery with operational runbooks and exceeding the 200-character ceiling. |
| Approach | Every `[McpServerTool]` method `[Description]` in the listed files is a capability statement of ~150–200 chars (what it does, when to use); operational guidance moves to XML `<remarks>` on the same method, ServerInstructions, or a prompt.<br>No capability statement is dropped — each tool still names its discriminating trigger versus sibling tools (ToolSearch discovery depends on it).<br>One slice ratchet asserts the per-tool ceiling, the slice total, non-empty descriptions, and this slice's discriminating-trigger substrings — calling the SHARED harness from `desc-budget-harness-method-family`, not a new ~98-line copy. |
| Scope | Production: 4 (`src/RoslynMcp.Host.Stdio/Tools/UndoTools.cs`, `WorkspaceWarmTools.cs`, `ApplyWithVerifyTool.cs`, `WorkspaceDriftTool.cs`). Tests: 1 (`tests/RoslynMcp.Tests/MethodDescriptionDietUndoWorkspaceLifecycleTests.cs`). Deletions: none. |
| Tool policy | edit-only |
| Estimated context cost | 50000 |
| Risks | Preserve refusal/recovery capabilities and discovery keywords; the ceiling is not a target. Per-target definition probe is exactly the 4 listed production files, so fanout estimate is 4. |
| Validation | Focused description-budget slice; Roslyn `compile_check`; `./eng/verify-release.ps1 -Configuration Release`. |
| Performance review | N/A — metadata-only changes. |
| CHANGELOG category | Maintenance |
| CHANGELOG entry (draft) | Condense undo and workspace-lifecycle tool descriptions for discovery. |
| Backlog sync | Close rows: [method-diet-undo-workspace-lifecycle]. |

### 25. method-diet-fixall-bulk-operations

| Field | Content |
|---|---|
| Route | direct |
| Diagnosis | The four named tool files contain oversized method descriptions up to 737 characters, embedding operational detail in the discovery surface. |
| Approach | Every `[McpServerTool]` method `[Description]` in the listed files is a capability statement of ~150–200 chars (what it does, when to use); operational guidance moves to XML `<remarks>` on the same method, ServerInstructions, or a prompt.<br>No capability statement is dropped — each tool still names its discriminating trigger versus sibling tools (ToolSearch discovery depends on it).<br>One slice ratchet asserts the per-tool ceiling, the slice total, non-empty descriptions, and this slice's discriminating-trigger substrings — calling the SHARED harness from `desc-budget-harness-method-family`, not a new ~98-line copy. |
| Scope | Production: 4 (`src/RoslynMcp.Host.Stdio/Tools/FixAllTools.cs`, `BulkRefactoringTools.cs`, `OperationTools.cs`, `ScriptingTools.cs`). Tests: 1 (`tests/RoslynMcp.Tests/MethodDescriptionDietFixallBulkOperationsTests.cs`). Deletions: none. |
| Tool policy | edit-only |
| Estimated context cost | 50000 |
| Risks | Preserve risk boundaries, refusal clauses, and recovery triggers; the ceiling is not a target. Per-target definition probe is exactly 4 production files, so fanout estimate is 4. |
| Validation | Focused description-budget slice; Roslyn `compile_check`; `./eng/verify-release.ps1 -Configuration Release`. |
| Performance review | N/A — metadata-only changes. |
| CHANGELOG category | Maintenance |
| CHANGELOG entry (draft) | Condense fix-all, bulk, operation, and scripting tool descriptions. |
| Backlog sync | Close rows: [method-diet-fixall-bulk-operations]. |
