| Field | Content |
|---|---|
| Route | `direct` |
| Diagnosis | Each anchored class carries exactly one `[DoNotParallelize]` (confirmed by grep, one hit per file). Most declaring class-doc comments describe test *coverage*, not a parallelization rationale — no source-adjacent comment currently names the concrete process-global/shared-workspace dependency the attribute exists for. Root cause per class: any class deriving `SharedWorkspaceTestBase`/`TestBase` reads through the assembly-shared `TestAssemblyFixture` (`tests/RoslynMcp.Tests/TestInfrastructure/TestAssemblyFixture.cs`), whose workspace-id cache is already synchronized (`WorkspaceIdCache` — `ConcurrentDictionary` + `SemaphoreSlim(1,1)`, `tests/RoslynMcp.Tests/TestInfrastructure/WorkspaceIdCache.cs:8-9`); a class that only performs read-side queries through that cache has no remaining concurrency hazard and is a removal candidate, while a class that mutates shared server-side state (workspace reload, `*_apply` tools, DI/service singletons, static caches outside the synchronized cache) still needs serialization. This diagnosis is the shared root-cause note; the per-file mechanism (or removal) is decided against each file's own body during implementation, per Approach. |
| Approach | Row Acceptance verbatim, applied per anchored file: for each of `tests/RoslynMcp.Tests/BacklogFixTests.cs`, `tests/RoslynMcp.Tests/BuildTestToolsShimTests.cs`, `tests/RoslynMcp.Tests/BulkRefactoringTests.cs` — (1) read the class body and its base class to identify what state (if any) each test method touches outside its own locals; (2) if every test method only reads through the already-synchronized `WorkspaceIdCache`/read-only service calls with no shared mutable state, remove `[DoNotParallelize]` and validate with a bounded repeated-run (>=3x) of the class alongside its wave siblings, confirming green every time; (3) if a method mutates shared server-side or static state (workspace reload, `_apply` tools, static caches, environment/registry writes), retain `[DoNotParallelize]` and add a one-line comment immediately above the attribute naming the concrete dependency and the mechanism. Do not weaken assertions, add sleeps/delays, or reclassify an unrelated concurrency failure as a pass. |
| Scope | Test files (3): `tests/RoslynMcp.Tests/BacklogFixTests.cs`, `tests/RoslynMcp.Tests/BuildTestToolsShimTests.cs`, `tests/RoslynMcp.Tests/BulkRefactoringTests.cs`. No production files — the row's own diagnosis budget (`WorkspaceIdCache.cs`) is cited as evidence, not edited. |
| Tool policy | `edit-only` |
| Estimated context cost | 28000 |
| Risks | A class wrongly cleared of `[DoNotParallelize]` would surface as an intermittent CI failure days later rather than in this run's gate — mitigated by the required >=3x repeated-run evidence before removal, and by `parallel_safety.parallelSafe: true` already covering scoped/targeted test-process isolation (addenda, `TestTempRoot`-scoped fixtures) so cross-class contamination is the only remaining hazard class to rule out per file. Sibling waves (other `donotparallelize-audit-wave-*` initiatives) touch disjoint file sets, so no cross-initiative conflict. |
| Validation | Per-file: `mcp__roslyn__test_run --filter "FullyQualifiedName~<ClassName>"` (or `dotnet test --filter` fallback) run individually, then repeated back-to-back with sibling classes in the same wave to exercise the concurrency claim; full addenda `ci_equivalent` before PR. |
| Performance review | N/A — correctness fix, no hot-path changes. |
| CHANGELOG category | Maintenance |
| CHANGELOG entry (draft) | Re-audited `[DoNotParallelize]` opt-outs on `BacklogFixTests.cs`, `BuildTestToolsShimTests.cs`, `BulkRefactoringTests.cs` — removed opt-outs proven safe by repeated concurrent runs, and documented the retained ones with their concrete shared-state dependency. |
| Backlog sync | Close rows: [donotparallelize-audit-wave-03]. Mark obsolete: []. |
