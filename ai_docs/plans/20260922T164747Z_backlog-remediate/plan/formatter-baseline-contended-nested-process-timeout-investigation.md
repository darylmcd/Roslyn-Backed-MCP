| Field | Content |
|---|---|
| Route | deepen |
| Diagnosis | Two compounding, source-confirmed mechanisms. (1) `eng/generate-format-baseline.ps1:138-147` (`Invoke-OwnedDotNetProcess`) never disables MSBuild node reuse for its nested `dotnet restore`/`dotnet format` children — unlike `src/RoslynMcp.Roslyn/Helpers/DotnetCommandRunner.cs:176-180`, whose own comment documents a reused MSBuild/VBCSCompiler node can inherit the parent's redirected pipe handles and idle up to 15 min holding the write end open. (2) `tests/RoslynMcp.Tests/FormatterBaselineContractTests.cs:980-982` (`RunGeneratorCheckAsync` success path) awaits `stdoutCapture.Completion`/`stderrCapture.Completion` with NO bound — the `CancellationTokenSource(processTimeout)` at line 930 wraps only `WaitForExitAsync`; the TIMEOUT path already bounds the equivalent drain (`TerminateAndDrainOwnedTreeAsync`, 793-807, `_drainTimeout`), and `DotnetCommandRunner.ReadBoundedAsync` (207-257) already treats an un-EOF'd post-exit pipe as a lingering-descendant signal, not something to wait on forever. Under `[assembly: Parallelize(Workers=0)]` (`AssemblyInfo.cs:8`), 48 concurrent processes at start (row Evidence) raise the odds a restore/format child attaches to a reused node that outlives it; the harness then hangs on the unbounded drain past the process's own 32-39s completion. Addenda `:97-99` independently records 2026-09-20 host-contention failures, corroborating this class. Cold review already ruled out pipe-buffer deadlock (row Evidence); this is a distinct handle-inheritance hang. |
| Approach | (1) `generate-format-baseline.ps1:138-147` — set `$startInfo.Environment['MSBUILDDISABLENODEREUSE']='1'`, mirroring `DotnetCommandRunner.cs:176-180`'s rationale (one-shot children gain nothing from reuse). (2) `FormatterBaselineContractTests.cs:980-982` — replace the unbounded `Completion` awaits with a bounded drain reusing `DrainAsync(...)` (752-772) + `_drainTimeout`, so a lingering descendant degrades to a diagnostic instead of hanging. (3) Add one `[TestMethod]` regression reusing `GeneratorExecutionSeam` (1147-1150), mirroring `TimedOutCapture_RetainsPhaseMarkerAndTerminatesRecordedNestedChildAsync`: launch a fixture whose child spawns a grandchild inheriting the redirected handles and outliving the immediate child; assert the SUCCESS path returns within the bound with the grandchild still alive, and no fixture process remains (cleanup mirrors 470-496). |
| Scope | Production (1): `eng/generate-format-baseline.ps1`. Test (1): `tests/RoslynMcp.Tests/FormatterBaselineContractTests.cs`. No deletions; both anchors are the row's own cited files. |
| Tool policy | edit-only |
| Estimated context cost | 50000 |
| Risks | Diagnosis mirrors an already-shipped analogous fix + the addenda's 2026-09-20 note but is not execution-verified this pass — executor MUST run the new regression, confirm pre-fix hang / post-fix pass within bound, before closing. `MSBUILDDISABLENODEREUSE=1` only affects this script's 2 one-shot children (repo precedent: `DotnetCommandRunner` already disables it everywhere). Bounded drain is a no-op on the common path; re-run `Generator_IsDeterministicAndTheTrackedInventoryCoversTheLiveRunAsync` to confirm byte-identical output still holds. `donotparallelize-audit-wave-01..08` siblings touch none of this row's files — no overlap. Not refactor-shaped: both edited functions are private/internal, no other caller — `fanoutEstimate: null`. |
| Validation | `mcp__roslyn__test_run --filter "FullyQualifiedName~FormatterBaselineContractTests"` (fallback `dotnet test --filter ...`), run >=3x back-to-back; new regression must reproduce pre-fix hang and pass post-fix within bound, zero leftover process. Manual `pwsh eng/generate-format-baseline.ps1 -Check` and `-Check -NoRestore` to confirm byte-identical output. Full addenda `ci_equivalent` before PR. |
| Performance review | N/A — correctness fix to a CI/dev-time script and its own contract test, no runtime hot-path changes. |
| CHANGELOG category | Fixed |
| CHANGELOG entry (draft) | Fixed a formatter-baseline generator stall under host contention: spawned `dotnet` children now disable MSBuild node reuse, and the contract test's stdout/stderr drain is bounded on the success path so a lingering handle-inheriting descendant can no longer hang the harness past the nominal timeout. |
| Backlog sync | Close rows: [formatter-baseline-contended-nested-process-timeout-investigation]. Mark obsolete: []. |
