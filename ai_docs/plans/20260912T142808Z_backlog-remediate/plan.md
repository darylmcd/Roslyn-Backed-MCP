# Backlog sweep plan — 20260912T142808Z_backlog-remediate

Generated by `/backlog-remediate`. 15 initiative(s) selected off `ai_docs/backlog.md` (snapshot 2026-09-12T00:17:15Z).

<!-- BSWEEP:STATUS-TABLE BEGIN — generated from state.json; do not edit by hand -->
## Status (generated)

| # | id | status | PR | rows closed |
|---|----|--------|----|-------------|
| 1 | method-diet-refactoring-editorconfig | obsolete | — | method-diet-refactoring-editorconfig |
| 2 | method-diet-symbol-advanced-analysis | obsolete | — | method-diet-symbol-advanced-analysis |
| 3 | method-diet-restructure-interface | obsolete | — | method-diet-restructure-interface |
| 4 | method-diet-signature-extraction | obsolete | — | method-diet-signature-extraction |
| 5 | method-diet-ratchet-only-clean-slices | obsolete | — | method-diet-ratchet-only-clean-slices |
| 6 | cohesion-multifile-partial-semantic-model | obsolete | — | cohesion-multifile-partial-semantic-model |
| 7 | unused-code-analyzer-static-lazy-field-read-safety | obsolete | — | unused-code-analyzer-static-lazy-field-read-safety |
| 8 | unused-code-analyzer-captured-local-read-safety | obsolete | — | unused-code-analyzer-captured-local-read-safety |
| 9 | diagnostic-details-project-diagnostic-location-contract | obsolete | — | diagnostic-details-project-diagnostic-location-contract |
| 10 | test-run-full-suite-timeout-envelope | obsolete | — | test-run-full-suite-timeout-envelope |
| 11 | reconcile-plan-force-worktree-removal-safety | obsolete | — | reconcile-plan-force-worktree-removal-safety |
| 12 | scripting-startup-cancellation-causal-barrier | pending | — | scripting-startup-cancellation-causal-barrier |
| 13 | apply-composite-preview-destructive-misnomer | pending | — | apply-composite-preview-destructive-misnomer |
| 14 | change-signature-callsite-summary-stale-row-comments | pending | — | change-signature-callsite-summary-stale-row-comments |
| 15 | surface-test-shipped-prompt-local-skill-reference | pending | — | surface-test-shipped-prompt-local-skill-reference |
<!-- BSWEEP:STATUS-TABLE END -->

## Initiatives

### 1. method-diet-refactoring-editorconfig

| Field | Content |
|---|---|
| Route | direct |
| Diagnosis | The shared harness exists but this four-type slice has no ratchet. |
| Approach | Trim the selected method descriptions, preserve discriminating triggers, move operational guidance to remarks, and add the shared-harness ratchet. |
| Scope | Production files (4): src/RoslynMcp.Host.Stdio/Tools/RefactoringTools.cs, src/RoslynMcp.Host.Stdio/Tools/EditorConfigTools.cs, src/RoslynMcp.Host.Stdio/Tools/DeadCodeTools.cs, src/RoslynMcp.Host.Stdio/Tools/TypeMoveTools.cs. Test files (1): tests/RoslynMcp.Tests/MethodDescriptionDietRefactoringEditorconfigTests.cs. |
| Tool policy | edit-only |
| Estimated context cost | 60000 |
| Risks | Preserve stable wire behavior and avoid unrelated edits. |
| Validation | Focused regression and ./eng/verify-release.ps1 -Configuration Release. |
| Performance review | N/A — no hot-path change. |
| CHANGELOG category | Fixed |
| CHANGELOG entry (draft) | Fix method-diet-refactoring-editorconfig. |
| Backlog sync | Close rows: [method-diet-refactoring-editorconfig]. Mark obsolete: []. |

### 2. method-diet-symbol-advanced-analysis

| Field | Content |
|---|---|
| Route | direct |
| Diagnosis | This three-type slice has remaining over-budget method descriptions. |
| Approach | Trim each description to a capability statement, retain triggers, move detail to remarks, and add the shared-harness ratchet. |
| Scope | Production files (3): src/RoslynMcp.Host.Stdio/Tools/SymbolTools.cs, src/RoslynMcp.Host.Stdio/Tools/AdvancedAnalysisTools.cs, src/RoslynMcp.Host.Stdio/Tools/AnalysisTools.cs. Test files (1): tests/RoslynMcp.Tests/MethodDescriptionDietSymbolAdvancedAnalysisTests.cs. |
| Tool policy | edit-only |
| Estimated context cost | 60000 |
| Risks | Preserve stable wire behavior and avoid unrelated edits. |
| Validation | Focused regression and ./eng/verify-release.ps1 -Configuration Release. |
| Performance review | N/A — no hot-path change. |
| CHANGELOG category | Fixed |
| CHANGELOG entry (draft) | Fix method-diet-symbol-advanced-analysis. |
| Backlog sync | Close rows: [method-diet-symbol-advanced-analysis]. Mark obsolete: []. |

### 3. method-diet-restructure-interface

| Field | Content |
|---|---|
| Route | direct |
| Diagnosis | Five method descriptions remain over the selected slice budget. |
| Approach | Trim capability statements without losing triggers and add the shared-harness ratchet. |
| Scope | Production files (4): src/RoslynMcp.Host.Stdio/Tools/RestructureTools.cs, src/RoslynMcp.Host.Stdio/Tools/RemoveInterfaceMemberTool.cs, src/RoslynMcp.Host.Stdio/Tools/SyntaxTools.cs, src/RoslynMcp.Host.Stdio/Tools/InterfaceExtractionTools.cs. Test files (1): tests/RoslynMcp.Tests/MethodDescriptionDietRestructureInterfaceTests.cs. |
| Tool policy | edit-only |
| Estimated context cost | 60000 |
| Risks | Preserve stable wire behavior and avoid unrelated edits. |
| Validation | Focused regression and ./eng/verify-release.ps1 -Configuration Release. |
| Performance review | N/A — no hot-path change. |
| CHANGELOG category | Fixed |
| CHANGELOG entry (draft) | Fix method-diet-restructure-interface. |
| Backlog sync | Close rows: [method-diet-restructure-interface]. Mark obsolete: []. |

### 4. method-diet-signature-extraction

| Field | Content |
|---|---|
| Route | direct |
| Diagnosis | Three marginal descriptions lack a slice-level ratchet. |
| Approach | Trim descriptions without losing refusal behavior and add the shared-harness ratchet. |
| Scope | Production files (3): src/RoslynMcp.Host.Stdio/Tools/ParameterObjectTools.cs, src/RoslynMcp.Host.Stdio/Tools/ChangeSignatureTools.cs, src/RoslynMcp.Host.Stdio/Tools/ExtractMethodTools.cs. Test files (1): tests/RoslynMcp.Tests/MethodDescriptionDietSignatureExtractionTests.cs. |
| Tool policy | edit-only |
| Estimated context cost | 50000 |
| Risks | Preserve stable wire behavior and avoid unrelated edits. |
| Validation | Focused regression and ./eng/verify-release.ps1 -Configuration Release. |
| Performance review | N/A — no hot-path change. |
| CHANGELOG category | Fixed |
| CHANGELOG entry (draft) | Fix method-diet-signature-extraction. |
| Backlog sync | Close rows: [method-diet-signature-extraction]. Mark obsolete: []. |

### 5. method-diet-ratchet-only-clean-slices

| Field | Content |
|---|---|
| Route | direct |
| Diagnosis | The selected descriptions are compliant but unratcheted. |
| Approach | Leave compliant descriptions unchanged and add ceiling, total, non-empty, and trigger assertions. |
| Scope | Production files (4): src/RoslynMcp.Host.Stdio/Tools/CodeActionTools.cs, src/RoslynMcp.Host.Stdio/Tools/FileOperationTools.cs, src/RoslynMcp.Host.Stdio/Tools/OrchestrationTools.cs, src/RoslynMcp.Host.Stdio/Tools/TypeExtractionTools.cs. Test files (1): tests/RoslynMcp.Tests/MethodDescriptionDietRatchetOnlyCleanSlicesTests.cs. |
| Tool policy | edit-only |
| Estimated context cost | 45000 |
| Risks | Preserve stable wire behavior and avoid unrelated edits. |
| Validation | Focused regression and ./eng/verify-release.ps1 -Configuration Release. |
| Performance review | N/A — no hot-path change. |
| CHANGELOG category | Fixed |
| CHANGELOG entry (draft) | Fix method-diet-ratchet-only-clean-slices. |
| Backlog sync | Close rows: [method-diet-ratchet-only-clean-slices]. Mark obsolete: []. |

### 6. cohesion-multifile-partial-semantic-model

| Field | Content |
|---|---|
| Route | direct |
| Diagnosis | Cross-tree members are resolved with the wrong semantic model. |
| Approach | Resolve each member against its own syntax tree model and add a two-file partial regression. |
| Scope | Production files (2): src/RoslynMcp.Roslyn/Services/CohesionAnalysisService.cs, src/RoslynMcp.Roslyn/Services/RefactoringSuggestionService.cs. Test files (2): tests/RoslynMcp.Tests/CohesionAnalysisTests.cs, tests/RoslynMcp.Tests/RefactoringSuggestionTests.cs. |
| Tool policy | edit-only |
| Estimated context cost | 50000 |
| Risks | Preserve stable wire behavior and avoid unrelated edits. |
| Validation | Focused regression and ./eng/verify-release.ps1 -Configuration Release. |
| Performance review | N/A — no hot-path change. |
| CHANGELOG category | Fixed |
| CHANGELOG entry (draft) | Fix cohesion-multifile-partial-semantic-model. |
| Backlog sync | Close rows: [cohesion-multifile-partial-semantic-model]. Mark obsolete: []. |

### 7. unused-code-analyzer-static-lazy-field-read-safety

| Field | Content |
|---|---|
| Route | direct |
| Diagnosis | Lazy.Value reads are not classified as field reads. |
| Approach | Classify Lazy-backed semantic reads as live and add the static index regression. |
| Scope | Production files (2): src/RoslynMcp.Roslyn/Services/UnusedCodeAnalyzer.cs, src/RoslynMcp.Host.Stdio/Catalog/PromptParameterIndex.cs. Test files (1): tests/RoslynMcp.Tests/DeadFieldDetectorTests.cs. |
| Tool policy | edit-only |
| Estimated context cost | 45000 |
| Risks | Preserve stable wire behavior and avoid unrelated edits. |
| Validation | Focused regression and ./eng/verify-release.ps1 -Configuration Release. |
| Performance review | N/A — no hot-path change. |
| CHANGELOG category | Fixed |
| CHANGELOG entry (draft) | Fix unused-code-analyzer-static-lazy-field-read-safety. |
| Backlog sync | Close rows: [unused-code-analyzer-static-lazy-field-read-safety]. Mark obsolete: []. |

### 8. unused-code-analyzer-captured-local-read-safety

| Field | Content |
|---|---|
| Route | direct |
| Diagnosis | Nested local-function captures are missed. |
| Approach | Treat captured outer locals as live and retain genuine dead-local results. |
| Scope | Production files (1): src/RoslynMcp.Roslyn/Services/UnusedCodeAnalyzer.cs. Test files (1): tests/RoslynMcp.Tests/DeadLocalDetectorTests.cs. |
| Tool policy | edit-only |
| Estimated context cost | 35000 |
| Risks | Preserve stable wire behavior and avoid unrelated edits. |
| Validation | Focused regression and ./eng/verify-release.ps1 -Configuration Release. |
| Performance review | N/A — no hot-path change. |
| CHANGELOG category | Fixed |
| CHANGELOG entry (draft) | Fix unused-code-analyzer-captured-local-read-safety. |
| Backlog sync | Close rows: [unused-code-analyzer-captured-local-read-safety]. Mark obsolete: []. |

### 9. diagnostic-details-project-diagnostic-location-contract

| Field | Content |
|---|---|
| Route | direct |
| Diagnosis | List-tool locations do not round-trip to detail lookup. |
| Approach | Accept the emitted tuple unchanged and add the MCP002 round-trip regression. |
| Scope | Production files (2): src/RoslynMcp.Roslyn/Services/DiagnosticService.cs, src/RoslynMcp.Host.Stdio/Tools/AnalysisTools.cs. Test files (2): tests/RoslynMcp.Tests/AnalysisToolsTests.cs, tests/RoslynMcp.Tests/DiagnosticFixIntegrationTests.cs. |
| Tool policy | edit-only |
| Estimated context cost | 50000 |
| Risks | Preserve stable wire behavior and avoid unrelated edits. |
| Validation | Focused regression and ./eng/verify-release.ps1 -Configuration Release. |
| Performance review | N/A — no hot-path change. |
| CHANGELOG category | Fixed |
| CHANGELOG entry (draft) | Fix diagnostic-details-project-diagnostic-location-contract. |
| Backlog sync | Close rows: [diagnostic-details-project-diagnostic-location-contract]. Mark obsolete: []. |

### 10. test-run-full-suite-timeout-envelope

| Field | Content |
|---|---|
| Route | direct |
| Diagnosis | Unfiltered cancellation escapes as an unstructured tool error. |
| Approach | Project timeout/cancellation into the documented structured failure envelope and add controlled coverage. |
| Scope | Production files (2): src/RoslynMcp.Host.Stdio/Tools/ValidationTools.cs, src/RoslynMcp.Roslyn/Services/TestRunnerService.cs. Test files (1): tests/RoslynMcp.Tests/ValidationToolsTests.cs. |
| Tool policy | edit-only |
| Estimated context cost | 45000 |
| Risks | Preserve stable wire behavior and avoid unrelated edits. |
| Validation | Focused regression and ./eng/verify-release.ps1 -Configuration Release. |
| Performance review | N/A — no hot-path change. |
| CHANGELOG category | Fixed |
| CHANGELOG entry (draft) | Fix test-run-full-suite-timeout-envelope. |
| Backlog sync | Close rows: [test-run-full-suite-timeout-envelope]. Mark obsolete: []. |

### 11. reconcile-plan-force-worktree-removal-safety

| Field | Content |
|---|---|
| Route | direct |
| Diagnosis | Normal reconciliation recommends force-removal of retained worktrees. |
| Approach | Refuse dirty normal worktrees, retain the disposable-audit exception, and extend the static verifier. |
| Scope | Production files (1): .claude/skills/reconcile-backlog-sweep-plan/SKILL.md. Test files (0): . |
| Tool policy | edit-only |
| Estimated context cost | 25000 |
| Risks | Preserve stable wire behavior and avoid unrelated edits. |
| Validation | Focused regression and ./eng/verify-release.ps1 -Configuration Release. |
| Performance review | N/A — no hot-path change. |
| CHANGELOG category | Fixed |
| CHANGELOG entry (draft) | Fix reconcile-plan-force-worktree-removal-safety. |
| Backlog sync | Close rows: [reconcile-plan-force-worktree-removal-safety]. Mark obsolete: []. |

### 12. scripting-startup-cancellation-causal-barrier

| Field | Content |
|---|---|
| Route | direct |
| Diagnosis | `tests/RoslynMcp.Tests/ScriptingServiceTests.cs:83` still waits on the causal `FirstStartEntered` barrier through a per-await `WaitAsync(TimeSpan.FromSeconds(5))`, and the test (`:68`) has no method-level `[Timeout]` and runs once. Dep row `scripting-supervisor-outer-cancellation-contended-timeout` is absent from the backlog (shipped in PR #1506), so the dep is satisfied. |
| Approach | Acceptance, verbatim: (1) The startup-cancellation regression observes worker startup through an explicit causal barrier instead of a per-await wall-clock timeout. (2) The test retains a method-level deadlock guard and proves cancellation releases capacity before a succeeding recovery evaluation. (3) The controlled shape remains repeatable enough to expose a startup/cancellation ordering regression. Mirror the sibling `ControlledCancellationRepetitions` (=5) loop and `[Timeout(60_000)]` guard already used in the same class; give `CancellableStartWorkerProcess` a per-repetition barrier. |
| Scope | Production files (0). Test files (1): `tests/RoslynMcp.Tests/ScriptingServiceTests.cs`. |
| Tool policy | edit-only |
| Estimated context cost | 20000 |
| Risks | Fake worker must reset its first-start barrier per repetition or repetitions 2–5 prove nothing. Keep `MaxConcurrentEvaluations = 1` so the recovery evaluation is the capacity proof. |
| Validation | `dotnet test --filter FullyQualifiedName~ScriptingServiceTests` then addenda `ci_equivalent`. |
| Performance review | N/A — test-only change. |
| CHANGELOG category | Maintenance |
| CHANGELOG entry (draft) | Startup-cancellation scripting regression now uses a causal barrier, a method-level deadlock guard, and five controlled repetitions instead of a per-await wall-clock timeout. |
| Backlog sync | Close rows: [scripting-startup-cancellation-causal-barrier]. Mark obsolete: []. |

### 13. apply-composite-preview-destructive-misnomer

| Field | Content |
|---|---|
| Route | direct |
| Diagnosis | Partially shipped by PR #471: `src/RoslynMcp.Host.Stdio/Tools/OrchestrationTools.cs:94-97` and `src/RoslynMcp.Host.Stdio/Catalog/ServerSurfaceCatalog.Orchestration.cs:32` already lead with `DESTRUCTIVE`, and `tests/RoslynMcp.Tests/CatalogDestructiveWarningTests.cs:16` asserts it. Still open: the acceptance clause "if the name stays, document loudly why" (neither text says why the `_preview` suffix is retained), and no guard stops a second non-read-only `_preview`-suffixed tool — live catalog scan shows `apply_composite_preview` is today the only `_preview` entry that is not `readOnly: true, destructive: false`. |
| Approach | Acceptance, verbatim: (1) Tool + catalog entry renamed (`apply_composite`), OR a description warning added that this `_preview`-suffixed tool actually applies — if the name stays, document loudly why. (2) Regression: catalog test asserts name + Destructive flag are consistent with the tool's semantics. Keep the published name (Directive #4 — public package; a rename is an ADR-level contract change not justified by a naming nit). Add the retention rationale (the suffix names the composite *preview token* it redeems; name kept for published-contract stability) to the `[Description]`, `McpToolMetadata` summary, and catalog summary in lockstep. Add a catalog-wide test: every `_preview`-suffixed tool is read-only and non-destructive except an explicit allowlist `{apply_composite_preview}` whose description must state the rationale. |
| Scope | Production files (2): `src/RoslynMcp.Host.Stdio/Tools/OrchestrationTools.cs`, `src/RoslynMcp.Host.Stdio/Catalog/ServerSurfaceCatalog.Orchestration.cs`. Test files (1): `tests/RoslynMcp.Tests/CatalogDestructiveWarningTests.cs`. Rule 3 exemption: tool-surface-only, 2 files — description text only, no count change, so the README companions are dropped. |
| Tool policy | edit-only |
| Estimated context cost | 25000 |
| Risks | `MethodDescriptionDietRatchetOnlyCleanSlicesTests.cs:46-48` pins required substrings and a description byte budget — keep those substrings and stay inside the budget. RMCP001/RMCP002 require attribute summary and catalog summary to agree. |
| Validation | `dotnet test --filter "FullyQualifiedName~CatalogDestructiveWarningTests|FullyQualifiedName~MethodDescriptionDiet|FullyQualifiedName~SurfaceCatalog"` then addenda `ci_equivalent`. |
| Performance review | N/A — description text and a test. |
| CHANGELOG category | Changed |
| CHANGELOG entry (draft) | `apply_composite_preview` now states why its `_preview` suffix is retained, and a catalog test rejects any other `_preview`-suffixed tool that is not read-only. |
| Backlog sync | Close rows: [apply-composite-preview-destructive-misnomer]. Mark obsolete: []. |

### 14. change-signature-callsite-summary-stale-row-comments

| Field | Content |
|---|---|
| Route | direct |
| Diagnosis | The limitation is no longer live: `CHANGELOG.md:1620` records `change-signature-preview-callsite-summary` as shipped (related-symbol walk + `CallsiteUpdates` summary, `src/RoslynMcp.Roslyn/Services/ChangeSignatureService.cs:453,483`), pinned by `ChangeSignaturePreview_AddOp_OnInterfaceMethod_EnumeratesAllImplementerCallsiteFiles` (`tests/RoslynMcp.Tests/ChangeSignaturePreviewTests.cs:178`). Yet `skills/mcp-server-surface-test/prompts/phases/apply-and-test.md:87` still tells auditors the preview only enumerates the declaration-owner file (a false "Known limitation — backlog `change-signature-preview-callsite-summary`"), and `ChangeSignaturePreviewTests.cs:168,452` cite the dead id as if tracked. |
| Approach | Acceptance, verbatim: (1) Decided whether the callsite-summary limitation remains live; either a bounded backlog row restored for it, or the comments rewritten to describe shipped behavior/current limitation without a stale row id. (2) Regression: static grep asserts `change-signature-preview-callsite-summary` appears only if the id exists in `ai_docs/backlog.md`. Decision: not live — rewrite the prompt step to verify the shipped behavior (preview enumerates every callsite file; `callsiteUpdates` summarizes per-file counts) and rewrite both test comments without the stale id. Generalize the regression: a skills test asserting every "backlog `<id>`" citation in shipped `skills/**/*.md` resolves to a live `ai_docs/backlog.md` row. |
| Scope | Production files (1): `skills/mcp-server-surface-test/prompts/phases/apply-and-test.md`. Test files (2): `tests/RoslynMcp.Tests/ChangeSignaturePreviewTests.cs` (comments only), new `tests/RoslynMcp.Tests/Skills/ShippedSkillBacklogCitationTests.cs`. |
| Tool policy | edit-only |
| Estimated context cost | 25000 |
| Risks | Shipped-skill edit → `eng/verify-skills-on-edit.ps1` PostToolUse + genericity gate; avoid banned markers (`ai_docs/`, `backlog.md`) in the prompt text. The new test must build the stale id at run time or scan generically so it does not itself cite the id. `skills/` edit makes this a code PR under the CI topology rule. |
| Validation | `dotnet test --filter "FullyQualifiedName~ShippedSkillBacklogCitationTests|FullyQualifiedName~McpServerSurfaceTestSkillTests"`; `./eng/verify-skills-are-generic.ps1`; then addenda `ci_equivalent`. |
| Performance review | N/A — docs and a static test. |
| CHANGELOG category | Fixed |
| CHANGELOG entry (draft) | The surface-test apply phase no longer reports the long-fixed `change_signature_preview` callsite-summary gap as a known limitation, and a static test rejects shipped-skill citations of backlog rows that no longer exist. |
| Backlog sync | Close rows: [change-signature-callsite-summary-stale-row-comments]. Mark obsolete: []. |

### 15. surface-test-shipped-prompt-local-skill-reference

| Field | Content |
|---|---|
| Route | direct |
| Diagnosis | Prose half shipped in PR #1240 (item amendment 2026-08-13). Guard half open: `eng/banned-skill-markers.json` `bannedPatterns` has no `\.claude/` entry, so the gate (`eng/verify-skills-are-generic.ps1:55`) and its C# echo (`tests/RoslynMcp.Tests/Skills/McpServerSurfaceTestSkillTests.cs:286`) both accept a shipped `.claude/skills/<any>/SKILL.md` citation. Two live citations would trip a new pattern: `skills/update/SKILL.md:48` and `skills/mcp-server-surface-test/prompts/full.md:25`. The PowerShell gate reads the shared JSON, so the release-managed `.ps1` needs no edit. |
| Approach | Acceptance, verbatim: (1) Reference replaced with portable wording or a public/repo-shipped anchor. (2) Considered: `eng/verify-skills-are-generic.ps1` rejecting `.claude/skills/` references in shipped prompts. (3) Regression: static check proves shipped prompt files do not reference `.claude/skills/` unless an explicit allowlist entry documents why. Amendment: `\.claude/` added to `$bannedPatterns` and to the C# mirror; a shipped file citing `.claude/skills/<any>/SKILL.md` fails the gate. Add `\.claude/` to the shared policy (both gate and mirror consume it), reword the two live citations portably, and add a `GenericityPolicy_ReproducedLaunderingShapes_RemainVisible` DataRow for `.claude/skills/bump/SKILL.md`. |
| Scope | Production files (3): `eng/banned-skill-markers.json`, `skills/update/SKILL.md`, `skills/mcp-server-surface-test/prompts/full.md`. Test files (1): `tests/RoslynMcp.Tests/Skills/McpServerSurfaceTestSkillTests.cs`. `eng/verify-skills-are-generic.ps1` is NOT edited (release-managed; consumes the JSON unchanged). |
| Tool policy | edit-only |
| Estimated context cost | 25000 |
| Risks | `\.claude/` must not match `.claude-plugin/` (it cannot — slash required) or URL-stripped GitHub links. Rewording must preserve meaning for maintainers without naming a maintainer-local path. Skill edits trip the PostToolUse genericity hook in-session. |
| Validation | `./eng/verify-skills-are-generic.ps1`; `dotnet test --filter FullyQualifiedName~McpServerSurfaceTestSkillTests`; then addenda `ci_equivalent`. |
| Performance review | N/A — policy data and docs. |
| CHANGELOG category | Fixed |
| CHANGELOG entry (draft) | The shipped-skill genericity gate now rejects `.claude/` maintainer-local path citations; the two remaining citations were reworded portably. |
| Backlog sync | Close rows: [surface-test-shipped-prompt-local-skill-reference]. Mark obsolete: []. |
