| Field | Content |
|---|---|
| Route | `direct` |
| Diagnosis | Each anchored file carries `[DoNotParallelize]` (grep-confirmed at selection). No source-adjacent comment is required to exist yet naming the concrete process-global/shared-workspace dependency. Classes reading only through the synchronized `WorkspaceIdCache` (`tests/RoslynMcp.Tests/TestInfrastructure/WorkspaceIdCache.cs`) with no shared mutable state are removal candidates; classes that mutate shared server-side, static, environment, or process state (workspace reload, `*_apply`, static caches, child `dotnet` builds against shared fixtures) still need serialization. Per-file decision is made against each file's body during implementation. |
| Approach | Row Acceptance verbatim, per anchored file (`tests/RoslynMcp.Tests/PreviewRouteBindingEditingTests.cs`, `tests/RoslynMcp.Tests/Progress/ProgressEmissionTests.cs`, `tests/RoslynMcp.Tests/ProjectFilterTests.cs`): (1) for every `[DoNotParallelize]`, either remove it after repeated concurrent evidence or retain it with a source-adjacent comment naming the concrete process-global/shared-workspace dependency; (2) run each affected class repeatedly before and after the decision — a retained opt-out cites the observed failure mechanism, a removed opt-out stays green across the bounded repetition (>=3x); (3) do not weaken assertions, add sleeps, or classify unrelated concurrency failures as success. |
| Scope | Test files (3): `tests/RoslynMcp.Tests/PreviewRouteBindingEditingTests.cs`, `tests/RoslynMcp.Tests/Progress/ProgressEmissionTests.cs`, `tests/RoslynMcp.Tests/ProjectFilterTests.cs`. No production files. |
| Tool policy | `edit-only` |
| Estimated context cost | 28000 |
| Risks | A wrongly cleared class surfaces as an intermittent CI failure later; mitigated by >=3x repeated-run evidence and by inspecting base classes and child-process use (the wave-07 hold: `ExpandedSurfaceIntegrationTests_CoverageProcess` spawned a real `dotnet test` against shared `SampleLib.Tests`). Sibling waves touch disjoint file sets. Fanout probe: test-only, 0 production ripple. |
| Validation | Per class: `test_run --filter "FullyQualifiedName~<ClassName>"` (or `dotnet test --filter`) repeated >=3x, then with wave siblings concurrently; full addenda `ci_equivalent` (`just ci`) before PR. |
| Performance review | N/A — correctness fix, no hot-path changes. |
| CHANGELOG category | Maintenance |
| CHANGELOG entry (draft) | Re-audited `[DoNotParallelize]` opt-outs on `PreviewRouteBindingEditingTests.cs`, `ProgressEmissionTests.cs`, `ProjectFilterTests.cs` — removed opt-outs proven safe by repeated concurrent runs and documented retained ones with their concrete shared-state dependency. |
| Backlog sync | Close rows: [donotparallelize-audit-wave-20]. Mark obsolete: []. |
