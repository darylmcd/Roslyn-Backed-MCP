# Backlog sweep plan — 20260825T214500Z_backlog-sweep

Generated by `/backlog-sweep:prepare`. 15 initiative(s) selected off `ai_docs/backlog.md` (snapshot 2026-08-25T21:13:14Z).

<!-- BSWEEP:STATUS-TABLE BEGIN — generated from state.json; do not edit by hand -->
## Status (generated)

| # | id | status | PR | rows closed |
|---|----|--------|----|-------------|
| 1 | preview-token-route-map-centralization | merged | [#1362](https://github.com/darylmcd/Roslyn-Backed-MCP/pull/1362) | — |
| 3 | preview-token-route-binding-extraction-family | pending | — | — |
| 4 | preview-token-route-binding-orchestration-family | pending | — | — |
| 5 | preview-token-route-binding-bulk-family | pending | — | preview-token-apply-route-binding-remaining-families |
| 6 | shipped-skills-prefix-batch-c1 | merged | [#1359](https://github.com/darylmcd/Roslyn-Backed-MCP/pull/1359) | — |
| 7 | shipped-skills-prefix-batch-c2 | merged | [#1370](https://github.com/darylmcd/Roslyn-Backed-MCP/pull/1370) | shipped-skills-hardcode-bare-roslyn-tool-prefix |
| 8 | method-description-diet-analysis-metrics | merged | [#1361](https://github.com/darylmcd/Roslyn-Backed-MCP/pull/1361) | — |
| 9 | method-description-diet-diagnostics-security | merged | [#1360](https://github.com/darylmcd/Roslyn-Backed-MCP/pull/1360) | — |
| 10 | method-description-diet-server-surface | merged | [#1364](https://github.com/darylmcd/Roslyn-Backed-MCP/pull/1364) | — |
| 11 | method-description-diet-test-tooling | merged | [#1363](https://github.com/darylmcd/Roslyn-Backed-MCP/pull/1363) | — |
| 12 | method-description-diet-editing-msbuild | merged | [#1365](https://github.com/darylmcd/Roslyn-Backed-MCP/pull/1365) | — |
| 13 | param-description-dedupe-workspace-ops | merged | [#1366](https://github.com/darylmcd/Roslyn-Backed-MCP/pull/1366) | — |
| 14 | param-description-dedupe-refactoring-core | merged | [#1372](https://github.com/darylmcd/Roslyn-Backed-MCP/pull/1372) | — |
| 15 | param-description-dedupe-validation-signature | merged | [#1367](https://github.com/darylmcd/Roslyn-Backed-MCP/pull/1367) | — |
| 2.01 | preview-token-route-binding-editing-substrate | merged | [#1371](https://github.com/darylmcd/Roslyn-Backed-MCP/pull/1371) | — |
| 2.02 | preview-token-route-binding-multifile-codeaction | pending | — | — |
| 2.03 | preview-token-route-binding-fileops-fixall | pending | — | — |
<!-- BSWEEP:STATUS-TABLE END -->

## Initiatives

### 1. preview-token-route-map-centralization

| Field | Content |
|---|---|
| Diagnosis | All cited anchors verified live on `main` (2026-08-25). The kind→route knowledge is encoded **four** times, not three: (1) `ToolDispatch.PreviewToolFor` at `src/RoslynMcp.Host.Stdio/Tools/ToolDispatch.cs:207-215` (switch over `PreviewKind`, silent catch-all `_ => "an untagged *_preview tool"`); (2) `ToolDispatch.ApplyRouteFor` at `src/RoslynMcp.Host.Stdio/Tools/ToolDispatch.cs:221-229` (parallel switch, silent catch-all `_ => "apply_with_verify"`); (3) the five `applyRoute:` string literals at `src/RoslynMcp.Host.Stdio/Tools/RefactoringTools.cs:60/95/130/169/210` (each redundant with the `expectedKind:` on the line above it); and — **found by this pass's probe, NOT named in the backlog row** — (4) `ServerSurfaceCatalog.PreviewApplyRoutes` at `src/RoslynMcp.Host.Stdio/Catalog/ServerSurfaceCatalog.cs:42-69`, a 24-entry `previewTool → applyRoute` dictionary already covering all five concrete `PreviewKind` members' preview tools (`rename_preview`, `organize_usings_preview`, `format_document_preview`, `code_fix_preview`, `format_range_preview`), exposed via the same-assembly `internal TryGetCompatibleApplyRoute` at `:242-246`. `PreviewKind` (`src/RoslynMcp.Core/Models/PreviewKind.cs:21-43`) has exactly 5 concrete members plus `Unspecified = 0`. The concrete failure the row predicts is real: a new concrete member added without editing both switches falls through both catch-alls, so `RequireCompatibleProducer` (`ToolDispatch.cs:196-200`) emits a factually wrong, actionable-looking message telling the caller the token came from "an untagged *_preview tool" and to redeem via `apply_with_verify`. |
| Approach | (1) In `src/RoslynMcp.Host.Stdio/Tools/ToolDispatch.cs`, replace the two parallel switches with ONE `static readonly` map keyed by `PreviewKind` holding the `*_preview` tool name (`PreviewKind.SymbolRename => "rename_preview"`, etc.). Derive the apply-route half from the existing catalog rather than re-listing it: `ApplyRouteFor(kind)` looks the kind's preview-tool name up in the map, then calls `ServerSurfaceCatalog.TryGetCompatibleApplyRoute(previewTool, out var route)`. That collapses encodings (1),(2),(4) into a single source of truth — `PreviewApplyRoutes` — with only the kind→previewTool hop living in `ToolDispatch`. (2) Fail loud: when `kind` is concrete and either the map lookup or the catalog lookup misses, throw `InvalidOperationException` naming the unmapped member and pointing at both map sites, instead of returning the `"an untagged *_preview tool"` / `"apply_with_verify"` catch-all strings. `PreviewKind.Unspecified` keeps its documented permissive fall-through (`ToolDispatch.cs:185-188` early return is unchanged) — the fail-loud arm must never fire for `Unspecified`. (3) Delete the now-redundant `string? applyRoute` parameter from `ApplyByTokenAsync` (`ToolDispatch.cs:133`) and `RequireCompatibleProducer` (`ToolDispatch.cs:183`), and its `<param>` doc at `:109`; the invoked-route half of the message becomes `ApplyRouteFor(expectedKind)` unconditionally (it is already the fallback at `:199`). (4) Delete the five `applyRoute:` literals at `src/RoslynMcp.Host.Stdio/Tools/RefactoringTools.cs:60/95/130/169/210`, leaving each call site with `expectedKind:` only — the exact shape children 2-5 will copy. (5) Add a pin test in `tests/RoslynMcp.Tests/ToolDispatchTests.cs` iterating `Enum.GetValues<PreviewKind>()` and asserting every non-`Unspecified` member resolves to BOTH a preview tool and a catalog apply route, and that an unmapped concrete kind throws rather than returning a catch-all string. Mirror the existing message-assertion style at `ToolDispatchTests.cs:63-68`. **Fallback if the executor judges the `ToolDispatch → ServerSurfaceCatalog` reach objectionable:** keep a self-contained `kind → (previewTool, applyRoute)` record map in `ToolDispatch.cs` and add a coherence assertion in the same pin test that each pair agrees with `ServerSurfaceCatalog.PreviewApplyRoutes`. Same file count, same Rule 3/4 budget. **Do NOT bind any new apply family here** — orders 2-5 do that on top of this map. |
| Scope | Production files: 2 — `src/RoslynMcp.Host.Stdio/Tools/ToolDispatch.cs`, `src/RoslynMcp.Host.Stdio/Tools/RefactoringTools.cs`. Test files modified: 1 — `tests/RoslynMcp.Tests/ToolDispatchTests.cs`. Files deleted: none. Within Rule 3 (2 ≤ 4) and Rule 4 (1 ≤ 3); **no exemption claimed**. `src/RoslynMcp.Host.Stdio/Catalog/ServerSurfaceCatalog.cs` is READ (its existing `internal TryGetCompatibleApplyRoute`) but NOT edited, so the addenda hotspot wave-rule for the catalog is not engaged. Negative space honored: no other `*Tools.cs` file is touched. |
| Tool policy | edit-only |
| Estimated context cost | 38000 |
| Risks | (a) Removing the optional `applyRoute` parameter is a signature change — probe confirms only 2 production files and 1 test file pass it (5 literals in `RefactoringTools.cs`, 3 in `ToolDispatchTests.cs:60/92/118`); the other 14 `ApplyByTokenAsync` callers never pass it and are source-unaffected. (b) The three existing message assertions at `ToolDispatchTests.cs:63-68` (`code_fix_preview` / `code_fix_apply` / `rename_apply`) must still pass unchanged — the invoked-route string is now derived from `expectedKind: PreviewKind.SymbolRename`, which yields the same `rename_apply`; if it does not, the map is wrong. (c) The fail-loud arm must not fire for `PreviewKind.Unspecified` — `ApplyByTokenAsync_UnboundRoute_DoesNotEnforceProvenance` (`ToolDispatchTests.cs:130-147`) and `ApplyByTokenAsync_UnspecifiedProducer_SkipsGuard_AndApplies` (`:105-122`) both pin that permissive contract and are the regression canaries; **do not retire either here** — retiring the unbound-route escape is child 5's job (order 5). (d) File-overlap edge: `RefactoringTools.cs` is also owned by initiative `param-description-dedupe-refactoring-core` (order 14), whose negative space explicitly reserves these routing lines for this initiative — the two must not share a parallel wave. (e) `size: L` on the source row describes the PARENT (15 anchored files); this Phase-A child slice probes at 2 production files, so `fanoutOversize` is false despite the `size_is_L` prior. (f) No anchors were stale. |
| Validation | `mcp__roslyn__compile_check` on `RoslynMcp.Host.Stdio` after each edit (read-side, safe in the main checkout under the addenda self-edit caveat; `*_preview`/`*_apply` are forbidden there). Then `mcp__roslyn__test_run --filter "ToolDispatchTests"` — all existing tests must pass UNCHANGED except for the three mechanical `applyRoute:` argument deletions, plus the new exhaustiveness pin. Also run `--filter "StartupDiagnosticsTests"` (it exercises `TryGetCompatibleApplyRoute` at `StartupDiagnosticsTests.cs:285-290`) and `--filter "PreviewApplyBoundaryRevalidationTests"` (6 `ApplyByTokenAsync` call sites, none passing `applyRoute`). Negative check: temporarily add a scratch concrete `PreviewKind` member locally and confirm the pin test FAILS loudly rather than silently emitting `apply_with_verify`; revert before commit. CI gate: `./eng/verify-release.ps1 -Configuration Release` runs on the self-hosted runner via PR — do not duplicate it locally; `dotnet build RoslynMcp.slnx -c Release -p:TreatWarningsAsErrors=true` is the local minimum substitute. |
| Performance review | N/A — correctness/structure refactor on a redemption-time guard that runs once per apply call; a dictionary lookup replaces a switch, no hot-path change. |
| CHANGELOG category | Maintenance |
| CHANGELOG entry (draft) | Centralized the preview-token `PreviewKind` → (`*_preview` tool, `*_apply` route) mapping in `ToolDispatch`, sourcing the apply-route half from `ServerSurfaceCatalog.PreviewApplyRoutes` instead of a third and fourth hand-maintained copy, and removed the redundant `applyRoute` parameter from `ApplyByTokenAsync`. An unmapped concrete `PreviewKind` now fails loudly at redemption instead of silently reporting the token as coming from "an untagged \*_preview tool" and directing the caller to `apply_with_verify`; a new test pins that every non-`Unspecified` member has a mapping. Fragment: `changelog.d/preview-token-route-map-centralization.md`. |
| Backlog sync | Close rows: none — this is child 1 of 5 of `preview-token-apply-route-binding-remaining-families`; the row stays open until the four family-binding children (orders 2-5) land. Mark obsolete: none. Update related: `preview-token-apply-route-binding-remaining-families` (record in its detail file that the triple-encoding is in fact a QUADRUPLE encoding — `ServerSurfaceCatalog.PreviewApplyRoutes` at `src/RoslynMcp.Host.Stdio/Catalog/ServerSurfaceCatalog.cs:42-69` is the fourth site and is now the single source for the apply-route half). |

<details>
<summary>Executor handoff notes</summary>

**Executor handoff:**

- **Pattern coordinates (verified 2026-08-25):** `ToolDispatch.cs:207-215` / `:221-229` are the two switches to collapse; `ServerSurfaceCatalog.cs:42-69` is the `PreviewApplyRoutes` dictionary; `ServerSurfaceCatalog.cs:242-246` is `internal static bool TryGetCompatibleApplyRoute(string, out string?)` — same assembly as `ToolDispatch`, so the reach compiles with no visibility change.
- **Visibility gotcha:** `PreviewToolFor` / `ApplyRouteFor` are `private static` (`:207`, `:221`). The pin test cannot call them as-is. `RoslynMcp.Host.Stdio.csproj:47` already declares `InternalsVisibleTo("RoslynMcp.Tests")` — promote both to `internal static` and assert directly; do NOT reach them via reflection.
- **Test target:** add the pin test to `ToolDispatchTests.cs` (class at `:29`); mirror the `StringAssert.Contains` + rationale-message style at `:63-68`. Drive the unmapped-concrete-kind arm with `(PreviewKind)999` — a concrete-but-undefined value that misses the map without adding a scratch enum member.
- **Mechanical deletions:** `applyRoute:` is the LAST argument at `RefactoringTools.cs:60/95/130/169/210` and `ToolDispatchTests.cs:60/92/118` — remove the trailing comma on the preceding `expectedKind:` line in each.
- **Negative space:** do NOT edit `ServerSurfaceCatalog.cs` (read-only here — its 24 entries already cover all five kinds); do NOT retire `ApplyByTokenAsync_UnboundRoute_DoesNotEnforceProvenance` (`:130-147`) or `..._UnspecifiedProducer_SkipsGuard_AndApplies` (`:105-122`) — those are child 5's; touch no other `*Tools.cs`.

**FAMILY CONTRACT — cross-initiative correction (orchestrator, post-review cycle 2; resolves the surviving `3` / `3b` warn findings recorded in `review.md`). Read this before following the Approach verbatim.**

- **This initiative OWNS the `applyRoute:` parameter deletion.** Every downstream family child (2.01, 2.02, 2.03, 3, 4, 5) has been briefed that `applyRoute:` no longer exists after this PR and that they bind with `expectedKind:` only. Do not soften step (3) into "keep the parameter for compatibility" — the children’s briefs now depend on the deletion actually landing.
- **Scope the pin test to the members that exist at THIS PR’s base.** Verified live on `main` 2026-08-25: `PreviewKind` has 5 concrete members and `ServerSurfaceCatalog.PreviewApplyRoutes` (`src/RoslynMcp.Host.Stdio/Catalog/ServerSurfaceCatalog.cs:42-69`) covers all five of their preview-tool names, so the "every non-`Unspecified` member resolves to a catalog apply route" pin holds today. It only becomes a constraint as children add members — the intended forcing function, and the children have been briefed to add both companions. Do NOT edit `ServerSurfaceCatalog.cs` here: it stays read-only, keeping this initiative at 2 production files and off the addenda hotspot wave-rule.

</details>

### 3. preview-token-route-binding-extraction-family

| Field | Content |
|---|---|
| Diagnosis | Verified on `main` 2026-08-25. Each of the four owned files carries EXACTLY ONE `ToolDispatch.ApplyByTokenAsync` call and all four are unbound (no `expectedKind`, no `applyRoute`): `src/RoslynMcp.Host.Stdio/Tools/ExtractMethodTools.cs:51` (`extract_method_apply`), `src/RoslynMcp.Host.Stdio/Tools/InterfaceExtractionTools.cs:50` (`extract_interface_apply`), `src/RoslynMcp.Host.Stdio/Tools/TypeExtractionTools.cs:55` (`extract_type_apply`), `src/RoslynMcp.Host.Stdio/Tools/TypeMoveTools.cs:54` (`move_type_to_file_apply`). The guard they bypass is `ToolDispatch.RequireCompatibleProducer` at `src/RoslynMcp.Host.Stdio/Tools/ToolDispatch.cs:180-201`, invoked from inside `gate.RunWriteAsync` at `:140` before `RevalidateChangedPathsAsync` and before the service call. **Load-bearing finding the slice text did not anticipate:** `src/RoslynMcp.Core/Models/PreviewKind.cs:21-43` declares only FIVE concrete members (`SymbolRename`, `FormatDocument`, `FormatRange`, `OrganizeUsings`, `CodeFix`), all five already consumed by the `RefactoringTools.cs:59/94/129/168/209` bindings. There is no existing kind an extraction route could bind to, so this child is not a pure four-file edit-in-place — it must first MINT four new concrete `PreviewKind` members, which is a fan-in (new enum member) rather than a per-target edit. Fail-open semantics make the binding safe without producer-side tagging: `RequireCompatibleProducer` returns early when the stored kind is `Unspecified` (`ToolDispatch.cs:190-194`), and all four extraction producers store untagged today (`ExtractMethodService.cs:83`, `InterfaceExtractionService.cs:184`, `TypeExtractionService.cs:142`, `TypeMoveService.cs:174` all call the no-kind `Store` overload). |
| Approach | 1. `src/RoslynMcp.Core/Models/PreviewKind.cs` — append four concrete members `ExtractMethod`, `ExtractInterface`, `ExtractType`, `MoveTypeToFile`, each with a one-line XML doc mirroring the existing five. Append only; never renumber, the enum is persisted in `PreviewStore` entries. 2. Register each new member's `previewTool` / `applyRoute` pair in the centralized map introduced by initiative `preview-token-route-map-centralization` — pairs are `extract_method_preview`→`extract_method_apply`, `extract_interface_preview`→`extract_interface_apply`, `extract_type_preview`→`extract_type_apply`, `move_type_to_file_preview`→`move_type_to_file_apply`. That initiative's pin test asserts every non-`Unspecified` member has a mapping, so adding members without pairs turns its own gate red. **Seam requirement:** the map must be extensible from outside `ToolDispatch.cs` — a Core-side table or per-member metadata next to the enum. If order 1 lands it as a `ToolDispatch`-private switch, STOP and flag for remediation rather than editing `ToolDispatch.cs`, which this slice's negative space forbids and which siblings 2/4/5 would collide on for the same reason. 3. Bind the four routes by adding two trailing arguments to each `ApplyByTokenAsync` call, byte-for-byte mirroring the pattern at `src/RoslynMcp.Host.Stdio/Tools/RefactoringTools.cs:51-61` including its two-line `preview-token-apply-route-provenance` rationale comment. 4. **Explicitly OUT of scope:** producer-side tagging of the four services. It is a one-line change each but adds four more production files and busts every Rule-3 ceiling; file it as a follow-on row at execute time. Untagged producers stay permissive by the enum's documented contract, so the bindings are correct without it — they refuse foreign-family tokens today and become symmetric when tagging lands. |
| Scope | Production files (7): `src/RoslynMcp.Core/Models/PreviewKind.cs`, `src/RoslynMcp.Host.Stdio/Tools/ToolDispatch.cs`, `src/RoslynMcp.Host.Stdio/Catalog/ServerSurfaceCatalog.cs`, `src/RoslynMcp.Host.Stdio/Tools/ExtractMethodTools.cs`, `src/RoslynMcp.Host.Stdio/Tools/InterfaceExtractionTools.cs`, `src/RoslynMcp.Host.Stdio/Tools/TypeExtractionTools.cs`, `src/RoslynMcp.Host.Stdio/Tools/TypeMoveTools.cs`. **Rule 3 exemption: gate-forced-companion — gate: the C# compiler (CS0117 on an undeclared enum member) plus the `PreviewKindRouteMap_EveryConcreteKind_ResolvesPreviewToolAndApplyRoute` pin test added by order 1; evidence: binding the four routes to `PreviewKind.ExtractMethod` / `ExtractInterface` / `ExtractType` / `MoveTypeToFile` emits four CS0117 errors until the members exist in `PreviewKind.cs`; each new member additionally needs a `ToolDispatch` kind->previewTool map row or the pin test fails; and `ServerSurfaceCatalog.PreviewApplyRoutes` (`ServerSurfaceCatalog.cs:42-69`) has NO `extract_interface_preview` key at this base (verified live 2026-08-26 — it holds `extract_method_preview`, `extract_type_preview` and `move_type_to_file_preview` but not `extract_interface_preview`), so `ApplyRouteFor(PreviewKind.ExtractInterface)` throws and the same pin test fails unless that entry is added here; total: 4 base + 3 additional = 7 <= 7.** Because this initiative edits `ServerSurfaceCatalog.cs`, the addenda hotspot wave-rule (<=1 catalog-touching initiative per parallel wave) IS engaged — schedule accordingly. Test files added/modified (2, Rule 4 satisfied: 2 <= 3): new `tests/RoslynMcp.Tests/ExtractionApplyRouteBindingTests.cs`, and `tests/RoslynMcp.Tests/ToolDispatchTests.cs` (gate-forced: order 1's content pin ends in `CollectionAssert.AreEquivalent(concreteKinds, expected)`, so four new members require four new expected rows). Deliberately NOT touched: every `*Tools.cs` outside the four named files, and the four producer services. `PreviewKind.cs` and `ToolDispatch.cs` are shared with sibling family children 4/5 — a real conflict edge; these children serialize rather than parallelize. |
| Tool policy | edit-only |
| Estimated context cost | 48000 |
| Risks | Fanout probe (shape C, new enum member) run over `src` and `tests`: the must-touch set is 6 files — `PreviewKind.cs`, the centralized map file, and the 4 owned tool files. No exhaustive `switch` over `PreviewKind` outside `ToolDispatch.PreviewToolFor`/`ApplyRouteFor` at `:207-217`/`:221-231`, no `Enum.GetValues(typeof(PreviewKind))` anywhere in `src`, and `ApplyWithVerifyTool.cs`'s single mention is a doc comment on the deliberately producer-agnostic route — so the fan-in is bounded at the map plus order 1's pin test. Adjacent behavior to verify: `apply_with_verify` must keep redeeming extraction tokens (it never calls the guard); `revert_apply` coverage of these routes is unaffected; `extract_shared_expression_to_helper_preview` (`ExtractMethodService.cs:589`) shares the service but has NO named apply route, so it must NOT be tagged `ExtractMethod` when producer tagging lands — a wrong tag there would make its `apply_with_verify` redemption look like a mismatched extract-method token. Seam risk repeated from Approach: if order 1's map is `ToolDispatch`-private, this child cannot register its pairs without breaking its negative space. Bad code observed while probing, recommend a backlog row, do NOT fix here: `src/RoslynMcp.Host.Stdio/Catalog/ServerSurfaceCatalog.cs:42-69` holds `PreviewApplyRoutes`, a FOURTH encoding of the same preview→apply knowledge, and it is MISSING an `extract_interface_preview` entry while carrying `extract_method`, `extract_type`, and `move_type_to_file` — so `TryGetCompatibleApplyRoute` at `:241-245` reports no pairing and tier selection at `:235-238` can surface `extract_interface_preview` in a tier that excludes `extract_interface_apply`. |
| Validation | Per-edit `mcp__roslyn__compile_check` on `RoslynMcp.Core` then `RoslynMcp.Host.Stdio`. New tests in `tests/RoslynMcp.Tests/ExtractionApplyRouteBindingTests.cs`, using local fakes modelled on the `FakeGate` / `FakePreviewStore` nested in `ToolDispatchTests.cs` — the fakes are private there, so duplicate them locally rather than widening a shared file: (a) four wrong-kind cases, one per route, with the fake store returning `PreviewKind.SymbolRename` and a throwing `IRefactoringService`, asserting `InvalidOperationException` whose message names both `rename_apply` and the invoked route and proving the service was never called; (b) one permissive case with the store's kind left `Unspecified`, asserting the apply still succeeds. Then `dotnet test --filter "FullyQualifiedName~ExtractionApplyRouteBinding"` plus the existing `ToolDispatchTests` and `PreviewStoreTests` classes, and finally `./eng/verify-release.ps1 -Configuration Release`. Manual repro: mint a token with `rename_preview`, redeem it through `extract_method_apply` — pre-change it reaches `ApplyRefactoringAsync`, post-change it is refused before any mutation. |
| Performance review | N/A — correctness fix. The guard is one non-consuming `PeekKind` lookup plus a map hit on a redemption path that already performs a solution-wide apply; no hot-path change. |
| CHANGELOG category | Fixed |
| CHANGELOG entry (draft) | Fragment `changelog.d/preview-token-route-binding-extraction-family.md` with `category: Fixed` — "**Fixed:** the four extraction/move apply routes (`extract_method_apply`, `extract_interface_apply`, `extract_type_apply`, `move_type_to_file_apply`) now bind to their producer families and reject a preview token minted by a different `*_preview` before any workspace mutation, with an error naming the token's real producer and the route it should be redeemed through. Adds the matching `PreviewKind` members so the mismatch message stays factual instead of falling back to the generic `apply_with_verify` advice. Tokens with unrecorded provenance still redeem unchanged, and `apply_with_verify` remains producer-agnostic by design." |
| Backlog sync | Close rows: []. Partial slice — child 3 of 5 of `preview-token-apply-route-binding-remaining-families`; the row closes with child 5 (`preview-token-route-binding-bulk-family`). Update related: file two follow-on rows at execute time — (1) producer-side `PreviewKind` tagging for `ExtractMethodService` / `InterfaceExtractionService` / `TypeExtractionService` / `TypeMoveService`, excluding the shared-expression-to-helper producer; (2) the missing `extract_interface_preview` entry in `ServerSurfaceCatalog.PreviewApplyRoutes`, plus a test pinning that every preview tool with a named apply route appears in that map. |

<details>
<summary>Executor handoff notes</summary>

**Executor handoff:**

- **Pattern coordinates (verified on `main`):** copy `src/RoslynMcp.Host.Stdio/Tools/RefactoringTools.cs:51-60` verbatim — the `ApplyByTokenAsync` call ending `ct,` + the two-line `preview-token-apply-route-provenance:` comment + `expectedKind:` / `applyRoute:`. Each owned file has exactly one call site, all terminating `ct);` at `ExtractMethodTools.cs:56`, `InterfaceExtractionTools.cs:55`, `TypeExtractionTools.cs:60`, `TypeMoveTools.cs:59`. Append the four enum members after `CodeFix,` at `PreviewKind.cs:42` — implicit ordinals, no explicit values, no renumbering.
- **Silent-degradation trap (no compiler help):** `ToolDispatch.PreviewToolFor` / `ApplyRouteFor` (`ToolDispatch.cs:207-231`) end in `_ =>` fallbacks. New members with no map entry compile green and emit "an untagged *_preview tool" / "apply_with_verify" instead of the real names. Confirm order 1's map is Core-side/externally extensible before step 2; if it is still a `ToolDispatch`-private switch, STOP and flag.
- **Test target:** new `tests/RoslynMcp.Tests/ExtractionApplyRouteBindingTests.cs`. **MSTest**, not xUnit — `[TestMethod]`, `Assert.ThrowsExactlyAsync`, `StringAssert.Contains`. Mirror `ToolDispatchTests.cs:39-69`; copy `FakeGate` (`:400-435`) and `FakePreviewStore` (`:442-491`) locally. These tests call the *tool* methods, so you also need an `IRefactoringService` fake — mirror `ApplyUndoWorkflowServiceTests.cs:289-317`, with `ApplyRefactoringAsync` throwing.
- **Negative space:** do NOT bind `extract_shared_expression_to_helper_preview` (`ExtractMethodTools.cs:58+`) or `change_type_namespace_preview` (`TypeMoveTools.cs:61+`) — same files, no named apply route. Do NOT edit `ToolDispatch.cs` / `ToolDispatchTests.cs`.

**FAMILY CONTRACT — cross-initiative correction (orchestrator, post-review cycle 2; resolves the surviving `3` / `3b` warn findings recorded in `review.md`). Read this before following the Approach verbatim.**

- **`applyRoute:` NO LONGER EXISTS after order 1.** Verified live on `main` 2026-08-25: `ToolDispatch.ApplyByTokenAsync` declares `string? applyRoute = null` at `src/RoslynMcp.Host.Stdio/Tools/ToolDispatch.cs:133`, and initiative `preview-token-route-map-centralization` (order 1) Approach step (3) **deletes that parameter**. Every family child branches from a post-order-1 base via its `dependsOn` chain. Therefore: **bind with `expectedKind:` ONLY.** Any Approach step in this stanza instructing you to add an `applyRoute: "..."` named argument is superseded — drop it, keep `expectedKind:`. Following it verbatim is a guaranteed **CS1739**. Nothing is lost: the invoked-route half of the error message is derived from `expectedKind` by the centralized map.
- **The `RefactoringTools.cs:51-60` copy template is PRE-order-1.** It still shows the `expectedKind:` + `applyRoute:` pair. Copy the `expectedKind:` line and the two-line `preview-token-apply-route-provenance` comment; do NOT copy the `applyRoute:` line — order 1 removes all five of them.
- **Adding a concrete `PreviewKind` member obligates TWO companion edits, not one.** Order 1 pins that every non-`Unspecified` member resolves to BOTH a preview tool AND a catalog apply route. So a new member needs (a) its row in the `ToolDispatch` kind→previewTool map (`src/RoslynMcp.Host.Stdio/Tools/ToolDispatch.cs`; substitute order 1’s extracted map file if it created one — still one file), and (b) a `ServerSurfaceCatalog.PreviewApplyRoutes` entry (`src/RoslynMcp.Host.Stdio/Catalog/ServerSurfaceCatalog.cs:42-69`) **iff** its `*_preview` tool name is not already a key there. Verified live: that dictionary holds 24 entries and has **no** `preview_multi_file_edit` and **no** `fix_all_preview` key; all five of today’s concrete members ARE covered. Check your slice’s preview-tool names against the live dictionary FIRST — omitting either companion reds order 1’s pin test.
- **If the companions push you past Rule 3’s 4-file base, claim the gate-forced-companion exemption explicitly rather than exceeding silently** — gate: the C# compiler (CS0117 on an undeclared enum member) plus order 1’s exhaustiveness pin test; show the arithmetic; cap is 7. If it would exceed 7, **STOP and report** a needed re-split. Note that `ServerSurfaceCatalog.cs` is an addenda-listed hotspot (RMCP001/RMCP002-gated, ≤1 catalog-touching initiative per wave), so touching it also constrains wave scheduling.
- **THIS STANZA’S SCOPE UNDER-COUNTS BY ONE — the fix is arithmetic, not a stop-and-remediate.** Scope says "Deliberately NOT touched: `ToolDispatch.cs`" while the Approach adds four concrete `PreviewKind` members (`ExtractMethod` / `ExtractInterface` / `ExtractType` / `MoveTypeToFile`). Per the bullet above, each needs a `ToolDispatch` map row, so the real count is **6 production files, not 5**. All four preview-tool names (`extract_method_preview`, `extract_type_preview`, `move_type_to_file_preview`) are already keys in `PreviewApplyRoutes` — verified live — so **no `ServerSurfaceCatalog.cs` edit is needed and the hotspot wave-rule is NOT engaged**. Restate the existing gate-forced-companion exemption as **4 base + 2 additional = 6 ≤ 7** (adding `ToolDispatch.cs` alongside `PreviewKind.cs`) and proceed. Sibling orders 4 and 5 already scope their `ToolDispatch.cs` map rows this way; this stanza simply missed it.

</details>

### 4. preview-token-route-binding-orchestration-family

| Field | Content |
|---|---|
| Diagnosis | The slice assignment names three files, but only ONE of them holds a bindable route. Verified on `main` 2026-08-25. (a) `src/RoslynMcp.Host.Stdio/Tools/OrchestrationTools.cs:104` (`apply_composite_preview`) and `src/RoslynMcp.Host.Stdio/Tools/ProjectMutationTools.cs:242` (`apply_project_mutation`) both call the **delegate-peek overload** `ToolDispatch.ApplyByTokenAsync(gate, peekWorkspaceId, token, serviceCall, ct)` declared at `src/RoslynMcp.Host.Stdio/Tools/ToolDispatch.cs:316-329`. That overload has NO `expectedKind`/`applyRoute` parameter and never calls `RequireCompatibleProducer`; their stores (`ICompositePreviewStore`, `IProjectMutationPreviewStore`) do not derive from `IPreviewStore` and expose no `PeekKind`. Binding them requires widening the ToolDispatch overload plus adding a kind peek to two store contracts and their impls plus tagging two producers — none of which is reachable inside the declared negative space. They are therefore OUT of this initiative's scope, with the blocker recorded. (b) `src/RoslynMcp.Host.Stdio/Tools/ScaffoldingTools.cs:93` (`scaffold_type_apply`) and `:236` (`scaffold_test_apply`) DO use the `IPreviewStore` overload (`ToolDispatch.cs:126-152`) and are bindable. (c) Their tokens are not minted by any scaffolder: `TypeScaffolder.cs:43-45` and `SingleTestScaffolder.cs:100` both delegate to `IFileOperationService.PreviewCreateFileAsync`, whose single `Store` call at `src/RoslynMcp.Roslyn/Services/FileOperationService.cs:53` omits the kind argument and therefore records `PreviewKind.Unspecified` (`PreviewStore.cs:148` default). So an `expectedKind` on the two scaffold routes is a **no-op today** — `RequireCompatibleProducer` (`ToolDispatch.cs:185-194`) fails open on `Unspecified`. (d) `src/RoslynMcp.Core/Models/PreviewKind.cs:21-43` carries only 5 concrete members, all owned by `RefactoringService`; no member describes the create-file producer, so a new member is mandatory. |
| Approach | Bind the two `ScaffoldingTools` routes for real, by adding the missing producer kind and tagging the producer. (1) `src/RoslynMcp.Core/Models/PreviewKind.cs` — add one concrete member `FileCreate` ("a new-file creation preview minted by `IFileOperationService.PreviewCreateFileAsync`"), documented at FAMILY granularity because `create_file_preview`, `scaffold_type_preview`, and `scaffold_test_preview` all mint through the same method; per-tool granularity would require splitting that producer and is explicitly NOT attempted here. (2) `src/RoslynMcp.Roslyn/Services/FileOperationService.cs:53` — pass `PreviewKind.FileCreate` as the trailing `Store` argument, mirroring `RefactoringService.cs:91` exactly (positional kind on the changes-carrying `Store` overload). Do NOT touch the move/rename `Store` calls at `:106` and `:156` — those belong to the editing-family child. (3) The centralized kind map from initiative `preview-token-route-map-centralization` — add the `FileCreate` row (`preview tool: create_file_preview`, `apply route: create_file_apply`). If order 1 extracted the map into its own type/file, edit THAT file; if the map is still the pair of switches at `ToolDispatch.cs:207-217` / `:221-231`, add the two arms there. One file either way. This step is mandatory, not optional: order 1 ships a pin test asserting every non-`Unspecified` member has a mapping, so a new member without a map row fails that test. (4) `src/RoslynMcp.Host.Stdio/Tools/ScaffoldingTools.cs` — add `expectedKind: PreviewKind.FileCreate, applyRoute: "scaffold_type_apply"` to the call at `:93` and `expectedKind: PreviewKind.FileCreate, applyRoute: "scaffold_test_apply"` at `:236`, mirroring the five existing bindings at `RefactoringTools.cs:60/95/130/169/210`. Leave `OrchestrationTools.cs` and `ProjectMutationTools.cs` untouched and record the delegate-overload blocker in the PR body so the residue is visible. |
| Scope | Production files: 4 — `src/RoslynMcp.Core/Models/PreviewKind.cs`, `src/RoslynMcp.Host.Stdio/Tools/ToolDispatch.cs` (map rows only; substitute order 1's extracted map file if it created one — still one file), `src/RoslynMcp.Roslyn/Services/FileOperationService.cs` (line 53 only), `src/RoslynMcp.Host.Stdio/Tools/ScaffoldingTools.cs`. At the Rule 3 cap of 4; no exemption claimed. Test files added/modified: 2 — `tests/RoslynMcp.Tests/ScaffoldingIntegrationTests.cs` (extend), `tests/RoslynMcp.Tests/FileOperationIntegrationTests.cs` (extend). Files deleted: none. Deviation from the slice assignment's negative space, stated plainly: this initiative touches `ToolDispatch.cs` (two map rows) because a new concrete `PreviewKind` member without a map row breaks order 1's exhaustiveness pin test, and it does NOT touch two of its three assigned files because their routes are structurally unbindable (see Diagnosis). |
| Tool policy | edit-only |
| Estimated context cost | 50000 |
| Risks | Fanout probe (shape C — new enum member) run against `PreviewKind`: the only non-doc consumers are `PreviewKind.cs` (definition), the two parallel switches in `ToolDispatch.cs:207-231` (the centralized map), producer tagging in `FileOperationService.cs:53`, and the two binding sites in `ScaffoldingTools.cs` — 4 distinct production files, matching `productionFilesTouched`. `ApplyWithVerifyTool.cs` mentions `PreviewKind` in XML doc only and is deliberately producer-agnostic; `RefactoringTools.cs`'s 5 bindings are unaffected. Real conflict edge with initiative `preview-token-route-map-centralization` (order 1) on `ToolDispatch.cs` — this initiative MUST land strictly after it; encoded in `dependsOn`. Second, softer edge: `FileOperationService.cs` is also the natural home for the editing-family child (order 2) to tag `create_file_preview`; if that child tags line 53 first, this initiative's step 2 becomes a no-op merge, not a conflict — but the two must not be cut from the same base for the same line. Adjacent behavior to verify: `scaffold_test_batch_preview` mints through `BatchTestScaffolder.cs:229` (`Store` without a kind, so `Unspecified`), and there is no `scaffold_test_batch_apply` route — its tokens are redeemed through `scaffold_test_apply` or `apply_with_verify` and MUST keep working; `Unspecified` stays permissive by `ToolDispatch.cs:185-188`, so the batch flow is unaffected, but pin it with a test. Honest limitation: `FileCreate` is family-granular, so a `create_file_preview` token still redeems through `scaffold_type_apply` and vice-versa. That is a strict improvement over today (a rename or code-fix token currently redeems through either) and matches acceptance bullet 2's "compatible token kinds" wording, but it is not per-tool provenance — say so in the PR body rather than overclaiming. |
| Validation | `mcp__roslyn__compile_check` after each edit; then `mcp__roslyn__test_run --filter "ScaffoldingIntegrationTests"`, `--filter "FileOperationIntegrationTests"`, `--filter "ToolDispatchTests"`, and `--filter "PreviewStoreTests"` (the last two are regression-only — they must stay green unmodified, and `ToolDispatchTests` is order 1's exhaustiveness pin, which is the gate proving the new member got its map row). New cases: (a) `scaffold_type_preview` then `PeekKind(token)` returns `PreviewKind.FileCreate`; (b) `scaffold_type_apply` refuses a token minted by `rename_preview` with a message naming `rename_apply` as the correct route and makes no workspace change; (c) `scaffold_test_apply` still redeems a `scaffold_test_batch_preview` token (`Unspecified`, permissive); (d) `create_file_preview` token now carries `FileCreate`. Full gate before PR: `./eng/verify-release.ps1 -Configuration Release` plus `./eng/verify-ai-docs.ps1`. Self-edit caveat applies — no `mcp__roslyn__*_apply` / `*_preview` in the main checkout; do this in the worktree. |
| Performance review | N/A — correctness/contract change. `PeekKind` is an O(1) dictionary lookup on a bounded in-memory store; the guard runs once per apply, before mutation. |
| CHANGELOG category | Added |
| CHANGELOG entry (draft) | **Added:** Preview-token provenance for the file-creation producer family. `PreviewKind` gains a `FileCreate` member, `IFileOperationService.PreviewCreateFileAsync` now tags the tokens it mints, and the `scaffold_type_apply` / `scaffold_test_apply` routes declare it as their compatible producer set — so a rename, format, or code-fix token redeemed through a scaffold apply route is refused with an actionable message naming the correct route, before any workspace mutation. Untagged tokens (including `scaffold_test_batch_preview`) stay permissive. The `apply_composite_preview` and `apply_project_mutation` routes remain unbound: their preview stores do not implement `IPreviewStore` and expose no provenance peek. |
| Backlog sync | Close rows: []. Mark obsolete: []. Update related: [preview-token-apply-route-binding-remaining-families] — record in its detail file that (i) the delegate-peek `ApplyByTokenAsync` overload has no provenance contract, so `apply_composite_preview` and `apply_project_mutation` need `ICompositePreviewStore`/`IProjectMutationPreviewStore` to gain a kind peek before they can be bound, and (ii) `scaffold_*`/`create_file` provenance is family-granular by construction because they share one producer method. |

<details>
<summary>Executor handoff notes</summary>

**Executor handoff:**

- **Pattern coordinates (all re-verified on `main`):** producer tagging mirrors `src/RoslynMcp.Roslyn/Services/RefactoringService.cs:91` — positional `PreviewKind.SymbolRename` as the 6th arg of the changes-carrying `Store`; apply the same shape at `FileOperationService.cs:53`. Route binding mirrors `src/RoslynMcp.Host.Stdio/Tools/RefactoringTools.cs:51-60` (named `expectedKind:` / `applyRoute:` after `ct`, with the 2-line provenance comment). Target call sites are `ScaffoldingTools.cs:93-98` and `:236-241`. Map arms live in the two parallel switches `ToolDispatch.cs:207-215` and `:221-229` — add BOTH (`create_file_preview` / `create_file_apply`); order 1 may have extracted them to a new file, edit that instead.
- **Test target:** case (d) → extend `tests/RoslynMcp.Tests/FileOperationIntegrationTests.cs`, mirroring `Create_File_Preview_And_Apply_Adds_File_To_Isolated_Workspace_Copy` (:22-46); `PreviewStore` is inherited from `TestBase.cs:49` as `IPreviewStore`, so `PreviewStore.PeekKind(preview.PreviewToken)` works directly. Cases (a)–(c) → extend `ScaffoldingIntegrationTests.cs` (1325 LOC hotspot): put (a)/(b) adjacent to `Scaffold_Type_Preview_And_Apply_Creates_New_Type_File` (:57-74) and (c) adjacent to `Scaffold_Test_Batch_Preview_And_Apply_Creates_Multiple_Test_Files` (:936). Do NOT create new test files. Refusal-message assertion shape: `ToolDispatchTests.cs:39` (`ApplyByTokenAsync_IncompatibleProducer_RefusesBeforeServiceCall`) — leave that file unmodified.
- **Negative space:** do NOT touch `FileOperationService.cs:106` / `:156` (move + rename `Store` calls — editing-family child's rows), nor `OrchestrationTools.cs` / `ProjectMutationTools.cs`.

**FAMILY CONTRACT — cross-initiative correction (orchestrator, post-review cycle 2; resolves the surviving `3` / `3b` warn findings recorded in `review.md`). Read this before following the Approach verbatim.**

- **`applyRoute:` NO LONGER EXISTS after order 1.** Verified live on `main` 2026-08-25: `ToolDispatch.ApplyByTokenAsync` declares `string? applyRoute = null` at `src/RoslynMcp.Host.Stdio/Tools/ToolDispatch.cs:133`, and initiative `preview-token-route-map-centralization` (order 1) Approach step (3) **deletes that parameter**. Every family child branches from a post-order-1 base via its `dependsOn` chain. Therefore: **bind with `expectedKind:` ONLY.** Any Approach step in this stanza instructing you to add an `applyRoute: "..."` named argument is superseded — drop it, keep `expectedKind:`. Following it verbatim is a guaranteed **CS1739**. Nothing is lost: the invoked-route half of the error message is derived from `expectedKind` by the centralized map.
- **The `RefactoringTools.cs:51-60` copy template is PRE-order-1.** It still shows the `expectedKind:` + `applyRoute:` pair. Copy the `expectedKind:` line and the two-line `preview-token-apply-route-provenance` comment; do NOT copy the `applyRoute:` line — order 1 removes all five of them.
- **Adding a concrete `PreviewKind` member obligates TWO companion edits, not one.** Order 1 pins that every non-`Unspecified` member resolves to BOTH a preview tool AND a catalog apply route. So a new member needs (a) its row in the `ToolDispatch` kind→previewTool map (`src/RoslynMcp.Host.Stdio/Tools/ToolDispatch.cs`; substitute order 1’s extracted map file if it created one — still one file), and (b) a `ServerSurfaceCatalog.PreviewApplyRoutes` entry (`src/RoslynMcp.Host.Stdio/Catalog/ServerSurfaceCatalog.cs:42-69`) **iff** its `*_preview` tool name is not already a key there. Verified live: that dictionary holds 24 entries and has **no** `preview_multi_file_edit` and **no** `fix_all_preview` key; all five of today’s concrete members ARE covered. Check your slice’s preview-tool names against the live dictionary FIRST — omitting either companion reds order 1’s pin test.
- **If the companions push you past Rule 3’s 4-file base, claim the gate-forced-companion exemption explicitly rather than exceeding silently** — gate: the C# compiler (CS0117 on an undeclared enum member) plus order 1’s exhaustiveness pin test; show the arithmetic; cap is 7. If it would exceed 7, **STOP and report** a needed re-split. Note that `ServerSurfaceCatalog.cs` is an addenda-listed hotspot (RMCP001/RMCP002-gated, ≤1 catalog-touching initiative per wave), so touching it also constrains wave scheduling.

</details>

### 5. preview-token-route-binding-bulk-family

| Field | Content |
|---|---|
| Diagnosis | Both routes this slice owns redeem preview tokens with no provenance claim. `src/RoslynMcp.Host.Stdio/Tools/BulkRefactoringTools.cs:49` (`bulk_replace_type_apply`) and `src/RoslynMcp.Host.Stdio/Tools/DeadCodeTools.cs:50` (`remove_dead_code_apply`) both call `ToolDispatch.ApplyByTokenAsync` without `expectedKind:`/`applyRoute:`, so `RequireCompatibleProducer` short-circuits at `src/RoslynMcp.Host.Stdio/Tools/ToolDispatch.cs:185-188` and a `SymbolRename`/`CodeFix`/format token redeems here unchecked. **Probe finding the Phase-A slice assignment did not anticipate:** `PreviewKind` declares only five concrete members (`src/RoslynMcp.Core/Models/PreviewKind.cs:27-42` — `SymbolRename`, `FormatDocument`, `FormatRange`, `OrganizeUsings`, `CodeFix`); none covers bulk-replace, replace-invocation, or dead-code, and all three producers mint UNTAGGED tokens via the defaulting `Store` overloads (`src/RoslynMcp.Roslyn/Services/BulkRefactoringService.cs:74`, `:274`, `src/RoslynMcp.Roslyn/Services/DeadCodeService.cs:76` → `src/RoslynMcp.Roslyn/Services/PreviewStore.cs:88/109/148`, each passing `PreviewKind.Unspecified`). Binding these two families is therefore NOT the 2-file edit the slice assumed — it requires new enum members, producer tagging, and a map entry. Second finding: `replace_invocation_preview` deliberately redeems through `bulk_replace_type_apply` (`src/RoslynMcp.Host.Stdio/Tools/BulkRefactoringTools.cs:56-60`), while `expectedKind` is a single value, not a set (`src/RoslynMcp.Host.Stdio/Tools/ToolDispatch.cs:132`) — so the two bulk producers must share ONE kind. The escape this child retires is live at `tests/RoslynMcp.Tests/ToolDispatchTests.cs:130-147`. |
| Approach | 1. `src/RoslynMcp.Core/Models/PreviewKind.cs` — add exactly two concrete members: `BulkReplaceType` (shared by `bulk_replace_type_preview` AND `replace_invocation_preview`, because both redeem through the single `bulk_replace_type_apply` route) and `RemoveDeadCode`. Document the deliberate 2-producers→1-kind collapse on `BulkReplaceType` so a later reader does not "fix" it into two members and break the single-valued `expectedKind` contract. 2. `src/RoslynMcp.Host.Stdio/Tools/ToolDispatch.cs` — add the two entries to the centralized kind→(previewTool, applyRoute) map introduced by initiative `preview-token-route-map-centralization`; `BulkReplaceType` → (`bulk_replace_type_preview` or `replace_invocation_preview`, `bulk_replace_type_apply`), `RemoveDeadCode` → (`remove_dead_code_preview`, `remove_dead_code_apply`). This is the ONLY ToolDispatch.cs touch — no logic change, map data only. 3. Tag the producers: `src/RoslynMcp.Roslyn/Services/BulkRefactoringService.cs:74` and `:274` and `src/RoslynMcp.Roslyn/Services/DeadCodeService.cs:76` switch to the kind-carrying `Store` overload, mirroring `src/RoslynMcp.Roslyn/Services/RefactoringService.cs:91` (`…, changes, PreviewKind.SymbolRename)`). 4. Bind the routes: add `expectedKind:`/`applyRoute:` named arguments at `src/RoslynMcp.Host.Stdio/Tools/BulkRefactoringTools.cs:49` and `src/RoslynMcp.Host.Stdio/Tools/DeadCodeTools.cs:50`, mirroring `src/RoslynMcp.Host.Stdio/Tools/RefactoringTools.cs:59-60`. 5. `tests/RoslynMcp.Tests/ToolDispatchTests.cs` — re-scope `ApplyByTokenAsync_UnboundRoute_DoesNotEnforceProvenance` (`:130-147`) to out-of-tree `IPreviewStore` implementers only per parent Acceptance bullet 4: rename it to name the out-of-tree case, rewrite the assertion message away from "a route that declares no expectedKind must keep its pre-binding behavior", and add positive/negative binding tests for the two new kinds. |
| Scope | Production files (6): `src/RoslynMcp.Core/Models/PreviewKind.cs`, `src/RoslynMcp.Host.Stdio/Tools/ToolDispatch.cs`, `src/RoslynMcp.Roslyn/Services/BulkRefactoringService.cs`, `src/RoslynMcp.Roslyn/Services/DeadCodeService.cs`, `src/RoslynMcp.Host.Stdio/Tools/BulkRefactoringTools.cs`, `src/RoslynMcp.Host.Stdio/Tools/DeadCodeTools.cs`. Test files (3, all extended not created): `tests/RoslynMcp.Tests/ToolDispatchTests.cs`, `tests/RoslynMcp.Tests/BulkRefactoringTests.cs`, `tests/RoslynMcp.Tests/DeadCodeIntegrationTests.cs` — exactly at the Rule 4 cap. Files deleted: none. **Rule 3 exemption: gate-forced-companion — gate: the C# compiler (`dotnet build` / `mcp__roslyn__compile_check`, CS0117 on an undeclared enum member) plus the map-exhaustiveness pin test that initiative `preview-token-route-map-centralization` adds to `tests/RoslynMcp.Tests/ToolDispatchTests.cs` (asserts every non-`Unspecified` `PreviewKind` member has a mapping); evidence: `PreviewKind` today declares only the five concrete members at `src/RoslynMcp.Core/Models/PreviewKind.cs:27-42`, so referencing `PreviewKind.BulkReplaceType` from `BulkRefactoringTools.cs:49` without first declaring it is CS0117, and declaring it without the ToolDispatch map entry fails the order-1 pin test — neither companion can be deferred to a follow-up PR; total: 6 ≤ 7 (4 base — the two owned `*Tools.cs` plus the two producer services that must mint the kinds — plus 2 additional: `PreviewKind.cs` and `ToolDispatch.cs`).** |
| Tool policy | edit-only |
| Estimated context cost | 60000 |
| Risks | **Slice-assignment departure (loud):** the Phase-A negative space said "do NOT edit ToolDispatch.cs" and "no shared file" with order 1. The probe shows that is not achievable — see the gate citation in Scope. Executor MUST land this after `preview-token-route-map-centralization` merges and rebase onto it; the ToolDispatch.cs touch is map data only, so the textual conflict surface is one region. **Sibling collision:** `src/RoslynMcp.Core/Models/PreviewKind.cs` and the ToolDispatch map will be needed by orders 2/3/4 for the same reason (none of their families has a kind either) — Phase C should treat `PreviewKind.cs` + `ToolDispatch.cs` as shared across all four family children and serialize them, not parallelize. **Design choice:** collapsing two bulk producers onto one `BulkReplaceType` kind means a `replace_invocation_preview` token rejected elsewhere is described by the shared `previewTool` label; the alternative (a set-valued `expectedKind` overload) is a `ToolDispatch` API change owned by order 1 and out of this slice. **Behavior tightening:** cross-family redemptions that silently "worked" before now throw before mutation — verify no in-repo test or skill redeems a bulk/dead-code token through a foreign apply route. **Adjacent:** `apply_with_verify` must keep accepting both new kinds; confirm the generic route is unaffected. Fanout probe run (shape C, new members of an enumerated contract): 6 distinct production files, matching `productionFilesTouched`; no exhaustive `PreviewKind` enumeration exists in `tests/` today (`Enum.GetValues` sweep found only `ToolErrorCategory`), so the only enumeration site is the order-1 pin test. |
| Validation | `mcp__roslyn__compile_check` after each edit. Targeted: `mcp__roslyn__test_run --filter "ToolDispatchTests"`, `--filter "BulkRefactoringTests"`, `--filter "DeadCodeIntegrationTests"`, `--filter "PreviewStoreTests"`. New assertions: (a) a `PreviewKind.SymbolRename` token redeemed at `bulk_replace_type_apply` throws `InvalidOperationException` naming `rename_preview`/`rename_apply` with no workspace mutation; (b) same for `remove_dead_code_apply`; (c) a `bulk_replace_type_preview` token AND a `replace_invocation_preview` token both redeem successfully at `bulk_replace_type_apply`; (d) a `remove_dead_code_preview` token round-trips `PeekKind` == `RemoveDeadCode`; (e) a `PreviewKind.Unspecified` token still redeems on both bound routes (out-of-tree store permissiveness — the re-scoped escape). Full gate before PR: `./eng/verify-release.ps1 -Configuration Release` and `./eng/verify-ai-docs.ps1`. |
| Performance review | N/A — correctness fix; `PeekKind` is a non-consuming dictionary peek already on the apply path, no hot-path change. |
| CHANGELOG category | Changed — BREAKING |
| CHANGELOG entry (draft) | `- **Changed — BREAKING:** `bulk_replace_type_apply` and `remove_dead_code_apply` now refuse preview tokens minted by a different producer family before any workspace mutation, completing the apply-route/preview-kind binding started in PR #1354. Adds `PreviewKind.BulkReplaceType` (shared by `bulk_replace_type_preview` and `replace_invocation_preview`, which share one apply route) and `PreviewKind.RemoveDeadCode`, and tags their producers so provenance is enforced in both directions. The `ToolDispatch` "unbound route keeps pre-binding behavior" escape is re-scoped to out-of-tree `IPreviewStore` implementers only.` (fragment path: `changelog.d/preview-token-apply-route-binding-remaining-families.md`) |
| Backlog sync | Close rows: [preview-token-apply-route-binding-remaining-families]. Mark obsolete: []. Update related: []. |

<details>
<summary>Executor handoff notes</summary>

**Executor handoff:**

- **Pattern coordinates:** producer tagging mirrors `src/RoslynMcp.Roslyn/Services/RefactoringService.cs:91` — the `changes`-list + kind `Store` overload declared at `src/RoslynMcp.Roslyn/Services/PreviewStore.cs:154`. Route binding mirrors `src/RoslynMcp.Host.Stdio/Tools/RefactoringTools.cs:56-60` (two named args after `ct`, with the two-line `preview-token-apply-route-provenance:` comment).
- **Map seam:** today the kind→name data is TWO switch expressions, `PreviewToolFor` at `ToolDispatch.cs:207-215` and `ApplyRouteFor` at `:221-229`. After rebasing onto `preview-token-route-map-centralization` these become one map — add entries there, not to the switches. `BulkReplaceType`'s preview label must name both producers; do not add a second enum member for `replace_invocation`.
- **Test targets:** unit cases (a),(b),(e) go in `tests/RoslynMcp.Tests/ToolDispatchTests.cs` — mirror `ApplyByTokenAsync_IncompatibleProducer_RefusesBeforeServiceCall` (:39-69) for the throw-before-service-call shape and `ApplyByTokenAsync_UnspecifiedProducer_SkipsGuard_AndApplies` (:105-122) for (e). Round-trips (c),(d) need a real `PreviewStore`: extend `BulkRefactoringTests` (mirror `BulkReplaceType_Preview_ReturnsChanges`, :23) and `DeadCodeIntegrationTests` (mirror `Remove_Dead_Code_Preview_And_Apply_Removes_Unused_Field`, :22) — both are `SharedWorkspaceTestBase`. Create no new test file.
- **Negative space:** do NOT touch `RequireCompatibleProducer` (`ToolDispatch.cs:179-201`) — logic is correct and order-1-owned. Do NOT bind `src/RoslynMcp.Host.Stdio/Tools/ApplyWithVerifyTool.cs`; it must stay unbound so both new kinds redeem generically. Leave the `PreviewKind.Unspecified`-defaulting `Store` overloads (`PreviewStore.cs:88/109/148`) in place, and retag no other producer family.

**FAMILY CONTRACT — cross-initiative correction (orchestrator, post-review cycle 2; resolves the surviving `3` / `3b` warn findings recorded in `review.md`). Read this before following the Approach verbatim.**

- **`applyRoute:` NO LONGER EXISTS after order 1.** Verified live on `main` 2026-08-25: `ToolDispatch.ApplyByTokenAsync` declares `string? applyRoute = null` at `src/RoslynMcp.Host.Stdio/Tools/ToolDispatch.cs:133`, and initiative `preview-token-route-map-centralization` (order 1) Approach step (3) **deletes that parameter**. Every family child branches from a post-order-1 base via its `dependsOn` chain. Therefore: **bind with `expectedKind:` ONLY.** Any Approach step in this stanza instructing you to add an `applyRoute: "..."` named argument is superseded — drop it, keep `expectedKind:`. Following it verbatim is a guaranteed **CS1739**. Nothing is lost: the invoked-route half of the error message is derived from `expectedKind` by the centralized map.
- **The `RefactoringTools.cs:51-60` copy template is PRE-order-1.** It still shows the `expectedKind:` + `applyRoute:` pair. Copy the `expectedKind:` line and the two-line `preview-token-apply-route-provenance` comment; do NOT copy the `applyRoute:` line — order 1 removes all five of them.
- **Adding a concrete `PreviewKind` member obligates TWO companion edits, not one.** Order 1 pins that every non-`Unspecified` member resolves to BOTH a preview tool AND a catalog apply route. So a new member needs (a) its row in the `ToolDispatch` kind→previewTool map (`src/RoslynMcp.Host.Stdio/Tools/ToolDispatch.cs`; substitute order 1’s extracted map file if it created one — still one file), and (b) a `ServerSurfaceCatalog.PreviewApplyRoutes` entry (`src/RoslynMcp.Host.Stdio/Catalog/ServerSurfaceCatalog.cs:42-69`) **iff** its `*_preview` tool name is not already a key there. Verified live: that dictionary holds 24 entries and has **no** `preview_multi_file_edit` and **no** `fix_all_preview` key; all five of today’s concrete members ARE covered. Check your slice’s preview-tool names against the live dictionary FIRST — omitting either companion reds order 1’s pin test.
- **If the companions push you past Rule 3’s 4-file base, claim the gate-forced-companion exemption explicitly rather than exceeding silently** — gate: the C# compiler (CS0117 on an undeclared enum member) plus order 1’s exhaustiveness pin test; show the arithmetic; cap is 7. If it would exceed 7, **STOP and report** a needed re-split. Note that `ServerSurfaceCatalog.cs` is an addenda-listed hotspot (RMCP001/RMCP002-gated, ≤1 catalog-touching initiative per wave), so touching it also constrains wave scheduling.

</details>

### 6. shipped-skills-prefix-batch-c1

| Field | Content |
|---|---|
| Diagnosis | All three assigned skills still carry the identical PRE-FIX bare-prefix connectivity block, verbatim: `skills/semantic-find/SKILL.md:29-39`, `skills/test-coverage/SKILL.md:21-31`, `skills/trace-flow/SKILL.md:34-44`. Each is the same 11-line shape — heading, `Before running any \`mcp__roslyn__*\` tool call, probe the server once:`, `1. Call \`mcp__roslyn__server_info\` …`, a bail blockquote naming `mcp__roslyn__server_heartbeat`, and `3. If \`connection.state\` is "ready" …`. These are exactly the shapes `Assert-PrefixAgnostic` flags (`eng/verify-skills-are-generic.ps1:104-124` against `eng/banned-skill-markers.json` `prefixAgnostic.imperativePatterns:19-21`); the only reason CI is green is the amnesty at `eng/banned-skill-markers.json:32-38` (`residualUnsweptAllowlist`), which the guard consults at `eng/verify-skills-are-generic.ps1:107` and `:129`. Confirmed by grep that `mcp__` appears in these three files ONLY inside those blocks — no other bare literal hides elsewhere, so swapping the block plus dropping the allowlist entries is sufficient for all three to clear both assertions. Detail-file anchors reconciled: the row's 2026-08-25 amendment measured exactly 5 unswept files and the live allowlist still lists exactly those 5, so the amendment is current, not stale. |
| Approach | Pure text substitution, three files, one recipe. (1) In each of `skills/semantic-find/SKILL.md` (replace lines 29-39), `skills/test-coverage/SKILL.md` (21-31), `skills/trace-flow/SKILL.md` (34-44), delete the legacy block and insert the canonical resolve-once-then-pin block. The canonical source is the `canonicalPrecheckBlocks[0]` (`id: connectivity-precheck-section`) `text` array in `eng/banned-skill-markers.json:45-61` — 14 lines ending in one empty line, so the following `## Workflow` heading stays separated by exactly one blank line. Mirror the already-swept exemplar `skills/review/SKILL.md:34-47` (byte-identical form shipped by PR #1341); do NOT hand-retype — copy from the JSON policy array or from `review/SKILL.md`, because `Assert-CanonicalBlockIdentity` (`eng/verify-skills-are-generic.ps1:126-162`) compares with `StringComparison.Ordinal` and a single smart-quote or spacing difference reds CI. (2) Remove exactly the three entries `skills/semantic-find/SKILL.md`, `skills/test-coverage/SKILL.md`, `skills/trace-flow/SKILL.md` from `residualUnsweptAllowlist` in `eng/banned-skill-markers.json:33-35`, leaving the two batch-c2 files. (3) Lower the shrink ratchet `ResidualUnsweptRatchet` in `tests/RoslynMcp.Tests/Skills/SkillPrefixGenericityTests.cs:38` from `5` to `2` and update its doc-comment — the comment at `:32-37` states lowering as batches land is the intended edit, and leaving it at 5 banks three slots of un-used amnesty that a regression could silently refill. Leave the downstream prose alone: `skills/semantic-find/SKILL.md:166` and `skills/trace-flow/SKILL.md:181` reference the precheck generically with no prefix literal and stay valid. |
| Scope | Production files: 4 — `skills/semantic-find/SKILL.md`, `skills/test-coverage/SKILL.md`, `skills/trace-flow/SKILL.md`, `eng/banned-skill-markers.json`. Exactly at the Rule 3 cap of 4; no exemption claimed or needed. Test files modified: 1 — `tests/RoslynMcp.Tests/Skills/SkillPrefixGenericityTests.cs` (ratchet constant + doc comment). Files deleted: none. NEGATIVE SPACE honored: `skills/version-bump/SKILL.md` and `skills/workspace-health/SKILL.md` untouched (batch-c2), and no `.claude/**` or `ai_docs/**` file is edited — those legitimately use the bare dev-build prefix. |
| Tool policy | edit-only |
| Estimated context cost | 25000 |
| Risks | (1) Byte-identity is the whole failure mode — `Assert-CanonicalBlockIdentity` is Ordinal, so a re-typed em dash, curly quote, or lost trailing blank line fails the gate with a `drifted from eng/banned-skill-markers.json` message. Copy the block, never retype it. (2) Real conflict edge with sibling initiative `shipped-skills-prefix-batch-c2` (order 7): both edit `eng/banned-skill-markers.json` AND `tests/RoslynMcp.Tests/Skills/SkillPrefixGenericityTests.cs`. This initiative must land FIRST; c2 empties the allowlist and drops the ratchet to 0. Do not parallel-wave the two. (3) Removing an allowlist entry activates BOTH assertions for that file, not just the imperative one — verified there is no second `mcp__` literal outside the replaced block in any of the three, so no surprise second offender. (4) Does not close the backlog row: acceptance bullets 3+4 are satisfied only when the allowlist empties in batch-c2. (5) Adjacent bad code, out of scope, already tracked: `skill-prefix-guard-tests-assert-bcl-not-gate` — the guard's negative-path tests assert BCL string semantics instead of exercising the gate; do not fix inline here. (6) Fanout probe RAN (shape B, per-target edit of 3 named files): the 3 named SKILL.md defining files plus the one registration/policy file that enumerates them = 4 distinct production files, matching `productionFilesTouched: 4`. A repo-wide grep for the three skill names outside `skills/**` and `ai_docs/**` returned only `CHANGELOG.md`, `docs/setup.md`, `src/RoslynMcp.Host.Stdio/README.md` (prose skill catalogs, no prefix literal, no edit forced), `eng/banned-skill-markers.json` (counted), and `SkillPrefixGenericityTests.cs` (counted as the test file) — no hidden fan-in. |
| Validation | (1) `./eng/verify-skills-are-generic.ps1` must exit 0 and report all shipped `.md` files checked — this is the canonical gate and covers both assertions. (2) `dotnet test --filter "FullyQualifiedName~SkillPrefixGenericityTests"` — `ImperativeRule_ClearsEveryShippedSkillOutsideTheAllowlist`, `CanonicalPrecheckBlocks_AreByteIdenticalWhereverTheyAppear`, and `ResidualUnsweptAllowlist_OnlyShrinksAndEveryEntryExists` must all pass with the three files no longer allowlisted. (3) Byte-identity spot check: extract lines from `## Connectivity precheck` up to (exclusive) the next `## ` heading in each of the three files plus `skills/review/SKILL.md` and confirm all four hashes are equal. (4) `./eng/verify-ai-docs.ps1` for link/doc health. (5) Confirm `residualUnsweptAllowlist` now contains exactly `skills/version-bump/SKILL.md` and `skills/workspace-health/SKILL.md`. (6) `./eng/verify-release.ps1 -Configuration Release` is the full CI equivalent if the runner is free. |
| Performance review | N/A — documentation/policy correctness fix, no runtime code touched. |
| CHANGELOG category | Fixed |
| CHANGELOG entry (draft) | Fragment `changelog.d/shipped-skills-prefix-batch-c1.md` (frontmatter `category: Fixed`): "Shipped skills `semantic-find`, `test-coverage`, and `trace-flow` now resolve the Roslyn MCP tool prefix once at runtime and pin it, instead of instructing agents to call a hard-coded `mcp__roslyn__` literal — marketplace-plugin installs no longer hit a false connectivity halt. Their residual-unswept amnesty entries were removed from the genericity guard's allowlist, and the shrink ratchet tightened from 5 to 2." |
| Backlog sync | Close rows: []. Mark obsolete: []. Update related: [shipped-skills-hardcode-bare-roslyn-tool-prefix — append a batch-c1 amendment noting 3 of the 5 remaining files swept, allowlist down to 2 (`version-bump`, `workspace-health`), ratchet lowered to 2, row stays OPEN pending batch-c2]. |

### 7. shipped-skills-prefix-batch-c2

| Field | Content |
|---|---|
| Diagnosis | Two shipped skills still carry the pre-fix bare-prefix precheck, verified live this pass. `skills/version-bump/SKILL.md:24-32` has `## Connectivity precheck` with three hard-coded literals: `:25` "Before running any `mcp__roslyn__*` tool call", `:27` "Call `mcp__roslyn__server_info`", `:30` "Run `mcp__roslyn__server_heartbeat`". `skills/workspace-health/SKILL.md:27-38` has the identical block at `:29`/`:31`/`:34`, plus a per-skill trailer at `:38` ("this skill's purpose is to report server/workspace status, so a failing precheck is itself the answer"). Both files are still exempt via `eng/banned-skill-markers.json:32-38` `prefixAgnostic.residualUnsweptAllowlist`, which is why `eng/verify-skills-are-generic.ps1` `Assert-PrefixAgnostic`/`Assert-CanonicalBlockIdentity` (lines 104-162, both `continue` on an allowlisted path) currently pass. Because the MCP tool prefix is client-assigned, a marketplace-plugin consumer (`mcp__plugin_roslyn-mcp_roslyn__*`) hits a literal it never exposes and halts — the confirmed misfire recorded in the detail file's 2026-08-10 amendment. The canonical replacement text is the SSOT array `canonicalPrecheckBlocks[0].text` (id `connectivity-precheck-section`) in `eng/banned-skill-markers.json:40-62`; a byte-identical shipped copy exists at `skills/exception-audit/SKILL.md` (landed in PR #1342) and can be used as the copy source. |
| Approach | (1) In `skills/version-bump/SKILL.md`, replace lines 24-32 wholesale with the canonical `connectivity-precheck-section` text, byte-for-byte including its trailing blank line; nothing else in the file changes. (2) In `skills/workspace-health/SKILL.md`, replace lines 27-37 with the same canonical text and KEEP the `:38` trailer note plus its following blank line immediately after — `Assert-CanonicalBlockIdentity` compares the canonical text as the LEADING slice of the anchor-to-`^## ` span and explicitly permits per-skill trailing prose (`eng/verify-skills-are-generic.ps1:146-152`; mirrored by `CanonicalBlockIdentity_AllowsPerSkillTrailingProse`). (3) In `eng/banned-skill-markers.json`, delete the two remaining `residualUnsweptAllowlist` entries so the array is `[]` — KEEP THE KEY: the gate does `$residualUnsweptAllowlist = @($prefixPolicy.residualUnsweptAllowlist)` at `:82`, and a missing key yields `@($null)` whose `$allowed.ToLowerInvariant()` at `:93` throws under `$ErrorActionPreference = "Stop"`, crashing the gate instead of passing it. (4) In `tests/RoslynMcp.Tests/Skills/SkillPrefixGenericityTests.cs`, lower `ResidualUnsweptRatchet` from `5` to `0` (`:38`) and update its doc comment (`:32-37`) to record that the amnesty is now closed — the constant's own comment says lowering it as batches land is the intended edit. Mirror the pattern used by the already-swept files: copy the block from `skills/exception-audit/SKILL.md` rather than retyping it, then hash-compare. |
| Scope | Production files (3): `skills/version-bump/SKILL.md`, `skills/workspace-health/SKILL.md`, `eng/banned-skill-markers.json`. Test files modified (1): `tests/RoslynMcp.Tests/Skills/SkillPrefixGenericityTests.cs` (ratchet constant + doc comment). No files deleted. Within Rule 3 (3 of 4) and Rule 4 (1 of 3) — no exemption claimed. `eng/verify-skills-are-generic.ps1` needs NO change: it already tolerates an empty allowlist (`:91-97` loops zero times). |
| Tool policy | edit-only |
| Estimated context cost | 25000 |
| Risks | Fanout probe (shape B, per-target defining files): a regex sweep of `skills/**` for `mcp__[A-Za-z0-9_-]*roslyn[A-Za-z0-9_-]*__` returned 49 occurrences across 24 files; the swept files sit at exactly 2 (the canonical block's two disclaimed illustrative prefixes), and only 6 files show 3 — the 5 allowlisted ones plus `skills/mcp-server-surface-test/SKILL.md`, whose third hit is the "examples, not an allowed list" prose the gate already exempts. That confirms all 3 offending literals in each target file live inside the precheck section, so replacing that section clears the file with no stray call sites; distinct edited files = 3 (2 skills + the policy JSON), matching `productionFilesTouched`. Conflict with `shipped-skills-prefix-batch-c1` (order 6) on TWO files, not one: `eng/banned-skill-markers.json` (both edit `residualUnsweptAllowlist`) and, if c1 also lowers the ratchet, `SkillPrefixGenericityTests.cs`; this initiative MUST land after and expects a rebase on both hunks — verify the allowlist is `[]` (not 2 or 3 entries) after rebasing. Byte-identity is the sharp edge: any smart-quote, em-dash, or trailing-whitespace mutation red-lines `CanonicalPrecheckBlocks_AreByteIdenticalWhereverTheyAppear` and the PS gate; `CanonicalBlockIdentity_DetectsASingleCharacterMutation` proves a one-character drift is caught. Emptying the allowlist removes the last amnesty, so both files are newly subject to `Assert-PrefixAgnostic` over their ENTIRE body — re-scan each whole file, not just the edited section. Keep the existing line endings so the diff stays to the touched hunks. Parent row `size: L` seeded a fanout-oversize prior; the live probe (3 files) contradicts it because Phase A already split the batch-c sweep 3+2, so the flag is cleared deliberately, not by omission. |
| Validation | 1. `pwsh -NoProfile -File ./eng/verify-skills-are-generic.ps1` — expect exit 0 and "Shipped skills under ./skills/ are generic (N .md files checked)" with zero issues, now that both files are gate-covered. 2. `dotnet test --filter "FullyQualifiedName~SkillPrefixGenericityTests"` — all tests green, specifically `ImperativeRule_ClearsEveryShippedSkillOutsideTheAllowlist`, `CanonicalPrecheckBlocks_AreByteIdenticalWhereverTheyAppear`, and `ResidualUnsweptAllowlist_OnlyShrinksAndEveryEntryExists` at ratchet 0. 3. Byte-identity spot check: extract the `## Connectivity precheck` span from each of `skills/version-bump/SKILL.md`, `skills/workspace-health/SKILL.md`, and `skills/exception-audit/SKILL.md`, truncate to the canonical line count, and confirm identical hashes. 4. Negative check: `rg -c "mcp__[A-Za-z0-9_-]*roslyn[A-Za-z0-9_-]*__" skills/version-bump/SKILL.md skills/workspace-health/SKILL.md` must report exactly 2 per file. 5. Confirm `skills/workspace-health/SKILL.md` still carries its trailer note and that `## Workflow` follows it. 6. CI parity: `./eng/verify-release.ps1 -Configuration Release`. |
| Performance review | N/A — documentation/config correctness fix, no hot-path changes. |
| CHANGELOG category | Fixed |
| CHANGELOG entry (draft) | Fixed: the shipped `version-bump` and `workspace-health` skills now resolve the Roslyn MCP tool prefix once from a `server_info` response shape and pin it, instead of instructing a hard-coded `mcp__roslyn__` literal — marketplace-plugin installs (which surface `mcp__plugin_roslyn-mcp_roslyn__*`) no longer halt on a prefix they never expose. This completes the shipped-skills sweep: `residualUnsweptAllowlist` in `eng/banned-skill-markers.json` is now empty and its shrink ratchet is pinned at 0, so `eng/verify-skills-are-generic.ps1` enforces the prefix-agnostic rule and canonical-block byte-identity across every shipped skill with zero amnesty. |
| Backlog sync | Close rows: [shipped-skills-hardcode-bare-roslyn-tool-prefix] (acceptance bullets 3 and 4 are satisfied only once the allowlist empties — this is the child that empties it). Mark obsolete: none. Update related: [skill-prefix-guard-tests-assert-bcl-not-gate] — note in that row that the guard now covers 100% of shipped skills, so its negative-path tests exercising BCL string semantics instead of the gate are the last remaining hole. |

<details>
<summary>Executor handoff notes</summary>

**Executor handoff:**

- **Copy source (verified):** the canonical 15-line block is `eng/banned-skill-markers.json:46-60` (`canonicalPrecheckBlocks[0].text`), shipped byte-identical at `skills/exception-audit/SKILL.md:23-37` — header line, blank, one-liner, blank, prefix blockquote, blank, steps 1-4, blank, bail blockquote, blank, step 5, trailing blank. Copy that 15-line span, do not retype.
- **Exact replacement spans:** `skills/version-bump/SKILL.md:23-33` (header at 23, blank at 33; `## Workflow` at 34 must stay put). `skills/workspace-health/SKILL.md:27-37` — the trailer at `:38` ("this skill's purpose is to report server/workspace status…") plus the blank at `:39` stay, so `## Workflow` remains at 40. `Assert-CanonicalBlockIdentity` (`eng/verify-skills-are-generic.ps1:146-152`) only compares the LEADING slice, so trailing prose is legal.
- **Test target:** edit `tests/RoslynMcp.Tests/Skills/SkillPrefixGenericityTests.cs:38` (`ResidualUnsweptRatchet = 5` → `0`) and its doc comment at `:32-37`. No new test file; assertions at `:224-225` already read the constant.
- **Rebase check:** the allowlist is currently 5 entries (`:32-38`); batch-c1 removes the first three. After rebase the array must be `[]` with the `residualUnsweptAllowlist` key retained, and the ratchet 0 (not 2).
- **Negative space:** do NOT edit `skills/semantic-find|test-coverage|trace-flow/SKILL.md` (batch-c1's files), the second canonical block `tool-prefix-inline-blockquote` (`banned-skill-markers.json:63-69`), or `eng/verify-skills-are-generic.ps1`.

</details>

### 8. method-description-diet-analysis-metrics

| Field | Content |
|---|---|
| Diagnosis | All four owned files exist and carry live `[McpServerTool]` methods. **The slice assignment's "10 `[McpServerTool]` methods total" is a miscount** — the real count is **6** tools; the extra 4 come from each file's `[McpServerToolType]` class attribute matching a `McpServerTool` substring grep. Measured method-level `[Description]` lengths today: `src/RoslynMcp.Host.Stdio/Tools/CohesionAnalysisTools.cs:17` get_cohesion_metrics 182 chars, `:67` find_shared_members 184; `src/RoslynMcp.Host.Stdio/Tools/ConsumerAnalysisTools.cs:16` find_consumers 382; `src/RoslynMcp.Host.Stdio/Tools/CouplingAnalysisTools.cs:15` get_coupling_metrics 293; `src/RoslynMcp.Host.Stdio/Tools/FlowAnalysisTools.cs:21` analyze_data_flow 173, `:44` analyze_control_flow 327. Slice aggregate **1,541 chars, avg 257**. Only **3 of 6** exceed the 250-char per-tool ceiling the sibling slices adopted (`tests/RoslynMcp.Tests/ToolDescriptionDietWorkspaceValidationTests.cs:26`); the other three are already compliant and need no edit. Root cause of the overage is duplicated guidance, not capability text: find_consumers's description restates the `projectFilter` clause nearly verbatim from that parameter's own `[Description]` at `ConsumerAnalysisTools.cs:26`, and analyze_control_flow carries a 150-char `EndPointIsReachable` semantics caveat that is operational guidance, not a discriminating capability statement. Verified the wire `[Description]` is NOT mirrored anywhere else: `ServerSurfaceCatalog.Analysis.cs:40/42` and `Catalog/Snapshots/catalog-v2.3.1.json:1414` carry the `[McpToolMetadata]` **summary**, and `SurfaceCatalogTests.McpToolMetadata_RequiredOnEveryTool_MatchesCatalogEntry` compares that summary against the catalog — so editing `[Description]` alone ripples into zero registration files. No test pins the exact description text of any of these 6 tools. |
| Approach | Method-level `[Description]` only — do NOT touch `[McpToolMetadata]` summaries (they are catalog-drift-gated) and do NOT touch parameter `[Description]`s (owned by the param-dedupe family). Edit exactly three descriptions, mirroring the pattern established by `ToolDescriptionDietWorkspaceValidationTests.cs` ("move operational guidance into XML `<remarks>` on the same method instead of the wire description"): (1) `ConsumerAnalysisTools.cs:16` find_consumers — KEEP the full dependency-kind enumeration (Constructor, Field, Parameter, BaseType, LocalVariable, Property, ReturnType, GenericArgument, StaticMemberAccess — that IS the capability and the ToolSearch discriminator vs find_references / find_type_usages) and KEEP a one-clause mention that `projectFilter` scopes the walk; drop only the case-sensitivity/comma-separated/`semantic_grep`-parity prose already carried verbatim by the parameter description at `:26`, relocating it to XML `<remarks>` if worth preserving in-source. (2) `CouplingAnalysisTools.cs:15` get_coupling_metrics — KEEP the Ca/Ce/Instability naming, the four classification buckets, and the "companion to get_cohesion_metrics, cohesion looks inward / coupling looks outward" discriminator; compress the formula prose. (3) `FlowAnalysisTools.cs:44` analyze_control_flow — KEEP "entry/exit points, reachability, return statements" plus a short pointer that EndPointIsReachable uses Roslyn fall-through semantics; relocate the full caveat sentence to XML `<remarks>` on `AnalyzeControlFlow`. Leave get_cohesion_metrics (182), find_shared_members (184) and analyze_data_flow (173) byte-identical — they are already inside the ceiling and rewriting them risks the PR #1348 dropped-clause failure for zero payload gain. Add one slice-scoped ratchet test `tests/RoslynMcp.Tests/ToolDescriptionDietAnalysisMetricsTests.cs`, a direct copy of the `ToolDescriptionDietWorkspaceValidationTests` reflection harness with `s_sweptToolTypes = [CohesionAnalysisTools, ConsumerAnalysisTools, CouplingAnalysisTools, FlowAnalysisTools]`, `MaxPerToolDescriptionCharacters = 250`, and `MaxSweptSetTotalCharacters = 1_300`. **Budget derivation (PR #1345 lesson — the ceiling is not a target):** 1,300 comes from real per-tool content — 182 + 184 + ~240 + ~215 + 173 + ~230 ~= 1,224 measured post-edit, plus ~75 headroom. It is NOT `6 x 250 = 1500`. Measure the actual post-edit aggregate and ratchet the constant to that number plus a small margin; if the real total lands materially under 1,224, lower the constant rather than banking slack. Do NOT add these four types to the shared `SurfaceCatalogTests.DietedToolMethodDescriptions_StayWithinSliceCeiling` array at `SurfaceCatalogTests.cs:319` — that file would collide with sibling diet slices 9-12 in this same sweep. |
| Scope | Production files: 4 (Rule 3 base cap, exactly at the limit, no exemption claimed) — `src/RoslynMcp.Host.Stdio/Tools/CohesionAnalysisTools.cs` (owned by the slice and enumerated by the new ratchet test; expected to need zero text edits), `src/RoslynMcp.Host.Stdio/Tools/ConsumerAnalysisTools.cs`, `src/RoslynMcp.Host.Stdio/Tools/CouplingAnalysisTools.cs`, `src/RoslynMcp.Host.Stdio/Tools/FlowAnalysisTools.cs`. The addenda's tool-surface-only 2-file shape is deliberately NOT invoked: it is an alternative shape allowance, not a narrowing of the base 4-file cap, and the four preceding sibling slices (PRs #1343, #1346, #1348, #1345) each shipped 3-4 `Tools/*.cs` files under the base cap. Test files added/modified: 1 new — `tests/RoslynMcp.Tests/ToolDescriptionDietAnalysisMetricsTests.cs`. Files deleted: none. Zero catalog/registration/README touches (verified: the catalog and its snapshot mirror `[McpToolMetadata]` summaries, not `[Description]`). |
| Tool policy | edit-only |
| Estimated context cost | 32000 |
| Risks | **Capability-loss risk (PR #1348 failure mode) is the primary one:** find_consumers's dependency-kind list and get_coupling_metrics's "companion to get_cohesion_metrics" clause are the ToolSearch discriminators against sibling tools — condensing must not delete them. Verify post-edit that each of the 6 descriptions still names what the tool does AND what distinguishes it from its siblings. **Budget-inflation risk (PR #1345 failure mode):** do not justify the aggregate constant as `toolCount x ceiling`; derive it from the measured post-edit total. **Formatter gate:** `eng/verify-changed-format.ps1` blocks NEW formatter debt on changed files; `eng/format-baseline.json:436/446/516` records a pre-existing `IMPORTS` finding for CohesionAnalysisTools, ConsumerAnalysisTools and FlowAnalysisTools (CouplingAnalysisTools is clean) — leave the using blocks alone so the baseline count does not move. **Test-class proliferation:** this adds a 4th per-slice ratchet class; the parent detail file already records a filed follow-on to consolidate them into one data-driven test — do not attempt that consolidation here (it would pull in sibling-owned files). **Anchor-count discrepancy:** the slice assignment says 10 tools, live probe says 6; the extra 4 are `[McpServerToolType]` class attributes. Executor should trust 6. `size: L` on the parent row is a parent-level split flag; the live probe on this bounded child returns 4 files, so `fanoutOversize` stays false. Fanout probe run (shape B, per-target defining files): 4 distinct defining files, 0 wiring/registration files. **Self-edit caveat:** work in the worktree; `toolPolicy: edit-only` means no `*_apply` calls are needed at all. |
| Validation | 1) `mcp__roslyn__compile_check` (or `dotnet build RoslynMcp.slnx -c Release -p:TreatWarningsAsErrors=true`) after the edits. 2) `dotnet test --filter "FullyQualifiedName~ToolDescriptionDietAnalysisMetricsTests"` — all three methods green (per-tool budget, slice aggregate, non-empty description). 3) `dotnet test --filter "FullyQualifiedName~SurfaceCatalogTests"` — proves the 2,000-char global ceiling, the `McpToolMetadata`-to-catalog drift check, and the existing dieted-slice ratchet are all unaffected. 4) `dotnet test --filter "FullyQualifiedName~ExpandedSurfaceIntegrationTests"` for tool-contract regressions. 5) `pwsh ./eng/verify-changed-format.ps1` — no new formatter findings on the four changed files. 6) `pwsh ./eng/verify-ai-docs.ps1` if any doc changed. 7) Manual check: re-run the char-count measurement and record the real post-edit aggregate in the PR body; confirm each of the 6 descriptions still carries its discriminating trigger. |
| Performance review | N/A — description-text-only change; reduces `tools/list` payload, touches no hot path. |
| CHANGELOG category | Maintenance |
| CHANGELOG entry (draft) | **Maintenance:** trimmed method-level tool descriptions for the cohesion, consumer, coupling, and flow analysis tool groups to 150-250-char capability statements (6 tools, ~1.5k chars down to ~1.2k), relocating the `projectFilter` restatement and the `EndPointIsReachable` semantics caveat to in-source XML remarks while keeping every discriminating trigger. Added a slice-scoped description-length regression test. |
| Backlog sync | Close rows: []. Mark obsolete: []. Update related: [method-description-diet] — append a slice amendment to `ai_docs/items/method-description-diet.md` recording the four files swept, the corrected tool count (6, not the 10 the slice text claimed — `[McpServerToolType]` inflates a naive grep by one per file), and the measured post-diet aggregate. The row stays OPEN; roughly 19 `Tools/*.cs` files remain unswept after this sweep's five diet children. |

<details>
<summary>Executor handoff notes</summary>

**Executor handoff:**

- **Pattern coordinates:** mirror `src/RoslynMcp.Host.Stdio/Tools/WorkspaceTools.cs:28-35` (PR #1346) — the `/// <remarks><para>…</para></remarks>` block sits *above* the whole attribute list and the trimmed `[Description(...)]` stays inline on the `[McpServerTool(...)]` line. Same shape at `:100-103` and `:126-129`.
- **Test target:** copy `tests/RoslynMcp.Tests/ToolDescriptionDietWorkspaceValidationTests.cs:26-51` verbatim into the new class, changing only the two constants and `s_sweptToolTypes` (lines 26-35). Keep the filter at line 46 as `.Where(entry => entry.Tool is not null)` — do NOT copy `SurfaceCatalogTests.cs:334`'s `&& entry.Description is not null` variant; that would make `SweptTools_AllHaveNonEmptyDescription` vacuous. Include `typeof(CohesionAnalysisTools)` even though it needs zero text edits.
- **Edge cases to cover:** (1) relocated prose containing `<` (e.g. `<remarks>`, generic angle brackets) breaks the XML doc comment under `TreatWarningsAsErrors` — PR #1346 reworded `<Compile>` to `Compile`; (2) backticks and `—` inside `<remarks>` were flattened to plain text/`-` there — follow suit; (3) re-measure the aggregate after editing and set `MaxSweptSetTotalCharacters` to that value plus a small margin.
- **Negative space:** the `[McpToolMetadata]` summary string sits on the line immediately *before* each target `[Description]` (`ConsumerAnalysisTools.cs:15`, `CouplingAnalysisTools.cs:13-14`, `FlowAnalysisTools.cs:42-43`) — leave it byte-identical (catalog-drift-gated). Do NOT edit the `projectFilter` param description at `ConsumerAnalysisTools.cs:26`, and do NOT add these types to `SurfaceCatalogTests.cs:319`.

</details>

### 9. method-description-diet-diagnostics-security

| Field | Content |
|---|---|
| Diagnosis | All four owned files exist and still carry oversized method-level `[Description]` attributes. Measured live (attribute-literal extraction, concatenations joined): **6** `[McpServerTool]` methods across the cluster totalling **3,130 chars** — `trace_exception_flow` 1,012 (`src/RoslynMcp.Host.Stdio/Tools/ExceptionFlowTools.cs:22`), `analyze_snippet` 682 (`src/RoslynMcp.Host.Stdio/Tools/SnippetAnalysisTools.cs:15`), `list_analyzers` 640 (`src/RoslynMcp.Host.Stdio/Tools/AnalyzerInfoTools.cs:15-18`, a 3-part concatenated literal), `nuget_vulnerability_scan` 449 (`src/RoslynMcp.Host.Stdio/Tools/SecurityTools.cs:55`), `security_diagnostics` 175 (`SecurityTools.cs:22`), `security_analyzer_status` 172 (`SecurityTools.cs:38`). Four exceed the ~200-char capability-statement ceiling; the last two already comply and MUST be left byte-identical. The bloat is uniformly operational detail that belongs in XML `<remarks>`: response-field dictionaries (`list_analyzers` documents `analyzerCount`/`totalRules`/`returnedRules`/`hasMore` inline), ranking/annotation mechanics (`trace_exception_flow` documents `rethrowAsTypeMetadataName`, `countOmitted`, catch-site ranking, untyped-`catch` treatment), and wrapper-kind semantics (`analyze_snippet` re-states the five `kind` values that `SnippetAnalysisTools.cs:20`'s own parameter `[Description]` already enumerates). **Slice-assignment correction:** the do-cell says "10 `[McpServerTool]` methods"; the live count is 6 (`grep -c McpServerTool` over-counted by matching the four `[McpServerToolType]` class attributes). The file set is unchanged and correct — only the tool count was wrong, and it makes this the cheapest cluster in the family. |
| Approach | Per file, for the four over-ceiling tools ONLY: (1) compress the `[Description]` to a ≤200-char capability statement that keeps the discriminating trigger, (2) move every displaced clause verbatim-in-substance into an XML `<remarks>` block placed immediately above the attribute list — mirror the shipped #1345 pattern at `src/RoslynMcp.Host.Stdio/Tools/MultiFileEditTools.cs:26-38` (a `<remarks>` block above `[McpServerTool(...)]` carrying the operational detail, with the `[Description]` reduced to capability + one trigger). Non-negotiable retained discriminators (PR #1348 lesson — condense, never drop): `trace_exception_flow` must still say it returns BOTH `catch` handling sites AND `throw new` origination sites and that `find_references` returns usage sites, not handling sites; `nuget_vulnerability_scan` must keep the direct-vs-transitive default (`includeTransitive=false` → direct references only) and the .NET 8+ SDK requirement; `analyze_snippet` must keep "ephemeral workspace, no solution load" plus a pointer that `kind` selects the wrapper (the five values stay documented on the `kind` parameter at `SnippetAnalysisTools.cs:20`, which is NOT owned by any param-dedupe sibling this sweep, so relocating the "use this for code without a return value" nuance there is permitted); `list_analyzers` must keep "which diagnostic IDs are available for `code_fix_preview` / `fix_all_preview`" and the "offset/limit paginate the flattened RULE list, not whole analyzers" pagination discriminator. Do NOT touch `security_diagnostics` / `security_analyzer_status` (already ≤200), any `McpToolMetadata` summary, or any `ServerSurfaceCatalog` entry — `SurfaceCatalogTests`' metadata-drift assertion (`tests/RoslynMcp.Tests/SurfaceCatalogTests.cs:280`) pins catalog summary == `McpToolMetadata` summary, and neither side changes here. Add one slice-scoped ratchet test file `tests/RoslynMcp.Tests/MethodDescriptionDietDiagnosticsSecurityTests.cs` modelled on `tests/RoslynMcp.Tests/MethodDescriptionDietScaffoldingMutationTests.cs`: per-tool ceiling 200, an aggregate ceiling, and a `TrimmedDescriptions_KeepTheirDiscriminatingTriggers` test asserting the four retained triggers above. **Budget the aggregate from measured post-edit content, not `toolCount x 200` (PR #1345 lesson)** — projected post-diet aggregate is ~1,050-1,150 chars; set the constant to the ACTUAL measured value plus modest headroom and document the pre/post figures in the test's XML doc comment. Deliberately do NOT extend `SurfaceCatalogTests.DietedToolMethodDescriptions_StayWithinSliceCeiling` (`SurfaceCatalogTests.cs:312-357`) — four sibling diet initiatives run in this same sweep and that shared list is a guaranteed merge conflict; the already-filed consolidation follow-on owns unifying the per-slice classes later. |
| Scope | Production files: 4 — `src/RoslynMcp.Host.Stdio/Tools/ExceptionFlowTools.cs`, `src/RoslynMcp.Host.Stdio/Tools/SecurityTools.cs`, `src/RoslynMcp.Host.Stdio/Tools/SnippetAnalysisTools.cs`, `src/RoslynMcp.Host.Stdio/Tools/AnalyzerInfoTools.cs`. Exactly at the Rule 3 cap of 4 — no exemption claimed or needed. Test files added/modified: 1 — new `tests/RoslynMcp.Tests/MethodDescriptionDietDiagnosticsSecurityTests.cs` (Rule 4: 1 of 3). Changelog fragment `changelog.d/method-description-diet-diagnostics-security.md` (convention-exempt per addenda). Files deleted: none. |
| Tool policy | edit-only |
| Estimated context cost | 42000 |
| Risks | (1) The parent row's acceptance names ServerInstructions / prompts / the paginated catalog as relocation targets, but both shipped precedents (#1345, #1348) relocated to XML `<remarks>`; this stanza follows the shipped precedent, which keeps the slice inside 4 production files and avoids touching the hotspot `ServerSurfaceCatalog` partials. If a reviewer insists on the literal acceptance wording, that is a family-wide decision, not a per-slice one. (2) The parent row declares `deps: server-instructions-discovery-hint`; the remarks-relocation approach does not consume that dependency, so this slice is not blocked by it. (3) Fanout probe (shape B — per-target edit of N named things): resolved each of the 6 tools to its DEFINING file — 4 distinct files, zero wiring/registration files. Cross-checked that no other production or test file mirrors the description text (`rg` for the distinctive tokens `rethrowAsTypeMetadataName` and `IncludesTransitive` returns only the owning `Tools/*.cs` file plus archived review docs), and no test asserts these six tools' method-description substrings — the tool-name hits in `PromptSmokeTests.cs:117/296/298`, `FixAllServiceTests.cs:278/294/327` and `SecurityDiagnosticIntegrationTests.cs:216/218` assert prompt text and tool registration, not descriptions. `fanoutEstimate: 4`, matching `productionFilesTouched`. (4) The parent row is `size: L`, which seeds an oversize prior; the live probe contradicts it because this is a Phase-A per-cluster child — 6 tools / 4 files is the smallest cluster in the family, so `fanoutOversize` is false. (5) Slice-assignment tool count was wrong (10 claimed, 6 actual) — verify the count before sizing the aggregate ceiling. (6) `analyze_snippet`'s description encodes a user-visible correctness contract (diagnostic line AND column are 1-based relative to the user snippet, wrapper offsets subtracted — UX-001/FLAG-C); that clause is regression-tested by `tests/RoslynMcp.Tests/AuditFixesTests.cs:34-51`, so moving it to `<remarks>` is safe, but do not delete it outright. (7) No rejected bundle peers — `bundleCandidatePeers` was empty. |
| Validation | `mcp__roslyn__compile_check` on the four edited files after each edit (self-edit caveat: worktree session, so `*_preview`/`*_apply` are permitted but unnecessary here — this is edit-only). Then `dotnet test --filter "FullyQualifiedName~MethodDescriptionDietDiagnosticsSecurityTests"` (new per-tool + aggregate ratchet must pass with the measured constant), `dotnet test --filter "FullyQualifiedName~SurfaceCatalogTests"` (the assembly-wide 2,000-char guard, the `McpToolMetadata` drift assertion, and the existing dieted-slice ratchet must all stay green), and `dotnet test --filter "FullyQualifiedName~AuditFixesTests"` (proves the `analyze_snippet` 1-based-column contract still holds after the description move). Re-measure the six descriptions post-edit and confirm the aggregate is materially below `6 x 200` — if it lands near 1,200 the trim was under-done. Final gate: `./eng/verify-release.ps1 -Configuration Release` plus `./eng/verify-ai-docs.ps1`. |
| Performance review | N/A — description-text-only change; no hot-path code touched. Net effect is a reduction of the `tools/list` payload (~2,000 chars removed from the wire). |
| CHANGELOG category | Maintenance |
| CHANGELOG entry (draft) | **Maintenance:** Trimmed method-level tool descriptions for the exception-flow, security, snippet-analysis, and analyzer-info tool groups to ~200-char capability statements, moving operational detail to XML remarks. Cuts the `tools/list` payload for these 6 tools from 3,130 to ~1,100 characters with no capability statement or discriminating trigger dropped. |
| Backlog sync | Close rows: []. Mark obsolete: []. Update related: [method-description-diet] — append a per-slice amendment recording this cluster (`ExceptionFlowTools`, `SecurityTools`, `SnippetAnalysisTools`, `AnalyzerInfoTools`; 6 tools, 3,130 → ~1,100 chars) and note that the do-cell tool count for the remaining clusters should be re-derived with `grep -c "McpServerTool(Name"`, not `grep -c McpServerTool`. The parent row stays OPEN — ~36 `Tools/*.cs` files remain unswept. |

<details>
<summary>Executor handoff notes</summary>

**Executor handoff:**

- **Pattern coordinates:** mirror `src/RoslynMcp.Host.Stdio/Tools/MultiFileEditTools.cs:26-38` (verified) — `/// <remarks>` block sits ABOVE the whole attribute list, then `[McpServerTool(...)]`, `McpToolMetadata(...)`, `Description(...)` last, comma-joined in one attribute group.
- **Test target:** create `tests/RoslynMcp.Tests/MethodDescriptionDietDiagnosticsSecurityTests.cs`; copy the shape of `MethodDescriptionDietScaffoldingMutationTests.cs` — aggregate-constant XML doc at lines 21-27 (records measured pre/post figures), `AssertDescriptionContains` helper at lines 81-90, reflection enumerator at 92-102. Do NOT extend `SurfaceCatalogTests.cs:319-324`'s `dietedTypes` array.
- **Per-file seams:** `AnalyzerInfoTools.cs:15-18` is a 3-part `+`-concatenated literal — collapse to ONE literal, don't leave orphan fragments. `ExceptionFlowTools.cs:8-15` already has a class-level `<summary>`; the new `<remarks>` attaches to the METHOD at line 19, not merged upward. `SecurityTools.cs` — edit only the `nuget_vulnerability_scan` `Description` at line 55.
- **Edge cases:** (1) `analyze_snippet`'s 1-based line/column UX-001/FLAG-C clause moves to `<remarks>`, never deleted (behavior pinned by `AuditFixesTests.cs:34-52`); (2) the five `kind` values already live on the param `[Description]` at `SnippetAnalysisTools.cs:20` — relocating nuance there is in-scope.
- **Negative space:** do NOT touch `SecurityTools.cs:22` and `:38` (`security_diagnostics` / `security_analyzer_status` — already ≤200, attribute+Description share one line, so a careless reflow mutates them); do NOT edit any `McpToolMetadata(...)` summary string in the four files — `SurfaceCatalogTests.cs:280` pins catalog↔metadata equality.

</details>

### 10. method-description-diet-server-surface

| Field | Content |
|---|---|
| Diagnosis | The four assigned files carry **5** `[McpServerTool]` methods (not 9 as the slice assignment states — verified by grepping `[McpServerTool(` across all four: `ServerTools.cs:79` `server_info`, `ServerTools.cs:292` `server_heartbeat`, `WorkflowRecommendationTools.cs:11` `recommend_workflow`, `SuggestionTools.cs:11` `suggest_refactorings`, `PromptShimTools.cs:45` `get_prompt_text`). Measured method-level `[Description]` lengths: `server_info` 1,349 chars (`src/RoslynMcp.Host.Stdio/Tools/ServerTools.cs:83`), `server_heartbeat` 689 (`ServerTools.cs:296`), `get_prompt_text` 333 (`src/RoslynMcp.Host.Stdio/Tools/PromptShimTools.cs:48`), `suggest_refactorings` 253 (`src/RoslynMcp.Host.Stdio/Tools/SuggestionTools.cs:14`), `recommend_workflow` 202 (`src/RoslynMcp.Host.Stdio/Tools/WorkflowRecommendationTools.cs:14`). Cluster aggregate **2,826 chars across 5 tools (565 avg)** — 1.4x the 393-char surface-wide average the parent row cites. Root cause is duplication, not density: the idle/ready/degraded state machine is written out THREE times — canonically in the XML `<summary>` on `BuildConnection` (`ServerTools.cs:22-52`), then verbatim again in both tool `[Description]`s; and `suggest_refactorings`'s `[Description]` (`SuggestionTools.cs:14`) is a near-verbatim restatement of its own `McpToolMetadata` summary two lines above (`SuggestionTools.cs:12-13`). Only the `[Description]` copy is billed on every `tools/list`. |
| Approach | Method-level `[Description]` only; parameter `[Description]`s are the `param-description-dedupe` family's and stay untouched. Mirror the shipped pattern from PR #1345 (`ScaffoldingTools`) and PR #1346 (`WorkspaceTools`): condense each `[Description]` to a capability statement plus its discriminating trigger, and relocate operational detail to XML `<remarks>` on the tool method — never delete it. (1) `ServerTools.cs:83` `server_info` to <=200 chars, keeping the discriminators that ToolSearch keys on: returns version/capabilities/runtime/workspace count plus the `connection` readiness block; points at `workspace_list` for authoritative session enumeration and at `server_heartbeat` as the cheap variant. Move the state machine, the prompts-tier `stable=0` note, and the `workspaceCount` lag caveat to `<remarks>` on `GetServerInfo`, cross-referencing the existing canonical copy on `BuildConnection` (`ServerTools.cs:22-52`) instead of writing a third one. (2) `ServerTools.cs:296` `server_heartbeat` to <=200, keeping its discriminator vs `server_info` (connection block only; no version/catalog/update payload) AND the behavioral refusal clause "do not poll waiting for `idle` to self-advance — a `workspace_load` call is required" — relocate it to `<remarks>` rather than dropping it (PR #1348 lesson). (3) `SuggestionTools.cs:14` `suggest_refactorings` to <=200, keeping all three discriminating signals (complexity, LCOM4 cohesion, unused-symbol detection) and the ranked-plus-recommended-tool-sequence output shape. (4) `PromptShimTools.cs:48` `get_prompt_text` to <=200, keeping the discriminator "for clients that cannot use the `prompts/get` channel" and the `promptName` plus `parametersJson` contract; move the example prompt names to `<remarks>`. (5) `WorkflowRecommendationTools.cs:14` `recommend_workflow` is already 202 chars — trim only redundant words to land <=200; the `primaryTools`/`followUpTools`/`avoid`/`why`/`requiredWorkspaceState` field list IS its discriminator and must survive intact. (6) Add `tests/RoslynMcp.Tests/MethodDescriptionDietServerSurfaceTests.cs` modeled on `tests/RoslynMcp.Tests/MethodDescriptionDietScaffoldingMutationTests.cs:17-60`: per-tool ceiling 200 plus a slice aggregate ceiling. **Do NOT touch the `McpToolMetadata` summary strings** — `SurfaceCatalogTests` asserts summary parity against `src/RoslynMcp.Host.Stdio/Catalog/ServerSurfaceCatalog.Workspace.cs:7-8`, so editing one drags the catalog hotspot in as a 5th file; PRs #1345/#1346 touched zero catalog files for exactly this reason. |
| Scope | Production files: 4 — `src/RoslynMcp.Host.Stdio/Tools/ServerTools.cs`, `src/RoslynMcp.Host.Stdio/Tools/WorkflowRecommendationTools.cs`, `src/RoslynMcp.Host.Stdio/Tools/SuggestionTools.cs`, `src/RoslynMcp.Host.Stdio/Tools/PromptShimTools.cs`. Exactly at the Rule 3 cap; no exemption claimed or needed. Test files added: 1 — `tests/RoslynMcp.Tests/MethodDescriptionDietServerSurfaceTests.cs`. Files deleted: none. Also emits `changelog.d/method-description-diet-server-surface.md` (fragment convention; not counted in the production budget). |
| Tool policy | edit-only |
| Estimated context cost | 45000 |
| Risks | **Aggregate ceiling must be MEASURED, not computed.** Set the slice aggregate constant from the real post-diet total plus small headroom — never `toolCount x 200` (5 x 200 = 1,000 is a ceiling product, not a floor; PR #1345 shipped a first attempt asserting 2,800 against a spec'd 2,200 on exactly that bad reasoning). Realistic post-diet aggregate here is roughly 850-1,050 from 2,826. **Never drop a clause where the Approach says condense** (PR #1348 dropped `extract_shared_expression_to_helper_preview`'s refusal clause) — the two at risk here are `server_heartbeat`'s do-not-poll-for-idle clause and `server_info`'s workspace_list-is-authoritative pointer. **Discovery-surface blast radius:** these five ARE the discovery front door; a lost discriminator degrades ToolSearch routing silently with no failing test. Diff each old/new description side by side and confirm every trigger word survived. **Slice-assignment count is stale:** it says 9 `[McpServerTool]` methods; the live count is 5 — budget and ratchet re-derived from the live count. **`recommend_workflow` is already at 202 chars** — near-zero token payoff there; do not damage its field list to win 2 chars. **No test pins these strings:** verified `ServerHeartbeatTests.cs`, `PromptShimToolsTests.cs`, `ServerDiscoveryWireTests.cs` (its only description assertion, at :266, targets a resource catalog listing, not these tools) and `PromptSmokeTests.cs` assert behavior only. Fanout probe (shape B, per-target edit of 5 named tools resolved to their defining files): 4 distinct defining files, 0 registration/enumeration companions — the assembly-wide 2,000-char guard at `tests/RoslynMcp.Tests/SurfaceCatalogTests.cs:287` only relaxes under a trim, and the catalog partial is out of scope by design. |
| Validation | Per-edit: `mcp__roslyn__compile_check` (worktree only — the self-edit caveat forbids `*_preview`/`*_apply` in the main checkout). Targeted: `dotnet test --filter "FullyQualifiedName~MethodDescriptionDietServerSurfaceTests"`, then the pinning neighbors `SurfaceCatalogTests`, `ServerHeartbeatTests`, `ServerDiscoveryWireTests`, `PromptShimToolsTests`, `PromptSmokeTests`. Full gate: `./eng/verify-release.ps1 -Configuration Release` plus `./eng/verify-ai-docs.ps1`. Manual check before opening the PR: print each tool's before/after `[Description]` and confirm (a) every one is <=200 chars, (b) the measured aggregate matches the constant asserted in the new test, (c) each of the 5 still names its discriminating trigger vs its sibling tools, and (d) every relocated sentence lands in an XML `<remarks>` block rather than being deleted. |
| Performance review | N/A — description-text-only change, no hot-path code. The payload reduction on `tools/list` is the intended effect, not a runtime optimization. |
| CHANGELOG category | Maintenance |
| CHANGELOG entry (draft) | - **Maintenance:** Trimmed method-level tool descriptions for the server/discovery surface (`server_info`, `server_heartbeat`, `recommend_workflow`, `suggest_refactorings`, `get_prompt_text`) to <=200-char capability statements, relocating the connection state-machine, prompts-tier note, and usage guidance to XML remarks. Cuts the `tools/list` payload for these 5 tools from 2,826 characters to <measured> with no capability statement or discriminating trigger dropped. |
| Backlog sync | Close rows: []. Mark obsolete: []. Update related: [method-description-diet] — parent row stays OPEN (this is one per-cluster child; ~40 `Tools/*.cs` remain across this and sibling clusters). Append this cluster, its measured before/after aggregate, and the corrected 5-tool count to the amendment section of `ai_docs/items/method-description-diet.md`. |

<details>
<summary>Executor handoff notes</summary>

**Executor handoff:**

- **Pattern coordinates:** the shipped shape is `src/RoslynMcp.Host.Stdio/Tools/ScaffoldingTools.cs:25-32` — XML `<remarks>` block sits *above* the attribute list, `[Description]` is the last attribute in the comma-joined list. Verified live targets: `ServerTools.cs:83` (server_info), `ServerTools.cs:296` (server_heartbeat), `SuggestionTools.cs:14`, `PromptShimTools.cs:48`, `WorkflowRecommendationTools.cs:14`.
- **Doc-comment state (per target):** `GetServerHeartbeat` already has a `<summary>` at `ServerTools.cs:285-291` — *append* a `<remarks>` sibling, do not overwrite it. `GetServerInfo`, `SuggestRefactorings`, `GetPromptText`, `RecommendWorkflow` have no doc comment; create `<remarks>` fresh. `SuggestionTools.cs:11-14` uses separate bracketed attributes, not the comma-joined form — keep its existing style.
- **Test target:** create `tests/RoslynMcp.Tests/MethodDescriptionDietServerSurfaceTests.cs` mirroring `MethodDescriptionDietScaffoldingMutationTests.cs` — copy the const pair (`:19`, `:27`), both test methods (`:37-68`), and the `EnumerateSliceTools` reflection helper (`:92-102`) verbatim. Also port `TrimmedDescriptions_KeepTheirDiscriminatingTriggers` (`:70-90`) and pin the two at-risk clauses: heartbeat's do-not-poll-for-`idle` and server_info's `workspace_list`-is-authoritative pointer.
- **Negative space:** do NOT edit the `BuildConnection` `<summary>` at `ServerTools.cs:22-52` — that is the canonical state-machine copy your `<remarks>` should cross-reference. Do NOT touch any `McpToolMetadata` summary (`ServerTools.cs:81-82`/`294-295`, `SuggestionTools.cs:12-13`, `PromptShimTools.cs:46-47`, `WorkflowRecommendationTools.cs:12-13`), any parameter `[Description]`, or the 2,000-char constant at `SurfaceCatalogTests.cs:287`.

</details>

### 11. method-description-diet-test-tooling

| Field | Content |
|---|---|
| Diagnosis | The four owned files exist and carry **5** `[McpServerTool]` methods, not the 9 the Phase-A slice assignment states (verified by reflection-shaped grep over `[McpServerTool(Name = ` in each file). Measured method-level description lengths: `src/RoslynMcp.Host.Stdio/Tools/TestCoverageTools.cs:16` `test_coverage` = 418 chars and `:225` `get_test_coverage_map` = 246; `src/RoslynMcp.Host.Stdio/Tools/TestReferenceMapTools.cs:17` `test_reference_map` = 349; `src/RoslynMcp.Host.Stdio/Tools/ImpactSweepTools.cs:17` `symbol_impact_sweep` = 382; `src/RoslynMcp.Host.Stdio/Tools/RecordImpactTools.cs:18` `preview_record_field_addition` = 460. Slice aggregate = **1,855 chars across 5 tools (avg 371)** — squarely the 393-avg pattern the parent row targets. The over-length mass is guidance, not capability: `test_coverage` spends ~230 chars enumerating `TestCoverageResultDto` field names and the alias `deprecation` envelope; `symbol_impact_sweep` spends ~180 chars on a Jellyfin payload-size anecdote and pagination advice that its own parameter descriptions already carry; `preview_record_field_addition` spends ~250 chars re-listing site categories that the response DTO names. **No hotspot file is dragged in:** `ServerSurfaceCatalog.Analysis.cs:32/33/41/52` and the pinned `Catalog/Snapshots/catalog-v2.3.1.json` mirror the `McpToolMetadata` *Summary* string, not the method-level description — verified by reading both — so this slice touches zero catalog/registration files. Existing ratchet homes found: `tests/RoslynMcp.Tests/SurfaceCatalogTests.cs:312` (3 types, 250 per-tool / 9,500 aggregate), `tests/RoslynMcp.Tests/MethodDescriptionDietScaffoldingMutationTests.cs:19` (200 / 2,200), `tests/RoslynMcp.Tests/RefactoringCoreDescriptionBudgetTests.cs:17` (220), `tests/RoslynMcp.Tests/ToolDescriptionDietWorkspaceValidationTests.cs`. The assembly-wide 2,000-char guard at `SurfaceCatalogTests.cs:285` stays globally owned and is untouched. No test asserts any substring of these 5 descriptions today (grepped `tests/` for all 5 tool names against Description/StringAssert usage — only `PromptSmokeTests.cs:792` matches, and it asserts prompt body text containing the tool *name*). |
| Approach | Method-level `[Description]` only — parameter descriptions, `McpToolMetadata` summaries, catalog entries, and the snapshot JSON are out of scope. Trim each of the 5 to a ≤200-char capability statement, relocating operational detail into an XML `<remarks>` block on the same method, mirroring the rationale recorded at `tests/RoslynMcp.Tests/MethodDescriptionDietScaffoldingMutationTests.cs:9-13`. Discriminators that MUST survive verbatim-in-substance (PR #1348 lesson — condense never means drop): `test_coverage` keeps "runs tests" + "requires coverlet.collector" (its discriminator against `test_run` and against `test_reference_map`); `get_test_coverage_map` keeps "alias for `test_coverage`" + "prefer `test_coverage`" (its only reason to exist); `test_reference_map` keeps "static, without running tests" + covered/uncovered + coverage percent (its discriminator against `test_coverage`); `symbol_impact_sweep` keeps the three result buckets — references, non-exhaustive switches (CS8509/CS8524/IDE0072), mapper/converter-suffix callsites; `preview_record_field_addition` keeps "positional field added to a record" + "catches breaking shapes the compiler does NOT flag" + the site categories (construction, deconstruction, property-pattern, with-expression). Moved-to-`<remarks>` material: the `TestCoverageResultDto` field roll-call and alias-envelope mechanics, the `summary`/`maxItemsPerCategory` payload-size advice and Jellyfin anecdote, and the `projectName` dual-scoping paragraph (already stated in full on that parameter's own description at `TestReferenceMapTools.cs`). Add ONE slice-scoped regression, `tests/RoslynMcp.Tests/MethodDescriptionDietTestToolingTests.cs`, mirroring `MethodDescriptionDietScaffoldingMutationTests.cs` structure: (a) per-tool ceiling 200 over the four slice types, (b) an aggregate ceiling set from the **measured** post-diet total plus ≤10 percent headroom — NOT `5 x 200` (PR #1345 lesson; state the measured before/after numbers in the constant's doc comment), (c) discriminator-retention asserts, one `StringAssert.Contains` per tool from the list above. Deliberately do NOT extend `SurfaceCatalogTests.cs:312`'s `dietedTypes` array: that test's per-tool cap is 250 (looser than this row's target) and its shared 9,500 aggregate would couple this PR to the four sibling diet slices running in the same sweep. |
| Scope | Production files (4): `src/RoslynMcp.Host.Stdio/Tools/TestCoverageTools.cs`, `src/RoslynMcp.Host.Stdio/Tools/TestReferenceMapTools.cs`, `src/RoslynMcp.Host.Stdio/Tools/ImpactSweepTools.cs`, `src/RoslynMcp.Host.Stdio/Tools/RecordImpactTools.cs`. Exactly at the Rule 3 cap of 4 — **no exemption claimed or needed**. Test files added/modified (1): `tests/RoslynMcp.Tests/MethodDescriptionDietTestToolingTests.cs` (new). Rule 4: 1 of 3. Files deleted: none. Changelog fragment `changelog.d/method-description-diet-test-tooling.md` and `ai_docs/items/method-description-diet.md` are excluded from the production count per Rule 3. Explicitly NOT touched: any `ServerSurfaceCatalog*.cs` partial, `Catalog/Snapshots/catalog-v2.3.1.json`, `SurfaceCatalogTests.cs`, the `Core/Services/I*Service.cs` XML docs that carry similar prose, and every other `*Tools.cs` file (owned by sibling initiatives orders 8/9/10/12). |
| Tool policy | edit-only |
| Estimated context cost | 40000 |
| Risks | **Slice-assignment count is wrong** — the input says "9 `[McpServerTool]` methods"; the live tree has 5. Budget the slice from the measured 1,855-char baseline, not from the stale count. **PR #1345 trap (ceiling-not-target):** the aggregate ratchet constant must be derived by measuring the post-diet total, never by multiplying tool count by the 200-char ceiling; realistic post-diet aggregate is ~800-900 chars, so a `5 x 200 = 1000` constant would silently license 100+ chars of un-dieted text. **PR #1348 trap (dropped clause):** `get_test_coverage_map` is 246 chars of almost-pure discriminator (alias identity + migration pointer) — trimming it to fit 200 is the most likely place a capability statement gets lost; if it cannot reach 200 without losing the "prefer `test_coverage`" pointer, keep the pointer and record the tool at its honest length rather than dropping it. **ToolSearch discovery:** `test_coverage` and `test_reference_map` are near-synonyms by name; the runtime-vs-static distinction is the only thing separating them for capability search, so both descriptions must retain it. **Ratchet-home fragmentation (Directive #3 flag, out of scope here):** four slice-scoped ratchet classes now exist with three different per-tool caps (200 / 220 / 250) plus the assembly-wide 2,000 guard, and `ai_docs/items/method-description-diet.md:42` claims the consolidation follow-on was "filed separately" — no such row exists in `ai_docs/backlog.md` (grepped). Recommend a new row `method-description-diet-ratchet-consolidation` (1 test file, 4 deletions, one data-driven table) before the remaining ~36 files are swept; do not fold it into this slice. **Fanout:** probe shape B (per-target edit of named things) — the 5 targets resolve to 4 distinct defining files and 0 registration/wiring files, because the catalog and snapshot mirror `McpToolMetadata` summaries rather than method descriptions; probe count 4 equals `productionFilesTouched` 4. |
| Validation | Per-edit: `mcp__roslyn__compile_check` scoped to `RoslynMcp.Host.Stdio` after each file (read-side, safe in the main checkout; `*_apply` and `*_preview` remain forbidden there per the addenda self-edit caveat, and this initiative is edit-only regardless). Targeted tests: `dotnet test --filter "FullyQualifiedName~MethodDescriptionDietTestTooling"` (new ratchet), `--filter "FullyQualifiedName~SurfaceCatalogTests"` (assembly-wide 2,000-char guard plus the `McpToolMetadata` drift test that would catch an accidental catalog-summary edit), and `--filter "FullyQualifiedName~PromptSmokeTests"`. Gates: `./eng/verify-changed-format.ps1` (formatter debt gate on changed C# files), `./eng/verify-changelog-fragments.ps1` (fragment needs the `category: Maintenance` YAML frontmatter). Full CI equivalent: `./eng/verify-release.ps1 -Configuration Release`. Manual evidence to paste in the PR body: the per-tool char counts before and after for all 5 tools plus the aggregate delta (baseline 1,855). |
| Performance review | N/A — description text only. Net effect is a reduction in every `tools/list` payload (est. ~1,000 chars, ~250 tokens); no hot-path code changes. |
| CHANGELOG category | Maintenance |
| CHANGELOG entry (draft) | - **Maintenance:** Trimmed method-level `[Description]` text on the test-and-impact tools (`test_coverage`, `get_test_coverage_map`, `test_reference_map`, `symbol_impact_sweep`, `preview_record_field_addition`) to ~200-char capability statements, cutting ~1,000 chars (~250 tokens) from every `tools/list` response. Response-field roll-calls, payload-size advice, and pagination detail moved to XML `<remarks>` and were already stated on the parameter descriptions — no capability or discriminating trigger was dropped. Added a slice-scoped description-length regression. |
| Backlog sync | Close rows: none — this is a per-cluster slice of `method-description-diet` and roughly 36 `Tools/*.cs` files remain unswept. Mark obsolete: none. Update related: append a slice record to `ai_docs/items/method-description-diet.md` (files swept, measured before/after aggregate) and recommend a new row `method-description-diet-ratchet-consolidation` for the four fragmented slice ratchet classes. |

<details>
<summary>Executor handoff notes</summary>

**Executor handoff:**

- **Pattern coordinates:** copy `tests/RoslynMcp.Tests/MethodDescriptionDietScaffoldingMutationTests.cs:16-103` wholesale — class doc `:8-14`, per-tool const `:19`, measured-aggregate doc comment `:21-27` (state 1,855 before / measured after), the two ratchet tests `:37-68`, `AssertDescriptionContains` `:81-90`, and the reflection enumerator `:92-102` (it already handles static tool methods).
- **`<remarks>` placement:** XML `<remarks>` goes ABOVE the attribute list, not between attributes — see `src/RoslynMcp.Host.Stdio/Tools/ScaffoldingTools.cs:25-32` (remarks block, then `[McpServerTool…, McpToolMetadata…, Description…]`).
- **Discriminator-assert trap:** `test_reference_map`'s method `[Description]` (`TestReferenceMapTools.cs:21`) does NOT contain "covered/uncovered" or "coverage percent" today — that wording lives only in the `McpToolMetadata` summary at `:19-20`. Anchor its assert on "statically" + "without running tests"; do not import summary wording (would grow, not trim, and touches catalog-mirrored text).
- **Preserve in place:** the alias-linking comments at `TestCoverageTools.cs:35-39` and `:222-224` — they explain `RunTestCoverageCore`, not description prose.
- **Negative space:** do NOT touch `ValidationTools.cs:199` (`test_run` — near-synonym, different slice); do NOT edit any `McpToolMetadata(...)` summary string on the four scope files (`TestCoverageTools.cs:17-18`/`:226-227`, `TestReferenceMapTools.cs:19-20`, `ImpactSweepTools.cs:18-19`, `RecordImpactTools.cs:19-20`) — the pinned catalog snapshot mirrors those; do NOT add types to `SurfaceCatalogTests.cs:319-324` `dietedTypes`.

No hotspot seam: all four scope files are ≤288 LOC.

</details>

### 12. method-description-diet-editing-msbuild

| Field | Content |
|---|---|
| Diagnosis | Measured live: the four owned files carry **11** `[McpServerTool]` methods (not 15 — the slice assignment's count matched the four `[McpServerToolType]` class attributes too), totalling **3,770** method-`[Description]` chars, avg 343. Eight exceed the ~200-char capability-statement ceiling: `apply_text_edit` 774 (`src/RoslynMcp.Host.Stdio/Tools/EditTools.cs:18`), `record_field_add_with_satellites_preview` 496 (`src/RoslynMcp.Host.Stdio/Tools/SymbolRefactorTools.cs:57`), `split_service_with_di_preview` 435 (`src/RoslynMcp.Host.Stdio/Tools/SymbolRefactorTools.cs:37`), `pragma_scope_widen` 402 (`src/RoslynMcp.Host.Stdio/Tools/SuppressionTools.cs:84`), `evaluate_msbuild_items` 385 (`src/RoslynMcp.Host.Stdio/Tools/MSBuildTools.cs:36`), `get_msbuild_properties` 317 (`src/RoslynMcp.Host.Stdio/Tools/MSBuildTools.cs:53`), `symbol_refactor_preview` 311 (`src/RoslynMcp.Host.Stdio/Tools/SymbolRefactorTools.cs:21`), `verify_pragma_suppresses` 294 (`src/RoslynMcp.Host.Stdio/Tools/SuppressionTools.cs:65`). Three are already compliant and need no edit: `evaluate_msbuild_property` 115, `set_diagnostic_severity` 140, `add_pragma_suppression` 101. The relocation target already exists in-file: every tool carries a short `McpToolMetadata` summary that the catalog mirrors (`src/RoslynMcp.Host.Stdio/Catalog/ServerSurfaceCatalog.Editing.cs:7,21`), and that summary is a SEPARATE string from the method `[Description]`, so trimming descriptions cannot trip the `McpToolMetadata` drift assertion and no catalog file is touched. |
| Approach | For each of the eight over-ceiling tools, rewrite the method `[Description]` to a ≤200-char capability statement (what it does plus its one discriminating trigger) and move the displaced operational detail into an XML `<remarks>` block on the method — mirroring the shape shipped by PR #1345 and pinned by `tests/RoslynMcp.Tests/MethodDescriptionDietScaffoldingMutationTests.cs`. Clauses that MUST survive verbatim-in-substance (PR #1348 lesson — condense, never drop): `apply_text_edit` keeps "prefer a semantic preview/apply tool when one exists" plus revertible-via-`revert_last_apply` (its verify / autoRevertOnError mechanics are already stated on the `verify` and `autoRevertOnError` parameter descriptions, so they move to remarks as pure duplication); `pragma_scope_widen` keeps the REFUSAL clause (refuses when relocating the restore crosses a `#region` boundary or nests into another disable for the same id); `verify_pragma_suppresses` keeps the "cosmetic pragma" detection trigger and read-only marker; `record_field_add_with_satellites_preview` keeps the conservative ≥2-sibling-field refusal and `patternDetectionReason`; `symbol_refactor_preview` keeps atomic-abort-on-any-step-failure; `split_service_with_di_preview` keeps partitions plus forwarding facade plus DI-registration deltas; `evaluate_msbuild_items` keeps the item-type examples and moves the DocumentCount+3 note to remarks; `get_msbuild_properties` keeps "output is large, pass a filter" and moves the totalCount/returnedCount/appliedFilter detail to remarks. Then add one slice ratchet test file `tests/RoslynMcp.Tests/MethodDescriptionDietEditingMsBuildTests.cs` modeled directly on `MethodDescriptionDietScaffoldingMutationTests.cs`: per-tool ceiling 200, slice aggregate ceiling derived from the MEASURED post-diet total (projected ~1,800 from real per-tool content — never `toolCount x 200`, PR #1345 lesson), plus a discriminator-retention test asserting the refusal/trigger clauses above still appear. Do NOT add these four types to `SurfaceCatalogTests.DietedToolMethodDescriptions_StayWithinSliceCeiling` — four sibling method-diet initiatives run in this same sweep and would all collide on that one file; the consolidation into a single data-driven ratchet is an already-filed follow-on. |
| Scope | Production files: 4 (exactly the Rule 3 cap, no exemption claimed) — `src/RoslynMcp.Host.Stdio/Tools/EditTools.cs`, `src/RoslynMcp.Host.Stdio/Tools/MSBuildTools.cs`, `src/RoslynMcp.Host.Stdio/Tools/SuppressionTools.cs`, `src/RoslynMcp.Host.Stdio/Tools/SymbolRefactorTools.cs`. Test files added/modified: 1 new — `tests/RoslynMcp.Tests/MethodDescriptionDietEditingMsBuildTests.cs`. No catalog, README, DI, or Core/Roslyn-layer file is touched. Files deleted: none. |
| Tool policy | edit-only |
| Estimated context cost | 45000 |
| Risks | Rule 5 check answered explicitly for the do cell's "propose a 2+2 split if over cap": 45,000 is well under the 80,000 cap, so NO split is warranted — the cluster is 11 tools across 358 total production lines, not the 15 the slice assignment estimated. PARAMETER `[Description]` strings in these files are OUT OF SCOPE (already deduped by PRs #1349/#1350) and must stay byte-identical; `tests/RoslynMcp.Tests/ParameterDescriptionCanonicalizationTests.cs:42` pins the MSBuildTools `includedNames` "native JSON array" wording and will fail if the executor drifts into params. The aggregate ceiling constant must be set from a real post-edit measurement, not projected from the ceiling times tool count (PR #1345 spec-review failure). Fanout probe was shape B (per-target edit of 11 named tools): each target resolves to one of the four named defining files, and a text sweep of distinctive description phrases confirms no other file mirrors the method-description prose, so the count is 4 and matches Scope. Adjacent bad code observed, not fixed here: `tests/RoslynMcp.Tests/SurfaceCatalogTests.cs:317` sets `maxAggregateCharacters = 9_500` for three dieted types, which is exactly the `toolCount x ceiling` slop PR #1345's cold review rejected — it belongs to the already-filed ratchet-consolidation follow-on, not this slice. All cited anchors resolved. |
| Validation | After the edits run `mcp__roslyn__compile_check` on the Host.Stdio project. Then `dotnet test --filter "FullyQualifiedName~MethodDescriptionDietEditingMsBuild"` (new ratchet passes), `dotnet test --filter "FullyQualifiedName~SurfaceCatalogTests"` (assembly-wide 2,000-char guard plus `McpToolMetadata` drift still green — proves the catalog was not disturbed), and `dotnet test --filter "FullyQualifiedName~ParameterDescriptionCanonicalization"` (proves no parameter description drifted). Re-measure the slice aggregate and record the before/after pair (3,770 to measured) in the new test class's XML doc, matching the `MethodDescriptionDietScaffoldingMutationTests` precedent. CI gate is `./eng/verify-release.ps1 -Configuration Release` via PR validation on the self-hosted runner — do not duplicate that run locally. |
| Performance review | N/A — attribute-text-only change. Net effect is a smaller `tools/list` payload (roughly 2,000 fewer chars, about 490 tokens, for this cluster); no runtime code path changes. |
| CHANGELOG category | Maintenance |
| CHANGELOG entry (draft) | Fragment `changelog.d/method-description-diet-editing-msbuild.md` with `category: Maintenance`. Body: "**Maintenance:** Trimmed method-level tool descriptions for the direct-edit, MSBuild-evaluation, pragma-suppression, and composite symbol-refactor tool groups to ~200-char capability statements, moving operational detail to XML remarks. Cuts the tools/list payload for these 11 tools from 3,770 to \<measured\> characters with no capability statement, discriminating trigger, or refusal clause dropped. Advances `method-description-diet`; the row stays open for the remaining unswept `Tools/*.cs` files." |
| Backlog sync | Close rows: none — this is a partial slice. Mark obsolete: none. Update related: `method-description-diet` (append an amendment to `ai_docs/items/method-description-diet.md` recording the shipped cluster, the measured before/after aggregate, and the corrected 11-vs-15 tool count for future slice sizing). |

<details>
<summary>Executor handoff notes</summary>

**Executor handoff:**

- **Pattern coordinates (remarks shape):** mirror `src/RoslynMcp.Host.Stdio/Tools/MultiFileEditTools.cs:26-38` — the `/// <remarks>` block sits **above** the whole attribute list (`[McpServerTool…, McpToolMetadata…, Description…]`), carries **no `<summary>`**, and wraps identifiers in `<c>…</c>`. Do not convert it to a summary block or place it between attributes.
- **Test target:** create `tests/RoslynMcp.Tests/MethodDescriptionDietEditingMsBuildTests.cs` modeled 1:1 on `tests/RoslynMcp.Tests/MethodDescriptionDietScaffoldingMutationTests.cs` — constants at :19 and :27 (with the measured before/after pair in the XML doc, :21-26), the three `[TestMethod]`s at :37, :54, :70, and the `EnumerateSliceTools` reflection helper at :92-102.
- **Edge cases:** (1) `pragma_scope_widen` (`SuppressionTools.cs:84`) already stores HTML-escaped `&lt;id&gt;` in its `Description` — text moved into `<remarks>` still needs XML escaping, and retention assertions compare the **runtime literal** (`&lt;id&gt;`, not `<id>`); pick substrings like `#region/#endregion` that dodge escapes. (2) `SuppressionTools.cs` tools use expression-bodied `=> ToolDispatch…` members — the remarks comment still goes above the attributes.
- **Negative space:** do NOT touch parameter `[Description]`s (e.g. `EditTools.cs:23-29`) — `ParameterDescriptionCanonicalizationTests.cs:36-41` pins `edits`/`verify`/`autoRevertOnError`/`includedNames`. Do NOT edit the `McpToolMetadata` summary strings (e.g. `EditTools.cs:17`) — catalog drift assertion. Do NOT add these four types to `SurfaceCatalogTests.cs:311-317`.

</details>

### 13. param-description-dedupe-workspace-ops

| Field | Content |
|---|---|
| Diagnosis | All four owned files exist and still carry the un-deduped boilerplate: `src/RoslynMcp.Host.Stdio/Tools/WorkspaceWarmTools.cs:33`, `src/RoslynMcp.Host.Stdio/Tools/WorkspaceDriftTool.cs:33`, `src/RoslynMcp.Host.Stdio/Tools/UndoTools.cs:20` and `:70`, `src/RoslynMcp.Host.Stdio/Tools/EditorConfigTools.cs:26` and `:42` — 6 `workspaceId` param descriptions on the 59-char legacy string, plus 2 role-specific `filePath` descriptions at `EditorConfigTools.cs:27` and `:43`. **The load-bearing finding is that PRs #1349 and #1350 shipped TWO MUTUALLY INCOMPATIBLE "canonical" forms in the same sweep, so the slice assignment's phrase "the canonical one-liner established by PRs #1349/#1350" does not resolve to one string.** #1349 pinned the SHORT form `Workspace session id from workspace_load.` (`tests/RoslynMcp.Tests/ParameterDescriptionCanonicalizationTests.cs:28`, live at `src/RoslynMcp.Host.Stdio/Tools/EditTools.cs`, `FileOperationTools.cs`, `MSBuildTools.cs`, `TypeMoveTools.cs` — 9 sites). #1350 pinned the LONG form `The workspace session identifier returned by workspace_load` (`tests/RoslynMcp.Tests/ParamDescriptionCanonicalFormCodeActionSuppressionTests.cs:18-19`, live at `CodeActionTools.cs`, `SuppressionTools.cs`, `SymbolRefactorTools.cs`, `BulkRefactoringTools.cs`) — which is the pre-existing repo-wide boilerplate, so that guard entrenches a **0%** char cut against a row whose Acceptance bullet 2 demands ~40%. Counted live: 128 legacy long-form `[Description]` occurrences across 43 `Tools/*.cs` files vs 9 short-form across 4. Second finding: the two shipped guards are per-slice `Type[]` lists, so nothing prevents a third phrasing appearing in the 42 unswept files — exactly the drift the cold reviews flagged. Third finding (out of scope, flag only): `src/RoslynMcp.Core/Services/ISymbolRefactorService.cs` carries a copy of the same Host.Stdio param boilerplate in the Core layer. |
| Approach | (1) **Adopt #1349's short form as THE canon** — it is the only one of the two that satisfies the row's char-cut Acceptance; #1350's form is the status quo it was supposed to replace. Rewrite the 6 `workspaceId` descriptions in the four owned files to exactly `Workspace session id from workspace_load.`. (2) Normalize the two `EditorConfigTools` `filePath` descriptions to #1349's `^Absolute path to the .+\.$` shape while KEEPING their disambiguators: `Absolute path to the source file to inspect.` and `Absolute path to the C# source file whose .editorconfig should be updated.` (the second mirrors the deliberate `set_diagnostic_severity`/`filePath` load-bearing exception at `ParamDescriptionCanonicalFormCodeActionSuppressionTests.cs:27-31`). (3) Leave every semantically load-bearing description untouched: `WorkspaceWarmTools.projects` (native-JSON-array format contract, case-insensitivity, omit semantics), `UndoTools.sequenceNumber` (`workspace_changes` discriminator), `EditorConfigTools.key`/`value` (worked examples). (4) **Fold in the carried-forward advisory** — add ONE new test `tests/RoslynMcp.Tests/ToolParameterCanonicalFormTests.cs` that replaces the per-slice type-list pattern with an assembly-wide reflection walk over every `[McpServerTool]`-attributed method in Host.Stdio (mirroring the walk in `src/RoslynMcp.Host.Stdio/Catalog/ToolParameterIndex.cs:53-82`; `InternalsVisibleTo RoslynMcp.Tests` is already declared at `src/RoslynMcp.Host.Stdio/RoslynMcp.Host.Stdio.csproj:47`). Three assertions: (a) every `workspaceId` description equals EXACTLY one of the two known strings — a third phrasing fails anywhere in the assembly, closing the drift hole; (b) the count of legacy-long-form sites is `<=` a `LegacyWorkspaceIdCeiling` constant the executor pins to the post-edit reflection-derived count — a monotone ratchet each later slice lowers; (c) the eight already-canonical types (#1349's four + this slice's four) carry ONLY the short form, asserted as an exact string per the advisory, not a shape. Use `<=` not `==` deliberately so sibling param-cluster initiatives landing in either order cannot red this test. (5) Do NOT modify `ToolParameterIndex.cs` to expose an all-tools enumerator — that would be a 5th production file over the Rule 3 cap; the test does its own equivalent walk and the promotion is deferred. Leave both existing per-slice test classes untouched and green. |
| Scope | Production files: 4 — `src/RoslynMcp.Host.Stdio/Tools/WorkspaceWarmTools.cs`, `src/RoslynMcp.Host.Stdio/Tools/WorkspaceDriftTool.cs`, `src/RoslynMcp.Host.Stdio/Tools/UndoTools.cs`, `src/RoslynMcp.Host.Stdio/Tools/EditorConfigTools.cs`. Exactly at the Rule 3 cap of 4; NO exemption claimed and none needed. Test files added/modified: 1 — new `tests/RoslynMcp.Tests/ToolParameterCanonicalFormTests.cs`. Files deleted: none. No catalog or DI touch: the catalog snapshot `src/RoslynMcp.Host.Stdio/Catalog/Snapshots/catalog-v2.3.1.json` was probed and carries ZERO parameter descriptions (only method-level `McpToolMetadata` summaries), so no hotspot `ServerSurfaceCatalog*.cs` regeneration is forced. |
| Tool policy | edit-only |
| Estimated context cost | 45000 |
| Risks | **Canon-fork risk (primary).** This slice picks #1349's short form over #1350's long form. The long form is currently the numerical majority (128 sites / 43 files vs 9 / 4), so after this slice the canonical island is 8 files against ~40 legacy files. The sibling param clusters in this same sweep (`param-description-dedupe-refactoring-core` order 14, `param-description-dedupe-validation-signature` order 15) are deepened independently and may pick the other form — three-way drift is the failure mode. Mitigation is assertion (a): a third phrasing is impossible, and whichever of the two a sibling picks, the `<=` ceiling stays satisfiable in either landing order. If a sibling picks the long form the guard still passes but the ceiling does not drop; that is visible, not silent. **Cross-initiative CI friction.** Assertion (a) is assembly-wide, so it can red a sibling's branch if that sibling invents a new phrasing; the failure message must name both accepted strings and point at row `param-description-dedupe`. **Char-savings honesty.** The mechanical saving here is ~120 chars (6 sites x 20) — trivially small against the row's 58,679 -> ~35k target. The real deliverable of this slice is the canon decision plus the generalized ratchet; do not let a reviewer read the small delta as under-delivery, and do not pad the trim by cutting load-bearing text to hit a number (PR #1348 lesson). **Deferred generalization.** Promoting the assembly walk into `ToolParameterIndex` as a first-class enumerator is the cleaner home but is a 5th production file; deferred, not forgotten. **Fanout probe (shape B, per-target edit).** Resolved each named target to its defining file: 4 distinct files, and probed for forced companions — no test pins these six param strings (the two existing canonical-form guards are scoped to 8 other types), the catalog snapshot has no param descriptions, and no `*Tests.cs` references the four owned tool types for description content. Probe result 4 == `productionFilesTouched` 4. The `size: L` on the input is the PARENT row's index-level flag; Phase A's per-cluster split already resolved it, so `fanoutOversize` is false on this bounded child. **Anchors:** all cited paths and constructs verified live; none stale. |
| Validation | `mcp__roslyn__compile_check` on the four edited files after each edit. Then `mcp__roslyn__test_run --filter "ToolParameterCanonicalFormTests"` (new guard), plus `--filter "ParameterDescriptionCanonicalizationTests"` and `--filter "ParamDescriptionCanonicalFormCodeActionSuppressionTests"` to prove neither shipped guard regresses. Also run `--filter "SurfaceCatalogTests"` and `--filter "StructuredContentWireContractTests"` since both reference the owned tool types. Verify assertion (b) by deliberately reverting one `workspaceId` edit and confirming the ceiling assertion fails, then restoring. Full gate: `./eng/verify-release.ps1 -Configuration Release`; docs gate `./eng/verify-ai-docs.ps1`. Manual spot-check: confirm the 6 rewritten strings are byte-identical to `Workspace session id from workspace_load.` including the terminal period. |
| Performance review | N/A — description-text and test-only change; no hot-path code. The new guard's reflection walk runs once per test class, matching the existing cached pattern in `ToolParameterIndex`. |
| CHANGELOG category | Maintenance |
| CHANGELOG entry (draft) | Standardized `workspaceId` and `filePath` parameter descriptions on the workspace-warm, workspace-drift, undo, and editorconfig tool surfaces to the canonical short form, and replaced the per-slice canonicalization guards with an assembly-wide ratchet that rejects any third phrasing and monotonically lowers the legacy-form ceiling. Load-bearing parameter guidance (JSON-array format contracts, `workspace_changes` sequence semantics, editorconfig key/value examples) is unchanged. Fragment: `changelog.d/param-description-dedupe-workspace-ops.md`. |
| Backlog sync | Close rows: []. This is a partial per-cluster slice — `param-description-dedupe` stays OPEN (~42 `Tools/*.cs` still unswept). Update related: amend `ai_docs/items/param-description-dedupe.md` with (1) the PR #1349-vs-#1350 canon fork and the decision to adopt the short form as THE canon, (2) the generalized assembly-wide ratchet replacing per-slice type lists, so later slices extend the ceiling constant instead of adding another test class. Mark obsolete: []. New rows for the executor's Step 9: (a) re-canonicalize the four PR #1350 tool types (`CodeActionTools`, `SuppressionTools`, `SymbolRefactorTools`, `BulkRefactoringTools`) plus `ParamDescriptionCanonicalFormCodeActionSuppressionTests` onto the short form and retire the duplicate guard; (b) remove the Host.Stdio param-description copy from `src/RoslynMcp.Core/Services/ISymbolRefactorService.cs` (Core layer holding a tool-surface description string). |

<details>
<summary>Executor handoff notes</summary>

**Executor handoff:**

- **A THIRD phrasing already exists — assertion (a) reds on day one unless handled.** `src/RoslynMcp.Host.Stdio/Tools/ValidationBundleTools.cs:76` carries `"The source workspace session identifier returned by workspace_load"` (the *source* workspace of a two-workspace tool — a real disambiguator, not drift). Add it to the new guard as a named load-bearing exception keyed by `(toolName, paramName)`, mirroring `ParamDescriptionCanonicalFormCodeActionSuppressionTests.cs:27-31`. Do NOT rewrite that string, and do NOT loosen the assertion to a regex.
- **Pattern coordinates:** assembly walk — `ToolParameterIndex.cs:56-67` (`typeof(Tools.AnalysisTools).Assembly`, `GetTypes()`, `McpServerToolAttribute` filter with `Public|NonPublic|Static|Instance`). Per-parameter yield shape and violation-accumulation style — `ParameterDescriptionCanonicalizationTests.cs:45-69` and `:74-103`.
- **Exact strings:** `EditorConfigTools.cs:27` (`...source file to inspect` → add terminal period) and `:43` currently read "Absolute path to **a** C# source file…" — the canonical regex `^Absolute path to the .+\.$` requires **the** plus a period. Both `workspaceId` sites are `EditorConfigTools.cs:26` and `:42`.
- **Ceiling constant:** derive `LegacyWorkspaceIdCeiling` by running the new test once and reading the reflection-observed count — do not hand-count from grep (multi-line declarations and non-tool helpers skew text counts).
- **Negative space:** do NOT touch `MultiFileEditTools.cs` (2 legacy sites, sibling of the #1349 slice), the 42 other `Tools/*.cs`, or `RoslynMcp.Core/Services/ISymbolRefactorService.cs` — those are follow-up rows.

</details>

### 14. param-description-dedupe-refactoring-core

| Field | Content |
|---|---|
| Diagnosis | Anchors verified — all four cluster files exist and carry 19 `[McpServerTool]` methods (RefactoringTools 12, RestructureTools 3, ScriptingTools 2, OperationTools 2). Boilerplate census (grep, exact strings): `src/RoslynMcp.Host.Stdio/Tools/RefactoringTools.cs` carries 6 long-form `workspaceId` (lines 27, 68, 103, 138, 178, 219), 5 `"The preview token returned by <tool>_preview"` (49, 84, 119, 158, 199) and 4 required `"Absolute path to the source file"` (69, 104, 140, 179); `src/RoslynMcp.Host.Stdio/Tools/RestructureTools.cs` carries 2 `workspaceId`; `src/RoslynMcp.Host.Stdio/Tools/OperationTools.cs` carries 1 `workspaceId` + 1 required `filePath`; `src/RoslynMcp.Host.Stdio/Tools/ScriptingTools.cs` carries ZERO boilerplate — its three params (`code`, `imports`, `timeoutSeconds`) are all load-bearing, so it needs no production edit. ROOT CAUSE beyond the row text: the two shipped slices minted TWO mutually exclusive "canonical" forms. `tests/RoslynMcp.Tests/ParameterDescriptionCanonicalizationTests.cs:28` pins `Workspace session id from workspace_load.` (40 chars) for the PR #1349 types, while `tests/RoslynMcp.Tests/ParamDescriptionCanonicalFormCodeActionSuppressionTests.cs:19` pins `The workspace session identifier returned by workspace_load` (59 chars — the untouched PRE-sweep form) for the PR #1350 types. That divergence, not tooling, is why every slice has been forced into a fresh per-slice type list and why an assembly-wide guard cannot go green today. Secondary defect found in-scope: `RefactoringTools.cs:34` leaks the internal tracker id `Item #8 — ` into the public wire schema for `rename_preview`'s `summary` param. |
| Approach | Adopt the PR #1349 TERSE form as THE canonical form — it is the one that actually delivers the row's ~40% char-cut acceptance; PR #1350 only pinned the pre-sweep long form. (1) `src/RoslynMcp.Host.Stdio/Tools/RefactoringTools.cs`: 6 `workspaceId` to `Workspace session id from workspace_load.`; 5 `previewToken` to `Preview token from <tool>_preview.` matching the `^Preview token from [a-z0-9_]+_preview\.$` ratchet; 4 required `filePath` to `Absolute path to the source file.`; strip the `Item #8 — ` prefix from `summary` while keeping its >150-refs / output-cap semantics. Leave untouched: `PreviewRename`'s OPTIONAL locator params (`filePath`/`line`/`column`/`symbolHandle`/`metadataName` — the "Optional: ... containing the symbol" defaulting form is a discriminator per the existing guard's line 88-90 carve-out), `newName`, `diagnosticId`, `fixId`, `projectName`, and the four format-range coordinates. (2) `src/RoslynMcp.Host.Stdio/Tools/RestructureTools.cs`: 2 `workspaceId` only — `pattern`/`goal`/`replacements` (placeholder contract, "same syntactic kind", "redeemable via preview_multi_file_edit_apply") and the two scope-filter `filePath`/`projectName` params stay verbatim. (3) `src/RoslynMcp.Host.Stdio/Tools/OperationTools.cs`: `workspaceId` + required `filePath`; keep `column`'s UX-003 token-targeting text (load-bearing discriminator). (4) `ScriptingTools.cs`: no production edit. (5) Tests — answer the cold-review advisory by CONSOLIDATING onto the existing shared ratchet rather than minting a third per-slice class: extend `SweptToolTypes` at `tests/RoslynMcp.Tests/ParameterDescriptionCanonicalizationTests.cs:20-26` with the four cluster types, and add exact-string `LoadBearingContracts` rows — (RefactoringTools, `summary`, ">150 refs"), (RestructureTools, `pattern`, "__"), (RestructureTools, `goal`, "same syntactic kind"), (OperationTools, `column`, "UX-003"), (ScriptingTools, `timeoutSeconds`, "ROSLYNMCP_SCRIPT_TIMEOUT_SECONDS"). NEGATIVE SPACE HONORED: do not touch the 5 `expectedKind:`/`applyRoute:` bindings at `RefactoringTools.cs:59-60, 94-95, 129-130, 168-169, 209-210` — those belong to initiative `preview-token-route-map-centralization`. |
| Scope | Production files: 3 — `src/RoslynMcp.Host.Stdio/Tools/RefactoringTools.cs`, `src/RoslynMcp.Host.Stdio/Tools/RestructureTools.cs`, `src/RoslynMcp.Host.Stdio/Tools/OperationTools.cs`. `src/RoslynMcp.Host.Stdio/Tools/ScriptingTools.cs` is in the assigned cluster but needs NO production edit (all three of its param descriptions are load-bearing); it enters the ratchet type list only, which lives in the test file. Test files added/modified: 1 — `tests/RoslynMcp.Tests/ParameterDescriptionCanonicalizationTests.cs` (extended, not created). Files deleted: none. Rule 3: 3 of 4, no exemption claimed. Rule 4: 1 of 3. |
| Tool policy | edit-only |
| Estimated context cost | 42000 |
| Risks | File-overlap conflict: initiative `preview-token-route-map-centralization` (order 1) may remove the 5 `applyRoute:` string literals from `RefactoringTools.cs` — same file, disjoint lines, real conflict-graph edge; schedule in different waves. Shared-test-file edge: siblings `param-description-dedupe-workspace-ops` (order 13) and `param-description-dedupe-validation-signature` (order 15) carry the same "generalize the guard" advisory; if they also extend `ParameterDescriptionCanonicalizationTests.cs` the `SweptToolTypes` array literal collides (trivial merge, but the conflict graph will NOT model it because test files are excluded from `productionFiles`). The canonical-form divergence SURVIVES this slice — the PR #1350 four types stay on the 59-char form and their duplicate ratchet stays live; resolving it means editing files owned by sibling initiatives, so it is deliberately deferred to a new row rather than smuggled in. Assembly-wide generalization via `ToolParameterIndex` is mechanically feasible (`InternalsVisibleTo Include="RoslynMcp.Tests"` at `src/RoslynMcp.Host.Stdio/RoslynMcp.Host.Stdio.csproj:47`) but is blocked by that divergence plus ~42 still-unswept `Tools/*.cs`; not attempted here per Rule 6. Fanout probe (shape B — per-target edit, count = DEFINING files) returned 3 production files, matching `productionFilesTouched`. Fan-IN probe for exact-string pins: only `ParamDescriptionCanonicalFormCodeActionSuppressionTests.cs` pins the old string and it is scoped to four OTHER types; `Item #8` appears elsewhere only as a doc-comment in `tests/RoslynMcp.Tests/RenameSummaryModeTests.cs:8`, never asserted; `RefactoringCoreDescriptionBudgetTests.cs` ratchets METHOD-level text on six unrelated tools and is unaffected. |
| Validation | `mcp__roslyn__compile_check` scoped to `RoslynMcp.Host.Stdio` after each file edit. Then `mcp__roslyn__test_run --filter "ParameterDescriptionCanonicalizationTests"` (must go green with the four new types), plus regression sweep over `ParamDescriptionCanonicalFormCodeActionSuppressionTests`, `RefactoringCoreDescriptionBudgetTests`, `RenameSummaryModeTests`, `ServerDiscoveryWireTests`, `SurfaceCatalogTests`. CI equivalent: `./eng/verify-release.ps1 -Configuration Release`; doc gate `./eng/verify-ai-docs.ps1`. Manual wire check: a `tools/list` probe confirming `rename_preview`, `organize_usings_apply`, `restructure_preview` and `get_operations` schemas carry the canonical one-liners AND still expose the `summary` >150-refs guidance, the `__placeholder__` capture contract, and the UX-003 column-targeting note. |
| Performance review | N/A — schema-text-only change, no hot-path code touched. |
| CHANGELOG category | Maintenance |
| CHANGELOG entry (draft) | Standardized `workspaceId`, `previewToken` and required `filePath` parameter descriptions to the canonical one-liner across the refactoring-core tool cluster (`rename`/`organize_usings`/`format_document`/`format_range`/`code_fix`/`format_check`, `restructure_preview`, `replace_string_literals_preview`, `get_operations`), and removed an internal tracker id that was leaking into the `rename_preview` wire schema. Load-bearing parameter guidance (rename summary mode, restructure placeholder contract, UX-003 column targeting, script timeout budget) is unchanged and is now locked by the shared canonicalization ratchet, which this slice extends instead of adding another per-slice guard. |
| Backlog sync | Close rows: []. Mark obsolete: []. Update related: `param-description-dedupe` (record this cluster as swept — RefactoringTools, RestructureTools, ScriptingTools, OperationTools; ~42 `Tools/*.cs` remain). New row to file at execute-time Step 9: reconcile the two contradictory canonical parameter-description forms — `ParamDescriptionCanonicalFormCodeActionSuppressionTests.cs:19` pins the 59-char form while `ParameterDescriptionCanonicalizationTests.cs:28` pins the 40-char form — converge the PR #1350 types onto the terse form and retire the duplicate ratchet in favor of one `ToolParameterIndex`-backed guard with a shrinking residual-unswept allowlist. |

<details>
<summary>Executor handoff notes</summary>

**Executor handoff:**

- **Canonical forms (verified live):** `ParameterDescriptionCanonicalizationTests.cs:28,30-34` — `workspaceId` must equal `Workspace session id from workspace_load.`; `previewToken` must match `^Preview token from [a-z0-9_]+_preview\.$`; required paths must match `^Absolute path to the .+\.$`. **The trailing period is load-bearing** — today's `"Absolute path to the source file"` (RefactoringTools.cs:69,104,140,179; OperationTools.cs:22) has none and will red the ratchet the instant the types are added.
- **Optional-path carve-out:** the guard keys on `!parameter.IsOptional` (line 92), so `PreviewRename.filePath` (RefactoringTools.cs:29) and both `RestructureTools` scope filters (lines 29-30, 49-50) are already exempt — leave them byte-for-byte.
- **Test target:** extend the array at `ParameterDescriptionCanonicalizationTests.cs:20-26` and the tuple list at `:37-43`; mirror row shape `(nameof(EditTools), "verify", "compile_check")` at line 40. Do NOT create a third guard class. Also refresh the now-stale `<remarks>` at lines 12-16 ("four tool types swept by `param-description-dedupe-edit-file-ops`").
- **`summary` edit:** strip only the `Item #8 — ` prefix at RefactoringTools.cs:34; the substring `(>150 refs)` must survive verbatim.
- **Negative space:** do NOT touch RefactoringTools.cs:59-60, 94-95, 129-130, 168-169, 209-210 (`expectedKind:`/`applyRoute:` — sibling initiative); do NOT touch the method-level `Description` at line 21; do NOT edit `ParamDescriptionCanonicalFormCodeActionSuppressionTests.cs` (scoped to four disjoint types); do NOT sweep the internal `Item #8` comments in `RefactoringService.cs:74,184` / `IRefactoringService.cs:21` — those are not wire schema.

</details>

### 15. param-description-dedupe-validation-signature

| Field | Content |
|---|---|
| Diagnosis | All four cluster files exist and still carry the un-deduped boilerplate. Measured baseline (script over `[Description("…")]` parameter attributes): 55 parameter descriptions, 5,352 chars, avg 97 — above the repo-wide 81-char average from the source audit. Boilerplate repeats: `workspaceId` ×8 (`ValidationBundleTools.cs:29`, `:55`, `:76`, `CrossProjectRefactoringTools.cs:25`, `:52`, `:77`, `ChangeSignatureTools.cs:26`, `ParameterObjectTools.cs:26`), path-family ×3 required (`CrossProjectRefactoringTools.cs:26`, `:53`, `:78`) plus 2 optional (`ChangeSignatureTools.cs:28`, `ParameterObjectTools.cs:29`), `line`/`column`/`symbolHandle` ×3 each across the two locator tools, and a verbatim-duplicated `summary` essay at `ValidationBundleTools.cs:32` and `:57` whose only divergence is a trailing version-archaeology clause ("preserves the v1.18 shape" vs "preserves the full response shape"). **Load-bearing blocker found — two mutually-exclusive "canonical" forms shipped in the same sweep:** PR #1349's ratchet `tests/RoslynMcp.Tests/ParameterDescriptionCanonicalizationTests.cs:28` pins `workspaceId` to `"Workspace session id from workspace_load."` (40 chars) and paths to regex `^Absolute path to the .+\.$` (trailing period required), while PR #1350's `tests/RoslynMcp.Tests/ParamDescriptionCanonicalFormCodeActionSuppressionTests.cs:18-21` pins the OLD long form `"The workspace session identifier returned by workspace_load"` (58 chars) and `"Absolute path to the source file"` (no period). Only 4 files repo-wide use the short form; 44 still use the long one. A slice cannot honor both, so the advisory's "generalize the guard via `ToolParameterIndex`" is structurally blocked until one form wins. |
| Approach | (1) **Adopt PR #1349's short form as canonical for this cluster** — it is the only one of the two that achieves the row's char-reduction acceptance criterion (18 chars saved per site). Rewrite the 7 plain `workspaceId` descriptions to `"Workspace session id from workspace_load."`; keep the fork tool's discriminator as `"Source workspace session id from workspace_load."` (`ValidationBundleTools.cs:76` — two workspaces are in play, so "source" is call-accuracy, not boilerplate). (2) Canonicalize the 3 required path params in `src/RoslynMcp.Host.Stdio/Tools/CrossProjectRefactoringTools.cs` to the `^Absolute path to the <role>.$` shape (add the trailing period `#1349`'s regex requires); leave the two OPTIONAL `filePath` params in `ChangeSignatureTools.cs`/`ParameterObjectTools.cs` on their `"Optional: …"` form — `#1349`'s guard deliberately exempts optional path params because the defaulting rule is a discriminator. (3) Rewrite `ValidationBundleTools.cs:77` `previewToken` to `"Preview token from any *_preview tool."` and widen `#1349`'s `CanonicalPreviewToken` regex with an alternation for the any-tool form (the fork tool accepts a token from ANY preview tool, so the single-tool `<tool>_preview` shape is wrong here). (4) Drop only the version-archaeology tails on the two `summary` params (`:32`, `:57`) — condense to `"Default: false."`; keep the OverallStatus/counts sentence. (5) **Test surface — do NOT create a third per-slice ratchet class.** Extend `tests/RoslynMcp.Tests/ParameterDescriptionCanonicalizationTests.cs` exactly as its own remarks invite ("later slices widen `SweptToolTypes`"): add the four types to `SweptToolTypes:20-26`, add a `LoadBearingWorkspaceIdExceptions` set mirroring `#1350`'s `LoadBearingFilePathExceptions:27-31` for the fork tool's "source" variant, and add `LoadBearingContracts` entries pinning `ValidationBundleTools.changedFilePaths` → `"native JSON array"`, `ParameterObjectTools.dtoFolders` → `"native JSON array"`, `ChangeSignatureTools.metadataName` → `"NOT accepted"`, `ChangeSignatureTools.newOrder` → `"exactly once"`, `ValidationBundleTools.retention` → `"drop-on-success"`. Partial `ToolParameterIndex` generalization is possible (`src/RoslynMcp.Host.Stdio/Catalog/ToolParameterIndex.cs:119-123` exposes `Description`, and `WorkspaceIdOptionalSurfaceTests.cs` proves test access) but it is keyed by TOOL NAME, so it swaps a type list for a tool-name list rather than eliminating the list — take it only if free; full repo-wide generalization is blocked per Diagnosis and belongs to the follow-up row. |
| Scope | Production files: 4 of 4 under plain Rule 3 (no exemption claimed; the addenda's tool-surface-only allowance is narrower at 2 files and is not invoked) — `src/RoslynMcp.Host.Stdio/Tools/ValidationBundleTools.cs`, `src/RoslynMcp.Host.Stdio/Tools/CrossProjectRefactoringTools.cs`, `src/RoslynMcp.Host.Stdio/Tools/ChangeSignatureTools.cs`, `src/RoslynMcp.Host.Stdio/Tools/ParameterObjectTools.cs`. Test files modified: 1 (`tests/RoslynMcp.Tests/ParameterDescriptionCanonicalizationTests.cs`, extended — Rule 4: 1 of 3). No files deleted. Changelog fragment `changelog.d/param-description-dedupe-validation-signature.md` per the addenda fragment convention (not counted in the file budget). |
| Tool policy | edit-only |
| Estimated context cost | 45000 |
| Risks | **Fan-in pin (verified):** `tests/RoslynMcp.Tests/SurfaceCatalogTests.cs:558-568` asserts `validate_workspace.changedFilePaths` and `parameter_object_preview.dtoFolders` descriptions contain the literal `"native JSON array"` — both params are in this cluster; trimming them reds CI. The two method-level ratchets that also cover these types (`ToolDescriptionDietWorkspaceValidationTests.cs`, `RefactoringCoreDescriptionBudgetTests.cs`) are explicitly parameter-excluded, so they cannot break — verified by reading both. **Cross-slice merge edge (unmodeled by the conflict graph):** sibling initiatives `param-description-dedupe-workspace-ops` (order 13) and `param-description-dedupe-refactoring-core` (order 14) may also widen `ParameterDescriptionCanonicalizationTests.cs`; the conflict graph only tracks production files, so this test file is a real rebase hazard. The edit is a 4-entry array addition plus tuples — mechanically trivial to rebase, but the executor should expect it. **Do not over-trim (PR #1348 hard lesson):** this cluster is discriminator-dense — `op`, `newOrder`, `parameterType`, `defaultValue`, `position`, `retention`, `dtoProjectName`, and `metadataName` all carry refusal/format contracts that must survive verbatim. Realizable cut here is ~10-15% of the cluster's 5,352 chars, NOT the parent row's 40% aggregate figure; the executor must budget from real per-param content, never from a target percentage. **Row-text drift (non-blocking):** the slice assignment says "12 `[McpServerTool]` methods"; the real count across the four files is 8 tools (55 described params) — the 12 counts the `[McpServerToolType]` class attributes. No anchor is stale; all four files and both cited ratchet tests were read this pass. **Bad code observed, out of scope (Directive #3):** (a) the two-canonical-form divergence above needs a repo-wide unification row before the `ToolParameterIndex` guard can generalize; (b) `CrossProjectRefactoringTools.cs:31-32` places `CancellationToken ct = default` BEFORE the described `preserveNamespace` parameter — CancellationToken-not-last, a CA1068-shaped smell that also puts an optional described param after the token in the emitted schema order. |
| Validation | `mcp__roslyn__compile_check` on the four edited files after each edit. Then `mcp__roslyn__test_run --filter "ParameterDescriptionCanonicalizationTests"` (must pass with the widened `SweptToolTypes`), `--filter "SurfaceCatalogTests"` (guards the `native JSON array` pins), `--filter "ParamDescriptionCanonicalFormCodeActionSuppressionTests"` (must stay green — proves this slice did not disturb the #1350 four types), and `--filter "ToolDescriptionDietWorkspaceValidationTests|RefactoringCoreDescriptionBudgetTests"` (proves the method-level budgets are untouched). Re-run the char-count baseline over the four files and record the before/after totals in the PR body (before: 55 params / 5,352 chars / avg 97). Full gate: `./eng/verify-release.ps1 -Configuration Release`; docs gate `./eng/verify-ai-docs.ps1`. |
| Performance review | N/A — parameter description text only; no hot-path or runtime behavior change. Net effect is a small reduction in the `tools/list` payload. |
| CHANGELOG category | Maintenance |
| CHANGELOG entry (draft) | **Parameter-description dedupe — validation/signature cluster (`param-description-dedupe-validation-signature`):** standardized the `workspaceId`, required-path, and `previewToken` boilerplate across `validate_workspace`, `validate_recent_git_changes`, `workspace_fork_apply`, `move_type_to_project_preview`, `extract_interface_cross_project_preview`, `dependency_inversion_preview`, `change_signature_preview`, and `parameter_object_preview` onto the canonical one-liners, and dropped duplicated version-archaeology text from the two `summary` parameters. Discriminating guidance (`op`/`newOrder`/`retention`/`metadataName` contracts, the `native JSON array` encoding hints, and the fork tool's "source workspace" qualifier) is unchanged. `ParameterDescriptionCanonicalizationTests` now ratchets these four tool types so the phrasing cannot drift back. |
| Backlog sync | Close rows: none — partial cluster slice; `param-description-dedupe` stays OPEN (~42 `Tools/*.cs` remain). Update related: `param-description-dedupe` (append an amendment naming this cluster, the measured before/after char totals, and the canonical-form decision). New rows to file: (1) unify the two conflicting canonical parameter-description forms repo-wide (`ParameterDescriptionCanonicalizationTests` short form vs `ParamDescriptionCanonicalFormCodeActionSuppressionTests` long form) and collapse the per-slice ratchets into one `ToolParameterIndex`-driven guard once every cluster is swept; (2) move `CancellationToken ct` to the trailing position in `CrossProjectRefactoringTools.PreviewMoveTypeToProject`. |

<details>
<summary>Executor handoff notes</summary>

**Executor handoff:**

- **Pattern coordinates:** the canonical constants + regexes live at `tests/RoslynMcp.Tests/ParameterDescriptionCanonicalizationTests.cs:28-34`; the enforcement `switch` is `:80-95`. The exception-set shape to copy is `ParamDescriptionCanonicalFormCodeActionSuppressionTests.cs:27-31`.
- **Test target:** extend `ParameterDescriptionCanonicalizationTests.cs` — add the four types to `SweptToolTypes:20-26`; append tuples to `LoadBearingContracts:37-43` (mirror `(nameof(MSBuildTools), "includedNames", "native JSON array")` at `:42`). Do NOT create a new test class.
- **Guard-edit traps (both red CI if missed):** (1) the `workspaceId` case at `:82` is an exact-string compare with no exception hook — adding a `LoadBearingWorkspaceIdExceptions` set is not enough, you must also wire it into that case, and key it by *type/parameter* (this test enumerates `Type`, unlike #1350's tool-name key). (2) Widen `CanonicalPreviewToken:30-31` **before** rewriting `ValidationBundleTools.cs:77`.
- **Required vs optional paths:** `CrossProjectRefactoringTools.cs:26` (`sourceFilePath`), `:53`, `:78` (`filePath`) have no default → guard applies → add the trailing period. `ChangeSignatureTools.cs:28` and `ParameterObjectTools.cs:29` are `= null` → `IsOptional` exempts them at `:92`; leave verbatim.
- **Negative space:** do NOT touch `CodeActionTools`/`SuppressionTools`/`SymbolRefactorTools`/`BulkRefactoringTools` (pinned to the long form at `ParamDescriptionCanonicalFormCodeActionSuppressionTests.cs:33-39`). Do NOT trim `ValidationBundleTools.cs:30` or `ParameterObjectTools.cs:36` — `SurfaceCatalogTests.cs:563,568` pins `native JSON array`. Do NOT move `CancellationToken ct` at `CrossProjectRefactoringTools.cs:30` — separate row.

</details>

### 2.01. preview-token-route-binding-editing-substrate

| Field | Content |
|---|---|
| Diagnosis | Split from `preview-token-route-binding-editing-family` per Rules 3, 5 and 5b — part 1 of 3. The parent slice honestly counted 11 production files and 85K, over the Rule 3 cap of 4, over the 7-file gate-forced-companion ceiling, and over the Rule 5 cap; no exemption was claimable and its own Scope said so. This piece carries the shared substrate the other two parts compile against. Verified on `main` 2026-08-25. FACT 1: `src/RoslynMcp.Core/Models/PreviewKind.cs:21-43` declares exactly FIVE concrete members and all five are already consumed by `RefactoringTools.cs:59/94/129/168/209`, so binding any of the six unbound apply routes requires new concrete enum members first. FACT 2: a new concrete member is only safe once it is registered in the centralized kind to (previewTool, applyRoute) map delivered by initiative `preview-token-route-map-centralization` — today the two parallel switches at `src/RoslynMcp.Host.Stdio/Tools/ToolDispatch.cs:207-215` and `:221-229`, each ending in a silent catch-all that would tell a caller to redeem via `apply_with_verify`. FACT 3: three of the four producers that must be tagged call the 4-arg `IPreviewStore.Store(workspaceId, modifiedSolution, workspaceVersion, description)` shape — `EditService.cs:302`, `CodeActionService.cs:152`, `FixAllService.cs:207` — and `src/RoslynMcp.Roslyn/Contracts/IPreviewStore.cs` declares a kind-carrying overload ONLY for the `changes`-shaped call at `:57-64`; the concrete `PreviewStore.cs:94` already implements `Store(workspaceId, modifiedSolution, workspaceVersion, description, diffTruncated, kind)` but the interface never declares it, so tagging those three producers is impossible until it does. |
| Approach | Three files, one pass. (1) `src/RoslynMcp.Core/Models/PreviewKind.cs` — append six concrete members `MultiFileEdit`, `FileCreate`, `FileDelete`, `FileMove`, `CodeAction`, `FixAll`, each with an XML doc line mirroring the existing five (`:29-42`); append only, never renumber, since `Unspecified = 0` is the load-bearing permissive default. (2) `src/RoslynMcp.Host.Stdio/Tools/ToolDispatch.cs` — register all six in order 1's centralized map with the verified tool names: `MultiFileEdit` to (`preview_multi_file_edit`, `preview_multi_file_edit_apply`), `FileCreate` to (`create_file_preview`, `create_file_apply`), `FileDelete` to (`delete_file_preview`, `delete_file_apply`), `FileMove` to (`move_file_preview`, `move_file_apply`), `CodeAction` to (`preview_code_action`, `apply_code_action`), `FixAll` to (`fix_all_preview`, `fix_all_apply`). All twelve names were read off the `[McpServerTool(Name = ...)]` attributes on `main`. (3) `src/RoslynMcp.Roslyn/Contracts/IPreviewStore.cs` — declare `string Store(string workspaceId, Solution modifiedSolution, int workspaceVersion, string description, bool diffTruncated, PreviewKind kind)` with a kind-discarding default body delegating to the existing untagged `:31` overload, mirroring the default-body pattern already used at `:57-64` and `:141`/`:157` so out-of-tree implementers and test fakes keep compiling. No producer is tagged and no route is bound here — those are parts 2 and 3. |
| Scope | Production files: 3 — `src/RoslynMcp.Core/Models/PreviewKind.cs`, `src/RoslynMcp.Host.Stdio/Tools/ToolDispatch.cs`, `src/RoslynMcp.Roslyn/Contracts/IPreviewStore.cs`. Test files: 1 — extend `tests/RoslynMcp.Tests/ToolDispatchTests.cs`. Within the Rule 3 cap of 4; no exemption claimed or needed. NEGATIVE-SPACE OVERRIDE: the parent row's Phase-A slice text forbids editing `ToolDispatch.cs`; that assignment is structurally unsatisfiable (see Diagnosis FACT 2) and this piece deliberately owns that file, serialized behind order 1 via `dependsOn`. |
| Tool policy | edit-only |
| Estimated context cost | 32000 |
| Risks | ORDER-1 COUPLING: this piece shares `ToolDispatch.cs` with initiative `preview-token-route-map-centralization` and compile-depends on the map shape order 1 introduces, so it carries `dependsOn: [preview-token-route-map-centralization]` and must be cut from a base that already has order 1 merged; running them in one generation guarantees a rebase conflict on the map body. GATE INHERITED FROM ORDER 1: order 1's fail-loud arm plus its "every non-`Unspecified` member is mapped" pin test turn a forgotten map entry into a red test rather than a wrong error message — that is the intended safety net for step 1. SUBSTRATE-ONLY SHIP: this PR adds enum members, map entries and an interface declaration with no producer tagging them and no route consuming them, so runtime behavior is unchanged — intentional, and it is what makes parts 2 and 3 parallelizable against each other. FANOUT PROBE (shape: new enum member plus new interface member): 3 production files — recorded as `fanoutEstimate: 3`, matching `productionFilesTouched: 3` (Rule 5b "within ±1" branch, so no `fanoutOversize` and no `heroic-last`). Probe method — 7 `src/` files reference the `PreviewKind` type, but only 3 require an edit: the CONCRETE-member consumers are `ToolDispatch.cs`, `RefactoringTools.cs` and `RefactoringService.cs`, and the last two consume only the existing five (`RefactoringService.cs:91/490/510/591/641/694`, `RefactoringTools.cs:59/94/129/168/209`) so they are untouched; `ApplyWithVerifyTool.cs` references the type only in a doc comment at `:23` and stays producer-agnostic by design; `PreviewStore.cs:88/94/109/122/148/160` already implements every kind-carrying concrete overload, so only the interface declaration changes. Probe ran, count recorded, matches `productionFilesTouched`. No stale anchors: every path and line cited resolved on `main` at 2026-08-25. |
| Validation | Per-edit: `mcp__roslyn__compile_check` after each file. Targeted: `mcp__roslyn__test_run --filter "ToolDispatchTests"`. New/extended cases in `ToolDispatchTests.cs`: (a) exhaustiveness — order 1's pin test stays green with ELEVEN concrete `PreviewKind` members and no catch-all reachable for a concrete kind; (b) map content — for each of the six new members the map returns the exact preview-tool and apply-route strings listed in the Approach, so a typo in a literal fails a test rather than shipping a misleading error message; (c) permissive path unchanged — `ApplyByTokenAsync_UnboundRoute_DoesNotEnforceProvenance` (`ToolDispatchTests.cs:130-146`) still passes, since no route is bound in this piece. Regression: `mcp__roslyn__test_run --filter "PreviewStoreTests"` confirms the new interface declaration's default body does not shadow `PreviewStore`'s concrete implementation. Full gate: `./eng/verify-release.ps1 -Configuration Release` plus `./eng/verify-ai-docs.ps1`. |
| Performance review | N/A — correctness/substrate change. No hot-path work is added: the map is consulted only on the mismatch-message path, which no route reaches until parts 2 and 3 land. |
| CHANGELOG category | Maintenance |
| CHANGELOG entry (draft) | **Maintenance:** added the `PreviewKind` members for the multi-file-edit, file-create, file-delete, file-move, code-action and fix-all producer families, registered all six in the centralized preview-kind to apply-route map, and declared the kind-carrying `IPreviewStore.Store` overload for the non-`changes` call shape. No runtime behavior change on its own — this is the substrate the remaining apply-route bindings build on. |
| Backlog sync | Close rows: []. Mark obsolete: []. Update related: [preview-token-apply-route-binding-remaining-families] — split from `preview-token-route-binding-editing-family`; the row closes with child 5 (`preview-token-route-binding-bulk-family`). |

<details>
<summary>Executor handoff notes</summary>

**Executor handoff:**

- **Pattern coordinates:** enum members — append after `CodeFix` at `PreviewKind.cs:42`, one-line `<summary>` each, matching `:29-42`; do not renumber (only `Unspecified = 0` is explicit; 1–5 are positional and consumed by `RefactoringTools.cs:59/94/129/168/209`). Interface overload — mirror the default-body shape at `IPreviewStore.cs:57-64`; the new `(…, bool diffTruncated, PreviewKind kind)` member delegates to the `bool`-shaped `:31`, exactly as the concrete `PreviewStore.cs:87-88` already does. All twelve tool-name literals re-verified on `main` against `[McpServerTool(Name=…)]` in `CodeActionTools.cs:51/76`, `FileOperationTools.cs:26/48/65/85/102/129`, `FixAllTools.cs:18/37`, `MultiFileEditTools.cs:76/100`.
- **Test target:** extend the existing `ToolDispatchTests` class (`ToolDispatchTests.cs:29`) — do NOT create a new file. Mirror `ApplyByTokenAsync_UnboundRoute_DoesNotEnforceProvenance` (`:130-146`) for the permissive-path re-pin.
- **Edge cases to cover:** (1) the default body is load-bearing — three in-tree fakes implement `IPreviewStore` explicitly (`ToolDispatchTests.cs:442-490`, `ApplyUndoWorkflowServiceTests.cs`, `ApplyWithVerifyCancellationAndScopeTests.cs`); a bodyless declaration breaks all three; (2) confirm the new overload does not shadow `PreviewStore.cs:94`'s concrete impl (that is what the `PreviewStoreTests` regression run checks); (3) overload resolution stays unambiguous against the `IReadOnlyList<FileChangeDto>`-shaped `:57`.
- **Negative space:** do NOT tag producers (`EditService.cs:302`, `CodeActionService.cs:152`, `FixAllService.cs:207`) — part 2. Do NOT pass `expectedKind`/`applyRoute` on any apply shim — part 3. Leave `RefactoringTools.cs` and `RefactoringService.cs` untouched. If order 1 replaced the `ToolDispatch.cs:207-229` switches with a map, add entries to the map — do not reintroduce switch arms.

**FAMILY CONTRACT — cross-initiative correction (orchestrator, post-review cycle 2; resolves the surviving `3` / `3b` warn findings recorded in `review.md`). Read this before following the Approach verbatim.**

- **`applyRoute:` NO LONGER EXISTS after order 1.** Verified live on `main` 2026-08-25: `ToolDispatch.ApplyByTokenAsync` declares `string? applyRoute = null` at `src/RoslynMcp.Host.Stdio/Tools/ToolDispatch.cs:133`, and initiative `preview-token-route-map-centralization` (order 1) Approach step (3) **deletes that parameter**. Every family child branches from a post-order-1 base via its `dependsOn` chain. Therefore: **bind with `expectedKind:` ONLY.** Any Approach step in this stanza instructing you to add an `applyRoute: "..."` named argument is superseded — drop it, keep `expectedKind:`. Following it verbatim is a guaranteed **CS1739**. Nothing is lost: the invoked-route half of the error message is derived from `expectedKind` by the centralized map.
- **The `RefactoringTools.cs:51-60` copy template is PRE-order-1.** It still shows the `expectedKind:` + `applyRoute:` pair. Copy the `expectedKind:` line and the two-line `preview-token-apply-route-provenance` comment; do NOT copy the `applyRoute:` line — order 1 removes all five of them.
- **Adding a concrete `PreviewKind` member obligates TWO companion edits, not one.** Order 1 pins that every non-`Unspecified` member resolves to BOTH a preview tool AND a catalog apply route. So a new member needs (a) its row in the `ToolDispatch` kind→previewTool map (`src/RoslynMcp.Host.Stdio/Tools/ToolDispatch.cs`; substitute order 1’s extracted map file if it created one — still one file), and (b) a `ServerSurfaceCatalog.PreviewApplyRoutes` entry (`src/RoslynMcp.Host.Stdio/Catalog/ServerSurfaceCatalog.cs:42-69`) **iff** its `*_preview` tool name is not already a key there. Verified live: that dictionary holds 24 entries and has **no** `preview_multi_file_edit` and **no** `fix_all_preview` key; all five of today’s concrete members ARE covered. Check your slice’s preview-tool names against the live dictionary FIRST — omitting either companion reds order 1’s pin test.
- **If the companions push you past Rule 3’s 4-file base, claim the gate-forced-companion exemption explicitly rather than exceeding silently** — gate: the C# compiler (CS0117 on an undeclared enum member) plus order 1’s exhaustiveness pin test; show the arithmetic; cap is 7. If it would exceed 7, **STOP and report** a needed re-split. Note that `ServerSurfaceCatalog.cs` is an addenda-listed hotspot (RMCP001/RMCP002-gated, ≤1 catalog-touching initiative per wave), so touching it also constrains wave scheduling.

</details>

### 2.02. preview-token-route-binding-multifile-codeaction

| Field | Content |
|---|---|
| Diagnosis | Split from `preview-token-route-binding-editing-family` per Rules 3, 5 and 5b — part 2 of 3. The parent slice was 11 production files / 85K; this piece takes the two families whose producers both call the 4-arg `IPreviewStore.Store` shape and whose apply routes are single. Verified on `main` 2026-08-25: `src/RoslynMcp.Host.Stdio/Tools/MultiFileEditTools.cs:110` (`preview_multi_file_edit_apply`) and `src/RoslynMcp.Host.Stdio/Tools/CodeActionTools.cs:85` (`apply_code_action`) both call `ToolDispatch.ApplyByTokenAsync` WITHOUT `expectedKind:` or `applyRoute:`, so `ToolDispatch.RequireCompatibleProducer` (`ToolDispatch.cs:185-188`) returns early and either route redeems any token in the shared store. Enforcement is producer-gated on the other side too: even a bound route no-ops when the STORED kind is `Unspecified` (`ToolDispatch.cs:191-194`), and both producers mint untagged — `EditService.cs:302` and `CodeActionService.cs:152` call `Store(workspaceId, solution, version, description)`, the legacy overload that `PreviewStore.cs:68-69` forwards as `diffTruncated: false` / `PreviewKind.Unspecified`. Binding the routes without tagging the producers would therefore buy only half the guarantee (a `rename_preview` token would start being refused by `apply_code_action`, but a code-action token would still redeem anywhere), which is why producer and route travel together in this piece. `apply_multi_file_edit` (`MultiFileEditTools.cs:35`) is a direct non-token apply and is untouched. |
| Approach | Four files, producer-then-route per family. (1) `src/RoslynMcp.Roslyn/Services/EditService.cs:302` — change the mint call to `Store(workspaceId, accumulator, _workspace.GetCurrentVersion(workspaceId), description, diffTruncated: false, kind: PreviewKind.MultiFileEdit)` using the interface overload declared by initiative `preview-token-route-binding-editing-substrate`; `diffTruncated: false` preserves today's exact semantic, since `PreviewStore.cs:68-69` is what the 4-arg form already forwards to. (2) `src/RoslynMcp.Host.Stdio/Tools/MultiFileEditTools.cs:110` — add `expectedKind: PreviewKind.MultiFileEdit` and `applyRoute: "preview_multi_file_edit_apply"` as trailing named arguments, mirroring `RefactoringTools.cs:51-60` verbatim including its two-line `preview-token-apply-route-provenance` provenance comment. (3) `src/RoslynMcp.Roslyn/Services/CodeActionService.cs:152` — same tagging with `kind: PreviewKind.CodeAction`. (4) `src/RoslynMcp.Host.Stdio/Tools/CodeActionTools.cs:85` — bind with `expectedKind: PreviewKind.CodeAction`, `applyRoute: "apply_code_action"`. Do NOT touch `ToolDispatch.cs`, `PreviewKind.cs` or `IPreviewStore.cs` — part 1 owns all three. Do NOT bind `apply_with_verify`, which is producer-agnostic by design (`ApplyWithVerifyTool.cs:23`). |
| Scope | Production files: 4 — `src/RoslynMcp.Roslyn/Services/EditService.cs`, `src/RoslynMcp.Host.Stdio/Tools/MultiFileEditTools.cs`, `src/RoslynMcp.Roslyn/Services/CodeActionService.cs`, `src/RoslynMcp.Host.Stdio/Tools/CodeActionTools.cs`. Test files: 1 — add `tests/RoslynMcp.Tests/PreviewRouteBindingEditingTests.cs`. Exactly at the Rule 3 cap of 4; no exemption claimed or needed. Zero production-file overlap with part 1 or part 3. |
| Tool policy | edit-only |
| Estimated context cost | 45000 |
| Risks | COMPILE DEPENDENCY, NO FILE OVERLAP: this piece needs `PreviewKind.MultiFileEdit` / `PreviewKind.CodeAction` and the kind-carrying `IPreviewStore.Store` declaration, all introduced by `preview-token-route-binding-editing-substrate` while sharing ZERO production files with it — the conflict graph cannot see that, so `dependsOn: [preview-token-route-binding-editing-substrate]` is load-bearing; cutting this branch from a base without part 1 fails to compile. BEHAVIOR TIGHTENING: a caller that today redeems, say, a `rename_preview` token through `apply_code_action` starts receiving an `InvalidOperationException` envelope naming both the real producer and the correct apply route, before any mutation — intended, but a live error-path behavior change. WRONG-OVERLOAD SILENT DROP: `IPreviewStore` carries several `Store` overloads and the kind-discarding default body means passing the kind to the wrong shape compiles but silently records `Unspecified`; the producer round-trip assertion in Validation is the guard. PERMISSIVE PATHS PRESERVED: untagged producers and out-of-tree `IPreviewStore` implementers still redeem anywhere via the `PeekKind` default (`IPreviewStore.cs:157`), and `ToolDispatchTests.cs:130-146` stays valid — retiring or re-scoping it is child 5's job per the parent Acceptance. FANOUT PROBE (shape: named-argument addition at 2 call sites plus 2 mint-site tags): measured 4 production files, enumerated by walking each of the two routes from its `ApplyByTokenAsync` call site to its minting `Store` call — `MultiFileEditTools.cs:110` → `EditService.cs:302`, `CodeActionTools.cs:85` → `CodeActionService.cs:152`. Probe ran; measured count recorded as `fanoutEstimate: 4`, equal to `productionFilesTouched: 4` (Rule 5b within ±1, `fanoutOversize: false`, no `heroic-last`). No stale anchors: every path and line cited re-resolved on `main` at 2026-08-25. |
| Validation | Per-edit: `mcp__roslyn__compile_check` after each file. Targeted: `mcp__roslyn__test_run --filter "PreviewRouteBindingEditingTests"` and `--filter "ToolDispatchTests"`. New cases in `PreviewRouteBindingEditingTests.cs`, per bound route: (a) mismatch — a token stored with `PreviewKind.SymbolRename` redeemed via `preview_multi_file_edit_apply` throws before the service lambda runs, and the message names both `rename_preview` and `rename_apply`; (b) match — a token stored with `PreviewKind.MultiFileEdit` redeemed via `preview_multi_file_edit_apply` succeeds; (c) permissive — a token stored `Unspecified` still redeems through both routes; (d)-(f) the same triple for `apply_code_action` / `PreviewKind.CodeAction`. Producer round-trip (guards the wrong-overload risk): call `preview_multi_file_edit` and `preview_code_action`, then assert `IPreviewStore.PeekKind` returns `MultiFileEdit` and `CodeAction` respectively rather than `Unspecified`. Regression: `mcp__roslyn__test_run --filter "ApplyUndoWorkflowServiceTests"` (existing `PreviewKind` consumer). Full gate: `./eng/verify-release.ps1 -Configuration Release` plus `./eng/verify-ai-docs.ps1`. |
| Performance review | N/A — correctness fix. The added work is one non-consuming `PeekKind` dictionary lookup per apply, already on the redemption path since PR #1354; token minting gains only an enum field assignment. |
| CHANGELOG category | Fixed |
| CHANGELOG entry (draft) | **Fixed:** `preview_multi_file_edit_apply` and `apply_code_action` are now bound to their producer families. Each records a concrete `PreviewKind` when its paired preview tool mints the token and refuses a token minted by a different family BEFORE any workspace mutation, naming the token's actual producer and the apply route it should be redeemed through. `apply_with_verify` stays producer-agnostic by design; tokens whose producer is unrecorded still redeem unchanged. |
| Backlog sync | Close rows: []. Mark obsolete: []. Update related: [preview-token-apply-route-binding-remaining-families] — split from `preview-token-route-binding-editing-family`; the row closes with child 5 (`preview-token-route-binding-bulk-family`). |

<details>
<summary>Executor handoff notes</summary>

**Executor handoff:**

- **Pattern coordinates:** the route-binding template is `src/RoslynMcp.Host.Stdio/Tools/RefactoringTools.cs:51-60` — `ApplyByTokenAsync(gate, previewStore, previewToken, serviceCall, ct, expectedKind:, applyRoute:)` with the two-line `preview-token-apply-route-provenance` comment at `:57-58`. Copy that trailing-argument shape verbatim into `MultiFileEditTools.cs:110-115` and `CodeActionTools.cs:85-90` (both currently end at `ct);`).
- **Missing using (compile blocker):** `PreviewKind` lives in `RoslynMcp.Core.Models` (`src/RoslynMcp.Core/Models/PreviewKind.cs:21`). `CodeActionTools.cs` has no `using RoslynMcp.Core.Models;` (its usings stop at line 5) — add it. The other three files already import it.
- **Test target:** new file `tests/RoslynMcp.Tests/PreviewRouteBindingEditingTests.cs`; mirror `ToolDispatchTests.cs:38-69` (mismatch, asserts service lambda never ran + all three names in the message), `:76-95` (match), `:100-118` (permissive `Unspecified`). Note `FakeGate` (`ToolDispatchTests.cs:400`) and `FakePreviewStore` (`:442`, `PeekKind` at `:471`) are **private nested** — redeclare equivalents in the new file rather than referencing them.
- **Mint sites:** `EditService.cs:302` and `CodeActionService.cs:152` — both are single-line `_previewStore.Store(workspaceId, <solution>, _workspace.GetCurrentVersion(workspaceId), description);`. Pass `diffTruncated:`/`kind:` as named arguments to avoid the wrong-overload silent-`Unspecified` drop.
- **Negative space:** do NOT touch `MultiFileEditTools.cs:35` (`apply_multi_file_edit` — direct, tokenless), `ApplyWithVerifyTool.cs:23` (producer-agnostic by design), or `ToolDispatch.cs` / `PreviewKind.cs` / `IPreviewStore.cs` (part 1 owns all three).

**FAMILY CONTRACT — cross-initiative correction (orchestrator, post-review cycle 2; resolves the surviving `3` / `3b` warn findings recorded in `review.md`). Read this before following the Approach verbatim.**

- **`applyRoute:` NO LONGER EXISTS after order 1.** Verified live on `main` 2026-08-25: `ToolDispatch.ApplyByTokenAsync` declares `string? applyRoute = null` at `src/RoslynMcp.Host.Stdio/Tools/ToolDispatch.cs:133`, and initiative `preview-token-route-map-centralization` (order 1) Approach step (3) **deletes that parameter**. Every family child branches from a post-order-1 base via its `dependsOn` chain. Therefore: **bind with `expectedKind:` ONLY.** Any Approach step in this stanza instructing you to add an `applyRoute: "..."` named argument is superseded — drop it, keep `expectedKind:`. Following it verbatim is a guaranteed **CS1739**. Nothing is lost: the invoked-route half of the error message is derived from `expectedKind` by the centralized map.
- **The `RefactoringTools.cs:51-60` copy template is PRE-order-1.** It still shows the `expectedKind:` + `applyRoute:` pair. Copy the `expectedKind:` line and the two-line `preview-token-apply-route-provenance` comment; do NOT copy the `applyRoute:` line — order 1 removes all five of them.
- **Adding a concrete `PreviewKind` member obligates TWO companion edits, not one.** Order 1 pins that every non-`Unspecified` member resolves to BOTH a preview tool AND a catalog apply route. So a new member needs (a) its row in the `ToolDispatch` kind→previewTool map (`src/RoslynMcp.Host.Stdio/Tools/ToolDispatch.cs`; substitute order 1’s extracted map file if it created one — still one file), and (b) a `ServerSurfaceCatalog.PreviewApplyRoutes` entry (`src/RoslynMcp.Host.Stdio/Catalog/ServerSurfaceCatalog.cs:42-69`) **iff** its `*_preview` tool name is not already a key there. Verified live: that dictionary holds 24 entries and has **no** `preview_multi_file_edit` and **no** `fix_all_preview` key; all five of today’s concrete members ARE covered. Check your slice’s preview-tool names against the live dictionary FIRST — omitting either companion reds order 1’s pin test.
- **If the companions push you past Rule 3’s 4-file base, claim the gate-forced-companion exemption explicitly rather than exceeding silently** — gate: the C# compiler (CS0117 on an undeclared enum member) plus order 1’s exhaustiveness pin test; show the arithmetic; cap is 7. If it would exceed 7, **STOP and report** a needed re-split. Note that `ServerSurfaceCatalog.cs` is an addenda-listed hotspot (RMCP001/RMCP002-gated, ≤1 catalog-touching initiative per wave), so touching it also constrains wave scheduling.

</details>

### 2.03. preview-token-route-binding-fileops-fixall

| Field | Content |
|---|---|
| Diagnosis | Split from `preview-token-route-binding-editing-family` per Rules 3, 5 and 5b — part 3 of 3. The parent slice was 11 production files / 85K; this piece takes the file-operation trio plus fix-all. Verified on `main` 2026-08-25: four unbound routes — `src/RoslynMcp.Host.Stdio/Tools/FileOperationTools.cs:58/95/139` (`create_file_apply`, `delete_file_apply`, `move_file_apply`) and `src/RoslynMcp.Host.Stdio/Tools/FixAllTools.cs:47` (`fix_all_apply`) — call `ToolDispatch.ApplyByTokenAsync` with no `expectedKind:` / `applyRoute:`, so `RequireCompatibleProducer` (`ToolDispatch.cs:185-188`) returns early. The two producers differ in call shape, which is the reason they pair cleanly here: `FileOperationService.cs:53/106/156` already uses the `changes`-shaped 5-arg `Store`, whose kind-carrying overload the interface ALREADY declares (`IPreviewStore.cs:57-64`), while `FixAllService.cs:207` uses the legacy 4-arg `Store(workspaceId, newSolution, version, description)` and needs the overload part 1 adds. `move_file_preview` (`FileOperationService.cs:156`) can also emit warnings; tagging happens at the `Store` call and does not interact with the warnings path. |
| Approach | Four files, producer-then-route per family. (1) `src/RoslynMcp.Roslyn/Services/FileOperationService.cs` — append `kind:` to the three existing `changes`-shaped mint calls using the overload already on the interface: `:53` gets `PreviewKind.FileCreate`, `:106` gets `PreviewKind.FileDelete`, `:156` gets `PreviewKind.FileMove`. (2) `src/RoslynMcp.Host.Stdio/Tools/FileOperationTools.cs` — bind the three routes with trailing named arguments: `:58` gets `expectedKind: PreviewKind.FileCreate`, `applyRoute: "create_file_apply"`; `:95` gets `FileDelete` / `"delete_file_apply"`; `:139` gets `FileMove` / `"move_file_apply"` — mirroring `RefactoringTools.cs:51-60` verbatim including its provenance comment. (3) `src/RoslynMcp.Roslyn/Services/FixAllService.cs:207` — move to the kind-carrying non-`changes` overload declared by `preview-token-route-binding-editing-substrate`: `Store(workspaceId, newSolution, _workspace.GetCurrentVersion(workspaceId), description, diffTruncated: false, kind: PreviewKind.FixAll)`, where `diffTruncated: false` preserves today's exact semantic per `PreviewStore.cs:68-69`. (4) `src/RoslynMcp.Host.Stdio/Tools/FixAllTools.cs:47` — bind with `expectedKind: PreviewKind.FixAll`, `applyRoute: "fix_all_apply"`. Do NOT touch `ToolDispatch.cs`, `PreviewKind.cs` or `IPreviewStore.cs` — part 1 owns all three. Do NOT bind `apply_with_verify` (`ApplyWithVerifyTool.cs:23`). |
| Scope | Production files: 4 — `src/RoslynMcp.Roslyn/Services/FileOperationService.cs`, `src/RoslynMcp.Host.Stdio/Tools/FileOperationTools.cs`, `src/RoslynMcp.Roslyn/Services/FixAllService.cs`, `src/RoslynMcp.Host.Stdio/Tools/FixAllTools.cs`. Test files: 1 — add `tests/RoslynMcp.Tests/PreviewRouteBindingFileOpsTests.cs`. Exactly at the Rule 3 cap of 4; no exemption claimed or needed. Fanout probe measured 4 (see Risks), recorded as `fanoutEstimate: 4` — equal to `productionFilesTouched`, so Rule 5b is satisfied with `fanoutOversize: false` and no `heroic-last` hint. Zero production-file overlap with part 1 or part 2, so this ships in parallel with part 2 once part 1 lands. |
| Tool policy | edit-only |
| Estimated context cost | 50000 |
| Risks | COMPILE DEPENDENCY, NO FILE OVERLAP: this piece needs the four new `PreviewKind` members and (for `FixAllService`) the kind-carrying non-`changes` `IPreviewStore.Store` declaration, both from `preview-token-route-binding-editing-substrate`, while sharing ZERO production files with it — `dependsOn: [preview-token-route-binding-editing-substrate]` is load-bearing; without it the conflict graph reports this parallelizable and the branch fails to compile. THREE KINDS IN ONE FILE: `FileOperationService.cs` mints three different kinds at three sites; a copy-paste that tags delete with `FileCreate` compiles cleanly and only surfaces as a wrong error message, so the per-kind producer round-trip assertion in Validation is mandatory, not optional. BEHAVIOR TIGHTENING: a caller that today redeems a code-action token through `fix_all_apply` (or a create token through `move_file_apply`) starts receiving an `InvalidOperationException` envelope before any mutation — intended, but a live error-path behavior change. PERMISSIVE PATHS PRESERVED: untagged producers and out-of-tree `IPreviewStore` implementers still redeem anywhere via the `PeekKind` default (`IPreviewStore.cs:157`), and `ToolDispatchTests.cs:130-146` stays valid — retiring or re-scoping it is child 5's job per the parent Acceptance. FANOUT PROBE (shape: named-argument addition at 4 call sites plus 4 mint-site tags): probe ran, measured count = 4 production files, enumerated by walking each of the four routes from its `ApplyByTokenAsync` call site to its minting `Store` call — `FileOperationTools.cs` → `FileOperationService.cs` (3 routes, 3 mint sites) and `FixAllTools.cs` → `FixAllService.cs` (1 route, 1 mint site). No fifth file is pulled in: `ToolDispatch.cs`, `PreviewKind.cs` and `IPreviewStore.cs` are owned by part 1 and are consumed, not edited, here. Measured fanout 4 == `productionFilesTouched` 4 (delta 0, within Rule 5b's ±1 band); recorded as `fanoutEstimate: 4`, `fanoutOversize: false`, `scheduleHint: null`. No stale anchors: every path and line cited was re-resolved on `main` at 2026-08-25. |
| Validation | Per-edit: `mcp__roslyn__compile_check` after each file. Targeted: `mcp__roslyn__test_run --filter "PreviewRouteBindingFileOpsTests"` and `--filter "ToolDispatchTests"`. New cases in `PreviewRouteBindingFileOpsTests.cs`, per bound route (four routes, three cases each): (a) mismatch — a token stored with `PreviewKind.SymbolRename` redeemed via the route throws before the service lambda runs, and the message names both `rename_preview` and `rename_apply`; (b) match — a token stored with the route's own kind succeeds; (c) permissive — a token stored `Unspecified` still redeems. Cross-family case: a token stored `PreviewKind.FileCreate` redeemed via `move_file_apply` is refused and the message names `create_file_preview` and `create_file_apply` — this is the case that catches a mis-tagged mint site inside `FileOperationService.cs`. Producer round-trip: call `create_file_preview`, `delete_file_preview`, `move_file_preview` and `fix_all_preview`, then assert `IPreviewStore.PeekKind` returns `FileCreate`, `FileDelete`, `FileMove` and `FixAll` respectively. Regression: `mcp__roslyn__test_run --filter "PreviewStoreTests"` confirms the `FixAllService` overload move keeps `diffTruncated` false. Full gate: `./eng/verify-release.ps1 -Configuration Release` plus `./eng/verify-ai-docs.ps1`. |
| Performance review | N/A — correctness fix. The added work is one non-consuming `PeekKind` dictionary lookup per apply, already on the redemption path since PR #1354; token minting gains only an enum field assignment. |
| CHANGELOG category | Fixed |
| CHANGELOG entry (draft) | **Fixed:** `create_file_apply`, `delete_file_apply`, `move_file_apply` and `fix_all_apply` are now bound to their producer families. Each records a concrete `PreviewKind` when its paired preview tool mints the token and refuses a token minted by a different family BEFORE any workspace mutation, naming the token's actual producer and the apply route it should be redeemed through. `apply_with_verify` stays producer-agnostic by design; tokens whose producer is unrecorded still redeem unchanged. |
| Backlog sync | Close rows: []. Mark obsolete: []. Update related: [preview-token-apply-route-binding-remaining-families] — split from `preview-token-route-binding-editing-family`; the row closes with child 5 (`preview-token-route-binding-bulk-family`). |

<details>
<summary>Executor handoff notes</summary>

**Executor handoff:**

- **Pattern coordinates:** copy `src/RoslynMcp.Host.Stdio/Tools/RefactoringTools.cs:51-60` verbatim — the `ct,` line is followed by the two-line `// preview-token-apply-route-provenance:` comment (`:57-58`) then `expectedKind:` / `applyRoute:`. Reproduce the comment at all four bind sites.
- **Path correction:** the interface is `src/RoslynMcp.Roslyn/Contracts/IPreviewStore.cs` (not `Services/`); the kind-carrying `changes`-shaped `Store` is declared there with a default body that drops `kind`.
- **Mint-site disambiguation (highest drift risk):** `FileOperationService.cs:53/:106/:156` are **byte-identical lines**. Do not search-replace. Anchor on the `description` line immediately above each: `:52` = `Create file` → `FileCreate`; `:105` = `Delete file` → `FileDelete`; `:155` = `Move file` → `FileMove`.
- **FixAllService:** a `changes` local exists at `:205` (DTO-only). Do **not** switch `:207` to the `changes`-shaped overload — use the non-`changes` kind overload with `diffTruncated: false`; `PreviewStore.cs:68-69` confirms today's 4-arg call already means `false`.
- **Test target:** new `tests/RoslynMcp.Tests/PreviewRouteBindingFileOpsTests.cs`. Mirror `ToolDispatchTests.cs:39` (mismatch), `:77` (match), `:105` (permissive). Its `FakePreviewStore` (`:442-490`, `Kind` init `:465`, `PeekKind` `:471`) is a **private nested** class — copy the shape into the new file; do not widen its accessibility.
- **Negative space:** do NOT edit `ToolDispatch.cs`, `PreviewKind.cs`, `Contracts/IPreviewStore.cs` (part 1 owns), `ApplyWithVerifyTool.cs:23`, or the `*_preview` tool methods in `FileOperationTools.cs` (`:40`, `:77`, `:117`) — tagging happens at the service `Store` call only.

**FAMILY CONTRACT — cross-initiative correction (orchestrator, post-review cycle 2; resolves the surviving `3` / `3b` warn findings recorded in `review.md`). Read this before following the Approach verbatim.**

- **`applyRoute:` NO LONGER EXISTS after order 1.** Verified live on `main` 2026-08-25: `ToolDispatch.ApplyByTokenAsync` declares `string? applyRoute = null` at `src/RoslynMcp.Host.Stdio/Tools/ToolDispatch.cs:133`, and initiative `preview-token-route-map-centralization` (order 1) Approach step (3) **deletes that parameter**. Every family child branches from a post-order-1 base via its `dependsOn` chain. Therefore: **bind with `expectedKind:` ONLY.** Any Approach step in this stanza instructing you to add an `applyRoute: "..."` named argument is superseded — drop it, keep `expectedKind:`. Following it verbatim is a guaranteed **CS1739**. Nothing is lost: the invoked-route half of the error message is derived from `expectedKind` by the centralized map.
- **The `RefactoringTools.cs:51-60` copy template is PRE-order-1.** It still shows the `expectedKind:` + `applyRoute:` pair. Copy the `expectedKind:` line and the two-line `preview-token-apply-route-provenance` comment; do NOT copy the `applyRoute:` line — order 1 removes all five of them.
- **Adding a concrete `PreviewKind` member obligates TWO companion edits, not one.** Order 1 pins that every non-`Unspecified` member resolves to BOTH a preview tool AND a catalog apply route. So a new member needs (a) its row in the `ToolDispatch` kind→previewTool map (`src/RoslynMcp.Host.Stdio/Tools/ToolDispatch.cs`; substitute order 1’s extracted map file if it created one — still one file), and (b) a `ServerSurfaceCatalog.PreviewApplyRoutes` entry (`src/RoslynMcp.Host.Stdio/Catalog/ServerSurfaceCatalog.cs:42-69`) **iff** its `*_preview` tool name is not already a key there. Verified live: that dictionary holds 24 entries and has **no** `preview_multi_file_edit` and **no** `fix_all_preview` key; all five of today’s concrete members ARE covered. Check your slice’s preview-tool names against the live dictionary FIRST — omitting either companion reds order 1’s pin test.
- **If the companions push you past Rule 3’s 4-file base, claim the gate-forced-companion exemption explicitly rather than exceeding silently** — gate: the C# compiler (CS0117 on an undeclared enum member) plus order 1’s exhaustiveness pin test; show the arithmetic; cap is 7. If it would exceed 7, **STOP and report** a needed re-split. Note that `ServerSurfaceCatalog.cs` is an addenda-listed hotspot (RMCP001/RMCP002-gated, ≤1 catalog-touching initiative per wave), so touching it also constrains wave scheduling.

</details>

