# Backlog sweep plan — 20260518T221744Z

**Generated:** 2026-05-18T22:17:44Z
**Backlog snapshot:** 2026-05-18T03:09:14Z
**Mode:** `/backlog-sweep:prepare count=15`
**Initiative count:** 15 (all P2 Medium; skipped `firewallanalyzer-p2-polish-aggregate-20260516` aggregator row — intake-tracking only, not a single-initiative target)
**Phase:** complete (readyForExecute: true)
**Conflict graph:** 2 edges — (6,13) share `WorkspaceValidationService.cs`; (9,12) share `SymbolTools.cs`. 11 zero-degree initiatives safely parallelizable.
**Review:** passed (0 block, 0 warn, 3 info — cycle 0 only; no remediation needed)
**Handoff:** 1 spawned (initiative 3, judgmentHeavy), 14 skipped per auto policy

## Plan summary

Fifteen P2 Medium initiatives selected from the 24-row claimable backlog after the 20260517T235058Z sweep cleared all 8 P1 High rows. Selection covers refactor-tool correctness (extract_interface duplicate, change_type_namespace, symbol_refactor, migrate_package), response-shape bugs (project_diagnostics, symbol_signature_help, validate_workspace overallStatus, find_property_writes), payload-overflow patterns (find_overrides corlib, analyze_dependencies prompt, review_test_coverage prompt), timeout-handling (validate_workspace 25s, workspace_status verbose), test-runner integration (test_run FQDN drift), and a doc-gap (source_file resource URL encoding).

## Bundle considerations (Rule 1)

Two pairs flagged as bundle candidates — the deepener will verify or split:

- **Initiatives 6 + 13:** `validate-workspace-overallstatus-analyzer-error-with-empty-errordiagnostics` (gh #751) + `validate-workspace-25s-internalvalidationtimeoutexception-on-medium-solution` (gh #759). Both touch `WorkspaceValidationService.cs` but likely different code paths (overallStatus computation vs timeout-exception graceful-failure-envelope). Deepener verifies Rule 1 four conditions.
- **Initiatives 10 + 11:** `analyze-dependencies-prompt-payload-overflow` (gh #755) + `review-test-coverage-prompt-payload-overflow` (gh #756). Both prompt-payload-overflow patterns mirroring the shipped `guided_extract_interface` precedent. Different prompt renderers — Rule 1 four conditions unlikely to hold; expect split.

## Initiatives

### 1. project-diagnostics-totaldiagnostics-collapses-under-severity-filter

| Field | Content |
|---|---|
| Status | pending |
| Backlog rows closed | `project-diagnostics-totaldiagnostics-collapses-under-severity-filter` |
| Source | gh #746 |
| Diagnosis | Root cause is in `src/RoslynMcp.Host.Stdio/Tools/AnalysisTools.cs` at lines 98 and 113. The `totalDiagnostics` field in both the summary and non-summary response branches is computed as `allDiagnostics.Count`, where `allDiagnostics` is assembled from `results.WorkspaceDiagnostics`, `results.CompilerDiagnostics`, and `results.AnalyzerDiagnostics`. These three arrays are the *severity-filtered* outputs from `DiagnosticService.GetDiagnosticsAsync`. When `severity=Error` is passed, those arrays contain only Error-level items, so `allDiagnostics.Count` collapses to the error count, not the true diagnostic total. The service already computes severity-filter-invariant totals (`results.TotalErrors`, `results.TotalWarnings`, `results.TotalInfo`) that ignore the severity filter — confirmed by the BUG-fix comment at `DiagnosticService.cs:122-125` and the corresponding test `DiagnosticServiceFilterTotalsTests.GetDiagnosticsAsync_TotalsAreInvariantUnderSeverityFilter`. The tool description on line 24 already documents the invariant ("Totals totalErrors/totalWarnings/totalInfo count the full queried scope and ignore the severity filter") but `totalDiagnostics` contradicts it. **Anchor stale:** backlog row cites `src/RoslynMcp.Roslyn/Services/ProjectDiagnosticsService.cs` which does not exist. Bug is in tool-surface layer `AnalysisTools.cs`. |
| Approach | Edit `src/RoslynMcp.Host.Stdio/Tools/AnalysisTools.cs`. In `GetProjectDiagnostics`, replace both occurrences of `totalDiagnostics = allDiagnostics.Count` (lines 98 and 113) with `totalDiagnostics = results.TotalErrors + results.TotalWarnings + results.TotalInfo`. This expression is filter-invariant by construction (confirmed from `DiagnosticService.cs:126-135`). The `allDiagnostics.Count` variable continues to be used for the `hasMore` pagination sentinel (line 63) and `returnedDiagnostics` (line 116) — those uses are correct and untouched. No service-layer changes needed. Add 1 regression test in `tests/RoslynMcp.Tests/DiagnosticServiceFilterTotalsTests.cs` (extend the existing class): assert that when `project_diagnostics` is called with `severity=Error`, the returned `totalDiagnostics` equals `totalErrors + totalWarnings + totalInfo` from the same response. |
| Scope | Production files: 1 — `src/RoslynMcp.Host.Stdio/Tools/AnalysisTools.cs`. Test files modified: 1 — `tests/RoslynMcp.Tests/DiagnosticServiceFilterTotalsTests.cs`. Rule 3 exemption: tool-surface-only, 1 file (within the 2-file cap). |
| Tool policy | edit-only |
| Estimated context cost | 22000 |
| Risks | (1) Both `summary=true` and default branches must be fixed — missing one leaves partial regression. (2) `hasMore` and `returnedDiagnostics` keep using filtered `allDiagnostics.Count` — must NOT change. (3) `Hidden`-severity diagnostics are excluded per `DiagnosticService.cs:249`; acceptable. (4) **Anchor stale:** cited `ProjectDiagnosticsService.cs` does not exist; real file `AnalysisTools.cs`. |
| Validation | (1) Existing `DiagnosticServiceFilterTotalsTests` pass. (2) New test asserts `totalDiagnostics == totalErrors + totalWarnings + totalInfo` under `severity=Error`. (3) Default (`severity=null`) yields same value. (4) `summary=true` path also verified. (5) `mcp__roslyn__compile_check` zero errors. (6) `mcp__roslyn__test_run --filter DiagnosticServiceFilterTotalsTests`. |
| Performance review | N/A — correctness fix, no hot-path changes. |
| CHANGELOG category | Fixed |
| CHANGELOG entry (draft) | Fixed `project_diagnostics` returning a severity-filtered `totalDiagnostics` count when a `severityFilter` is applied (e.g. `severity=Error` collapsed `totalDiagnostics` to the error count only). `totalDiagnostics` now matches `totalErrors + totalWarnings + totalInfo` and is invariant under severity filtering, consistent with the documented behavior of the other total fields. Closes gh #746. |
| Backlog sync | Close rows: [`project-diagnostics-totaldiagnostics-collapses-under-severity-filter`]. |

### 2. symbol-signature-help-returns-bare-null-for-resolvable-method-metadata

| Field | Content |
|---|---|
| Status | pending |
| Backlog rows closed | `symbol-signature-help-returns-bare-null-for-resolvable-method-metadata` |
| Source | gh #747 |
| Diagnosis | `GetSignatureHelpAsync` in `src/RoslynMcp.Roslyn/Services/SymbolRelationshipService.cs:224-248` returns `null` whenever `SymbolResolver.ResolveAsync` returns null for a `metadataName` locator whose method name contains a parenthesized parameter list (e.g. `SampleLib.AnimalService.CountAnimals(System.Collections.Generic.List<SampleLib.IAnimal>)`). Root cause: `SymbolResolver.ResolveByMetadataNameAsync` splits on the last `.` — when that `.` sits inside the parameter list (e.g. `System.Threading.CancellationToken`), the split produces a bogus containing-type name and returns null. The sibling method `GetCallersCalleesAsync` (line 250) has an identical failure mode and was fixed in gh #616 by adding a `TryResolveByQualifiedSignatureAsync` fallback at lines 265-268; `GetSignatureHelpAsync` never received that fallback. **Anchor stale:** backlog row cites `src/NetworkDocumentation.Core/...:57` (sibling-repo); in-repo anchors verified at `SymbolRelationshipService.cs:265-268` (pattern to mirror) and `tests/RoslynMcp.Tests/MetadataNameLocatorTests.cs:130-159` (test pattern). |
| Approach | One change in `src/RoslynMcp.Roslyn/Services/SymbolRelationshipService.cs`. Inside `GetSignatureHelpAsync` (line 228), after the `if (symbol is null) return null;` guard at line 229, add the fallback block mirroring callers-callees (lines 265-268): `if (symbol is null && locator.HasMetadataName) { symbol = await TryResolveByQualifiedSignatureAsync(solution, locator.MetadataName!, ct).ConfigureAwait(false); }`, then keep the existing null guard. Extend `tests/RoslynMcp.Tests/MetadataNameLocatorTests.cs` with `SignatureHelp_Accepts_Fully_Qualified_Method_Signature_With_Parameter_Types` mirroring the existing callers-callees test at line 130. |
| Scope | Production files: 1 (`SymbolRelationshipService.cs`). Test files modified: 1 (`MetadataNameLocatorTests.cs`). No new files. |
| Tool policy | edit-only |
| Estimated context cost | 28000 |
| Risks | `TryResolveByQualifiedSignatureAsync` is `internal static` — no visibility change. Verify: (1) plain `metadataName` (no parens) still works via original path; (2) type-symbol metadataName still reaches `PromoteToDeclaringMemberIfRequestedAsync`; (3) `preferDeclaringMember=false` with metadataName still behaves correctly. **Anchor stale.** |
| Validation | (1) `mcp__roslyn__compile_check` zero errors. (2) `mcp__roslyn__test_run --filter "MetadataNameLocatorTests"` — new test passes. (3) Existing `SemanticExpansionTests.Symbol_Signature_Help_Returns_Parameters_And_Return_Type` regression guard. (4) Manual repro returns non-null JSON object with `displaySignature`, `parameters`, `returnType`. |
| Performance review | N/A — correctness fix. |
| CHANGELOG category | Fixed |
| CHANGELOG entry (draft) | Fixed `symbol_signature_help` returning bare null for method metadataName inputs containing parenthesized parameter types (e.g. `Namespace.Type.Method(ParamType, CancellationToken)`). Adds the same qualified-signature fallback resolver that `callers_callees` received in gh #616. Fixes gh #747. |
| Backlog sync | Close rows: [`symbol-signature-help-returns-bare-null-for-resolvable-method-metadata`]. |

### 3. extract-interface-preview-duplicate-interface-when-already-implements

| Field | Content |
|---|---|
| Status | pending |
| Backlog rows closed | `extract-interface-preview-duplicate-interface-when-already-implements` |
| Source | gh #748 |
| Diagnosis | Root cause in `src/RoslynMcp.Roslyn/Services/InterfaceExtractionService.cs`. In `PreviewExtractInterfaceAsync`, the `alreadyImplements` guard at line 67-68 (`typeSymbol.AllInterfaces.Any(...)`) correctly gates the base-list modification (lines 71-117) but does NOT gate the interface document addition at lines 133-140. When the type already implements a covering interface (defined externally — e.g. in a Contracts project), `alreadyImplements==true`, the base-list rewrite is skipped correctly, but `targetProject.AddDocument(...)` at line 139 is still executed unconditionally. Result: the preview diff includes a freshly generated `IFoo.cs` even though the type already has `IFoo` in its base list. **Anchor stale:** backlog row cites `NetworkDocumentation.Core/DeviceClassifier/DeviceClassifierService.cs:11` (sibling-repo); real anchor `InterfaceExtractionService.cs:133-140`. A parallel guard exists in `ExtractAndWireOrchestrator` (tested in `OrchestrationIntegrationTests.cs:368`) but `InterfaceExtractionService` lacks the equivalent guard on its document-add path. |
| Approach | Fix in `src/RoslynMcp.Roslyn/Services/InterfaceExtractionService.cs`. Move the document-addition block (lines 133-140) inside `if (!alreadyImplements)`, mirroring the pattern at lines 71-117. When `alreadyImplements==true`, produce a no-op diff and surface a note in the `description` field of `RefactoringPreviewDto`. Move `alreadyImplements` computation before `ValidateNoConflictsAsync` (line 54) and short-circuit the conflict check when `alreadyImplements==true`. Extend `tests/RoslynMcp.Tests/ExtractInterfaceSemanticUsingsTests.cs` with one new `[TestMethod]`: type already implements `IFoo` defined in a sibling namespace; call `PreviewExtractInterfaceAsync(interfaceName: "IFoo")`; assert preview contains zero new-file changes and zero base-list mutations. |
| Scope | Production files: 1 (`InterfaceExtractionService.cs`). Test files: 1 (extend `ExtractInterfaceSemanticUsingsTests.cs`). |
| Tool policy | edit-only |
| Estimated context cost | 28000 |
| Risks | (1) `ValidateNoConflictsAsync` reorder could mask a real "same-name different namespace" conflict — ensure `alreadyImplements` check validates namespace parity OR keep ValidateNoConflicts running with suppressed throw on alreadyImplements. (2) `replaceUsages` path (lines 142-146) may also need guarding — verify. (3) **Anchor stale.** |
| Validation | (1) New test: fixture where class already implements `IFoo` from sibling namespace; `PreviewExtractInterfaceAsync` returns zero file changes + zero base-list diff. (2) Existing `ExtractInterfaceSemanticUsingsTests` pass. (3) `OrchestrationIntegrationTests.Extract_And_Wire_Interface_Preview_Declines_When_Type_Already_Implements_Same_Named_Interface` still passes. (4) `mcp__roslyn__compile_check`. |
| Performance review | N/A — correctness fix. |
| CHANGELOG category | Fixed |
| CHANGELOG entry (draft) | Fixed `extract_interface_preview` emitting a duplicate interface file when the target type already implements a covering interface. Previously the preview diff included a new `IFoo.cs` even when the type's base list already contained `IFoo`; the apply would create a conflicting file. The base list is also correctly left unmodified in this case. Fixes gh #748. |
| Backlog sync | Close rows: [`extract-interface-preview-duplicate-interface-when-already-implements`]. |

<details>
<summary>Sonnet handoff notes</summary>

**Sonnet handoff:**

- **Pattern coordinates:** the existing guard at `InterfaceExtractionService.cs:67-68` (`alreadyImplements = typeSymbol.AllInterfaces.Any(...)`) and the `if (!alreadyImplements)` block at lines 71-117 are the shape to mirror. The document-add block at lines 133-140 plus the `EnsureOpeningBraceOnOwnLine` call at lines 121-124 plus the `replaceUsages` path at lines 142-146 ALL must be moved inside `if (!alreadyImplements)` — risk #2 in the stanza calls out `replaceUsages` explicitly.
- **Test target:** add new `[TestMethod]` to `ExtractInterfaceSemanticUsingsTests.cs` (existing class at line 22); mirror the fixture setup of `ExtractInterface_Emits_Using_For_Parameter_Type_Namespace` at line 37 (sibling-namespace model + service pattern). Do NOT create a new test file. Pre-seed the test class to already implement `IFoo` defined in a sibling-namespace file, then assert `previewDto.Changes.Count == 0` and `previewDto.Warnings` contains the no-op note. Do NOT call `ApplyRefactoringAsync` — assert on the preview DTO directly.
- **Note-surfacing mechanism:** the `RefactoringPreviewDto` has no `description` mutation channel — surface the no-op via the `Warnings` parameter (4th ctor arg). Pattern: see `DeadCodeService.cs:77` (`warnings.Count > 0 ? warnings : null`). Do NOT change the DTO shape.
- **Negative space:** do NOT touch `ExtractAndWireOrchestrator.cs:158-173` — that guard THROWS by design (and is already test-covered at `OrchestrationIntegrationTests.cs:368`). This initiative converts `InterfaceExtractionService` to a soft no-op, not a throw. Also do NOT modify `ValidateNoConflictsAsync` body (lines 495-523) — only its call-site ordering at line 54; the conflict check itself is correct.
- **Edge case — namespace parity (risk #1):** the guard at line 67 matches on simple name only (`i.Name`). For the no-op path that's fine (it correctly catches the gh #748 scenario where `IFoo` exists in any namespace). When moving `ValidateNoConflictsAsync` to after the `alreadyImplements` check, short-circuit ONLY when `alreadyImplements==true` — do not skip the conflict check otherwise. The existing `Validate` uses fully-qualified-name lookup (line 510), so the "same-name different-namespace" case still surfaces when the type does NOT already implement.

</details>

### 4. change-type-namespace-preview-omits-consumer-using-additions

| Field | Content |
|---|---|
| Status | pending |
| Backlog rows closed | `change-type-namespace-preview-omits-consumer-using-additions` |
| Source | gh #749 |
| Diagnosis | Root cause at `src/RoslynMcp.Roslyn/Services/NamespaceRelocationService.cs:591-594`. `IsAmbientTo(consumerNamespace, targetNamespace)` contains a third condition — `targetNamespace.StartsWith(consumerNamespace + ".", StringComparison.Ordinal)` — that returns `true` when the consumer's namespace is an **ancestor** of the target namespace (e.g., consumer in `A.B`, target namespace `A.B.C`). This is incorrect C# ambient-resolution semantics: ambient lookup only climbs (ancestors), not descends. A file in `A.B` cannot resolve `A.B.C` symbols without `using A.B.C;`. The mis-classification causes `AdjustUsings` (line 525) to skip the `using toNamespace;` addition for any consumer whose namespace is an ancestor of `toNamespace`. **Anchor stale:** backlog row cites sibling-repo `NxosVersionParser.cs`; real anchor `NamespaceRelocationService.cs:591-594` + `tests/RoslynMcp.Tests/NamespaceRelocationTests.cs:33`. The `consumerCoversOldByAmbient` path uses the same function: returning `true` there is at least safe (keeps the old using); damage is asymmetric — only new-using omission is the correctness bug. |
| Approach | Fix `IsAmbientTo` in `NamespaceRelocationService.cs`. Remove lines 591-594 (the "consumer is ancestor of target = ambient" branch — wrong for C#). Retain: (1) exact-match branch, (2) `consumerNamespace.StartsWith(targetNamespace + ".")` — consumer is descendant, IS ambient. Update XML doc comment to remove the incorrect "we also treat descendant namespaces as ambient" claim. Extend `tests/RoslynMcp.Tests/NamespaceRelocationTests.cs` with one regression test method: consumer in `A.B` referencing TypeX moved from `A.B.Sub` to `A.B.Child`; assert preview contains `+using A.B.Child;` on consumer file. |
| Scope | Production files: 1 (`NamespaceRelocationService.cs`). Test files: 1 (extend `NamespaceRelocationTests.cs`). |
| Tool policy | edit-only |
| Estimated context cost | 28000 |
| Risks | (1) `consumerCoversOldByAmbient` also calls `IsAmbientTo` — removing third condition makes it drop old usings on ancestor-consumer cases. Actually MORE correct, but verify `Preview_Moves_Type_Up_Namespace_Chain_And_Adjusts_Consumers` still passes. (2) **Anchor stale** (sibling-repo). |
| Validation | (1) New test asserts ancestor-consumer case adds `+using A.B.C;`. (2) All existing `NamespaceRelocationTests` pass. (3) `mcp__roslyn__compile_check` zero errors. (4) `mcp__roslyn__test_run --filter NamespaceRelocationTests`. |
| Performance review | N/A — `IsAmbientTo` is O(1); removing one branch marginally faster. |
| CHANGELOG category | Fixed |
| CHANGELOG entry (draft) | Fixed `change_type_namespace_preview` omitting `using` additions on consumer files whose namespace is an ancestor of the destination namespace (e.g., consumer in `A.B` when type moves to `A.B.C`). The `IsAmbientTo` helper incorrectly treated ancestor namespaces as having ambient access to descendant namespaces, causing the consumer-side using-directive pass to skip the required `using toNamespace;` addition. Closes gh #749. |
| Backlog sync | Close rows: [`change-type-namespace-preview-omits-consumer-using-additions`]. |

### 5. symbol-refactor-preview-empty-appliedfiles-on-success

| Field | Content |
|---|---|
| Status | pending |
| Backlog rows closed | `symbol-refactor-preview-empty-appliedfiles-on-success` |
| Source | gh #750 |
| Diagnosis | Root cause in two cooperating locations. First: `src/RoslynMcp.Roslyn/Services/SymbolRefactorService.cs:200-239` — `BuildCompositeMutationsAsync` calls `finalSolution.GetChanges(initialSolution)` and iterates `GetProjectChanges() → GetChangedDocuments()/GetAddedDocuments()/GetRemovedDocuments()`. When Roslyn's solution-change walk returns no document-set changes for an op (edge case with certain rename targets or restructure outcomes updating only project-level metadata), the returned `IReadOnlyList<CompositeFileMutation>` is empty. Second: `src/RoslynMcp.Roslyn/Services/CompositeApplyOrchestrator.cs:39-70` — `ApplyCompositeAsync` builds `appliedFiles` by appending `mutation.FilePath` inside `foreach (var mutation in mutations)`. When `mutations` is empty the loop never executes, `distinctFiles` is empty, and `return new ApplyResultDto(true, distinctFiles, null)` silently returns success=true with empty `appliedFiles` even though files may have been mutated. The existing test at `tests/RoslynMcp.Tests/SymbolRefactorPreviewTests.cs:218` asserts `Success` but NOT `AppliedFiles.Count > 0`, so the empty-list case passes the suite undetected. |
| Approach | (1) In `src/RoslynMcp.Roslyn/Services/CompositeApplyOrchestrator.cs`: add empty-mutations guard after the `Retrieve` call (line 30). If `mutations.Count == 0`, return `new ApplyResultDto(false, [], "Preview token yielded no file mutations — the composite preview produced no file-level changes. Re-issue the preview with a different operation set.")`. Skip `ReloadAsync` and `Invalidate` so the token remains valid for caller inspection. (2) In `tests/RoslynMcp.Tests/SymbolRefactorPreviewTests.cs`: (a) Extend `RedeemCompositeToken_AppliesAllChainedEdits_AndChangeTrackerHasSingleEntry` to assert `AppliedFiles.Count > 0` after the Success assertion (line 218). (b) Add `SingleRenameOp_ApplyComposite_AppliedFilesIsNonEmpty`: preview single rename, call `ApplyCompositeAsync`, assert `Success == true`, `AppliedFiles.Count >= 1`, expected path appears in `AppliedFiles`. |
| Scope | Production files: 1 (`CompositeApplyOrchestrator.cs`). Test files modified: 1 (`SymbolRefactorPreviewTests.cs`). |
| Tool policy | edit-only |
| Estimated context cost | 28000 |
| Risks | (1) The empty-mutations guard changes behavior: callers receiving `{success:true, appliedFiles:[]}` now receive `{success:false, error:...}`. Breaking change for that edge case, but the prior behavior was silent data corruption — new behavior is strictly more honest. Verify no client pattern-matches on `success=true && appliedFiles.length===0` to detect no-op. (2) `ReloadAsync`+`Invalidate` skipped on empty-mutations path, token remains redeemable — verify callers don't double-call. (3) `CompositeApplyOrchestrator.cs` not a hotspot per addenda. |
| Validation | (1) Extend `RedeemCompositeToken_AppliesAllChainedEdits_AndChangeTrackerHasSingleEntry` with new assertion — passes after fix. (2) New test `SingleRenameOp_ApplyComposite_AppliedFilesIsNonEmpty`: preview rename of `SampleLib.Dog → Puppy`, apply via `ApplyCompositeAsync`, assert success + appliedFiles ≥ 1 + path contains `Dog.cs`. (3) `mcp__roslyn__compile_check`. (4) `dotnet test --filter "SymbolRefactorPreviewTests"`. (5) Build with `TreatWarningsAsErrors=true`. |
| Performance review | N/A — correctness fix; `Count == 0` guard is O(1). |
| CHANGELOG category | Changed — BREAKING |
| CHANGELOG entry (draft) | Fixed `apply_composite_preview` returning `success: true` with an empty `appliedFiles` list when the composite preview produced no file-level mutations (gh #750). The tool now returns an explicit error in this case so callers can distinguish a genuine no-op preview from a successful apply. Breaking: callers previously pattern-matching `success=true && appliedFiles=[]` to detect no-op must now check `success=false`. |
| Backlog sync | Close rows: [`symbol-refactor-preview-empty-appliedfiles-on-success`]. |

### 6. validate-workspace-overallstatus-analyzer-error-with-empty-errordiagnostics

| Field | Content |
|---|---|
| Status | pending |
| Backlog rows closed | `validate-workspace-overallstatus-analyzer-error-with-empty-errordiagnostics` |
| Source | gh #751 |
| Diagnosis | Root cause at `src/RoslynMcp.Core/Services/IWorkspaceValidationService.cs:75-85` and `src/RoslynMcp.Roslyn/Services/WorkspaceValidationService.cs:243`. `WorkspaceValidationDto` is a positional record with `ErrorDiagnostics` (the full per-item list) but no companion `ErrorCount` field. In `ValidateInternalAsync` line 243 the summary-mode branch suppresses the list: `var emittedErrors = summary ? Array.Empty<DiagnosticDto>() : allErrors`. `ComputeOverallStatus` at lines 353-356 correctly classifies the verdict from `allErrors` (always the full set). The mismatch: when `summary=true`, the caller receives `overallStatus=analyzer-error` (computed from N analyzer errors) but `errorDiagnostics: []` (list suppressed) with no count field to explain the discrepancy. `ValidateWorkspaceMarkdownFormatter.cs:75` metrics row reads `dto.ErrorDiagnostics.Count` directly, so the markdown format has the identical gap. **Bundle with #13 REJECTED:** different code paths (`ComputeOverallStatus`/summary branch vs `RunValidationPhaseAsync`/timeout propagation). |
| Approach | (1) In `src/RoslynMcp.Core/Services/IWorkspaceValidationService.cs`: add `int ErrorCount` as a new positional parameter on `WorkspaceValidationDto` (after `ErrorDiagnostics`, before `WarningCount`). (2) In `src/RoslynMcp.Roslyn/Services/WorkspaceValidationService.cs`: in `ValidateInternalAsync` pass `ErrorCount: allErrors.Count`; in `CreateTimeoutResult` pass `ErrorCount: 0`. Mirror the `WarningCount` pattern. (3) In `src/RoslynMcp.Host.Stdio/Formatters/ValidateWorkspaceMarkdownFormatter.cs`: change line 75 from `dto.ErrorDiagnostics.Count` to `dto.ErrorCount`. Extend `tests/RoslynMcp.Tests/WorkspaceValidationOverallStatusTests.cs` with `ComputeOverallStatus_AnalyzerError_SummaryMode_ErrorCountNonZero_ErrorDiagnosticsEmpty` asserting `ErrorCount > 0` + `ErrorDiagnostics.Count == 0` + `OverallStatus == "analyzer-error"`. Update test-construction sites in `ValidateWorkspaceMarkdownFormatterTests.cs` `MakeDto` helper and `ValidationBundleToolsTests.cs` mock services. |
| Scope | Production files: 3 — `IWorkspaceValidationService.cs`, `WorkspaceValidationService.cs`, `ValidateWorkspaceMarkdownFormatter.cs`. Test files modified: 1+ (extend `WorkspaceValidationOverallStatusTests.cs`; update DTO-construction helpers in `ValidateWorkspaceMarkdownFormatterTests.cs` + `ValidationBundleToolsTests.cs`). |
| Tool policy | edit-only |
| Estimated context cost | 30000 |
| Risks | (1) Positional-record field add requires updating every construction site (2 in `WorkspaceValidationService.cs`, 2 in test files). `compile_check` catches misses. (2) Peer initiative #13 also touches `WorkspaceValidationService.cs` at a different code path — no merge conflict expected; if shipped in parallel, executor rebases cleanly. |
| Validation | (1) `mcp__roslyn__compile_check` zero errors. (2) New test passes: `OverallStatus=="analyzer-error"`, `ErrorCount==1`, `ErrorDiagnostics.Count==0`. (3) `ValidateWorkspaceMarkdownFormatterTests` regression — metrics row renders `ErrorCount`. (4) Manual repro returns `errorCount > 0` + `errorDiagnostics: []` + `overallStatus: "analyzer-error"`. |
| Performance review | N/A — correctness fix; `allErrors.Count` is O(1). |
| CHANGELOG category | Fixed |
| CHANGELOG entry (draft) | Fixed `validate_workspace` / `validate_recent_git_changes` returning `overallStatus=analyzer-error` with empty `errorDiagnostics` and no count when `summary=true`. A new `errorCount` field on the response DTO is now always populated (mirrors the existing `warningCount` pattern) so callers in summary mode can see how many errors drove the verdict without needing the full per-item list. Closes gh #751. |
| Backlog sync | Close rows: [`validate-workspace-overallstatus-analyzer-error-with-empty-errordiagnostics`]. |

### 7. test-run-fqdn-drift-vs-test-discover

| Field | Content |
|---|---|
| Status | pending |
| Backlog rows closed | `test-run-fqdn-drift-vs-test-discover` |
| Source | gh #752 |
| Diagnosis | Root cause confirmed at `src/RoslynMcp.Roslyn/Services/TestDiscoveryService.cs:76`. The FQDN is synthesized as `$"{project.Name}.{containingType}.{method.Identifier.Text}"` — it prefixes with the MSBuild project name rather than the test class's actual declared namespace. When a test class lives in a folder-scoped sub-namespace (e.g. `SampleLib.Tests.Unit.AnimalServiceTests`) but the project name is `SampleLib.Tests`, the emitted FQDN is `SampleLib.Tests.AnimalServiceTests.CountAnimals` while xunit/MSTest registers the test under `SampleLib.Tests.Unit.AnimalServiceTests.CountAnimals`. Passing a `FullyQualifiedName~<discover-fqdn>` filter to `dotnet test --filter` produces zero matches — silent zero-hits. The namespace infix (`Unit`) is present in the syntax tree but the current code never inspects it. The `SampleLib.Tests` fixture in `SampleLib.Tests/AnimalServiceTests.cs` masks this because project name and namespace happen to be identical there. The `SynthesizeDotnetTestFilter` helper at line 164 is otherwise correct — bug is upstream in FQDN construction. |
| Approach | Modify `DiscoverTestsAsync` in `src/RoslynMcp.Roslyn/Services/TestDiscoveryService.cs`. In the inner loop walking `MethodDeclarationSyntax` nodes (lines 61-79), extract the actual namespace from the syntax tree by walking ancestors of the containing `ClassDeclarationSyntax` to find the nearest `NamespaceDeclarationSyntax` or `FileScopedNamespaceDeclarationSyntax`. Compose the FQDN as `{namespace}.{containingType}.{methodName}` when found, falling back to `{project.Name}.{containingType}.{methodName}` when no enclosing namespace is present. Extract a small `GetEnclosingNamespace` private static helper. Add `tests/RoslynMcp.Tests/TestDiscoverFqdnDriftTests.cs` (new) using an in-memory Roslyn compilation fixture with a test class declared inside a sub-namespace; assert (a) FQDN uses sub-namespace prefix, (b) synthesized `--filter` matches what `dotnet test` would accept. |
| Scope | Production files: 1 (`TestDiscoveryService.cs`). Test files: 1 NEW (`TestDiscoverFqdnDriftTests.cs`). |
| Tool policy | edit-only |
| Estimated context cost | 28000 |
| Risks | (1) Tests in global namespace (no enclosing decl) must fall back to `project.Name` — handle null-namespace case. (2) Nested namespace declarations (rare): walker should concatenate, not pick innermost. (3) Existing `ValidationIntegrationTests.Test_Discover_Returns_Test_Methods_For_Test_Project` asserts `DisplayName` only — verify still passes. (4) `SynthesizeDotnetTestFilter` called from 3 sites (`FindRelatedTestsAsync`, `FindRelatedTestsForFilesAsync`, `ValidationTools.DiscoverTests`); `test_related` filters become more accurate as side-effect (desirable). |
| Validation | (1) New `TestDiscoverFqdnDriftTests`: class inside `namespace MyProject.SubFolder` produces `FullyQualifiedName == "MyProject.SubFolder.MyTestClass.MyTestMethod"`. (2) Global-namespace fallback: starts with `project.Name`. (3) Existing `ValidationIntegrationTests` pass. (4) `mcp__roslyn__compile_check`. (5) `dotnet test --filter "ClassName=TestDiscoverFqdnDriftTests"`. |
| Performance review | N/A — ancestor walk is shallow. |
| CHANGELOG category | Fixed |
| CHANGELOG entry (draft) | Fixed `test_discover` reporting incorrect `fullyQualifiedName` for test classes declared inside a sub-namespace (folder-infix drift). Previously the FQDN used the MSBuild project name as its namespace prefix; it now uses the class's actual declared namespace. Passing a `test_discover` FQDN as a `test_run --filter` expression will no longer produce silent zero-hits when the project name and namespace diverge. Closes gh #752. |
| Backlog sync | Close rows: [`test-run-fqdn-drift-vs-test-discover`]. |

### 8. migrate-package-preview-misses-analyzer-only-references

| Field | Content |
|---|---|
| Status | pending |
| Backlog rows closed | `migrate-package-preview-misses-analyzer-only-references` |
| Source | gh #753 |
| Diagnosis | Root cause in `src/RoslynMcp.Roslyn/Services/PackageMigrationOrchestrator.cs`, `BuildPackageReferenceEditAsync` (lines 85-112). The LINQ filter at line 77 correctly locates `PackageReference` elements by `Include` attribute — including analyzer-only references. However, the replacement element at lines 96-100 is always built as a bare `new XElement("PackageReference", new XAttribute("Include", newPackageId))` with an optional `Version` attribute. Child-element metadata on the original reference — `<PrivateAssets>all</PrivateAssets>`, `<IncludeAssets>...analyzers...</IncludeAssets>`, `<ExcludeAssets>runtime</ExcludeAssets>`, `<VersionOverride>` — is silently discarded. After apply, an analyzer-only package that should carry `PrivateAssets=all` is rewritten as a full runtime dependency; asset-isolation semantics lost. Existing test helper `InjectPackageReference` (line 413 of `OrchestrationIntegrationTests.cs`) only injects bare elements, so no existing test covers this path. |
| Approach | Fix `BuildPackageReferenceEditAsync` in `PackageMigrationOrchestrator.cs`: after finding each matching `packageReference` element (line 83), collect its child `XElement` nodes (`PrivateAssets`, `IncludeAssets`, `ExcludeAssets`, `VersionOverride`, etc.) AND any non-`Include`/non-`Version` attributes (attribute-form metadata like `ExcludeAssets="runtime"`). When building the replacement at line 97, copy those metadata children and attributes onto the new `XElement` before adding it to the `ItemGroup`. If the non-CPM path adds a `Version` attribute, only do so when the original did not carry a `VersionOverride` child element. Add `Migrate_Package_Preview_Preserves_Analyzer_Only_Metadata_On_Replacement` in `tests/RoslynMcp.Tests/OrchestrationIntegrationTests.cs` using a new `InjectAnalyzerOnlyPackageReference` helper (mirrors existing but appends `<PrivateAssets>all</PrivateAssets>` + `<IncludeAssets>...analyzers...</IncludeAssets>` child elements). Assert replacement carries child metadata intact + no bare `Version` for CPM-managed analyzer reference. |
| Scope | Production files: 1 (`PackageMigrationOrchestrator.cs`). Test files: 1 (extend `OrchestrationIntegrationTests.cs`). |
| Tool policy | edit-only |
| Estimated context cost | 30000 |
| Risks | (1) Distinguish child-element metadata (`<PrivateAssets>` as `XElement`) from attribute-form (`PrivateAssets="all"` as `XAttribute`); copy both. (2) CPM scenario: replacement should not get `Version` attribute (lines 98-100 already guard), but `<VersionOverride>` child should be preserved if original had it. (3) New test injects XML before `workspace.LoadAsync()` — same pattern as FORMAT_BUG_003 test at line 62. (4) Existing test at line 21 covers non-analyzer path; new test is additive. |
| Validation | (a) New test passes — asserts `PrivateAssets` + `IncludeAssets` children present on replacement after preview+apply. (b) All existing `OrchestrationIntegrationTests` pass. (c) `mcp__roslyn__compile_check`. (d) `dotnet test --filter "FullyQualifiedName~OrchestrationIntegrationTests"`. |
| Performance review | N/A — correctness fix. |
| CHANGELOG category | Fixed |
| CHANGELOG entry (draft) | `migrate_package_preview` now preserves asset-isolation metadata (`PrivateAssets`, `IncludeAssets`, `ExcludeAssets`, `VersionOverride`) from analyzer-only `PackageReference` entries onto the replacement element; previously these child-element and attribute-form constraints were silently stripped, causing analyzer packages to be rewritten as full runtime dependencies after apply. Fixes gh #753. |
| Backlog sync | Close rows: [`migrate-package-preview-misses-analyzer-only-references`]. |

### 9. find-overrides-payload-overflow-on-corlib-virtual

| Field | Content |
|---|---|
| Status | pending |
| Backlog rows closed | `find-overrides-payload-overflow-on-corlib-virtual` |
| Source | gh #754 |
| Diagnosis | Root cause in `src/RoslynMcp.Roslyn/Services/ReferenceService.cs:164`. `FindOverridesAsync` calls `PromoteToVirtualRoot(symbol)` at line 156 which correctly walks the override chain up to the virtual declaration site. When the user anchors on a type like `MyClass.ToString` or supplies `metadataName="System.Object.ToString"`, `PromoteToVirtualRoot` returns `System.Object.ToString` (a corlib virtual). The subsequent `SymbolFinder.FindOverridesAsync(promoted, solution)` at line 164 enumerates every class in the entire solution that overrides `ToString`/`Equals`/`GetHashCode` — potentially hundreds — producing unbounded payload. The same pattern has a precedent fix in `src/RoslynMcp.Roslyn/Services/SymbolRelationshipService.cs:165` (PR #815, shipped), where `GetSymbolRelationshipsAsync` guards `SpecialType != SpecialType.None` and returns suppressed empty envelope with `Hint`. The corlib guard is absent from `ReferenceService.FindOverridesAsync`, leaving `find_overrides` AND `member_hierarchy` (which delegates to it via `SymbolRelationshipService.cs:133`) unprotected. The fix in `ReferenceService` also closes the overflow for `member_hierarchy.overrides`. |
| Approach | (1) In `ReferenceService.cs`, after `PromoteToVirtualRoot` (line 156), add corlib guard: if `promoted` is `IMethodSymbol`/`IPropertySymbol`/`IEventSymbol` whose `ContainingType` has `SpecialType != SpecialType.None`, return empty list immediately. Mirror the comment convention from `SymbolRelationshipService.cs:157-174`. (2) In `src/RoslynMcp.Host.Stdio/Tools/SymbolTools.cs`, update the `find_overrides` tool description to document new behavior. Include a `hint` field in serialized response when suppression fires (`new { count = 0, items = [], hint = "..." }` vs normal `{ count, items }`). Tool wrapper checks if the promoted symbol is corlib before calling service, emits hint accordingly. Keeps `IReferenceService` signature unchanged. New `tests/RoslynMcp.Tests/FindOverridesCorlibSuppressionTests.cs` verifies (a) `FindOverridesAsync` with `metadataName="System.Object.ToString"` returns count=0, (b) same for `Equals` + `GetHashCode`, (c) source-positional anchor at a method overriding `ToString` in `SampleLib` still returns correct non-empty chain (verifies suppression only fires after promotion). |
| Scope | Production files: 2 (`ReferenceService.cs`, `SymbolTools.cs`). Test files: 1 NEW (`FindOverridesCorlibSuppressionTests.cs`). |
| Tool policy | edit-only |
| Estimated context cost | 28000 |
| Risks | (1) `member_hierarchy.overrides` silently protected by `ReferenceService.FindOverridesAsync` fix — verify `MemberHierarchyCrossToolConsistencyTests` still pass (don't use corlib virtuals, no regression risk). (2) A caller legitimately anchored on `System.Object.ToString` via `metadataName` now receives `count=0` instead of large list — correct behavior; hint field must clearly communicate. (3) Corlib check uses `ContainingType.SpecialType != SpecialType.None` — `System.Object`'s SpecialType is `System_Object`; covers all three problematic virtuals. (4) `SymbolRelationshipsDto` has `Hint`; `MemberHierarchyDto` does not. `member_hierarchy` returns empty `overrides: []` without hint for corlib — acceptable; adding `Hint` to `MemberHierarchyDto` would touch Core model, follow-on. |
| Validation | (1) New `FindOverridesCorlibSuppressionTests` passes for all three corlib virtuals. (2) Source-positional anchor at SampleLib `ToString` override still returns non-empty chain. (3) Existing `MemberHierarchyCrossToolConsistencyTests` pass unmodified. (4) `mcp__roslyn__compile_check`. (5) `mcp__roslyn__test_run --filter "FindOverridesCorlibSuppression|MemberHierarchyCrossToolConsistency"`. |
| Performance review | Strictly additive: one `SpecialType` property check before the expensive `SymbolFinder.FindOverridesAsync` enumeration. Significant performance win on corlib-virtual path. |
| CHANGELOG category | Fixed |
| CHANGELOG entry (draft) | Fixed `find_overrides` returning an unbounded payload when anchored on corlib virtual members (`System.Object.ToString`, `Equals`, `GetHashCode`). The tool now returns `count=0` with an explanatory hint for corlib virtuals, matching the existing `symbol_relationships` suppression behavior. `member_hierarchy.overrides` benefits from the same guard via its shared FindOverridesAsync code path (gh #754). |
| Backlog sync | Close rows: [`find-overrides-payload-overflow-on-corlib-virtual`]. |

### 10. analyze-dependencies-prompt-payload-overflow

| Field | Content |
|---|---|
| Status | pending |
| Backlog rows closed | `analyze-dependencies-prompt-payload-overflow` |
| Source | gh #755 |
| Diagnosis | Root cause in `AnalyzeDependencies` at `src/RoslynMcp.Host.Stdio/Prompts/RoslynPrompts.cs:252-303`. The method builds three JSON blocks and concatenates them: (1) project graph capped at 50 nodes via `SerializeTruncatedList`, (2) namespace dependency graph where nodes/edges are each capped at 100 via `with` expression but `CircularDependencies` is NOT capped at all, (3) NuGet packages capped at 50 via `SerializeTruncatedList`. On a 9-project workspace with heavily-namespaced code, the namespace block alone produces 15-25 KB (namespace strings 60-100 chars each); combined with project graph + NuGet entries (with `UsedByProjects` lists), aggregate prompt body reaches 63 KB — beyond MCP inline payload cap. The `guided_extract_interface` prompt hit the same class of overflow before PR #790 (gh #776) fixed it by lowering caps + adding regression test in `PromptSmokeTests.cs`. No equivalent guard exists for `analyze_dependencies`. **Bundle with #11 REJECTED:** different methods in different files; conditions 1+2 fail. |
| Approach | (1) In `RoslynPrompts.cs` `AnalyzeDependencies`, lower namespace-node and namespace-edge caps from 100 to 50; add `CircularDependencies` cap (e.g. 20). Add count footer for each truncated list. (2) Mirror the pattern from `GuidedExtractInterface` at `src/RoslynMcp.Host.Stdio/Prompts/RoslynPrompts.RefactoringWorkflows.cs:263-269` (`SerializeTruncatedList` with explicit max + footer). (3) In `tests/RoslynMcp.Tests/PromptSmokeTests.cs`, add `AnalyzeDependencies_LargeWorkspace_PromptFitsInlineCap` following the stub-service pattern from `GuidedExtractInterface_LargeWorkspace_PromptFitsInlineCap` at line 310. Stub wires `IWorkspaceManager` (large project graph), `INamespaceDependencyService` (many nodes/edges), `INuGetDependencyService` (many packages). Assert rendered prompt body < 25000 chars and each truncation footer present. |
| Scope | Production files: 1 (`RoslynPrompts.cs`). Test files: 1 (extend `PromptSmokeTests.cs`). |
| Tool policy | edit-only |
| Estimated context cost | 30000 |
| Risks | (1) Lowering caps means fewer namespaces surfaced — review whether 50+50 is sufficient context; add user-visible note in prompt body directing callers to `get_namespace_dependencies` for full detail. (2) `CircularDependencies` cycles are high-value signal; capping at 20 should retain all meaningful cases. (3) New stubs for `INuGetDependencyService` + `INamespaceDependencyService` don't yet exist — implementing adds test file length but is mechanical. (4) `review_test_coverage` (peer #11) has separate 121 KB overflow from test discovery data — separate initiative. |
| Validation | (1) New test stub workspace: 60 projects, 120 namespace nodes/edges, 20 circular cycles, 60 NuGet packages — rendered prompt < 25000 chars with truncation footers. (2) `mcp__roslyn__compile_check`. (3) `mcp__roslyn__test_run --filter "PromptSmokeTests"`. (4) Manual inspection of rendered prompt body confirms all three JSON blocks + correct `[Showing N of M items]` footers. |
| Performance review | N/A — correctness fix. |
| CHANGELOG category | Fixed |
| CHANGELOG entry (draft) | Fixed `analyze_dependencies` prompt payload overflow: namespace-dependency nodes and edges are now capped at 50 each (down from 100) and circular-dependency cycles are capped at 20, preventing the 63 KB inline-payload overflow observed on 9-project workspaces. Closes gh #755. |
| Backlog sync | Close rows: [`analyze-dependencies-prompt-payload-overflow`]. |

### 11. review-test-coverage-prompt-payload-overflow

| Field | Content |
|---|---|
| Status | pending |
| Backlog rows closed | `review-test-coverage-prompt-payload-overflow` |
| Source | gh #756 |
| Diagnosis | `RoslynPrompts.AnalysisWorkflows.cs:210-261` — `ReviewTestCoverage` fetches `TestDiscoveryDto` via `ITestDiscoveryService.DiscoverTestsAsync` and applies a per-project cap of `200 / project_count` test cases (line 220). For a single-project solution this keeps all 200 cases; each `TestCaseDto` serializes four fields including `FullyQualifiedName` (100-200 chars in large suites), producing 100+ KB of JSON inline in the prompt message. The existing truncation guard at line 224 appends a footer only when `totalTests > 200` but does not reduce the JSON body size. **Bundle with #10 REJECTED:** `AnalyzeDependencies` lives in `RoslynPrompts.cs:245`, `ReviewTestCoverage` in `RoslynPrompts.AnalysisWorkflows.cs:210` — different files and methods; SPLIT confirmed. |
| Approach | In `src/RoslynMcp.Host.Stdio/Prompts/RoslynPrompts.AnalysisWorkflows.cs` modify `ReviewTestCoverage` (lines 219-225): (1) Lower the total-test cap from 200 to 50 (consistent with `SerializeTruncatedList` cap used elsewhere). (2) Replace inline `JsonSerializer.Serialize(new TestDiscoveryDto(truncatedProjects), ...)` with `PromptMessageBuilder.SerializeTruncatedList(truncatedProjects, 50, JsonDefaults.Indented)` — reuses existing helper at `PromptMessageBuilder.cs:20` and auto-appends `[Showing N of M items]` footers. Add guard-comment referencing gh #756 above the cap constant (mirrors `RoslynPrompts.RefactoringWorkflows.cs:263`). In `tests/RoslynMcp.Tests/PromptSmokeTests.cs` add `ReviewTestCoverage_LargeTestSuite_PromptFitsInlineCap` following `GuidedExtractInterface_LargeWorkspace_PromptFitsInlineCap` (lines 310-350): stub `ITestDiscoveryService` returning 300 synthetic test cases across 1 project; assert rendered text < 25000 chars + `[Showing 50 of 300 items]` footer present. |
| Scope | Production files: 1 (`RoslynPrompts.AnalysisWorkflows.cs`). Test files: 1 (extend `PromptSmokeTests.cs` with 1 method + 1 stub class). |
| Tool policy | edit-only |
| Estimated context cost | 28000 |
| Risks | (1) 50-test cap tighter than prior 200 — callers needing full list see fewer inline tests. Acceptable: prompt instructs caller to invoke `test_coverage` directly; discovery section is orientation-only. (2) `perProject` recomputation logic (lines 219-220) should be removed entirely once `SerializeTruncatedList` takes over — no dead code. (3) Adjacent prompt `review_complexity` at line 263 embeds raw `metricsJson = JsonSerializer.Serialize(metrics, ...)` with no cap — potential sibling overflow. Spin-off row if confirmed at executor time; do not bundle here (separate method, no Rule 1 shared code path). |
| Validation | (1) `mcp__roslyn__compile_check` zero errors. (2) New `ReviewTestCoverage_LargeTestSuite_PromptFitsInlineCap` passes. (3) Existing `PromptSmokeTests` no regression. (4) Manual repro: invoke `review_test_coverage` against RoslynMcp.Tests workspace; payload < 30 KB. |
| Performance review | N/A — correctness fix. |
| CHANGELOG category | Fixed |
| CHANGELOG entry (draft) | Fixed `review_test_coverage` prompt payload overflow: the rendered prompt now caps the embedded test-discovery list at 50 entries (previously up to 200) using the existing `SerializeTruncatedList` helper, keeping prompt payloads well under the MCP inline cap for large test suites. Closes gh #756. |
| Backlog sync | Close rows: [`review-test-coverage-prompt-payload-overflow`]. |

### 12. find-property-writes-metadataname-hint-mismatches-input-shape

| Field | Content |
|---|---|
| Status | pending |
| Backlog rows closed | `find-property-writes-metadataname-hint-mismatches-input-shape` |
| Source | gh #758 |
| Diagnosis | Root cause in `src/RoslynMcp.Host.Stdio/Tools/SymbolTools.cs:629-637`, inside the `FindPropertyWrites` tool wrapper. After calling `mutationAnalysisService.FindPropertyWritesWithMetadataAsync`, the code builds a `hint` string via two branches: (1) when `results.Count == 0 && resolvedKind != "Property"` (line 632): `"Position resolved to a {resolvedKind}, not a property..."` — hardcodes "Position" even when caller used `metadataName`. (2) when `results.Count == 0 && resolvedKind is null` (line 636): `"No symbol resolved at the given position. Verify the column points..."` — hardcodes "position" and "column" even when caller passed only `metadataName`. The `SymbolLocator` record (`SymbolLocator.cs:34-45`) exposes `HasSourceLocation`/`HasHandle`/`HasMetadataName` predicates specifically for this branching. `SymbolLocatorFactory.FormatSymbolNotFoundMessage` (`SymbolLocatorFactory.cs:100-125`) demonstrates the correct locator-aware branching for thrown errors; `FindPropertyWrites` applies the same concept for soft hints but ignores locator mode. **Anchor stale:** backlog row cites sibling-repo `src/TradeWise.Domain/Alerts/AlertRule.cs`; real anchor `SymbolTools.cs:629-637`. |
| Approach | Edit `src/RoslynMcp.Host.Stdio/Tools/SymbolTools.cs`. In `FindPropertyWrites` (lines 629-637), replace hardcoded hint strings with locator-aware variants mirroring `SymbolLocatorFactory.FormatSymbolNotFoundMessage` (`SymbolLocatorFactory.cs:104-118`). (a) Non-property-kind branch (line 632): `"Symbol resolved to a {resolvedKind}..."` when `locator.HasSourceLocation`, `"metadataName resolved to a {resolvedKind}..."` when `locator.HasMetadataName`, `"Symbol handle resolved to a {resolvedKind}..."` when `locator.HasHandle`. (b) Null-resolve branch (line 636): `"No symbol resolved at the given position..."` only when `HasSourceLocation`, `"No symbol resolved for the given metadataName..."` when `HasMetadataName`, `"No symbol resolved for the given symbol handle..."` when `HasHandle`. No service-layer changes. Add new test file `tests/RoslynMcp.Tests/FindPropertyWritesHintLocatorShapeTests.cs` with `FindPropertyWrites_MetadataName_NonPropertySymbol_HintNamesMetadataNotPosition` (metadataName → field, assert hint excludes "position"/"column" and includes "metadataName") and `FindPropertyWrites_MetadataName_NoResolution_HintNamesMetadataNotPosition` (bogus metadataName, same invariant). |
| Scope | Production files: 1 (`SymbolTools.cs`). Test files: 1 NEW (`FindPropertyWritesHintLocatorShapeTests.cs`). Rule 3 exemption: tool-surface-only, 1 file. |
| Tool policy | edit-only |
| Estimated context cost | 22000 |
| Risks | (1) Three locator modes (source-location, handle, metadataName) — all three branches must be handled. (2) Existing hint for null-position case is the only safe-to-keep wording; metadataName wording must guide toward checking FQN, not column. (3) `resolvedKind` populated from service regardless of locator mode — fix is purely in hint-string selection. (4) **Anchor stale.** |
| Validation | (1) New tests pass: metadataName + non-property → hint contains "metadataName", not "position"/"column"; metadataName + no resolution → same shape. (2) Existing `FindPropertyWritesPositionalRecordTests` pass (position-mode hint untouched). (3) `mcp__roslyn__compile_check` zero errors. (4) `mcp__roslyn__test_run --filter FindPropertyWritesHintLocatorShapeTests`. (5) Manual repro confirms metadataName hint says "metadataName resolved to a Field" not "Position resolved to a Field". |
| Performance review | N/A — correctness fix. |
| CHANGELOG category | Fixed |
| CHANGELOG entry (draft) | Fixed `find_property_writes` returning position-shaped hint text (`"Position resolved to a Field"`, `"Verify the column points at the symbol identifier"`) when the caller supplied `metadataName` instead of `filePath+line+column`. The `hint` field in the response now reflects the locator mode that was actually used, matching the behavior of other locator-aware error messages in the server. Closes gh #758. |
| Backlog sync | Close rows: [`find-property-writes-metadataname-hint-mismatches-input-shape`]. |

### 13. validate-workspace-25s-internalvalidationtimeoutexception-on-medium-solution

| Field | Content |
|---|---|
| Status | pending |
| Backlog rows closed | `validate-workspace-25s-internalvalidationtimeoutexception-on-medium-solution` |
| Source | gh #759 |
| Diagnosis | Root cause at `src/RoslynMcp.Roslyn/Services/WorkspaceValidationService.cs:72-78`. The public `ValidateAsync` method delegates unconditionally to `ValidateInternalAsync`, which calls `RunValidationPhaseAsync` (line 617-632). When a validation phase exceeds the 25-second internal timeout, `RunValidationPhaseAsync` throws the private nested `InternalValidationTimeoutException` (line 690). `ValidateAsync` has no try/catch for this exception, so it escapes as an unhandled exception and surfaces as a bare MCP error string instead of a graceful `FailureEnvelope`. By contrast, `ValidateRecentGitChangesAsync` (lines 87-127) already catches `InternalValidationTimeoutException` at both code paths (lines 109-111, 123-125) and calls `CreateTimeoutResult` to return a structured DTO with `OverallStatus: "timeout"`. The `validate_workspace` tool calls `validationService.ValidateAsync(...)` and would receive the structured DTO on success — but on timeout the exception propagates through `gate.RunReadAsync` and up to the SDK transport. **Bundle with #6 REJECTED:** peer's bug is in `ComputeOverallStatus` (static classifier), an entirely different code path from the timeout-propagation in `ValidateAsync`. Rule 1 condition 2 fails. |
| Approach | Edit `src/RoslynMcp.Roslyn/Services/WorkspaceValidationService.cs`. In `ValidateAsync` (lines 72-78), wrap the call to `ValidateInternalAsync` in try/catch for `InternalValidationTimeoutException`, mirroring `ValidateRecentGitChangesAsync` (lines 102-112). Catch block calls `CreateTimeoutResult(ex, changedFilePaths ?? Array.Empty<string>(), Array.Empty<string>())`. `OperationCanceledException` must NOT be caught here (must propagate as cooperative cancellation). Also update `validate_workspace` tool description at `ValidationBundleTools.cs:24` to add `"timeout"` to the documented `overallStatus` enum. Add new test file `tests/RoslynMcp.Tests/WorkspaceValidationTimeoutTests.cs`. Use internal `WorkspaceValidationService` constructor that accepts custom `validationPhaseTimeout` (line 52), inject near-zero timeout (`TimeSpan.FromMilliseconds(1)`) + stub service that delays long enough. Assert returned DTO has `OverallStatus == "timeout"`, `CompileResult.Cancelled == true`, `Warnings` containing timeout phase name. |
| Scope | Production files: 2 (`WorkspaceValidationService.cs`, `ValidationBundleTools.cs` description string). Test files: 1 NEW (`WorkspaceValidationTimeoutTests.cs`). |
| Tool policy | edit-only |
| Estimated context cost | 28000 |
| Risks | (1) `CreateTimeoutResult` currently only populates `ChangedFilePaths`; when called from `ValidateAsync`, caller-supplied `changedFilePaths` may be null — pass `?? Array.Empty<string>()`. (2) Tool description string change is a 2nd production file (small). (3) Internal-constructor test requires stub services to introduce delay via `Task.Delay(100, ct)` so the 1ms timeout fires reliably. (4) Peer #6 SPLIT — different code paths; ships independently. |
| Validation | (1) New `ValidateAsync_WhenPhaseExceedsInternalTimeout_ReturnsTimeoutDto` passes: `OverallStatus=="timeout"`, `CompileResult.Cancelled==true`, `Warnings.Count > 0`. (2) `validate_workspace` description includes `"timeout"` in enum. (3) `mcp__roslyn__compile_check` zero errors. (4) `mcp__roslyn__test_run --filter WorkspaceValidationTimeoutTests`. (5) Existing `WorkspaceValidationOverallStatusTests` + `ValidationBundleToolsTests` pass. |
| Performance review | N/A — correctness fix. |
| CHANGELOG category | Fixed |
| CHANGELOG entry (draft) | Fixed `validate_workspace` throwing an unhandled `InternalValidationTimeoutException` after 25 seconds on large solutions (11 projects / 759 documents) instead of returning a structured failure envelope. The tool now returns `overallStatus: "timeout"` with `compileResult.cancelled: true` and a `warnings` entry naming the timed-out phase, matching the graceful-timeout behavior already present in `validate_recent_git_changes`. Closes gh #759. |
| Backlog sync | Close rows: [`validate-workspace-25s-internalvalidationtimeoutexception-on-medium-solution`]. |

### 14. workspace-status-verbose-5s-timeout-race-on-ready-workspace

| Field | Content |
|---|---|
| Status | pending |
| Backlog rows closed | `workspace-status-verbose-5s-timeout-race-on-ready-workspace` |
| Source | gh #761 |
| Diagnosis | `WorkspaceManager.GetStatus` (line 432) and `GetStatusAsync` (line 455) both acquire `session.LoadLock` (a `SemaphoreSlim(1,1)`) with a 5-second hard timeout before calling `BuildStatus`. `LoadIntoSessionAsync` (line 1146) also acquires `LoadLock` at line 1152 and holds it for the entire MSBuild workspace load — which can run 30+ seconds. When `workspace_status(verbose=true)` is called on a workspace that just returned `isReady=true` from `workspace_status(verbose=false)` moments earlier, the prior call may have released `LoadLock` but an in-progress background reload triggered by staleness auto-reload (line 376 in `WorkspaceExecutionGate.cs`) is still holding it. The 5s `WaitAsync` inside `GetStatusAsync` then times out and throws `TimeoutException`. The resource sibling `roslyn://workspace/{id}/status/verbose` (`WorkspaceResources.cs:60-65`) takes the identical code path. `BuildStatus` only reads `session.ProjectStatuses` (`ImmutableArray`, set atomically), `session.WorkspaceDiagnostics` (`ConcurrentQueue`, concurrent-read safe), and simple scalar reads (`LoadedPath`, `Version`, `LoadedAtUtc`, `RestoreRequired`). None require holding `LoadLock`; the lock was added defensively but provides no safety the field types don't already guarantee. |
| Approach | Modify `src/RoslynMcp.Roslyn/Services/WorkspaceManager.cs`: (1) In `GetStatus` (line 432), remove `session.LoadLock.Wait(TimeSpan.FromSeconds(5))`, its timeout-throw, the `try`/`finally`, and the `Release()` — reduce to direct `return BuildStatus(session)` after `GetRequiredSession`. (2) In `GetStatusAsync` (line 455), same reduction — remove `LoadLock.WaitAsync`, timeout throw, try/finally, release — reduce to `return BuildStatus(session)` (wrap in `Task.FromResult` or make sync). Add comment on each method explaining `BuildStatus` reads only immutable/thread-safe-concurrent fields and outer `gate.RunReadAsync` already serializes against writes. No other files. `BuildStatus` itself not modified. Add `tests/RoslynMcp.Tests/WorkspaceToolsIntegrationTests.cs` test extending the existing class with a method that simulates concurrent load-in-progress + asserts `workspace_status(verbose=true)` completes in < 500 ms rather than timing out. |
| Scope | Production files: 1 (`WorkspaceManager.cs`). Test files: 1 (extend `WorkspaceToolsIntegrationTests.cs`). |
| Tool policy | edit-only |
| Estimated context cost | 28000 |
| Risks | (1) Thread safety of `BuildStatus` without `LoadLock`: confirmed safe — `ProjectStatuses` is `ImmutableArray` (assigned atomically), `WorkspaceDiagnostics` is `ConcurrentQueue`, scalar fields are simple ref/value reads. Reader catching partially-replaced `ProjectStatuses` gets old immutable array — valid snapshot, same eventual-consistency the system already relies on. (2) `GetStatus` sync overload called only in `WorkspaceTools.cs:130` (close drain path) — safe. (3) Callers previously relying on blocking until load completed will no longer block — intended behavior change; that's what `isReady` and `workspace_load`'s own response are for. (4) Fanout probe: 12 callers of `GetStatusAsync` across 12 files; none changed — fix is internal. |
| Validation | (1) Test in `WorkspaceToolsIntegrationTests.cs`: concurrently hold `session.LoadLock` (via simulated slow-load mock) and call `WorkspaceTools.GetWorkspaceStatus(verbose: true)`; assert completes ≪ 1s without `TimeoutException`. (2) `WorkspaceTools_GetWorkspaceStatus_Verbose_Includes_Projects` still passes. (3) `dotnet build RoslynMcp.slnx -c Release -p:TreatWarningsAsErrors=true` succeeds. (4) `mcp__roslyn__compile_check` zero errors. (5) Manual: load TradeWise.sln, call `workspace_status(verbose=false)` → `isReady=true`, immediately call `workspace_status(verbose=true)` → returns in ms without 5s stall. |
| Performance review | This IS a hot-path fix — `workspace_status` is the first cheap health check after every `workspace_load`. Removing unnecessary `SemaphoreSlim.WaitAsync` eliminates the 5-second timeout race; baseline verbose-path latency drops from up to 5005 ms to < 1 ms `BuildStatus` compute time. No new allocations. |
| CHANGELOG category | Fixed |
| CHANGELOG entry (draft) | Fixed `workspace_status(verbose=true)` timing out at 5 s on a ready workspace when a concurrent or recently-completed load held `session.LoadLock`. `GetStatus` and `GetStatusAsync` no longer acquire `LoadLock`; `BuildStatus` reads only thread-safe fields (`ImmutableArray`, `ConcurrentQueue`, scalar references) that do not require the lock, and the outer `gate.RunReadAsync` already serializes against concurrent writes. Fixes gh #761. |
| Backlog sync | Close rows: [`workspace-status-verbose-5s-timeout-race-on-ready-workspace`]. |

### 15. source-file-resource-requires-url-encoded-absolute-path-undocumented

| Field | Content |
|---|---|
| Status | pending |
| Backlog rows closed | `source-file-resource-requires-url-encoded-absolute-path-undocumented` |
| Source | gh #762 |
| Diagnosis | The doc gap is confirmed but narrower than the backlog row implies. `GetSourceFile` (`source_file` resource, `WorkspaceResources.cs:162`) already carries accurate description: "filePath accepts URL-encoded form (recommended) or raw absolute path; both normalized server-side." `NormalizeFilePathForResource` at `WorkspaceResources.cs:205-211` applies `Uri.UnescapeDataString` (idempotent) + separator normalization to both resources. However the sibling `GetSourceFileLines` method (`WorkspaceResources.cs:228`) still says "filePath must be URL-encoded" in its `[Description]` attribute and its `filePath` parameter description (line 233) also says "URL-encoded" only — implying raw paths are rejected, which is false. Both resources share the same normalizer. `WindowsPathResourceTests` covers all encoding shapes for `GetSourceFile` but its single `source_file_lines` test only exercises encoded form. **Anchor stale:** backlog row cites sibling-repo `src/TradeWise.Domain/Alerts/AlertRuleType.cs`; real anchor `WorkspaceResources.cs:228+233`. |
| Approach | (1) In `src/RoslynMcp.Host.Stdio/Resources/WorkspaceResources.cs`: update `[Description]` on `GetSourceFileLines` (line 228) to mirror `GetSourceFile` — replace "filePath must be URL-encoded" with "URL-encoded form (recommended) or raw absolute path; both normalized server-side". Update `filePath` parameter `[Description]` on `GetSourceFileLines` (line 233) to match line 167. No logic changes; `NormalizeFilePathForResource` already shared. (2) In `tests/RoslynMcp.Tests/WindowsPathResourceTests.cs`: extend `GetSourceFileLines_AcceptsEncodedWindowsPath` test class with three additional methods mirroring `source_file` suite — `GetSourceFileLines_UnencodedWindowsPath_Resolves`, `GetSourceFileLines_ForwardSlashOnly_Resolves`, `GetSourceFileLines_EncodedSeparatorsOnly_Resolves`. Mirror pattern from `GetSourceFile_*` tests at lines 34-97. |
| Scope | Production files: 1 (`WorkspaceResources.cs`). Test files: 1 (extend `WindowsPathResourceTests.cs`). Rule 3 exemption: tool-surface-only description-text-on-already-registered-resource, 1 file. |
| Tool policy | edit-only |
| Estimated context cost | 22000 |
| Risks | Description update is purely additive/clarifying — no logic change. Only risk: phrasing inconsistency if future maintainers update one description and not the other; new tests mitigate by locking parity. **Anchor stale.** Executor uses `AnimalService.cs` fixture path (established in `WindowsPathResourceTests`) as regression vehicle. |
| Validation | (1) `mcp__roslyn__compile_check` after editing — confirms no typo broke `[Description]` attribute syntax. (2) `mcp__roslyn__test_run --filter "WindowsPathResourceTests"` — existing tests pass, three new `GetSourceFileLines_*` pass. (3) Updated descriptions appear in `roslyn://server/resource-templates` output. (4) `verify-release.ps1` as final gate. |
| Performance review | N/A — correctness fix. |
| CHANGELOG category | Fixed |
| CHANGELOG entry (draft) | Fixed `source_file_lines` resource description incorrectly stating filePath "must be URL-encoded" — both URL-encoded and raw absolute paths have always been accepted by the shared normalizer. Description now matches `source_file`'s accurate phrasing. Closes gh #762. |
| Backlog sync | Close rows: [`source-file-resource-requires-url-encoded-absolute-path-undocumented`]. |
