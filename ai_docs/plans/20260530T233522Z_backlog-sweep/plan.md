# Backlog sweep plan — 20260530T233522Z
<!-- scope: in-repo -->
<!-- artifact: plan -->
<!-- status: active -->

**Generated:** 2026-05-30T23:35:22Z
**Backlog snapshot:** 2026-05-30T23:10:00Z
**Initiative count:** 2
**Anchor verification:** pending (deepener fan-out)

<!-- BSWEEP:STATUS-TABLE BEGIN — generated from state.json; do not edit by hand -->
## Status (generated)

| # | id | status | PR | rows closed |
|---|----|--------|----|-------------|
| 1 | compilation-cache-adoption-read-side | merged | [#936](https://github.com/darylmcd/Roslyn-Backed-MCP/pull/936) | compilation-cache-adoption-read-side |
| 2 | promotion-scorecard-execution-batch | merged | [#937](https://github.com/darylmcd/Roslyn-Backed-MCP/pull/937) | promotion-scorecard-execution-batch |
<!-- BSWEEP:STATUS-TABLE END -->

## Initiatives (in order)

### 1. compilation-cache-adoption-read-side

| Field | Content |
|---|---|
| Diagnosis | Group (a) static-helper sites still call `project.GetCompilationAsync` directly, bypassing the singleton `ICompilationCache`. Three production files are the cleanest next batch: (1) `TypeConsumersService` — raw call at line 108 inside private static `ResolveTypeAsync(Solution solution, string typeName, CancellationToken ct)`; the `workspaceId` is in scope in the public method `FindTypeConsumersAsync` at line 30 but not threaded down to the static helper. (2) `CodePatternAnalyzer` — two raw calls: line 34 inside `FindReflectionUsagesAsync` (instance method, `workspaceId` in scope) and line 229 inside private static `CollectSemanticSearchMatchesAsync(Solution, IEnumerable<Project>, …)` (called from `SemanticSearchAsync` where `workspaceId` is in scope). (3) `SymbolSearchService` — raw call at line 117 inside private static `RunFqnSubstringFallbackAsync(Solution, …)`; `workspaceId` is in scope in `SearchSymbolsAsync`. All three hold live `_workspace.GetCurrentSolution(workspaceId)` solutions — none are forked. Confirmed: `ICompilationCache` is registered singleton in `src/RoslynMcp.Roslyn/ServiceCollectionExtensions.cs:23`. Batch-1 adopters (`CouplingAnalysisService`, `ExceptionFlowService`, `AnalyzerInfoService`) establish the precedent: add `ICompilationCache _compilationCache` field, inject via constructor, replace `project.GetCompilationAsync(ct)` with `_compilationCache.GetCompilationAsync(workspaceId, project, ct)`, threading `workspaceId` into any static helper that currently lacks it. Group (c) HAZARD sites (`SymbolResolver:285`, `SymbolResolver:125`, `SymbolHandleSerializer:204`) are confirmed forked-solution callers per the backlog description and are excluded from this batch. `TestReferenceMapService` (2 sites at lines 150 and 178 in static helpers) and `ReferenceService:231` (static helper lacks `workspaceId`) are deferred to the next batch for orderly threading. |
| Approach | 1. `src/RoslynMcp.Roslyn/Services/TypeConsumersService.cs` — add `ICompilationCache _compilationCache` field; inject via constructor (alongside existing `IWorkspaceManager workspace`). Add `workspaceId` parameter to private static `ResolveTypeAsync` (currently `ResolveTypeAsync(Solution solution, string typeName, CancellationToken ct)`). Replace the raw `project.GetCompilationAsync(ct)` call at line 108 with `_compilationCache.GetCompilationAsync(workspaceId, project, ct)`. Update the single call-site at line 47 in `FindTypeConsumersAsync` to pass `workspaceId`. Mirror pattern from `src/RoslynMcp.Roslyn/Services/CouplingAnalysisService.cs` lines 20–32. 2. `src/RoslynMcp.Roslyn/Services/CodePatternAnalyzer.cs` — add `ICompilationCache _compilationCache` field; inject via constructor. For the instance-method site at line 34 (`FindReflectionUsagesAsync`): replace with `_compilationCache.GetCompilationAsync(workspaceId, project, ct)` — `workspaceId` is already in scope. For the static-helper site at line 229 (`CollectSemanticSearchMatchesAsync`): add `string workspaceId, ICompilationCache compilationCache` parameters; replace raw call. Update the three call-sites in `SemanticSearchAsync` (lines ~154, ~167, ~193) to pass `workspaceId, _compilationCache`. 3. `src/RoslynMcp.Roslyn/Services/SymbolSearchService.cs` — add `ICompilationCache _compilationCache` field; inject via constructor. Add `string workspaceId, ICompilationCache compilationCache` parameters to private static `RunFqnSubstringFallbackAsync`. Replace raw call at line 117 with `compilationCache.GetCompilationAsync(workspaceId, project, ct)`. Update the single call-site at line 57 in `SearchSymbolsAsync` to pass `workspaceId, _compilationCache`. No DI registration changes needed — `ICompilationCache` is already registered singleton. |
| Scope | Production files (3): `src/RoslynMcp.Roslyn/Services/TypeConsumersService.cs`, `src/RoslynMcp.Roslyn/Services/CodePatternAnalyzer.cs`, `src/RoslynMcp.Roslyn/Services/SymbolSearchService.cs`. Test files (2, both extended — within Rule 4 ≤3): (1) `tests/RoslynMcp.Tests/CompilationCacheAdoptionTests.cs` — add three new `[TestMethod]` entries following the `RunTwiceAndCaptureAsync` + `AssertRoutedThroughCacheAndShared` pattern; (2) **`tests/RoslynMcp.Tests/TestInfrastructure/TestServiceContainer.cs`** — MANDATORY addenda test-fixture-DI edit (review cycle-0 warn): the three services are constructed there (`new TypeConsumersService(...)`, `new CodePatternAnalyzer(...)`, `new SymbolSearchService(...)`, ~lines 141/146/193); adding the `ICompilationCache` ctor param forces passing the existing `compilationCache` local to each (exactly as batch-1's PR #913 did for its three services). No new test files; both classes already exist. Remaining group (a) sites deferred: `TestReferenceMapService:150/178`, `ReferenceService:231` (next batch). Group (b) preview-service sites and group (c) forked-solution HAZARD sites remain in the backlog row. |
| Tool policy | edit-only |
| Estimated context cost | 35000 |
| Risks | (1) Static-helper signature changes: each helper gains `string workspaceId, ICompilationCache compilationCache` parameters. Because all helpers are `private static`, callers are fully enumerated within each file — no external API breakage. (2) `CodePatternAnalyzer.CollectSemanticSearchMatchesAsync` has three call-sites (structured, name-substring, token-or-match fallbacks) — all three must be updated; missing one leaves a raw call in a fallback branch that tests may not exercise. Executor must search for all three call-sites. (3) Group (c) forked-solution HAZARD sites (`SymbolResolver:285`, `SymbolResolver:125`, `SymbolHandleSerializer:204`, `InterfaceExtractionService:515`, `RefactoringService:415`, `TypeMoveService:217`) are explicitly excluded from ALL batches until a `solution == GetCurrentSolution(workspaceId)` guard is confirmed in the callers. These must NOT be adopted without that guard check. (4) Roslyn already memoizes `Compilation` per `Project` call; the cache benefit is cross-call sharing under GC pressure and in-flight dedup — adoption does not regress correctness, only adds sharing. (5) `WorkspaceWarmService:97` uses `project.GetCompilationAsync` intentionally as the warm-up driver — it should NOT be converted. |
| Validation | 1. Extend `tests/RoslynMcp.Tests/CompilationCacheAdoptionTests.cs` with `TypeConsumersService_ObtainsCompilations_ThroughSharedCache`, `CodePatternAnalyzer_FindReflection_ObtainsCompilations_ThroughSharedCache`, `SymbolSearchService_ObtainsCompilations_ThroughSharedCache` — each following the `RunTwiceAndCaptureAsync` + `AssertRoutedThroughCacheAndShared` pattern. 2. `mcp__roslyn__compile_check` after each file edit — zero new errors. 3. Run `dotnet test --filter "CompilationCacheAdoptionTests"` — all 6 methods (3 existing + 3 new) green. 4. Verify no remaining `project.GetCompilationAsync` calls in the three modified files via Grep scoped to those files — zero hits. |
| Performance review | N/A — correctness/consistency fix, no hot-path changes. Caching adds minor allocation overhead (dictionary lookup); benefit is GC-pressure resilience and in-flight dedup. |
| CHANGELOG category | Maintenance |
| CHANGELOG entry (draft) | Route `TypeConsumersService`, `CodePatternAnalyzer`, and `SymbolSearchService` compilations through the shared `ICompilationCache` (batch 2 of the compilation-cache adoption sweep; batch 1 shipped in PR #913). |
| Backlog sync | Update related: [compilation-cache-adoption-read-side] — note batch 2 shipped for group (a) sites: `TypeConsumersService`, `CodePatternAnalyzer`, `SymbolSearchService`. Remaining open: `TestReferenceMapService:150/178`, `ReferenceService:231` (group a deferred), group (b) preview-service sites, group (c) HAZARD sites. |

### 2. promotion-scorecard-execution-batch

| Field | Content |
|---|---|
| Diagnosis | **Two duplicate `_latest-promotion-scorecard.json` files confirmed in the working tree.** `audit-reports/_latest-promotion-scorecard.json` (serverVersion `1.38.1`, generatedAt `2026-05-16T06:25:47Z`, `schemaVersion: 1`, summary `promote: 18`) is the newer file and lives alongside the timestamped audit `.md` reports and `_aggregated-promotion-scorecard.json` in `audit-reports/` — making that the clearly canonical directory. `ai_docs/audit-reports/_latest-promotion-scorecard.json` (serverVersion `1.34.2`, generatedAtUtc `2026-05-08T16:15:25Z`, no `schemaVersion` field, summary `promoteReady: 25`) is the older file and the origin of the backlog row's "promoteReady=25" claim. The current server version is `2.3.1` (from `Directory.Build.props` line 8). The two files differ in schema shape (`schemaVersion` presence, field names `entries[]` vs `scorecard[]`, `generatedAtUtc` vs `generatedAt`). The second concern in the backlog row — re-running the scorecard against v2.3.1 and shipping tier promotions via `/promote-tier` — is out of scope for this initiative: it is investigation-heavy, requires touching `ServerSurfaceCatalog.*.cs` partials (documented hotspot), and on its own would likely exceed Rule 5's 80K token ceiling. Splitting is mandatory. This initiative covers only the source-of-truth dedup (bounded, decidable). The tier-promotion execution is a follow-on row. |
| Approach | 1. Delete `ai_docs/audit-reports/_latest-promotion-scorecard.json` (the stale, schema-inconsistent, lower-version copy). 2. Update `ai_docs/audit-reports/README.md` to remove any reference to `_latest-promotion-scorecard.json` as living in that directory, and add a pointer to `audit-reports/_latest-promotion-scorecard.json` as the canonical location. 3. Verify no other file in the repo hard-references the `ai_docs/audit-reports/_latest-promotion-scorecard.json` path (Grep check). No `ServerSurfaceCatalog.*.cs` changes. No `/promote-tier` execution. The tier-promotion execution is intentionally deferred to a follow-on initiative (see Backlog sync). |
| Scope | Production files touched: 2 — `ai_docs/audit-reports/README.md` (pointer update), `ai_docs/audit-reports/_latest-promotion-scorecard.json` (deleted). Files deleted: 1 — `ai_docs/audit-reports/_latest-promotion-scorecard.json`. Test files added/modified: 0. Note: `ai_docs/` files are doc/config; no `ServerSurfaceCatalog.*.cs` hotspot is touched. Satisfies Rule 3 (≤4 production files). Satisfies Rule 4 (0 test files). |
| Tool policy | edit-only |
| Estimated context cost | 20000 |
| Risks | (1) Some doc or script may reference `ai_docs/audit-reports/_latest-promotion-scorecard.json` by path — executor must Grep for the path before deleting. (2) `ai_docs/audit-reports/README.md` content is unknown at plan time; if it currently documents the stale file as canonical, the update is load-bearing. (3) The `audit-reports/_aggregated-promotion-scorecard.json` file was not read — executor should confirm it does not depend on the `ai_docs/` copy. (4) The two files use different JSON schemas; this schema gap is not resolved by this initiative — it is a data-quality note for the scorecard generator to normalize on next run. Fanout probe skipped — surgical doc deletion, no cross-cutting symbol changes. |
| Validation | 1. Confirm `ai_docs/audit-reports/_latest-promotion-scorecard.json` no longer exists in the working tree. 2. Run Grep for `ai_docs/audit-reports/_latest-promotion-scorecard.json` across the repo — expect zero hits. 3. Run `./eng/verify-ai-docs.ps1` — must pass (no broken links to the deleted file). 4. Confirm `audit-reports/_latest-promotion-scorecard.json` is untouched (serverVersion still `1.38.1`). |
| Performance review | N/A — correctness fix, no hot-path changes. |
| CHANGELOG category | Maintenance |
| CHANGELOG entry (draft) | Removed duplicate promotion scorecard at `ai_docs/audit-reports/_latest-promotion-scorecard.json` (serverVersion 1.34.2, 2026-05-08); `audit-reports/_latest-promotion-scorecard.json` (serverVersion 1.38.1, 2026-05-16) is the canonical location. |
| Backlog sync | Close rows: [promotion-scorecard-execution-batch] (partial — source-of-truth dedup only). Update related: add a new backlog row for the follow-on: re-run promotion scorecard against server v2.3.1 surface + ship tier promotions in bounded batches via `/promote-tier`; anchors `audit-reports/_latest-promotion-scorecard.json`, `src/RoslynMcp.Host.Stdio/Catalog/ServerSurfaceCatalog.*.cs`; hotspot — schedule separately. |

> **Note (orchestrator):** deepener flagged `judgmentHeavy: true`. The original backlog row bundles two concerns; this initiative is scoped to the source-of-truth dedup only. The tier-promotion execution (hotspot catalog edits + re-run against v2.3.1) MUST become a separate backlog row at execute-time Backlog sync — do not let it disappear when this row is partially closed.
