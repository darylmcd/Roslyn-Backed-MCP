| Field | Content |
|---|---|
| Route | deepen |
| Diagnosis | The timeout remains evidence-neutral: the existing code does not prove a formatter deadlock. The concrete root cause of the unexplained result is the supervision boundary in `tests/RoslynMcp.Tests/FormatterBaselineContractTests.cs:608-630`: `RunGeneratorCheckAsync` starts whole-stream `ReadToEndAsync` tasks, kills the outer PowerShell tree without awaiting its exit, and only then waits for pipe EOF before extracting phase markers. A nested descendant retaining a pipe handle therefore yields no partial markers and no proof that the owned tree exited. `eng/generate-format-baseline.ps1:115-131` compounds the blind spot by invoking restore and format through synchronous `& dotnet`, which exposes neither direct-child PID nor child lifetime to the marker stream. |
| Approach | First add a deterministic, bounded nested-process seam in `FormatterBaselineContractTests` that reproduces the current inherited-pipe/non-drain signature; do not change behavior unless that reproduction is observed. Then replace the script's two opaque `& dotnet` calls with one local owned-process runner that preserves the existing exit-code and output contracts while emitting stderr-only phase records for restore and formatter start/end with elapsed time and owned child PID, never command arguments, source paths, or environment values. Update `RunGeneratorCheckAsync` to capture stdout/stderr incrementally, record the outer generator PID and harness-wait phase, retain partial phase records before EOF, kill the owned tree on timeout, await outer-process teardown under a bounded cleanup budget, and verify the phase-recorded child no longer exists. Keep the five-minute production bound and byte-identical stdout contract unchanged. |
| Scope | Production files: 1 (`eng/generate-format-baseline.ps1`). Test files: 1 (`tests/RoslynMcp.Tests/FormatterBaselineContractTests.cs`, extended with the deterministic nested-process regression and supervision assertions). Doc files: 0. Deletions: none. The per-row changelog fragment and orchestrator-owned backlog mutation are excluded from the production count. |
| Tool policy | edit-only |
| Estimated context cost | 40000 |
| Risks | Incremental capture must preserve every formatter diagnostic and deterministic stdout byte-for-byte; phase and PID evidence must remain stderr-only and secret-safe. Process exit, stream completion, and descendant teardown can occur in either order, so cleanup assertions must use the signaled test child and bounded waits rather than machine-wide process termination. The 2026-09-19 hosted recurrence produced `stderrDrained=False` and `phases=[]`, confirming this evidence-loss shape but not the underlying resource stall; if the deterministic reproduction instead proves a cause outside the two anchors, leave production behavior unchanged and file a cause-specific child row rather than widening scope. No Rule 5b refactor shape is planned, so fanout is not applicable. |
| Validation | Run the new deterministic nested-process regression and require: the controlled child reaches its handshake, the old inherited-pipe signature is reproduced before the fix, the fixed path reports harness/restore-or-format/build-contention identity within the derived teardown bound, and the recorded owned child is absent afterward. Run `pwsh -NoProfile -File ./eng/generate-format-baseline.ps1 -Check` and the `-Check -NoRestore` variant, compare stdout to the tracked inventory, and confirm phase/PID records appear only on stderr. Run Roslyn `compile_check`, focused `FormatterBaselineContractTests`, then `just ci` for the repository-equivalent gate. |
| Performance review | N/A — validation-harness correctness fix; retain the five-minute production timeout and measure only bounded teardown overhead. |
| CHANGELOG category | Fixed |
| CHANGELOG entry (draft) | Make formatter-baseline timeouts retain phase and owned-child evidence and prove bounded process-tree teardown under nested contention. |
| Backlog sync | Close rows: [formatter-baseline-contended-nested-process-timeout-investigation]. |
