| Field | Content |
|---|---|
| Route | deepen |
| Diagnosis | `tests/RoslynMcp.Tests/ResourceReadWireContractTests.cs:249-280` performs one read per era and dereferences `ttlMs` with `GetProperty` at line 270 before asserting presence, so the observed failure discards the negotiated era, read ordinal, and captured response frame. The production path still selects the era in `src/RoslynMcp.Host.Stdio/ProtocolCompatibility/RequestProtocolFeatureGate.cs:14-23` and stamps or removes both hints in `src/RoslynMcp.Host.Stdio/Middleware/ResourceReadResultFilter.cs:44-75`; direct normalization coverage at `tests/RoslynMcp.Tests/StaticListResultFilterTests.cs:87-99` remains green, the failed PR reran green unchanged, and current-session targeted repetition did not reproduce a production defect. |
| Approach | In `ResourceRead_MigratedEndpointSuccess_KeepsCacheHintNormalizationPerEra`, keep one harness per protocol era but issue repeated reads through that same client. Before each read, assert `Client.NegotiatedProtocolVersion`; after it, require exactly one new raw response frame and use `TryGetProperty`-based cache-hint assertions that include the era, read ordinal, and full frame before reading values, mirroring the fail-loud wire assertions at `tests/RoslynMcp.Tests/ResourceReadWireContractTests.cs:194-201`. Strengthen `FindSingleNewResponseFrame` at lines 361-377 to include captured post-skip messages when the client/harness emits zero or multiple response frames. This makes a future failure classify as protocol state (negotiated-version mismatch), harness/cache behavior (no fresh response frame), or normalization/serialization (fresh modern frame missing or mis-shaping hints) without inventing an unproved production change. |
| Scope | Production files: 0. Test files: 1 — `tests/RoslynMcp.Tests/ResourceReadWireContractTests.cs` (extend the existing wire-contract method and helper; one regression shape). Doc files: 1 — `changelog.d/resource-cache-hint-wire-failure-diagnostics.md`. Deletions: none. |
| Tool policy | edit-only |
| Estimated context cost | 30000 |
| Risks | Repeated reads must stay sequential within each era-specific harness so the test diagnoses client caching without introducing concurrency noise. Preserve the modern `ttlMs=0`/`cacheScope=private` values and legacy omission contract; do not weaken a missing-field failure into a warning. No fanout probe applies because the edit is assertion-local and changes no production symbol or signature. |
| Validation | Run `dotnet test tests/RoslynMcp.Tests/RoslynMcp.Tests.csproj -c Release --filter FullyQualifiedName=RoslynMcp.Tests.ResourceReadWireContractTests.ResourceRead_MigratedEndpointSuccess_KeepsCacheHintNormalizationPerEra`; repeat the filtered test to exercise fresh harness creation as well as the new repeated reads inside each modern/legacy harness. Run `./eng/verify-changelog-fragments.ps1`, then the repo-required `just ci`. |
| Performance review | N/A — correctness fix, no hot-path changes. |
| CHANGELOG category | Fixed |
| CHANGELOG entry (draft) | Improve resource cache-hint wire failures with era-specific frames and repeated-read coverage. |
| Backlog sync | Close rows: [resource-cache-hint-wire-failure-diagnostics]. Mark obsolete: []. |
