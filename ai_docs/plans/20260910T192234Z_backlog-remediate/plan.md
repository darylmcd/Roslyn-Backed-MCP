# Backlog sweep plan — 20260910T192234Z_backlog-remediate

Generated by `/backlog-remediate`. 8 initiative(s) selected off `ai_docs/backlog.md` (snapshot 2026-09-10T19:21:20Z).

<!-- BSWEEP:STATUS-TABLE BEGIN — generated from state.json; do not edit by hand -->
## Status (generated)

| # | id | status | PR | rows closed |
|---|----|--------|----|-------------|
| 1 | workspace-warm-cancellation-propagation | pending | — | workspace-warm-cancellation-propagation |
| 2 | scaffold-fqn-target-type-disambiguation | pending | — | scaffold-fqn-target-type-disambiguation |
| 3 | test-service-container-production-di-lifetime | pending | — | test-service-container-production-di-lifetime |
| 4 | scripting-supervisor-outer-cancellation-contended-timeout | pending | — | scripting-supervisor-outer-cancellation-contended-timeout |
| 5 | pr-reconciler-fail-closed-ship-cleanup | pending | — | pr-reconciler-fail-closed-ship-cleanup |
| 6 | backlog-intake-v15-writer-contract | pending | — | backlog-intake-v15-writer-contract |
| 7 | structured-call-tool-filter-pipeline-decomposition | pending | — | structured-call-tool-filter-pipeline-decomposition |
| 8 | tool-consolidation-deprecated-alias-registry | pending | — | tool-consolidation-deprecated-alias-registry |
<!-- BSWEEP:STATUS-TABLE END -->

## Initiatives

## Selection rationale

| requested upper bound | selected | decision |
|---:|---:|---|
| 15 | 8 | Prioritize dependency-ready Medium correctness/safety work and one Low prerequisite that unlocks the alias migration. Do not pad with lower-leverage description cleanup. |

| Skipped row | reason |
|---|---|
| `promotion-scorecard-refresh-toplevel-run` | execution-blocked — partial audit was discarded; no scorecard fabricated. |
| `formatter-baseline-contended-nested-process-timeout-investigation` | execution-blocked — no causal reproduction. |
| `coverlet-net10-session-end-crash-upgrade` | execution-blocked — upstream fix remains unreleased. |
| `coverage-baseline-stale` | execution-blocked — acceptance anchors drifted. |
| `test-assembly-donotparallelize-audit` | effective-L-unsplit — must partition by opt-out cause first. |
| method-description diet slices | lower leverage than correctness/safety; retained for a later batch. |

| Conflict edge | handling |
|---|---|
| `pr-reconciler-fail-closed-ship-cleanup` ↔ `backlog-intake-v15-writer-contract` | Serial; both update `eng/verify-ai-docs.ps1`. |
| structured filter ↔ alias registry | Separate worktrees; no shared source/test anchor. |

### 1. workspace-warm-cancellation-propagation

| Field | Contract |
|---|---|
| Route | direct |
| Scope | `WorkspaceWarmService.cs`; `WorkspaceWarmServiceTests.cs`; `changelog.d/workspace-warm-cancellation-propagation.md` |
| Fix | Check/throw the caller token before and between projects. Preserve per-project non-cancellation isolation and inner Roslyn cancellation propagation. |
| Regression | Pre-cancel a loaded multi-project workspace and assert `OperationCanceledException`, never a partial result. |
| Validation | Focused warm-service tests; `./eng/verify-release.ps1 -Configuration Release`. |
| Risk | Do not convert ordinary per-project failures into request cancellation. |

### 2. scaffold-fqn-target-type-disambiguation

| Field | Contract |
|---|---|
| Route | direct |
| Scope | `SingleTestScaffolder.cs`; `BatchTestScaffolder.cs`; `ScaffoldingIntegrationTests.cs`; `changelog.d/scaffold-fqn-target-type-disambiguation.md` |
| Fix | Retain qualified input through resolution; use the resolved simple name only for generated identifiers. |
| Regression | Two `Widget` types: simple name rejects ambiguity; `Alpha.Widget` selects only Alpha in single and batch modes. |
| Validation | Focused scaffolding integration test; `./eng/verify-release.ps1 -Configuration Release`. |
| Risk | Preserve simple-name ambiguity errors and generated-test naming. |

### 3. test-service-container-production-di-lifetime

| Field | Contract |
|---|---|
| Route | direct |
| Scope | `TestServiceContainer.cs`; `TestAssemblyFixture.cs`; `TestAssemblyFixtureTests.cs`; `changelog.d/test-service-container-production-di-lifetime.md` |
| Fix | Build the test graph through production registrations plus explicit overrides; give provider/gate/manager/watcher one idempotent owner. |
| Regression | Resolve standalone fixture, assert representative singleton identity, dispose twice, and observe deterministic disposed gate/manager behavior. |
| Validation | Focused fixture/container tests; `./eng/verify-release.ps1 -Configuration Release`. |
| Risk | Preserve explicit test execution options and defer only the genuine construction cycle. |

### 4. scripting-supervisor-outer-cancellation-contended-timeout

| Field | Contract |
|---|---|
| Route | direct |
| Scope | `ScriptExecutionSupervisor.cs`; `ScriptingServiceTests.cs`; `changelog.d/scripting-supervisor-outer-cancellation-contended-timeout.md` |
| Fix | Add a test-owned monitor-start/completion seam; remove fixed wall-clock waits from the outer-cancellation regression. |
| Regression | Hold a worker, wait for monitor start, cancel caller, release worker, then prove `OuterCancelled` and zero active/abandoned slots across repeated runs. |
| Validation | Focused scripting tests repeated under contention; `./eng/verify-release.ps1 -Configuration Release`. |
| Risk | Do not expose a production-only test hook or alter timeout classification. |

### 5. pr-reconciler-fail-closed-ship-cleanup

| Field | Contract |
|---|---|
| Route | direct, serial with initiative 6 |
| Scope | `.claude/agents/pr-reconciler.md`; `ai_docs/prompts/backlog-sweep-addenda.md`; `eng/verify-ai-docs.ps1`; `changelog.d/pr-reconciler-fail-closed-ship-cleanup.md` |
| Fix | Make the reconciler readiness-only and hand merge/cleanup to parent `/ship --land=<pr>`; report `SHIP_STATUS=landed-cleanup-failed` instead of suppressing failures. |
| Regression | Static verifier rejects force removal, swallowed remote deletion, and continue-on-cleanup-error guidance in these active files. |
| Validation | `pwsh -NoProfile -File eng/verify-ai-docs.ps1`; `./eng/verify-release.ps1 -Configuration Release`. |
| Risk | Keep retained-dirty-worktree reporting explicit; do not rewrite historical evidence. |

### 6. backlog-intake-v15-writer-contract

| Field | Contract |
|---|---|
| Route | direct, serial with initiative 5 |
| Scope | `.claude/skills/backlog-intake/SKILL.md`; `.claude/agents/backlog-intake-extractor.md`; `ai_docs/items/backlog-d-fragment-schema.md`; `eng/verify-ai-docs.ps1`; `changelog.d/backlog-intake-v15-writer-contract.md` |
| Fix | Define raw fragment P0→Critical/P1→High/P2→Medium/P3→Low mapping; emit v15 bands; route all mutations through global `backlog.mjs`. |
| Regression | Static verifier rejects legacy P2/P3/P4 backlog priority output, direct edits, stale counts, and missing canonical writer guidance. |
| Validation | `pwsh -NoProfile -File eng/verify-ai-docs.ps1`; `./eng/verify-release.ps1 -Configuration Release`. |
| Risk | Preserve raw external P-severity as source metadata, not backlog priority. |

### 7. structured-call-tool-filter-pipeline-decomposition

| Field | Contract |
|---|---|
| Route | direct, judgment-heavy |
| Scope | `StructuredCallToolFilter.cs`; new `StructuredWorkspaceResolver.cs`, `StructuredDispatchPipeline.cs`, `StructuredResultProjector.cs`; three named structured-filter wire tests; `changelog.d/structured-call-tool-filter-pipeline-decomposition.md` |
| Fix | Retain `Create` as composition boundary while moving workspace resolution, retry/elicitation dispatch, and result/error projection into focused internal collaborators. |
| Regression | Table-driven matrix for explicit/single/zero/multiple workspace resolution plus binder/handler/retry/error wire paths. |
| Validation | Focused structured filter/unit/wire tests; MCP compile check; `./eng/verify-release.ps1 -Configuration Release`. |
| Risk | Public protocol boundary: preserve tool names, argument mutation, cancellation, error envelopes, structured content, and `_meta` exactly. |

### 8. tool-consolidation-deprecated-alias-registry

| Field | Contract |
|---|---|
| Route | direct |
| Scope | `Catalog/ToolAliasDeprecation.cs`; `Tools/ToolAliasDeprecation.cs`; `Catalog/ServerSurfaceCatalog.cs`; `SurfaceCatalogTests.cs`; `AliasToolsTests.cs`; `changelog.d/tool-consolidation-deprecated-alias-registry.md` |
| Fix | Create catalog-owned alias lifecycle declaration; relocate the existing response envelope; register `get_symbol_outline` → `document_symbols`. |
| Regression | Alias/canonical metadata and payload parity except declared deprecation; old tool remains callable. |
| Validation | Focused catalog/alias tests; live MCP alias/canonical probe; `./eng/verify-release.ps1 -Configuration Release`. |
| Risk | Preserve public alias compatibility; no name removal or semver-breaking change. |
