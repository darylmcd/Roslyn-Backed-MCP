# Backlog sweep plan — 20260910T192234Z_backlog-remediate

Generated by `/backlog-remediate`. 8 initiative(s) selected off `ai_docs/backlog.md` (snapshot 2026-09-10T19:21:20Z).

<!-- BSWEEP:STATUS-TABLE BEGIN — generated from state.json; do not edit by hand -->
## Status (generated)

| # | id | status | PR | rows closed |
|---|----|--------|----|-------------|
| 1 | workspace-warm-cancellation-propagation | pending | — | workspace-warm-cancellation-propagation |
| 2 | scaffold-fqn-target-type-disambiguation | pending | — | scaffold-fqn-target-type-disambiguation |
| 3 | test-service-container-production-di-lifetime | pending | — | test-service-container-production-di-lifetime, workspace-manager-file-watcher-disposal-ownership |
| 4 | scripting-supervisor-outer-cancellation-contended-timeout | pending | — | scripting-supervisor-outer-cancellation-contended-timeout |
| 5 | pr-reconciler-fail-closed-ship-cleanup | pending | — | pr-reconciler-fail-closed-ship-cleanup |
| 6 | backlog-intake-v15-writer-contract | pending | — | backlog-intake-v15-writer-contract |
| 7 | structured-call-tool-filter-pipeline-decomposition | pending | — | structured-call-tool-filter-pipeline-decomposition |
| 8 | tool-consolidation-deprecated-alias-registry | pending | — | tool-consolidation-deprecated-alias-registry, get-symbol-outline-workspace-id-alias-parity |
<!-- BSWEEP:STATUS-TABLE END -->

## Initiatives

## Selection rationale

| requested upper bound | selected | decision |
|---:|---:|---|
| 15 | 8 | Prioritize dependency-ready Medium correctness/safety work and one Low prerequisite that unlocks the alias migration. Do not pad with lower-leverage description cleanup. |

| Skipped row | reason |
|---|---|
| `promotion-scorecard-refresh-toplevel-run` | execution-blocked — partial audit was discarded; no scorecard fabricated. |
| `formatter-baseline-contended-nested-process-timeout-investigation` | execution-blocked — no causal reproduction. |
| `coverlet-net10-session-end-crash-upgrade` | execution-blocked — upstream fix remains unreleased. |
| `coverage-baseline-stale` | execution-blocked — acceptance anchors drifted. |
| `test-assembly-donotparallelize-audit` | effective-L-unsplit — must partition by opt-out cause first. |
| method-description diet slices | lower leverage than correctness/safety; retained for a later batch. |

| Conflict edge | handling |
|---|---|
| `pr-reconciler-fail-closed-ship-cleanup` ↔ `backlog-intake-v15-writer-contract` | Serial; both update `eng/verify-ai-docs.ps1`. |
| structured filter ↔ alias registry | Separate worktrees; no shared source/test anchor. |

### 1. workspace-warm-cancellation-propagation

| Field | Content |
|---|---|
| Route | `direct` |
| Diagnosis | `src/RoslynMcp.Roslyn/Services/WorkspaceWarmService.cs:54` breaks on caller cancellation and returns a partial result; the per-project catch at `:89` already rethrows cancellation, so the outer loop is the missing contract boundary. |
| Approach | Check/throw the caller token before loading, at each project-loop boundary, and before result projection; preserve per-project non-cancellation isolation and existing inner cancellation propagation. |
| Scope | Production files (1): `src/RoslynMcp.Roslyn/Services/WorkspaceWarmService.cs`. Test files (1): `tests/RoslynMcp.Tests/WorkspaceWarmServiceTests.cs`. Deletions: none. |
| Tool policy | `edit-only` |
| Estimated context cost | 35000 |
| Risks | Verify that ordinary per-project failures still produce the current diagnostic/result shape; the source-only fanout probe is 2 files. |
| Validation | Add the pre-cancelled loaded multi-project regression; run focused `WorkspaceWarmServiceTests`, `mcp__roslyn__compile_check`, then `./eng/verify-release.ps1 -Configuration Release`. |
| Performance review | N/A — correctness fix, no hot-path changes. |
| CHANGELOG category | `Fixed` |
| CHANGELOG entry (draft) | `- **Fixed:** Fail workspace warming on caller cancellation instead of reporting a partial successful result. Closes \`workspace-warm-cancellation-propagation\`.` |
| Backlog sync | Close rows: [workspace-warm-cancellation-propagation]. Mark obsolete: []. |

### 2. scaffold-fqn-target-type-disambiguation

| Field | Content |
|---|---|
| Route | `direct` |
| Diagnosis | `src/RoslynMcp.Roslyn/Services/SingleTestScaffolder.cs:60`, `:120`, and `:614` strip a qualified request before semantic lookup; `src/RoslynMcp.Roslyn/Services/BatchTestScaffolder.cs:153` repeats the loss, making distinct `Alpha.Widget`/`Beta.Widget` unresolvable. |
| Approach | Preserve the raw requested type through semantic candidate resolution; enumerate by simple tail but filter qualified input by the matched symbol display name; use the resolved simple name only for generated identifiers, filenames, and sampling. Keep simple-name ambiguity errors unchanged. |
| Scope | Production files (2): `src/RoslynMcp.Roslyn/Services/SingleTestScaffolder.cs`, `src/RoslynMcp.Roslyn/Services/BatchTestScaffolder.cs`. Test files (1): `tests/RoslynMcp.Tests/ScaffoldingIntegrationTests.cs`. Deletions: none. |
| Tool policy | `edit-only` |
| Estimated context cost | 55000 |
| Risks | The sampling preflight must become qualification-aware rather than reject a valid FQN as ambiguous; the symbol-lookup fanout probe is 3 files. |
| Validation | Add isolated `Alpha.Widget`/`Beta.Widget` coverage proving simple-name rejection and correct qualified single/batch output; run focused `ScaffoldingIntegrationTests`, `mcp__roslyn__compile_check`, then `./eng/verify-release.ps1 -Configuration Release`. |
| Performance review | N/A — correctness fix, no hot-path changes. |
| CHANGELOG category | `Fixed` |
| CHANGELOG entry (draft) | `- **Fixed:** Preserve fully qualified scaffold targets through semantic resolution so duplicate simple type names remain selectable. Closes \`scaffold-fqn-target-type-disambiguation\`.` |
| Backlog sync | Close rows: [scaffold-fqn-target-type-disambiguation]. Mark obsolete: []. |

### 3. test-service-container-production-di-lifetime

| Field | Content |
|---|---|
| Route | `deepen` |
| Diagnosis | `tests/RoslynMcp.Tests/TestInfrastructure/TestServiceContainer.cs:75-316` hand-builds the production graph and adds a bespoke deferred gate despite `src/RoslynMcp.Roslyn/ServiceCollectionExtensions.cs:24-36` having the production `Lazy<IWorkspaceExecutionGate>` cycle break. `tests/RoslynMcp.Tests/TestInfrastructure/TestAssemblyFixture.cs:136-147` disposes only the manager, and standalone construction at `TestServiceContainer.cs:365` leaks it. `src/RoslynMcp.Roslyn/Services/WorkspaceManager.cs:1053` disposes its watcher while production DI owns the singleton at `ServiceCollectionExtensions.cs:108`, exposing the same double-owner lifecycle. Bundled with `workspace-manager-file-watcher-disposal-ownership` because one production-provider disposal path and one exactly-once watcher regression resolve both symptoms. |
| Approach | Preserve direct `WorkspaceManager` construction as watcher owner; add an internal provider-owned construction path used by `AddRoslynServices` without changing public constructor callers. Replace the handwritten test graph with a test-owned provider built from production registrations and explicit test overrides, project checked interface singletons back to existing concrete test APIs, delete the deferred gate, and let the container be the sole idempotent disposal root. |
| Scope | Production files (2): `src/RoslynMcp.Roslyn/ServiceCollectionExtensions.cs`, `src/RoslynMcp.Roslyn/Services/WorkspaceManager.cs`. Test files (3): `tests/RoslynMcp.Tests/TestInfrastructure/TestServiceContainer.cs`, `tests/RoslynMcp.Tests/TestInfrastructure/TestAssemblyFixture.cs`, `tests/RoslynMcp.Tests/TestAssemblyFixtureTests.cs`. Deletions: `DeferredWorkspaceExecutionGate` and its obsolete bind test. |
| Tool policy | `edit-only` |
| Estimated context cost | 55000 |
| Risks | Preserve direct-manager watcher disposal (`MissingWorkspaceRootRetirementTests` depends on it); retain the unlimited test gate and concrete test-facing APIs. The public constructor remains unchanged, so the 10 constructor callers and four registration callers remain untouched; production fanout probe is 2 files. |
| Validation | Construct a production `AddRoslynServices` provider with a counted watcher override, resolve its manager, dispose the manager, then dispose the provider and prove watcher disposal executes exactly once; prove representative interface/concrete singleton identity, unlimited test gate behavior, and repeat fixture disposal/cache clearance. Run focused `TestServiceContainerTests` and `TestAssemblyFixtureTests`, `mcp__roslyn__compile_check`, then `./eng/verify-release.ps1 -Configuration Release`. |
| Performance review | N/A — lifecycle correctness and test composition only. |
| CHANGELOG category | `Maintenance` |
| CHANGELOG entry (draft) | `- **Maintenance:** Align shared test services with production DI and give their lifetime one explicit owner. Closes \`test-service-container-production-di-lifetime\` and \`workspace-manager-file-watcher-disposal-ownership\`.` |
| Backlog sync | Close rows: [test-service-container-production-di-lifetime, workspace-manager-file-watcher-disposal-ownership]. Mark obsolete: []. |

### 4. scripting-supervisor-outer-cancellation-contended-timeout

| Field | Content |
|---|---|
| Route | `direct` |
| Diagnosis | `tests/RoslynMcp.Tests/ScriptingServiceTests.cs:164-238` infers monitor startup from worker entry and then uses 5-second/2-second wall-clock waits; `src/RoslynMcp.Roslyn/Services/ScriptExecutionSupervisor.cs:40` and `:107` have no observable monitor-start boundary. |
| Approach | Add an internal test-owned monitor-start callback through the delegate-worker path; replace both affected regressions with `TaskCompletionSource` barriers for worker entry, monitor start, cancellation classification, release, and worker exit; repeat the controlled sequence five times while retaining only the class timeout as a deadlock guard. |
| Scope | Production files (1): `src/RoslynMcp.Roslyn/Services/ScriptExecutionSupervisor.cs`. Test files (1): `tests/RoslynMcp.Tests/ScriptingServiceTests.cs`. Deletions: none. |
| Tool policy | `edit-only` |
| Estimated context cost | 45000 |
| Risks | Keep the hook internal and test-owned; do not change production timeout classification or leak a public control surface. Source fanout probe is 2 files. |
| Validation | Run the focused outer-cancellation regression five controlled repetitions plus the affected scripting test class, `mcp__roslyn__compile_check`, then `./eng/verify-release.ps1 -Configuration Release`. |
| Performance review | N/A — test-causality change; production polling behavior is unchanged. |
| CHANGELOG category | `Maintenance` |
| CHANGELOG entry (draft) | `- **Maintenance:** Make contended scripting cancellation regressions causal instead of timing-sensitive. Closes \`scripting-supervisor-outer-cancellation-contended-timeout\`.` |
| Backlog sync | Close rows: [scripting-supervisor-outer-cancellation-contended-timeout]. Mark obsolete: []. |

### 5. pr-reconciler-fail-closed-ship-cleanup

| Field | Content |
|---|---|
| Route | `direct` |
| Diagnosis | `.claude/agents/pr-reconciler.md` currently promises standalone merge/cleanup while `ai_docs/prompts/backlog-sweep-addenda.md` says the parent ships; `eng/verify-ai-docs.ps1` lacks a guard against force worktree removal or swallowed cleanup errors. |
| Approach | Make the reconciler readiness-only, hand merge/cleanup to parent `/ship --land=<pr>`, and make its frontmatter, procedure, addenda entry, and `SHIP_STATUS=landed-cleanup-failed` handoff agree. Add static verification that rejects raw force removal, swallowed remote-delete failures, and continuation after cleanup errors. |
| Scope | Production files (3): `.claude/agents/pr-reconciler.md`, `ai_docs/prompts/backlog-sweep-addenda.md`, `eng/verify-ai-docs.ps1`. Test files (0): none; the verifier is the regression. Deletions: unsafe cleanup guidance only. |
| Tool policy | `edit-only` |
| Estimated context cost | 35000 |
| Risks | Preserve explicit dirty-residue reporting and historical evidence; this conflicts with intake only at `eng/verify-ai-docs.ps1`, so the intake initiative depends on this one. Fanout probe is 3 files. |
| Validation | Run `pwsh -NoProfile -File eng/verify-ai-docs.ps1`, test the verifier's accepted/rejected snippets, then `./eng/verify-release.ps1 -Configuration Release`. |
| Performance review | N/A — maintainer workflow safety only. |
| CHANGELOG category | `Maintenance` |
| CHANGELOG entry (draft) | `- **Maintenance:** Make backlog-reconciliation cleanup fail closed and route landing through the canonical ship owner. Closes \`pr-reconciler-fail-closed-ship-cleanup\`.` |
| Backlog sync | Close rows: [pr-reconciler-fail-closed-ship-cleanup]. Mark obsolete: []. |

### 6. backlog-intake-v15-writer-contract

| Field | Content |
|---|---|
| Route | `direct` |
| Diagnosis | `.claude/skills/backlog-intake/SKILL.md` and `.claude/agents/backlog-intake-extractor.md` still describe retired priority/direct-edit behavior, while `ai_docs/items/backlog-d-fragment-schema.md` documents the v15 fragment boundary and `eng/verify-ai-docs.ps1` lacks the matching contract guard. |
| Approach | Define P0→Critical/P1→High/P2→Medium/P3→Low only for raw source severity, emit v15 backlog bands, and require all mutations through global `backlog.mjs`; add verifier coverage rejecting legacy P2/P3/P4 output, direct edits, stale counts, and missing writer guidance. |
| Scope | Production files (4): `.claude/skills/backlog-intake/SKILL.md`, `.claude/agents/backlog-intake-extractor.md`, `ai_docs/items/backlog-d-fragment-schema.md`, `eng/verify-ai-docs.ps1`. Test files (0): none; the verifier is the regression. Deletions: retired writer/priority instructions only. |
| Tool policy | `edit-only` |
| Estimated context cost | 40000 |
| Risks | Preserve upstream raw severity as source metadata rather than confusing it with local backlog priority; serial after reconciler because both modify the verifier. Fanout probe is 4 files. |
| Validation | Run `pwsh -NoProfile -File eng/verify-ai-docs.ps1`, test acceptance/rejection snippets, then `./eng/verify-release.ps1 -Configuration Release`. |
| Performance review | N/A — maintainer documentation and verifier contract only. |
| CHANGELOG category | `Maintenance` |
| CHANGELOG entry (draft) | `- **Maintenance:** Align backlog intake artifacts with the v15 transactional writer contract. Closes \`backlog-intake-v15-writer-contract\`.` |
| Backlog sync | Close rows: [backlog-intake-v15-writer-contract]. Mark obsolete: []. |

### 7. structured-call-tool-filter-pipeline-decomposition

| Field | Content |
|---|---|
| Route | `deepen` |
| Diagnosis | `src/RoslynMcp.Host.Stdio/Middleware/StructuredCallToolFilter.cs` has CC33/299 LOC/nesting 7 and owns normalization, workspace resolution, recovery, dispatch, terminal shaping, and error reporting. Existing `StructuredCallElicitationCoordinator.cs` already owns retry/form mechanics and `StructuredCallContentProjector.cs` owns lossless `_meta` projection; duplicating either would recreate split ownership. The local error path classifies once for logging and again for formatting, risking divergent internal-error handling. |
| Approach | Keep `StructuredCallToolFilter.Create` as only the null guard/composition boundary. Extract a workspace-only resolver, a dispatch pipeline that uses the existing elicitation coordinator for temporary-argument dispatch, and a terminal projector that calls `ToolErrorHandler.ClassifyError` once then uses that same `ErrorInfo` for formatting and logging. Reuse the existing content projector unchanged; remove the second classifier path. |
| Scope | Production files (4): `src/RoslynMcp.Host.Stdio/Middleware/StructuredCallToolFilter.cs`, `src/RoslynMcp.Host.Stdio/Middleware/StructuredWorkspaceResolver.cs`, `src/RoslynMcp.Host.Stdio/Middleware/StructuredDispatchPipeline.cs`, `src/RoslynMcp.Host.Stdio/Middleware/StructuredResultProjector.cs`. Test files (3): `tests/RoslynMcp.Tests/StructuredCallToolFilterResolutionTests.cs`, `tests/RoslynMcp.Tests/StructuredCallToolFilterTests.cs`, `tests/RoslynMcp.Tests/StructuredContentWireContractTests.cs`. Deletions: obsolete local classifier/helper code only. |
| Tool policy | `edit-only` |
| Estimated context cost | 65000 |
| Risks | Public wire boundary: preserve bound-handler routing when `Params.Name` changes, cancellation rechecks after every recovery await, input-required flow, `_meta`, result discriminator, modern/legacy envelope shape, and error category. The stale row anchor `StructuredToolRawWireIntegrationTests.cs` is replaced by the live wire guard `StructuredContentWireContractTests.cs`; fanout probe is 7 files including the three test owners. |
| Validation | Add a resolver matrix for explicit/single/zero/multiple/file-owner cases; in `StructuredCallToolFilterTests` execute named legacy and modern missing-workspace → elicitation → retry wire frames plus binder/handler/error paths; use `StructuredContentWireContractTests` to lock `_meta`, result discriminator, and protocol shapes. Run `dotnet test tests/RoslynMcp.Tests/RoslynMcp.Tests.csproj --nologo --filter "FullyQualifiedName~StructuredCallToolFilterResolutionTests|FullyQualifiedName~StructuredCallToolFilterTests|FullyQualifiedName~StructuredContentWireContractTests"`, `mcp__roslyn__compile_check`, then `./eng/verify-release.ps1 -Configuration Release`. |
| Performance review | The request middleware path is touched; retain one stopwatch/lifetime boundary and avoid new reflection or duplicate projection on successful calls. |
| CHANGELOG category | `Maintenance` |
| CHANGELOG entry (draft) | `- **Maintenance:** Split structured tool dispatch into focused resolution, dispatch, and terminal-projection owners while preserving its wire contract. Closes \`structured-call-tool-filter-pipeline-decomposition\`.` |
| Backlog sync | Close rows: [structured-call-tool-filter-pipeline-decomposition]. Mark obsolete: []. |

### 8. tool-consolidation-deprecated-alias-registry

| Field | Content |
|---|---|
| Route | `deepen` |
| Diagnosis | `src/RoslynMcp.Host.Stdio/Catalog/ServerSurfaceCatalog.cs:479` exposes `SurfaceEntry` as a public positional record, so adding a constructor member would break callers. `src/RoslynMcp.Host.Stdio/Tools/SymbolTools.cs:445` requires `workspaceId` for `get_symbol_outline` while canonical `document_symbols` is optional, making the selected row’s required parameter-parity proof impossible. Bundled with `get-symbol-outline-workspace-id-alias-parity` because the same public alias declaration and one end-to-end alias/canonical contract regression must change together; a registry that deliberately retains a mismatched representative alias cannot meet either row’s acceptance. |
| Approach | Move the existing response envelope record into the catalog as the single immutable alias lifecycle declaration. Preserve `SurfaceEntry`'s primary constructor and add an init-only `Deprecation` property, enriched by the existing `Tool(...)` factory; include deprecation in catalog field-diff logic. Deliberately evolve the alias response envelope additively with lifecycle fields while preserving the existing `canonicalName`/`reason` names and values; assert that shape so it cannot drift silently. Make `get_symbol_outline` use the same optional `workspaceId` parameter/default/resolution path as its canonical twin. Register all existing aliases from one map and retain every old tool name. |
| Scope | Production files (4): delete `src/RoslynMcp.Host.Stdio/Tools/ToolAliasDeprecation.cs`; add `src/RoslynMcp.Host.Stdio/Catalog/ToolAliasDeprecation.cs`; modify `src/RoslynMcp.Host.Stdio/Catalog/ServerSurfaceCatalog.cs`; modify `src/RoslynMcp.Host.Stdio/Tools/SymbolTools.cs`. Test files (3): `tests/RoslynMcp.Tests/AliasToolsTests.cs`, `tests/RoslynMcp.Tests/SurfaceCatalogTests.cs`, `tests/RoslynMcp.Tests/ServerDiscoveryWireTests.cs`. Deletions: only the relocated duplicate source file. |
| Tool policy | `edit-only` |
| Estimated context cost | 65000 |
| Risks | Public compatibility: do not alter the positional record constructor, remove/rename aliases, or claim a preview/apply risk bucket for ordinary aliases. The response evolution is deliberately additive, and canonical `deprecation` remains null; test old `canonicalName`/`reason` plus all lifecycle fields. The bounded bundle is one alias contract at `SymbolTools.cs:380,445`; fanout probe is 7 files including three test owners. |
| Validation | Assert legacy `canonicalName`/`reason`, all lifecycle fields, and full payload parity after removing only `deprecation`; verify catalog serialization and version diffs include deprecation; host `tools/list` must give aliases and canonical tool identical input schema, then execute both on one workspace/file and compare payloads. Run `dotnet test tests/RoslynMcp.Tests/RoslynMcp.Tests.csproj --nologo --filter "FullyQualifiedName~AliasToolsTests|FullyQualifiedName~SurfaceCatalogTests|FullyQualifiedName~ServerDiscoveryWireTests"`, `mcp__roslyn__compile_check`, then `./eng/verify-release.ps1 -Configuration Release`. |
| Performance review | N/A — catalog initialization only; preserve the existing immutable lazy catalog shape. |
| CHANGELOG category | `Maintenance` |
| CHANGELOG entry (draft) | `- **Maintenance:** Declare deprecated tool aliases in the catalog and keep `get_symbol_outline` contract-compatible with `document_symbols`. Closes \`tool-consolidation-deprecated-alias-registry\` and \`get-symbol-outline-workspace-id-alias-parity\`.` |
| Backlog sync | Close rows: [tool-consolidation-deprecated-alias-registry, get-symbol-outline-workspace-id-alias-parity]. Mark obsolete: []. |
