| Field | Content |
|---|---|
| Route | deepen |
| Diagnosis | Root cause is the cache contract. `src/RoslynMcp.Roslyn/Services/CompilationCache.cs:61-62` sets the single factory slot to `SourceGeneratorCompilation.CreateAsync` (`SourceGeneratorCompilation.cs:37-54`: drops materialized generated trees, reruns the generator driver, returns a Compilation the Solution does not own). `GetCompilationAsync` (`:89-93`) projects that snapshot, so symbols fed to `SymbolFinder.FindReferencesAsync(symbol, solution)` at `UnusedCodeAnalyzer.cs:970`/`:96` and `CouplingAnalysisService.cs:102`->`:238` resolve zero references. Introduced by #1402 (`92ac5a9c`) for diagnostic parity; its real consumers already use `GetCompilationSnapshotAsync` (`DiagnosticDocumentLookup.cs:101,177`, `DiagnosticProjectAnalyzer.cs:26`, `CompilationCache.cs:237`). Departs from the row's per-site mapping: fix the slot, not the sites. |
| Approach | In `CompilationCache.cs`: (1) `GetCompilationAsync` gets its own `(workspaceId, projectId)` slot whose default factory is the Solution-owned `project.GetCompilationAsync`; (2) `GetCompilationSnapshotAsync` keeps the generator-rerun factory in a separate map, and `BuildCompilationWithAnalyzersAsync` keeps reading the snapshot. Mirror the Lazy + AddOrUpdate + `EvictWhenBroken` + `ObserveWithCallerToken` pattern at `:95-132`; `Invalidate` (`:262-285`) clears all maps; the internal test-ctor `compilationFactory` drives the plain slot and the snapshot wraps it with empty generator diagnostics as `:63-69` does. In `src/RoslynMcp.Roslyn/Contracts/ICompilationCache.cs` rewrite both XML docs: plain = only source for `SymbolFinder` symbols; snapshot = diagnostic-only. Analyzers unedited. |
| Scope | Production (2): `src/RoslynMcp.Roslyn/Services/CompilationCache.cs`, `src/RoslynMcp.Roslyn/Contracts/ICompilationCache.cs`. Tests (2): new `tests/RoslynMcp.Tests/GeneratorProjectSymbolIdentityTests.cs` (pattern of `DiagnosticSourceGeneratorParityTests.cs:11-42`; SampleLib probe with a `[GeneratedRegex]` partial, a read private static field, a cross-referenced type; asserts no dead-field / unused-symbol report and Ca > 0); extend `tests/RoslynMcp.Tests/CompilationCacheTests.cs` (plain slot == `project.GetCompilationAsync()` instance; snapshot and plain slots independent). One regression shape. |
| Tool policy | edit-only |
| Estimated context cost | 40000 |
| Risks | Fanout: 18 production callers of `cache.GetCompilationAsync`; signature unchanged so 0 edits (`fanoutEstimate: 2`), but all revert to pre-#1402 behavior. `DiagnosticSourceGeneratorParityTests` must stay green (proves the snapshot still runs generators). `SymbolResolver.cs:102-111` metadata-name resolution uses the same cache and is fixed too. Sibling `find-type-consumers-mutations-generator-blind` stays out of scope; re-verify after merge. Memory: one extra cached reference per project, no extra compile pass. |
| Validation | `compile_check`; `test_run` filter `CompilationCacheTests\|CompilationCacheAdoptionTests\|GeneratorProjectSymbolIdentityTests\|DiagnosticSourceGeneratorParityTests\|DeadFieldDetectorTests\|CouplingAnalysisTests`; then `just ci`. |
| Performance review | N/A — plain slot serves Roslyn's own cached compilation; diagnostic callers keep one generator pass. |
| CHANGELOG category | Fixed |
| CHANGELOG entry (draft) | Fixed `find_dead_fields`, `find_unused_symbols` and `get_coupling_metrics` reporting live code as unreferenced in projects with source generators; the compilation cache now serves the Solution-owned compilation for symbol analysis and keeps the generator-rerun compilation for diagnostics only. |
| Backlog sync | Close rows: [compilation-cache-generator-rerun-blinds-unused-analysis]. |
