| Field | Content |
|---|---|
| Route | deepen |
| Diagnosis | `src/RoslynMcp.Host.Stdio/Middleware/PromptBindingStageAdapter.cs:81` pre-validates each argument with strict default `JsonSerializer` options, so `"19"` for an `int` throws and becomes -32602 at `:85`. The SDK binder is not the rejecter: `WithPromptsFromAssembly()` (`Program.cs:93`) passes no options, so the SDK uses `McpJsonUtilities.DefaultOptions`, which carry `NumberHandling=AllowReadingFromString` (live probe of ModelContextProtocol.Core 2.2.0: `"19"` -> 19). The adapter is stricter than the binder it guards; `request.Arguments` needs no rewrite. `InvalidParameters(promptName)` (`:90-94`) drops `parameter.Name`, so missing (`:73`) and wrong-type (`:85`) errors never name the argument. Int-arg prompts: `consumer_impact`, `explain_error`, `guided_extract_method`, `refactor_and_validate`, `suggest_refactoring`. |
| Approach | Edit only `PromptBindingStageAdapter.cs`. (1) At `:81` deserialize with `McpJsonUtilities.DefaultOptions` (same options as the SDK binder; comment why), covering `int`/`int?` while non-numeric strings still fail. (2) Replace `InvalidParameters` with two factories taking the parameter name: `Missing required argument '<name>' for prompt '<prompt>'.` and `Invalid value for argument '<name>' of prompt '<prompt>': expected <json type>.` (type from `Nullable.GetUnderlyingType(t) ?? t`, style of `PromptShimTools.GetExpectedJsonType` at `Tools/PromptShimTools.cs:242`). Keep `McpErrorCode.InvalidParams`; NEVER echo the caller value (sanitization test asserts the sentinel stays absent). |
| Scope | Production (1): `src/RoslynMcp.Host.Stdio/Middleware/PromptBindingStageAdapter.cs`. Tests (1, extended): `tests/RoslynMcp.Tests/PromptCallErrorFilterTests.cs` — (a) wire test `integer_prompt {count:"19"}` renders `count=19` across `_protocolEras`; (b) an `int?` test prompt + string-arg wire test; (c) missing-arg message contains `'target'`; (d) wrong-type message names `'count'`, excludes the sentinel (update assertion at `:164`); (e) unit test: `PromptBindingStageAdapter.Default.Validate` with string int args for the 5 real prompts does not throw. Doc: `changelog.d/prompt-argument-binding-spec-strings-and-names.md`. |
| Tool policy | edit-only |
| Estimated context cost | 30000 |
| Risks | Fanout: all `InvalidParameters` call sites are in-file; adapter consumed only by `GetPromptErrorFilter.cs:71-74` (unchanged) and the test harness; production ripple 1 file. If the host later passes custom `serializerOptions` to `WithPromptsFromAssembly`, the adapter must follow — wire test (a) is the tripwire. Out of scope: `Tools/PromptShimTools.cs:230` also uses strict options (tool-call JSON, not spec prompt args). |
| Validation | `test_run` filter `PromptCallErrorFilterTests\|ServerDiscoveryWireTests`; repro `prompts/get consumer_impact {line:"19",column:"21"}` renders and `prompts/get review_file {}` names `filePath`; then `just ci`. |
| Performance review | N/A — correctness fix, no hot-path changes. |
| CHANGELOG category | Fixed |
| CHANGELOG entry (draft) | `prompts/get` now accepts spec-compliant string values for integer prompt arguments (e.g. `consumer_impact {line:"19"}`), and invalid-params errors name the missing or mistyped argument. |
| Backlog sync | Close rows: [prompt-argument-binding-spec-strings-and-names]. |
