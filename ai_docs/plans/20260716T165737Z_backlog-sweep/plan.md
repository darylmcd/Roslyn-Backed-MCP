# Backlog sweep plan — 20260716T165737Z_backlog-sweep

Generated by `/backlog-sweep:prepare`. 10 initiative(s) selected off `ai_docs/backlog.md` (snapshot 2026-07-14T16:05:00Z).

<!-- BSWEEP:STATUS-TABLE BEGIN — generated from state.json; do not edit by hand -->
## Status (generated)

| # | id | status | PR | rows closed |
|---|----|--------|----|-------------|
| 1 | prompt-workflows-missing-test-coverage | pending | — | prompt-workflows-missing-test-coverage |
| 2 | scaffoldingservice-god-class-decompose | pending | — | scaffoldingservice-god-class-decompose |
| 3 | analysis-services-dedup-type-traversal-helpers | pending | — | analysis-services-dedup-type-traversal-helpers |
| 4 | apply-with-verify-undo-thin-shim-extraction | pending | — | apply-with-verify-undo-thin-shim-extraction |
| 5 | core-dto-location-quartet-consolidation-primary | pending | — | — |
| 6 | workspace-fork-apply-extract-service | pending | — | workspace-fork-apply-extract-service |
| 7 | workspacetools-god-class-decomposition | pending | — | workspacetools-god-class-decomposition |
| 8 | host-tools-complexity-hotspot-cleanup | pending | — | host-tools-complexity-hotspot-cleanup |
| 9 | analyzer-catalog-untested-drift-suppression-branches | pending | — | analyzer-catalog-untested-drift-suppression-branches |
| 10 | dedupe-csharp-features-assembly-load-helper | pending | — | dedupe-csharp-features-assembly-load-helper |
<!-- BSWEEP:STATUS-TABLE END -->

## Initiatives

### 1. prompt-workflows-missing-test-coverage

| Field | Content |
|---|---|
| Diagnosis | All seven detail-file anchors resolve: `ReviewComplexity` at `src/RoslynMcp.Host.Stdio/Prompts/RoslynPrompts.AnalysisWorkflows.cs:270`, `CohesionAnalysis` at `src/RoslynMcp.Host.Stdio/Prompts/RoslynPrompts.AnalysisWorkflows.cs:318`, `ConsumerImpact` at `src/RoslynMcp.Host.Stdio/Prompts/RoslynPrompts.AnalysisWorkflows.cs:371`, `GuidedExtractMethod` at `src/RoslynMcp.Host.Stdio/Prompts/RoslynPrompts.GuidedWorkflows.cs:15`, and `RefactorAndValidate`, `FixAllDiagnostics`, and `GuidedPackageMigration` at `src/RoslynMcp.Host.Stdio/Prompts/RoslynPrompts.RefactoringWorkflows.cs:75`, `:149`, and `:199`. A repository-wide test search finds no direct invocation of any of them. `tests/RoslynMcp.Tests/PromptSmokeTests.cs:33-47` demonstrates the established direct-builder smoke-test pattern and the class already owns the shared workspace and required services. The defect is therefore missing test coverage, not missing production wiring. |
| Approach | Modify `tests/RoslynMcp.Tests/PromptSmokeTests.cs` and add independent direct smoke tests named `ReviewComplexity_Returns_Text`, `CohesionAnalysis_Returns_Text`, `ConsumerImpact_Returns_Text`, `GuidedExtractMethod_Returns_Text`, `RefactorAndValidate_Returns_Text`, `FixAllDiagnostics_Returns_Text`, and `GuidedPackageMigration_Returns_Text`. Reuse the class workspace, `FindDocumentPath` helper at `tests/RoslynMcp.Tests/PromptSmokeTests.cs:469`, and service properties exposed by `tests/RoslynMcp.Tests/TestBase.cs:47-87`. Use the known selection in `samples/SampleSolution/SampleLib/RefactoringProbe.cs:13-16` for extract/refactor prompts and the type declaration at `samples/SampleSolution/SampleLib/AnimalService.cs:5` for consumer analysis. Each test should assert one text message plus a workflow-specific marker, not merely non-null output. Leave the production prompt builders unchanged. Add `changelog.d/prompt-workflows-missing-test-coverage.md` using the repository fragment convention. |
| Scope | Production files: 0. Test files added or modified: 1 — `tests/RoslynMcp.Tests/PromptSmokeTests.cs`. Changelog fragment: `changelog.d/prompt-workflows-missing-test-coverage.md`, excluded from production and test counts. Files deleted: none. |
| Tool policy | edit-only |
| Estimated context cost | 30000 |
| Risks | The live-service calls perform complexity, cohesion, reference, diagnostic, code-action, and MSBuild package evaluation, so use the smallest sample-project filters and known fixture spans; the `RefactorAndValidate` assertion must distinguish its successful workflow body from its 20-second timeout message. The slim backlog row names six workflows, while its authoritative detail acceptance and seventh anchor also require `GuidedPackageMigration`; cover all seven. Fanout probe skipped — test-only surgical extension with no cross-cutting symbol changes. Adjacent defect, excluded from this initiative: several target templates still lead with `build_workspace` or `build_project` at `AnalysisWorkflows.cs:303`, `:356`, `:407` and `RefactoringWorkflows.cs:128`, `:184`, `:227`, contrary to the repository's `compile_check`-first policy. Recommend backlog row `prompt-workflows-fast-validation-guidance-refresh`, scoped to those two production files and one prompt regression-test file. |
| Validation | Run `mcp__roslyn__compile_check`, then `mcp__roslyn__test_run --filter "FullyQualifiedName~RoslynMcp.Tests.PromptSmokeTests"`. Verify every new test directly invokes its named `RoslynPrompts` method, receives exactly one text message, and asserts a distinctive workflow marker such as `get_code_actions`, `LCOM4`, `bulk_replace_type_preview`, `extract_method_preview`, `preview_code_action`, `code_fix_preview`, or `migrate_package_preview`. Finish with `./eng/verify-release.ps1 -Configuration Release`. |
| Performance review | N/A — test-only maintenance, no production hot-path changes. |
| CHANGELOG category | Maintenance |
| CHANGELOG entry (draft) | - **Maintenance:** Added direct `PromptSmokeTests` coverage for the seven previously uninvoked prompt workflow builders: `review_complexity`, `cohesion_analysis`, `consumer_impact`, `guided_extract_method`, `refactor_and_validate`, `fix_all_diagnostics`, and `guided_package_migration`. |
| Backlog sync | Close rows: [prompt-workflows-missing-test-coverage]. Mark obsolete: []. Update related: []. |

### 2. scaffoldingservice-god-class-decompose

| Field | Content |
|---|---|
| Diagnosis | The four files only provide a physical split: `ScaffoldingService` remains one partial type at `src/RoslynMcp.Roslyn/Services/ScaffoldingService.cs:23`, `src/RoslynMcp.Roslyn/Services/ScaffoldingService.TypePreview.cs:12`, `src/RoslynMcp.Roslyn/Services/ScaffoldingService.TestPreview.cs:12`, and `src/RoslynMcp.Roslyn/Services/ScaffoldingService.TestBatchAndFirstTestPreview.cs:12`. Concern boundaries leak across partials: batch scaffolding calls the single-test renderer at `ScaffoldingService.TestBatchAndFirstTestPreview.cs:148` and `ScaffoldingService.TestPreview.cs:486`. The hotspot also retains the CC19 `TrimUsingsToReferencedNamespaces` at `ScaffoldingService.TestPreview.cs:399`, CC18 `BuildArgExpression` at `ScaffoldingService.cs:378`, and an 11-parameter `BuildTestContent` at `ScaffoldingService.TestPreview.cs:486`. |
| Approach | Keep the public `ScaffoldingService` constructor and `IScaffoldingService` surface unchanged, but make `ScaffoldingService.cs` a thin facade that delegates `PreviewScaffoldTypeAsync`, `PreviewScaffoldTestAsync`, `PreviewScaffoldTestBatchAsync`, and `PreviewScaffoldFirstTestFileAsync` to internally composed `TypeScaffolder`, `SingleTestScaffolder`, and `BatchTestScaffolder` collaborators. Mirror the facade-owned collaborator composition in `src/RoslynMcp.Roslyn/Services/ScriptingService.cs:20-55`, avoiding new DI registrations or public constructor parameters. Convert the three existing concern partial files into those collaborator types; let the batch collaborator own both batch and first-test-file workflows while reusing a shared test renderer/support type rather than duplicating the single-test templates. Replace `BuildTestContent` parameters with a `TestScaffoldRenderRequest` record. Split `TrimUsingsToReferencedNamespaces` into tree lookup, captured-surface enumeration, and namespace-resolution helpers; split `BuildArgExpression` into collection, abstraction/test-double, and concrete-construction branches so both methods fall below CC15. Update direct helper tests to target the new internal support types and replace reflection over private `ScaffoldingService` methods with direct collaborator-level tests. |
| Scope | Production files: 4 — `src/RoslynMcp.Roslyn/Services/ScaffoldingService.cs`, `src/RoslynMcp.Roslyn/Services/ScaffoldingService.TypePreview.cs`, `src/RoslynMcp.Roslyn/Services/ScaffoldingService.TestPreview.cs`, `src/RoslynMcp.Roslyn/Services/ScaffoldingService.TestBatchAndFirstTestPreview.cs`. Test files modified: 2 — `tests/RoslynMcp.Tests/ScaffoldingIntegrationTests.cs`, `tests/RoslynMcp.Tests/ScaffoldingFirstTestFileTests.cs`. Do not modify `IScaffoldingService`, `ServiceCollectionExtensions`, or host tool wrappers; preserving those keeps this non-breaking in the public repository. |
| Tool policy | edit-only |
| Estimated context cost | 60000 |
| Risks | The collaborator boundary has multiple plausible shapes; avoid replacing one god class with circular collaborators or copy-pasted renderers. Preserve exact generated text, warnings, logger behavior, preview-token semantics, and the one-compilation-per-project batch cache at `ScaffoldingService.TestBatchAndFirstTestPreview.cs:67-94`. The fanout probe found exactly the four partial-definition files, so consumer files remain untouched only if the facade constructor and interface stay stable. Adjacent bad code: `src/RoslynMcp.Roslyn/Services/ParameterObjectService.cs:343-360` copy-pastes `ResolveFolderSegmentsForNamespace`; keep it outside this capped initiative and file a follow-up `dedupe-namespace-folder-segment-resolution` row scoped to the two production services and one path-mapping regression test. |
| Validation | Run `compile_check` for `RoslynMcp.Roslyn`; run targeted tests for `RoslynMcp.Tests.ScaffoldingIntegrationTests` and `RoslynMcp.Tests.ScaffoldingFirstTestFileTests`; exercise type, single-test, batch-test, and first-test-file preview/apply cases. Run `get_complexity_metrics` on the four production files and assert `TrimUsingsToReferencedNamespaces` and `BuildArgExpression` are both below 15. Confirm the facade still implements every `IScaffoldingService` member and DI resolves it through the unchanged registration. Finish with `./eng/verify-release.ps1 -Configuration Release`. |
| Performance review | N/A — maintenance refactor with no intended hot-path change; preserve the existing batch compilation cache and single workspace snapshot. |
| CHANGELOG category | Maintenance |
| CHANGELOG entry (draft) | Decomposed `ScaffoldingService` into focused type, single-test, and batch/first-test collaborators while preserving the public scaffolding surface and generated output. |
| Backlog sync | Close rows: [scaffoldingservice-god-class-decompose]. Mark obsolete: []. Update related: [build-test-services-swallowed-exceptions-no-logging] to remove its already-satisfied ScaffoldingService slice and retarget moved anchors; add follow-up `dedupe-namespace-folder-segment-resolution`. |

<details>
<summary>Sonnet handoff notes</summary>

**Sonnet handoff:**

- **Pattern coordinates:** keep the facade/constructor at `ScaffoldingService.cs:23-40`; mirror constructor-owned collaborator composition at `ScriptingService.cs:36-40`. Delegate the existing entry blocks at `ScaffoldingService.TypePreview.cs:14-35`, `ScaffoldingService.TestPreview.cs:14-75`, and `ScaffoldingService.TestBatchAndFirstTestPreview.cs:21-35,232-304`.
- **Hotspot seam:** place `TestScaffoldRenderRequest` and shared renderer/support beside `SingleTestScaffolder`, covering `TestPreview.cs:329-540,709-1098`. Batch must call that support directly at `TestBatchAndFirstTestPreview.cs:148-158,524,584,646,702`, never depend on `SingleTestScaffolder`. Keep the one-compilation-per-project cache at `:67-94` inside `BatchTestScaffolder`.
- **Test target:** extend `ScaffoldingIntegrationTests` (class at line 7); retarget `BuildArgExpression_InterfaceArg_WithNSubstitute_EmitsSubstituteFor` at line 221 and preserve the batch fixtures at lines 737 and 772. Extend `ScaffoldingFirstTestFileTests` (class at line 23); replace reflection tests at lines 406-470 with direct support tests and retain the end-to-end fixture at line 38.
- **Edge cases:** preserve all three framework outputs; nullable/collection/dictionary/NSubstitute constructor branches; sibling-tree-not-in-compilation fallback; nested/FQN, static, private, and inaccessible targets; property/event-accessor exclusion.
- **Negative space:** do not modify `IScaffoldingService.cs:8-52`, `ServiceCollectionExtensions.cs:94`, `ScaffoldingTools.cs:22-117,205-224`, or `TestServiceContainer.cs:226-229`.

</details>
### 3. analysis-services-dedup-type-traversal-helpers

| Field | Content |
|---|---|
| Diagnosis | The duplication spans exactly four production files. `CouplingAnalysisService.cs:121-148` recursively enumerates namespaces and arbitrarily deep nested types, while `SymbolSearchService.cs:211-227` and `ImpactSweepService.cs:397-422` independently walk the same symbol tree but stop after one nested level. ImpactSweep also applies `allowedKinds` before descending, so a matching nested class beneath a non-class parent is omitted. `CouplingAnalysisService.cs:352-365` and `ConsumerAnalysisService.cs:135-147` duplicate the same nearest-containing-`TypeDeclarationSyntax` lookup. The detail anchor `ImpactSweepService.cs:352` is stale; the live helper is at `ImpactSweepService.cs:397-422`. The existing internal `NamespaceTypeEnumeration` can be generalized in place, allowing the row to remain within Rule 3. |
| Approach | 1. In `src/RoslynMcp.Roslyn/Services/ImpactSweepService.cs`, rename/generalize `NamespaceTypeEnumeration` to `RoslynSymbolTraversal`. Replace `GetAllTypes` with one lazy depth-first iterator that traverses namespaces and arbitrarily deep nested types, applies `allowedKinds` independently to each yielded type without pruning descendants, and preserves member order by reverse-pushing siblings onto an explicit stack. Add `FindContainingType(SyntaxNode, SemanticModel, CancellationToken)` with the shared ancestor-walk body. 2. In `CouplingAnalysisService.GetCouplingMetricsAsync`, call `compilation.Assembly.GlobalNamespace.GetAllTypes()`, remove `EnumerateDeclaredTypes` and `EnumerateNestedTypes`, and route `ComputeAfferentCouplingAsync` through `RoslynSymbolTraversal.FindContainingType`. 3. In `SymbolSearchService.CollectFqnMatchesForCompilation`, call `compilation.GlobalNamespace.GetAllTypes()` and remove `EnumerateNamedTypes`. 4. In `ConsumerAnalysisService.FindConsumersAsync`, use `RoslynSymbolTraversal.FindContainingType` and remove the local duplicate. 5. Add `tests/RoslynMcp.Tests/SymbolTraversalTests.cs`, mirroring the lightweight compilation fixture pattern in `TypeNamespaceWalkerTests.cs:1-101`. |
| Scope | Production files touched (4): `src/RoslynMcp.Roslyn/Services/ImpactSweepService.cs`, `src/RoslynMcp.Roslyn/Services/CouplingAnalysisService.cs`, `src/RoslynMcp.Roslyn/Services/SymbolSearchService.cs`, `src/RoslynMcp.Roslyn/Services/ConsumerAnalysisService.cs`. Test files added/modified (1): new `tests/RoslynMcp.Tests/SymbolTraversalTests.cs`. No files deleted. The utility is generalized in its existing production file; creating a fifth helper file would violate Rule 3 and requires a split instead. |
| Tool policy | edit-only |
| Estimated context cost | 55000 |
| Risks | The canonical complete traversal expands SymbolSearch and ImpactSweep results for deeply nested types and fixes ImpactSweep's descendant-pruning behavior; preserve deterministic depth-first ordering because SymbolSearch truncates at `maxResults`. Keep enumeration lazy and avoid whole-tree materialization. Do not merge this utility with `Helpers/TypeNamespaceWalker.cs:5-41`, which collects namespaces referenced by type signatures, or `SymbolResolver.EnumerateNamedTypesAndMembers`, which additionally emits members. The stale ImpactSweep line anchor requires executor re-verification. |
| Validation | Add direct tests proving `GetAllTypes` reaches a deeply nested class beneath a non-class outer type while `allowedKinds: TypeKind.Class` excludes only nonmatching yielded types, and proving `FindContainingType` returns the nearest nested declaration and null outside a type. Run `mcp__roslyn__compile_check`; run targeted `SymbolTraversalTests`, `CouplingAnalysisTests`, `ConsumerAnalysisTests`, `IntegrationTests` symbol-navigation tests, and `CompilationCacheAdoptionTests`. Run `find_duplicated_methods` against the loaded repository and confirm the removed traversal/containing-type bodies no longer form duplicate clusters. Confirm `rg` finds no remaining private definitions named `EnumerateDeclaredTypes`, `EnumerateNestedTypes`, `EnumerateNamedTypes`, `FindContainingTopLevelType`, or service-local `FindContainingType`. Finish with `./eng/verify-release.ps1 -Configuration Release`. |
| Performance review | Hot-path refactor: the shared iterator must remain lazy with O(namespaces + named types) work and O(tree depth) stack storage. The explicit stack avoids recursive call-stack growth; reverse-pushing preserves depth-first source-member order without allocating a full result list. Existing coupling and compilation-cache performance tests must remain green. |
| CHANGELOG category | Fixed |
| CHANGELOG entry (draft) | Fixed inconsistent Roslyn analysis type traversal by sharing one complete nested-type and containing-type walker across coupling analysis, symbol search, impact sweep, and consumer analysis. |
| Backlog sync | Close rows: [analysis-services-dedup-type-traversal-helpers]. Mark obsolete: []. Update related: []. |

<details>
<summary>Sonnet handoff notes</summary>

**Sonnet handoff:**

- **Pattern coordinates:** replace the bottom-of-file helper at `ImpactSweepService.cs:397-422` in place. Preserve the arbitrary-depth preorder demonstrated by `CouplingAnalysisService.cs:121-148`; filtering must control yielding only, never descendant traversal. Preserve ordering because `SymbolSearchService.cs:144-147` truncates during enumeration.
- **Containing-type seam:** unify the identical nearest-ancestor walks at `ConsumerAnalysisService.cs:135-147` and `CouplingAnalysisService.cs:352-365`; return the first enclosing `TypeDeclarationSyntax` symbol.
- **Test target:** create `tests/RoslynMcp.Tests/SymbolTraversalTests.cs` with `[TestClass] SymbolTraversalTests`. Mirror the compilation/symbol assertion style in `TypeNamespaceWalkerTests.cs:10-33` and its `CompileAsync` fixture at `:86-100`. Add explicit tests for deep filtered traversal, deterministic preorder, nearest nested type, and null outside a type.
- **Edge cases to cover:** a class nested multiple levels beneath a struct/interface while `allowedKinds: TypeKind.Class`; sibling nested types retain declaration order; a node inside a nested record/class resolves to that nearest type, while a using or compilation-unit node resolves null.
- **Negative space:** do not create a fifth helper file. Do not merge with `Helpers/TypeNamespaceWalker.cs:5-41` (signature-namespace collection) or `Helpers/SymbolResolver.cs:318-343` (types-plus-members enumeration).

</details>

### 4. apply-with-verify-undo-thin-shim-extraction

| Field | Content |
|---|---|
| Diagnosis | The detail anchors resolve. `src/RoslynMcp.Host.Stdio/Tools/ApplyWithVerifyTool.cs:50-192` is no longer a thin endpoint: it derives project scope, captures the diagnostic baseline, applies the preview, computes introduced errors, selects five response states, and performs cancellation-safe compensation. `src/RoslynMcp.Host.Stdio/Tools/UndoTools.cs:83-122` independently owns the three-way `revert_apply_by_sequence` payload and reason-to-message mapping. The established separation is visible at `src/RoslynMcp.Host.Stdio/Tools/OrchestrationTools.cs:98-109`, where the endpoint retains gating/token dispatch and delegates workflow behavior. The backlog's “Core service” intent means moving behavior out of Host; the implementation must live in the Roslyn service layer because it depends on Roslyn-only `IPreviewStore` and `DiagnosticIdentitySet`. |
| Approach | 1. Add `src/RoslynMcp.Roslyn/Services/ApplyUndoWorkflowService.cs`, co-locating `IApplyUndoWorkflowService`, its implementation, and transport-neutral result records; mirror the interface-plus-implementation file shape at `src/RoslynMcp.Roslyn/Services/NamespaceRelocationService.cs:25-46`. Inject `IRefactoringService`, `ICompileCheckService`, `IUndoService`, `IPreviewStore`, and `ILogger<ApplyUndoWorkflowService>`. Move project-filter derivation, pre/post diagnostic identity diffing, apply outcome selection, rollback, and cancellation compensation into `ApplyWithVerifyAsync`; move `RevertBySequenceAsync` invocation plus success, dependency-blocked, and reason-mapped result shaping into a second method. Mark nullable response fields with `JsonIgnoreCondition.WhenWritingNull` so the current JSON field-presence contract remains byte-shape compatible. 2. Register the interface and implementation as one singleton in `src/RoslynMcp.Roslyn/ServiceCollectionExtensions.cs`. 3. Reduce `src/RoslynMcp.Host.Stdio/Tools/ApplyWithVerifyTool.cs` to token-to-workspace lookup, `RunWriteAsync`, service invocation, and serialization. 4. Reduce only `UndoTools.RevertApplyBySequence` in `src/RoslynMcp.Host.Stdio/Tools/UndoTools.cs` to validation, gating, service invocation, and serialization; leave the separately scoped `RevertLastApply` behavior unchanged. 5. Add `changelog.d/apply-with-verify-undo-thin-shim-extraction.md`. |
| Scope | Production files touched (4): new `src/RoslynMcp.Roslyn/Services/ApplyUndoWorkflowService.cs`; `src/RoslynMcp.Roslyn/ServiceCollectionExtensions.cs`; `src/RoslynMcp.Host.Stdio/Tools/ApplyWithVerifyTool.cs`; `src/RoslynMcp.Host.Stdio/Tools/UndoTools.cs`. Test files added or modified (3): `tests/RoslynMcp.Tests/ApplyWithVerifyCancellationAndScopeTests.cs`; `tests/RoslynMcp.Tests/Top10V2RegressionTests.cs`; new `tests/RoslynMcp.Tests/ApplyUndoWorkflowServiceTests.cs`. Changelog fragment excluded from production/test counts. No files deleted. |
| Tool policy | edit-only |
| Estimated context cost | 60000 |
| Risks | Preserve all existing `apply_with_verify` statuses, messages, applied-file values, diagnostic rows, and pre/post counts; the extraction must not add compile passes or consume the preview token during project-scope inspection. Preserve `revert_apply_by_sequence` optional-field behavior: success omits reason/message/blockers, dependency-blocked includes blockers, and other failures omit blockers. The hardcoded 30-second cancellation revert budget remains intentionally out of scope and is already tracked by `apply-with-verify-revertbudget-configurable`; update that detail file's anchor to the new service. Adjacent duplication remains in `EditService.RunVerifyAndMaybeRevertAsync` at `src/RoslynMcp.Roslyn/Services/EditService.cs:731-821`; recommend backlog row `editservice-apply-verify-workflow-consolidation`, scoped to that file, the new workflow service, `VerifyOutcomeDto.cs`, and one regression-test file. Also update existing row `refactor-services-non-atomic-write-rollback` to cover the swallowed cleanup failure at `CompositeApplyOrchestrator.cs:154-157`; do not fold either concern into this extraction. |
| Validation | Extend `ApplyWithVerifyCancellationAndScopeTests` to exercise the extracted service while retaining the fresh-token cancellation, logged revert failure, pre-apply cancellation, and zero/one/many-project filter assertions. Update `Top10V2RegressionTests` direct endpoint calls and retain the applied/rolled-back and pre-existing-error regressions. In `ApplyUndoWorkflowServiceTests`, assert DI resolution plus all three sequence-revert shapes, exact reason/message mapping, and absence of nullable fields when inapplicable; also pin the property sets for `apply_failed`, `applied`, `applied_with_errors`, `rolled_back`, and `rollback_failed`. Run `mcp__roslyn__compile_check`, then targeted `mcp__roslyn__test_run` filters for the three test classes; use `dotnet build RoslynMcp.slnx -c Release -p:TreatWarningsAsErrors=true` and targeted `dotnet test` if the MCP server is unavailable. Finish with `./eng/verify-release.ps1 -Configuration Release`. |
| Performance review | No additional compile checks, preview retrievals, or workspace reloads are allowed. The refactor preserves the current two compile-check legs and diagnostic-set complexity; no hot-path expansion is intended. |
| CHANGELOG category | Maintenance |
| CHANGELOG entry (draft) | - **Maintenance:** Moved `apply_with_verify` orchestration and `revert_apply_by_sequence` result shaping out of Host tool methods into a reusable Roslyn workflow service without changing tool response schemas. |
| Backlog sync | Close rows: [apply-with-verify-undo-thin-shim-extraction]. Mark obsolete: []. Update related: [apply-with-verify-revertbudget-configurable, refactor-services-non-atomic-write-rollback]. |

<details>
<summary>Sonnet handoff notes</summary>

**Sonnet handoff:**

- **Pattern coordinates:** co-locate interface and implementation like `NamespaceRelocationService.cs:25-60`; register beside peer workflow services at `ServiceCollectionExtensions.cs:104-110`. Keep the Host boundary shaped like `OrchestrationTools.cs:98-109`.
- **Service seam:** move `ApplyWithVerifyTool.cs:52-190`; retain token lookup/stale handling and gating at `:45-50`. Move only `UndoTools.cs:83-121`; leave validation at `:73-80` and `RevertLastApply` at `:16-60` unchanged.
- **Test targets:** extend `ApplyWithVerifyCancellationAndScopeTests` (class `:33`), mirroring fresh-token cancellation at `:38` and project filtering at `:165`. Update `Top10V2RegressionTests` calls at `:154-156` and `:210-212`. In new `ApplyUndoWorkflowServiceTests.cs`, mirror DI setup from `ServiceCollectionExtensionsTests.cs:16-38` and the three sequence outcomes from `UndoServiceTests.cs:23-148`.
- **Contract edges:** pin exact property sets for all five apply statuses. Result records need `JsonIgnore(WhenWritingNull)` because `JsonDefaults` does not globally omit nulls. Preserve two compile checks, non-consuming preview retrieval, fresh-token compensation, exact reason messages, and the three sequence optional-field shapes. Do not modify `TestBase` or `TestServiceContainer`.

</details>

### 5. core-dto-location-quartet-consolidation-primary

| Field | Content |
|---|---|
| Diagnosis | The cited quartets are live at `src/RoslynMcp.Core/Models/SymbolDto.cs:14-18`, `src/RoslynMcp.Core/Models/DiagnosticDto.cs:11-15`, and `src/RoslynMcp.Core/Models/TypeUsageDto.cs:35-39`. However, `LocationDto` at `src/RoslynMcp.Core/Models/LocationDto.cs:6-14` requires a non-null path and coordinates while `SymbolDto` and `DiagnosticDto` deliberately support metadata, workspace, scripting, and snippet results with partial or absent locations. These DTOs feed stable tools at `src/RoslynMcp.Host.Stdio/Catalog/ServerSurfaceCatalog.Symbols.cs:7-18` and `src/RoslynMcp.Host.Stdio/Catalog/ServerSurfaceCatalog.Analysis.cs:7-13,46`; direct removal or nesting is prohibited by `.github/copilot-instructions.md:77` and requires the deprecation and major-version process in `docs/release-policy.md:17-29`. The construction-site probe found 9 production files and 5 test files constructing the three DTOs; with the 3 definition files, a direct required-constructor replacement has a minimum 12-production-file fanout before property readers, so it cannot close this row within Rules 3-4. |
| Approach | Implement the bounded additive migration seam and keep the removal row open. In `src/RoslynMcp.Core/Models/LocationDto.cs`, add an internal `CreateOrNull` factory that returns a `LocationDto` only when the nullable path-and-span inputs form a complete source location. In `src/RoslynMcp.Core/Models/SymbolDto.cs` and `src/RoslynMcp.Core/Models/DiagnosticDto.cs`, add a computed nullable `Location` property using that factory. In `src/RoslynMcp.Core/Models/TypeUsageDto.cs`, add a computed non-null `Location` property populated with the existing containing-member, preview, and classification data. Mark all three composed properties with `[JsonIgnore]`; retain the current positional constructors and five flat properties unchanged so stable wire shape, source construction, record equality, and `with` expressions such as `BuildService.cs:122` remain intact. Add `tests/RoslynMcp.Tests/CoreDtoLocationCompositionTests.cs` covering complete, metadata-only, file-only, and no-location cases plus wire-shape preservation. Add `changelog.d/core-dto-location-quartet-consolidation-primary.md`. |
| Scope | Production files touched: 4 — `src/RoslynMcp.Core/Models/LocationDto.cs`, `src/RoslynMcp.Core/Models/SymbolDto.cs`, `src/RoslynMcp.Core/Models/DiagnosticDto.cs`, `src/RoslynMcp.Core/Models/TypeUsageDto.cs`. Test files added or modified: 1 — new `tests/RoslynMcp.Tests/CoreDtoLocationCompositionTests.cs`. Changelog fragment excluded from production count. Files deleted: none. |
| Tool policy | edit-only |
| Estimated context cost | 50000 |
| Risks | This is the compatibility-required migration phase, not the breaking removal; the backlog row must remain open. `LocationDto` is richer and stricter than a nullable source span, so `SymbolDto.Location` and `DiagnosticDto.Location` must be null for incomplete locations while their legacy flat fields remain authoritative. Do not encode missing coordinates as zero. A future major-version removal needs an ADR, migration note, at least one minor release of deprecation, and a split across constructor/property-consumer groups; retarget this existing row to an optional `SourceSpanDto` abstraction if nested partial diagnostic locations are required. The direct-removal fanout probe found at least 12 production files and 5 test files, so attempting full replacement here would be fanout-oversize. Roslyn MCP was declared but unavailable in this agent runtime; anchors and counts were verified with `rg` and direct reads. |
| Validation | Add tests asserting complete `SymbolDto` and `DiagnosticDto` spans project to equivalent `LocationDto` values, metadata/no-location and file-only diagnostics return `Location == null` without losing legacy flat values, and `TypeUsageDto.Location` carries span, containing member, preview, and classification. Serialize all three with `JsonDefaults.Indented` and assert `location` is absent while `filePath`, `startLine`, `startColumn`, `endLine`, and `endColumn` remain unchanged. Run `mcp__roslyn__compile_check`, then `mcp__roslyn__test_run --filter "FullyQualifiedName~RoslynMcp.Tests.CoreDtoLocationCompositionTests"` plus `DiagnosticIdentitySetTests`, `ValidateWorkspaceMarkdownFormatterTests`, and `WorkspaceValidationOverallStatusTests`. Finish with `./eng/verify-release.ps1 -Configuration Release`. |
| Performance review | High-volume stable DTO serialization remains byte-shape compatible because `Location` is ignored by JSON. The computed DTO allocates only when an in-process caller explicitly reads it; avoid eager materialization or serialization of the nested view during this migration phase. |
| CHANGELOG category | Added |
| CHANGELOG entry (draft) | Added non-serialized composed `Location` views to `SymbolDto`, `DiagnosticDto`, and `TypeUsageDto` while preserving their stable flat location fields and JSON response shapes. |
| Backlog sync | Close rows: []. Mark obsolete: []. Update related: [core-dto-location-quartet-consolidation-primary] to record the additive migration seam and retain the deprecation-plus-major-removal follow-up; leave core-dto-location-quartet-consolidation-secondary blocked. |

<details>
<summary>Sonnet handoff notes</summary>

**Sonnet handoff:**

- **Pattern coordinates:** expand `LocationDto.cs:6-14` into a record body with internal `CreateOrNull` over the five nullable legacy fields. Return null unless path and all four coordinates are present; construct with the three trailing context fields null. Presence-check only—never coalesce missing coordinates to zero.
- **DTO seams:** expand the terminating records at `SymbolDto.cs:6-27`, `DiagnosticDto.cs:6-15`, and `TypeUsageDto.cs:34-42`. Add `System.Text.Json.Serialization` and expression-bodied `[JsonIgnore]` properties with no backing field/init accessor: nullable factory projections for Symbol/Diagnostic; a non-null `new LocationDto(...)` for TypeUsage carrying `ContainingMember`, `PreviewText`, and `Classification.ToString()`.
- **Test target:** create `tests/RoslynMcp.Tests/CoreDtoLocationCompositionTests.cs` with `[TestClass] CoreDtoLocationCompositionTests`; do not extend another class. Mirror named-argument DTO setup from `DiagnosticIdentitySetTests.cs:23-39` and wire-absence-plus-legacy-presence assertions from `HostProcessMetadataTests.cs:323-342`.
- **Contract edges:** test complete spans; all-null metadata/no-location; file-only and any partially missing span returning null; TypeUsage context/classification projection. Serialize each via `JsonDefaults.Indented` (`JsonDefaults.cs:8-13`): `location` must be absent while all five camel-case legacy keys remain present, including explicit nulls.
- **Negative space:** do not touch `SymbolMapper.cs:123-134`, constructor call sites, `JsonDefaults.cs`, `PropertyWriteDto.cs:21-29`, or `TypeMutationDto.cs:26-32`; those are existing materialization/configuration or the blocked secondary migration.

</details>

### 6. workspace-fork-apply-extract-service

| Field | Content |
|---|---|
| Diagnosis | `ValidationBundleTools.WorkspaceForkApply` at `src/RoslynMcp.Host.Stdio/Tools/ValidationBundleTools.cs:127-251` currently owns preview-token validation, fork locking, directory copy and replay, restore, workspace load and close, validation and explicit-test execution, retention, serialization, and exception cleanup. Its supporting state and helpers span `ValidationBundleTools.cs:28-59` and `:273-725`, so transport code owns the domain workflow. `RestoreForkAsync` at `ValidationBundleTools.cs:499-562` also duplicates the process creation, `MSBUILDDISABLENODEREUSE`, cancellation kill, and bounded stream-drain behavior already centralized by `DotnetCommandRunner.RunAsync` and `CreateStartInfo` at `src/RoslynMcp.Roslyn/Helpers/DotnetCommandRunner.cs:81-159`. Because `IPreviewStore` exposes Roslyn `Solution` snapshots, the implementation belongs in the Roslyn layer rather than Core; mirror the interface, result DTO, and implementation co-location used by `src/RoslynMcp.Roslyn/Services/InterfaceMemberRemovalOrchestrator.cs:18-44`. The detail file's `RequireResolvedWorkspaceId` acceptance is a separate duplication at `CompileCheckTools.cs:67-73` and `SymbolTools.cs:177-184`; combining it would exceed Rule 3. |
| Approach | 1. Add `src/RoslynMcp.Roslyn/Services/WorkspaceForkApplyOrchestrator.cs` containing `IWorkspaceForkApplyOrchestrator`, a transport-neutral `WorkspaceForkApplyResultDto`, the orchestrator implementation, and its internal executable-runner seam. Move fork locking, preview validation, copy and replay, restore, load and validation, retention, cleanup, TTL, secret filtering, and environment-resolution helpers into `ApplyAsync`. 2. Extend `DotnetCommandRunner.RunAsync` and `CreateStartInfo` with an executable-path overload while preserving the existing `"dotnet"` overload; have fork restore pass `ROSLYNMCP_FORK_DOTNET_PATH`, use a linked timeout token, and translate only the internal timeout cancellation to `TimeoutException`. 3. Update `RoslynMcp.Roslyn.ServiceCollectionExtensions.AddRoslynServices` so the same `DotnetCommandRunner` singleton backs the existing `IDotnetCommandRunner`, the executable-runner seam, and `IWorkspaceForkApplyOrchestrator`. 4. Reduce `ValidationBundleTools.WorkspaceForkApply` to retention/input forwarding, `gate.RunWriteAsync`, one orchestrator call, and DTO serialization while preserving `applyStalenessPolicy: false` and the public MCP schema. 5. Replace reflection-bound helper tests with direct orchestrator tests. Before closing this row, create follow-up row `require-resolved-workspace-id-shared-helper`, scoped to `ToolDispatch.cs`, `CompileCheckTools.cs`, `SymbolTools.cs`, and `WorkspaceIdOptionalSurfaceTests.cs`. |
| Scope | Production files touched (4): `src/RoslynMcp.Host.Stdio/Tools/ValidationBundleTools.cs`, new `src/RoslynMcp.Roslyn/Services/WorkspaceForkApplyOrchestrator.cs`, `src/RoslynMcp.Roslyn/Helpers/DotnetCommandRunner.cs`, `src/RoslynMcp.Roslyn/ServiceCollectionExtensions.cs`. Test files modified (3): `tests/RoslynMcp.Tests/Workspace/WorkspaceForkApplyTests.cs`, `tests/RoslynMcp.Tests/ValidationBundleToolsTests.cs`, `tests/RoslynMcp.Tests/WorkspaceForkApplyCancellationTests.cs`. Changelog fragment: `changelog.d/workspace-fork-apply-extract-service.md`. No files deleted. |
| Tool policy | edit-only |
| Estimated context cost | 60000 |
| Risks | Preserve source-root serialization, cancellation-independent exception cleanup, retained failed-fork loadability, secret exclusions, TTL behavior, and the distinction between caller cancellation and restore timeout. The production fanout probe found `WorkspaceForkApply` only in `ValidationBundleTools.cs`; the remaining three production files are the new orchestrator, command-runner support, and DI wiring, giving an estimated fanout of four. The existing reflection tests are brittle implementation coupling and must be converted while this row moves those helpers. Adjacent defects are already tracked: `forkapplylocks-semaphore-bound` for the unbounded semaphore map, `fork-secret-exclusion-extend` for denylist gaps, and `fork-ttl-nonfinite-guard` for non-finite environment values; retarget their anchors after the move rather than expanding this initiative. The duplicate workspace-id guard is intentionally split into the bounded follow-up row before this row closes. |
| Validation | Add direct orchestrator tests for cancellable copy and delete, normalized source-root lock sharing, secret exclusion, TTL sweep, retention decisions, cleanup warnings, caller cancellation, internal restore timeout, and forwarding of a configured dotnet executable path. Update `WorkspaceForkApplyTests` to exercise the DI-resolved orchestrator through the unchanged tool wrapper and retain the success and failed-fork integration cases. Assert the generated MCP parameter schema for `workspace_fork_apply` is unchanged. Run `mcp__roslyn__compile_check`; run targeted tests separately for `WorkspaceForkApplyTests`, `WorkspaceForkApplyCancellationTests`, and `ValidationBundleToolsTests`; run complexity metrics and confirm the remaining tool method is a thin shim. Finish with `./eng/verify-release.ps1 -Configuration Release`. |
| Performance review | N/A — maintenance refactor, no intended hot-path change; preserve one directory-copy pass, one preview replay, one restore, and bounded output draining. |
| CHANGELOG category | Maintenance |
| CHANGELOG entry (draft) | - **Maintenance:** Extracted `workspace_fork_apply` orchestration into a Roslyn service and routed fork restores through the shared bounded command runner without changing the tool wire contract. |
| Backlog sync | Close rows: [workspace-fork-apply-extract-service]. Mark obsolete: []. Update related: [forkapplylocks-semaphore-bound, fork-secret-exclusion-extend, fork-ttl-nonfinite-guard] to retarget moved anchors. Add `require-resolved-workspace-id-shared-helper` before closing this row. |

<details>
<summary>Sonnet handoff notes</summary>

**Sonnet handoff:**

- **Pattern coordinates:** co-locate interface, result DTO, and implementation like `InterfaceMemberRemovalOrchestrator.cs:18-44`. Extend the bounded runner at `DotnetCommandRunner.cs:81-160`: existing overloads must still select `"dotnet"` while the new internal executable-runner seam supplies `FileName`. Register the concrete singleton plus interface aliases using the pattern at `ServiceCollectionExtensions.cs:68-69`.
- **Extraction seam:** replace only `ValidationBundleTools.cs:127-251`; move fork state/helpers at `:28-59,273-725`. Leave validation endpoints at `:61-121` and response rendering at `:263-270` untouched.
- **Test target:** replace reflection fields/tests in `WorkspaceForkApplyCancellationTests.cs:21-33,53-151` with direct internal orchestrator tests. Retarget the secret/copy/TTL/environment fixtures in `ValidationBundleToolsTests.cs:139-318`. Preserve the wrapper integration cases in `Workspace/WorkspaceForkApplyTests.cs:20-109`.
- **Edge cases to cover:** changed/added/removed preview files and outside-root rejection; all four retention modes; cleanup warnings and cancellation-independent exception cleanup; caller cancellation remains `OperationCanceledException` while only the linked restore deadline becomes `TimeoutException`; configured executable path reaches the runner.
- **Negative space:** do not modify `Core/Services/IDotnetCommandRunner.cs`, `ToolDispatch.cs`, `CompileCheckTools.cs`, or `SymbolTools.cs`. Do not fold in the tracked semaphore-bound, non-finite-TTL, or broader secret-exclusion fixes.

</details>

### 7. workspacetools-god-class-decomposition

| Field | Content |
|---|---|
| Diagnosis | Both anchors are live. `WorkspaceTools` begins at `src/RoslynMcp.Host.Stdio/Tools/WorkspaceTools.cs:18`; the readiness and support-bundle endpoints at `:391-526` perform target resolution, service probes, and serialization, while their verdict, limits, workflow, ledger, and DTO-construction helpers occupy `:703-1107`. The same 1,165-line type also owns lifecycle/process cleanup at `:30-323`, status and source access at `:325-382` and `:528-623`, and the change ledger endpoint at `:1149-1164`. The root cause is that the MCP transport type also owns two substantial report-building workflows. The fanout probe found every private report-helper symbol only in this file; retaining the two attributed public endpoint signatures means existing consumers do not change. Moving the endpoint bodies plus helper block projects `WorkspaceTools.cs` to roughly 650 lines. The row's declared size `S` underestimates the required existing-file plus new-file extraction. |
| Approach | Add `src/RoslynMcp.Host.Stdio/Tools/WorkspaceReportBuilder.cs` with an internal static `WorkspaceReportBuilder`. Move the readiness verdict constants, support-bundle cap constants, the complete bodies currently behind `GetWorkspaceReadinessReport` and `GetWorkspaceSupportBundle`, and the report-helper block at `WorkspaceTools.cs:703-1107` into `BuildReadinessReportAsync` and `BuildSupportBundleAsync`. Keep both `[McpServerTool]` methods, their parameter defaults, metadata, and output-schema references in `WorkspaceTools.cs` as thin delegates, mirroring the endpoint delegation at `src/RoslynMcp.Host.Stdio/Tools/OrchestrationTools.cs:98-109`; mirror the focused internal-static-helper shape at `src/RoslynMcp.Host.Stdio/Prompts/PromptMessageBuilder.cs:11`. Leave lifecycle, source-access, and `ResolveOptionalWorkspaceId` in `WorkspaceTools`; the resolver is consumed by `src/RoslynMcp.Host.Stdio/Middleware/StructuredCallToolFilter.cs:411-437`. Make only the top-level DTO assembly methods internal for direct tests and keep subordinate classification/formatting helpers private; `src/RoslynMcp.Host.Stdio/RoslynMcp.Host.Stdio.csproj:47` already grants test internals access. Add `tests/RoslynMcp.Tests/WorkspaceReportBuilderTests.cs` and `changelog.d/workspacetools-god-class-decomposition.md`. |
| Scope | Production files: 2 — `src/RoslynMcp.Host.Stdio/Tools/WorkspaceTools.cs`, new `src/RoslynMcp.Host.Stdio/Tools/WorkspaceReportBuilder.cs`. Test files added or modified: 1 — new `tests/RoslynMcp.Tests/WorkspaceReportBuilderTests.cs`. Changelog fragment excluded from production and test counts. No DTO, catalog, middleware, DI-registration, or existing test-file changes; no files deleted. |
| Tool policy | edit-only |
| Estimated context cost | 50000 |
| Risks | Preserve the public repository's stable tool surface exactly: do not move the attributed endpoints to another `[McpServerToolType]`, rename parameters, change optional defaults, or alter JSON field presence. Preserve one source-generated-document probe for readiness and one status, drift, diagnostics, and ledger probe for support bundles; retain cap clamping, newest-change ordering, action deduplication, server/catalog versions, and build-versus-restore precedence. Move `HasBuildRequiredDiagnostic` verbatim because its divergence from `WorkspaceStatusDto.BuildRequired` is already tracked by `restore-build-required-classifier-consistency`; retarget that row's anchor after extraction rather than fixing it here. Fanout is two production files: all current helper definitions and uses are confined to `WorkspaceTools.cs`, plus the new destination file. Observed bad test code: `WorkspaceReadinessReportTests.cs:181-243` and `WorkspaceSupportBundleTests.cs:249-312` duplicate `FakeGate` and `FakeWorkspaceManager`; recommend a bounded `workspace-report-test-double-dedup` backlog row covering one new test-infrastructure helper plus those two test files. |
| Validation | Add direct builder tests for ready and no-target readiness DTOs, verdict/workflow selection, support-bundle clean, changed, stale, and attention-needed statuses, newest-change and drift caps, and no-target recovery actions. Run `mcp__roslyn__compile_check`, then targeted tests for `WorkspaceReportBuilderTests`, `WorkspaceReadinessReportTests`, `WorkspaceSupportBundleTests`, `WorkspaceReadinessReportIntegrationTests`, `WorkspaceToolsIntegrationTests`, and `SurfaceCatalogTests`. Confirm existing readiness/support tests remain unmodified. Verify `(Get-Content src/RoslynMcp.Host.Stdio/Tools/WorkspaceTools.cs).Count` is below 700 and `rg` finds report-construction implementations only in `WorkspaceReportBuilder.cs`, while the two attributed endpoint methods and `ResolveOptionalWorkspaceId` remain in `WorkspaceTools.cs`. Finish with `./eng/verify-release.ps1 -Configuration Release`. |
| Performance review | N/A — maintenance extraction with no intended hot-path change; preserve the current probe count, lazy collection behavior, and capped report payloads. |
| CHANGELOG category | Maintenance |
| CHANGELOG entry (draft) | - **Maintenance:** Extracted workspace readiness and support-bundle assembly into a focused report builder while preserving the existing `workspace_readiness_report` and `workspace_support_bundle` contracts. |
| Backlog sync | Close rows: [workspacetools-god-class-decomposition]. Mark obsolete: []. Update related: [restore-build-required-classifier-consistency] to retarget the moved classifier anchor. Add follow-up: [workspace-report-test-double-dedup]. |

<details>
<summary>Sonnet handoff notes</summary>

**Sonnet handoff:**

- **Pattern/hotspot seam:** keep the attributed wrappers at `WorkspaceTools.cs:383-448` and `:451-525`; reduce them to thin delegates like `OrchestrationTools.cs:98-109`. Shape the new internal static type like `PromptMessageBuilder.cs:11-20`. Move report constants `WorkspaceTools.cs:21-28` and report construction `:703-1107`; leave `AutoPrewarmProjectThreshold` at `:20`. Only `BuildReadinessReportAsync` and `BuildSupportBundleAsync` should be internal; subordinate helpers stay private.
- **Test target:** create `WorkspaceReportBuilderTests.cs`; do not extend the existing endpoint suites. Mirror readiness cases at `WorkspaceReadinessReportTests.cs:14-40,54-76` and support cases at `WorkspaceSupportBundleTests.cs:34-52,76-132,135-224`.
- **Edge cases to cover:** no/one/multiple/unknown workspace; source-generated probe failure remains a limitation; readiness precedence is build before restore before analyzer-limited; support status precedence is stale before attention-needed before changed/clean; clamp negative and oversized caps; preserve newest-ledger ordering and action deduplication. Assert one readiness generated-document probe and one support status/drift/diagnostics/ledger probe.
- **Negative space:** do not move `ResolveOptionalWorkspaceId` at `WorkspaceTools.cs:691-700`—`StructuredCallToolFilter.cs:411-437` consumes it. Do not alter endpoint attributes/defaults, catalog entries, DTOs, DI, lifecycle methods, `project_graph`/source access at `WorkspaceTools.cs:528-623`, or `workspace_changes` at `:1149-1164`.

</details>

### 8. host-tools-complexity-hotspot-cleanup

| Field | Content |
|---|---|
| Diagnosis | All four anchors are live. `ServerTools.GetServerInfo` at `src/RoslynMcp.Host.Stdio/Tools/ServerTools.cs:99-192` combines assembly-version resolution, semver comparison, workspace hints, catalog/registration counts, connection state, update-state mapping, and serialization; its branch-bearing conditionals produce the reported CC 11. `ClientRootPathValidator.IsPathUnderAnyRoot` at `src/RoslynMcp.Host.Stdio/Tools/ClientRootPathValidator.cs:119-163` nests root enumeration, optional parent widening, drive-root exclusion, normalization, and exact/prefix matching, producing CC 11 and three nesting levels. `PromptShimTools.BuildParameterValuesAsync` at `src/RoslynMcp.Host.Stdio/Tools/PromptShimTools.cs:112-198` is synchronously implemented despite its async signature and combines JSON parsing, root validation, required-parameter discovery, DI resolution, cancellation injection, defaults, and per-value deserialization. `ToolErrorHandler.ClassifyError` at `src/RoslynMcp.Host.Stdio/Tools/ToolErrorHandler.cs:230-306` combines four precedence-sensitive stages: wrapped/raw binding classification, reload-race override, registered-handler dispatch, and internal-error fallback. The shared root cause is concern aggregation inside otherwise thin Host shims, not cross-file coupling. |
| Approach | Modify only the four anchored Host files and keep every existing public/internal signature and wire contract stable. In `ServerTools.cs`, mirror the focused builder pattern of `BuildConnection` at `:48-70`: extract running-version resolution, workspace-count hint creation, surface-count construction, and update-block construction into small private helpers, then leave `GetServerInfo` as orchestration plus serialization; preserve the current ordering of `GetLatestVersion`, `LastCheckStatus`, and `LastCheckedAt`. In `ClientRootPathValidator.cs`, extract a lazy allowed-root enumerator, a one-level non-drive-root parent helper, and a single-root exact/prefix matcher so `IsPathUnderAnyRoot` becomes an empty-input guard plus `Any` over normalized candidates. In `PromptShimTools.cs`, replace the no-op-async private helper with synchronous `BuildParameterValues`; extract JSON-object parsing, missing-required discovery, per-parameter resolution, and deserialization/error translation while preserving resolution precedence: cancellation token, DI service, supplied JSON, default value, then required-parameter error. In `ToolErrorHandler.cs`, mirror `TryClassifyBindingLike` at `:317-356` with focused `TryClassifyBindingFailure`, `TryClassifyReloadRace`, and `TryClassifyRegisteredError` helpers plus an internal-error factory; keep classification precedence and message text unchanged. Modify `tests/RoslynMcp.Tests/PromptSmokeTests.cs` with a successful `get_prompt_text` regression using `dead_code_audit` to exercise DI injection, supplied JSON, omitted optional default, cancellation-token injection, prompt invocation, and response serialization; current direct shim tests at `:157-258` cover only failure paths. Add `changelog.d/host-tools-complexity-hotspot-cleanup.md`. |
| Scope | Production files touched: 4 — `src/RoslynMcp.Host.Stdio/Tools/ServerTools.cs`, `src/RoslynMcp.Host.Stdio/Tools/ClientRootPathValidator.cs`, `src/RoslynMcp.Host.Stdio/Tools/PromptShimTools.cs`, `src/RoslynMcp.Host.Stdio/Tools/ToolErrorHandler.cs`. Test files added or modified: 1 — `tests/RoslynMcp.Tests/PromptSmokeTests.cs`. Changelog fragment excluded from production/test counts. Files deleted: none. |
| Tool policy | edit-only |
| Estimated context cost | 60000 |
| Risks | Do not merely move each hotspot into one equally complex replacement helper; measure extracted helpers as well as the four original methods. Preserve `GetServerInfo`'s two independent workspace snapshots and its single consume-once `BuildConnection` call, because consolidating them would change concurrent-load and prior-host metadata behavior. Preserve root widening at exactly one level, drive-root exclusion, case-insensitive comparison, trailing-separator handling, and prefix-trap rejection. Preserve prompt parameter error wording, `JsonDocument` disposal, service/default/cancellation precedence, and reflection invocation behavior. Preserve ToolErrorHandler's binding-before-reload-before-dictionary-before-fallback ordering and dictionary iteration order. Adjacent performance debt is already tracked by `host-tools-prompt-reflection-and-path-io-perf`: do not fold prompt reflection caching or `ResolvePath` synchronous parent-directory I/O into this capped initiative; retarget that row's anchors after line movement. |
| Validation | Run `mcp__roslyn__get_complexity_metrics` for each production file and verify `GetServerInfo`, `IsPathUnderAnyRoot`, `BuildParameterValues`, and `ClassifyError` each report cyclomatic complexity at most 8, with no extracted helper exceeding 8. Run `mcp__roslyn__compile_check`. Run targeted `mcp__roslyn__test_run` filters separately for `ServerInfoUpdateLatestTests`, `ServerHeartbeatTests`, `HostProcessMetadataTests`, `ClientRootPathValidatorTests`, `PromptSmokeTests`, `ToolErrorHandlerParameterValidationTests`, `ToolErrorHandlerWorkspaceReloadRaceTests`, `WorkspaceReloadedNotFoundConflationTests`, and `ErrorResponseObservabilityTests`. Confirm the new prompt-shim success test returns the expected prompt name, parameter schema, and text message. Finish with `./eng/verify-release.ps1 -Configuration Release`. |
| Performance review | Hot-path neutrality required: helper extraction must not add catalog/reflection scans, filesystem probes, root-list materialization, version-provider calls, or handler-dictionary traversals. Keep path matching O(number of sanctioned roots) and error dispatch to one registered-handler walk. Separate reflection-caching and path-I/O improvements remain in `host-tools-prompt-reflection-and-path-io-perf`. |
| CHANGELOG category | Maintenance |
| CHANGELOG entry (draft) | - **Maintenance:** Reduced cyclomatic complexity in `server_info`, client-root matching, prompt parameter binding, and tool-error classification by extracting focused helpers while preserving existing MCP responses, security checks, and error categories. |
| Backlog sync | Close rows: [host-tools-complexity-hotspot-cleanup]. Mark obsolete: []. Update related: [host-tools-prompt-reflection-and-path-io-perf] to retarget moved anchors. |

<details>
<summary>Sonnet handoff notes</summary>

**Sonnet handoff:**

- **Server seam:** keep `BuildConnection` at `ServerTools.cs:48-70`. Reduce `GetServerInfo` (`:99-192`) by extracting version resolution (`:103-105`), workspace hint creation (`:130-132`), surface counts (`:140-153`), and update construction (`:110-120,173-190`). Preserve two independent `ListWorkspaces()` snapshots (`:50,108`), one consume-once `BuildConnection` call (`:138`), and version-provider read order: latest, status, timestamp.
- **Path seam:** reduce `ClientRootPathValidator.IsPathUnderAnyRoot` (`:119-163`) to the empty guard plus lazy `Any`; extract allowed-root enumeration (`:126-144`), one-level non-drive-root parent widening, and exact/separator-bounded prefix matching (`:146-159`). Do not alter `ValidatePathAgainstRootsAsync` or `ResolvePath` (`:168-215`).
- **Prompt seam:** replace `BuildParameterValuesAsync` (`PromptShimTools.cs:112-198`) with synchronous `BuildParameterValues`; update only its call at `:44`. Extract JSON-object parsing, aggregate missing-required discovery, single-parameter resolution, and deserialization translation. Preserve precedence: cancellation token → DI service → supplied JSON → default → required error, plus exact messages and `JsonDocument` disposal.
- **Error seam:** split `ToolErrorHandler.ClassifyError` (`:230-306`) into binding classification (`:249-256`), reload-race classification (`:280-288`), registered-handler dispatch (`:291-295`), and internal fallback (`:297-305`). Preserve binding-before-reload-before-dictionary-before-fallback precedence, `WorkspaceEvictedException`/confirmed-not-found exclusions, dictionary iteration order, and message text.
- **Prompt regression:** add a `PromptSmokeTests` success case before the failure block at `:158-258`. Register a capturing `IUnusedCodeAnalyzer` fake, invoke `dead_code_audit` with only `workspaceId` and a non-default token, then assert captured workspace/token, null `ProjectFilter`, `promptName`, two-entry parameter schema, and one user message containing `remove_dead_code_preview`.
- **Test targets:** run separate class filters for `ServerInfoUpdateLatestTests`, `ServerHeartbeatTests`, `HostProcessMetadataTests`, `ClientRootPathValidatorTests`, `PromptSmokeTests`, `ToolErrorHandlerParameterValidationTests`, `ToolErrorHandlerWorkspaceReloadRaceTests`, `WorkspaceReloadedNotFoundConflationTests`, and `ErrorResponseObservabilityTests`. Also run `StartupDiagnosticsTests` and `SurfaceCatalogTests`, which uniquely pin populated/null registered counts and surface-summary/resource-name output.
- **Edge/negative space:** keep every original and extracted method at CC ≤8. Preserve semver suffix stripping and stale/equal/invalid-version behavior; idle/ready and consume-once metadata; empty/exact/sibling/one-level/drive-root/trailing-separator/prefix-trap/case-insensitive path cases; malformed/non-object/mismatched/missing prompt JSON; and wrapped/raw binding, reload-race, registered-handler, and fallback classifications. Do not change signatures, DTOs, catalog entries, DI wiring, reflection caching, filesystem probing, response schemas, or error wording.

</details>

### 9. analyzer-catalog-untested-drift-suppression-branches

| Field | Content |
|---|---|
| Diagnosis | `AnalyzeCatalogInvocation` takes the conservative branch at `analyzers/ServerSurfaceCatalogAnalyzer/ServerSurfaceCatalogAnalyzer.cs:247-255` whenever a catalog factory's first argument is not string-literal syntax, recording the unresolved entry through the per-kind counter at `ServerSurfaceCatalogAnalyzer.cs:355-361`. `ReportDrift` then checks that flag at `ServerSurfaceCatalogAnalyzer.cs:265-291` and skips RMCP001 only for the affected surface kind. `tests/RoslynMcp.Tests/ServerSurfaceCatalogAnalyzerTests.cs:113-155` already proves RMCP001 is reported when the flag is false, but every catalog invocation in the analyzer tests uses a string literal, leaving the true branch and its kind isolation uncovered. The defect is missing regression coverage; no production change is required. |
| Approach | Modify `tests/RoslynMcp.Tests/ServerSurfaceCatalogAnalyzerTests.cs`. Add `RMCP001_SuppressedOnlyForKindWithNonLiteralCatalogEntry`, reusing `McpAttributeStubs`, `DiagnosticResult`, and `VerifyAsync` at lines 30-67 and 243-252. Build a fixture where `ServerSurfaceCatalog.Tools` calls `Tool(GetCatalogName())`, a tool attribute is absent from the literal catalog, and a resource attribute is also absent while Resources has no unresolved entry. Expect exactly the resource RMCP001 diagnostic: the missing tool diagnostic must be suppressed, while the resource diagnostic proves `ReportDrift` still executes its false branch for other kinds. Update the file header's scenario list to include non-literal suppression. Add `changelog.d/analyzer-catalog-untested-drift-suppression-branches.md` using Maintenance frontmatter. |
| Scope | Production files: 0. Test files added or modified: 1 — `tests/RoslynMcp.Tests/ServerSurfaceCatalogAnalyzerTests.cs`. Changelog fragment: `changelog.d/analyzer-catalog-untested-drift-suppression-branches.md`, excluded from production and test counts. Files deleted: none. |
| Tool policy | edit-only |
| Estimated context cost | 25000 |
| Risks | The non-literal call must still bind semantically to `ServerSurfaceCatalog.Tool`; otherwise `AnalyzeCatalogInvocation` returns before recording the unresolved entry and a zero-diagnostic assertion could pass for the wrong reason. Requiring an RMCP001 for the unresolved-free Resource kind makes the test prove analyzer initialization, attribute matching, and per-kind short-circuiting in the same compilation. Preserve the existing RMCP002 tests and production analyzer behavior. Fanout probe skipped — surgical test-only extension with no cross-cutting symbol changes. |
| Validation | Run `mcp__roslyn__compile_check`, then `mcp__roslyn__test_run --filter "FullyQualifiedName~RoslynMcp.Tests.ServerSurfaceCatalogAnalyzerTests"`. Confirm the new fixture reports exactly one RMCP001 at the missing resource attribute and none at the missing tool attribute; retain the existing literal-catalog RMCP001 and RMCP002 regressions. If Roslyn MCP is unavailable, run `dotnet test tests/RoslynMcp.Tests/RoslynMcp.Tests.csproj --filter "FullyQualifiedName~RoslynMcp.Tests.ServerSurfaceCatalogAnalyzerTests"`. Finish with `./eng/verify-release.ps1 -Configuration Release`. |
| Performance review | N/A — test-only maintenance, no production hot-path changes. |
| CHANGELOG category | Maintenance |
| CHANGELOG entry (draft) | - **Maintenance:** Added analyzer regression coverage proving non-literal `ServerSurfaceCatalog` names suppress RMCP001 only for their own surface kind while other kinds continue reporting catalog drift. |
| Backlog sync | Close rows: [analyzer-catalog-untested-drift-suppression-branches]. Mark obsolete: []. Update related: []. |

### 10. dedupe-csharp-features-assembly-load-helper

| Field | Content |
|---|---|
| Diagnosis | Both detail anchors resolve. `CodeActionService.LoadCSharpFeaturesAssembly` at `src/RoslynMcp.Roslyn/Services/CodeActionService.cs:263-273` and `FixAllService.LoadFeaturesAssembly` at `src/RoslynMcp.Roslyn/Services/FixAllService.cs:422-432` contain the same `Assembly.Load("Microsoft.CodeAnalysis.CSharp.Features")`, non-cancellation exception filter, and null fallback. Their four callers are at `CodeActionService.cs:279,311` and `FixAllService.cs:438,476`. The root cause is service-local ownership of a shared assembly-resolution policy despite both services already consuming the `RoslynMcp.Roslyn.Helpers` namespace. A live probe also found a non-identical direct load in `CodeFixProviderRegistry.cs:161-189`; its broader catch logs the exception, so folding it into a null-only helper would silently reduce observability and is excluded from this bounded row. |
| Approach | Add `src/RoslynMcp.Roslyn/Helpers/CSharpFeaturesAssemblyLoader.cs`, mirroring the focused internal-static-helper placement in `src/RoslynMcp.Roslyn/Helpers/ProjectFilterHelper.cs:5-13`. Implement one `TryLoad()` method containing the fixed assembly name, `Assembly.Load`, the existing `OperationCanceledException` exclusion, and null fallback. Replace all four calls in `CodeActionService` and `FixAllService` with `CSharpFeaturesAssemblyLoader.TryLoad()`, delete both private loader methods, preserve each caller's current null handling and logging, and leave lazy provider/analyzer initialization unchanged. Remove the now-unused `System.Reflection` import from `CodeActionService`; retain it in `FixAllService` because `Assembly.LoadFrom` remains at line 375. Add `changelog.d/dedupe-csharp-features-assembly-load-helper.md`. |
| Scope | Production files touched (3): new `src/RoslynMcp.Roslyn/Helpers/CSharpFeaturesAssemblyLoader.cs`, `src/RoslynMcp.Roslyn/Services/CodeActionService.cs`, and `src/RoslynMcp.Roslyn/Services/FixAllService.cs`. Test files added or modified: 0; existing integration coverage exercises both consumers. Changelog fragment excluded from production/test counts. Files deleted: none. |
| Tool policy | edit-only |
| Estimated context cost | 40000 |
| Risks | Preserve cancellation propagation, null-on-load-failure behavior, caller-specific warnings, and the existing lazy-load boundaries; do not add shared assembly caching as part of this behavior-preserving refactor. The fanout probe found only the two private helper definitions and their four same-file callers, so the new helper yields three production files. Adjacent bad code remains: `src/RoslynMcp.Roslyn/Contracts/ICodeFixProviderRegistry.cs:9-10` claims `fix_all_preview` uses the registry, while `FixAllService.cs:20,74-77` still owns a parallel provider-discovery path and `CodeFixProviderRegistry.cs:165` independently loads the Features assembly. Recommend backlog row `fixall-codefix-provider-registry-consolidation`, scoped to those three production files and one provider-parity regression-test file, proving code-fix preview and fix-all resolve the same static and project provider sets. |
| Validation | Run `mcp__roslyn__compile_check`. Run targeted tests for `RoslynMcp.Tests.CodeActionServiceTests`, `RoslynMcp.Tests.SelectionRangeCodeActionTests`, `RoslynMcp.Tests.FixAllServiceIntegrationTests`, and `RoslynMcp.Tests.FixAllServiceGuidanceTests`. Use `rg` to confirm `LoadCSharpFeaturesAssembly` and `LoadFeaturesAssembly` no longer exist, all four anchored consumers call `CSharpFeaturesAssemblyLoader.TryLoad()`, and the duplicated try/catch body exists only in the shared helper. Confirm the known `CodeFixProviderRegistry` direct-load path remains behaviorally unchanged. Finish with `./eng/verify-release.ps1 -Configuration Release`. |
| Performance review | N/A — maintenance refactor with no hot-path expansion; preserve the existing per-service `Lazy<T>` initialization and do not introduce extra reflection or assembly-load attempts. |
| CHANGELOG category | Maintenance |
| CHANGELOG entry (draft) | - **Maintenance:** Deduplicated CSharp.Features assembly loading across code-action and fix-all services through one shared helper without changing provider or analyzer discovery behavior. |
| Backlog sync | Close rows: [dedupe-csharp-features-assembly-load-helper]. Mark obsolete: []. Update related: []. |

<details>
<summary>Sonnet handoff notes</summary>

**Sonnet handoff:**

- **Pattern coordinates:** replace the identical loaders at `CodeActionService.cs:263-273` and `FixAllService.cs:422-432`; redirect the four callers at `CodeActionService.cs:279,311` and `FixAllService.cs:438,476`.
- **Exact helper seam:** add internal static `CSharpFeaturesAssemblyLoader` under `RoslynMcp.Roslyn.Helpers`, shaped like `ProjectFilterHelper`. `TryLoad()` must contain only `Assembly.Load("Microsoft.CodeAnalysis.CSharp.Features")`, catch non-`OperationCanceledException`, and return `null`.
- **Preservation constraints:** keep caller-specific warnings and outer `GetTypes`/activation exception handling unchanged. Let cancellation propagate. Remove `System.Reflection` only from `CodeActionService`; retain it in `FixAllService` for `Assembly.LoadFrom` at line 375.
- **Lazy boundaries:** preserve the four existing `Lazy<T>` factories at both constructors. Do not cache the assembly in the helper or alter the number/timing of load attempts.
- **Test evidence:** `SelectionRangeCodeActionTests` lines 74, 105, and 146 positively exercise CSharp.Features refactoring discovery. `FixAllServiceGuidanceTests` lines 110 and 209 exercise loadable IDE providers and the analyzer path. Retain the broader `CodeActionServiceTests` and `FixAllServiceIntegrationTests` regressions.
- **Validation:** run compile check, the four planned test classes, then `./eng/verify-release.ps1 -Configuration Release`. Verify both old loader names are absent, exactly four calls use `CSharpFeaturesAssemblyLoader.TryLoad()`, and the new helper owns the sole duplicated null-return try/catch.
- **Edge cases:** preserve null fallback for load failures, cancellation escape, caller-owned handling of `GetTypes`/constructor failures, and unchanged successful provider/analyzer enumeration.
- **Negative space:** do not modify `CodeFixProviderRegistry.cs:161-189`, `ICodeFixProviderRegistry.cs`, DI, project files, or tests. The registry’s direct load intentionally logs failures. Add only the planned changelog fragment besides the three production files.

</details>
