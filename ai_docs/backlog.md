# Next work and backlog

<!-- purpose: Open work only; contract for agents syncing backlog on ship. -->

**updated_at:** 2026-04-08T03:00:00Z

## Agent contract

**Scope:** This file lists **unfinished work only**. It is not a changelog.

| | |
|---|---|
| **MUST** | Remove or update backlog rows when work ships; do it in the **same PR** as the implementation (or an immediate follow-up). |
| **MUST** | End implementation plans with a final todo: `backlog: sync ai_docs/backlog.md` when closing backlog-related items. |
| **MUST** | Use stable, kebab-case `id` values per open row (grep-friendly). |
| **MUST NOT** | Add “Completed” tables, dated history sections, or narrative changelogs here. |
| **MUST NOT** | Leave done items in the open table. |

## Standing rules (ongoing)

These are **not** backlog rows; do not delete them when clearing completed work.

- Reprioritize on each audit pass.
- Bug and tool items: put reproduction and session context in the PR or linked issue when it helps reviewers.
- Broader planning and complexity notes stay in this file; `ai_docs/archive/` holds policy only (see `archive/README.md`).

## Open work

| id | blocker | deps | do |
|----|---------|------|-----|
| `testbase-srp-split` | none | — | **P3 / refactor.** `TestBase` is a 318-line god class owning: 40+ static service properties, MSBuild init, filesystem helpers, path resolution, fixture copy logic, and now workspace caching. The recent fix made it idempotent and added a workspace cache, but the class still violates SRP. Plan: extract `TestServiceContainer` (services + lifecycle), `TestFixtures` (path resolution + copy helpers), and keep `TestBase` as a thin pass-through. Defer until a touch-the-class change is needed — the current split would touch 30+ test classes. |
| `vuln-scan-network-mock` | none | — | **P3 / test isolation.** `ScanNuGetVulnerabilitiesAsync_ReturnsStructuredResult` shells out to `dotnet list package --vulnerable`, which hits `api.nuget.org`. Now passes reliably with the workspace-cache fix, but still fails in air-gapped environments. Consider mocking the NuGet client at the gated-executor boundary, or marking the test `[Category("Network")]` so it can be filtered. |
| `verify-release-test-output-policy` | none | — | **P3 / process.** `eng/verify-release.ps1` runs `dotnet test` with default verbosity. Add `--logger "console;verbosity=normal"` and `$ErrorActionPreference = 'Stop'` so failing tests are not masked by tail/pipe operations in any wrapper script. |
| `semantic-search-async-modifier-doc` | none | — | **P3 / docs.** `semantic_search` query `async methods returning Task<bool>` returns 0 because Roslyn `IMethodSymbol.IsAsync` requires the `async` modifier on the declaration; pass-through `Task.FromResult` methods are not matched. Document this gotcha in `CodePatternAnalyzer` tool description and the `/roslyn-mcp:review` skill so users know to query for "methods returning Task<bool>" without "async" if they want all Task-returning methods. |
| `dependency-service-srp-split` | none | — | **P3 / refactor.** `DependencyAnalysisService` (530 LOC) owns four responsibilities: namespace dependency graphs, DI registration scanning, NuGet package listing, and NuGet vulnerability scanning. Split into `NamespaceDependencyService`, `DiRegistrationService`, and `NuGetDependencyService`; `DependencyAnalysisService` becomes a façade or is deleted. Also covers `ParseNuGetVulnerabilityJson` (cyclomatic 27, 86 LOC with nested local function capturing 4 outer variables) — extract `NuGetVulnerabilityJsonParser` static class and eliminate the nested function. Defer until a touch-the-class change is needed. |
| `orchestration-migrate-package-extract` | none | — | **P3 / refactor.** `OrchestrationService.PreviewMigratePackageAsync` (102 LOC, cyclomatic 17) handles project-file and `Directory.Packages.props` mutation logic inline. Extract `BuildPackageReferenceEdit`, `BuildCentralVersionEdit`, and `BuildRemoveVersionAttributeEdit` helpers to make control flow readable and allow isolated testing of each mutation shape. Defer until a touch-the-method change is needed. |
| `compile-check-emit-validation-regression-check` | none | — | **P3 / investigation.** The 2026-04-07 firewallanalyzer rw-lock audit reported `compile_check` with `emitValidation=true` returning byte-identical output (1 char shorter) to the default call, with no measurable wall-clock difference. The 2026-04-06 prior audit reported `emitValidation=true` was "~94× slower" with additional findings. Either (a) `emitValidation` regressed to a no-op when the base compilation has unresolved package references, or (b) the prior measurement was incidental. Re-test on a clean workspace with restored packages to determine which. If regressed, restore the emit path; if not, document the precondition. |
| `workspace-payload-summary-modes` | none | — | **P3 / improvement.** `workspace_load`, `workspace_list`, `workspace_status`, and the `roslyn://workspaces` resource each return a full ~30 KB project tree even when callers only need `{IsLoaded, IsStale, ProjectCount, DocumentCount, WorkspaceVersion}`. On a 34-project ITChatBot.sln a single status check exhausts a meaningful share of the per-turn output budget, making the deep-review prompt's cross-cutting principles #12 (output budget per turn) and #13 (workspace heartbeat between phases) nearly impossible to satisfy. Add a `verbose=false` / `summary=true` mode (or a separate `workspace_list_summary` tool) that returns the lean shape; keep the verbose payload as opt-in. Subsumes ITChatBot rw-lock audit FLAG-002 (workspace_status echoes full payload) and FLAG-003 (workspace_list returns full project trees per workspace). |
| `workspace-failure-severity-normalize` | none | — | **P3 / consistency.** WORKSPACE_FAILURE diagnostics surface inconsistently: `workspace_load`, `workspace_status`, and `roslyn://workspaces` mark them `Severity=Error`, while `project_diagnostics` re-classifies the same diagnostics as `Warning`. Concrete instance from the 2026-04-07 ITChatBot rw-lock audit: `workspace_load ITChatBot.sln` returns three "PackageReference X will not be pruned" entries with `Severity=Error, Category=Workspace, Id=WORKSPACE_FAILURE` even though `IsLoaded=true` and the workspace works. .NET 9+ pruning warnings are informational MSBuild guidance and should be `Warning` (or `Hidden`/`Info`). Pick one severity (Warning matches MSBuild semantics) and emit it consistently from every reader. Downstream impact: agents that gate on `workspace_load.Severity==Error` mark cleanly-loaded workspaces as failed. Subsumes FLAG-001/FLAG-005/FLAG-014. |
| `project-diagnostics-filter-totals` | none | — | **P3 / contract clarity.** `project_diagnostics severity=Error limit=20` returns `totalErrors=0, totalWarnings=0, totalInfo=0, totalDiagnostics=0` even though calling the same tool with no filter on the same workspace returns 182 warnings (observed against ITChatBot 2026-04-07). Either keep totals invariant (filter only narrows the returned set) OR rename the fields to `totalErrorsMatchingFilter` etc. and document the semantics. Schema currently says `totalWarnings`, not `totalWarningsMatchingFilter`. |
| `cohesion-metrics-source-gen-aware` | none | — | **P3 / accuracy.** `get_cohesion_metrics` mis-handles two common patterns: (a) `[LoggerMessage]` and `[GeneratedRegex]` source-generator partial methods are counted as separate clusters, so a class like `QuestionClassifier` with one real method (`Classify`) gets `Lcom4Score=4`, falsely implying SRP violation; (b) local helper methods (e.g., `CreateFailure`, `MapToQueryParameters` in `SysLogServerAdapter`/`GitRepositoryAdapter` per the 2026-04-07 ITChatBot rw-lock audit) are listed simultaneously as standalone methods AND inside the `SharedFields` array. Fix: filter out source-gen partials by syntax/attribute detection before clustering, and decide canonically whether a local helper appears as a method OR a shared field, not both. |
| `find-type-usages-cref-classification` | none | — | **P3 / cosmetic.** `find_type_usages` buckets `<see cref="X"/>` doc-comment references as `Other` instead of as a `Documentation` classification (observed against ITChatBot 2026-04-07). Enrich the classification enum so doc-comment refs are visually distinguishable from real consumers. |
| `error-response-observability` | none | — | **P3 / observability.** Two related error-shape gaps surfaced in the 2026-04-07 ITChatBot rw-lock audit: (a) `find_references` (and likely `find_consumers`/`find_type_usages`) with an invalid `symbolHandle` (e.g. fabricated base64 of `{"MetadataName":"fake"}`) returns `{count:0, totalCount:0, references:[], hasMore:false}` instead of a structured NotFound error — callers cannot distinguish "valid handle, zero references" from "junk handle"; (b) error responses on workspace-scoped tools (`workspace_status` and likely siblings sharing the same wrapper) populate `tool: "unknown"` instead of the actual tool name. Fix both: emit a structured NotFound envelope from the symbol-handle tools (matching `workspace_status`'s NotFound shape), and have the error wrapper read the originating tool name from the dispatcher. |
| `semantic-search-html-decode` | none | — | **P3 / UX.** `semantic_search` queries containing encoded `<>` (e.g. `Task&lt;bool&gt;`) return 0 results with no hint why. AI clients commonly double-encode angle brackets when constructing queries from JSON; HTML-decode the query string on ingress (or emit a warning if encoded entities are detected) so a query like `async methods returning Task<bool>` works regardless of the caller's encoding discipline. |
| `tool-substring-matching-docs` | none | — | **P3 / docs.** `symbol_search`'s `query` parameter and `get_msbuild_properties`'s `propertyNameFilter` both work as substring matches (observed against ITChatBot 2026-04-07), but neither tool description documents this. Update the schemas/descriptions to explicitly say "substring match (case-insensitive)". |
| `server-info-prompts-tier-doc` | none | — | **P3 / docs.** `server_info` reports `prompts: stable=0, experimental=16` but the live catalog only exposes 16 experimental prompts — it is unclear whether prompts have a stable tier at all. Confirm the tiering convention is intentional and document it in the `server_info` tool description (or rename the field if prompts don't have tiers). |
| `scaffold-defaults-improvements` | none | — | **P3 / convention.** `scaffold_type_preview` and `scaffold_test_preview` defaults need three improvements observed across the 2026-04-07 ITChatBot legacy-mutex and FirewallAnalyzer audits: (a) `scaffold_type_preview` should default to `internal sealed class` instead of `public class` (modern .NET convention); (b) `scaffold_type_preview` should place generated files in a natural subfolder matching the namespace (e.g. `Drift/`, `Analysis/`) instead of the project root, and the namespace should match — currently lands generated types at the project root; (c) `scaffold_test_preview` should generate stub constructor args that compile AND run green — currently uses `default(IEnumerable<ISourceAdapter>)` which throws NRE on any actual run; prefer `Array.Empty<T>()` / `[]` / `new T[0]` for empty-constructible interface parameters. Subsumes FLAG-12A and improvement suggestions #19/#20 from the legacy-mutex audit. |
| `get-completions-ranking` | none | — | **P3 / UX.** `get_completions filterText="To"` returned 8 items with `IsIncomplete=false`, but non-in-scope external types (`ToBase64Transform` from `System.Security.Cryptography`) ranked near in-scope `ToString` (observed against ITChatBot 2026-04-07). In-scope instance members and locals should outrank namespace-qualified external types for a given prefix. Add a ranking step that boosts in-scope candidates. |
| `find-type-mutations-side-effects` | none | — | **P3 / semantic gap.** `find_type_mutations` reports only instance-field reassignments (Roslyn `IAssignmentOperation` targeting instance fields). For a class like `FileSnapshotStore` whose whole purpose is disk IO, none of `WriteManifestAsync`/`WriteMergedRulesAsync`/etc. are flagged as mutations because they don't reassign instance fields (observed in the 2026-04-07 FirewallAnalyzer audit). Add a `mutationScope=fields|io|sideEffects` selector OR a separate `find_side_effects` tool that detects disk-IO, network calls, file operations, and process invocations. |
| `flow-analysis-expression-bodied-lift` | none | — | **P3 / improvement.** `analyze_data_flow` and `analyze_control_flow` cannot inspect expression-bodied members (they fail with `InvalidOperation: No statements found in the line range...`). The error is actionable, but expression-bodied methods are common in modern .NET and developers often want flow analysis on complexity hotspots written in that style (concrete case from 2026-04-07 FirewallAnalyzer audit: `DriftDetector.RulesEqual` is a 14-line `=>`-bodied method with cyclomatic complexity 14 — exactly the kind of method users would want to flow-analyze). Add an internal lift step that wraps `=> expr` as `{ return expr; }` and runs flow analysis on the synthetic block, so callers can audit `=>`-bodied methods without rewriting the source. |
| `reader-tool-elapsed-ms` | none | — | **P3 / instrumentation.** Reader-family tools (`find_references`, `symbol_search`, `find_unused_symbols`, `get_complexity_metrics`, `get_cohesion_metrics`, etc.) do not surface `ElapsedMs` in their responses — only `compile_check`, `build_project`, `test_run`, `evaluate_csharp`, and `nuget_vulnerability_scan` emit timing. Result: agents cannot fill the deep-review prompt's Phase 8b sequential-baseline / parallel-fan-out concurrency matrix with quantitative measurements from inside the agent loop, so concurrency audits produce only behavioral evidence (per the 2026-04-07 FirewallAnalyzer audit). Add an optional `ElapsedMs` field to all reader responses so concurrency audits can produce numeric speedup ratios without external instrumentation. |
| `compile-items-vs-document-count-doc` | none | — | **P3 / docs.** `evaluate_msbuild_items Compile <project>` returns N items but Roslyn `workspace_load` reports `DocumentCount=N+3` for the same project (observed in the 2026-04-07 FirewallAnalyzer audit: `FirewallAnalyzer.Application` had 17 Compile items vs 20 documents). The 3-item gap is normal — auto-generated implicit-usings, assembly-info, and GlobalUsings files are added by the SDK and aren't in the explicit Compile item list. Document this expectation in the `workspace_load` and `evaluate_msbuild_items` tool descriptions so downstream agents can interpret the difference. |
| `audit-feasibility-companion-cli` | none | — | **P3 / tooling.** A complete walkthrough of `ai_docs/prompts/deep-review-and-refactor.md` against a 34-project / 751-document solution does not fit in a single Claude Code conversation today, primarily because workspace-payload bloat tools (`workspace_load`, `workspace_status`, `workspace_list`, `roslyn://workspaces`, `get_nuget_dependencies`, `list_analyzers`, `get_msbuild_properties`, `find_unused_symbols`) exhaust the per-turn ~250 KB output budget by mid-prompt — observed against the 34-project ITChatBot.sln in the 2026-04-07 rw-lock audit. Consider shipping a `roslyn-mcp-audit` companion CLI that drives the deep-review prompt headlessly and emits the canonical raw audit file as an artifact, freeing the LLM from having to render every tool result back through context. Cross-references `workspace-payload-summary-modes` (would also help) and the cross-cutting principle #13 output-budget ceiling. |

## Refs

| Path | Role |
|------|------|
| `ai_docs/workflow.md` | Branch/PR workflow; **Backlog closure** |
| `ai_docs/backlog.md` | This file (open work + contract) |
