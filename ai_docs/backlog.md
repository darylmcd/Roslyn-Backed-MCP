# Next work and backlog

<!-- purpose: Open work only; contract for agents syncing backlog on ship. -->
<!-- scope: in-repo -->

**updated_at: 2026-05-12T01:06:17Z**

## Agent contract

| | |
|---|---|
| **Scope** | This file lists unfinished work only. It is not a changelog. |
| **MUST** | Remove or update backlog rows when work ships; do it in the same PR or an immediate follow-up. |
| **MUST** | End implementation plans with a final todo: `backlog: sync ai_docs/backlog.md`. |
| **MUST** | Use stable, kebab-case `id` values per open row. |
| **MUST** | Every row's `do` cell summarizes the current need + the concrete next deliverable. Include `Anchors:` (specific source file paths) when the row references code, and evidence (audit/retro/CI signal) when one exists. |
| **MUST** | Size every row to a single bounded initiative — ≤4 production files, ≤3 test files, one regression-test shape. Split heroic multi-bug rows into per-bug children before planning against them. |
| **MUST NOT** | Add `Completed`, `Shipped`, `Done`, `History`, or `Changelog` sections. Git is the archive. |
| **MUST NOT** | Leave done items in the open table. |
| **MUST NOT** | Use `### <id>` body sections per item. The table row IS the canonical form. Items needing long-form depth (more than ~10 lines) link to `ai_docs/items/<id>.md` from the `do` cell. |

## Standing rules

- **Reprioritize on each audit pass.** Stale priority order is a finding.
- **Keep rows planner-ready.** A row is ready when an agent can read it cold and start a plan: name the live anchors and the next concrete deliverable or investigation output.
- **Replace stale umbrella rows with concrete follow-ons** before planning against them.
- **Long-form audit evidence belongs in referenced reports**, not in this file. The `do` cell carries a one-line evidence summary plus the report path.
- **Weak-evidence flag.** When a row's signal is thin (single retro session, self-audit only, etc.) say so explicitly in the `do` cell ("Weaker evidence — N until external session reproduces").
- **GitHub-issue cross-link.** When a row has a corresponding open GitHub Issue, surface the link at the start of the `do` cell so the backlog and issue stay paired. Two flavors:
  - **Reserved for contributor pickup** (`good first issue` / `help wanted` labels): prefix the `do` cell with `**Reserved — [gh #NNN](https://github.com/darylmcd/Roslyn-Backed-MCP/issues/NNN) (good first issue|help wanted); skip in sweeps until contributor pickup.**` — `/backlog-sweep:plan` skips these per its Step 1 hard-skip rule. Remove the marker (or close the row) when a contributor PR lands or when the maintainer reclaims the work.
  - **Tracked-only** (auto-filed audit issues that aren't promoted to a contributor label): prefix the `do` cell with `[gh #NNN](https://github.com/darylmcd/Roslyn-Backed-MCP/issues/NNN) — `. Sweep treats these as normal claimable rows; the implementing PR closes both the issue (via `Fixes #NNN`) and the backlog row.
- **Priority tiers:** Critical > High > Medium > Low > Defer.
- See `workflow.md` → **Backlog closure** for close-in-PR expectations.

---

## Critical

<!-- Production-breaking or blocking work. Empty section is fine; keep the header. -->

| id | pri | deps | do |
|----|-----|------|-----|

## High

| id | pri | deps | do |
|----|-----|------|-----|

## Medium

| id | pri | deps | do |
|----|-----|------|-----|
| `file-lock-aware-prompt-validation-guidance` | Medium | `build-test-self-analyzer-file-lock` | Prompt and audit workflows should be file-lock-aware so they do not repeatedly re-trigger the self-hosted analyzer DLL lock tracked by `build-test-self-analyzer-file-lock`. During the 2026-05-08 stress run, Phase 8 `build_workspace`/`test_run` and the `debug_test_failure` prompt path reproduced `MSB3027`/`MSB3021` while `roslynmcp.exe` held the analyzer DLL. Next deliverable: update prompt templates and `.claude/skills/mcp-server-stress/prompts/prompt.md` to recognize `failureEnvelope.errorKind=FileLock` or `MSB3027`/`MSB3021` text as infrastructure (not test failure), stop retrying full build/test validation in the same loaded self-hosted workspace, prefer `compile_check` / targeted read-side evidence where sufficient, and instruct operators to close/reload the workspace or run validation from an isolated process after `dotnet build-server shutdown`. Anchors: `.claude/skills/mcp-server-stress/prompts/prompt.md:345,348,484`, `src/RoslynMcp.Host.Stdio/Prompts/PromptMessageBuilder.cs:117,137`, `src/RoslynMcp.Host.Stdio/Prompts/RoslynPrompts.RefactoringWorkflows.cs:13`. Regression test shape: 1 prompt-rendering test that exercises the file-lock failure envelope path and asserts the rendered prompt includes the bypass guidance. Evidence: `review-inbox/archive/20260508T154415Z/20260508T154415Z_roslyn-backed-mcp_mcp-server-audit.md` Phase 16 + fragment id `roslyn-backed-mcp-file-lock-aware-prompt-validation-guidance`. |
| `find-consumers-static-class-classification` | Medium | none | `find_consumers` classifies all consumers of a `public static class` as `dependencyKinds=["Other"]`; `find_type_consumers` classifies the same call sites as `kinds=["local"]` (incorrect — no `var x = StaticClass...` exists). Static-method invocation is a very common C# pattern and the uninformative bucket makes both tools nearly useless for static-class targets. The two tools also use incompatible classification vocabularies (`Constructor/Field/Parameter/.../Other` vs `using/ctor/inherit/field/local/other`), making cross-tool analysis harder. Next deliverable: add an `Invocation` (or `StaticReference`) kind to both tools' classifiers; document the taxonomy alignment between the two tools. Anchors: `src/RoslynMcp.Roslyn/Services/ConsumerAnalysisService.cs`, `src/RoslynMcp.Host.Stdio/Tools/ConsumerAnalysisTools.cs`, `src/RoslynMcp.Core/Models/ConsumerAnalysisDto.cs`. Regression test shape: 1 fixture covering a `public static class` target and asserting `dependencyKinds` includes `Invocation` (or equivalent) rather than `Other`. Evidence: `review-inbox/archive/20260510T064835Z/20260510T053000Z_firewallanalyzer_mcp-server-surface-test.md` §13.3 (P2); confirmed independently on `CommandDispatcher` (static class) in `review-inbox/archive/20260510T064835Z/20260510T053142Z_network-documentation_mcp-server-surface-test.md` §3.3. |
| `get-syntax-tree-maxtotalbytes-not-enforced` | Medium | none | `get_syntax_tree` with `maxTotalBytes=40000` returned a 109 KB JSON payload — the byte budget was ignored. The tool correctly returns the requested tree, but callers relying on `maxTotalBytes` to bound transcript size will find the parameter unreliable, which can cause agent context overruns on large method bodies. Next deliverable: locate the byte-accumulation logic in `SyntaxService.cs`, confirm the cap is applied at the walker (not just the root node), and assert that `maxTotalBytes` truncates output to within ≤10% of the requested ceiling. Anchors: `src/RoslynMcp.Roslyn/Services/SyntaxService.cs`, `src/RoslynMcp.Host.Stdio/Tools/SyntaxTools.cs`. Regression test shape: 1 fixture calling `get_syntax_tree` on a large method with `maxTotalBytes=5000` and asserting the response JSON byte length ≤ 5500. Evidence: `review-inbox/archive/20260510T064835Z/20260510T053142Z_network-documentation_mcp-server-surface-test.md` §4.3 (FLAG, get_syntax_tree budget not enforced — 109 KB vs 40 KB cap). |
| `parallel-fanout-auto-reload-timeout-floor` | Medium | none | When a workspace mutation (e.g. `git worktree add`) triggers an auto-reload mid–parallel read fan-out, per-call held-time timeouts fire at the 5 s floor before the reload settles. Four of 14 concurrent reads timed out with `_meta.staleAction=auto-reloaded` and `heldMs≈5007ms` despite completing in <2 s held-time post-reload. Two fix paths: (a) extend the held-time budget when `staleAction=auto-reloaded` is in flight ("hold the clock" during reload); (b) surface a distinct error category such as `WorkspaceStaleReloading` so callers can distinguish these from genuine timeouts and retry. Without a fix, agents that fan-out reads after any repo mutation see flaky timeouts that look identical to real performance issues. Anchors: `src/RoslynMcp.Roslyn/Services/WorkspaceExecutionGate.cs`, `src/RoslynMcp.Roslyn/Services/WorkspaceManager.cs`. Regression test shape: 1 concurrency fixture covering parallel read fan-out under a simulated auto-reload; assert either (a) reads complete without timeout or (b) the error category is `WorkspaceStaleReloading`, not `Timeout`. Evidence: `review-inbox/archive/20260510T064835Z/20260510T052934Z_roslyn-backed-mcp_mcp-server-surface-test.md` §13.1 (P2, Finding F3). |
| `promote-tier-supports-prompts-and-resources` | Medium | none | `/promote-tier` currently handles only **tools** (flips `[McpToolMetadata]` attribute + `ServerSurfaceCatalog.*` entry). Prompts and resources also carry an experimental→stable tier in the catalog but no attribute marker to flip, so the skill cannot be invoked against them. The aggregated promotion scorecard rates all three kinds (tool / resource / prompt) — the scorecard verdict for resources and prompts is currently un-actionable from the maintainer's tooling. Two fix paths: (a) extend `/promote-tier` to handle resource/prompt entries by editing the catalog only (no attribute side, since none exists for these kinds) — single-side mutation is a different contract than the dual-write tool path and needs an explicit decision; (b) introduce `[McpServerPromptMetadata]` / `[McpServerResourceMetadata]` attributes to mirror the tool pattern, then `/promote-tier` extends naturally to a dual-write for all three kinds. **Design note required** at `ai_docs/items/promote-tier-prompts-and-resources-design.md` before any code change — the attribute-or-no-attribute decision shapes the rest of the implementation. Anchors: `.claude/skills/promote-tier/SKILL.md`, `src/RoslynMcp.Host.Stdio/Catalog/ServerSurfaceCatalog.Prompts.cs`, `src/RoslynMcp.Host.Stdio/Catalog/ServerSurfaceCatalog.Resources.cs`. Regression test shape: 1 fixture covering a prompt promotion + 1 fixture covering a resource promotion; assert the catalog tier flipped and existing surface tests still pass. Evidence: 2026-05-11 adversarial audit of the post-/mcp-server-surface-test pipeline — `/promote-tier/SKILL.md` lines 105-113 confirm tool-only scope; the aggregated scorecard at `audit-reports/_aggregated-promotion-scorecard.json` rates all three kinds. **Weaker evidence — design pass not yet started; defer implementation until the design note lands.** |
| `sweep-executor-pr-collision-check` | Medium | none | `/backlog-sweep:execute` Step 2 needs a runtime collision check before claiming an initiative. Today's defense-in-depth only re-reads the backlog row for the `**Reserved**` marker — it does NOT query `gh pr list` to detect an open contributor PR that already targets the same anchor files. Risk: sweep claims a row a contributor has started, files collide at merge time. Next deliverable: in execute Step 2, after the reservation re-check, query `gh pr list --state open --search "<anchor-file-path>" --json number,headRefName,title` for each initiative's Scope file. If any open PR's diff touches an anchor file, mark the initiative `obsolete` with reason `"open contributor PR targets the same anchors"` and skip to the next pending. Anchors: `ai_docs/prompts/backlog-sweep-execute.md` Step 2 (the defense-in-depth section we added 2026-05-11). Regression test shape: 1 fixture covering an executor run where an open PR exists for an initiative's anchor file; assert the initiative is marked `obsolete` with the named reason. Evidence: 2026-05-11 adversarial audit, gap (d) in the backlog-sweep ↔ GH issues report. |
| `test-related-column-required-schema-mismatch` | Medium | none | [gh #618](https://github.com/darylmcd/Roslyn-Backed-MCP/issues/618) — `test_related` schema marks `column` as optional but the server requires it; callers following the schema (omitting `column`) receive an error. Schema/behaviour mismatch. Next deliverable: option A (preferred) — mark `column` as required in the tool schema; option B — accept column-absent calls by defaulting to column 0/1 (start of line). Anchors: `src/RoslynMcp.Host.Stdio/Tools/TestCoverageTools.cs` (`test_related`), `src/RoslynMcp.Host.Stdio/Catalog/ServerSurfaceCatalog.Testing.cs` (or wherever the schema lives). Regression test shape: 1 fixture calling `test_related(workspaceId, filePath, line)` without `column`; assert either successful call OR a schema-rejection before reaching the server. Evidence: gh #618; IT-Chat-Bot audit 2026-05-11 §13.7 (P2). |

## Low

| id | pri | deps | do |
|----|-----|------|-----|
| `add-project-reference-self-reference-not-rejected` | Low | none | **Reserved — [gh #608](https://github.com/darylmcd/Roslyn-Backed-MCP/issues/608) (good first issue); skip in sweeps until contributor pickup.** `add_project_reference_preview(projectName=RoslynMcp.Core, dependencyProjectName=RoslynMcp.Core)` produced a self-reference diff instead of returning an error. A self-reference is semantically invalid (it would create a cycle of length 0 in the project graph) and applying it would produce an MSBuild error. The preview path should detect and reject it with an `InvalidArgument` or `InvalidOperation` error before issuing a token. Anchors: `src/RoslynMcp.Host.Stdio/Tools/ProjectMutationTools.cs`, `src/RoslynMcp.Roslyn/Services/WorkspaceManager.cs`. Regression test shape: 1 fixture covering `add_project_reference_preview(projectName=X, dependencyProjectName=X)` and asserting a structured error (not a diff). Evidence: `review-inbox/archive/20260510T064835Z/20260508T154415Z_roslyn-backed-mcp_mcp-server-audit.md` §4 finding `roslyn-backed-mcp-add-project-reference-self-reference-preview`. |
| `compile-check-project-filter-miss-no-error-envelope` | Low | none | **Reserved — [gh #609](https://github.com/darylmcd/Roslyn-Backed-MCP/issues/609) (good first issue); skip in sweeps until contributor pickup.** `compile_check(projectName="NonExistent.Project")` returns `success=false` with the diagnostic buried in `restoreHint` rather than returning a structured error envelope with category `NotFound` or `InvalidArgument`. All other tools that fail to resolve an input (workspace ID, symbol handle, etc.) return a proper error category. This inconsistency means callers checking only `success` will miss the root cause (zero projects matched). Fix: return `InvalidArgument` (or `NotFound`) when the project filter matches 0 projects. Anchors: `src/RoslynMcp.Host.Stdio/Tools/CompileCheckTools.cs`, `src/RoslynMcp.Roslyn/Services/CompileCheckService.cs`. Regression test shape: 1 fixture calling `compile_check(projectName="DoesNotExist")` and asserting a structured error envelope with category `InvalidArgument`. Evidence: `review-inbox/archive/20260510T064835Z/20260510T053142Z_network-documentation_mcp-server-surface-test.md` §17.8 (FLAG-5, P3). |
| `diagnostic-details-empty-supportedfixes-ca-rules` | Low | none | [gh #620](https://github.com/darylmcd/Roslyn-Backed-MCP/issues/620) — `diagnostic_details` returns `supportedFixes:[]` for CA-series rules (tested CA1826, CA1848) from `Microsoft.CodeAnalysis.NetAnalyzers`. Rules ship with code fix providers but `CodeFixProviderRegistry` doesn't index them at workspace load. The `guidanceMessage` correctly redirects to `get_code_actions` (actionable degradation). Next deliverable: verify `CSharp.CodeStyle` / `CSharp.Analyzers` fix assemblies are indexed when `Microsoft.CodeAnalysis.NetAnalyzers` is loaded; alternatively document which analyzer packages have their fix providers registered. Anchors: `src/RoslynMcp.Roslyn/Services/CodeFixProviderRegistry.cs` (or equivalent), `src/RoslynMcp.Host.Stdio/Tools/DiagnosticDetailsTools.cs`. Regression test shape: 1 fixture calling `diagnostic_details(diagnosticId="CA1826", ...)` on a project referencing `Microsoft.CodeAnalysis.NetAnalyzers`; assert `supportedFixes.Count > 0`. Evidence: gh #620; IT-Chat-Bot audit 2026-05-11 §13.3 (P3). |
| `editorconfig-write-no-auto-invalidation` | Low | none | `set_editorconfig_option` writes the new value to `.editorconfig`, but `get_editorconfig_options` does not reflect the new value until `workspace_reload`. This is usable (the tool's write is correct) but violates the user's mental model — the agent reads back a stale value after writing. Fix path: auto-invalidate the editorconfig cache on write, or add a note in the tool's response that the new value takes effect after the next reload. Anchors: `src/RoslynMcp.Host.Stdio/Tools/EditorConfigTools.cs`, `src/RoslynMcp.Roslyn/Services/EditorConfigService.cs`. Regression test shape: 1 fixture writing an option and immediately calling `get_editorconfig_options` without a reload; assert the new value is reflected. Evidence: `review-inbox/archive/20260510T064835Z/20260508T154415Z_roslyn-backed-mcp_mcp-server-audit.md` §4 non-fragment note (P4, usable but worth documenting or auto-invalidating). |
| `eternal-experimental-age-audit` | Low | none | No automation flags tools/resources/prompts that have been on the **experimental** support tier for >180 days without promotion or deprecation. Without an age audit, the experimental tier becomes a graveyard where surfaces quietly ossify between releases. Next deliverable: new `eng/audit-experimental-age.ps1` script (or a hook in `.claude/skills/publish-preflight/SKILL.md` Step 8) that walks `ServerSurfaceCatalog.*` for entries with `SupportTier = Experimental`, runs `git blame` on the catalog line that introduced the tier annotation, computes age, and emits a table of entries >180 days old. The script does **not** auto-promote or auto-deprecate; the maintainer reads the table and decides whether to `/promote-tier <name> stable`, deprecate, or extend the dwell time with a noted rationale. Anchors: NEW `eng/audit-experimental-age.ps1`; optional surfacing hook in `.claude/skills/publish-preflight/SKILL.md` Step 8 ("Promotion candidates" section). Regression test shape: 1 fixture with a synthetic catalog entry whose tier-line git-blame is >180 days old; assert the script flags it with the correct age. Evidence: 2026-05-11 adversarial audit, gap #4 in the post-skill pipeline report — no time-based audit exists today; staleness contract only applies to scorecard JSON artifacts (30 d warn / 90 d ignore per `full.md` line 878), not to the experimental surface itself. |
| `extract-and-wire-interface-duplicate-cross-project` | Low | none | [gh #625](https://github.com/darylmcd/Roslyn-Backed-MCP/issues/625) — `extract_and_wire_interface_preview` generates a duplicate interface when the target type already implements a cross-project interface with the same name. No check of the implementing type's existing interface list before generation; would produce a name-collision compile error if applied. Next deliverable: before generating, inspect the target type's `type_hierarchy.interfaces[]`; if an interface with the same name already exists (even in a different project), decline with a message pointing the caller at `extract_interface_cross_project_preview` instead. Anchors: `src/RoslynMcp.Roslyn/Services/ExtractInterfaceService.cs`, `src/RoslynMcp.Host.Stdio/Tools/ExtractInterfaceTools.cs`. Regression test shape: 1 fixture with a type already implementing `IFoo` from another project; assert `extract_and_wire_interface_preview` returns a structured rejection rather than a duplicate. Evidence: gh #625; IT-Chat-Bot audit 2026-05-11 §13.15 (P3). |
| `find-duplicated-methods-mcp-wrapper-false-positive` | Low | none | **Reserved — [gh #612](https://github.com/darylmcd/Roslyn-Backed-MCP/issues/612) (good first issue); skip in sweeps until contributor pickup.** `find_duplicated_methods` clusters thin `[McpServerTool]`-attributed wrapper methods in `Host.Stdio/Tools/*` as a high-similarity (1.0) duplicate group. These wrappers are intentionally identical single-delegation shims — they cannot be de-duped because the MCP attribute model requires one method per tool name. The clustering is structurally correct but semantically a false positive that pollutes results for operators running this tool against the server itself or any MCP-server-shaped repo. Fix: add an `excludeMcpToolWrappers=true` (default `true`) parameter that skips `[McpServerTool]`-attributed methods whose body is a single delegation, similar to the existing `find_duplicate_helpers` framework-wrapper exclusion. Anchors: `src/RoslynMcp.Roslyn/Services/DuplicateMethodDetectorService.cs`, `src/RoslynMcp.Host.Stdio/Tools/AdvancedAnalysisTools.cs`, `src/RoslynMcp.Core/Services/IDuplicateMethodDetectorService.cs`. Regression test shape: 1 fixture confirming the 5-member wrapper cluster does not appear when `excludeMcpToolWrappers=true`. Evidence: `review-inbox/archive/20260510T064835Z/20260510T052934Z_roslyn-backed-mcp_mcp-server-surface-test.md` §14 (Finding F1, P3). |
| `find-overrides-interface-root-empty` | Low | none | `find_base_members` found the interface base member for a type that implements an interface method, but `find_overrides` on the same interface member returned zero results. The navigation contract is asymmetric: base-to-impl traversal works but impl-to-overrides from the interface root does not. This means the interface-rooted call in `find_overrides` silently fails where `find_base_members` would succeed. Anchors: `src/RoslynMcp.Host.Stdio/Tools/SymbolTools.cs` (`find_overrides` implementation), `src/RoslynMcp.Roslyn/Services/ConsumerAnalysisService.cs` or equivalent override-resolution logic. Regression test shape: 1 fixture covering a concrete interface member → `find_overrides` returns at least 1 result. Evidence: `review-inbox/archive/20260510T064835Z/20260508T154415Z_roslyn-backed-mcp_mcp-server-audit.md` §4 finding `roslyn-backed-mcp-find-overrides-interface-root-empty`. |
| `get-prompt-text-multi-step-required-param-errors` | Low | none | **Reserved — [gh #610](https://github.com/darylmcd/Roslyn-Backed-MCP/issues/610) (good first issue); skip in sweeps until contributor pickup.** `get_prompt_text` surfaces required parameters one error at a time: calling `explain_error` with no params returns "workspaceId required"; supplying that returns "filePath required", etc. An agent must call N+1 times to discover N required params, burning turns and confusing retry logic. The error message should enumerate all missing required parameters in a single response. Anchors: `src/RoslynMcp.Host.Stdio/Tools/PromptShimTools.cs`, `src/RoslynMcp.Host.Stdio/Prompts/PromptMessageBuilder.cs`. Regression test shape: 1 fixture calling `get_prompt_text(explain_error, {})` and asserting the error lists all required params (workspaceId, filePath) in one response. Evidence: `review-inbox/archive/20260510T064835Z/20260510T053000Z_firewallanalyzer_mcp-server-surface-test.md` §8 + §14 (improvement); `review-inbox/archive/20260510T064835Z/20260510T052934Z_roslyn-backed-mcp_mcp-server-surface-test.md` §14 improvement. |
| `get-syntax-tree-range-truncates-at-statement` | Low | none | [gh #621](https://github.com/darylmcd/Roslyn-Backed-MCP/issues/621) — `get_syntax_tree` with `startLine=85, endLine=100` returns only the `LocalDeclarationStatement` at L85; the sibling `TryStatement` at L86 within the range is excluded. Range walker roots at the syntactic statement containing `startLine` without collecting sibling statements at the same nesting depth. See also `get-syntax-tree-maxtotalbytes-not-enforced` (same tool, different bug). Next deliverable: option A — fix the range walker to collect all top-level statement nodes whose span overlaps the requested line range; option B — document the current behavior so callers can pre-compute the parent block span. Anchors: `src/RoslynMcp.Roslyn/Services/SyntaxService.cs`, `src/RoslynMcp.Host.Stdio/Tools/SyntaxTools.cs`. Regression test shape: 1 fixture with a method containing a local-decl + try-catch on adjacent lines; call `get_syntax_tree(startLine=N, endLine=N+10)`; assert both top-level statements appear in the response. Evidence: gh #621; IT-Chat-Bot audit 2026-05-11 §13.5 (P3). |
| `goto-type-definition-builtins-invalidoperation` | Low | none | **Reserved — [gh #607](https://github.com/darylmcd/Roslyn-Backed-MCP/issues/607) (good first issue); skip in sweeps until contributor pickup.** `goto_type_definition` on a primitive or BCL built-in type (e.g. `bool`) returns `InvalidOperation: Cannot navigate to type definition for 'bool' — neither the type nor any of its type arguments are defined in source`. The documented use case for this tool is "for a variable, go to its type"; calling it on a built-in is the natural consequence. The error category `InvalidOperation` is debatable — `NotFound` with a note about "metadata-only type" would be more consistent with the tool's other "target not in source" responses, or the tool could return a metadata-reference sentinel location. Anchors: `src/RoslynMcp.Host.Stdio/Tools/SymbolTools.cs` (`goto_type_definition`). Regression test shape: 1 fixture covering `goto_type_definition` on a `bool` position and asserting either `NotFound` category or a metadata-source sentinel payload. Evidence: `review-inbox/archive/20260510T064835Z/20260510T053000Z_firewallanalyzer_mcp-server-surface-test.md` §7 row 5 (P3). |
| `list-analyzers-totalrules-variance` | Low | none | `list_analyzers.totalRules` reported 495 in the quick-tier firewall audit and 408 in the full-tier audit run 28 minutes later against the same workspace. The 87-rule swing is unexplained. Possible causes: paging vs full-walk mode, or analyzer-set evolution between sessions. If the count is paged, the field should be named `returnedRuleCount` to avoid implying it is a total; if it is always a full walk, the result should be deterministic across sessions. Anchors: `src/RoslynMcp.Host.Stdio/Tools/AnalyzerInfoTools.cs`. Regression test shape: 2 back-to-back calls to `list_analyzers` on the same workspace asserting identical `totalRules`. **Weaker evidence — single audit pair; defer until reproducible across two independent sessions.** Evidence: `review-inbox/archive/20260510T064835Z/20260510T053000Z_firewallanalyzer_mcp-server-surface-test.md` §14 + §18. |
| `move-type-to-file-rejects-single-type` | Low | none | [gh #626](https://github.com/darylmcd/Roslyn-Backed-MCP/issues/626) — `move_type_to_file_preview` rejects single-type source files with *"Source file only contains one top-level type. Nested types cannot be extracted."* Error message is wrong (rejection is about single-type files, not nested types) and the constraint eliminates the most common "rename misnamed file" use case. Next deliverable: option A — reconsider the no-op constraint (allow single-type move-to-file for rename-style use cases); option B — improve the error message to point at `move_file_preview` for rename-style operations. Anchors: `src/RoslynMcp.Roslyn/Services/TypeMoveService.cs`, `src/RoslynMcp.Host.Stdio/Tools/MoveTypeTools.cs`. Regression test shape: 1 fixture calling `move_type_to_file_preview` on a single-type source file; assert either a successful preview OR a structured error pointing at the rename alternative. Evidence: gh #626; IT-Chat-Bot audit 2026-05-11 §13.16 (P3). |
| `scaffold-test-preview-missing-usings` | Low | none | [gh #624](https://github.com/darylmcd/Roslyn-Backed-MCP/issues/624) — `scaffold_test_preview` / `_apply` generate test stubs that reference constructor-injected types from multiple namespaces without emitting the required `using` directives. Result: 7 CS0246 errors when constructor takes args from non-default namespaces. Workaround is `organize_usings_apply` after scaffold — but the tool should emit usings itself since it identifies the types correctly. Next deliverable: collect all namespaces for constructor parameter types in `ScaffoldingService` and emit `using` directives at the top of the generated file. Anchors: `src/RoslynMcp.Roslyn/Services/ScaffoldingService.cs` (test-preview path; framework builders at lines 760/820/874). Regression test shape: 1 fixture scaffolding a test for a class with constructor params from ≥3 namespaces; assert the generated file compiles without `organize_usings_apply`. Evidence: gh #624; IT-Chat-Bot audit 2026-05-11 §13.14 (P3). |
| `scaffolding-service-split-by-scaffold-type` | Low | none | Split `ScaffoldingService.cs` (2521 LOC, largest file in repo) into per-scaffold-type partials: `ScaffoldingService.TypePreview.cs`, `ScaffoldingService.TestPreview.cs`, `ScaffoldingService.TestBatchPreview.cs`, `ScaffoldingService.FirstTestFilePreview.cs`. Pure code organization — no behavior change. Phase 2 (separate row, file later) extracts per-framework builders (`BuildFirstTestFileMSTest`/`Xunit`/`NUnit` at lines 760/820/874) into a `TestFrameworkScaffolder` strategy. Anchors: `src/RoslynMcp.Roslyn/Services/ScaffoldingService.cs:38,92,329,455,760,820,874,1338,1704`. Regression test shape: existing `tests/RoslynMcp.Tests/ScaffoldingIntegrationTests.cs` must continue to pass unchanged after the split. Evidence: 2026-05-09 sanity-check audit — file owns 4 scaffold types × 3 test frameworks + sibling-pattern inference + interface resolution + constructor-arg generation; three recent scaffold bugs landed here (`scaffold-test-internal-target-accessibility` and `scaffold-test-batch-nullable-constructor-output` shipped via PR #575 and the upcoming PR closing this row's prior dep). **Weaker evidence — pure organizational refactor; defer until a concrete bug or modification motivates the split.** |
| `semantic-search-grep-pattern-broken` | Low | none | [gh #627](https://github.com/darylmcd/Roslyn-Backed-MCP/issues/627) — Two related bugs in one row: (1) `semantic_search` falls back to token/name matching for all tested queries; no embedding match observed (fallback is honest but the "semantic" label implies embedding-based retrieval that isn't demonstrably active); (2) `semantic_grep` returns 0 matches for `CancellationToken.*=.*default` on a 36-project solution containing many such patterns; pattern syntax is undocumented. Next deliverable: (a) document whether `semantic_search` requires a pre-built embedding index and how to enable it (if not yet active, consider renaming to `code_search` until it is); (b) document `semantic_grep` pattern syntax (ripgrep regex? literal? custom?) and verify with a known-present literal pattern. Anchors: `src/RoslynMcp.Roslyn/Services/SemanticSearchService.cs` (or `CodePatternAnalyzer.cs` — verify in code), `src/RoslynMcp.Host.Stdio/Tools/SemanticSearchTools.cs`. Regression test shape: 2 fixtures — (i) `semantic_grep` with a literal pattern guaranteed to match on the fixture asserts match count > 0; (ii) `semantic_search` returns at least one embedding-ranked result OR a structured "not yet indexed" hint. Evidence: gh #627; IT-Chat-Bot audit 2026-05-11 §13.17 (P3). |
| `set-conditional-property-error-msg-quoting` | Low | none | [gh #622](https://github.com/darylmcd/Roslyn-Backed-MCP/issues/622) — `set_conditional_property_preview` requires MSBuild-style single-quoting on the condition variable (`'$(Configuration)' == 'Release'`). Calling with the bare form fails with a cryptic message that doesn't mention the required quoting syntax. First-time callers fail silently. See also `set-project-property-preview-directory-build-props-blindness` (same tool family, different concern). Next deliverable: update the error message to include the expected format ("Use MSBuild-style quoting: `'$(Configuration)' == 'Release'`"). Anchors: `src/RoslynMcp.Host.Stdio/Tools/ProjectMutationTools.cs` (`set_conditional_property_preview` error path). Regression test shape: 1 fixture calling `set_conditional_property_preview` with the bare `$(Configuration) == 'Release'` form; assert the error message contains the literal example `'$(Configuration)' == 'Release'`. Evidence: gh #622; IT-Chat-Bot audit 2026-05-11 §13.11 (P3). |
| `set-project-property-preview-directory-build-props-blindness` | Low | none | `set_project_property_preview` does not detect when a property is already inherited from `Directory.Build.props`. The tool generates a new `<PropertyGroup>` in the target `.csproj` creating a redundant (though harmless) property entry. Callers must manually pre-check `Directory.Build.props` before calling; there is no guidance in the tool description. Fix: inspect the inherited MSBuild property graph and warn (or reject) when the requested property-value pair is already globally effective. Anchors: `src/RoslynMcp.Host.Stdio/Tools/ProjectMutationTools.cs`. Regression test shape: 1 fixture calling `set_project_property_preview(Nullable=enable)` on a project that inherits `Nullable=enable` from `Directory.Build.props` and asserting a `warning: already inherited` annotation in the response. Evidence: `review-inbox/archive/20260510T064835Z/20260510T053142Z_network-documentation_mcp-server-surface-test.md` §13.4 (P4 observation). |
| `symbol-search-pagination` | Low | none | [gh #617](https://github.com/darylmcd/Roslyn-Backed-MCP/issues/617) — `symbol_search` has no pagination parameters (`offset`, `limit`, or cursor). Broad queries on large solutions produce 69K+ responses that exceed the MCP transport cap, making the tool unusable without `kind`-filter narrowing. Sibling tools (`list_analyzers`, `test_discover`) have pagination. See also `tool-surface-pagination-or-tool-sets` for the broader server-wide pagination concern. Next deliverable: add `offset` (integer, default 0) and `limit` (integer, default 50, max 200) parameters to `symbol_search`; include `totalCount` and `hasMore` in the response envelope (mirrors `list_analyzers`). Anchors: `src/RoslynMcp.Host.Stdio/Tools/SymbolTools.cs` (`symbol_search`), `src/RoslynMcp.Roslyn/Services/SymbolSearchService.cs`. Regression test shape: 1 fixture calling `symbol_search` with a broad query on a fixture workspace that overflows without pagination; assert the response stays under 50 KB when `limit=50` is passed and `hasMore=true` when appropriate. Evidence: gh #617; IT-Chat-Bot audit 2026-05-11 §13.6 (P2). |
| `test-run-unfiltered-no-failure-envelope` | Low | none | **Reserved — [gh #611](https://github.com/darylmcd/Roslyn-Backed-MCP/issues/611) (good first issue); skip in sweeps until contributor pickup.** `test_run` without a filter on a large test suite (1136 tests) exceeds the MCP server wall-clock timeout and returns a bare invocation error with no `FailureEnvelope`. The same issue was previously documented for `validate_recent_git_changes` (where a `FailureEnvelope` IS returned). `test_run` should consistently return `FailureEnvelope(ErrorKind=Timeout, IsRetryable=true)` so callers can detect the timeout programmatically and retry with a narrower filter or project scope. Anchors: `src/RoslynMcp.Host.Stdio/Tools/TestCoverageTools.cs` (or `ValidationBundleTools.cs` — whichever handles `test_run`). Regression test shape: 1 fixture simulating a long-running test run that exceeds timeout and asserting a structured `FailureEnvelope` response. Evidence: `review-inbox/archive/20260510T064835Z/20260510T053142Z_network-documentation_mcp-server-surface-test.md` §8.4 (BUG-3, P3). |
| `tool-surface-pagination-or-tool-sets` | Low | none | 167 tools is approaching small-model discovery saturation. MCP `tools/list` already supports cursor pagination per spec; layered on top, named "tool sets" (`navigation`, `refactoring`, `validation`, `analysis`) would let clients enable subsets via `server_info` user-config. Speculative — not yet causing observable friction in the retro window — but the surface continues to grow. Track only; act when (1) a small-model client reports tool-discovery friction, OR (2) surface count crosses a hard threshold (~200 tools). Anchors: `src/RoslynMcp.Host.Stdio/Catalog/ServerSurfaceCatalog.cs`, `src/RoslynMcp.Host.Stdio/Resources/ServerResources.cs`. Regression test shape: defer until acted on. Evidence: surface count drift across recent versions (`server_info.surface.registered.tools` 151 → 167 since v1.27); MCP spec § tools/list pagination. **Weaker evidence — N until small-model discovery friction reported externally.** Source: 2026-05-05 MCP-best-practices comparison §3 rec J. See `ai_docs/reports/20260505_mcp-best-practices-restored-rows.md` for provenance. |
| `validate-recent-git-changes-timeout` | Low | none | `validate_recent_git_changes` has two related timeout symptoms across runs: (1) the deep audit (2026-05-08) saw a 120 s full-MCP-timeout on a small dirty set with no FailureEnvelope returned; (2) the network-documentation audit (2026-05-10) saw `git status` exceed a 10 s sub-timeout on Windows and fall back to full-workspace scope silently, losing test-discovery scoping. The first symptom is the more severe correctness issue (bare transport error, no structured envelope). The second symptom is a Windows-specific `git status` shelling latency issue. Next deliverable: (a) ensure `validate_recent_git_changes` always returns a structured `FailureEnvelope` with `ErrorKind=Timeout, IsRetryable=true` instead of a bare transport error on the 120 s path; (b) document the `git status` 10 s sub-timeout and Windows PowerShell fallback in the tool description so callers understand the full-workspace-scope fallback. Anchors: `src/RoslynMcp.Host.Stdio/Tools/ValidationBundleTools.cs`, `src/RoslynMcp.Host.Stdio/Tools/ValidationTools.cs`. Regression test shape: 1 fixture triggering the timeout path and asserting a `FailureEnvelope.ErrorKind=Timeout` response (not a bare invocation error). Evidence: `review-inbox/archive/20260510T064835Z/20260508T154415Z_roslyn-backed-mcp_mcp-server-audit.md` §7; `review-inbox/archive/20260510T064835Z/20260510T053142Z_network-documentation_mcp-server-surface-test.md` §8.7 (FLAG-2). |
| `workspace-load-prewarm-double-nullable` | Low | none | **Reserved — [gh #606](https://github.com/darylmcd/Roslyn-Backed-MCP/issues/606) (good first issue); skip in sweeps until contributor pickup.** `workspace_load`'s schema hint advertises `prewarm: bool??` (double-nullable). Passing `prewarm=true` throws `JsonException: The JSON value could not be converted to System.Nullable\`1[System.Boolean]`; the only callable shape is to omit the parameter. The network-documentation audit succeeded with `prewarm=true` at the tool level (workspace loaded with prewarm), suggesting the JSON deserialization issue is schema-layer-only. Fix: change the schema type to `bool?` (single-nullable) and accept `true`/`false` explicitly, or remove the parameter from the public schema and rely on the unconditional pre-warm path. Anchors: `src/RoslynMcp.Host.Stdio/Tools/WorkspaceTools.cs`, `src/RoslynMcp.Host.Stdio/Catalog/ServerSurfaceCatalog.Workspace.cs`. Regression test shape: 1 fixture calling `workspace_load(path=..., prewarm=true)` and asserting success (not a JsonException). Evidence: `review-inbox/archive/20260510T064835Z/20260510T053000Z_firewallanalyzer_mcp-server-surface-test.md` §7 row 1 (P3) + §14 improvement; `review-inbox/archive/20260510T064835Z/20260510T053142Z_network-documentation_mcp-server-surface-test.md` header. |
| `workspace-reloaded-during-call-conflates-notfound` | Low | none | [gh #628](https://github.com/darylmcd/Roslyn-Backed-MCP/issues/628) — When `get_source_text` is called with a nonexistent file path concurrently with an idempotent `workspace_load` reload, the returned `category` is `WorkspaceReloadedDuringCall` rather than `NotFound`. True cause (file not found) is in the message text, but `category`-routing callers see a misleading error type. See also `parallel-fanout-auto-reload-timeout-floor` (auto-reload family, different symptom). Next deliverable: after the reload completes, re-evaluate the original request; if the file still doesn't exist, return `category=NotFound`. Reload-race should be an implementation detail, not observable to the caller when the root cause is a bad path. Anchors: `src/RoslynMcp.Roslyn/Services/WorkspaceExecutionGate.cs`, `src/RoslynMcp.Roslyn/Services/WorkspaceManager.cs`, `src/RoslynMcp.Host.Stdio/Tools/SourceTextTools.cs` (or wherever `get_source_text` resolves its return). Regression test shape: 1 concurrency fixture invoking `workspace_load` + `get_source_text` (bad path) simultaneously; assert the bad-path error returns `category=NotFound`, not `WorkspaceReloadedDuringCall`. Evidence: gh #628; IT-Chat-Bot audit 2026-05-11 §13.18 (P3). |

## Defer

<!-- Explicitly parked. Record WHY in the `do` cell. -->

| id | pri | deps | do |
|----|-----|------|-----|
| `scorecard-blocked-to-backlog-row` | Defer | `reconcile-backlog-vs-github-issues` lands first | The aggregated promotion scorecard (`audit-reports/_aggregated-promotion-scorecard.json`) classifies experimental entries into `promote: ready` / `promote: blocked` / `needs-more-evidence`. Today only `promote: ready` items get consumed (by `/publish-preflight` Step 8 → `/promote-tier`). `promote: blocked` items (e.g., "0 of 4 repos see this tool pass round-trip") and `needs-more-evidence` items don't get tracked as backlog rows for investigation, so they silently accumulate across scorecard runs. Deferred because (a) the blocked count is small today (2 in the most recent run), (b) `/publish-preflight` surfaces them to the maintainer who can decide ad-hoc, and (c) the larger pattern of "scorecard verdict → backlog row" needs the reconcile skill to land first so we have an idempotent mechanism for writing/updating rows over time. Re-evaluate after `reconcile-backlog-vs-github-issues` ships and the reconcile pattern is proven. Anchors (when un-deferred): extension to `.claude/skills/backlog-intake/SKILL.md` Phase 0.5 or NEW `.claude/skills/intake-scorecard-blockers/SKILL.md`; reads `audit-reports/_aggregated-promotion-scorecard.json`. Evidence: 2026-05-11 adversarial audit, gap #2 in the post-skill pipeline report. |
| `validate-locator-preflight-tool` | Defer | re-measurement window after `inv-arg-envelope-schema-hint` (merged 2026-05-05 via PR #483) | Deferred 2026-05-05 after `inv-arg-envelope-schema-hint` shipped because the schema hint may make the standalone preflight tool obsolete. Re-measure InvalidArgument-on-locator-tools rate over a 7-day window before deciding whether to ship this tool. If `schemaHint` reduces locator-shape errors >=80%, mark obsolete; otherwise re-plan and ship. Re-evaluate after 2026-05-12. Anchors: new tool surface in `src/RoslynMcp.Host.Stdio/Tools/SymbolLocatorTool.cs`, reuses existing `src/RoslynMcp.Host.Stdio/Tools/SymbolLocatorFactory.cs` helper from PR #474, catalog registration. Regression test shape: 1 fixture covering valid file/line, valid metadataName, malformed metadataName (parenthesized), unparseable symbolHandle, fully empty locator. Evidence: 2026-05-04 multi-session retro sessions `dd0a7e48`, `5687cbf9`, `f71cbc02`, `eac7f094`, `28e53529`, `b093b4b1` hit post-hoc locator-shape errors that could benefit from pre-flight validation. |
| `http-streamable-host-project` | Defer | concrete remote-deployment driver (named users, auth/observability/tenancy plan approved and staffed) | Add `src/RoslynMcp.Host.Http/` as a sibling project that reuses `Core` + `Roslyn`, exposing the same surface over MCP Streamable HTTP transport. Roadmap-aligned but requires multi-week design (auth flows, per-tenant rate limiting, TLS, observability, deployment story). The current local-first product is healthy and the roadmap explicitly punts on these concerns. Re-evaluate when there is a concrete remote-deployment ask with named users / SLA expectations. Anchors: new `src/RoslynMcp.Host.Http/` project; `Core` + `Roslyn` boundaries are already transport-agnostic per `ai_docs/architecture.md`. Evidence: `docs/roadmap.md` § HTTP/SSE Hosting (deferred to a second host project); MCP spec § Streamable HTTP transport (2025-03 stable). Source: 2026-05-05 MCP-best-practices comparison §3 rec H. See `ai_docs/reports/20260505_mcp-best-practices-restored-rows.md` for provenance. |
| `workspace-process-pool-or-daemon` | Defer | future worse-profile evidence | Representative 227-project OrchardCore profile captured on 2026-04-26 did not justify daemon/process-pool implementation: `workspace_load` P95 was 44.85s, `symbol_search` P95 was 1.18s, and `find_references` P95 was 997ms, all below `docs/large-solution-profiling-baseline.md` thresholds. Keep this deferred unless a larger/worse customer-scale profile or daily-use evidence shows `workspace_load` / reload P95 blocking work after `workspace_warm`; then produce a bounded design note comparing daemon, process-pool, and shared-workspace approaches, including lifecycle and failure-isolation hooks. Evidence: `docs/large-solution-profiling-baseline.md` recorded OrchardCore run; local raw artifacts under `artifacts/large-solution-profiling/20260426T212443Z/`. |
| `workspace-manager-cache-store-extraction` | Defer | needs design note first | Extract `IWorkspaceCacheStore` from `WorkspaceManager.cs` (currently 2106 LOC). The class bundles five distinct concerns: workspace lifecycle, version tracking, MSBuild solution-graph caching, restore-artifact polling, and eviction recording. Phase 1 = cache subsystem only (`TryProbeWorkspaceCacheAsync`, `TryEnumerateNewestCacheEntryAsync`, `ResolveAndWriteCacheAsync`, `WriteFreshCacheEntryAsync`, `ComputeMsbuildGraphHash`, `BuildCachedGraphFromSolution`, `MetadataReferencesStillStableOnDisk`); restore-watcher and graph-hash invariants are separate rows once cache extraction lands. Cache logic is correctness-sensitive (graph-hash stability + metadata-reference last-write-time freshness checks) — refactor needs a design note at `ai_docs/items/workspace-manager-cache-store-extraction-design.md` before any code change, per `parameter-object-preview-tool` precedent. Anchors: `src/RoslynMcp.Roslyn/Services/WorkspaceManager.cs:738,784,878,906,967,996,1041`, `src/RoslynMcp.Roslyn/ServiceCollectionExtensions.cs`, NEW `src/RoslynMcp.Core/Services/IWorkspaceCacheStore.cs`. Regression test shape: 1 fixture covering hash-key stability across SDK changes + metadata-reference invalidation under file-mtime drift. Evidence: 2026-05-09 sanity-check audit — concerns 3 and 4 (cache + restore-detection) operate on `Solution` + filesystem and could be unit-tested without `MSBuildWorkspace`. **Weaker evidence — design pass not yet started; defer until the design note lands.** |

## Refs

| Path | Role |
|------|------|
| `ai_docs/planning_index.md` | Planning router and scope boundary |
| `ai_docs/workflow.md` | Branch/PR workflow and backlog-closure rule |
| `ai_docs/prompts/backlog-sweep-plan.md` | Planner prompt; enforces per-initiative Rule 1 (bundle only on shared code path) / Rule 3 (≤4 prod files) / Rule 3b (toolPolicy) / Rule 4 (≤3 test files) / Rule 5 (≤80K context). Each row here is sized for one initiative. |
| `ai_docs/prompts/backlog-sweep-execute.md` | Executor companion; consumes the `state.json` the planner emits and vets each initiative against Rules 3/4/5 before starting work. |
| `ai_docs/bootstrap-read-tool-primer.md` | Self-edit session read-only tool primer (Roslyn-MCP read-side tools to prefer over Bash/Grep). |
| `ai_docs/runtime.md` | Bootstrap scope policy — distinguishes main-checkout self-edit (no `*_apply`) from worktree/parallel-subagent sessions. |
| `docs/large-solution-profiling-baseline.md` | Evidence gate for daemon/process-pool performance work. |
| `ai_docs/procedures/deep-review-backlog-intake.md` | Intake procedure for future audit batches. |
| `review-inbox/` | Staging folder for the NEXT audit batch (flat directory; `/backlog-intake` reads here). |
| `review-inbox/archive/<batch-ts>/` | Processed audit/retro/promotion batches — one subdirectory per successful intake. Delete a batch after every actionable item is either shipped, explicitly rejected as stale, or summarized in current backlog rows. |
| `ai_docs/reports/20260505_mcp-best-practices-restored-rows.md` | Provenance + recovery context for the 14 backlog rows added 2026-05-05 from the MCP-best-practices comparison (rec A–J + Tier 1 follow-on). Maps each row id → recommendation letter; lists cross-cutting evidence sources. Read this first when picking up any of the rec-A through rec-J rows. |
| `ai_docs/plans/20260507T145540Z_backlog-sweep/plan.md` | Backlog sweep (2026-05-07T14:55:40Z). Shipped 1 initiative across 1 PR; closed 1 backlog row (`workspace-id-recovery-hints` via PR #542). |
| `ai_docs/plans/20260507T182128Z_backlog-sweep/plan.md` | Backlog sweep (2026-05-07T18:21:28Z). Shipped 6 initiatives across 6 PRs (#547, #548, #549, #550, #551, #552); closed 5 backlog rows (`mcp-server-stress-single-mode`, `audit-deep-relocate-and-rename`, `extract-skills-audit-from-server-stress`, `backlog-d-fragment-pattern`, `per-repo-promotion-scorecard`). Theme: maintainer-audit-skill redesign — collapse modes, relocate to `.claude/skills/mcp-server-stress/`, extract skills audit into surface-audit, add `backlog.d/` fragment pattern, per-repo promotion scorecards with quorum aggregation. |
| `ai_docs/plans/20260508T171439Z_backlog-sweep/plan.md` | Backlog sweep (2026-05-08T17:14:39Z). Shipped 12 initiatives across 12 PRs (#563, #565, #567, #569, #571, #573, #574, #575, #576, #584, #586, #587); closed 12 backlog rows. Theme: medium-priority MCP-server stability + new refactoring tools — analyzer file-lock fix, find_references metadata dedupe, project-reference cycle preview, find_overrides interface coverage, symbol_relationships token bucket fix, scaffolding nullable+internal accessibility, validate_recent_git_changes timeouts, promotion-scorecard 2026-04-27 review, dry-run preview audit, `change_signature_preview(op=reorder)`, and the new `parameter_object_preview` tool. |
