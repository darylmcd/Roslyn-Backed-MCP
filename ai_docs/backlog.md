# Next work and backlog

<!-- purpose: Open work only; contract for agents syncing backlog on ship. -->

**updated_at:** 2026-04-14T18:00:00Z

## Agent contract

**Scope:** This file lists **unfinished work only**. It is not a changelog.

| | |
|---|---|
| **MUST** | Remove or update backlog rows when work ships; do it in the **same PR** as the implementation (or an immediate follow-up). |
| **MUST** | End implementation plans with a final todo: `backlog: sync ai_docs/backlog.md` when closing backlog-related items. |
| **MUST** | Use stable, kebab-case `id` values per open row (grep-friendly). |
| **MUST NOT** | Add "Completed" tables, dated history sections, or narrative changelogs here. |
| **MUST NOT** | Leave done items in the open table. |

## Standing rules (ongoing)

These are **not** backlog rows; do not delete them when clearing completed work.

- Reprioritize on each audit pass.
- Bug and tool items: put reproduction and session context in the PR or linked issue when it helps reviewers.
- Broader planning and complexity notes stay in this file; `ai_docs/archive/` holds policy only (see `archive/README.md`).

## Open work

Ordered by severity: **P2** (blocks major functionality on real-world repos) → **P3** (accuracy, UX, API clarity) → **P4** (docs, hygiene, cosmetic). Within a tier, rows are **alphabetical by `id`**.

| id | pri | blocker | deps | do |
|----|-----|---------|------|-----|
| `agent-symbol-refactor-unified-preview` | P3 | — | `roslyn-mcp-multi-file-preview-apply` |  **Agent UX:** Cross-project symbol work (e.g. new enum member, rename) today requires many hand edits or disjoint previews. **Goal:** One reviewed change unit spanning Domain → Infrastructure → Api → tests (or configurable project set) with a single preview token / apply path. **Plan:** Map to composite preview, `rename_preview`/`rename_apply` with multi-document diff, or extend `apply_composite_preview`; define rollback. **Evidence:** Consumer implementation used editor patches while Roslyn disconnected (`20260413T154959Z_firewallanalyzer_mcp-server-audit.md` narrative); overlaps `roslyn-mcp-guided-refactor-flow` / `roslyn-mcp-multi-file-preview-apply` — consolidate design in one PR or link deps explicitly. |
| `composite-split-service-di-registration` | P3 | — | `roslyn-mcp-multi-file-preview-apply` | **Agent gap:** Splitting one DI-consumed type into several types + a forwarding facade (same public methods) plus **new `AddTransient`/`AddSingleton` lines** is today many disconnected edits (`split_class` / new files / hand-edited composition root). **Goal:** One preview (or documented composite) that: extracts implementation classes, generates facade delegating to them, and proposes registration deltas for the host `Program`/`ApiHostBuilder`-style file. **Evidence:** DotNet-Firewall-Analyzer session (2026-04): `OfflineImporter` → `RunningConfigImporter` + `ApiResponseImporter` + `SnapshotBundleImporter` + facade + four registrations in `ApiHostBuilder.cs` — all done via editor/StrReplace without Roslyn MCP. **Overlaps:** `agent-symbol-refactor-unified-preview`. |
| `concurrent-mcp-instances-no-tools` | P3 | — | — | **Bug:** When multiple Roslyn MCP instances start concurrently (e.g. deep-review prompt dispatched against several repos in parallel), the **first** instance registers its tools correctly, but **subsequent** instances report "server started" with **no tools available**. Agent calls to `workspace_load` and other tools fail because the tool list appears empty to the client. **Reproducibility:** Observed when starting deep-review prompt across multiple repos in parallel (user report 2026-04-14). **Impact:** Blocks the primary multi-repo audit workflow; parallel audits fall back to serial execution or fail outright. Likely root cause of some observations previously attributed to cold-start latency (see `compilation-prewarm-on-load`). **Investigation:** Determine whether this is (a) stdio handle contention between instances, (b) a plugin registration race in Claude Code MCP host, (c) a shared resource (named pipe, socket, file lock) held by the first instance, or (d) the deferred-tool schema load failing silently on subsequent instances. **Fix:** Depends on root cause — options include per-instance isolation, explicit startup sequencing, or diagnostic logging when tool registration fails so the underlying error surfaces instead of returning an empty tool list. **Evidence:** User report in ship-pipeline session 2026-04-14: "subsequent repos would report the server was started but no tools being reported as available." **Refs:** `RoslynMcp.Host.Stdio.Program`; Claude Code MCP host; related `mcp-connection-session-resilience`; blocks `compilation-prewarm-on-load`. |
| `mapper-snapshot-dto-symmetry-check` | P3 | — | — | **Agent gap:** Adding a domain property that must round-trip through **snapshot/DTO records** (`JsonPropertyName`) and **bidirectional mappers** (`To*` / `From*`) spans 3+ files; asymmetry (e.g. set on write, null on read) is easy to miss and Roslyn rename alone does not cover JSON contracts. **Goal:** Tool: given a named type or file, list paired mapper methods + DTO record init paths and flag likely missing branches; or extend `mechanical-sweep-after-symbol-change` with “persistence layer” template. **Evidence:** Same session: `ManagedDevice.RegisteredIpTags` + `SnapshotManagedDevice` + `SnapshotMapper` To/From changes. **Refs:** `find_references`, `document_symbols`; complements `mechanical-sweep-after-symbol-change`. |
| `roslyn-mcp-claude-direct-tool-registration` | P3 | — | — | **Agent gap:** The Roslyn MCP server is exposed to Claude Code only via **skills** (`roslyn-mcp:analyze`, `roslyn-mcp:complexity`, etc.), which provide instructions for the agent to follow but do not register the underlying MCP tools (`workspace_load`, `compile_check`, `find_references`, etc.) as **deferred tools** that the agent can invoke directly via ToolSearch. This means every Roslyn tool call must be routed through a sub-agent (`Agent` tool with `general-purpose` subagent_type) that spawns `roslynmcp` on its own — adding 10s workspace_load overhead per agent and preventing the parent agent from making interleaved tool calls. **Comparison:** The `python-refactor` MCP server in the same harness exposes 88+ tools as `mcp__python-refactor__*` deferred tools, callable directly without spawning agents. **Impact:** A typical refactoring session that needs 5 Roslyn calls pays 5 × 10s = 50s of redundant workspace loads instead of 1 × 10s + 5 × tool-cost. Multi-agent orchestration also can't share workspace state. **Fix:** Register Roslyn MCP as a deferred-tool MCP server in the Claude Code plugin manifest (analogous to `python-refactor`), so tools appear as `mcp__roslyn-mcp__workspace_load` etc. and can be invoked directly via ToolSearch. **Evidence:** Audit session 2026-04-13 dispatched 9 agents because direct Roslyn tools were unavailable; stress test session 2026-04-14 dispatched 4 more (13 cumulative agent invocations × ~10s reload = ~130s of overhead). **Frequency:** Every Claude Code session that uses Roslyn MCP. **Refs:** Claude Code plugin manifest under `plugin/`; MCP server registration patterns. |
| `mcp-connection-session-resilience` | P3 | — | — | **Agent blocker:** MCP clients may report **`Not connected`** for `user-roslyn` / `roslyn`, causing agents to bypass Roslyn entirely (policy violation in consumer repos). **Goal:** Faster reconnect, clearer server-side readiness, or documented **minimum host setup** (single page: Cursor server id, stdio path, workspace_load order). **Plan:** Distinguish transport down vs workspace not loaded vs auth; optional heartbeat tool or enrich `server_info` with connection state. **Evidence:** Live consumer session: `call_mcp_tool` → `Not connected` during refactor; `20260413T154959Z_firewallanalyzer_mcp-server-audit.md` Phase 0 / closure. **Refs:** Host integration doc under `docs/` or plugin README; `server_info` implementation. **Note (2026-04):** A FirewallAnalyzer remediation run used **zero** Roslyn `call_mcp_tool` invocations even when implementing multi-file C# refactors — connection health and “default to Roslyn after `workspace_load`” docs are necessary but not sufficient; orchestrators still favor editor `apply_patch` unless constrained. |
| `mechanical-sweep-after-symbol-change` | P3 | — | — | **Agent UX:** After adding enum members or renaming symbols, agents manually grep for **non-exhaustive switches**, **mappers**, and **test factories**. **Goal:** Tool or guided recipe: given `workspaceId` + symbol handle (or name), return **tasks** or **diagnostics** list: switch exhaustiveness, `ParseEnum`/mapper callsites, test data. **Plan:** Compose `find_references`, `project_diagnostics`, optional analyzer IDs; or new `symbol_impact_sweep` returning structured JSON. **Evidence:** Consumer `RuleAction.Unknown` touched parser, API formatter, simulator, tests — mechanical follow-ups. **Refs:** `find_references`, `enclosing_symbol`, code-fix / analyzer integration. |
| `static-test-reference-map` | P3 | — | — | Feature request: add a tool that statically maps source symbols to test references. Current Roslyn MCP has `test_coverage` (runtime coverlet) and `test_related` (heuristic name matching), but no static reference-based mapping. **Suggested API:** `test_reference_map(workspaceId, project?)` → `{ coveredSymbols, uncoveredSymbols, coveragePercent }` based on reference analysis from test projects to source projects. |
| `string-literal-to-constant-extraction` | P3 | — | — | **Tool gap:** No Roslyn MCP tool supports finding scattered string literal usages and replacing them with references to a newly created constants class. `bulk_replace_type_preview` operates on **type references** only; `find_references` resolves **symbol** references, not string literals; `semantic_search` / `symbol_search` do not index string literal values. **Goal:** Tool or composite: given a set of `{ literalValue, replacementExpression }` pairs and an optional file/project scope, find all string literal usages matching each value and preview replacements with the constant expression (adding `using` directives as needed). Consider Roslyn `SyntaxWalker` over `LiteralExpressionSyntax` nodes. **Evidence:** DotNet-Firewall-Analyzer backlog remediation (2026-04, session `a721fbeb`): `object-type-constants` task required replacing ~49 string literal occurrences (`"address"`, `"address-group"`, `"service"`, `"service-group"`, `"application"`, `"security"`) across 8 C# files with `PanOsObjectTypes.*` constant references. All replacements were done via `Grep` + `StrReplace` — zero Roslyn calls. Manual approach was error-prone (must match exact string values, distinguish semantic context, add correct `using`). A Roslyn-backed tool would give: (a) semantic certainty that only string-literal-in-argument-position matches are replaced (not string content in XML comments or log messages), (b) automatic `using` directive addition, (c) preview/apply atomicity. **Frequency:** High — magic-string centralization is a recurring codebase hygiene pattern. **Refs:** `bulk_replace_type_preview` (type-only), `find_references` (symbol-only); consider new `find_string_literals` + `replace_literals_preview` pair. |
| `compilation-prewarm-on-load` | P4 | — | `concurrent-mcp-instances-no-tools` | **Perf:** Multiple tools suffer cold-start penalties because Roslyn lazily compiles projects on first access. First `symbol_search` takes 4669ms vs 40ms warm (100x); first `go_to_definition` cross-project takes 2517ms vs 1ms warm; first `symbol_info` takes 1445ms vs 1ms warm. **Impact:** Agents hit multi-second pauses on first interaction with each project. Every tool's first call per compilation unit pays this tax. **User preference (2026-04-14):** Cold-start penalty is acceptable; **do NOT** add aggressive pre-compilation that blocks tool registration. Observed failure mode: when running deep-review sessions against multiple repos concurrently, the first repo MCP instance registers tools cleanly, but subsequent instances report "server started" with **no tools available**. Keeping `workspace_load` lean is preferred to eliminate this failure mode. **Scope narrowed to:** (a) opt-in `workspace_warm(workspaceId, projects?)` tool only — never run automatically on load; (b) investigate whether observed cold starts are actually Roslyn lazy compilation, or are a symptom of the concurrent-instance tool-registration bug (`concurrent-mcp-instances-no-tools`) — fix that first. **Evidence:** `20260414T120000Z_jellyfin_stress-test.md` Phase 6 (symbol_info cold 1445ms, go_to_definition cold 2517ms), Phase 2 (symbol_search cold 4669ms); `20260413T120000Z_jellyfin_mcp-server-audit.md` §6 (Phase 3 cold starts); user report 2026-04-14 ship-pipeline session. **Refs:** Roslyn lazy `Compilation` resolution; `workspace_load` handler; dependency `concurrent-mcp-instances-no-tools`. |
| `host-parallel-mcp-reads` | P4 |  `host` | — | **Throughput:** Some MCP hosts **serialize** all tool calls; read-only tools (`find_references`, `document_symbols`, `project_diagnostics` preview-only) cannot run concurrently. **Server side:** Document thread-safety of read paths; avoid shared mutable state without gate. **Host side:** Allow parallel `call_mcp_tool` for read-only operations where the spec permits. **Evidence:** `20260413T154959Z_firewallanalyzer_mcp-server-audit.md` §16–17 (parallel sections N/A — Cursor). **Refs:** `WorkspaceExecutionGate`, per-workspace read locks; consumer Cursor docs. |
| `prompt-tools-exposable-to-agents` | P4 | `host` | — | **Coverage gap:** MCP **prompt** tools (e.g. `explain_error`, `review_file`) may be **non-invokable** via `call_mcp_tool` in some Cursor configurations — audits mark prompts blocked. **Goal:** Either expose equivalent as **regular tools**, document host limitation prominently in `server_info` / catalog, or ship Claude plugin shims. **Plan:** Inventory prompts in catalog; matrix host × invocation path. **Evidence:** `20260413T154959Z_firewallanalyzer_mcp-server-audit.md` §10 (19 prompts blocked). **Refs:** MCP prompts registration; Cursor MCP docs. |
| `preview-token-cross-process-handoff` | P4 | — | `roslyn-mcp-claude-direct-tool-registration` | **Agent gap:** Preview tokens (from `rename_preview`, `extract_method_preview`, `format_document_preview`, etc.) are scoped to the spawning `roslynmcp` process. When orchestrators dispatch separate sub-agents — e.g. Agent A discovers a refactoring opportunity and produces a preview, Agent B reviews and applies — the apply agent cannot redeem Agent A's token because it spawns its own process and gets a fresh empty token store. **Impact:** Multi-agent refactoring workflows are forced to bundle preview+review+apply into a single agent call (large context) instead of pipelined hand-offs (smaller, focused agents). Also blocks audit patterns where one agent records preview tokens and a later "apply phase" agent commits approved changes. **Goal options:** (a) Persist preview tokens in a workspace-scoped on-disk store (`%TEMP%/roslyn-mcp/{workspaceVersion}/{token}.json`) with TTL; (b) Long-lived daemon mode where multiple clients share one process; (c) Document that tokens are non-portable and provide a `serialize_preview(token)` / `deserialize_preview(blob)` pair for explicit cross-process handoff. **Evidence:** Audit session 2026-04-13: Phase 6j extract_method preview token `2153fcd659284ff78da6c16056a4bb18` was generated and applied within the same agent process; no cross-agent handoff was attempted because tokens were known to be non-portable. **Refs:** Preview token storage in `*_preview` handlers. |
| `roslyn-fetch-resource-timing` | P4 | — | — | **`fetch_mcp_resource`** intermittently returns "server not ready" for `roslyn://` workspace URIs in Cursor — determine whether host timing, server readiness gate, or documentation gap. **Agent impact:** Agents fall back to `workspace_list` / tool substitutes when URI fetch flakes; fix increases trust in catalog-driven workflows. **Evidence:** `20260413T154959Z_firewallanalyzer_mcp-server-audit.md` §9 / §15; Phase 15 resource rows in §3 ledger. **Refs:** Resource handlers; readiness gate vs `workspace_load` completion. |
| `roslyn-mcp-guided-refactor-flow` | P4 | — | — | **Goal:** Lower-friction “implement this refactor” loop: intent + target file(s) → single preview → apply → optional chained validation (build/diagnostics). **Plan inputs:** Evaluate existing prompts/tools (`refactor_and_validate`, `apply_composite_preview`, `split_class_preview`, code-action flows); identify gaps vs. common agent edits (extract private helpers, deduplicate blocks, split types to new file). **Evidence:** Same consumer session: discovery via MCP, implementation via manual editor edits. **Additional evidence (2026-04):** FirewallAnalyzer remediation (drift equality, `RulebasePanOs`/`HaStatePanOs` extractions, telemetry wiring, middleware tweak, importer split) was **entirely** hand-edited — no Roslyn tool calls in the orchestrator trace; reinforces need for a **single guided loop** so policy-compliant sessions default to preview/apply instead of parallel `Task` exploration + patch. **Refs:** Experimental vs stable tool tiers in catalog; promotion / audit docs under `ai_docs/audit-reports/`. |
| `roslyn-mcp-multi-file-preview-apply` | P4 | — | — | **Goal:** First-class multi-file preview/apply (or “extract type to new file + fix references”) so refactors spanning several files are one atomic proposal. **Plan inputs:** Map to `apply_multi_file_edit`, `move_file_*`, `scaffold_type_*`, or new composite; define rollback and partial-apply policy. **Evidence:** Consumer refactor split web collection DTOs into a new file while updating endpoints — coordinated manual edits. **Additional evidence (2026-04):** FirewallAnalyzer `OfflineImporter` split into three importers + facade + DI registrations — ideal atomic multi-file apply. **Refs:** Multi-file edit handlers; `move_type_to_project_*` / related tools. |
| `roslyn-mcp-post-edit-validation-bundle` | P4 | — | — | **Goal:** Single documented MCP recipe or composite tool: `compile_check` + `project_diagnostics` on touched projects + `test_related_files` / `test_run` with returned filter; fail loudly if any step is skipped. **Plan inputs:** Document host-specific MCP tool invocation (e.g. Cursor `call_mcp_tool` naming) in server or plugin docs; optional server-side `validate_workspace` wrapper chaining build + diagnostics + test filter. **Evidence:** Consumer session: post-refactor verification fell back to `dotnet build` / `dotnet test` when MCP compile was not invoked successfully; tool name mismatch observed (`CallMcpTool` vs `call_mcp_tool`). **Refs:** `compile_check`, `project_diagnostics`, `test_related_files`, `test_run` handlers. |
| `roslyn-mcp-workspace-staleness` | P4 | — | — | **Goal:** After out-of-band disk edits (agent editors, git checkout, etc.), make stale workspace state obvious and cheap to fix: auto-detect staleness, prompt `workspace_reload`, or document mandatory reload before `compile_check` / `project_diagnostics`. **Plan inputs:** MSBuildWorkspace on-disk model vs open workspace; define `workspaceVersion` / snapshot bump rules and client-facing messages in `workspace_status`. **Update (2026-04):** `workspace_status` / `workspace_health` summaries now include `isReady` + explicit stale/error signals; remaining scope is auto-reload or stronger staleness detection. **Refs:** `workspace_load`, `workspace_reload`, `workspace_status` implementation and MCP tool descriptions. |
| `scaffold-test-batch-mode` | P4 | — | — | **Agent UX:** `scaffold_test_preview` generates stubs for one type at a time. When writing tests for a set of related types (e.g., all validators in a namespace, all endpoint handler classes), agents must invoke the tool N times or — more commonly — skip it entirely and hand-write all test files. **Goal:** Batch mode: `scaffold_test_batch_preview(workspaceId, testProjectName, targetTypes: [{typeName, methodName?}])` → returns a composite preview with one test file per target type. Alternatively, accept a namespace or project name to auto-discover public types lacking test coverage and scaffold stubs for all of them. **Evidence:** DotNet-Firewall-Analyzer backlog remediation (2026-04, session `a721fbeb`): `test-validators` task created 5 new test files (~400 LOC) for `CollectionRequestValidator`, `AnalysisJobRequestValidator`, `RetryJobRequestValidator`, `RuleQueryParamsValidator`, and `ValidationFilter<T>`. `test-api-endpoints` task created 4 more test files + 2 helpers. All 9 files were written from scratch via `Write` tool — zero `scaffold_test_preview` calls. Batch scaffolding would have provided: correct namespaces, `using` directives, constructor injection patterns, and method stubs, saving 60%+ of boilerplate. **Frequency:** Medium-high — test creation for multiple related types is a standard task in every implementation PR. **Refs:** `scaffold_test_preview`, `test_related`, `test_discover` handlers. |
| `securitycodescan-currency` | P4 | — | `add-net-analyzers` | SecurityCodeScan.VS2019 (v5.6.7) is archived. Consider migrating to SecurityCodeScan.VS2022 or relying on NetAnalyzers once added. Low urgency — current rules still fire and `TreatWarningsAsErrors` is enabled. |
| `test-mock-drift-new-interface-usage` | P4 | — | `static-test-reference-map` | **Agent gap:** Production code adds calls to **new or newly used** `interface` methods (`IPanosClient.GetHaStateAsync`, etc.); tests using `Substitute.For<T>()` keep passing until runtime null/unconfigured behavior bites. Roslyn does not link “interface member referenced from production project” → “test setup must stub.” **Goal:** Heuristic tool: given interface symbol + optional callsite file, list test projects that substitute that interface and flag methods with no `.Returns()` / `Configure()` in fixture setup (best-effort syntactic scan). **Evidence:** FirewallAnalyzer session: `CollectionService` gained telemetry calls; `CollectionServiceTests.SetupDefaultMocks` had to add `GetHaStateAsync` / `GetRegisteredIpsAsync` defaults. **Refs:** `find_references`, `test_related`; overlaps `static-test-reference-map`. |
| `test-run-file-lock-fast-fail` | P4 | — | — | When `test_run` or `test_coverage` hits an MSBuild file lock (MSB3027/MSB3021), MSBuild retries 10 times with 1-second delays. Consider detecting the lock condition earlier and returning the FailureEnvelope sooner. |
| `workspace-process-pool-or-daemon` | P4 | — | `compilation-prewarm-on-load`, `roslyn-mcp-claude-direct-tool-registration` | **Perf:** Each new `roslynmcp` subprocess starts with zero workspaces and pays the full ~10s `workspace_load` cost on first call. For multi-agent orchestrators that spawn one process per sub-agent, the cumulative reload cost dominates wall-clock time. **Measured impact:** Audit session 2026-04-13 spawned 9 agents (~90s reload overhead); stress test session 2026-04-14 spawned 4 agents (~40s overhead). For a typical Claude Code session the cumulative reload tax is 1–3 minutes — comparable to actual analysis work. **Goal options:** (a) Long-running daemon mode (`roslynmcp --daemon --socket /tmp/roslyn.sock`) where short-lived clients connect over a unix/named-pipe socket and share the loaded workspace; (b) Process pool with workspace state hand-off via shared memory or temp files; (c) Allow the Claude Code plugin to reuse a single roslynmcp process across all tool calls (combined with `roslyn-mcp-claude-direct-tool-registration`). **Distinction from `compilation-prewarm-on-load`:** That item is about prewarming compilations within one process; this item is about sharing the loaded workspace state across multiple processes/clients. **Evidence:** Both audit sessions in 2026-04 showed the same per-agent reload signature (~9-10s) before any productive work began. **Refs:** `WorkspaceManager`, stdio host bootstrap. |

## Refs

| Path | Role |
|------|------|
| `ai_docs/workflow.md` | Branch/PR workflow; **Backlog closure** |
| `ai_docs/backlog.md` | This file (open work + contract) |
| `ai_docs/audit-reports/` | Raw deep-review audits (inputs for prioritization) |
| `ai_docs/audit-reports/20260413T120000Z_jellyfin_mcp-server-audit.md` | Full-surface audit (Jellyfin, 40 projects): §14 bugs (7 issues), §7 schema drift (11 entries), §6 perf baseline, §12 promotion scorecard, §16 prompt verification, §17 boundary testing |
| `ai_docs/audit-reports/20260414T120000Z_jellyfin_stress-test.md` | Performance stress test (Jellyfin, 40 projects): 95 tool calls, timing baselines per category, throughput/rapid-fire stats, recovery/edge cases, scalability assessment |
| `ai_docs/audit-reports/20260413T154959Z_firewallanalyzer_mcp-server-audit.md` | Full-surface audit: §6 perf, §7/§14 error paths, §10 prompts blocked, §16–17 concurrency, ledger/resource/tool evidence |
| `ai_docs/audit-reports/20260413T175254Z_firewallanalyzer_experimental-promotion.md` | Experimental tool promotion + apply/diff notes (cross-ref with §9 in roslyn-backed-mcp promotion doc) |
| `ai_docs/procedures/deep-review-backlog-intake.md` | Multi-repo audit import / backlog merge (`eng/new-deep-review-batch.ps1`) |
| `eng/import-deep-review-audit.ps1` | Ingest external audit markdown into this repo (if present at repo root) |
| `src/RoslynMcp.Host.Stdio/Tools/WorkspaceTools.cs` | Workspace lifecycle (`workspace_load`, `workspace_reload`, `workspace_close`) — cited in firewall audit Phase 16 concurrency prose |
| Consumer: `.github/copilot-instructions.md` (DotNet-Firewall-Analyzer) | **Roslyn MCP required when available** — documents agent policy when MCP disconnect forces hand edits |
| Qualitative: DotNet-Firewall-Analyzer backlog remediation (2026-04, orchestrator session) | **Roslyn MCP not used** (`call_mcp_tool` / Roslyn server): work used Read/StrReplace, explore `Task`, `dotnet build`/`test`/`format`. **Highest-efficiency Roslyn tools for that work:** `find_references` / `rename_preview` for shared helpers (`RulebasePanOs`, `HaStatePanOs`), `split_class` / `move_type_to_file` + multi-file apply for importer split, `project_diagnostics` scoped to touched projects post-edit. **New backlog rows:** `composite-split-service-di-registration`, `mapper-snapshot-dto-symmetry-check`, `test-mock-drift-new-interface-usage`. **Existing rows still primary:** `agent-symbol-refactor-unified-preview`, `mechanical-sweep-after-symbol-change`, `mcp-connection-session-resilience`, `roslyn-mcp-guided-refactor-flow`, `roslyn-mcp-multi-file-preview-apply`. |
| Qualitative: DotNet-Firewall-Analyzer backlog remediation session 2 (2026-04, session `a721fbeb`) | **Zero Roslyn MCP calls** across 5 C# refactoring tasks (25 `StrReplace`, 7 `Write`, 16 shell build/test/format invocations). **Task-by-tool mapping (missed opportunities):** (1) `sec-apikey-header-only` — low miss, single-line edit; `compile_check` would have been faster than `dotnet build`. (2) `test-validators` — medium miss; `scaffold_test_preview` should have generated stubs for 5 test classes. (3) `dead-code-cleanup` — **high miss**; `find_unused_symbols` → `find_implementations` → `remove_dead_code_preview/apply` was the exact tool chain for removing `GetSnapshotSummaryAsync`/`WriteRawXmlAsync` from `ISnapshotStore` + `FileSnapshotStore`. (4) `object-type-constants` — **high miss** for `move_type_to_project_preview` (moving `SnapshotObjectKind` from Application → Domain); **tool gap** for string-literal-to-constant replacement (~49 literals across 8 files). (5) `test-api-endpoints` — medium miss; `scaffold_test_preview` for endpoint test stubs. **Cross-cutting:** `compile_check` never called (agents used `dotnet build` via shell); `format_document_preview` never called (agents used `dotnet format` via shell); `workspace_load` never called (no `workspaceId` available). **New backlog rows:** `dead-interface-member-removal-guided`, `string-literal-to-constant-extraction`, `format-verify-solution-wide`, `scaffold-test-batch-mode`. **Existing rows reinforced:** `mcp-connection-session-resilience`, `roslyn-mcp-guided-refactor-flow`, `roslyn-mcp-post-edit-validation-bundle`, `roslyn-mcp-workspace-staleness`, `move-type-to-project-namespace`. |
| Qualitative: Jellyfin audit + stress test session 3 (2026-04-13/14, Claude Code 13-agent dispatch) | **Roslyn MCP usage profile:** Audit prompt designed to exercise every tool; agents used Roslyn MCP for ~95% of work via spawned `roslynmcp` subprocesses (no direct deferred-tool registration available to orchestrator). **Refactoring tasks performed (Phase 6 only):** `organize_usings_apply`, `set_diagnostic_severity`, `add_pragma_suppression`, `apply_text_edit`, `apply_multi_file_edit`, `extract_method_apply` — all via MCP. **One forced manual fallback:** `extract_method_apply` of `ApplyMinWidthFilter` produced CS0136+CS0841 (`var` redeclaration); agent had to make a manual `Edit` to remove `var` (validates `extract-method-apply-var-redeclaration` P2 bug). **Other manual fallbacks (none code-modification):** orchestrator wrote audit/stress test reports via `Write`/`Edit`; ran CLI `dotnet build` + `dotnet test` for baseline comparison (would have been MCP-routed if `compile_check`/`test_run` had been directly invokable from the orchestrator instead of via sub-agents). **Process-level observations:** 13 cumulative agents × ~10s reload = ~130s overhead; agents independently rediscovered NDJSON protocol details ~9 times; `find_unused_symbols`+`type_hierarchy`+`find_implementations`+`member_hierarchy`+`impact_analysis`+`suggest_refactorings` all blocked by `UnresolvedAnalyzerReference` (already P2). **New backlog rows:** `apply-with-verify-and-rollback`, `get-source-text-line-range-ignored`, `mcp-stdio-protocol-onboarding-docs`, `roslyn-mcp-claude-direct-tool-registration`, `long-running-tool-progress-notifications`, `preview-token-cross-process-handoff`, `workspace-process-pool-or-daemon`. **Existing rows reinforced:** `extract-method-apply-var-redeclaration`, `unresolved-analyzer-reference-crash`, `compilation-prewarm-on-load`, `code-fix-providers-missing-ca`, `project-diagnostics-large-solution-perf`. |
