# Next work and backlog

<!-- purpose: Open work only; contract for agents syncing backlog on ship. -->
<!-- scope: in-repo -->

**updated_at:** 2026-04-24T15:25:00Z

## Agent contract

**Scope:** this file lists unfinished work only. It is not a changelog.

| | |
|---|---|
| **MUST** | Remove or update backlog rows when work ships; do it in the same PR or an immediate follow-up. |
| **MUST** | End implementation plans with a final todo: `backlog: sync ai_docs/backlog.md`. |
| **MUST** | Use stable, kebab-case `id` values per open row. |
| **MUST NOT** | Add completed tables, dated history sections, or narrative changelogs here. |
| **MUST NOT** | Leave done items in the open table. |

## Standing rules

- Reprioritize on each audit pass.
- Keep rows terse: summarize the current need, not the full session history.
- Keep rows planner-ready: name live anchors and the next concrete deliverable or investigation output.
- Replace stale umbrella rows with concrete follow-ons before planning against them.
- Put long-form audit evidence in the referenced reports, not in this file.
- Size every row to a single `backlog-sweep-plan.md` initiative: one code path, ≤4 production files, ≤3 test files, one regression-test shape. Split heroic multi-bug rows into per-bug children before a sweep runs against them.

## P2 — open work

| id | pri | deps | do |
|----|-----|------|-----|

## P3 — open work

| id | pri | deps | do |
|----|-----|------|-----|
| `add-package-reference-preview-cpm-duplicate-detection` | P3 | — | `add_package_reference_preview(Meziantou.Analyzer 3.0.50)` emits a second `<PackageReference>` in the diff even when the package is already present transitively via `Directory.Build.props` / `Directory.Packages.props`. Consult the evaluated PackageReference graph (already available via `evaluate_msbuild_items`) inside `src/RoslynMcp.Roslyn/Services/ProjectMutationService.cs` (tool: `src/RoslynMcp.Host.Stdio/Tools/ProjectMutationTools.cs`) and return `status: "already-present"` (or a structured refusal) before writing. |
| `auto-reload-retry-inside-call` | P3 | — | `format_document_preview` / `get_completions` / `find_references_bulk` frequently return `"Document not found"` or empty with `_meta.staleAction="auto-reloaded"` set — the server self-healed but the caller still saw an error and had to retry. In the stale-snapshot handler shared across tools (`src/RoslynMcp.Roslyn/Services/WorkspaceManager.cs` + the per-tool wrappers under `src/RoslynMcp.Host.Stdio/Tools/`), retry the operation once against the refreshed snapshot before surfacing the error; annotate retried calls with `_meta.retriedAfterReload=true`. Cancellation token caps total time. |
| `change-signature-preview-add-unhelpful-error` | P3 | — | `change_signature_preview(op=add, name=cancellationToken, parameterType=System.Threading.CancellationToken)` fails with `"Parameter 'index' has an out-of-range value"` — "index" is an internal index, not a schema field. `op=rename` and `op=reorder` work cleanly. Improve the add-path error in `src/RoslynMcp.Roslyn/Services/ChangeSignatureService.cs` + `ChangeSignatureAddRemovePreviewBuilder.cs` (tool: `src/RoslynMcp.Host.Stdio/Tools/ChangeSignatureTools.cs`) to either accept the add (the intent is unambiguous) or emit an actionable error citing caller-supplied fields. |
| `ci-test-parallelization-audit` | P3 | — | Refresh the old test-parallelization audit around the real residual issue: `eng/verify-release.ps1` still has 3 pre-existing cleanup-race flakes even though the `[DoNotParallelize]` sweep is already done. Start by reproducing and naming the failing tests, then localize whether the race sits in `tests/RoslynMcp.Tests/AssemblyCleanup.cs`, `tests/RoslynMcp.Tests/TestBase.cs`, or another teardown path before planning the fix plus a repeated-release stress gate (`eng/verify-release-stress.ps1` / CI). |
| `connection-state-ready-unsatisfiable-preload` | P3 | — | `server_info.connection.state` reports `"initializing"` indefinitely on a fresh host with zero loaded workspaces; only flips to `ready` after the first `workspace_load`. Breaks Phase -1 hard gates in `ai_docs/prompts/deep-review-and-refactor.md` that require `state==ready` before load. Either rename the pre-load state to `idle` / `ready-no-workspace` in `src/RoslynMcp.Host.Stdio/Tools/ServerTools.cs` (where `server_info` is registered and composed), or document the current semantics in the tool description so prompts can target them. |
| `diagnostic-details-param-naming-drift` | P3 | — | `diagnostic_details` uses `line`/`column` where every other positional tool (`get_code_actions`, `find_references`, `goto_definition`, …) uses `startLine`/`startColumn`, so callers composing multi-step flows need per-tool schema checks. Accept both naming conventions on input in `src/RoslynMcp.Host.Stdio/Tools/AnalysisTools.cs:138` (map `startLine`→`line`, `startColumn`→`column` in the handler front matter) and update the tool description to list both. |
| `diagnostic-details-perf-budget-overrun` | P3 | — | `diagnostic_details` took 16045ms on a single-symbol probe (3× the 5s budget) across IT-Chat-Bot + FirewallAnalyzer runs because the handler re-runs project-wide diagnostics even when the caller has pre-resolved the diagnostic locator. In `src/RoslynMcp.Roslyn/Services/DiagnosticService.cs` (tool: `src/RoslynMcp.Host.Stdio/Tools/AnalysisTools.cs:138`), short-circuit the diagnostic scan when `filePath` + position are supplied — fetch the single-document diagnostic set instead of the solution-wide pass. Add a perf assertion (< 2s) as a regression test against the sample solution. |
| `find-base-members-vs-member-hierarchy-metadata-drift` | P3 | — | `find_base_members` / `find_overrides` return `count=0` for `IEquatable<T>.Equals` / metadata-boundary interfaces, but `member_hierarchy` correctly resolves them on the same symbols. Align the metadata-boundary resolver across the shared core (`src/RoslynMcp.Roslyn/Services/ReferenceService.cs`) and the tool-layer registrations in `src/RoslynMcp.Host.Stdio/Tools/SymbolTools.cs` (rows at `find_base_members`, `find_overrides`, `member_hierarchy`); failing that, emit a `warning: "metadata-boundary"` field so callers know to cross-check. |
| `find-dead-fields-compound-assignment-writes` | P3 | — | `find_dead_fields` reports `CompileCheckAccumulator.ErrorCount` / `WarningCount` / `CompletedProjects` as `writeReferenceCount=0` despite `acc.ErrorCount++` at `src/RoslynMcp.Roslyn/Services/CompileCheckService.cs:155`. Extend the write-reference sweep in `src/RoslynMcp.Roslyn/Services/DeadCodeService.cs` (tool: `src/RoslynMcp.Host.Stdio/Tools/AdvancedAnalysisTools.cs` `find_dead_fields`) to count `++` / `--` / `+=` / `-=` / `*=` / `/=` / `<<=` / `>>=` / `\|=` / `&=` / `^=` target-of-assignment semantics plus `Interlocked.*` calls; unit-test one operator per case. |
| `find-implementations-source-gen-partial-dedup` | P3 | — | `find_implementations(ISnapshotStore)` returned `FileSnapshotStore` plus two additional partial-class decls from `Logging.g.cs` and `RegexGenerator.g.cs` — same type, not distinct implementations. Deduplicate by containing `ISymbol` identity in `src/RoslynMcp.Roslyn/Services/ReferenceService.cs` (tool: `src/RoslynMcp.Host.Stdio/Tools/SymbolTools.cs` `find_implementations`); expose opt-in `includeGeneratedPartials=false` for callers that need the raw list. |
| `find-property-writes-positional-record-silent-zero` | P3 | — | `find_property_writes` returns `count=0, writes=[]` for positional-record primary-constructor bindings (repro: `WebSnapshotStoreContext.Store`) despite every consumer supplying the property at construction. Add a `PrimaryConstructorBind` bucket (or warning) in `src/RoslynMcp.Roslyn/Services/ReferenceService.cs` (tool: `src/RoslynMcp.Host.Stdio/Tools/SymbolTools.cs:323` `find_property_writes`) so callers know the property IS written, just not via traditional setter syntax. |
| `find-references-bulk-summary-mode` | P3 | — | `find_references_bulk` (2-symbol batch on `IWorkspaceManager`+`RefactoringService`) overflowed at 120KB because the paging cap applies to each symbol's inner list but not to the outer batch. Add `summary: bool` + `maxItemsPerSymbol` to `src/RoslynMcp.Host.Stdio/Tools/SymbolTools.cs:302` (delegating into `src/RoslynMcp.Roslyn/Services/ReferenceService.cs`) to mirror the existing `find_references(summary=true)` contract. |
| `find-type-mutations-undercounts-lifecycle-writes` | P3 | — | `find_type_mutations(WorkspaceManager, limit=10)` surfaces only 2 mutators (`GetSourceGeneratedDocumentsAsync`, `Dispose`) despite `LoadAsync` / `ReloadAsync` / `CloseAsync` mutating `_workspaces`. Extend the mutation-scope classifier in `src/RoslynMcp.Roslyn/Services/MutationAnalysisService.cs` (tool: `src/RoslynMcp.Host.Stdio/Tools/AnalysisTools.cs:274` `find_type_mutations`) to cover collection-field writes inside async lifecycle methods; cross-verify against `WorkspaceManager` and `SnapshotStore` before closing. |
| `find-unused-symbols-convention-filter-gaps` | P3 | — | `excludeConventionInvoked=true` still false-positives on: `[McpServerToolType]` / `[McpServerPromptType]` / `[McpServerResourceType]` (every row in self-audit), xUnit `[CollectionDefinition]` holders (`WebHostSerialCollection`), and ASP.NET health-response writers (`HealthResponseWriter.WriteAsync(HttpContext, HealthReport)`). Extend the default convention list in `src/RoslynMcp.Roslyn/Services/UnusedCodeAnalyzer.cs` (tool: `src/RoslynMcp.Host.Stdio/Tools/AdvancedAnalysisTools.cs:13` `find_unused_symbols`) and add fixture-based tests for each pattern. Single code path (convention list), trivially-additive tests — Rule 1 bundle. |
| `format-range-apply-preview-token-lifetime` | P3 | — | `format_range_apply(previewToken)` fails with `"Preview token not found or expired"` within 5s of the preview when another writer triggers auto-reload between preview and apply. Either pin preview tokens to a workspace-version range (instead of a single version) in `src/RoslynMcp.Roslyn/Services/PreviewStore.cs` (tool: `src/RoslynMcp.Host.Stdio/Tools/RefactoringTools.cs:174` `format_range_apply`), or expose a composite `preview_and_apply_if_clean` that holds the gate across both operations. |
| `get-code-actions-caret-only-inverted-range` | P3 | — | `get_code_actions(filePath, startLine=98, startColumn=13)` with no `endLine`/`endColumn` fails with `"'end' must not be less than 'start'. start='3930' end='3927'"` — default-end computation produces a position 3 chars before start. Fix the default-range selector in `src/RoslynMcp.Roslyn/Services/CodeActionService.cs` (tool: `src/RoslynMcp.Host.Stdio/Tools/CodeActionTools.cs`) so caret-only calls resolve to a non-empty or truly zero-width selection. |
| `get-prompt-text-publish-parameter-schema` | P3 | — | `roslyn://server/catalog/prompts/*` lists `name`/`summary`/`supportTier` only; callers must invoke `get_prompt_text` with empty `parametersJson` and parse error text to discover required params (`workspaceId`, `intent`, `diagnosticId`, `filePath`, `line`, `column`, …). For a 3-required-param prompt that's 3 failing round-trips. Extend the catalog resource DTO in `src/RoslynMcp.Host.Stdio/Catalog/ServerSurfaceCatalog.Resources.cs` (schema also touches `ServerSurfaceCatalog.cs` + `src/RoslynMcp.Host.Stdio/Tools/PromptShimTools.cs`) to include `parameters: [{name, type, required, defaultValue, description}]` per prompt (already captured by `[McpServerPrompt]` attributes). |
| `host-recycle-opacity` | P3 | — | `server_info.connection` carries no history of host-process restarts; mid-session the `stdioPid` changes and every workspace_id returns NotFound with no recycle reason. On the first `server_info`/`server_heartbeat` probe after a restart, populate `connection.previousStdioPid`, `connection.previousExitedAt`, and `connection.previousRecycleReason` (enum: `idle-eviction`/`watchdog`/`client-disconnect`/`unknown`) from process metadata. Anchors: `src/RoslynMcp.Host.Stdio/Tools/ServerTools.cs:64` (`server_info` registration) + the connection-state DTO under `src/RoslynMcp.Core/Models/`. |
| `impact-analysis-summary-mode` | P3 | — | `impact_analysis(referencesLimit=5)` overflowed at 86KB because the `declarations` list has no paging cap. Add `summary: bool` + `declarationsLimit` to `src/RoslynMcp.Host.Stdio/Tools/AnalysisTools.cs:242` (delegating into the shared impact helper also used by `symbol_impact_sweep`) so both summary-mode and paged declarations work. |
| `move-type-to-file-preview-enum-support` | P3 | — | `move_type_to_file_preview` rejects `IngestionTerminalStatus` (an enum in a multi-type file) with vague `"Type 'X' not found"` even though `symbol_search` + `find_type_mutations` resolve it. In `src/RoslynMcp.Roslyn/Services/TypeMoveService.cs` (tool: `src/RoslynMcp.Host.Stdio/Tools/TypeMoveTools.cs`), either support `TypeKind=Enum|Delegate` moves or emit an explicit `"type-kind Enum not supported for this refactor"` error instead of the misleading not-found text. |
| `revert-apply-by-sequence-number` | P3 | — | `revert_last_apply` is LIFO only and gets cleared by auto-reload, so a late-session revert of an earlier `set_editorconfig_option` audit-only apply requires hand-edits. `workspace_changes` already tracks ordered applies with sequence numbers — add `revert_apply_by_sequence(workspaceId, sequenceNumber)` in `src/RoslynMcp.Roslyn/Services/UndoService.cs` (tool: `src/RoslynMcp.Host.Stdio/Tools/UndoTools.cs`) returning `{reverted, revertedOperation, affectedFiles}` with a clean failure when later applies depend on the target. |
| `scaffold-test-preview-ctor-arg-stubs` | P3 | — | `scaffold_test_preview` emits `new T()` even when the target's ctor requires arguments (DI-registered services — repro: `NamespaceRelocationService`), producing non-compiling scaffolds. In `src/RoslynMcp.Roslyn/Services/ScaffoldingService.cs` (tool: `src/RoslynMcp.Host.Stdio/Tools/ScaffoldingTools.cs:87`), probe the target's primary ctor and emit TODO placeholders (or `NSubstitute.Substitute.For<T>()` stubs when the package is already referenced) for unresolvable args. Regression: scaffold for a DI-registered service; output must reference each ctor parameter with a synthesized placeholder. |
| `scaffold-test-preview-static-target-body` | P3 | — | `scaffold_test_preview` emits `new T()` in the Arrange section even when the target type is static (repro: `TenantConstants`, `SnapshotContentHasher`), producing tests that won't compile. In `src/RoslynMcp.Roslyn/Services/ScaffoldingService.cs` (tool: `src/RoslynMcp.Host.Stdio/Tools/ScaffoldingTools.cs:87`), detect `INamedTypeSymbol.IsStatic` and switch the body template to call static members directly (no `new`). Regression: scaffold for a static utility class and confirm output contains no `new <TargetType>` expression. |
| `set-editorconfig-option-round-trip-asymmetry` | P3 | — | `set_editorconfig_option(dotnet_diagnostic.CA9999.severity=none)` succeeds and writes to `.editorconfig`, but `get_editorconfig_options` immediately after returns the other 27 keys without the newly-set one (reproduces when the analyzer id isn't reported by any loaded analyzer). Align the get-side cache refresh with the set-side write in `src/RoslynMcp.Roslyn/Services/EditorConfigService.cs` (tool: `src/RoslynMcp.Host.Stdio/Tools/EditorConfigTools.cs`). |
| `test-related-response-envelope-parity` | P3 | — | `test_related` returns a bare `[{displayName, fullyQualifiedName, filePath, line}, …]` array (sometimes 100+ entries) while `test_related_files` returns `{tests:[…,projectName,triggeredByFiles], dotnetTestFilter}`. Wrap `test_related` in the same envelope with pagination metadata + synthesized `dotnetTestFilter` in `src/RoslynMcp.Roslyn/Services/TestDiscoveryService.cs` (tool-layer wrappers under `src/RoslynMcp.Host.Stdio/Tools/`). Cross-ref the existing P4 rows `test-related-files-empty-result-explainability` and `test-related-files-service-refactor-underreporting` — bundle them into the same service touch only if Rule 1's four conditions hold at plan time. |
| `workspace-changes-log-missing-editorconfig-writers` | P3 | — | `workspace_changes` omits `set_diagnostic_severity` / `set_editorconfig_option` applies (verified on disk) and buckets distinct writers (`rename_apply`, `code_fix_apply`, `format_document_apply`, `remove_dead_code_apply`) under a generic `toolName: "refactoring_apply"` / `"apply_text_edit"`. Wire editorconfig-level applies through the same log sink and record the originating tool name in `src/RoslynMcp.Roslyn/Services/ChangeTracker.cs` (tool: `src/RoslynMcp.Host.Stdio/Tools/WorkspaceTools.cs:312` `workspace_changes`). |
| `pretooluse-apply-preview-token-invariant` | P3 | — | The `PreToolUse:mcp__roslyn__*_apply` hook at `hooks/hooks.json` still scans the session transcript for a matching `*_preview` and false-positive-blocks `*_apply` 8 times across 4 refactor sessions in the 2026-04-10→04-24 retro (`format_range_apply`, `extract_interface_apply`, `create_file_apply`, `fix_all_apply`, `move_type_to_file_apply`) — prior work (PR #234, PR #230) softened cold-subagent and composite-preview cases but the structural scan remains. Return a short-lived `previewToken` on every `*_preview` response and require it on every `*_apply`; rewrite the hook to validate by token presence instead of transcript grep. Anchors: `src/RoslynMcp.Roslyn/Services/PreviewStore.cs` (already issues tokens for most previews — extend to the remaining preview tools), `src/RoslynMcp.Host.Stdio/Tools/*Apply*.cs` (add required `previewToken` parameter on every `*_apply`), `hooks/hooks.json` (flip scan → token compare). Distinct from `format-range-apply-preview-token-lifetime` (that row tunes token lifetime; this row tightens the apply-gate invariant). |
| `roslyn-mcp-sister-tool-name-aliases` | P3 | — | Agents repeatedly call Roslyn tools with python-refactor names and hit `<tool_use_error>Error: No such tool available: mcp__roslyn__<name></tool_use_error>` — 6 occurrences in the 2026-04-10→04-24 retro across `get_symbol_outline` (→ `document_symbols`), `find_duplicated_code` (→ `find_duplicated_methods` / `find_duplicate_helpers`), `get_test_coverage_map` (→ `test_coverage`). Register thin aliases in `src/RoslynMcp.Host.Stdio/Catalog/ServerSurfaceCatalog.cs` (and the relevant `ServerSurfaceCatalog.*.cs` partials) so agents land on the canonical tool with a deprecation-hint field in the response pointing at the new name. Low LOC; high DX payoff. |
| `find-type-consumers-file-granularity-rollup` | P3 | — | Agents looking for "which files touch this type" fall back to Grep (≥5 refactor sessions in the 2026-04-10→04-24 retro: 57a0d696, bfe7443f, ac8b4d93, 08adc1f1, b3e4cb62) because `find_references` returns per-site hits with no per-file rollup. Add `find_type_consumers(typeName, limit) → [{filePath, kinds: [using|ctor|inherit|field|local], count}]` on top of the existing reference index. Anchors: `src/RoslynMcp.Roslyn/Services/ReferenceService.cs` (reuse reference index), new tool in `src/RoslynMcp.Host.Stdio/Tools/SymbolTools.cs`. Shape mirrors what downstream rename/move/delete planning flows already want. |
| `skills-remove-server-heartbeat-polling` | P4 | — | The `/roslyn-mcp:complexity` skill (and any wait-loop in sibling skills under `.claude/plugins/roslyn-mcp/skills/`) polls `mcp__roslyn__server_heartbeat` in a loop — one session in the 2026-04-10→04-24 retro (b5464409) logged 78 heartbeat calls inside a single `/complexity` invocation; two others logged 8 and 6. Audit the shipped skill prompts and replace poll-until-ready with a single `server_info` check (or `workspace_status` + bounded backoff) so transcripts aren't inflated by heartbeat noise. Anchors: `src/RoslynMcp.Plugin/.claude-plugin/plugin.json` skill dir + `skills/complexity/SKILL.md`, cross-check with `skills/*/SKILL.md` for the same pattern. |
| `stdio-host-stdout-audit` | P3 | — | MCP best-practices (2026-04 ref): stdio servers must NOT write to stdout — the framing channel shares it. A stray `Console.WriteLine` / `Console.Out.Write` in `src/RoslynMcp.Host.Stdio/` would surface as transport closure (matches some of the `MCP error -32000: Connection closed` errors seen in the 2026-04-10→04-24 retro). Grep the host tree for `Console.Write`, `Console.Out.`, `stdout.Write`, `Trace.WriteLine` (default TraceListener writes stdout); route any hits through the existing `ILogger` (stderr). Add an analyzer or test that asserts no direct stdout writes in `src/RoslynMcp.Host.Stdio/**/*.cs`. |
| `tool-annotation-population-audit` | P4 | — | MCP best-practices: tools should declare `readOnlyHint` / `destructiveHint` / `idempotentHint` / `openWorldHint` annotations so clients (including the PreToolUse apply-gate hook) can route decisions off structured metadata instead of tool-name patterns. Verify via `server_info.surface` + a spot-check on `src/RoslynMcp.Host.Stdio/Tools/*Apply*.cs` and `*Preview*.cs` whether the `[McpServerTool]` attributes already carry these. If missing, populate: every `*_apply` → `destructiveHint: true`; every `*_preview` / read-shape tool → `readOnlyHint: true`; workspace-scoped tools → `openWorldHint: false`. Close-immediately if already populated. |
| `response-format-json-markdown-parameter` | P4 | — | MCP best-practices: summary-shaped tools should support `response_format: "json" \| "markdown"` (default `json` preserves wire compatibility). Current state: all 161 tools return JSON only. Highest-value candidates (heavy context cost, human-readable reframing): `server_info`, `workspace_list`, `workspace_status`, `validate_workspace`, `symbol_impact_sweep`, `project_diagnostics(summary)`. Add the parameter + markdown formatter per tool; start with `validate_workspace` (typically 120-line JSON → ~30-line table). Anchors: `src/RoslynMcp.Host.Stdio/Tools/ServerTools.cs`, `WorkspaceTools.cs`, `ValidationBundleTools.cs`. |
| `pretooluse-block-release-critical-file-edits` | P3 | — | Accidental agent edits to `Directory.Build.props`, `BannedSymbols.txt`, the 5 version files (`src/*/**.csproj` `<Version>` properties + `.claude-plugin/plugin.json` + `.claude-plugin/marketplace.json` + `eng/verify-version-drift.ps1` baseline), `hooks/hooks.json` itself, and `eng/verify-skills-are-generic.ps1` have shipped drift historically (cross-ref: 1.28.0 parallel-PR changelog-append friction). Add a PreToolUse hook in `hooks/hooks.json` matching `Edit\|Write\|MultiEdit` on those specific paths that blocks with an actionable override phrase ("release-managed file; run `/bump` or `/release-cut` instead, or include 'ack: release-managed' in the edit rationale to proceed"). Keep the list in the hook config; document in `ai_docs/workflow.md`. |
| `posttool-verify-skills-on-shipped-skill-edits` | P4 | — | `eng/verify-skills-are-generic.ps1` is gated by `just ci` + `verify-release.ps1`, so an `ai_docs/` / `eng/` / `backlog.md` leak inside a `./skills/**/SKILL.md` is caught at CI, not at edit-time — round-trip waste per retro observation. Add a PostToolUse hook on `Edit\|Write\|MultiEdit` matching `./skills/**` that runs `pwsh eng/verify-skills-are-generic.ps1` and surfaces violations the same turn. Timeout bounded (the script is <1s). |

## P4 — open work

| id | pri | deps | do |
|----|-----|------|-----|
| `diagnostic-details-supported-fixes-empty` | P4 | — | `diagnostic_details` returns `supportedFixes: []` for CA1826 / ASP0015 / MA0001 / SYSLIB1045 despite the tool description advertising "curated fix options." Either populate `supportedFixes` for those ids in `src/RoslynMcp.Host.Stdio/Tools/AnalysisTools.cs:138` (sourced from the loaded `CodeFixProviderRegistry`) or soften the tool description to "fix options when available" so the contract matches reality. |
| `fix-all-preview-silent-on-missing-provider-info-severity` | P4 | — | On Info-severity diagnostics (`IDE0130`/`IDE0290`/`IDE0008`) `fix_all_preview` returns `previewToken:"", fixedCount:0, guidanceMessage:null` — indistinguishable from "no occurrences"; on Warning-severity ids (`CA1826`/`xUnit1051`) it returns a helpful message pointing at `list_analyzers`. Emit the same "no fix provider loaded" guidance regardless of severity in `src/RoslynMcp.Roslyn/Services/FixAllService.cs` (tool: `src/RoslynMcp.Host.Stdio/Tools/FixAllTools.cs:18`). |
| `project-mutation-preview-xml-formatting` | P4 | — | `add_package_reference_preview`, `set_project_property_preview`, `add_central_package_version_preview` emit collapsed single-line XML (`</PropertyGroup><ItemGroup>`, trailing whitespace) in preview diffs. Cosmetic but hurts review friction. Post-process the generated diff through a format pass in `src/RoslynMcp.Roslyn/Services/ProjectMutationService.cs` + `OrchestrationMsBuildXml.cs` (tool: `src/RoslynMcp.Host.Stdio/Tools/ProjectMutationTools.cs`). |
| `scaffold-test-preview-sibling-inference-overbroad` | P4 | — | `scaffold_test_preview` copies `[Trait("Category","Playwright")]`, `using Microsoft.Playwright;`, and 10+ unused imports from the MRU sibling fixture when the MRU happens to be a Playwright file. Narrow sibling-inference in `src/RoslynMcp.Roslyn/Services/ScaffoldingService.cs` (tool: `src/RoslynMcp.Host.Stdio/Tools/ScaffoldingTools.cs:87`) to file-scoped namespace + base-class name; exclude `[Trait]`/`[Category]`/`[Collection]` attributes and trim usings to those actually used in the generated body. |
| `semantic-search-duplicate-results-and-fallback-signal` | P4 | — | `semantic_search("classes implementing IDisposable", limit=10)` returns `JobProcessor` twice (identical `fullyQualifiedName` + `symbolHandle`), and the description advertises a Debug fallback payload (`fallbackStrategy`, `matchKind` per result) that isn't actually returned. Deduplicate by `symbolHandle` and surface `Debug.fallbackStrategy` / `matchKind` fields in `src/RoslynMcp.Roslyn/Services/CodePatternAnalyzer.cs` (tool: `src/RoslynMcp.Host.Stdio/Tools/AdvancedAnalysisTools.cs:293` `semantic_search`; response DTO: `src/RoslynMcp.Core/Models/SemanticSearchResponseDto.cs`). |
| `split-class-preview-orphan-indent` | P4 | — | `split_class_preview` leaves orphan-indentation blank lines (`+    ` on otherwise-empty lines) where removed members lived. Trim trailing whitespace on post-split gaps in `src/RoslynMcp.Roslyn/Services/ClassSplitOrchestrator.cs` (tool: `src/RoslynMcp.Host.Stdio/Tools/OrchestrationTools.cs:43` `split_class_preview`). |
| `symbol-impact-sweep-suggested-tasks-count-drift` | P4 | — | `symbol_impact_sweep(summary=true, maxItemsPerCategory=10)` on `IWorkspaceManager` reports `suggestedTasks: ["Review 10 reference(s)…"]` despite `totalCount=193`. Use the pre-cap total in the task-string builder in `src/RoslynMcp.Roslyn/Services/ImpactSweepService.cs` (tool: `src/RoslynMcp.Host.Stdio/Tools/ImpactSweepTools.cs:17`) so agents see the true scope, not the truncated number. |
| `test-related-files-empty-result-explainability` | P4 | — | `test_related_files` currently returns only `tests` + `dotnetTestFilter` (`src/RoslynMcp.Core/Models/RelatedTestsForFilesDto.cs`), so an empty result cannot distinguish "no tests exist" from "heuristic miss." Extend the response, or add a companion diagnostic mode, to report scanned test projects, heuristics attempted, and miss reasons for each changed file. |
| `test-related-files-service-refactor-underreporting` | P4 | — | `src/RoslynMcp.Roslyn/Services/TestDiscoveryService.cs` still underreports service-layer refactors because its fallback path leans too heavily on type-name / filename affinity. Broaden the heuristic for multi-file service changes so it can surface likely integration or neighboring service tests before returning empty, and validate against the `MutationAnalysisService` + `ScaffoldingService` and `CodePatternAnalyzer` + `TestDiscoveryService` misses seen in the 2026-04-23 sweep. |
| `workspace-process-pool-or-daemon` | P4 | `large-solution profile` | Do not start a daemon/process-pool implementation blindly. First capture representative 50+ project timings per `docs/large-solution-profiling-baseline.md`; only if `workspace_load` / reload P95 still blocks daily use after `workspace_warm` should the next plan produce a bounded design note comparing daemon, process-pool, and shared-workspace approaches, including lifecycle and failure-isolation hooks. |
| `semantic-grep-identifier-scoped-search` | P4 | — | Agents want to grep inside C# code but exclude string/comment matches — current Grep returns hits inside CHANGELOG, markdown, and string literals (3 Roslyn-self-audit sessions in the 2026-04-10→04-24 retro: 57a0d696, 08adc1f1, cf2d0b85). Add `semantic_grep(pattern, scope=identifiers|strings|comments|all, projectFilter?) → [{filePath, line, column, tokenKind, snippet}]` as a thin wrapper on `SyntaxTree.DescendantTokens` + `SyntaxToken.Kind()` + regex match. Anchors: new service under `src/RoslynMcp.Roslyn/Services/`, new tool in `src/RoslynMcp.Host.Stdio/Tools/AnalysisTools.cs`. Weaker evidence (3 sessions, all self-audit) — P4 until an external-repo session reproduces the pattern. |

## Refs

| Path | Role |
|------|------|
| `ai_docs/planning_index.md` | Planning router and scope boundary |
| `ai_docs/workflow.md` | Branch/PR workflow and backlog-closure rule |
| `ai_docs/prompts/backlog-sweep-plan.md` | Planner prompt; enforces per-initiative Rule 1 (bundle only on shared code path) / Rule 3 (≤4 prod files) / Rule 3b (toolPolicy) / Rule 4 (≤3 test files) / Rule 5 (≤80K context). Each row here is sized for one initiative. |
| `ai_docs/prompts/backlog-sweep-execute.md` | Executor companion; consumes the `state.json` the planner emits and vets each initiative against Rules 3/4/5 before starting work. |
| `ai_docs/bootstrap-read-tool-primer.md` | Self-edit session read-only tool primer (Roslyn-MCP read-side tools to prefer over Bash/Grep). |
| `ai_docs/runtime.md` | Bootstrap scope policy — distinguishes main-checkout self-edit (no `*_apply`) from worktree/parallel-subagent sessions. |
| `ai_docs/plans/20260422T170500Z_test-parallelization-audit/plan.md` | Dedicated phased plan for `ci-test-parallelization-audit`. |
| `docs/large-solution-profiling-baseline.md` | Evidence gate for daemon/process-pool performance work. |
| `ai_docs/procedures/deep-review-backlog-intake.md` | Intake procedure for future audit batches. |
| `ai_docs/plans/20260422T223000Z_backlog-sweep/plan.md` | Prior sweep (20260422T223000Z). Shipped 14 initiatives across 14 PRs; closed 9 backlog rows (P2: 0, P3: 4, P4: 5). |
| `review-inbox/` | Staging folder for the NEXT audit batch (flat directory; `/backlog-intake` reads here). |
| `review-inbox/archive/<batch-ts>/` | Processed audit/retro/promotion batches — one subdirectory per successful intake, named by the intake commit's UTC timestamp. Backlog rows citing specific files are anchored here. Batch `20260424T031936Z` holds the 14-file cross-repo audit + retro batch that produced the current rows. Keep until every row sourced from a batch is closed or superseded, then delete that subdirectory. |
