# Next work and backlog

<!-- purpose: Open work only; contract for agents syncing backlog on ship. -->
<!-- scope: in-repo -->

**updated_at: 2026-05-20T20:39:48Z**

## Agent contract

| | |
|---|---|
| **Scope** | This file lists unfinished work only. It is not a changelog. |
| **MUST** | Remove or update backlog rows when work ships; do it in the same PR or an immediate follow-up. |
| **MUST** | End implementation plans with a final todo: `backlog: sync ai_docs/backlog.md`. |
| **MUST** | Use stable, kebab-case `id` values per open row. |
| **MUST** | Every row's `do` cell summarizes the current need + the concrete next deliverable. Include `Anchors:` (specific source file paths) when the row references code, and evidence (audit/retro/CI signal) when one exists. |
| **MUST** | Size every row to a single bounded initiative — ≤4 production files, ≤3 test files, one regression-test shape. Split heroic multi-bug rows into per-bug children before planning against them. |
| **MUST NOT** | Add `Completed`, `Shipped`, `Done`, `History`, or `Changelog` sections. Git is the archive. |
| **MUST NOT** | Leave done items in the open table. |
| **MUST NOT** | Use `### <id>` body sections per item. The table row IS the canonical form. Items needing long-form depth (more than ~10 lines) link to `ai_docs/items/<id>.md` from the `do` cell. |

## Standing rules

- **Reprioritize on each audit pass.** Stale priority order is a finding.
- **Keep rows planner-ready.** A row is ready when an agent can read it cold and start a plan: name the live anchors and the next concrete deliverable or investigation output.
- **Replace stale umbrella rows with concrete follow-ons** before planning against them.
- **Long-form audit evidence belongs in referenced reports**, not in this file. The `do` cell carries a one-line evidence summary plus the report path.
- **Weak-evidence flag.** When a row's signal is thin (single retro session, self-audit only, etc.) say so explicitly in the `do` cell ("Weaker evidence — N until external session reproduces").
- **GitHub-issue cross-link.** When a row has a corresponding open GitHub Issue, surface the link at the start of the `do` cell so the backlog and issue stay paired. Two flavors:
  - **Reserved for contributor pickup** (`good first issue` / `help wanted` labels): prefix the `do` cell with `**Reserved — [gh #NNN](https://github.com/darylmcd/Roslyn-Backed-MCP/issues/NNN) (good first issue|help wanted); skip in sweeps until contributor pickup.**` — `/backlog-sweep:plan` skips these per its Step 1 hard-skip rule. Remove the marker (or close the row) when a contributor PR lands or when the maintainer reclaims the work.
  - **Tracked-only** (auto-filed audit issues that aren't promoted to a contributor label): prefix the `do` cell with `[gh #NNN](https://github.com/darylmcd/Roslyn-Backed-MCP/issues/NNN) — `. Sweep treats these as normal claimable rows; the implementing PR closes both the issue (via `Fixes #NNN`) and the backlog row.
- **Priority tiers:** Critical > High > Medium > Low > Defer.
- See `workflow.md` → **Backlog closure** for close-in-PR expectations.

---

## Critical

<!-- Production-breaking or blocking work. Empty section is fine; keep the header. -->

| id | pri | deps | do |
|----|-----|------|-----|

## High

| id | pri | deps | do |
|----|-----|------|-----|


## Medium

| id | pri | deps | do |
|----|-----|------|-----|
| `agent-workflow-router-first-hop` | Medium | none | Add a compact first-hop routing surface so agents can ask "what should I use for this task?" without reading the full 169-tool catalog. Proposed shape: `recommend_workflow(task, filePath?, symbol?, intent?) -> { primaryTools, followUpTools, avoid, why, requiredWorkspaceState }`, backed by the same canonical pattern map as the bootstrap primer. Keep output to a small number of tools and explicitly steer away from generic fallbacks (`rg`, `dotnet build`, full `dotnet test`) when Roslyn primitives fit. Anchors: `src/RoslynMcp.Host.Stdio/Catalog/ServerSurfaceCatalog.cs`, `src/RoslynMcp.Host.Stdio/Catalog/ServerSurfaceCatalog.*.cs`, new `src/RoslynMcp.Host.Stdio/Tools/WorkflowRecommendationTools.cs` (or equivalent prompt/resource implementation), `tests/RoslynMcp.Tests/SurfaceCatalogTests.cs`. Regression test shape: 5 representative task strings (find callers, file outline, compile sanity, related tests, rename preview) return the intended first-hop tools and avoid-list without dumping a catalog. Evidence: 2026-05-20 no-context subagent probe showed agents choose the right Roslyn tools when primed, but the scalable answer is intent routing rather than exposing/summarizing the whole tool index. |
| `discover-capabilities-quick-path-steering` | Medium | none | Update the live `discover_capabilities` prompt and workflow hints so the default guidance matches the repo's fast Roslyn-first policy: prefer `compile_check` over `build_workspace` for routine compile sanity, `validate_recent_git_changes` / `validate_workspace` for post-edit bundles, `test_related_files` -> filtered `test_run` for tests, and `document_symbols` / `find_references` over file scanning. Today some generated prompt text still says "build_workspace or build_project" after any code change, which can steer agents toward slower paths despite `runtime.md` and the bootstrap primer saying otherwise. Anchors: `src/RoslynMcp.Host.Stdio/Prompts/RoslynPrompts.AnalysisWorkflows.cs`, `src/RoslynMcp.Host.Stdio/Prompts/PromptMessageBuilder.cs`, `src/RoslynMcp.Host.Stdio/Catalog/ServerSurfaceCatalog.cs`, `tests/RoslynMcp.Tests/PromptSmokeTests.cs`. Regression test shape: render `get_prompt_text(discover_capabilities)` for `navigation`, `refactoring`, `testing`, and `all`; assert the text names the fast first-hop tools and no longer presents `build_workspace`/`build_project` as the routine default for compile sanity. Evidence: 2026-05-20 agent-view review found the static docs steer correctly, but live prompt/workflow surfaces still contain broader/slower validation wording. |
| `test-related-files-direct-reference-ranking` | Medium | none | Improve `test_related_files` ranking so direct semantic test references to symbols in the changed file outrank broad namespace/name-neighbor fallback. In the 2026-05-20 no-context probe for `src/RoslynMcp.Host.Stdio/Tools/PromptShimTools.cs`, `test_related_files` broadened to 136 candidates and the agent had to switch to `symbol_search`/`find_references`/`test_related` to find the useful `PromptSmokeTests.GetPromptText_*` slice. Fix: derive top-level symbols from changed files, run/reference-rank direct test consumers first, then include heuristic fallback in a lower-confidence bucket with counts and a suggested filtered `test_run`. Anchors: `src/RoslynMcp.Host.Stdio/Tools/ValidationTools.cs`, `src/RoslynMcp.Roslyn/Services/TestDiscoveryService.cs`, `src/RoslynMcp.Core/Models/RelatedTestsForFilesDto.cs`, `tests/RoslynMcp.Tests/ValidationToolsIntegrationTests.cs`. Regression test shape: fixture where a tool file has direct tests plus many namespace-neighbor tests; assert direct references appear first and the recommended filter favors the smaller direct-reference set. Evidence: 2026-05-20 subagent probe reported `test_related_files` was too noisy for `PromptShimTools.cs`; symbol-level `test_related` was precise. |

## Low

| id | pri | deps | do |
|----|-----|------|-----|
| `find-duplicate-helpers-framework-wrapper-filter-leak` | Low | none | Split from gh #769 §13.19 (parent tracker — do not auto-close). `find_duplicate_helpers` flags `ApiHostBuilder.ConfigureSerilog/Cors/PanosHttpClient` at high/medium confidence despite documented exclusion of `Microsoft.AspNetCore.*` wrappers. Fix: extend the framework-wrapper exclusion logic in the live duplicate-helper implementation to cover `Serilog.Extensions.Hosting`, `Microsoft.Extensions.DependencyInjection.CorsServiceCollectionExtensions`, `Microsoft.Extensions.Http.Resilience`, and matching namespace-prefix assemblies. Anchors: `src/RoslynMcp.Roslyn/Services/UnusedCodeAnalyzer.cs`, `src/RoslynMcp.Core/Services/IUnusedCodeAnalyzer.cs`, `src/RoslynMcp.Host.Stdio/Tools/AdvancedAnalysisTools.cs`, `tests/RoslynMcp.Tests/DuplicateHelperDetectionTests.cs`. Regression test shape: 1 fixture using `ConfigureSerilog`/`UseSerilog` wrappers; assert no duplicate-helper cluster is reported while a true local duplicate still is. Evidence: audit §13.19; gh #769 P3 audit batch; 2026-05-20 backlog audit corrected stale anchors. |
| `compile-check-file-filter-scope-narrowing-inconsistency` | Low | none | Split from gh #769 §13.21 (parent tracker — do not auto-close). `compile_check(filePath=<path>)` narrows returned diagnostics but the call still scans all projects in the solution (full compilation). Fix: narrow the compilation scope to the project owning the supplied `filePath`/`filePaths` when all requested files resolve to a single project; fall back to the existing full-solution path only when ownership is ambiguous and make that fallback explicit in the result or tool description. Anchors: `src/RoslynMcp.Host.Stdio/Tools/CompileCheckTools.cs`, `src/RoslynMcp.Roslyn/Services/CompileCheckService.cs`. Regression test shape: 1 two-project fixture with a broken unrelated project; `compile_check(filePaths=[cleanProjectFile])` should compile/report only the owning project, while an ambiguous multi-project request exercises the documented fallback. Evidence: audit §13.21; gh #769 P3 audit batch. |
| `get-nuget-dependencies-summary-cpm-literal` | Low | none | Split from gh #769 §13.22 (parent tracker — do not auto-close). `get_nuget_dependencies(summary=true)` on a CPM-enabled repo returns packages with `version="centrally-managed"` instead of resolving from `Directory.Packages.props`. Fix: resolve the effective central package version into the existing summary `version` field wherever a matching `PackageVersion` entry is available; keep a clear fallback only for unresolved central references. Anchors: `src/RoslynMcp.Roslyn/Services/NuGetDependencyService.cs`, `src/RoslynMcp.Core/Models/NuGetDependencyDto.cs`, `src/RoslynMcp.Core/Services/INuGetDependencyService.cs`. Regression test shape: 1 CPM fixture with `summary=true`; assert resolved versions replace the literal string and non-CPM packages still report their direct versions. Evidence: audit §13.22; gh #769 P3 audit batch; 2026-05-20 live spot check reproduced the literal on this repo. |
| `get-cohesion-metrics-recommendation-null-for-unclassified-types` | Low | none | Split from gh #769 §13.23 (parent tracker — do not auto-close). Current output can return `lifecyclePattern=null` and `recommendation=null` for ordinary unclassified types; existing tests indicate lifecycle classification is wired for some known patterns, so the planner-ready gap is the dead/null recommendation path rather than claiming lifecycle classification is always absent. Fix: keep `lifecyclePattern` nullable for unclassified types, but populate `recommendation` deterministically for every result with either pattern-specific guidance or a concise generic recommendation. Anchors: `src/RoslynMcp.Roslyn/Services/CohesionAnalysisService.cs`, `src/RoslynMcp.Core/Models/CohesionMetricsDto.cs`, `src/RoslynMcp.Core/Services/ICohesionAnalysisService.cs`, `tests/RoslynMcp.Tests/CohesionAnalysisTests.cs`. Regression test shape: 1 fixture with a recognized lifecycle pattern and 1 unclassified type; assert the recognized type has a pattern-specific recommendation and the unclassified type has a non-null generic recommendation. Evidence: audit §13.23; gh #769 P3 audit batch; 2026-05-20 live spot check saw null values while code/tests showed classification is partially implemented. |
| `add-pragma-suppression-crlf-in-lf-file` | Low | none | Split from gh #769 §13.24 (parent tracker — do not auto-close). Inserted `#pragma warning disable CA1305` line uses CRLF while surrounding file uses LF (e.g. `DriftDetector.cs`). Fix: detect the file's existing line-ending style and match it on the inserted pragma line. Anchors: `src/RoslynMcp.Roslyn/Services/SuppressionService.cs`, `src/RoslynMcp.Core/Services/ISuppressionService.cs`, `src/RoslynMcp.Host.Stdio/Tools/SuppressionTools.cs`, `tests/RoslynMcp.Tests/SuppressionServiceTests.cs`, `tests/RoslynMcp.Tests/PragmaScopeManipulationTests.cs`. Regression test shape: 1 fixture on an LF file and 1 on a CRLF file; assert the inserted line uses the existing style. Evidence: audit §13.24; gh #769 P3 audit batch; 2026-05-20 backlog audit corrected stale anchors. |
| `find-type-mutations-error-template-diverges-from-siblings` | Low | none | Split from gh #769 §13.27 (parent tracker — do not auto-close). `find_type_mutations` returns "No named type found at the specified location" while other symbolHandle-fed tools use "No symbol could be resolved for the supplied symbol handle...". Same category (NotFound), same actionability, cosmetic inconsistency. Fix: first reproduce the current unresolvable-symbol path after the `MutationScope` -> `MutationScopes` BREAKING rename; if it still diverges, align the error message template with sibling tools, otherwise remove the stale row. Anchors: `src/RoslynMcp.Roslyn/Services/MutationAnalysisService.cs`, `src/RoslynMcp.Host.Stdio/Tools/AdvancedAnalysisTools.cs`. Regression test shape: 1 fixture invoking `find_type_mutations` with an unresolvable symbol handle; assert the canonical sibling-tool message, or document stale-row removal if current main already matches. Evidence: audit §13.27; gh #769 P3 audit batch. |
| `dependency-inversion-preview-newline-before-comma-formatting` | Low | none | Split from gh #769 §13.28 (parent tracker — do not auto-close). `dependency_inversion_preview` renders interface lists as `public class JobQueue : IJobQueue<JobRequest>\n, IJobQueueInverted` (newline before comma). Compiles but cosmetic noise. Fix: emit comma-trailing format (`IJobQueue<JobRequest>,\n    IJobQueueInverted`) consistent with .editorconfig style; ideally use the project's existing `csharp_new_line_*` settings if any apply. Anchors: `src/RoslynMcp.Roslyn/Services/CrossProjectRefactoringService.cs`, `src/RoslynMcp.Core/Services/ICrossProjectRefactoringService.cs`, `src/RoslynMcp.Host.Stdio/Tools/CrossProjectRefactoringTools.cs`, `tests/RoslynMcp.Tests/CrossProjectRefactoringIntegrationTests.cs`. Regression test shape: 1 fixture asserting the rendered interface list uses comma-trailing, not newline-leading. Evidence: audit §13.28; gh #769 P3 audit batch; 2026-05-20 backlog audit corrected stale anchors. |
| `go-to-definition-off-identifier-misleading-message` | Low | none | Split from gh #769 §13.30 (parent tracker — do not auto-close). For columns NOT on an identifier (e.g. whitespace between tokens), `go_to_definition` returns NotFound with "Ensure the workspace is loaded and the identifier is correct" — misleading because the workspace IS loaded and the user's "identifier" isn't wrong, the cursor is off the token. Fix: discriminate cause and emit a position-specific message like "No identifier at this position; cursor must be on a symbol token". Anchors: `src/RoslynMcp.Roslyn/Services/SymbolNavigationService.cs` (or similar containing `go_to_definition`), `src/RoslynMcp.Host.Stdio/Tools/SymbolTools.cs` (`goto_definition`). Regression test shape: 1 fixture clicking on a whitespace position; assert the error message indicates the position issue, not workspace/identifier. Evidence: audit §13.30; gh #769 P3 audit batch. |
| `compile-check-project-filter-miss-no-error-envelope` | Low | none | **Reserved — [gh #609](https://github.com/darylmcd/Roslyn-Backed-MCP/issues/609) (good first issue); skip in sweeps until contributor pickup.** `compile_check(projectName="NonExistent.Project")` returns `success=false` with the diagnostic buried in `restoreHint` rather than returning a structured error envelope with category `NotFound` or `InvalidArgument`. All other tools that fail to resolve an input (workspace ID, symbol handle, etc.) return a proper error category. This inconsistency means callers checking only `success` will miss the root cause (zero projects matched). Fix: return `InvalidArgument` (or `NotFound`) when the project filter matches 0 projects. Anchors: `src/RoslynMcp.Host.Stdio/Tools/CompileCheckTools.cs`, `src/RoslynMcp.Roslyn/Services/CompileCheckService.cs`. Regression test shape: 1 fixture calling `compile_check(projectName="DoesNotExist")` and asserting a structured error envelope with category `InvalidArgument`. Evidence: `review-inbox/archive/20260510T064835Z/20260510T053142Z_network-documentation_mcp-server-surface-test.md` §17.8 (FLAG-5, P3). |
| `find-duplicated-methods-mcp-wrapper-false-positive` | Low | none | **Reserved — [gh #612](https://github.com/darylmcd/Roslyn-Backed-MCP/issues/612) (good first issue); skip in sweeps until contributor pickup.** `find_duplicated_methods` clusters thin `[McpServerTool]`-attributed wrapper methods in `Host.Stdio/Tools/*` as a high-similarity (1.0) duplicate group. These wrappers are intentionally identical single-delegation shims — they cannot be de-duped because the MCP attribute model requires one method per tool name. The clustering is structurally correct but semantically a false positive that pollutes results for operators running this tool against the server itself or any MCP-server-shaped repo. Fix: add an `excludeMcpToolWrappers=true` (default `true`) parameter that skips `[McpServerTool]`-attributed methods whose body is a single delegation, similar to the existing `find_duplicate_helpers` framework-wrapper exclusion. Anchors: `src/RoslynMcp.Roslyn/Services/DuplicateMethodDetectorService.cs`, `src/RoslynMcp.Host.Stdio/Tools/AdvancedAnalysisTools.cs`, `src/RoslynMcp.Core/Services/IDuplicateMethodDetectorService.cs`. Regression test shape: 1 fixture confirming the 5-member wrapper cluster does not appear when `excludeMcpToolWrappers=true`. Evidence: `review-inbox/archive/20260510T064835Z/20260510T052934Z_roslyn-backed-mcp_mcp-server-surface-test.md` §14 (Finding F1, P3). |
| `get-prompt-text-multi-step-required-param-errors` | Low | none | **Reserved — [gh #610](https://github.com/darylmcd/Roslyn-Backed-MCP/issues/610) (good first issue); skip in sweeps until contributor pickup.** `get_prompt_text` surfaces required parameters one error at a time: calling `explain_error` with no params returns "workspaceId required"; supplying that returns "filePath required", etc. An agent must call N+1 times to discover N required params, burning turns and confusing retry logic. The error message should enumerate all missing required parameters in a single response. Anchors: `src/RoslynMcp.Host.Stdio/Tools/PromptShimTools.cs`, `src/RoslynMcp.Host.Stdio/Prompts/PromptMessageBuilder.cs`. Regression test shape: 1 fixture calling `get_prompt_text(explain_error, {})` and asserting the error lists all required params (workspaceId, filePath) in one response. Evidence: `review-inbox/archive/20260510T064835Z/20260510T053000Z_firewallanalyzer_mcp-server-surface-test.md` §8 + §14 (improvement); `review-inbox/archive/20260510T064835Z/20260510T052934Z_roslyn-backed-mcp_mcp-server-surface-test.md` §14 improvement. |
| `goto-type-definition-builtins-invalidoperation` | Low | none | **Reserved — [gh #607](https://github.com/darylmcd/Roslyn-Backed-MCP/issues/607) (good first issue); skip in sweeps until contributor pickup.** `goto_type_definition` on a primitive or BCL built-in type (e.g. `bool`) returns `InvalidOperation: Cannot navigate to type definition for 'bool' — neither the type nor any of its type arguments are defined in source`. The documented use case for this tool is "for a variable, go to its type"; calling it on a built-in is the natural consequence. The error category `InvalidOperation` is debatable — `NotFound` with a note about "metadata-only type" would be more consistent with the tool's other "target not in source" responses, or the tool could return a metadata-reference sentinel location. Anchors: `src/RoslynMcp.Host.Stdio/Tools/SymbolTools.cs` (`goto_type_definition`). Regression test shape: 1 fixture covering `goto_type_definition` on a `bool` position and asserting either `NotFound` category or a metadata-source sentinel payload. Evidence: `review-inbox/archive/20260510T064835Z/20260510T053000Z_firewallanalyzer_mcp-server-surface-test.md` §7 row 5 (P3); cross-confirmed `review-inbox/archive/20260512T194918Z/20260511T143600Z_itchatbot_mcp-server-surface-test.md` §13.12 (IT-Chat-Bot, roslyn-mcp 1.35.2). |
| `scorecard-blocked-to-backlog-row` | Low | none | Promotion-scorecard blocked and needs-more-evidence findings can silently accumulate because only `promote: ready` entries flow into normal intake. Fix: add an idempotent intake/reconcile path that reads `audit-reports/_aggregated-promotion-scorecard.json`, proposes or updates backlog rows for `promote: blocked` and `needs-more-evidence` entries, and leaves maintainer-readable rationale when an item is intentionally skipped. Anchors: `.claude/skills/backlog-intake/SKILL.md`, `.claude/skills/reconcile-backlog-vs-issues/SKILL.md`, `eng/aggregate-promotion-scorecards.ps1`, `eng/process-audit-reports.ps1`, `tests/RoslynMcp.Tests/Skills/AggregatePromotionScorecardsScriptTests.cs`. Regression test shape: fixture scorecard with one blocked item, one needs-more-evidence item, and one ready item; assert the blocked/evidence items produce stable proposed backlog ids while ready items continue through the existing promotion path. Evidence: 2026-05-11 adversarial audit gap #2; 2026-05-20 backlog audit found the previous defer dependency was stale because the reconcile skill now exists. |
| `validate-locator-preflight-measurement` | Low | none | Re-evaluation window for the deferred locator preflight tool has passed. Next deliverable is measurement, not implementation: measure current InvalidArgument-on-locator-tools failures after PR #483's `schemaHint`, write `ai_docs/items/validate-locator-preflight-measurement.md`, and either mark the preflight idea obsolete if locator-shape errors dropped >=80% or add a separate implementation row with current evidence. Anchors: `src/RoslynMcp.Host.Stdio/Tools/SymbolLocatorFactory.cs`, representative locator-consuming tools in `src/RoslynMcp.Host.Stdio/Tools/`, archived retro/surface-test evidence, and the new measurement note. Regression/output shape: measurement note records sample source, numerator/denominator, decision, and any follow-on backlog row; no new preflight tool code in this row. Evidence: 2026-05-04 multi-session retro showed post-hoc locator-shape errors; PR #483 shipped schema hints on 2026-05-05; 2026-05-20 backlog audit found the defer date had passed and corrected stale anchors. |
| `workspace-manager-cache-store-extraction-design` | Low | none | Write the design note that decides whether cache-probe orchestration, graph hashing, cache-entry enumeration, graph DTO construction, and metadata-reference freshness policy should move out of `WorkspaceManager` or remain there. This row is design-only; do not implement extraction until the note selects a bounded slice. Anchors: `src/RoslynMcp.Roslyn/Services/WorkspaceManager.cs`, `src/RoslynMcp.Roslyn/Services/WorkspaceCacheStore.cs`, `src/RoslynMcp.Core/Services/IWorkspaceCacheStore.cs`, `tests/RoslynMcp.Tests/Workspace/WorkspaceLoadCacheFastPathTests.cs`, `tests/RoslynMcp.Tests/Services/WorkspaceCacheStoreInvalidationTests.cs`. Regression/output shape: create `ai_docs/items/workspace-manager-cache-store-extraction-design.md` with ownership decision, risk analysis, and explicit follow-on row text only if code movement is justified. Evidence: 2026-05-13 refactor audit found the prior implementation row was stale because cache-store extraction mostly exists, but `WorkspaceManager` still owns policy-heavy cache/lifecycle helpers. |
| `scripting-service-runtime-state-extraction-design` | Low | none | Write a bounded design note for whether `ScriptingService` should extract execution coordination or runtime-state accounting while preserving the public `IScriptingService` contract and hard-deadline watchdog invariants. This row is design-only; do not move code until the note proves the extraction reduces risk. Anchors: `src/RoslynMcp.Roslyn/Services/ScriptingService.cs`, `tests/RoslynMcp.Tests/ScriptingServiceTests.cs`. Regression/output shape: create `ai_docs/items/scripting-service-runtime-state-extraction-design.md` covering current responsibilities, candidate extraction boundary, invariants to preserve, tests required for any follow-on implementation, and a reject/accept decision. Evidence: 2026-05-13 refactor audit via Roslyn `get_cohesion_metrics` reported LCOM4 7, 15 methods, and separate method clusters; counterargument remains that watchdog safety is tightly coupled and heavily documented. |
| `tool-surface-pagination-or-tool-sets` | Low | `agent-workflow-router-first-hop` lands or is rejected with evidence that routing is insufficient | 169 tools is approaching small-model discovery saturation, but current evidence points to a routing/steering problem more than a raw-count problem. Next deliverable, after the dependency gate fires: add named tool-set catalog resources (for example `roslyn://server/catalog/tool-sets` and `roslyn://server/catalog/tools/{toolSet}/{offset}/{limit}`) that expose bounded subsets such as `navigation`, `refactoring`, `validation`, and `analysis` without hiding tools from clients that can handle the full surface. Do not change MCP tool registration or default `tools/list` visibility in this row unless the implementation note proves the client supports that safely. Anchors: `src/RoslynMcp.Host.Stdio/Catalog/ServerSurfaceCatalog.cs`, `src/RoslynMcp.Host.Stdio/Resources/ServerResources.cs`, `tests/RoslynMcp.Tests/SurfaceCatalogTests.cs`. Regression test shape: catalog summary advertises the tool-set resource; each named set returns only matching categories with offset/limit/hasMore metadata; unknown set returns structured `InvalidArgument`; existing full and paginated catalog resources remain unchanged. Evidence: surface count drift across recent versions (`server_info.surface.registered.tools` 151 -> 169 since v1.27); MCP spec tools/list pagination; 2026-05-20 no-context subagent probe showed correctly steered agents chose Roslyn primitives without needing a full catalog dump. **Weaker evidence - N until small-model discovery friction reported externally.** Source: 2026-05-05 MCP-best-practices comparison §3 rec J plus 2026-05-20 agent-view review. |

## Defer

<!-- Explicitly parked. Record WHY in the `do` cell. -->

| id | pri | deps | do |
|----|-----|------|-----|
| `http-streamable-host-project` | Defer | concrete remote-deployment driver (named users, auth/observability/tenancy plan approved and staffed) | Add `src/RoslynMcp.Host.Http/` as a sibling project that reuses `Core` + `Roslyn`, exposing the same surface over MCP Streamable HTTP transport. Roadmap-aligned but requires multi-week design (auth flows, per-tenant rate limiting, TLS, observability, deployment story). The current local-first product is healthy and the roadmap explicitly punts on these concerns. Re-evaluate when there is a concrete remote-deployment ask with named users / SLA expectations. Anchors: new `src/RoslynMcp.Host.Http/` project; `Core` + `Roslyn` boundaries are already transport-agnostic per `ai_docs/architecture.md`. Evidence: `docs/roadmap.md` § HTTP/SSE Hosting (deferred to a second host project); MCP spec § Streamable HTTP transport (2025-03 stable). Source: 2026-05-05 MCP-best-practices comparison §3 rec H. |
| `workspace-process-pool-or-daemon` | Defer | future worse-profile evidence | Representative 227-project OrchardCore profile captured on 2026-04-26 did not justify daemon/process-pool implementation: `workspace_load` P95 was 44.85s, `symbol_search` P95 was 1.18s, and `find_references` P95 was 997ms, all below `docs/large-solution-profiling-baseline.md` thresholds. Keep this deferred unless a larger/worse customer-scale profile or daily-use evidence shows `workspace_load` / reload P95 blocking work after `workspace_warm`; then produce a bounded design note comparing daemon, process-pool, and shared-workspace approaches, including lifecycle and failure-isolation hooks. Evidence: `docs/large-solution-profiling-baseline.md` recorded OrchardCore run; local raw artifacts under `artifacts/large-solution-profiling/20260426T212443Z/`. |

## Refs

| Path | Role |
|------|------|
| `ai_docs/planning_index.md` | Planning router and scope boundary |
| `ai_docs/workflow.md` | Branch/PR workflow and backlog-closure rule |
| `ai_docs/prompts/backlog-sweep-plan.md` | Planner prompt; enforces per-initiative Rule 1 (bundle only on shared code path) / Rule 3 (≤4 prod files) / Rule 3b (toolPolicy) / Rule 4 (≤3 test files) / Rule 5 (≤80K context). Each row here is sized for one initiative. |
| `ai_docs/prompts/backlog-sweep-execute.md` | Executor companion; consumes the `state.json` the planner emits and vets each initiative against Rules 3/4/5 before starting work. |
| `ai_docs/bootstrap-read-tool-primer.md` | Self-edit session read-only tool primer (Roslyn-MCP read-side tools to prefer over Bash/Grep). |
| `ai_docs/runtime.md` | Bootstrap scope policy — distinguishes main-checkout self-edit (no `*_apply`) from worktree/parallel-subagent sessions. |
| `ai_docs/plans/20260513T140000Z_backlog-sweep/plan.md` | Backlog sweep (2026-05-13T14:00:00Z). Shipped 1 initiative across 1 PR; closed 2 backlog rows. |
| `docs/large-solution-profiling-baseline.md` | Evidence gate for daemon/process-pool performance work. |
| `ai_docs/procedures/deep-review-backlog-intake.md` | Intake procedure for future audit batches. |
| `review-inbox/` | Staging folder for the NEXT audit batch (flat directory; `/backlog-intake` reads here). |
| `review-inbox/archive/<batch-ts>/` | Processed audit/retro/promotion batches — one subdirectory per successful intake. Delete a batch after every actionable item is either shipped, explicitly rejected as stale, or summarized in current backlog rows. |
| `ai_docs/plans/20260513T010000Z_backlog-sweep/plan.md` | Backlog sweep (20260513T010000Z). Shipped 10 initiatives across 10 PRs (#716–#720, #722–#726); closed 10 backlog rows (skill-namespace-installed-as waves 2–11). |
| `ai_docs/plans/20260516T200033Z_backlog-sweep/plan.md` | Backlog sweep (2026-05-16T20:00:33Z). Shipped 15 initiatives across 15 PRs (#779–#782, #785–#786, #788–#790, #792–#795, #797–#798) over 5 parallel waves; closed 15 backlog rows; added 3 spin-off rows (`compact-semantic-grep-pagination`, `test-suite-fixture-reuse-cohesion-and-validate-git`, `test-suite-expanded-surface-class-split`). Precursor #783 fixed pre-existing `audit-phase-runner.md` test path breakage from PR #734. |
| `ai_docs/plans/20260517T025647Z_backlog-sweep/plan.md` | Backlog sweep (2026-05-17T02:56:47Z). Shipped 3 initiatives across 3 PRs (#803, #804, #805) in one parallel wave; closed 3 backlog rows (all spin-offs from the 20260516T200033Z sweep). Init 4 (`tool-surface-pagination-or-tool-sets`) deferred per reviewer warning (track-only row whose act-on triggers have not fired). Workflow improvement shipped mid-sweep in PR #802 — `initiative-executor` PR-body contract now auto-injects `Fixes #NNN` for non-Reserved `[gh #NNN]` row references; fired correctly on Init 1's PR #803 closing gh #760 (manually closed earlier in same session because PR #780 used `Refs` not `Fixes`). Observed flake on `WorkspaceExecutionGateTests.AutoReload_ResetsTimeoutBudget_ToolActionGetsFullBudget` failed validate on PR #803 + #804 before passing on retry — needs registry entry, see spin-off note in this PR. |
| `ai_docs/plans/20260517T235058Z_backlog-sweep/plan.md` | Backlog sweep (2026-05-17T23:50:58Z). Shipped 10 initiatives across 10 PRs (#810–#819) over 3 parallel waves; closed 11 backlog rows (8 High + 3 Medium). Closed `gh #735, #736, #737, #744, #745, #757, #763, #764, #765, #766, #767`. Init 10 was a Rule 1 bundle closing gh #736 + gh #737 with a single semantic split in `ReferenceService.FindOverridesAsync` (true overrides) vs new `FindSiblingInterfaceImplementationsAsync`. Init 5 + Init 10 shipped as `Changed — BREAKING` (overallStatus="test-zero-run" replaces vacuous "clean"; `member_hierarchy.overrides` and `symbol_relationships.overrides` drop sibling-interface-implementations into a new bucket). PR #819 validate failed once with self-hosted runner communication loss; passed on rerun. Pre-existing MSTest reference resolution friction in 3 ExtractMethod fixture tests observed by Inits 1 + 6 — reproduces on baseline, unrelated to changes. |
| `ai_docs/plans/20260518T221744Z_backlog-sweep/plan.md` | Backlog sweep (2026-05-18T22:17:44Z). Shipped 15 initiatives across 15 PRs (#823–#829 + #831–#838) over 2 waves in the prior session plus 2 parallel waves in this session; closed 15 backlog rows. Closed `gh #753, #754, #755, #756, #758, #759, #761, #762` in waves 1–2 of this session and `gh #616–#710 family` (project_diagnostics/symbol_signature_help/extract_interface/change_type_namespace/symbol_refactor/validate_workspace-overallStatus/test_run-fqdn) in prior session. Cross-session shape: prior session shipped initiatives 1–7 then opened reconcile PR #830; this session shipped 8–15 across two 4-initiative waves and reconciled both waves in a single combined chore PR. PR #834 (find-overrides) validate failed once mid-session with self-hosted runner communication loss after the local machine crashed; passed on rerun. Tested patterns: corlib-virtual suppression (mirroring #815 in `ReferenceService`), prompt-payload caps via `SerializeTruncatedList`, graceful timeout DTO mirroring `validate_recent_git_changes`, LoadLock removal proving hot-path latency win on `workspace_status(verbose=true)`. |
| `ai_docs/plans/20260519T145945Z_backlog-sweep/plan.md` | Backlog sweep (2026-05-19T14:59:46Z). Shipped 6 initiatives across 6 PRs (#842–#847) over 2 parallel waves (wave 1: 4 inits; wave 2: 2 inits, warn-aware cap due to init 3's Rule 5b warn); closed 6 backlog rows (all Low/P3 from the 2026-05-16 self-audit; closed `gh #738, #739, #740, #741, #742, #743`). One BREAKING change (#847 `find_type_mutations`: `MutationScope` string → `MutationScopes` IReadOnlyList<string>). PR #842 validate failed first run on registered known-flake `WorkspaceExecutionGateTests.AutoReload_ResetsTimeoutBudget_ToolActionGetsFullBudget`; passed cleanly on rerun. 5 of 6 backlog rows had stale anchors (paths refactored since the 2026-05-16 audit); deepeners corrected before execute. Tested patterns: `/backlog-sweep:prepare` consolidated planner-review-handoff pipeline (single-orchestrator instead of separate :plan + :review); Sonnet-handoff `<details>` blocks on judgment-heavy initiatives. |
| `ai_docs/plans/20260519T193650Z_backlog-sweep/plan.md` | Backlog sweep (2026-05-19T19:36:50Z). Shipped 15 initiatives across 15 PRs (#852–#857, #860–#865, #868–#871) over 5 parallel waves spanning 2 sessions (prior session: 5 initiatives in 2 waves; this session: 6 initiatives wave-of-4 + wave-of-2 + 4 initiatives wave-of-4); closed 15 backlog rows (11 Medium-P2 from gh #768 child rows + 4 Low-P3 from gh #769 child rows). Two BREAKING changes (#847 family + #870 `symbol_info`/`document_symbols` `kind=Record`/`RecordStruct` for positional records). PR #864 validate failed first run on a NEW `ExternalEditStalenessTests.EnsureFreshForWritePreview_RefusesWithReloadHint_WhenExternalEdit` flake (FileSystemWatcher 2000ms threshold; spin-off PR #866 registers it); PR #868 validate failed first run on a NEW `WorkspaceExecutionGateTests.AutoReload_ParallelFanout_AllReadersSucceedAfterReload` flake (same call-site family as the registered AutoReload_ResetsTimeoutBudget entry — needs registry sibling row). Both passed cleanly on rerun. 11 of 15 backlog rows had stale anchors (the 2026-05-16 audit's paths predated multiple refactors); deepeners corrected each before execute. Sonnet-handoff `<details>` blocks were critical on #7 (`CompletionTrigger.TriggerOnInsertion` → `CreateInsertionTrigger` API correction) and #13 (`SharedWorkspaceTestBase` vs `AdhocWorkspace` test-base preference). Tested patterns: warn-aware wave sizing (cap-at-2 when warn-flagged initiative included), wave-combining reconcile (single chore PR carrying state for both waves), spin-off chip for new known-flake registry rows. |
