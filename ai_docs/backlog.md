# Next work and backlog

<!-- purpose: Open work only; contract for agents syncing backlog on ship. -->
<!-- scope: in-repo -->

**updated_at: 2026-05-18T03:09:14Z**

## Agent contract

| | |
|---|---|
| **Scope** | This file lists unfinished work only. It is not a changelog. |
| **MUST** | Remove or update backlog rows when work ships; do it in the same PR or an immediate follow-up. |
| **MUST** | End implementation plans with a final todo: `backlog: sync ai_docs/backlog.md`. |
| **MUST** | Use stable, kebab-case `id` values per open row. |
| **MUST** | Every row's `do` cell summarizes the current need + the concrete next deliverable. Include `Anchors:` (specific source file paths) when the row references code, and evidence (audit/retro/CI signal) when one exists. |
| **MUST** | Size every row to a single bounded initiative — ≤4 production files, ≤3 test files, one regression-test shape. Split heroic multi-bug rows into per-bug children before planning against them. |
| **MUST NOT** | Add `Completed`, `Shipped`, `Done`, `History`, or `Changelog` sections. Git is the archive. |
| **MUST NOT** | Leave done items in the open table. |
| **MUST NOT** | Use `### <id>` body sections per item. The table row IS the canonical form. Items needing long-form depth (more than ~10 lines) link to `ai_docs/items/<id>.md` from the `do` cell. |

## Standing rules

- **Reprioritize on each audit pass.** Stale priority order is a finding.
- **Keep rows planner-ready.** A row is ready when an agent can read it cold and start a plan: name the live anchors and the next concrete deliverable or investigation output.
- **Replace stale umbrella rows with concrete follow-ons** before planning against them.
- **Long-form audit evidence belongs in referenced reports**, not in this file. The `do` cell carries a one-line evidence summary plus the report path.
- **Weak-evidence flag.** When a row's signal is thin (single retro session, self-audit only, etc.) say so explicitly in the `do` cell ("Weaker evidence — N until external session reproduces").
- **GitHub-issue cross-link.** When a row has a corresponding open GitHub Issue, surface the link at the start of the `do` cell so the backlog and issue stay paired. Two flavors:
  - **Reserved for contributor pickup** (`good first issue` / `help wanted` labels): prefix the `do` cell with `**Reserved — [gh #NNN](https://github.com/darylmcd/Roslyn-Backed-MCP/issues/NNN) (good first issue|help wanted); skip in sweeps until contributor pickup.**` — `/backlog-sweep:plan` skips these per its Step 1 hard-skip rule. Remove the marker (or close the row) when a contributor PR lands or when the maintainer reclaims the work.
  - **Tracked-only** (auto-filed audit issues that aren't promoted to a contributor label): prefix the `do` cell with `[gh #NNN](https://github.com/darylmcd/Roslyn-Backed-MCP/issues/NNN) — `. Sweep treats these as normal claimable rows; the implementing PR closes both the issue (via `Fixes #NNN`) and the backlog row.
- **Priority tiers:** Critical > High > Medium > Low > Defer.
- See `workflow.md` → **Backlog closure** for close-in-PR expectations.

---

## Critical

<!-- Production-breaking or blocking work. Empty section is fine; keep the header. -->

| id | pri | deps | do |
|----|-----|------|-----|

## High

| id | pri | deps | do |
|----|-----|------|-----|


## Medium

| id | pri | deps | do |
|----|-----|------|-----|
| `project-diagnostics-totaldiagnostics-collapses-under-severity-filter` | Medium | none | [gh #746](https://github.com/darylmcd/Roslyn-Backed-MCP/issues/746): project_diagnostics totalDiagnostics collapses to filtered count under severityFilter Anchors: - src/RoslynMcp.Roslyn/Services/ProjectDiagnosticsService.cs (likely) Regression test shape: 1 fixture covering the repro scenario from gh #746. Evidence: gh #746. |
| `symbol-signature-help-returns-bare-null-for-resolvable-method-metadata` | Medium | none | [gh #747](https://github.com/darylmcd/Roslyn-Backed-MCP/issues/747): symbol_signature_help returns bare JSON null for resolvable method metadataName Anchors: - src/NetworkDocumentation.Core/NetworkDocumentation.Core/Snapshots/SnapshotStore.cs:57 Regression test shape: 1 fixture covering the repro scenario from gh #747. Evidence: gh #747. |
| `extract-interface-preview-duplicate-interface-when-already-implements` | Medium | none | [gh #748](https://github.com/darylmcd/Roslyn-Backed-MCP/issues/748): extract_interface_preview emits duplicate interface when type already implements a covering one Anchors: - src/NetworkDocumentation.Core/NetworkDocumentation.Core/DeviceClassifier/DeviceClassifierService.cs:11 Regression test shape: 1 fixture covering the repro scenario from gh #748. Evidence: gh #748. |
| `change-type-namespace-preview-omits-consumer-using-additions` | Medium | none | [gh #749](https://github.com/darylmcd/Roslyn-Backed-MCP/issues/749): change_type_namespace_preview omits consumer-side using-directive additions Anchors: - src/NetworkDocumentation.Parsers/NetworkDocumentation.Parsers/Commands/NxosVersionParser.cs Regression test shape: 1 fixture covering the repro scenario from gh #749. Evidence: gh #749. |
| `symbol-refactor-preview-empty-appliedfiles-on-success` | Medium | none | [gh #750](https://github.com/darylmcd/Roslyn-Backed-MCP/issues/750): symbol_refactor_preview apply returns success=true with empty appliedFiles despite real mutation Anchors: - preview_multi_file_edit_apply response shape Regression test shape: 1 fixture covering the repro scenario from gh #750. Evidence: gh #750. |
| `validate-workspace-overallstatus-analyzer-error-with-empty-errordiagnostics` | Medium | none | [gh #751](https://github.com/darylmcd/Roslyn-Backed-MCP/issues/751): validate_workspace returns overallStatus=analyzer-error with empty errorDiagnostics and compile success Anchors: - validate_workspace + validate_recent_git_changes response shape Regression test shape: 1 fixture covering the repro scenario from gh #751. Evidence: gh #751. |
| `test-run-fqdn-drift-vs-test-discover` | Medium | none | [gh #752](https://github.com/darylmcd/Roslyn-Backed-MCP/issues/752): test_run filter from test_discover FQDN produces silent zero-hits due to folder-infix drift Anchors: - test_discover output vs xunit assembly testcase fqdns Regression test shape: 1 fixture covering the repro scenario from gh #752. Evidence: gh #752. |
| `migrate-package-preview-misses-analyzer-only-references` | Medium | none | [gh #753](https://github.com/darylmcd/Roslyn-Backed-MCP/issues/753): migrate_package_preview misses analyzer-only PackageReference entries Anchors: - migrate_package_preview PackageReference walker Regression test shape: 1 fixture covering the repro scenario from gh #753. Evidence: gh #753. |
| `find-overrides-payload-overflow-on-corlib-virtual` | Medium | none | [gh #754](https://github.com/darylmcd/Roslyn-Backed-MCP/issues/754): find_overrides payload overflow on corlib virtuals (System.Object.ToString/Equals/GetHashCode) Anchors: - find_overrides auto-promotion behavior Regression test shape: 1 fixture covering the repro scenario from gh #754. Evidence: gh #754. |
| `analyze-dependencies-prompt-payload-overflow` | Medium | none | [gh #755](https://github.com/darylmcd/Roslyn-Backed-MCP/issues/755): analyze_dependencies prompt payload overflow (63 KB) on small 9-project workspace Anchors: - analyze_dependencies prompt rendering Regression test shape: 1 fixture covering the repro scenario from gh #755. Evidence: gh #755. |
| `review-test-coverage-prompt-payload-overflow` | Medium | none | [gh #756](https://github.com/darylmcd/Roslyn-Backed-MCP/issues/756): review_test_coverage prompt payload overflow (121 KB) embeds full per-method coverage map Anchors: - review_test_coverage prompt rendering Regression test shape: 1 fixture covering the repro scenario from gh #756. Evidence: gh #756. |
| `find-property-writes-metadataname-hint-mismatches-input-shape` | Medium | none | [gh #758](https://github.com/darylmcd/Roslyn-Backed-MCP/issues/758): find_property_writes(metadataName) returns position-shaped hint contradicting input shape Anchors: - src/TradeWise.Domain/Alerts/AlertRule.cs **finding**: `find_property_writes` accepts two input shapes: `metadataName` (symbol-string locator) and `filePath+line+column` (positional locator). When... Regression test shape: 1 fixture covering the repro scenario from gh #758. Evidence: gh #758. |
| `validate-workspace-25s-internalvalidationtimeoutexception-on-medium-solution` | Medium | none | [gh #759](https://github.com/darylmcd/Roslyn-Backed-MCP/issues/759): validate_workspace InternalValidationTimeoutException at 25s on 11-project solution; not graceful FailureEnvelope Anchors: - TradeWise.sln **finding**: `validate_workspace` throws `InternalValidationTimeoutException` after 25005 ms when run against an 11-project / 759-document .NET 10 solution. The thrown exception is... Regression test shape: 1 fixture covering the repro scenario from gh #759. Evidence: gh #759. |
| `workspace-status-verbose-5s-timeout-race-on-ready-workspace` | Medium | none | [gh #761](https://github.com/darylmcd/Roslyn-Backed-MCP/issues/761): workspace_status(verbose=true) 5s timeout on a Ready workspace; resource sibling returns same data in <1ms Anchors: - TradeWise.sln **finding**: `workspace_status(verbose=true)` times out at 5005 ms on a workspace that just returned `isReady=true, isStale=false` from `workspace_status(verbose=false)` moments... Regression test shape: 1 fixture covering the repro scenario from gh #761. Evidence: gh #761. |
| `source-file-resource-requires-url-encoded-absolute-path-undocumented` | Medium | none | [gh #762](https://github.com/darylmcd/Roslyn-Backed-MCP/issues/762): source_file / source_file_lines resources require URL-encoded absolute path; doc gap Anchors: - src/TradeWise.Domain/Alerts/AlertRuleType.cs **finding**: The `roslyn://workspace/{workspaceId}/file/{filePath}` resource (and its `/lines/{lineRange}` sibling) requires `{filePath}` to be a... Regression test shape: 1 fixture covering the repro scenario from gh #762. Evidence: gh #762. |
| `firewallanalyzer-p2-polish-aggregate-20260516` | Medium | none | [gh #768](https://github.com/darylmcd/Roslyn-Backed-MCP/issues/768): Aggregator row - 11 P2 findings from the 2026-05-16 firewallanalyzer surface-test (sections 13.3, 13.9-13.18). Each finding is independent and should be split into its own row when a maintainer claims it; this row tracks intake-completeness only. Findings span: static-extension-host cross-tool blind spot, code_fix_preview vs fix_all_preview shape inconsistency, suggest_refactorings facade-pattern recommendation, find_duplicated_methods symmetric-mapper false positives, test_coverage fail-fast on missing coverlet, set_conditional_property_preview allowlist narrowness, get_completions filterText ranking gap, semantic_grep dotted-identifier tokenization docs gap, get_di_registrations multi-registration overcounting, migrate_package_preview no-op silent mutation, scaffold_first_test_file_preview single-target heuristic. Anchors: per-finding in gh #768 body. Regression test shape: 1 fixture per split row. Evidence: gh #768; audit-reports/20260516T062913Z_firewallanalyzer_mcp-server-surface-test.md. |

## Low

| id | pri | deps | do |
|----|-----|------|-----|
| `validate-workspace-changetracker-no-disk-reconcile-after-git-checkout` | Low | none | [gh #738](https://github.com/darylmcd/Roslyn-Backed-MCP/issues/738): `validate_workspace(changedFilePaths=null)` ChangeTracker auto-scoped list still includes `.editorconfig` after a successful `git checkout -- .editorconfig` revert that restored the file to its `HEAD` content. `git status --porcelain .editorconfig` reports the file as clean, but the bundle's `changedFilePaths` still surfaces it. ChangeTracker's... Fix: ChangeTracker should reconcile its auto-scope against disk-content hashes (or `git status`) when computing the bundle, OR document the "intent recorded, not net change" semantic and rename the field (e.g. `mutatedSinceLoad`). Anchors: `src/RoslynMcp.Roslyn/Services/ChangeTracker.cs` Regression test shape: 1 fixture covering the repro scenario from gh #738. Evidence: gh #738; Â§13.5 / Phase 8 FLAG-7B (audit-reports/20260516T062547Z_roslyn-backed-mcp_mcp-server-surface-test.md); observed on roslyn-mcp roslyn-mcp 1.38.1. |
| `format-document-preview-empty-diff-instead-of-noop` | Low | none | [gh #739](https://github.com/darylmcd/Roslyn-Backed-MCP/issues/739): `format_document_preview` returns `changes: [{filePath, unifiedDiff: "<headers only>"}]` for a no-op format pass â€” the entry exists in the array but the diff body has only `--- a/path` / `+++ b/path` headers and no `@@` hunks. Callers that check `changes.length > 0` to decide "are there changes to apply?" misread no-op as pending changes. Fix: Filter empty-diff entries from `changes` (so `changes: []` is the canonical no-op shape), OR add an explicit `noOpResult: true` flag at the envelope level for callers to branch on. Anchors: `src/RoslynMcp.Roslyn/Services/FormatService.cs` Regression test shape: 1 fixture covering the repro scenario from gh #739. Evidence: gh #739; Â§13.6 / Phase 6 FLAG-6A (audit-reports/20260516T062547Z_roslyn-backed-mcp_mcp-server-surface-test.md); observed on roslyn-mcp roslyn-mcp 1.38.1. |
| `workspace-changes-atomic-batch-split-without-batchid` | Low | none | [gh #740](https://github.com/darylmcd/Roslyn-Backed-MCP/issues/740): `workspace_changes` splits an atomic `apply_multi_file_edit` two-file batch into 2 separate ledger entries (same timestamp, both tagged `toolName: "apply_multi_file_edit"`, separate descriptions "Apply text edit to X.cs"). No `batchId` field correlates the entries. Callers using `revert_apply_by_sequence(N)` on one half of the batch may be... Fix: Either (a) merge atomic batches into one ledger entry with `affectedFiles: [A.cs, B.cs]`, or (b) add a `batchId` correlation field so callers can group / atomically-revert a multi-file batch through `revert_apply_by_sequence`. Anchors: `src/RoslynMcp.Roslyn/Services/WorkspaceChangeLedger.cs` (search) Regression test shape: 1 fixture covering the repro scenario from gh #740. Evidence: gh #740; Â§13.7 / Phase 6 FLAG-6B (audit-reports/20260516T062547Z_roslyn-backed-mcp_mcp-server-surface-test.md); observed on roslyn-mcp roslyn-mcp 1.38.1. |
| `find-type-mutations-single-scope-misses-compound-io` | Low | none | [gh #741](https://github.com/darylmcd/Roslyn-Backed-MCP/issues/741): `find_type_mutations.MutationScope` is single-valued and misses compound scopes. For `WorkspaceManager` all 5 mutating members are classified `CollectionWrite` only, but `LoadAsync` and `GetSourceGeneratedDocumentsAsync` also do heavy disk IO (open `MSBuildWorkspace`, run `dotnet restore`, read package files, write log entries) â€” they should... Fix: Convert `MutationScope` to a `[Flags]` enum and OR together every detected scope per member. Update the classifier to record `IO` when a member calls `MSBuildWorkspace.OpenSolutionAsync`, `File.*`, `Process.Start("dotnet", ...)`, etc. Anchors: `src/RoslynMcp.Roslyn/Services/MutationAnalysisService.cs:87` (ClassifyMethod), MutationScope enum definition Regression test shape: 1 fixture covering the repro scenario from gh #741. Evidence: gh #741; Â§13.8 / Phase 3 FLAG-3A (audit-reports/20260516T062547Z_roslyn-backed-mcp_mcp-server-surface-test.md); observed on roslyn-mcp roslyn-mcp 1.38.1. |
| `callers-callees-previewtext-asymmetry` | Low | none | [gh #742](https://github.com/darylmcd/Roslyn-Backed-MCP/issues/742): `callers_callees` populates `previewText` on every entry in the `callers` array but returns `null` for every entry in the `callees` array. Tools that render call graphs uniformly across both directions hit a presentation gap. (Distinct from CLOSED #616, which was about metadata-name input parsing; this is about response-shape asymmetry between... Fix: Populate `previewText` on `callees` using the same per-location source-extract path used for `callers`, OR document the asymmetry in the tool schema and explain why callees are preview-only-on-demand. Anchors: `src/RoslynMcp.Roslyn/Services/CallersCalleesService.cs` Regression test shape: 1 fixture covering the repro scenario from gh #742. Evidence: gh #742; Â§13.9 / Phase 3 FLAG-3B (audit-reports/20260516T062547Z_roslyn-backed-mcp_mcp-server-surface-test.md); observed on roslyn-mcp roslyn-mcp 1.38.1. |
| `analyze-control-flow-partial-slice-warning-on-full-method` | Low | none | [gh #743](https://github.com/darylmcd/Roslyn-Backed-MCP/issues/743): `analyze_control_flow` emits a misleading "partial-slice" warning when the supplied range covers an entire method declaration plus body. The warning reads "Control-flow results may be incomplete for this line range. Prefer a range that covers full statement blocks within a single method body (not a partial slice of a method)." â€” but the range... Fix: When the range matches a single `MethodDeclaration` span exactly (or the `[bodyBlockStart, bodyBlockEnd]` extent), suppress the warning. Alternatively, change the warning text to "the method declaration line was included; pass only the method body Block span to get statement-level control-flow... Anchors: `src/RoslynMcp.Roslyn/Services/FlowAnalysisService.cs` Regression test shape: 1 fixture covering the repro scenario from gh #743. Evidence: gh #743; Â§13.10 / Phase 4 FLAG-4A (audit-reports/20260516T062547Z_roslyn-backed-mcp_mcp-server-surface-test.md); observed on roslyn-mcp roslyn-mcp 1.38.1. |
| `firewallanalyzer-p3-polish-aggregate-20260516` | Low | none | [gh #769](https://github.com/darylmcd/Roslyn-Backed-MCP/issues/769): Aggregator row - 8 P3 cosmetic/UX/docs-gap findings from the 2026-05-16 firewallanalyzer surface-test (sections 13.19-13.26). Each finding is independent and should be split into its own row when a maintainer claims it; this row tracks intake-completeness only. Findings span: find_duplicate_helpers framework-wrapper filter leak, document_symbols vs symbol_info record-kind disagreement, compile_check file-filter scope-narrowing inconsistency, get_nuget_dependencies(summary=true) CPM literal, get_cohesion_metrics always-null lifecyclePattern, add_pragma_suppression CRLF in LF file, get_msbuild_properties vs workspace_reload OutputType mismatch, source_file_lines off-by-one marker count. Anchors: per-finding in gh #769 body. Regression test shape: 1 fixture per split row. Evidence: gh #769; audit-reports/20260516T062913Z_firewallanalyzer_mcp-server-surface-test.md. |
| `compile-check-project-filter-miss-no-error-envelope` | Low | none | **Reserved — [gh #609](https://github.com/darylmcd/Roslyn-Backed-MCP/issues/609) (good first issue); skip in sweeps until contributor pickup.** `compile_check(projectName="NonExistent.Project")` returns `success=false` with the diagnostic buried in `restoreHint` rather than returning a structured error envelope with category `NotFound` or `InvalidArgument`. All other tools that fail to resolve an input (workspace ID, symbol handle, etc.) return a proper error category. This inconsistency means callers checking only `success` will miss the root cause (zero projects matched). Fix: return `InvalidArgument` (or `NotFound`) when the project filter matches 0 projects. Anchors: `src/RoslynMcp.Host.Stdio/Tools/CompileCheckTools.cs`, `src/RoslynMcp.Roslyn/Services/CompileCheckService.cs`. Regression test shape: 1 fixture calling `compile_check(projectName="DoesNotExist")` and asserting a structured error envelope with category `InvalidArgument`. Evidence: `review-inbox/archive/20260510T064835Z/20260510T053142Z_network-documentation_mcp-server-surface-test.md` §17.8 (FLAG-5, P3). |
| `find-duplicated-methods-mcp-wrapper-false-positive` | Low | none | **Reserved — [gh #612](https://github.com/darylmcd/Roslyn-Backed-MCP/issues/612) (good first issue); skip in sweeps until contributor pickup.** `find_duplicated_methods` clusters thin `[McpServerTool]`-attributed wrapper methods in `Host.Stdio/Tools/*` as a high-similarity (1.0) duplicate group. These wrappers are intentionally identical single-delegation shims — they cannot be de-duped because the MCP attribute model requires one method per tool name. The clustering is structurally correct but semantically a false positive that pollutes results for operators running this tool against the server itself or any MCP-server-shaped repo. Fix: add an `excludeMcpToolWrappers=true` (default `true`) parameter that skips `[McpServerTool]`-attributed methods whose body is a single delegation, similar to the existing `find_duplicate_helpers` framework-wrapper exclusion. Anchors: `src/RoslynMcp.Roslyn/Services/DuplicateMethodDetectorService.cs`, `src/RoslynMcp.Host.Stdio/Tools/AdvancedAnalysisTools.cs`, `src/RoslynMcp.Core/Services/IDuplicateMethodDetectorService.cs`. Regression test shape: 1 fixture confirming the 5-member wrapper cluster does not appear when `excludeMcpToolWrappers=true`. Evidence: `review-inbox/archive/20260510T064835Z/20260510T052934Z_roslyn-backed-mcp_mcp-server-surface-test.md` §14 (Finding F1, P3). |
| `get-prompt-text-multi-step-required-param-errors` | Low | none | **Reserved — [gh #610](https://github.com/darylmcd/Roslyn-Backed-MCP/issues/610) (good first issue); skip in sweeps until contributor pickup.** `get_prompt_text` surfaces required parameters one error at a time: calling `explain_error` with no params returns "workspaceId required"; supplying that returns "filePath required", etc. An agent must call N+1 times to discover N required params, burning turns and confusing retry logic. The error message should enumerate all missing required parameters in a single response. Anchors: `src/RoslynMcp.Host.Stdio/Tools/PromptShimTools.cs`, `src/RoslynMcp.Host.Stdio/Prompts/PromptMessageBuilder.cs`. Regression test shape: 1 fixture calling `get_prompt_text(explain_error, {})` and asserting the error lists all required params (workspaceId, filePath) in one response. Evidence: `review-inbox/archive/20260510T064835Z/20260510T053000Z_firewallanalyzer_mcp-server-surface-test.md` §8 + §14 (improvement); `review-inbox/archive/20260510T064835Z/20260510T052934Z_roslyn-backed-mcp_mcp-server-surface-test.md` §14 improvement. |
| `goto-type-definition-builtins-invalidoperation` | Low | none | **Reserved — [gh #607](https://github.com/darylmcd/Roslyn-Backed-MCP/issues/607) (good first issue); skip in sweeps until contributor pickup.** `goto_type_definition` on a primitive or BCL built-in type (e.g. `bool`) returns `InvalidOperation: Cannot navigate to type definition for 'bool' — neither the type nor any of its type arguments are defined in source`. The documented use case for this tool is "for a variable, go to its type"; calling it on a built-in is the natural consequence. The error category `InvalidOperation` is debatable — `NotFound` with a note about "metadata-only type" would be more consistent with the tool's other "target not in source" responses, or the tool could return a metadata-reference sentinel location. Anchors: `src/RoslynMcp.Host.Stdio/Tools/SymbolTools.cs` (`goto_type_definition`). Regression test shape: 1 fixture covering `goto_type_definition` on a `bool` position and asserting either `NotFound` category or a metadata-source sentinel payload. Evidence: `review-inbox/archive/20260510T064835Z/20260510T053000Z_firewallanalyzer_mcp-server-surface-test.md` §7 row 5 (P3); cross-confirmed `review-inbox/archive/20260512T194918Z/20260511T143600Z_itchatbot_mcp-server-surface-test.md` §13.12 (IT-Chat-Bot, roslyn-mcp 1.35.2). |
| `tool-surface-pagination-or-tool-sets` | Low | none | 167 tools is approaching small-model discovery saturation. MCP `tools/list` already supports cursor pagination per spec; layered on top, named "tool sets" (`navigation`, `refactoring`, `validation`, `analysis`) would let clients enable subsets via `server_info` user-config. Speculative — not yet causing observable friction in the retro window — but the surface continues to grow. Track only; act when (1) a small-model client reports tool-discovery friction, OR (2) surface count crosses a hard threshold (~200 tools). Anchors: `src/RoslynMcp.Host.Stdio/Catalog/ServerSurfaceCatalog.cs`, `src/RoslynMcp.Host.Stdio/Resources/ServerResources.cs`. Regression test shape: defer until acted on. Evidence: surface count drift across recent versions (`server_info.surface.registered.tools` 151 → 167 since v1.27); MCP spec § tools/list pagination. **Weaker evidence — N until small-model discovery friction reported externally.** Source: 2026-05-05 MCP-best-practices comparison §3 rec J. |

## Defer

<!-- Explicitly parked. Record WHY in the `do` cell. -->

| id | pri | deps | do |
|----|-----|------|-----|
| `scorecard-blocked-to-backlog-row` | Defer | `reconcile-backlog-vs-github-issues` lands first | The aggregated promotion scorecard (`audit-reports/_aggregated-promotion-scorecard.json`) classifies experimental entries into `promote: ready` / `promote: blocked` / `needs-more-evidence`. Today only `promote: ready` items get consumed (by `/publish-preflight` Step 8 → `/promote-tier`). `promote: blocked` items (e.g., "0 of 4 repos see this tool pass round-trip") and `needs-more-evidence` items don't get tracked as backlog rows for investigation, so they silently accumulate across scorecard runs. Deferred because (a) the blocked count is small today (2 in the most recent run), (b) `/publish-preflight` surfaces them to the maintainer who can decide ad-hoc, and (c) the larger pattern of "scorecard verdict → backlog row" needs the reconcile skill to land first so we have an idempotent mechanism for writing/updating rows over time. Re-evaluate after `reconcile-backlog-vs-github-issues` ships and the reconcile pattern is proven. Anchors (when un-deferred): extension to `.claude/skills/backlog-intake/SKILL.md` Phase 0.5 or NEW `.claude/skills/intake-scorecard-blockers/SKILL.md`; reads `audit-reports/_aggregated-promotion-scorecard.json`. Evidence: 2026-05-11 adversarial audit, gap #2 in the post-skill pipeline report. |
| `validate-locator-preflight-tool` | Defer | re-measurement window after `inv-arg-envelope-schema-hint` (merged 2026-05-05 via PR #483) | Deferred 2026-05-05 after `inv-arg-envelope-schema-hint` shipped because the schema hint may make the standalone preflight tool obsolete. Re-measure InvalidArgument-on-locator-tools rate over a 7-day window before deciding whether to ship this tool. If `schemaHint` reduces locator-shape errors >=80%, mark obsolete; otherwise re-plan and ship. Re-evaluate after 2026-05-12. Anchors: new tool surface in `src/RoslynMcp.Host.Stdio/Tools/SymbolLocatorTool.cs`, reuses existing `src/RoslynMcp.Host.Stdio/Tools/SymbolLocatorFactory.cs` helper from PR #474, catalog registration. Regression test shape: 1 fixture covering valid file/line, valid metadataName, malformed metadataName (parenthesized), unparseable symbolHandle, fully empty locator. Evidence: 2026-05-04 multi-session retro sessions `dd0a7e48`, `5687cbf9`, `f71cbc02`, `eac7f094`, `28e53529`, `b093b4b1` hit post-hoc locator-shape errors that could benefit from pre-flight validation. |
| `http-streamable-host-project` | Defer | concrete remote-deployment driver (named users, auth/observability/tenancy plan approved and staffed) | Add `src/RoslynMcp.Host.Http/` as a sibling project that reuses `Core` + `Roslyn`, exposing the same surface over MCP Streamable HTTP transport. Roadmap-aligned but requires multi-week design (auth flows, per-tenant rate limiting, TLS, observability, deployment story). The current local-first product is healthy and the roadmap explicitly punts on these concerns. Re-evaluate when there is a concrete remote-deployment ask with named users / SLA expectations. Anchors: new `src/RoslynMcp.Host.Http/` project; `Core` + `Roslyn` boundaries are already transport-agnostic per `ai_docs/architecture.md`. Evidence: `docs/roadmap.md` § HTTP/SSE Hosting (deferred to a second host project); MCP spec § Streamable HTTP transport (2025-03 stable). Source: 2026-05-05 MCP-best-practices comparison §3 rec H. |
| `workspace-process-pool-or-daemon` | Defer | future worse-profile evidence | Representative 227-project OrchardCore profile captured on 2026-04-26 did not justify daemon/process-pool implementation: `workspace_load` P95 was 44.85s, `symbol_search` P95 was 1.18s, and `find_references` P95 was 997ms, all below `docs/large-solution-profiling-baseline.md` thresholds. Keep this deferred unless a larger/worse customer-scale profile or daily-use evidence shows `workspace_load` / reload P95 blocking work after `workspace_warm`; then produce a bounded design note comparing daemon, process-pool, and shared-workspace approaches, including lifecycle and failure-isolation hooks. Evidence: `docs/large-solution-profiling-baseline.md` recorded OrchardCore run; local raw artifacts under `artifacts/large-solution-profiling/20260426T212443Z/`. |
| `workspace-manager-cache-store-extraction` | Defer | needs design note first | Narrowed 2026-05-13 audit: `IWorkspaceCacheStore`, `WorkspaceCacheStore`, host DI wiring, and direct store tests already exist; the remaining concern is that `WorkspaceManager` still owns cache-probe orchestration, graph hashing, cache-entry enumeration, graph DTO construction, and metadata-reference freshness policy. Write `ai_docs/items/workspace-manager-cache-store-extraction-design.md` deciding whether those policy helpers should move into a cache probe/policy component or stay in `WorkspaceManager`, then implement only the selected bounded slice. Anchors: `src/RoslynMcp.Roslyn/Services/WorkspaceManager.cs:797,837,852,965,1026,1061,1110,1234`, `src/RoslynMcp.Roslyn/Services/WorkspaceCacheStore.cs`, `src/RoslynMcp.Core/Services/IWorkspaceCacheStore.cs`, `tests/RoslynMcp.Tests/Workspace/WorkspaceLoadCacheFastPathTests.cs`, `tests/RoslynMcp.Tests/Services/WorkspaceCacheStoreInvalidationTests.cs`. Regression test shape: preserve existing cache fast-path, hash-key stability, and metadata-reference invalidation coverage; add one focused fixture only if ownership moves. Evidence: 2026-05-13 refactor audit found the prior row was stale and over-described already-shipped extraction work; Roslyn metrics still show `WorkspaceManager.LoadAsync` and `LoadIntoSessionAsync` as large cache/lifecycle hotspots. |
| `scripting-service-runtime-state-extraction-design` | Defer | design note before code | `ScriptingService` now concentrates script option building, capacity admission, dedicated worker-thread startup, hard-deadline timers, heartbeat progress, abandoned-thread accounting, logging, and DTO shaping in one 15-method class. Write `ai_docs/items/scripting-service-runtime-state-extraction-design.md` with a bounded extraction plan, likely separating execution coordination or runtime-state accounting without changing the public `IScriptingService` contract. Anchors: `src/RoslynMcp.Roslyn/Services/ScriptingService.cs`, `tests/RoslynMcp.Tests/ScriptingServiceTests.cs`. Regression test shape: preserve hard-deadline return for infinite loops, capacity refusal, heartbeat count, runtime exception shaping, and cancellation behavior. Evidence: 2026-05-13 refactor audit via Roslyn `get_cohesion_metrics` reported LCOM4 7, 15 methods, and separate method clusters for run/start/timer/finalize/capacity/options. Counterargument: the current single-class design is defensible because the hard-deadline safety model is tightly coupled and heavily documented; defer until a design note proves the extraction reduces risk rather than scattering the watchdog invariants. |

## Refs

| Path | Role |
|------|------|
| `ai_docs/planning_index.md` | Planning router and scope boundary |
| `ai_docs/workflow.md` | Branch/PR workflow and backlog-closure rule |
| `ai_docs/prompts/backlog-sweep-plan.md` | Planner prompt; enforces per-initiative Rule 1 (bundle only on shared code path) / Rule 3 (≤4 prod files) / Rule 3b (toolPolicy) / Rule 4 (≤3 test files) / Rule 5 (≤80K context). Each row here is sized for one initiative. |
| `ai_docs/prompts/backlog-sweep-execute.md` | Executor companion; consumes the `state.json` the planner emits and vets each initiative against Rules 3/4/5 before starting work. |
| `ai_docs/bootstrap-read-tool-primer.md` | Self-edit session read-only tool primer (Roslyn-MCP read-side tools to prefer over Bash/Grep). |
| `ai_docs/runtime.md` | Bootstrap scope policy — distinguishes main-checkout self-edit (no `*_apply`) from worktree/parallel-subagent sessions. |
| `ai_docs/plans/20260513T140000Z_backlog-sweep/plan.md` | Backlog sweep (2026-05-13T14:00:00Z). Shipped 1 initiative across 1 PR; closed 2 backlog rows. |
| `docs/large-solution-profiling-baseline.md` | Evidence gate for daemon/process-pool performance work. |
| `ai_docs/procedures/deep-review-backlog-intake.md` | Intake procedure for future audit batches. |
| `review-inbox/` | Staging folder for the NEXT audit batch (flat directory; `/backlog-intake` reads here). |
| `review-inbox/archive/<batch-ts>/` | Processed audit/retro/promotion batches — one subdirectory per successful intake. Delete a batch after every actionable item is either shipped, explicitly rejected as stale, or summarized in current backlog rows. |
| `ai_docs/plans/20260513T010000Z_backlog-sweep/plan.md` | Backlog sweep (20260513T010000Z). Shipped 10 initiatives across 10 PRs (#716–#720, #722–#726); closed 10 backlog rows (skill-namespace-installed-as waves 2–11). |
| `ai_docs/plans/20260516T200033Z_backlog-sweep/plan.md` | Backlog sweep (2026-05-16T20:00:33Z). Shipped 15 initiatives across 15 PRs (#779–#782, #785–#786, #788–#790, #792–#795, #797–#798) over 5 parallel waves; closed 15 backlog rows; added 3 spin-off rows (`compact-semantic-grep-pagination`, `test-suite-fixture-reuse-cohesion-and-validate-git`, `test-suite-expanded-surface-class-split`). Precursor #783 fixed pre-existing `audit-phase-runner.md` test path breakage from PR #734. |
| `ai_docs/plans/20260517T025647Z_backlog-sweep/plan.md` | Backlog sweep (2026-05-17T02:56:47Z). Shipped 3 initiatives across 3 PRs (#803, #804, #805) in one parallel wave; closed 3 backlog rows (all spin-offs from the 20260516T200033Z sweep). Init 4 (`tool-surface-pagination-or-tool-sets`) deferred per reviewer warning (track-only row whose act-on triggers have not fired). Workflow improvement shipped mid-sweep in PR #802 — `initiative-executor` PR-body contract now auto-injects `Fixes #NNN` for non-Reserved `[gh #NNN]` row references; fired correctly on Init 1's PR #803 closing gh #760 (manually closed earlier in same session because PR #780 used `Refs` not `Fixes`). Observed flake on `WorkspaceExecutionGateTests.AutoReload_ResetsTimeoutBudget_ToolActionGetsFullBudget` failed validate on PR #803 + #804 before passing on retry — needs registry entry, see spin-off note in this PR. |
| `ai_docs/plans/20260517T235058Z_backlog-sweep/plan.md` | Backlog sweep (2026-05-17T23:50:58Z). Shipped 10 initiatives across 10 PRs (#810–#819) over 3 parallel waves; closed 11 backlog rows (8 High + 3 Medium). Closed `gh #735, #736, #737, #744, #745, #757, #763, #764, #765, #766, #767`. Init 10 was a Rule 1 bundle closing gh #736 + gh #737 with a single semantic split in `ReferenceService.FindOverridesAsync` (true overrides) vs new `FindSiblingInterfaceImplementationsAsync`. Init 5 + Init 10 shipped as `Changed — BREAKING` (overallStatus="test-zero-run" replaces vacuous "clean"; `member_hierarchy.overrides` and `symbol_relationships.overrides` drop sibling-interface-implementations into a new bucket). PR #819 validate failed once with self-hosted runner communication loss; passed on rerun. Pre-existing MSTest reference resolution friction in 3 ExtractMethod fixture tests observed by Inits 1 + 6 — reproduces on baseline, unrelated to changes. |
