# Next work and backlog

<!-- purpose: Open work only; contract for agents syncing backlog on ship. -->

**updated_at:** 2026-04-15T21:00:00Z

## Agent contract

**Scope:** This file lists **unfinished work only**. It is not a changelog.

| | |
|---|---|
| **MUST** | Remove or update backlog rows when work ships; do it in the **same PR** as the implementation (or an immediate follow-up). |
| **MUST** | End implementation plans with a final todo: `backlog: sync ai_docs/backlog.md` when closing backlog-related items. |
| **MUST** | Use stable, kebab-case `id` values per open row (grep-friendly). |
| **MUST NOT** | Add "Completed" tables, dated history sections, or narrative changelogs here. |
| **MUST NOT** | Leave done items in the open table. |

## Standing rules (ongoing)

These are **not** backlog rows; do not delete them when clearing completed work.

- Reprioritize on each audit pass.
- Bug and tool items: put reproduction and session context in the PR or linked issue when it helps reviewers.
- Broader planning and complexity notes stay in this file; `ai_docs/archive/` holds policy only (see `archive/README.md`).

## Open work

Ordered by severity: **P2** (blocks major functionality on real-world repos) → **P3** (accuracy, UX, API clarity) → **P4** (docs, hygiene, cosmetic). Within a tier, rows are **alphabetical by `id`**.

| id | pri | blocker | deps | do |
|----|-----|---------|------|-----|
| `change-type-namespace-preview` | P3 | — | — | **Agent gap:** Moving a single type between namespaces in the **same project** — e.g. relocating `DevicePlatform` from `NetworkDocumentation.Collection.Ssh` to `NetworkDocumentation.Collection` to break a namespace cycle — has no single Roslyn tool. Today's options each handle a different axis: `move_type_to_file_preview` moves to a different file but keeps the namespace; `move_file_preview` moves a file on disk but doesn't rewrite the namespace declaration; `move_type_to_project_preview` changes the project too. **None of them fit "same project, different namespace"**, which is the canonical fix for an accidental namespace cycle (a surface flagged by the already-shipped `get_namespace_dependencies circularOnly=true` tool). **Session evidence (DotNet-Network-Documentation no-holds-barred refactor F1, 2026-04-14):** Namespace-cycle fix took 1 `Write` (new file at new path) + 1 `Bash rm` (delete old) + 5 `Edit` calls across production and test files, plus manual `Grep`/`Read` per consumer to distinguish files that needed an added `using` from files that already had parent-namespace ambient resolution. 12 files touched, all manually — Roslyn MCP was only used for verify (`get_namespace_dependencies` before + after confirmed the cycle was broken). **Goal:** `change_type_namespace_preview(typeName, fromNamespace, toNamespace, newFilePath?)` that (a) rewrites the type's `namespace` declaration, (b) optionally relocates the file if `newFilePath` is provided, (c) adjusts every consumer's `using` directives — add the new namespace where needed, remove the old where it was sole-purpose for this type, respect ambient resolution for consumers inside the parent namespace. Pair with `get_namespace_dependencies` for a "suggest + preview + apply + verify cycle is closed" loop. **Medium-high recurrence:** Any codebase that reorganizes subsystems periodically or has namespace-cycle hygiene in CI will hit this. **Refs:** `move_type_to_project_preview` already contains the namespace-rewrite logic; `bulk_replace_type_preview` already contains the consumer-scope scan; this tool stitches the two with the specific constraint that the project stays the same. Raised by 2026-04-15 DotNet-Network-Documentation audit session. |
| `composite-split-service-di-registration` | P3 | — | — | **Agent gap:** Splitting one DI-consumed type into several types + a forwarding facade (same public methods) plus **new `AddTransient`/`AddSingleton` lines** is today many disconnected edits. **Goal:** One preview (or documented composite) that: extracts implementation classes, generates facade delegating to them, and proposes registration deltas for the host `Program`/`ApiHostBuilder`-style file. **Now implementable on top of v1.18 `symbol_refactor_preview`:** chain a `restructure` op for the facade body + `edit` ops for the registration lines. **Refs:** v1.18 `symbol_refactor_preview`, `move_type_to_file_preview`, `preview_multi_file_edit`. |
| `concurrent-mcp-instances-no-tools` | P3 | — | — | **Bug:** When multiple Roslyn MCP instances start concurrently (deep-review prompt dispatched against several repos in parallel), the **first** instance registers its tools correctly, but **subsequent** instances report "server started" with **no tools available**. Agent calls to `workspace_load` and other tools fail because the tool list appears empty to the client. **Reproducibility:** Observed when starting deep-review prompt across multiple repos in parallel (user report 2026-04-14). **Impact:** Blocks the primary multi-repo audit workflow; parallel audits fall back to serial execution or fail outright. **Investigation:** Determine whether this is (a) stdio handle contention, (b) a plugin registration race in Claude Code MCP host, (c) a shared resource (named pipe, socket, file lock), or (d) the deferred-tool schema load failing silently. **Fix:** Depends on root cause — per-instance isolation, explicit startup sequencing, or diagnostic logging when tool registration fails so the underlying error surfaces instead of returning an empty tool list. **Refs:** `RoslynMcp.Host.Stdio.Program`; Claude Code MCP host; related `mcp-connection-session-resilience`; blocks `compilation-prewarm-on-load`. |
| `duplicate-method-body-detector` | P3 | — | — | **Capability gap (Roslyn parity with python-refactor's `find_duplicated_code`, broader than `duplicate-helper-detection-across-modules`):** Detect **any** near-duplicate method bodies by AST-normalized hash across the solution — not just "reinvented BCL/NuGet helpers" (which the P4 row targets). **Concrete session example (2026-04-15 self-retrospective of the deep-review audit):** Finding F#2 (consolidate diff helpers) required ~15 minutes of `symbol_search` for `BuildUnifiedDiff` + manual `Read` of each hit to discover four near-duplicate fake-unified-diff helpers — `ChangeSignatureService.BuildMinimalDiff`, `RestructureService.BuildMinimalUnifiedDiff`, `StringLiteralReplaceService.BuildMinimalUnifiedDiff`, and `EditService.BuildUnifiedDiff` — all bypassing the correct `DiffGenerator.GenerateUnifiedDiff` helper. These are **internal-to-internal** duplicates (not BCL substitutes), so `duplicate-helper-detection-across-modules` (which matches against referenced-package symbols) would not flag them. A general `find_duplicated_methods(workspaceId, minLines=10, similarityThreshold=0.85)` would have produced all four as a single finding. **Proposed API:** returns method groups where bodies match under AST normalization (strip trivia, rename locals, compare raw kinds), regardless of whether any group member mirrors a BCL/NuGet symbol. **Refs:** `mcp__python-refactor__find_duplicated_code` for prior art (general-purpose); related but narrower P4 row `duplicate-helper-detection-across-modules`; reuse `UnusedCodeAnalyzer` symbol-walking infrastructure. Recurrence: **high** — every architecture audit of a codebase with accumulated internal copy-paste. Raised by 2026-04-15 self-retrospective. |
| `mcp-connection-session-resilience` | P3 | — | — | **Agent blocker:** MCP clients may report **`Not connected`** for `user-roslyn` / `roslyn`, causing agents to bypass Roslyn entirely (policy violation in consumer repos). **Goal:** Faster reconnect, clearer server-side readiness, or documented **minimum host setup** (Cursor server id, stdio path, workspace_load order). **Plan:** Distinguish transport down vs workspace not loaded vs auth; optional heartbeat tool or enrich `server_info` with connection state. **Refs:** Host integration doc under `docs/` or plugin README; `server_info` implementation. |
| `compilation-prewarm-on-load` | P4 | — | `concurrent-mcp-instances-no-tools` | **Perf:** Multiple tools suffer cold-start penalties because Roslyn lazily compiles projects on first access. First `symbol_search` takes 4669ms vs 40ms warm (100x); first `go_to_definition` cross-project takes 2517ms vs 1ms warm; first `symbol_info` takes 1445ms vs 1ms warm. **Impact:** Agents hit multi-second pauses on first interaction with each project. **User preference (2026-04-14):** Cold-start penalty is acceptable; **do NOT** add aggressive pre-compilation that blocks tool registration. **Scope narrowed to:** (a) opt-in `workspace_warm(workspaceId, projects?)` tool only — never run automatically on load; (b) investigate whether observed cold starts are actually Roslyn lazy compilation or a symptom of `concurrent-mcp-instances-no-tools` — fix that first. **Refs:** Roslyn lazy `Compilation` resolution; `workspace_load` handler. |
| `coupling-metrics-tool` | P4 | — | — | **Capability gap:** Afferent/efferent/instability coupling metrics per type — companion to the existing `get_cohesion_metrics`. **Concrete session example (2026-04-15 deep-review audit, self-retrospective):** During architecture review the auditor attempted `mcp__roslyn__get_coupling_metrics` to cross-check low-cohesion findings against coupling pressure (is `ChangeSignatureService`'s LCOM4=4 reinforced by high afferent coupling, or is the class a sink that only depends on plumbing?). The tool returned `"No such tool available"`. Cohesion metrics exist (`get_cohesion_metrics`); coupling metrics do not. Without Ca / Ce / I numbers the auditor had to counter-argue the cohesion finding on intuition rather than data. **Proposed API:** `get_coupling_metrics(workspaceId, projectFilter?)` → per-type Ca (# types importing this type), Ce (# types this type imports), I = Ce / (Ca + Ce), classified against the classic Martin stability thresholds. **Fix:** Implement in `CohesionAnalysisService` or a sibling `CouplingAnalysisService`. **Medium recurrence:** every architecture audit benefits; not every ordinary session. **Refs:** `src/RoslynMcp.Roslyn/Services/CohesionAnalysisService.cs` for parallel API shape; Robert Martin stability/abstractness metrics. Raised by 2026-04-15 self-retrospective. |
| `dead-local-variable-detector` | P4 | — | — | **Capability gap:** Method-local dead-variable detection — companion to `find_unused_symbols` which operates at the symbol-kind level (types, methods, properties) and doesn't descend into method bodies. **Concrete session example (2026-04-15 deep-review audit):** The auditor eyeballed `ChangeSignatureService.PreviewAddParameterAsync` at line 75 and spotted `var paramText = ...` being assigned but never read — a real dead local. `find_unused_symbols` did not surface it. `find_errors_static` in python-refactor catches these via a semantic pass; Roslyn MCP has no equivalent. `TreatWarningsAsErrors` in this repo doesn't help because IDE0059 (unused value) isn't elevated to warning severity. **Proposed API:** `find_dead_locals(workspaceId, projectFilter?)` → returns locals whose only write is not followed by any read. Alternative: expose IDE0059 via `project_diagnostics` when the editorconfig upgrades its severity. **Low-medium recurrence:** low base rate in well-maintained code but catches real waste every audit. **Refs:** `UnusedCodeAnalyzer` infrastructure to reuse; Roslyn `DataFlowAnalysis.WrittenInside` + `ReadInside`. Raised by 2026-04-15 self-retrospective. |
| `duplicate-helper-detection-across-modules` | P4 | — | — | **Agent gap:** Teams frequently reimplement small helper extensions locally (internal `static class {Type}Extensions` with a single method) instead of importing the canonical symbol from a referenced package. These helpers are **not unused** (`find_unused_symbols` passes over them) and **not dead code** (`remove_dead_code` passes over them) — they are semantically duplicate private extensions that should be consolidated to a library canonical. **Goal:** New analyzer `find_duplicate_helpers` (or a category inside `find_unused_symbols`) that flags private/internal extension methods whose method-signature + body-shape duplicates a public symbol reachable from a referenced package (BCL or NuGet). Should recognize the specific "reinvented `Enumerable.FirstOrDefault` / `ArgumentNullException.ThrowIfNull` / `string.IsNullOrWhiteSpace`" shape — method body is a single `switch` / pattern-match / null-check on a well-known upstream type. **Concrete session evidence (2026-04-15 cross-language refactor pattern observation, SysLog-Server Rust session):** Two modules (`alerting/store.rs`, `index/saved_search.rs`) each defined an identical private `OptionalExt` trait reimplementing the third-party `rusqlite::OptionalExtension::optional`. Discovery was by a general-purpose agent happening to notice in a broad code read, then confirmed via two manual `grep` calls for the trait name and four manual reads. Had this been a C# codebase with the equivalent shape (two `static class SqlHelperExtensions` each wrapping a BCL method), no tool in the current Roslyn MCP surface would have surfaced the duplicate pair. **Efficiency win:** Current path is manual `grep` + reads + subjective judgment (doesn't scale past ~10 modules); semantic path is one tool invocation with high confidence via signature + body-AST hash equivalence. **Recurrence:** Medium — most mid-to-large C# codebases accumulate 3–10 such ad-hoc extension classes over their lifetime. **Refs:** `src/RoslynMcp.Roslyn/Services/UnusedCodeAnalyzer.cs` (extend with new heuristic category); Python MCP `find_duplicated_code` (AST-hash prior art, general-purpose); `structural_search` as potential implementation primitive for matching specific upstream-equivalent body shapes. |
| `host-parallel-mcp-reads` | P4 |  `host` | — | **Throughput:** Some MCP hosts **serialize** all tool calls; read-only tools cannot run concurrently. **Server side:** Document thread-safety of read paths. **Host side:** Allow parallel `call_mcp_tool` for read-only operations where the spec permits. **Refs:** `WorkspaceExecutionGate`, per-workspace read locks; consumer Cursor docs. |
| `record-field-add-satellite-member-sync` | P4 | — | — | **Agent gap:** Adding one field to a record/class with a satellite-sync convention (e.g. POCO + its `Clone()` / `Snapshot` / `With(...)` / serialization-DTO / builder methods, or a metrics class with + its `Increment{Name}` accessor + its `MetricsSnapshot` mirror + its `FromSnapshot` / `ToJson` serializer) requires **5–6 coordinated edits** at known sites in known files, each following an established convention already visible in the class. Missing any one breaks the pattern silently — the field is declared but doesn't flow into the snapshot, or it flows but has no increment method, or it is incremented but never exposed on the public endpoint. **Distinct from `mechanical-sweep-after-symbol-change`**, which targets rename/existing-symbol edits; this is a template-driven *addition* pass that synthesizes new satellite members from the declaration by inspecting the pattern of existing fields. **Goal:** A `record_field_add_with_satellites_preview(workspaceId, typeMetadataName, newFieldSignature)` tool that: (1) inspects the target type and finds sibling fields with identical satellite coverage (the "pattern"), (2) proposes edits to every mirror type (`Snapshot`, `Dto`, `Builder`, etc.) plus methods (`Clone`, `Increment{Name}`, `With{Name}`, `ToJson` case statements), (3) returns a preview token. **Concrete session evidence (2026-04-15 SysLog-Server Rust refactor, b-155 + b-156):** Adding `checkpoint_save_failures: AtomicU64` to two separate counter structs (`ParseCounters`, `ForwardCounters`) required manual synchronization across: (a) struct field, (b) `Default::default()` body, (c) `record_checkpoint_save_failure(&self)` method, (d) `ParseMetricsSnapshot` / `ForwardMetricsSnapshot` struct field, (e) `snapshot()` impl line, (f) `handlers_metrics.rs` Prometheus exposure. Six places per field × two counter structs = 12 coordinated edits, each mechanical once the pattern is identified. **Efficiency win:** Manual path is ~12 `Edit` calls with the agent holding the full pattern in working memory; semantic path is one preview + one apply, eliminating the class of "forgot to add it to the snapshot" bug. **Recurrence:** High — every codebase with instrumented metrics, serialization mirrors, or `record with` patterns grows new fields frequently. **Refs:** compare with existing `mechanical-sweep-after-symbol-change` (rename-oriented); `change-signature-add-parameter-cross-callsite` (different shape — adds to *methods*, not fields); v1.18 `symbol_refactor_preview` as implementation primitive. |
| `replace-invocation-pattern-refactor` | P4 | — | — | **Capability gap / UX:** Ergonomic "replace all calls of method X(...) with method Y(...)" refactor — cleaner than today's options (`restructure_preview` with placeholder patterns, `bulk_replace_type` which is type-level not method-level, or per-site `Edit`). **Concrete session example (2026-04-15 deep-review audit, Finding F#2):** Finding F#2 required replacing four different callsites of `BuildMinimalDiff(a, b, c)` / `BuildMinimalUnifiedDiff(a, b, c)` / `BuildUnifiedDiff(a, b, c)` with `DiffGenerator.GenerateUnifiedDiff(b, c, a)` — four locations across four services, with argument reordering. `restructure_preview` theoretically supports this via `__a__` placeholder patterns, but the placeholder grammar constrains pattern and goal to the same syntactic kind and handles arg reordering awkwardly. The auditor fell back to per-site manual `Edit`. **Proposed API:** `replace_invocation_preview(workspaceId, oldMethod="FQ.Name(param1, param2, param3)", newMethod="FQ.OtherName(param2, param3, param1)", scope?)` → rewrites every callsite with the declared argument mapping, emits a preview. **Medium recurrence:** every "new library / helper adopted; migrate callers" scenario. **Refs:** `src/RoslynMcp.Roslyn/Services/BulkRefactoringService.cs` for parallel shape (type-level); `RestructureService` for AST-rewriter reuse. Raised by 2026-04-15 self-retrospective. |
| `roslyn-fetch-resource-timing` | P4 | — | — | **`fetch_mcp_resource`** intermittently returns "server not ready" for `roslyn://` workspace URIs in Cursor — determine whether host timing, server readiness gate, or documentation gap. **Refs:** Resource handlers; readiness gate vs `workspace_load` completion. |
| `self-edit-bootstrap-mode-mcp-development` | P4 | — | — | **Workflow gap:** When the codebase being edited **is** the Roslyn MCP server itself, `workspace_load` followed by `*_preview` / `*_apply` is impractical: every `Edit` invalidates the in-memory `MSBuildWorkspace` snapshot, and the binary under edit may be the same one servicing the call. **Partially mitigated by:** v1.17 `ROSLYNMCP_ON_STALE=auto-reload` — stale gate reloads transparently, but binary-identity issue remains. **Goal options:** (a) Document an explicit "self-edit workflow" recipe (build artifact at known path → use a separate roslynmcp instance pinned to that build → apply via the second instance); (b) Document the binary-identity limitation prominently in `Roslyn MCP client policy (AI sessions)`. **Refs:** `runtime.md` Roslyn MCP client policy section. |
| `workspace-process-pool-or-daemon` | P4 | — | `compilation-prewarm-on-load` | **Perf:** Each new `roslynmcp` subprocess starts with zero workspaces and pays the full ~10s `workspace_load` cost on first call. For multi-agent orchestrators that spawn one process per sub-agent, the cumulative reload cost dominates wall-clock time. **Measured impact:** Audit session 2026-04-13 spawned 9 agents (~90s reload overhead); stress test session 2026-04-14 spawned 4 agents (~40s overhead). For a typical Claude Code session the cumulative reload tax is 1–3 minutes — comparable to actual analysis work. **Goal options:** (a) Long-running daemon mode (`roslynmcp --daemon --socket /tmp/roslyn.sock`) where short-lived clients connect over a unix/named-pipe socket and share the loaded workspace; (b) Process pool with workspace state hand-off via shared memory or temp files. **Distinction from `compilation-prewarm-on-load`:** That item is about prewarming compilations within one process; this item is about sharing the loaded workspace state across multiple processes/clients. **Refs:** `WorkspaceManager`, stdio host bootstrap. |
| `change-signature-preview-callsite-summary` | P3 | — | — | **Agent-UX bug observed in firewall-analyzer session 2026-04-15:** Agent invoked `change_signature_preview op=remove` on `IPanosClient.GetRegisteredIpsAsync(string serial, string vsys, CT)` and got a `previewToken` back, but the preview's `changes[].unifiedDiff` contained **only the interface-owner file** (`IPanosClient.cs`). The 4 callsite files that Roslyn's `ChangeSignature` pass actually rewrites on apply (`PanosClient.Telemetry.cs`, `CollectionService.cs`, `CollectionServiceTests.cs`, `PanosClientTests.cs`) were invisible in the preview. Agent abandoned the preview and did 5 manual `Edit` calls instead, because without the callsite diff there was no way to verify the tool would do the right thing or scope the review. **Fix:** `change_signature_preview` response must enumerate all affected files and include their diffs — same shape as `rename_preview` already does for reference-aware rename. Minimum viable: a `callsiteUpdates` summary (`{filePath, callsiteCount}[]`) even if full diffs are elided for payload size. **High recurrence:** Every non-trivial signature change across an internal interface will trigger this. **Refs:** `src/RoslynMcp.Roslyn/Services/ChangeSignatureService.cs` PreviewRemove/PreviewAdd/PreviewRename paths; compare `RenameService.PreviewAsync` response shape. |
| `scaffold-test-sibling-pattern-inference` | P3 | — | — | **Agent gap observed in firewall-analyzer session 2026-04-15:** Agent needed to add `SecurityHeadersMiddlewareTests.cs` as an ASP.NET Core integration test with `IClassFixture<CustomWebApplicationFactory>`. `scaffold_test_preview` is available and would generate an xUnit skeleton, but the tool generates a generic class stub — it does not detect and replicate the sibling-test pattern (fixture class, `[Trait("Category", "Integration")]` attribute, `_client` field, constructor-injected factory) that `HealthEndpointTests.cs` / `DeepHealthEndpointTests.cs` / `ApiKeyMiddlewareTests.cs` all share. Agent wrote the test file with `Write` instead, copying the sibling shape manually. **Fix:** `scaffold_test_preview` takes an optional `referenceTestFile` parameter (or auto-detects the most common shape in the target project) and replicates the surrounding scaffolding. A dumber MVP: surface project-level xUnit fixture types in the generated stub's `using`s and inject them as `IClassFixture<T>` when present. **Medium recurrence** — every ASP.NET Core repo has this pattern; every first-test-in-area task hits it. **Refs:** `src/RoslynMcp.Roslyn/Services/ScaffoldingService.cs` test generation; related `scaffold-implementation-from-interface` for broader pattern-inference gap. |
| `change-signature-parameter-span-hint-for-remove` | P4 | — | — | **Agent-UX friction (firewall-analyzer 2026-04-15):** `change_signature_preview op=remove` requires `line`/`column` pointing at the method declaration *and* either a `name` or `position` to identify the parameter to drop. For a caret at the method name, the tool correctly finds the method but there's no hint path from "I want to drop parameter X" → the right `name` / `position` combo when the user knows the parameter name. Agents end up either guessing `position` (0-based, error-prone) or typing the parameter name verbatim. **Fix option:** Accept `parameterLine`/`parameterColumn` as an alternative locator — caret-on-parameter dispatches directly to the remove handler. This mirrors how `rename_preview` accepts any symbol token. **Low-medium recurrence.** **Refs:** `ChangeSignatureService.RemoveParameterAsync` parameter resolution. |
| `compile-check-stale-assembly-refs-post-reload` | P3 | — | — | **Bug:** After `workspace_reload`, `compile_check` returned **193 spurious CS1705** "assembly uses higher version than referenced assembly" errors during 2026-04-15 IT-Chat-Bot audit — the in-memory `Compilation` held stale `MetadataReference` handles from before the reload while the source code was fine (real `dotnet build` surfaced only 2 pre-existing errors unrelated to reload). Agent had to fall back to shell `dotnet build` for accurate post-edit validation, bypassing the fast in-memory path. **Impact:** Breaks the advertised fast-feedback loop — `compile_check` is the primary tool for post-edit validation, but its reliability drops to zero after any `workspace_reload`. Agents lose trust in the tool and default to shell `dotnet build` thereafter. **Fix:** Force `MetadataReference` re-resolution after reload before the next `compile_check` call, OR prominently document "after `workspace_reload`, prefer a fresh `workspace_load` rather than trusting `compile_check` on the reloaded snapshot". **Refs:** `WorkspaceManager.ReloadAsync`, `CompileCheckService`; v1.17 `ROSLYNMCP_ON_STALE=auto-reload`. Raised by 2026-04-15 IT-Chat-Bot audit session. |
| `di-lifetime-mismatch-detection` | P4 | — | — | **Capability gap:** `get_di_registrations` collects `AddSingleton`/`AddScoped`/`AddTransient` call sites but does not analyze **override chains or lifetime mismatches**. During 2026-04-15 IT-Chat-Bot audit, three interfaces (`ILearnedFactRepository`, `IConversationSummaryRepository`, `IUserPreferencesRepository`) were registered as **Singleton→InMemory*** in `AddConversationServices` and then **immediately re-registered** as **Scoped→Ef*** in `AddChatBotDbContext` (called right after in the composition root). This creates (a) 3 dead-code Singleton registrations that always lose to the later Scoped registration, (b) a latent captive-dependency trap if any Singleton service ever takes one of these interfaces, (c) a fragile ordering dependency on composition-root call order. Agent identified all three via `Grep` across both files, not tool-assisted. **Goal:** Extend `get_di_registrations` output (or add `analyze_di_lifetime` companion) to emit per-interface sections listing: (1) all registrations in order, (2) the "winning" registration, (3) whether lifetimes differ across overrides (Singleton→Scoped, etc.), (4) whether any overridden registration is dead in the active composition. **Frequency:** High — DI override anti-patterns are common in layered .NET codebases where domain, infrastructure, and host each contribute their own `ServiceCollectionExtensions`. Would have been a one-tool-call find instead of a 3-file grep + manual reasoning. **Refs:** `GetDiRegistrationsService`; Microsoft.Extensions.DependencyInjection override semantics (last-registration-wins for `GetService<T>`). Raised by 2026-04-15 IT-Chat-Bot audit session. |
| `find-references-metadataname-parameter-rejected` | P3 | — | — | **Bug:** `find_references` schema advertises **three** resolution options — `filePath+line+column`, `symbolHandle`, **or `metadataName`** — but the runtime resolver rejects `metadataName`-only calls. During 2026-04-15 IT-Chat-Bot audit, calling `find_references(workspaceId, metadataName='ITChatBot.Conversation.InMemoryConversationRepository')` returned `"Invalid argument: Provide either filePath with line and column, or symbolHandle."` and elapsed in 1 ms (rejected at argument validation before resolution). **Impact:** Forces a `Grep` fallback whenever the agent has a fully-qualified type name but no cursor position — a common case when (a) reviewing DI registrations that name types by string, (b) tracing symbols discovered via `get_symbol_outline`, (c) pivoting from `find_unused_symbols` output. The working alternative (`find_references_bulk`) **does** accept `metadataName`, creating a capability split that isn't documented. Same bug reproduced in DotNet-Network-Documentation session (`VersionParseState.Serial`) — **5th observed reproduction** across audit sessions. **Fix:** Either (a) honor `metadataName` in `FindReferencesService.ResolveSymbolAsync` by calling `Compilation.GetTypeByMetadataName` / `GetSymbolsWithName` before the error path — matching the behavior already in `find_references_bulk`; or (b) if intentionally unsupported, remove `metadataName` from the `find_references` schema and document the discrepancy in `tool-usage-guide.md`. **Refs:** `src/RoslynMcp.Roslyn/Services/FindReferencesService.cs`; `find_references` schema parameter `metadataName`; `find_references_bulk` (already supports it). Raised by 2026-04-15 IT-Chat-Bot audit session. |
| `lcom4-lifecycle-pattern-false-positive` | P4 | — | — | **Analysis UX gap:** `get_cohesion_metrics` repeatedly scores **lifecycle-pattern types** (command/action/handler with `Describe`/`Validate`/`Execute` methods) as LCOM4≥3 and recommends "split into cohesive types" — but these methods are **inherently orthogonal on field access by design** because they're distinct lifecycle phases, not operations on shared state. Concrete 2026-04-15 IT-Chat-Bot examples: `RequestApprovalAction` (LCOM4=4, 4 methods — all false positive), `EscalateToAgentAction`, `CheckTicketStatusAction`, `CreateTicketAction` (all LCOM4=3, all false positive). Same false-positive shape appeared in 2026-04-13 audit (switch-on-type renderer dispatchers, closed as `queue-refactor-cc-renderers`). **Impact:** Every audit session that runs `get_cohesion_metrics` spends effort manually triaging these false positives and reaches the same counterargument-won conclusion; noise crowds out genuine cohesion issues. **Fix options:** (a) Teach `GetCohesionMetricsService` to detect known lifecycle-pattern shapes (interface name ending `Action`/`Handler`/`Command`/`Stage` with `Describe`+`Validate*`+`Execute*` method triad) and suppress or downgrade LCOM4 output — analogous to `excludeConventionInvoked` in `UnusedCodeAnalyzer`; (b) emit a new field in the output, e.g. `lifecyclePattern: "action-triad"` when detected, so consumers can filter; (c) accept a `[CohesionExempt]` attribute annotation on types that are intentionally lifecycle-shaped. **Frequency:** High — every C# codebase using command/action/handler or pipeline-stage patterns hits this in every audit. **Refs:** `GetCohesionMetricsService`; IT-Chat-Bot `ai_docs/references/roslyn_cohesion_policy.md` (consumer-side interpretation guide); 2026-04-15 IT-Chat-Bot audit output (`RequestApprovalAction`, action types); 2026-04-13 `queue-refactor-cc-renderers` for prior art. |
| `pragma-directive-scope-manipulation` | P4 | — | — | **Capability gap:** `add_pragma_suppression` inserts a `#pragma warning disable` before a given line but there is no corresponding tool for **widening, moving, or balancing pragma scope** — fixing an incorrectly-scoped suppression requires plain text edit. Observed 2026-04-15 in IT-Chat-Bot `src/retrieval/Execution/RetrievalExecutor.cs:67-69`: `#pragma warning disable CA2025` + matching `restore` wrapped only line 68 (`using var concurrency = new SemaphoreSlim(...)`), but CA2025 actually fires at line 78 (where `concurrency` is captured into the task delegate). Result: the pragma pair was cosmetic — baseline build had 3 warnings because the `restore` was misplaced. Agent fixed it by moving the `restore` line past `Task.WhenAll` via `Edit`, with no semantic verification that the new scope actually covered the fire site. **Goal:** Add either (a) `pragma_scope_widen(filePath, line, diagnosticId)` — detect the matching `disable`/`restore` pair and extend `restore` past a specified statement, or (b) `verify_pragma_suppresses(filePath, line, diagnosticId)` — check whether an existing pragma actually covers the diagnostic's fire location and optionally auto-widen to cover it. **Frequency:** Medium — pragma scope errors accumulate silently because they don't fail compilation; only `format_check` / baseline diagnostics reveal them. Authors routinely write pragmas assuming the fire site is at the declaration when in practice it's at the use site. **Refs:** `src/RoslynMcp.Roslyn/Services/PragmaSuppressionService.cs` (add a companion service or extend existing); 2026-04-15 IT-Chat-Bot `RetrievalExecutor.cs` baseline CA2025 warning at line 78 vs pragma at lines 67-69. |
| `default-to-compile-check-over-dotnet-build` | P4 | — | — | **Discoverability / documentation gap (firewall-analyzer 2026-04-15):** Even MCP-aware agents instinctively reach for `Bash: dotnet build` / `dotnet test` / `dotnet format --verify-no-changes` as baseline and post-edit verification, rather than `compile_check` / `test_run` / `format_check`. In that session: 3× `dotnet build`, 2× `dotnet test`, 3× `dotnet format` via `Bash` vs zero MCP-equivalent calls. Root causes: (a) the MCP equivalents are per-workspace and require `workspaceId` which agents may not have at baseline-check time (before `workspace_load`); (b) agents distrust whether `compile_check`'s in-memory Roslyn diagnostics match what CI's `dotnet build` will say; (c) `test_run` isn't promoted in `domains/tool-usage-guide.md` as the default for post-edit verification. **Fix:** Add a "Verification workflow" section to `domains/tool-usage-guide.md` explicitly recommending the MCP triple (`compile_check` → `test_run --filter related` → `format_check`) for post-edit verification, with the shell equivalents listed only as CI-parity fallbacks. Also: document that `compile_check` and `dotnet build` produce identical diagnostic sets modulo analyzer packages. **High recurrence** — every code-modification session reaches for `Bash` by default. **Refs:** `domains/tool-usage-guide.md`; `CompileCheckService`, `TestRunService`, `FormatService` capability docs. |
| `post-edit-validate-workspace-scoped-to-touched-files` | P4 | — | — | **Agent UX gap observed in firewall-analyzer 2026-04-15 session:** After 5 semantic edits the agent ran full-solution `dotnet build` + `dotnet test --filter "Category!=E2E"` (~2s + ~5s). `validate_workspace` exists and bundles these, but the agent didn't use it because (a) the tool's default scope isn't clearly "only touched files" in the documentation, and (b) `changedFilePaths=null` behavior ("use the change tracker's session-wide change set") is session-scoped to the MCP server's own edits — it doesn't include external Claude Code `Edit`-tool edits. **Fix:** `validate_workspace` should accept `changedFilePaths` as primary input (already does) and the docs/tool-description should promote the external-edit use case. Optionally: a companion tool `validate_recent_git_changes(workspaceId)` that derives `changedFilePaths` from `git status --porcelain` — removes the need for the agent to enumerate paths manually. **Medium recurrence** — every multi-file refactor ends with a verify pass. **Refs:** `ValidateWorkspaceService`; `ChangeTrackerService` scope. |
| `semantic-edit-with-compile-check-wrapper` | P4 | — | — | **Workflow gap observed across multiple sessions (firewall-analyzer 2026-04-15 + self-development sessions):** Agents often make small text-level edits (`Edit` / `apply_text_edit`) that are semantically valid and don't warrant a full preview/apply cycle — adding a `ValidateXPathSegment(serial);` line inside an existing method, flipping a magic number, renaming a local variable inside one method, etc. After the edit, the agent runs `dotnet build` via shell (2-6s) to verify. **Gap:** There is no lightweight "edit + compile-check this compilation only" path. `apply_with_verify` requires a preview token from a semantic refactor tool; `apply_text_edit` has no verify mode; `compile_check` is a separate call. **Fix options:** (a) new `apply_text_edit_with_verify` that wraps `apply_text_edit` + `compile_check` scoped to the owning project + auto-revert on new errors; (b) add `verify: true` flag to existing `apply_text_edit` / `apply_multi_file_edit`. **High recurrence** — this is the dominant edit shape for non-refactor changes (single-line additions, test edits, literal changes). Would remove 2-5 seconds per edit across a typical session. **Refs:** `apply_text_edit` / `apply_with_verify` wiring; related `roslyn-mcp-post-edit-validation-bundle`. |
| `workspace-stale-after-external-edit-feedback` | P4 | — | — | **Observed in firewall-analyzer 2026-04-15 session:** After the agent applied `Edit` changes through Claude Code's tool (outside the MCP server's change tracker), subsequent `change_signature_preview` against freshly-edited files could have returned stale results unless the agent had called `workspace_reload`. The agent did call `workspace_reload` explicitly once between the format-sweep and the signature change, but there is no automatic `isStale` signal surfaced when a Claude Code `Edit` happens outside MCP's apply-channel. **Fix options:** (a) `workspace_status verbose=false` already returns `isStale`, but it's not called between tool invocations — teach the tool-usage guide to check `isStale` before any semantic-refactor preview; (b) FileSystemWatcher-based `isStale` auto-detection on the underlying `.cs` files, so `change_signature_preview` can refuse with `"Workspace stale — external edit detected, reload first"` rather than silently using cached syntax trees. **Low-medium recurrence** — any mixed-tool session (Edit + MCP refactor) can trigger this. Relates to `self-edit-bootstrap-mode-mcp-development` for the MCP-edits-itself variant. **Refs:** `WorkspaceManager.IsStale`. |

## Refs

| Path | Role |
|------|------|
| `ai_docs/workflow.md` | Branch/PR workflow; **Backlog closure** |
| `ai_docs/backlog.md` | This file (open work + contract) |
| `ai_docs/audit-reports/` | Raw deep-review audits (inputs for prioritization) |
| `ai_docs/audit-reports/20260413T120000Z_jellyfin_mcp-server-audit.md` | Full-surface audit (Jellyfin, 40 projects): §14 bugs (7 issues), §7 schema drift (11 entries), §6 perf baseline, §12 promotion scorecard, §16 prompt verification, §17 boundary testing |
| `ai_docs/audit-reports/20260414T120000Z_jellyfin_stress-test.md` | Performance stress test (Jellyfin, 40 projects): 95 tool calls, timing baselines per category, throughput/rapid-fire stats, recovery/edge cases, scalability assessment |
| `ai_docs/audit-reports/20260413T154959Z_firewallanalyzer_mcp-server-audit.md` | Full-surface audit: §6 perf, §7/§14 error paths, §10 prompts blocked, §16–17 concurrency, ledger/resource/tool evidence |
| `ai_docs/audit-reports/20260413T175254Z_firewallanalyzer_experimental-promotion.md` | Experimental tool promotion + apply/diff notes (cross-ref with §9 in roslyn-backed-mcp promotion doc) |
| `ai_docs/procedures/deep-review-backlog-intake.md` | Multi-repo audit import / backlog merge (`eng/new-deep-review-batch.ps1`) |
| `eng/import-deep-review-audit.ps1` | Ingest external audit markdown into this repo (if present at repo root) |
| `src/RoslynMcp.Host.Stdio/Tools/WorkspaceTools.cs` | Workspace lifecycle (`workspace_load`, `workspace_reload`, `workspace_close`) — cited in firewall audit Phase 16 concurrency prose |
| Consumer: `.github/copilot-instructions.md` (DotNet-Firewall-Analyzer) | **Roslyn MCP required when available** — documents agent policy when MCP disconnect forces hand edits |
| Qualitative: DotNet-Firewall-Analyzer backlog remediation (2026-04, orchestrator session) | **Roslyn MCP not used** (`call_mcp_tool` / Roslyn server): work used Read/StrReplace, explore `Task`, `dotnet build`/`test`/`format`. **Highest-efficiency Roslyn tools for that work:** `find_references` / `rename_preview` for shared helpers (`RulebasePanOs`, `HaStatePanOs`), `split_class` / `move_type_to_file` + multi-file apply for importer split, `project_diagnostics` scoped to touched projects post-edit. **New backlog rows:** `composite-split-service-di-registration`, `mapper-snapshot-dto-symmetry-check`, `test-mock-drift-new-interface-usage`. **Existing rows still primary:** `agent-symbol-refactor-unified-preview`, `mechanical-sweep-after-symbol-change`, `mcp-connection-session-resilience`, `roslyn-mcp-guided-refactor-flow`, `roslyn-mcp-multi-file-preview-apply`. |
| Qualitative: DotNet-Firewall-Analyzer backlog remediation session 2 (2026-04, session `a721fbeb`) | **Zero Roslyn MCP calls** across 5 C# refactoring tasks (25 `StrReplace`, 7 `Write`, 16 shell build/test/format invocations). **Task-by-tool mapping (missed opportunities):** (1) `sec-apikey-header-only` — low miss, single-line edit; `compile_check` would have been faster than `dotnet build`. (2) `test-validators` — medium miss; `scaffold_test_preview` should have generated stubs for 5 test classes. (3) `dead-code-cleanup` — **high miss**; `find_unused_symbols` → `find_implementations` → `remove_dead_code_preview/apply` was the exact tool chain for removing `GetSnapshotSummaryAsync`/`WriteRawXmlAsync` from `ISnapshotStore` + `FileSnapshotStore`. (4) `object-type-constants` — **high miss** for `move_type_to_project_preview` (moving `SnapshotObjectKind` from Application → Domain); **tool gap** for string-literal-to-constant replacement (~49 literals across 8 files). (5) `test-api-endpoints` — medium miss; `scaffold_test_preview` for endpoint test stubs. **Cross-cutting:** `compile_check` never called (agents used `dotnet build` via shell); `format_document_preview` never called (agents used `dotnet format` via shell); `workspace_load` never called (no `workspaceId` available). **New backlog rows:** `dead-interface-member-removal-guided`, `string-literal-to-constant-extraction`, `format-verify-solution-wide`, `scaffold-test-batch-mode`. **Existing rows reinforced:** `mcp-connection-session-resilience`, `roslyn-mcp-guided-refactor-flow`, `roslyn-mcp-post-edit-validation-bundle`, `roslyn-mcp-workspace-staleness`, `move-type-to-project-namespace`. |
| Qualitative: Jellyfin audit + stress test session 3 (2026-04-13/14, Claude Code 13-agent dispatch) | **Roslyn MCP usage profile:** Audit prompt designed to exercise every tool; agents used Roslyn MCP for ~95% of work via spawned `roslynmcp` subprocesses (no direct deferred-tool registration available to orchestrator). **Refactoring tasks performed (Phase 6 only):** `organize_usings_apply`, `set_diagnostic_severity`, `add_pragma_suppression`, `apply_text_edit`, `apply_multi_file_edit`, `extract_method_apply` — all via MCP. **One forced manual fallback:** `extract_method_apply` of `ApplyMinWidthFilter` produced CS0136+CS0841 (`var` redeclaration); agent had to make a manual `Edit` to remove `var` (validates `extract-method-apply-var-redeclaration` P2 bug). **Other manual fallbacks (none code-modification):** orchestrator wrote audit/stress test reports via `Write`/`Edit`; ran CLI `dotnet build` + `dotnet test` for baseline comparison (would have been MCP-routed if `compile_check`/`test_run` had been directly invokable from the orchestrator instead of via sub-agents). **Process-level observations:** 13 cumulative agents × ~10s reload = ~130s overhead; agents independently rediscovered NDJSON protocol details ~9 times; `find_unused_symbols`+`type_hierarchy`+`find_implementations`+`member_hierarchy`+`impact_analysis`+`suggest_refactorings` all blocked by `UnresolvedAnalyzerReference` (already P2). **New backlog rows:** `apply-with-verify-and-rollback`, `get-source-text-line-range-ignored`, `mcp-stdio-protocol-onboarding-docs`, `roslyn-mcp-claude-direct-tool-registration`, `long-running-tool-progress-notifications`, `preview-token-cross-process-handoff`, `workspace-process-pool-or-daemon`. **Existing rows reinforced:** `extract-method-apply-var-redeclaration`, `unresolved-analyzer-reference-crash`, `compilation-prewarm-on-load`, `code-fix-providers-missing-ca`, `project-diagnostics-large-solution-perf`. |
| Qualitative: Roslyn MCP self-development sessions — v1.15.0 + v1.16.0 ship pipelines (2026-04-14) | **Zero Roslyn MCP calls across implementation work** for three back-to-back top-10 remediation passes (PRs #150, #151 v1.15.0, #153 v1.16.0). ~30 distinct C# source edits across ~20 files (Core/Roslyn/Host.Stdio/Tests projects), zero `mcp__roslyn__*` invocations until the post-reinstall health check at the very end. **Root cause:** Self-edit bootstrap problem — the binary doing the analysis is the binary being changed, so `workspace_load` + `*_preview/*_apply` cycles are impractical (snapshot stale after every Edit, binary mid-rebuild). All work used `Edit`/`Write` + `Bash`-driven `dotnet build`/`dotnet test`. **Highest-confidence missed opportunities (would have helped even with bootstrap constraint):** (a) `find_references` before adding `filePaths?` parameter to `ICodeMetricsService.AnalyzeAsync` (4 callsites hand-grepped, 2 of them broke at compile from positional shift); (b) `compile_check` after each Edit instead of full `dotnet build` cycle (3-30s vs ~30s); (c) `format_check` after edits (the very tool shipped in v1.16.0 — never used during its own development); (d) `apply_with_verify` for the symbol-resolver member-fallback extension. **New backlog rows:** `change-signature-add-parameter-cross-callsite`, `mcp-tool-catalog-attribute-binding`, `roslyn-structural-search-and-replace`, `scaffold-implementation-from-interface`, `self-edit-bootstrap-mode-mcp-development`. **Existing rows reinforced:** `roslyn-mcp-guided-refactor-flow`, `roslyn-mcp-multi-file-preview-apply`, `roslyn-mcp-post-edit-validation-bundle`, `roslyn-mcp-workspace-staleness`. **Pattern-level observation:** Even a Roslyn-MCP-friendly orchestrator falls back to plain Edit when the `workspaceId` doesn't exist (no `workspace_load` ever called) and the cost-of-load exceeds the cost-of-edit for one-off changes — argues for either (a) a `workspace_load_lazy` mode that auto-loads on first tool call or (b) a documented "in-bootstrap-mode use Edit + compile_check" recipe. |
| Qualitative: IT-Chat-Bot no-holds-barred refactor audit (2026-04-15, senior-engineer session, 5-finding cap) | **Roslyn MCP usage profile: strong read-side, zero write-side.** **Analysis phase (~12 tool calls):** `workspace_load`, `workspace_health`, `project_graph`, `project_diagnostics` (summary + warning filter), `get_complexity_metrics`, `get_cohesion_metrics` (excludeTestProjects, minMethods=5), `find_unused_symbols`, `suggest_refactorings`, `find_reflection_usages`, `security_diagnostics`, `nuget_vulnerability_scan`, `compile_check` (post-edit, **failed with 193 spurious CS1705 — see below**), `workspace_reload`. All analysis drove finding selection cleanly. **Implementation phase (4 of 5 findings applied, F05 deferred):** **zero `*_preview`/`*_apply` calls** — same economics-of-Edit pattern observed in DotNet-Network-Documentation session. **Task-by-tool mapping (missed opportunities):** (1) **F01 (OCE catch filter × 3 sites, 2 files)** — *high miss*: textbook case for `restructure_preview` (identical C# pattern `catch (OperationCanceledException) when (X)` → `when (Y)`) across 3 sites, OR `preview_multi_file_edit` + `apply_with_verify`. Agent used 3 separate `Edit` calls + manual Bash `dotnet build/test`. Reinforces `roslyn-structural-search-and-replace`, `roslyn-mcp-multi-file-preview-apply`, `apply-text-edit-with-verify-bundle`. (2) **F02 (remove dead `packedEvidence` parameter + 2 callsites)** — *high miss*: **`change_signature_preview` with `op='remove'` is the literal canonical tool for this task**. Agent used 3 `Edit` calls. Reinforces `roslyn-mcp-guided-refactor-flow` (tool discoverability). (3) **F03 (widen `#pragma warning disable CA2025` scope)** — *gap*: no semantic pragma-scope tool exists; `Edit` is currently the only option. **Raised new row: `pragma-directive-scope-manipulation`.** (4) **F04 (remove 3 dead DI registrations)** — *high miss*: `get_di_registrations` was never called — agent identified the Singleton→Scoped override pattern via 3× `Grep` across two `ServiceCollectionExtensions.cs` files. **Raised new row: `di-lifetime-mismatch-detection`.** **Cross-cutting bugs observed:** (a) `find_references(metadataName='ITChatBot.Conversation.InMemoryConversationRepository')` returned `InvalidArgument: Provide either filePath with line and column, or symbolHandle.` — **5th reproduction** of `find-references-metadataname-parameter-rejected`; Grep fallback was forced. (b) **`compile_check` after `workspace_reload` returned 193 spurious CS1705 stale-assembly-reference errors** (EntityFrameworkCore 10.0.5 vs 10.0.6 version drift surfaced as version-mismatch against in-memory stale `MetadataReference` handles); real `dotnet build` produced only 2 pre-existing errors. **Raised new row: `compile-check-stale-assembly-refs-post-reload`.** (c) `get_cohesion_metrics` flagged `RequestApprovalAction` LCOM4=4 + three other `IAction` implementors LCOM4=3 as "split into cohesive types" — every hit was a lifecycle-pattern false positive (Describe/Validate/Execute are orthogonal on fields by design). **Raised new row: `lcom4-lifecycle-pattern-false-positive`.** **Validation:** Bypassed `validate_workspace` bundle — ran 15 separate Bash `dotnet build`/`dotnet test` invocations across 9 test projects. 744/746 tests passed, 2 failures pre-existing on `main` (confirmed via stash test). **New backlog rows:** `compile-check-stale-assembly-refs-post-reload`, `di-lifetime-mismatch-detection`, `lcom4-lifecycle-pattern-false-positive`, `pragma-directive-scope-manipulation`. **Existing rows reinforced:** `find-references-metadataname-parameter-rejected` (5th reproduction — observed in every MCP-aware audit session), `roslyn-mcp-guided-refactor-flow` (canonical `change_signature_preview` tool missed for literal canonical use case), `roslyn-mcp-multi-file-preview-apply`, `roslyn-mcp-post-edit-validation-bundle`, `roslyn-structural-search-and-replace`, `apply-text-edit-with-verify-bundle`. **Pattern-level observation:** Agent's instinct on a clean Release build (0 errors, 3 warnings) was "just Edit these 4 small things" — tool friction of each MCP preview (~3-5s round-trip vs <100ms for `Edit`) outweighs semantic-correctness benefit when changes are pinpoint. But the downside is cumulative: F03's pragma-scope bug was the **same kind of mistake** a prior session made (original pragma placed before use site); the loss of atomic preview+auto-verify means these subtle errors persist across sessions. This is the strongest argument yet for `apply-text-edit-with-verify-bundle` — make `Edit`'s verify gap zero-cost, then agents keep the speed AND get the safety. |
| Qualitative: DotNet-Network-Documentation no-holds-barred refactor (2026-04-14/15, senior-engineer audit session) | **Roslyn MCP usage split sharply by phase.** **Analysis phase (~20 tool calls):** intensive, idiomatic use — `server_info`, `workspace_load`, `project_graph`, `compile_check`, `project_diagnostics` (summary before + after), `get_complexity_metrics`, `get_cohesion_metrics`, `find_unused_symbols`, `nuget_vulnerability_scan`, `security_diagnostics`, `get_namespace_dependencies` (`circularOnly`, before + after F1), `find_reflection_usages`, `get_di_registrations`, `semantic_search`, `find_type_usages` (`metadataName=DevicePlatform`), `workspace_reload`. All ran without friction and drove the finding-selection pass. **Implementation phase (5 findings, 19+ edits):** **zero `*_preview`/`*_apply` calls**. Every finding used plain `Edit` -> `workspace_reload` -> `compile_check` as the verify bundle. Raw `Edit` was faster than preview-token round-trips for pinpoint changes (one-liners to ~20-line changes). **Task-by-tool mapping (missed opportunities):** (1) **F1 (namespace cycle fix)** — *high miss*: no single tool fits "same project, different namespace." Took `Write` + `Bash rm` + 5 `Edit` + manual `Grep`/`Read` per consumer (12 files touched). `move_type_to_file_preview` keeps the namespace; `move_file_preview` keeps the namespace declaration; `move_type_to_project_preview` changes the project. **Raised new row: `change-type-namespace-preview`.** (2) **F4 (field retyping `object? -> string?`)** — *medium miss*: `find_references` called once with `metadataName` only -> `InvalidArgument`, fell back to `Grep`. Existing row `find-references-metadataname-parameter-rejected` covers this exact bug; reproduction validated here (6th reproduction across audit sessions). `find_property_writes` on `VersionParseState.Serial` was the ideal tool but requires cursor position (same API inconsistency). (3) **F3 (signature refactor with typed record)** — *medium miss*: `change_signature_preview` only supports add/remove/rename of individual parameters, not "replace 10 parameters with 1 record parameter" — had to use `Edit` + manual record definition. (4) **F2 (eager schema init, remove lock + 3 call-site edits)** — *low miss*: `remove_dead_code_preview` could have handled the field/method removal if pointed at `EnsureSchema` + `_schemaLock` + `_schemaInitialized`; raw `Edit` was faster for 3 scoped edits. (5) **F5 (add method + wire into processor + new test file)** — *medium miss*: `scaffold_test_preview` could have generated a test skeleton for `WebhookStore.PruneCompleted`; used `Write` instead because the test needed seeded JSON on disk (API didn't cover that shape). **Cross-cutting:** `get_coupling_metrics` returned `"No such tool available"` at 2026-04-15T04:17Z — unclear if removed, unreleased, or dropped from the deferred-tool schema in this catalog version; covered by existing `coupling-metrics-tool`. **Verify loop:** `compile_check` reliably clean after each edit (no false positives like `compile-check-stale-assembly-refs-post-reload` — this was a .NET 10.0.5 workspace with no EF drift); `workspace_reload` auto-fire on stale detection worked transparently post-`Edit`. **New backlog rows:** `change-type-namespace-preview`. **Existing rows reinforced:** `semantic-edit-with-compile-check-wrapper` (19+ sequences of `Edit` -> `workspace_reload` -> `compile_check` in this session validate the dominant-pattern claim); `find-references-metadataname-parameter-rejected`; `roslyn-mcp-guided-refactor-flow`; `self-edit-bootstrap-mode-mcp-development` (pattern seen here was NOT self-edit — peer codebase — suggesting the "Edit beats preview-ceremony for pinpoint changes" rule is **workload-driven, not bootstrap-driven**); `composite-split-service-di-registration` (F3's "replace N params with 1 record param" is a close cousin); `coupling-metrics-tool`. **Pattern-level observation:** The economics pushing an MCP-friendly agent toward raw `Edit` are cumulative: each preview+apply adds ~1s round-trip vs `Edit`'s <100ms; a 5-edit refactor is 5s of dead time vs a 500ms `Edit` run. `apply_with_verify` fixes this **only if** the agent was already in a preview flow — which is rare for pinpoint changes. This is the single strongest qualitative datapoint for `semantic-edit-with-compile-check-wrapper` landing. **Recovery note:** This row was initially appended, then lost when a concurrent IT-Chat-Bot audit session rewrote the file; re-added at 2026-04-15T21:00Z. |
