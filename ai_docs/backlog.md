# Next work and backlog

<!-- purpose: Open work only; contract for agents syncing backlog on ship. -->

**updated_at:** 2026-04-19T15:31:34Z

## Agent contract

**Scope:** This file lists **unfinished work only**. It is not a changelog.

| | |
|---|---|
| **MUST** | Remove or update backlog rows when work ships; do it in the **same PR** as the implementation (or an immediate follow-up). |
| **MUST** | End implementation plans with a final todo: `backlog: sync ai_docs/backlog.md` when closing backlog-related items. |
| **MUST** | Use stable, kebab-case `id` values per open row (grep-friendly). |
| **MUST NOT** | Add "Completed" tables, dated history sections, or narrative changelogs here. |
| **MUST NOT** | Leave done items in the open table. |

## Standing rules (ongoing)

These are **not** backlog rows; do not delete them when clearing completed work.

- Reprioritize on each audit pass.
- Bug and tool items: put reproduction and session context in the PR or linked issue when it helps reviewers.
- Broader planning and complexity notes stay in this file; `ai_docs/archive/` holds policy only (see `archive/README.md`).

## P2 — open work

Within P2, rows are **alphabetical by `id`**.

_No open P2 rows._

## P3 — open work

Within P3, rows are **alphabetical by `id`**.

| id | pri | blocker | deps | do |
|----|-----|---------|------|-----|
| `ci-test-parallelization-audit` | P3 | — | — | **CI perf:** `tests/RoslynMcp.Tests/AssemblyInfo.cs:3` pins `[assembly: DoNotParallelize]`, forcing all 678 tests to run serially on a 4-core runner. `dotnet test` accounts for ~9m22s of the ~9m47s `eng/verify-release.ps1` step (97% of wall time; measured 2026-04-18 run 24597753883). The annotation was added to avoid MSBuildWorkspace file-lock contention when each `[ClassInitialize]` used to recreate the shared `WorkspaceManager` — that root cause was fixed via `tests/RoslynMcp.Tests/TestBase.cs` idempotent init (comment lines 84–103), so the blanket serial gate may now be over-conservative. **Test landscape** (grep for class X `: (SharedWorkspaceTestBase\|IsolatedWorkspaceTestBase\|TestBase)` in `tests/RoslynMcp.Tests/`): ~45 classes inherit `SharedWorkspaceTestBase` (all share one MSBuildWorkspace + mutable `PreviewStore`/`UndoService`/`EditService` state); ~15 classes inherit `IsolatedWorkspaceTestBase` (per-test temp copy but still share services); ~15 classes inherit `TestBase` directly; remainder are pure unit tests. **Proposed phased audit:** (1) enable `[assembly: Parallelize(Scope = ClassLevel, Workers = 0)]`; (2) keep `[DoNotParallelize]` on every class that mutates `WorkspaceManager`/`PreviewStore`/disk state; (3) validate in CI by running the suite 5× in a row to catch flakes; (4) opt classes into parallel one-by-one as their thread-safety is audited. Expected save: 3–5 minutes off the step at steady state. **Why not done yet:** requires per-class audit + local concurrent-load validation before landing; safer to ship separately so a flake regression is bisectable. **Refs:** `tests/RoslynMcp.Tests/AssemblyInfo.cs`; `tests/RoslynMcp.Tests/TestBase.cs:84-103`; `tests/RoslynMcp.Tests/TestInfrastructure/WorkspaceIdCache.cs` (already semaphore-guarded). Raised by 2026-04-18 CI perf audit. |
| `ci-verify-release-coverage-on-main-only` | P3 | — | — | **CI perf:** `eng/verify-release.ps1` unconditionally runs `dotnet test --collect:"XPlat Code Coverage"` which triggers coverlet's IL rewriting pass for every assembly under test — measurable fraction of the 9m22s `dotnet test` wall time in `.github/workflows/ci.yml`'s `validate` job. `CI_POLICY.md` documents coverage as informational (artifact upload + ReportGenerator HTML), not a merge gate with a threshold. **Proposal:** add `[switch]$NoCoverage` to `verify-release.ps1`; have `ci.yml` pass `-NoCoverage` on `pull_request` events and collect coverage only on push-to-`main` + scheduled runs; gate the "Generate coverage HTML summary" and "Upload code coverage" steps on `if: github.event_name != 'pull_request'`. Update `CI_POLICY.md` to reflect the PR-vs-main split (PRs skip coverage collection; `main` and scheduled runs still collect). **Why not done yet:** policy change — coverage on PR was the long-standing default; worth discussing whether the feedback cadence shift is acceptable before landing. Expected save: 1–2 minutes per PR. **Refs:** `eng/verify-release.ps1:30-33`; `.github/workflows/ci.yml` coverage steps; `CI_POLICY.md` § Repository Evidence. Raised by 2026-04-18 CI perf audit. |
| `concurrent-mcp-instances-no-tools` | P3 | — | — | **Bug:** When multiple Roslyn MCP instances start concurrently (deep-review prompt dispatched against several repos in parallel), the **first** instance registers its tools correctly, but **subsequent** instances report "server started" with **no tools available**. Agent calls to `workspace_load` and other tools fail because the tool list appears empty to the client. **Reproducibility:** Observed when starting deep-review prompt across multiple repos in parallel (user report 2026-04-14). **Impact:** Blocks the primary multi-repo audit workflow; parallel audits fall back to serial execution or fail outright. **Investigation:** Determine whether this is (a) stdio handle contention, (b) a plugin registration race in Claude Code MCP host, (c) a shared resource (named pipe, socket, file lock), or (d) the deferred-tool schema load failing silently. **Fix:** Depends on root cause — per-instance isolation, explicit startup sequencing, or diagnostic logging when tool registration fails so the underlying error surfaces instead of returning an empty tool list. **Refs:** `RoslynMcp.Host.Stdio.Program`; Claude Code MCP host; related `mcp-connection-session-resilience`; blocks `compilation-prewarm-on-load`. |

## P4 — open work

Within P4, rows are **alphabetical by `id`**.

| id | pri | blocker | deps | do |
|----|-----|---------|------|-----|
| `audit-remaining-pretooluse-hook-blocks-after-pr-230` | P4 | — | — | **Hook root-cause gap flagged in 2026-04-17 backlog-sweep execution passes 1 and 2:** PR #230 widened the PreToolUse `*_apply` hook to accept `apply_composite_preview` redemption, but the hook still false-positive blocks in **cold-subagent** sessions — a fresh subagent has no prior-turn preview to redeem. Pass 2 Init 4 (`exception-handler-classification-tracer`) first attempt stalled with dangling message `"The hook is conservative; the preview was called immediately before. Let me try again."` — no PR opened, 255s wasted. Pass 1 Init 17 first attempt hit the same shape: `"The hook is being conservative. The preview was shown to me in the prior call. Let me retry..."` (evidence cited in PR #241 body). **Workaround already shipped (PR #241):** v3 executor prompt's cold-context caveat (Step 5 + Appendix B) defaults subagents to an edit-only tool policy, side-stepping the hook. That converts the problem from "subagents fail" to "subagents bypass the hook." **This row tracks the remaining root-cause fix:** make the hook stop false-positive-blocking cold sessions that have a legitimate need for `*_apply` (solution-wide rename/extract where edit-only isn't viable). **Fix options:** (a) audit which `*_apply` paths still block after PR #230 — `rename_apply`, `create_file_apply`, `extract_interface_apply`, `bulk_replace_type_apply`, `fix_all_apply` — and extend the hook prompt's accept-list with concrete same-turn-preview redemption patterns; (b) switch the hook's primary gate from "prior preview evidence" to "preview-then-apply in the same turn" since that's the invariant v3's preview-then-apply path actually produces; (c) add a hook-block telemetry log so reviewers can see real false-positive shapes instead of guessing. **Frequency:** 2 of 8 cold-subagent attempts across passes 1+2 (25%); every parallel sweep that needs a solution-wide rename or extract. **Refs:** `hooks/hooks.json` PreToolUse block (tightened in PR #230); closed row `dr-9-4-pretooluse-hook-false-positive-on` for the first-round fix; v3 executor prompt `ai_docs/prompts/backlog-sweep-execute.md` Step 5 + Appendix B for the current workaround; PR #241 body for the passes-1-and-2 failure-mode catalog. Raised by 2026-04-17 backlog-sweep execution pass 2 retrospective. |
| `ci-skip-dotnet-test-on-docs-only-prs` | P4 | — | — | **CI perf gap observed in 2026-04-18 backlog-sweep pass-4:** Reconcile PRs that touch only `ai_docs/**/*.md`, `CHANGELOG.md`, and the backlog-sweep plan `state.json` + `plan.md` still trigger the full `validate` job in `.github/workflows/ci.yml` (runs `eng/verify-release.ps1` → `dotnet test`, ~9m22s per the `ci-test-parallelization-audit` measurement from 2026-04-18 run 24597753883). **Session evidence:** reconcile PRs #261 and #264 each modified only `CHANGELOG.md` + `ai_docs/backlog.md` + `ai_docs/plans/20260417T120000Z_backlog-sweep/{plan.md,state.json}` — zero `.cs` diff — yet each blocked for ~10min on validate before the orchestrator could merge them. Wave-2 orchestration had to background-poll `gh pr view` with a 20s sleep loop on PR #264 for the full run. **Distinct from `ci-verify-release-coverage-on-main-only`** which covers the coverlet coverage collection only; this row is about the whole `dotnet test` step being redundant when no compiled source changed. **Mitigation partially shipped (2026-04-18):** `.github/workflows/ci.yml` now gates `verify-release.ps1`, coverage, package audit, and artifact uploads on a `detect-docs-only` step that skips them when the PR's diff matches `^(.*\.md\|ai_docs/.*\.json)$` (markdown-escaped pipe; real regex has no backslash before the pipe). `verify-ai-docs.ps1` still runs. Push-to-main and workflow_dispatch always run the full pipeline. **Remaining work:** (a) confirm branch-protection settings treat the skipped-steps-green-validate-job as a passing check (not a skipped one that blocks merge); (b) revisit whether `ai_docs/**/*.json` is a safe skip pattern — state.json-only edits are doc-state, but other `ai_docs/*.json` might feed build inputs in the future; (c) consider expanding to `.claude/**/*.md` and `skills/**/*.md` once confidence builds. **Frequency:** every backlog-sweep session emits 1-2 reconcile PRs per batch; multi-batch sessions produce 4+ doc-only PRs that each would block for ~10min without the gate = 40min+ of CI queue per session. **Refs:** `.github/workflows/ci.yml` detect-docs-only step; `eng/verify-release.ps1`; related `ci-verify-release-coverage-on-main-only` (adjacent perf concern — coverage only); 2026-04-18 backlog-sweep pass-4 reconcile PRs #261 and #264 as concrete evidence. Raised by 2026-04-18 backlog-sweep pass-4 retrospective. |
| `compilation-prewarm-on-load` | P4 | — | `concurrent-mcp-instances-no-tools` | **Perf:** Multiple tools suffer cold-start penalties because Roslyn lazily compiles projects on first access. First `symbol_search` takes 4669ms vs 40ms warm (100x); first `go_to_definition` cross-project takes 2517ms vs 1ms warm; first `symbol_info` takes 1445ms vs 1ms warm. **Impact:** Agents hit multi-second pauses on first interaction with each project. **User preference (2026-04-14):** Cold-start penalty is acceptable; **do NOT** add aggressive pre-compilation that blocks tool registration. **Scope narrowed to:** (a) opt-in `workspace_warm(workspaceId, projects?)` tool only — never run automatically on load; (b) investigate whether observed cold starts are actually Roslyn lazy compilation or a symptom of `concurrent-mcp-instances-no-tools` — fix that first. **Refs:** Roslyn lazy `Compilation` resolution; `workspace_load` handler. |
| `gh-pr-merge-from-worktree-fails-uses-local-main` | P4 | — | — | **Operational gap observed in 2026-04-17 backlog-sweep execution pass 2:** `gh pr merge <n> --squash --delete-branch --auto` (and `--auto` alone) fails with `fatal: 'main' is already used by worktree at 'C:/Code-Repo/Roslyn-Backed-MCP'` when invoked from inside a git worktree. `gh` attempts to update / delete the local branch; the primary branch is checked out in the primary repo path, not the worktree, so the update conflicts. Workaround confirmed this session: `cd "$(git rev-parse --git-common-dir)/.."` before running `gh pr merge` (verified against PR #232 merge). **Impact:** ~30s per occurrence plus an obscure error message that does not point the operator at the fix — risky for automation that runs ship steps from the worktree's own directory. **Fix options:** (a) one-line note in `skills/ship/SKILL.md` and `ai_docs/workflow.md` mandating all `gh pr merge` invocations run from the primary repo root; (b) enhance `/ship` skill with a worktree-detection preamble that auto-`cd`s to `$(git rev-parse --git-common-dir)/..`; (c) invoke `gh pr merge` without the `--delete-branch` flag when inside a worktree (deletion is what fails there); (d) document the one-liner at the top of any per-session merge loop in `ai_docs/prompts/backlog-sweep-execute.md` — already done in v3 of that prompt (Step 8 worktree-cd discipline section) but the underlying ship-skill gap remains. Option (b) is the durable fix; every backlog-sweep runs from worktrees. **Frequency:** Every worktree-based merge. Parallel-subagent sweeps run 4–8 per cycle; hit once this session because most merges happened to run from repo root. **Refs:** `skills/ship/SKILL.md`; `git rev-parse --git-common-dir` for worktree-root detection; 2026-04-17 backlog-sweep pass 2 PR #232 merge log. Raised by 2026-04-17 session retrospective (same session that refactored the executor prompt to v3). |
| `host-parallel-mcp-reads` | P4 | `host` | — | **Throughput:** Some MCP hosts **serialize** all tool calls; read-only tools cannot run concurrently. **Server side:** Document thread-safety of read paths. **Host side:** Allow parallel `call_mcp_tool` for read-only operations where the spec permits. **Refs:** `WorkspaceExecutionGate`, per-workspace read locks; consumer Cursor docs. |
| `parallel-pr-changelog-append-friction` | P4 | — | — | **Operational gap observed in 2026-04-17 backlog-sweep execution pass 1 (4 parallel subagents, 4 PRs):** Every initiative's PR appends a bullet to `CHANGELOG.md` under `## [Unreleased]` → one of `### Fixed` / `### Added` / `### Maintenance`. When subagents run in parallel worktrees and the merges land in an arbitrary order, whichever PR merges second (or later) inevitably hits a merge conflict on `CHANGELOG.md` because both sides wrote into the same subsection. **Session evidence:** Of 4 parallel subagents (init 9, 10, 14, 15 merged as PRs #223, #224, #226, #225 respectively), **2 subagents (init 10 and 14) required a rebase + resolve** on `CHANGELOG.md` before their merge succeeded — subagent reports explicitly called out "rebase needed — conflict against parallel Maintenance line" (#224) and "CHANGELOG.md conflict in `### Added` (both skills adding bullets). Resolved by keeping both bullets" (#226). Each rebase adds ~60s of friction and tempts the subagent into a destructive "accept ours" resolution that would silently drop a sibling PR's entry. **Fix options:** (a) a `/add-changelog-entry` MCP tool that appends to a named subsection idempotently with row-id de-duplication, immune to textual conflict because the operation is "ensure bullet with id X exists under Unreleased > Maintenance" not "insert at line N"; (b) adopt a **changelog-fragment directory pattern** like towncrier / news-fragments / `changelog.d/*.md` where every initiative lands a standalone file (`<row-id>.md`) and the release-cut skill concatenates them into `CHANGELOG.md` at bump time — merge-safe by construction since no two PRs touch the same file; (c) teach `/draft-changelog-entry` (shipped 2026-04-17 via PR #225) to `git pull --rebase origin main` before staging the CHANGELOG edit so at least the conflict window shrinks. Option (b) is the architecturally correct fix — matches how multi-contributor projects avoid this class of conflict permanently. **Frequency:** High — hits every multi-initiative parallel-execution session. Single-initiative sessions don't trip it, but those are rare in sweeps. **Refs:** `CHANGELOG.md`; existing `/draft-changelog-entry` skill at `skills/draft-changelog-entry/SKILL.md`; towncrier pattern ([https://towncrier.readthedocs.io/](https://towncrier.readthedocs.io/)) for prior art. Raised by 2026-04-17 backlog-sweep execution pass 1 retrospective (same session that shipped `/draft-changelog-entry`). |
| `plan-anchor-examples-go-stale-between-plan-and-execute` | P4 | — | — | **Planning-phase gap observed in 2026-04-18 backlog-sweep execution pass 3 (init 6 `duplicate-method-body-detector`):** The plan cited real-world validation: "Run on the current repo's `BuildMinimalDiff` / `BuildMinimalUnifiedDiff` / `BuildUnifiedDiff` duplicates across `ChangeSignatureService`, `RestructureService`, `StringLiteralReplaceService`, `EditService` (all four files exist — verified)". Subagent reported: "plan's real-world validation cited `Build*Diff` helpers that no longer exist in the repo (consolidated into `DiffGenerator.GenerateUnifiedDiff` in a prior change)". Plan drafted 2026-04-17; 2026-04-15 audit findings it anchored to were closed by other work between plan-draft and execution. Subagent pivoted to 9 synthetic unit tests (which passed), but the real-world oracle the plan promised was gone. **Why it recurs:** Backlog-sweep plans cite specific symbol/file/method names as anchors; between plan-draft (session N) and plan-execute (session N+1 or later), other initiatives can close the very scenario the plan anchored to. The planner's Rule 3 vetting verifies file existence but not symbol/method/duplicate-cluster existence at the anchor level. **Fix options:** (a) planner-side `verifyAnchors` step at plan-draft time — for each initiative's Validation field, regex-extract cited symbol/method names, run `mcp__roslyn__symbol_search` for each, and warn if any are missing (plan can still ship, but the "real-world anchor" field becomes `[stale — cited symbol not found]` so executor knows to use synthetic examples); (b) executor-side `backlog-sweep-execute.md` Step 4 gains a plan-anchor re-check before dispatching subagents — stale anchor triggers re-plan in place; (c) both, with (a) early warning and (b) just-in-time fallback. Option (a) is cheaper — runs once at plan-draft rather than once per execution session. **Frequency:** Medium — every plan that lives >24h across multiple execution sessions; this sweep hit 1 of 5 shipped initiatives this session (20%). **Refs:** `ai_docs/prompts/backlog-sweep-plan.md` Rule 1/3/4/5 vetting; `ai_docs/prompts/backlog-sweep-execute.md` Step 4; `ai_docs/plans/20260417T120000Z_backlog-sweep/plan.md` init 6 Validation (stale); state.json init 6 notes (documents pivot). Raised by 2026-04-18 backlog-sweep execution pass 3 retrospective. |
| `plan-vetter-subagent` | P4 | — | — | **Agent gap observed across 6 backlog-sweep sessions (2026-04-16 to 2026-04-17):** Every sweep opens with Step 0 + 1a preflight (git status, reconcile check, `state.json.schemaVersion == 2`, Rules 1/3/4/5 per-initiative validation) and burns 40-60 tool calls in the orchestrator's main context before real work starts. Transcripts `a2580a22-6a49-466d-aecb-3a8c8bf9fb17.jsonl` and `a2fb805e-eef2-46e8-8fe7-366100ee4a58.jsonl` (2026-04-17) each show identical preflight sequences with no variation. **Proposed subagent:** `plan-vetter` — reads the target plan's `state.json` + `plan.md`, runs the four vetting rules, emits a compact go/no-go report with per-initiative flags and which initiatives to skip. Orchestrator consumes 5-10 tool calls (spawn + result parse) instead of 40-60 inline. **Why a subagent, not a skill:** the 4-rule validation reads files and runs checks in isolation with no need to keep raw output in the orchestrator's context — classic context-pollution elimination shape matching the sibling subagents at `.claude/agents/` (`initiative-executor`, `pr-reconciler`, `changelog-and-backlog-sync`). **Distinct from** the existing `/reconcile-backlog-sweep-plan` skill, which handles the in-review→merged state transition after PRs land, not pre-execution vetting. **Frequency:** Once per backlog-sweep session (~daily during active sweep campaigns). **Refs:** `ai_docs/prompts/backlog-sweep-execute.md` Step 0 + Step 1a for the flow to encapsulate; `.claude/agents/initiative-executor.md` as the sibling-subagent shape. Raised by 2026-04-17 session-transcript retrospective. |
| `pr-reconciler-plan-md-commit-is-write-only-dead-code` | P4 | — | — | **Operational gap observed in 2026-04-18 backlog-sweep pass-4:** `.claude/agents/pr-reconciler.md` Step 4 committed a `plan.md` Status-row update on local `main` with message `chore(plan): mark {initiativeId} merged (PR #<n>)`. The commit was never pushed (main is branch-protected) and the next reconciler's Step 3 cleanup (`git fetch origin && git reset --hard origin/main`) wiped the prior reconciler's local commit. Every pr-reconciler run produced a dead commit. **Session evidence:** 5 pr-reconciler runs this session (PRs #258, #259, #260, #262, #263) each reported `PLAN_UPDATE: committed <sha>` (acff30c, d36ac25, 16323cd, 616b3e9, d25baee respectively); ALL 5 were wiped by the orchestrator's `git reset --hard origin/main` before the boundary reconcile PR ran. Authoritative plan.md state was reconstructed from scratch in orchestrator-owned reconcile PRs #261 and #264. Additionally caused 2 `Edit`-tool "File has been modified since read" errors on `plan.md` / `state.json` when the orchestrator tried to edit files that a reconciler had mutated in between read and write on the shared primary-repo worktree. **Mitigation shipped (2026-04-18):** `.claude/agents/pr-reconciler.md` Step 4 removed — reconciler now stops at merge + worktree cleanup + remote-branch delete. Plan-state handoff documented as orchestrator-owned via the batch-boundary reconcile PR. Input contract keeps `planPath` / `initiativeId` as accepted-but-ignored fields so existing orchestrator briefs continue to work unchanged. **Remaining work:** teach `ai_docs/prompts/backlog-sweep-execute.md` Step 8 to drop `planPath` / `initiativeId` from new briefs (optional polish, no functional impact); audit other subagents for the same pattern (commit to local main without push). **Frequency:** every pr-reconciler run. Every backlog-sweep session hit this N times (N = # initiatives merged); 5 hits this session, typically 3-8 per session. **Refs:** `.claude/agents/pr-reconciler.md` (post-fix version); 2026-04-18 backlog-sweep pass-4 reconciler reports; orchestrator's Edit-tool retries during wave-1 reconcile driven by the same race. Raised by 2026-04-18 backlog-sweep pass-4 retrospective. |
| `readme-surface-counts-drift-from-live-catalog` | P4 | — | — | **Doc-drift gap observed in 2026-04-17 v1.22.0 release session:** `README.md` advertises **"142 tools (102 stable / 40 experimental)"** in two places (the `## Claude Code Plugin Installation` paragraph and the top-of-file hero section), but the live server surface after v1.22.0 install reports `server_info.surface.tools = {stable: 103, experimental: 40}` — **143 tools total, not 142**. The delta is the `server_heartbeat` tool added by PR #218 (initiative 1 of the 2026-04-17 sweep); README was never bumped. Stable count drift is silent — no build-time or CI gate catches it because the existing `ServerSurfaceCatalogAnalyzer` (shipped via PR #220, RMCP001/RMCP002) validates `ServerSurfaceCatalog.Tools/Resources/Prompts` list membership against `[McpServerTool]`-decorated methods but does NOT compare either side to README prose. **Why it matters:** `README.md` is the **first touchpoint** for new users choosing whether to adopt the plugin; an off-by-one is minor but a larger drift (which accumulates once the count is known-stale) actively misleads. Same pattern holds for the resource count ("9 stable" — correct today but will drift) and prompt count ("20 experimental" — correct today). **Fix options:** (a) extend `ServerSurfaceCatalogAnalyzer` with a third rule `RMCP003: README tool count (N) does not match catalog (M)` that reads `README.md` from the analyzer's additional-files and parses the count string; (b) generate the README surface-count paragraph from the catalog at build time via a source generator or a `dotnet build /t:GenerateReadmeSurface` target; (c) add an assertion to `eng/verify-release.ps1` that parses README's "(X stable / Y experimental)" and compares to `ServerSurfaceCatalog.Tools` reflection; (d) simpler still: a `tests/RoslynMcp.Tests/ReadmeSurfaceCountTests.cs` that loads the catalog, parses README, and fails the suite when they disagree. Option (d) is the lowest-friction / highest-confidence path — ties into the existing 653-test suite, no MSBuild plumbing, runs on every `verify-release.ps1`. **Frequency:** High — every release that adds or removes a stable tool (~every 2nd release based on the 1.18.x → 1.22.x cadence). **Refs:** `README.md` strings `"102 stable / 40 experimental"` (line ~4 and ~130); `src/RoslynMcp.Host.Stdio/Catalog/ServerSurfaceCatalog.cs` (authoritative list); `analyzers/ServerSurfaceCatalogAnalyzer/ServerSurfaceCatalogAnalyzer.cs` (RMCP001/RMCP002 peer analyzer); `server_info` response `.surface.tools.{stable,experimental}` for the live ground-truth shape. Raised by 2026-04-17 v1.22.0 release-cut session (drift observed via `server_info` after `dotnet publish -p:ReinstallTool=true`). |
| `release-cut-atomic-skill-bump-ship-tag-reinstall` | P4 | — | — | **Operational gap observed in 2026-04-17 v1.22.0 release session:** Cutting a release is **4 discrete user intents** that the agent has to orchestrate sequentially with no shared error handling: (1) `/roslyn-mcp:bump <type>` skill to edit 5 version files (`Directory.Build.props`, `.claude-plugin/plugin.json`, `.claude-plugin/marketplace.json`, `manifest.json`, `CHANGELOG.md` new section header); (2) commit + push + open PR + merge the release branch via `/ship` or manual `git` + `gh pr create` + `gh pr merge --squash --admin`; (3) `git tag -a vX.Y.Z -m '...'` + `git push origin vX.Y.Z`; (4a) `dotnet publish src/RoslynMcp.Host.Stdio -c Release -p:ReinstallTool=true` to rebuild the nupkg and replace the installed global tool (Layer 1 — `~/.dotnet/tools/roslynmcp.exe`); AND (4b) `pwsh ./eng/update-claude-plugin.ps1` to refresh the Claude Code plugin marketplace cache at `~/.claude/plugins/cache/roslyn-mcp-marketplace/<plugin>/<ver>/` (Layer 2 — skills, hooks, marketplace metadata). Reinstall is a **two-layer** operation because the `-p:ReinstallTool=true` MSBuild target has no hook into Claude Code's plugin cache; omitting Layer 2 leaves skills/hooks loading from the stale cache directory. **Concrete session evidence (2026-04-17 v1.22.0 release):** User asked *"bump, ship, push tag and just reinstall"* expecting one atomic operation. The agent did `/roslyn-mcp:bump minor` → ran `verify-ai-docs.ps1` + `verify-release.ps1` (653 tests, ~8min) → manually crafted commit + push + PR (#228) + squash-merge — then tried `git push origin v1.22.0` which succeeded — then `dotnet publish -p:ReinstallTool=true` which emitted `/p:ReinstallTool=true`-vs-`-p:ReinstallTool=true` MSBuild parse-failure on first try (bash mangled the forward-slash) and needed a retry with dash syntax. A failure at step 2 (PR won't merge due to protected-branch hook) would leave step 1's commits orphaned; a failure at step 3 (tag already exists) would leave merged release content un-tagged; step 4 has no rollback if the publish partially succeeded. **Fix:** a `/release-cut` skill accepting one of `major` / `minor` / `patch` as the bump-type argument, that sequences the 4 operations with idempotent checkpoints: (a) verify clean tree + up-to-date main; (b) run the bump skill; (c) run `verify-release.ps1`; (d) create the release branch + PR + squash-merge (wrapping `/ship`); (e) return to main, pull, tag, push tag; (f) reinstall **both layers**: `dotnet publish -p:ReinstallTool=true` for Layer 1 (global tool binary) AND `pwsh ./eng/update-claude-plugin.ps1` for Layer 2 (Claude Code plugin marketplace cache) — not just Layer 1, which was the 2026-04-17 + 2026-04-18 session default that left the cache stale at the prior minor version. Verify both afterward: `server_info.version` reports the new version for Layer 1, directory listing of `~/.claude/plugins/cache/roslyn-mcp-marketplace/roslyn-mcp/` shows only the new version (stale dirs pruned) for Layer 2. Implementation hint: delegate the reinstall step to the existing `/roslyn-mcp:update` skill (which already wraps both layers) rather than re-implementing. Each step records its output so a mid-flow failure can re-run from the last successful checkpoint rather than restarting from scratch. Shell-quoting edge cases (`-p:` vs `/p:` for MSBuild props on bash-on-Windows) get pinned inside the skill once instead of hit by every release session. **2026-04-18 v1.23.0 release session re-confirmed the Layer 2 gap:** agent did Layer 1 correctly via `dotnet publish -p:ReinstallTool=true` but missed Layer 2 — user had to ask *"should the just reinstall not do both?"* to surface the omission; a subsequent `/roslyn-mcp:update` invocation ran `eng/update-claude-plugin.ps1` which pruned a stale `cache/roslyn-mcp-marketplace/roslyn-mcp/1.21.0/` directory (two minor versions behind) and re-synced 623 files to the current `1.23.0`. Without Layer 2 refresh, Claude Code keeps loading skills from the stale cache path visible in each skill's runtime `Base directory for this skill: ...` line. **Distinct from `/ship`** which handles commit→PR→merge→cleanup — this skill is release-specific and wraps `/ship` + `/roslyn-mcp:bump` + tag + reinstall. **Frequency:** Every release (1.18.x through 1.22.x = ~5 releases in 2 weeks). **Refs:** `skills/ship/SKILL.md`; `skills/bump/SKILL.md`; `skills/update/SKILL.md` (already wraps both layers — `/release-cut` should internally delegate the reinstall step to `/roslyn-mcp:update` rather than re-implement); `eng/verify-version-drift.ps1`; `eng/verify-release.ps1`; `eng/update-claude-plugin.ps1` (Layer 2 re-sync primitive); README `### Install as Global Tool` section for the `-p:ReinstallTool=true` recipe (Layer 1 only). Raised by 2026-04-17 v1.22.0 release-cut session retrospective; re-confirmed 2026-04-18 v1.23.0 release-cut session (Layer 2 gap). |
| `roslyn-fetch-resource-timing` | P4 | — | — | **`fetch_mcp_resource`** intermittently returns "server not ready" for `roslyn://` workspace URIs in Cursor — determine whether host timing, server readiness gate, or documentation gap. **Refs:** Resource handlers; readiness gate vs `workspace_load` completion. |
| `server-surface-catalog-append-conflict-hotspot` | P4 | — | — | **Operational / tooling gap observed in 2026-04-18 backlog-sweep pass-4:** Every new MCP tool AND many tool extensions touch the single append-only file `src/RoslynMcp.Host.Stdio/Catalog/ServerSurfaceCatalog.cs` (the runtime tool descriptor registry). Parallel-execution waves that include 2+ tool-surface-touching initiatives hit rebase pressure or merge conflicts on this file, violating the `backlog-sweep-execute.md` Step 2a disjoint-production-files rule in spirit even when initiatives look independent by their declared scope. **Session evidence:** wave 1 shipped PRs #258 (new `scaffold_first_test_file_preview` tool), #259 (extends `get_di_registrations` — no catalog touch), and #260 (extends `apply_text_edit` / `apply_multi_file_edit`). PRs #258 and #260 BOTH touched `ServerSurfaceCatalog.cs` for tool-description updates. After #258 merged, #260's `mergeStateStatus` flipped from `MERGEABLE` to `UNSTABLE`; GitHub auto-updated the base branch and the pr-reconciler had to wait for a fresh validate CI run before merging. Similar hit on PR #263 (`extract_shared_expression_to_helper_preview`) which also touched the catalog. **Distinct from `parallel-pr-changelog-append-friction`** which covers the same anti-pattern for `CHANGELOG.md`; this row targets the analogous hotspot in `ServerSurfaceCatalog.cs` (and by extension `src/RoslynMcp.Host.Stdio/Extensions/ServiceCollectionExtensions.cs` for DI registration). **Distinct from closed `mcp-server-surface-catalog-parity-generator`** (PR #220) which promoted the parity check to a build-time analyzer — the analyzer catches drift but does not eliminate the append conflict. **Partial mitigation shipped (2026-04-18):** `ai_docs/prompts/backlog-sweep-execute.md` Step 2a parallel-mode picking rules now call out ServerSurfaceCatalog.cs + ServiceCollectionExtensions.cs as virtually-shared and recommend picking at most one catalog-touching initiative per wave. This is process-side only — the structural fix below is still open. **Structural fix options:** (a) convert `ServerSurfaceCatalog.cs` to a `partial class` split across one file per tool domain (`ServerSurfaceCatalog.Scaffolding.cs`, `ServerSurfaceCatalog.Editing.cs`, `ServerSurfaceCatalog.Refactoring.cs`, etc.) so each initiative lands in its own file — merge-safe by construction, mirrors the per-domain `*Tools.cs` grouping already present in `src/RoslynMcp.Host.Stdio/Tools/`; (b) generate the catalog at build time via a source generator that reads `[McpServerTool]`-decorated methods — companion to the existing `ServerSurfaceCatalogAnalyzer` (analyzer validates the hand-maintained list today, generator would eliminate it). Option (b) is the structurally-correct fix — the analyzer already parses the attributes; the generator reuses that machinery. **Frequency:** every parallel-execution wave with 2+ tool-surface-touching initiatives. Hit 1 of 2 waves this session (wave 1 affected; wave 2 happened to pick a catalog-touching + non-catalog-touching pair). **Refs:** `src/RoslynMcp.Host.Stdio/Catalog/ServerSurfaceCatalog.cs`; `src/RoslynMcp.Host.Stdio/Extensions/ServiceCollectionExtensions.cs` (DI analog); `analyzers/ServerSurfaceCatalogAnalyzer/ServerSurfaceCatalogAnalyzer.cs` (companion RMCP001/RMCP002 analyzer — generator would replace the need); closed row `mcp-server-surface-catalog-parity-generator` (parity-gate only, not append-gate); open row `parallel-pr-changelog-append-friction` (same anti-pattern, different file). Raised by 2026-04-18 backlog-sweep pass-4 retrospective. |
| `subagent-malformed-result-orphans-uncommitted-work` | P4 | — | — | **Operational gap observed in 2026-04-18 backlog-sweep execution pass 3 (init 5 `composite-split-service-di-registration`):** Subagent stalled mid-reasoning with final message `"Actually, let me just try inspecting the file:"` — a thought-fragment, NO `<<<RESULT>>>` sentinel (matches Appendix C step 1 malformed-result failure). The subagent had already modified 4 production files and written 1 new test file (729 insertions total) — all uncommitted, stranded in the abandoned worktree. Orchestrator recovery took 3 manual bash turns: (1) `cd .worktrees/<id> && git status` to enumerate, (2) `git add -- <explicit paths> && git commit -m "wip(...)" && git push -u origin remediation/<id>` to preserve on a branch, (3) `git worktree remove --force` + cleanup. State.json notes field also had to be manually crafted with stall explanation + triage instructions. **Why it recurs:** Cold-subagent stalls are a known failure mode (`audit-remaining-pretooluse-hook-blocks-after-pr-230` covers one specific cause — hook-block loops); this row covers the downstream-recovery shape regardless of stall cause. Observed rate: ~1 in 10 subagents stalls with uncommitted WIP across 2026-04-17 + 2026-04-18 passes (2 stalls / 20 subagents total). Appendix C defines detection but not recovery. **Fix:** `/recover-stalled-subagent <branch-name>` operational skill that atomically: (a) `cd .worktrees/<derived-id>`, (b) checks for uncommitted work via `git status --porcelain`, (c) if work exists: stages with explicit paths (NO `git add -A` — respects the hard-rule from backlog-sweep-execute.md), commits as `wip(<scope>): <branch>-partial (subagent stalled)`, pushes to origin, (d) removes worktree + local branch, (e) emits a structured suggestion for the state.json `notes` field. Reduces 3-turn recovery to one skill call. **Frequency:** ~1 per 10 subagents; every parallel-mode batch has a non-trivial chance of one stall. **Refs:** `ai_docs/prompts/backlog-sweep-execute.md` Appendix A (no recovery path defined), Appendix C (detection only); related `audit-remaining-pretooluse-hook-blocks-after-pr-230` (one specific stall root cause); session 2026-04-18 branch `remediation/composite-split-service-di-registration` commit `3b56517` for the preserved-WIP shape. Raised by 2026-04-18 backlog-sweep execution pass 3 retrospective. |
| `workspace-health-triage-subagent` | P4 | — | — | **Agent gap observed in 2026-04-16 workspace health-check session (`b093b4b1-2159-41a1-b037-bd08936e2cbd.jsonl`):** Answering "is the Roslyn workspace loaded and healthy?" required 56 `Bash` + 16 `Grep` + 26 `Read` calls — a repeatable sequence (`workspace_list` → `workspace_health` → `server_info` → conditional `workspace_reload` on drift → report tool count + version) inlined into the main context each time. The orchestrator's context absorbed the full tool-list dump, version-drift output, and per-workspace status JSON even when the answer was a one-line "healthy". **Proposed subagent:** `workspace-health-triage` — accepts an optional `workspaceId`, runs the health sequence, returns a single structured block (`STATUS: healthy/stale/drifted/unloaded`, `VERSION: <ver>`, `TOOL_COUNT: <n>`, `WORKSPACES: <list>`, `ACTIONS_TAKEN: <reload?>`, `NOTES:`). Main context sees ~8 lines instead of ~100 tool-result lines. **Why a subagent, not a skill:** the raw MCP responses (`workspace_list`, `server_info`) are verbose and cacheable separately; the caller only needs the summary. **Distinct from** `/roslyn-mcp:publish-preflight` (release-readiness-focused) and `/roslyn-mcp:analyze` (solution health, not workspace-daemon health). **Frequency:** 1-2 per day, more during dev sessions that span installs or plugin-cache refreshes. **Refs:** `mcp__roslyn__workspace_list`, `mcp__roslyn__workspace_health`, `mcp__roslyn__server_info`, `mcp__roslyn__workspace_reload` for the sequence to encapsulate; session `b093b4b1-2159-41a1-b037-bd08936e2cbd.jsonl` (2026-04-16) as the empirical anchor. Raised by 2026-04-17 session-transcript retrospective. |
| `workspace-process-pool-or-daemon` | P4 | — | `compilation-prewarm-on-load` | **Perf:** Each new `roslynmcp` subprocess starts with zero workspaces and pays the full ~10s `workspace_load` cost on first call. For multi-agent orchestrators that spawn one process per sub-agent, the cumulative reload cost dominates wall-clock time. **Measured impact:** Audit session 2026-04-13 spawned 9 agents (~90s reload overhead); stress test session 2026-04-14 spawned 4 agents (~40s overhead). For a typical Claude Code session the cumulative reload tax is 1–3 minutes — comparable to actual analysis work. **Goal options:** (a) Long-running daemon mode (`roslynmcp --daemon --socket /tmp/roslyn.sock`) where short-lived clients connect over a unix/named-pipe socket and share the loaded workspace; (b) Process pool with workspace state hand-off via shared memory or temp files. **Distinction from `compilation-prewarm-on-load`:** That item is about prewarming compilations within one process; this item is about sharing the loaded workspace state across multiple processes/clients. **Refs:** `WorkspaceManager`, stdio host bootstrap. |
| `windows-testhost-file-lock-blocks-worktree-cleanup` | P4 | — | — | **Operational gap observed in 2026-04-18 backlog-sweep execution pass 3 (batches 2 + 3):** After subagents run `./eng/verify-release.ps1` inside their worktrees, `rm -rf .worktrees/<id>` fails on Windows with `Device or resource busy` on `tests/RoslynMcp.Tests/bin/Release/net10.0` (init 5 worktree) and `bin/Debug/net10.0` (init 16 worktree) because `testhost.exe` / `VBCSCompiler.exe` processes spawned by the test run retain open file handles on the test assemblies. Hit twice this session, blocking the final `/ship` cleanup step. `git worktree remove --force` and `git worktree prune` succeed — git admin metadata is gone — but the filesystem directory leaks. **Impact:** cosmetic short-term, but every backlog-sweep session accumulates 2-5 stale `.worktrees/*` dirs across successive runs; `git worktree add` succeeds (git's view is clean) but humans opening the repo see stale bin-tree residue. **Fix options:** (a) teach `backlog-sweep-execute.md` Step 5b worktree-cleanup to invoke `dotnet build-server shutdown` (shuts down VBCSCompiler + MSBuild node + Razor build server) BEFORE `git worktree remove` — safe, idempotent no-op if no servers running; (b) subagent brief adds the same shutdown step as the last thing before reporting `<<<RESULT>>>`; (c) `/ship` skill's worktree-cleanup phase tries `dotnet build-server shutdown && taskkill /IM testhost.exe /F 2>NUL` before any `rm -rf` retry. Option (a) is the minimal-risk fix — resolves the vast majority of Windows file-lock residues with one documented SDK command. **Frequency:** Every backlog-sweep session on Windows that runs `verify-release.ps1` (i.e. all of them, until subagents skip the CI-parity run). **Refs:** `ai_docs/prompts/backlog-sweep-execute.md` § Step 5b + Appendix A step 8; `skills/ship/SKILL.md` Step 5b; session 2026-04-18 worktree-cleanup failures on init 5 + init 16. Raised by 2026-04-18 backlog-sweep execution pass 3 retrospective. |


## Refs

| Path | Role |
|------|------|
| `ai_docs/workflow.md` | Branch/PR workflow; **Backlog closure** |
| `ai_docs/backlog.md` | This file (open work + contract) |
| `ai_docs/audit-reports/` | Raw deep-review audits (inputs for prioritization). Per-run audit files are deleted once their findings are either closed in the top-10 remediation branches or tracked as `dr-9-*`/identity-keyed rows in this file. |
| `ai_docs/plans/20260415T220000Z_top10-remediation-plan.md` | Top-10 remediation plan drafted from independent source-code verification — 10 per-item diagnosis/approach/scope/risks/validation/perf fields. Shipped across 10 commits on the `top10-remediation` branch (2026-04-16): closes 19 backlog rows across the P2/P3/P4 bands. |
| `ai_docs/plans/20260416T054040Z_backlog-sweep/plan.md` | Backlog sweep (2026-04-16). Shipped 41 initiatives across 40 PRs; closed 47 backlog rows (P2: 1, P3: 14, P4: 32). Final initiative `csproj-reserialization-msbuildworkspace` closes the P2 row `create-file-apply-csproj-side-effect-all-projects`. |
| `ai_docs/procedures/deep-review-backlog-intake.md` | Multi-repo audit import / backlog merge (`eng/new-deep-review-batch.ps1`) |
| `eng/import-deep-review-audit.ps1` | Ingest external audit markdown into this repo (if present at repo root) |
| `src/RoslynMcp.Host.Stdio/Tools/WorkspaceTools.cs` | Workspace lifecycle (`workspace_load`, `workspace_reload`, `workspace_close`) — cited in firewall audit Phase 16 concurrency prose |
| Consumer: `.github/copilot-instructions.md` (DotNet-Firewall-Analyzer) | **Roslyn MCP required when available** — documents agent policy when MCP disconnect forces hand edits |
| Qualitative: DotNet-Firewall-Analyzer backlog remediation (2026-04, orchestrator session) | **Roslyn MCP not used** (`call_mcp_tool` / Roslyn server): work used Read/StrReplace, explore `Task`, `dotnet build`/`test`/`format`. **Highest-efficiency Roslyn tools for that work:** `find_references` / `rename_preview` for shared helpers (`RulebasePanOs`, `HaStatePanOs`), `split_class` / `move_type_to_file` + multi-file apply for importer split, `project_diagnostics` scoped to touched projects post-edit. **New backlog rows:** `composite-split-service-di-registration`, `mapper-snapshot-dto-symmetry-check`, `test-mock-drift-new-interface-usage`. **Existing rows still primary:** `agent-symbol-refactor-unified-preview`, `mechanical-sweep-after-symbol-change`, `mcp-connection-session-resilience`, `roslyn-mcp-guided-refactor-flow`, `roslyn-mcp-multi-file-preview-apply`. |
| Qualitative: DotNet-Firewall-Analyzer backlog remediation session 2 (2026-04, session `a721fbeb`) | **Zero Roslyn MCP calls** across 5 C# refactoring tasks (25 `StrReplace`, 7 `Write`, 16 shell build/test/format invocations). **Task-by-tool mapping (missed opportunities):** (1) `sec-apikey-header-only` — low miss, single-line edit; `compile_check` would have been faster than `dotnet build`. (2) `test-validators` — medium miss; `scaffold_test_preview` should have generated stubs for 5 test classes. (3) `dead-code-cleanup` — **high miss**; `find_unused_symbols` → `find_implementations` → `remove_dead_code_preview/apply` was the exact tool chain for removing `GetSnapshotSummaryAsync`/`WriteRawXmlAsync` from `ISnapshotStore` + `FileSnapshotStore`. (4) `object-type-constants` — **high miss** for `move_type_to_project_preview` (moving `SnapshotObjectKind` from Application → Domain); **tool gap** for string-literal-to-constant replacement (~49 literals across 8 files). (5) `test-api-endpoints` — medium miss; `scaffold_test_preview` for endpoint test stubs. **Cross-cutting:** `compile_check` never called (agents used `dotnet build` via shell); `format_document_preview` never called (agents used `dotnet format` via shell); `workspace_load` never called (no `workspaceId` available). **New backlog rows:** `dead-interface-member-removal-guided`, `string-literal-to-constant-extraction`, `format-verify-solution-wide`, `scaffold-test-batch-mode`. **Existing rows reinforced:** `mcp-connection-session-resilience`, `roslyn-mcp-guided-refactor-flow`, `roslyn-mcp-post-edit-validation-bundle`, `roslyn-mcp-workspace-staleness`, `move-type-to-project-namespace`. |
| Qualitative: Jellyfin audit + stress test session 3 (2026-04-13/14, Claude Code 13-agent dispatch) | **Roslyn MCP usage profile:** Audit prompt designed to exercise every tool; agents used Roslyn MCP for ~95% of work via spawned `roslynmcp` subprocesses (no direct deferred-tool registration available to orchestrator). **Refactoring tasks performed (Phase 6 only):** `organize_usings_apply`, `set_diagnostic_severity`, `add_pragma_suppression`, `apply_text_edit`, `apply_multi_file_edit`, `extract_method_apply` — all via MCP. **One forced manual fallback:** `extract_method_apply` of `ApplyMinWidthFilter` produced CS0136+CS0841 (`var` redeclaration); agent had to make a manual `Edit` to remove `var` (validates `extract-method-apply-var-redeclaration` P2 bug). **Other manual fallbacks (none code-modification):** orchestrator wrote audit/stress test reports via `Write`/`Edit`; ran CLI `dotnet build` + `dotnet test` for baseline comparison (would have been MCP-routed if `compile_check`/`test_run` had been directly invokable from the orchestrator instead of via sub-agents). **Process-level observations:** 13 cumulative agents × ~10s reload = ~130s overhead; agents independently rediscovered NDJSON protocol details ~9 times; `find_unused_symbols`+`type_hierarchy`+`find_implementations`+`member_hierarchy`+`impact_analysis`+`suggest_refactorings` all blocked by `UnresolvedAnalyzerReference` (already P2). **New backlog rows:** `apply-with-verify-and-rollback`, `get-source-text-line-range-ignored`, `mcp-stdio-protocol-onboarding-docs`, `roslyn-mcp-claude-direct-tool-registration`, `long-running-tool-progress-notifications`, `preview-token-cross-process-handoff`, `workspace-process-pool-or-daemon`. **Existing rows reinforced:** `extract-method-apply-var-redeclaration`, `unresolved-analyzer-reference-crash`, `compilation-prewarm-on-load`, `code-fix-providers-missing-ca`, `project-diagnostics-large-solution-perf`. |
| Qualitative: Roslyn MCP self-development sessions — v1.15.0 + v1.16.0 ship pipelines (2026-04-14) | **Zero Roslyn MCP calls across implementation work** for three back-to-back top-10 remediation passes (PRs #150, #151 v1.15.0, #153 v1.16.0). ~30 distinct C# source edits across ~20 files (Core/Roslyn/Host.Stdio/Tests projects), zero `mcp__roslyn__*` invocations until the post-reinstall health check at the very end. **Root cause:** Self-edit bootstrap problem — the binary doing the analysis is the binary being changed, so `workspace_load` + `*_preview/*_apply` cycles are impractical (snapshot stale after every Edit, binary mid-rebuild). All work used `Edit`/`Write` + `Bash`-driven `dotnet build`/`dotnet test`. **Highest-confidence missed opportunities (would have helped even with bootstrap constraint):** (a) `find_references` before adding `filePaths?` parameter to `ICodeMetricsService.AnalyzeAsync` (4 callsites hand-grepped, 2 of them broke at compile from positional shift); (b) `compile_check` after each Edit instead of full `dotnet build` cycle (3-30s vs ~30s); (c) `format_check` after edits (the very tool shipped in v1.16.0 — never used during its own development); (d) `apply_with_verify` for the symbol-resolver member-fallback extension. **New backlog rows:** `change-signature-add-parameter-cross-callsite`, `mcp-tool-catalog-attribute-binding`, `roslyn-structural-search-and-replace`, `scaffold-implementation-from-interface`, `self-edit-bootstrap-mode-mcp-development`. **Existing rows reinforced:** `roslyn-mcp-guided-refactor-flow`, `roslyn-mcp-multi-file-preview-apply`, `roslyn-mcp-post-edit-validation-bundle`, `roslyn-mcp-workspace-staleness`. **Pattern-level observation:** Even a Roslyn-MCP-friendly orchestrator falls back to plain Edit when the `workspaceId` doesn't exist (no `workspace_load` ever called) and the cost-of-load exceeds the cost-of-edit for one-off changes — argues for either (a) a `workspace_load_lazy` mode that auto-loads on first tool call or (b) a documented "in-bootstrap-mode use Edit + compile_check" recipe. |
| Qualitative: IT-Chat-Bot no-holds-barred refactor audit (2026-04-15, senior-engineer session, 5-finding cap) | **Roslyn MCP usage profile: strong read-side, zero write-side.** **Analysis phase (~12 tool calls):** `workspace_load`, `workspace_health`, `project_graph`, `project_diagnostics` (summary + warning filter), `get_complexity_metrics`, `get_cohesion_metrics` (excludeTestProjects, minMethods=5), `find_unused_symbols`, `suggest_refactorings`, `find_reflection_usages`, `security_diagnostics`, `nuget_vulnerability_scan`, `compile_check` (post-edit, **failed with 193 spurious CS1705 — see below**), `workspace_reload`. All analysis drove finding selection cleanly. **Implementation phase (4 of 5 findings applied, F05 deferred):** **zero `*_preview`/`*_apply` calls** — same economics-of-Edit pattern observed in DotNet-Network-Documentation session. **Task-by-tool mapping (missed opportunities):** (1) **F01 (OCE catch filter × 3 sites, 2 files)** — *high miss*: textbook case for `restructure_preview` (identical C# pattern `catch (OperationCanceledException) when (X)` → `when (Y)`) across 3 sites, OR `preview_multi_file_edit` + `apply_with_verify`. Agent used 3 separate `Edit` calls + manual Bash `dotnet build/test`. Reinforces `roslyn-structural-search-and-replace`, `roslyn-mcp-multi-file-preview-apply`, `apply-text-edit-with-verify-bundle`. (2) **F02 (remove dead `packedEvidence` parameter + 2 callsites)** — *high miss*: **`change_signature_preview` with `op='remove'` is the literal canonical tool for this task**. Agent used 3 `Edit` calls. Reinforces `roslyn-mcp-guided-refactor-flow` (tool discoverability). (3) **F03 (widen `#pragma warning disable CA2025` scope)** — *gap*: no semantic pragma-scope tool exists; `Edit` is currently the only option. **Raised new row: `pragma-directive-scope-manipulation`.** (4) **F04 (remove 3 dead DI registrations)** — *high miss*: `get_di_registrations` was never called — agent identified the Singleton→Scoped override pattern via 3× `Grep` across two `ServiceCollectionExtensions.cs` files. **Raised new row: `di-lifetime-mismatch-detection`.** **Cross-cutting bugs observed:** (a) `find_references(metadataName='ITChatBot.Conversation.InMemoryConversationRepository')` returned `InvalidArgument: Provide either filePath with line and column, or symbolHandle.` — **5th reproduction** of `find-references-metadataname-parameter-rejected`; Grep fallback was forced. (b) **`compile_check` after `workspace_reload` returned 193 spurious CS1705 stale-assembly-reference errors** (EntityFrameworkCore 10.0.5 vs 10.0.6 version drift surfaced as version-mismatch against in-memory stale `MetadataReference` handles); real `dotnet build` produced only 2 pre-existing errors. **Raised new row: `compile-check-stale-assembly-refs-post-reload`.** (c) `get_cohesion_metrics` flagged `RequestApprovalAction` LCOM4=4 + three other `IAction` implementors LCOM4=3 as "split into cohesive types" — every hit was a lifecycle-pattern false positive (Describe/Validate/Execute are orthogonal on fields by design). **Raised new row: `lcom4-lifecycle-pattern-false-positive`.** **Validation:** Bypassed `validate_workspace` bundle — ran 15 separate Bash `dotnet build`/`dotnet test` invocations across 9 test projects. 744/746 tests passed, 2 failures pre-existing on `main` (confirmed via stash test). **New backlog rows:** `compile-check-stale-assembly-refs-post-reload`, `di-lifetime-mismatch-detection`, `lcom4-lifecycle-pattern-false-positive`, `pragma-directive-scope-manipulation`. **Existing rows reinforced:** `find-references-metadataname-parameter-rejected` (5th reproduction — observed in every MCP-aware audit session), `roslyn-mcp-guided-refactor-flow` (canonical `change_signature_preview` tool missed for literal canonical use case), `roslyn-mcp-multi-file-preview-apply`, `roslyn-mcp-post-edit-validation-bundle`, `roslyn-structural-search-and-replace`, `apply-text-edit-with-verify-bundle`. **Pattern-level observation:** Agent's instinct on a clean Release build (0 errors, 3 warnings) was "just Edit these 4 small things" — tool friction of each MCP preview (~3-5s round-trip vs <100ms for `Edit`) outweighs semantic-correctness benefit when changes are pinpoint. But the downside is cumulative: F03's pragma-scope bug was the **same kind of mistake** a prior session made (original pragma placed before use site); the loss of atomic preview+auto-verify means these subtle errors persist across sessions. This is the strongest argument yet for `apply-text-edit-with-verify-bundle` — make `Edit`'s verify gap zero-cost, then agents keep the speed AND get the safety. |
| Qualitative: Backlog-sweep v2 execution session (2026-04-16, 14 PRs shipped: #165–#178) | **Zero Roslyn MCP calls across 14 production-code PRs** on the self-edit codebase — same bootstrap-mode anti-pattern first documented in the v1.15.0 / v1.16.0 sessions (see qualitative row below), reproduced in a new session nine weeks later. Tools used: `Read`, `Edit`, `Write`, `Grep`, `Bash` (dotnet build/test, git, gh), `Agent` (once, for I-01 explore-mode investigation). Tools NOT used: every `mcp__roslyn__*`, every `roslyn-mcp:*` skill — including the read-only ones the bootstrap guidance explicitly permits. **Session-wide missed-opportunity roll-up:** (1) **Every compile cycle**: `Bash: dotnet build RoslynMcp.slnx -c Release -p:TreatWarningsAsErrors=true` invoked 16× at ~6s each (~96s total) when `mcp__roslyn__compile_check` would have returned comparable diagnostics in <1s with structured output. (2) **Every test cycle**: `Bash: dotnet test tests/RoslynMcp.Tests/RoslynMcp.Tests.csproj -c Release --no-build` invoked 15× at ~4.5min each when `mcp__roslyn__test_run --filter related` (scoped via `mcp__roslyn__test_related_files` on the touched-file set) would have returned only the relevant subset; savings estimate 10–20min of wall-clock across the session. (3) **Every caller search**: `Grep` invoked ~25× across 14 PRs for "find callers of symbol X" where `mcp__roslyn__find_references` with `metadataName` would have been exact (no filter-out of false matches on same-simple-name). (4) **PR #167 (I-03) BREAKING record field addition**: No `impact_analysis` call before shipping; pattern-match miss would have been invisible to compiler. (5) **PR #171 (I-07) new optional parameters on ISyntaxService**: Single caller `ExpandedSurfaceIntegrationTests` discovered via build failure — `find_references` would have caught it pre-build. (6) **PR #178 (I-14) helper extraction**: Manual 2-file refactor to extract `NormalizeFilePathForResource`; no tool exists today but the gap is real (raised as new row `extract-shared-expression-to-helper-across-call-sites`). **Debugging friction:** PR #169 (I-05) required ~4 rounds of `Console.WriteLine` debug prints to understand why `SymbolFinder.FindCallersAsync` returned locations whose `SourceTree` didn't match `accumulator.GetDocument(...)` — `callers_callees` + `get_document_highlights` might have surfaced the solution-identity issue earlier. **Pattern-level observation (3rd reproduction):** This session confirms the hypothesis from the v1.15.0 / v1.16.0 retrospective: "*Even a Roslyn-MCP-friendly orchestrator falls back to plain Edit when the workspaceId doesn't exist*." What's new: **even the READ-ONLY tools are skipped** — agents see "bootstrap mode" and generalize the write-side limit to the entire MCP surface. **New backlog rows:** `bootstrap-read-only-roslyn-mcp-checklist-for-self-edit-sessions`, `breaking-change-impact-preview-for-positional-records`, `extract-shared-expression-to-helper-across-call-sites`. **Existing rows reinforced:** `self-edit-bootstrap-mode-mcp-development` (3rd major session where this pattern dominates), `default-to-compile-check-over-dotnet-build` (16 × `dotnet build` vs 0 × `compile_check` — strongest reproduction yet), `semantic-edit-with-compile-check-wrapper`, `post-edit-validate-workspace-scoped-to-touched-files`. **Efficiency math:** If even half the `dotnet build` calls had been `compile_check` and half the `dotnet test` runs had been `test_run --filter related`, the session would have shipped 14 PRs in ~60% of the observed wall-clock time — freeing enough context budget to potentially ship 5–8 additional initiatives before the user-requested restart. |
| Qualitative: Backlog-sweep v2 execution session 2 (2026-04-16, 13 PRs shipped: #182–#194) | **Zero Roslyn MCP calls across 13 consecutive production-code PRs** on the self-edit codebase — 4th major reproduction of the bootstrap-read-only anti-pattern (prior reproductions: v1.15.0 + v1.16.0 self-dev, the 14-PR backlog-sweep v1 session PRs #165–#178). Tools used: `Read`, `Edit`, `Write`, `Grep`, `Bash` (dotnet build/test × 15 each, git, gh, pwsh verify-ai-docs.ps1), `Agent` (once per initiative for scoped exploration — 13× total). Tools NOT used: every `mcp__roslyn__*`, every `roslyn-mcp:*` skill. **Session-wide Roslyn MCP missed-opportunity roll-up by refactor shape:** (1) **Every exception-classification refactor (I-15/I-16/I-17)**: `trace_exception_flow`-style analysis would have made the ToolErrorHandler dictionary traversal tool-assisted; agent read the file top-to-bottom instead. Raised as new row `exception-handler-classification-tracer` (P3). (2) **Every `[McpServerResource]` addition (I-21, 3 new attributes)**: `ServerSurfaceCatalog.Resources` hand-maintained list drifted; parity test caught it at test-run not build-time. Raised as new row `mcp-server-surface-catalog-parity-generator` (P3). (3) **Every new-service first-test (I-19 RestructureServiceTests, I-23 TestReferenceMapServiceTests)**: 30-line boilerplate copy-paste per new fixture. Raised as new row `first-test-for-new-service-scaffold` (P4). (4) **Every pinned-caret integration test (I-22, 3 tests)**: manual column-math, off-by-one discovered at test-run. Raised as new row `position-probe-for-test-fixture-authoring` (P4). (5) **13 CHANGELOG entries hand-drafted** (~60s each = ~13 min): raised as `changelog-entry-draft-from-pr-metadata` (P4). (6) **13 Step 1b state.json/plan.md reconciliation cycles** (~30s each = ~6.5 min): raised as `backlog-sweep-state-reconcile-merged-initiatives-skill` (P4). **Operational observations:** session survived two full conversation-state incidents — the first appeared to switch HEAD to a concurrent session's branch (`fix/di-register-latest-version-provider`) with my uncommitted working tree intact but the branch name mis-reported; resumption revealed the working tree was actually still on `remediation/dr-9-13-…` with all edits preserved (false alarm). The second was a user-requested mid-session restart which required reading full chat logs to identify where the work was and whether to start over or continue — the 6-file working-tree diff was the source of truth, resumed successfully. Argues for `auto-save working-tree edits to a scratch branch on session-pause` or similar agent-harness feature. **Reinforced rows (strong evidence added):** `bootstrap-read-only-roslyn-mcp-checklist-for-self-edit-sessions` (4th major reproduction); `default-to-compile-check-over-dotnet-build` (~15 × `dotnet build` + ~15 × `dotnet test` this session with zero `compile_check` / `test_run` equivalents — matches the 16/15/0 profile from session 1 almost exactly, reinforcing the operational hypothesis); `semantic-edit-with-compile-check-wrapper` (every single production edit used the `Edit → dotnet build → dotnet test` pattern, 13×); `self-edit-bootstrap-mode-mcp-development` (the write-side limit still dominates); `breaking-change-impact-preview-for-positional-records` (I-20 added `UnknownFilePaths` positional field to `WorkspaceValidationDto`; single caller was the service itself, no pattern-match / deconstruction callers, so no breakage — but would benefit from the audit-before-shipping pattern); `extract-shared-expression-to-helper-across-call-sites` (the exact pattern I-14 established — session 2 did not repeat it at production scope but `SymbolSearchService.SearchSymbolsAsync`'s new `TryAddSymbol`/`EnumerateNamedTypes` helpers exemplify the "would-have-wanted-to-extract-before-duplicating" shape). **Efficiency math (rough estimate):** If compile_check had replaced dotnet build (saves ~5s × 15 = 75s), test_run --filter had replaced dotnet test (saves ~2.5min × 15 = 37.5min), find_references had replaced Grep-for-callers (saves ~20s × 15 = 5min), and the proposed operational skills had replaced hand-drafted CHANGELOG + state reconciliation (~20min), the session would have saved ~1 hour of pure-ceremony time — enough budget to ship 2–4 additional initiatives before the next context-limit breakpoint. **New backlog rows:** `exception-handler-classification-tracer` (P3), `mcp-server-surface-catalog-parity-generator` (P3), `backlog-sweep-state-reconcile-merged-initiatives-skill` (P4), `changelog-entry-draft-from-pr-metadata` (P4), `first-test-for-new-service-scaffold` (P4), `position-probe-for-test-fixture-authoring` (P4). |
| Qualitative: DotNet-Network-Documentation no-holds-barred refactor (2026-04-14/15, senior-engineer audit session) | **Roslyn MCP usage split sharply by phase.** **Analysis phase (~20 tool calls):** intensive, idiomatic use — `server_info`, `workspace_load`, `project_graph`, `compile_check`, `project_diagnostics` (summary before + after), `get_complexity_metrics`, `get_cohesion_metrics`, `find_unused_symbols`, `nuget_vulnerability_scan`, `security_diagnostics`, `get_namespace_dependencies` (`circularOnly`, before + after F1), `find_reflection_usages`, `get_di_registrations`, `semantic_search`, `find_type_usages` (`metadataName=DevicePlatform`), `workspace_reload`. All ran without friction and drove the finding-selection pass. **Implementation phase (5 findings, 19+ edits):** **zero `*_preview`/`*_apply` calls**. Every finding used plain `Edit` -> `workspace_reload` -> `compile_check` as the verify bundle. Raw `Edit` was faster than preview-token round-trips for pinpoint changes (one-liners to ~20-line changes). **Task-by-tool mapping (missed opportunities):** (1) **F1 (namespace cycle fix)** — *high miss*: no single tool fits "same project, different namespace." Took `Write` + `Bash rm` + 5 `Edit` + manual `Grep`/`Read` per consumer (12 files touched). `move_type_to_file_preview` keeps the namespace; `move_file_preview` keeps the namespace declaration; `move_type_to_project_preview` changes the project. **Raised new row: `change-type-namespace-preview`.** (2) **F4 (field retyping `object? -> string?`)** — *medium miss*: `find_references` called once with `metadataName` only -> `InvalidArgument`, fell back to `Grep`. Existing row `find-references-metadataname-parameter-rejected` covers this exact bug; reproduction validated here (6th reproduction across audit sessions). `find_property_writes` on `VersionParseState.Serial` was the ideal tool but requires cursor position (same API inconsistency). (3) **F3 (signature refactor with typed record)** — *medium miss*: `change_signature_preview` only supports add/remove/rename of individual parameters, not "replace 10 parameters with 1 record parameter" — had to use `Edit` + manual record definition. (4) **F2 (eager schema init, remove lock + 3 call-site edits)** — *low miss*: `remove_dead_code_preview` could have handled the field/method removal if pointed at `EnsureSchema` + `_schemaLock` + `_schemaInitialized`; raw `Edit` was faster for 3 scoped edits. (5) **F5 (add method + wire into processor + new test file)** — *medium miss*: `scaffold_test_preview` could have generated a test skeleton for `WebhookStore.PruneCompleted`; used `Write` instead because the test needed seeded JSON on disk (API didn't cover that shape). **Cross-cutting:** `get_coupling_metrics` returned `"No such tool available"` at 2026-04-15T04:17Z — unclear if removed, unreleased, or dropped from the deferred-tool schema in this catalog version; covered by existing `coupling-metrics-tool`. **Verify loop:** `compile_check` reliably clean after each edit (no false positives like `compile-check-stale-assembly-refs-post-reload` — this was a .NET 10.0.5 workspace with no EF drift); `workspace_reload` auto-fire on stale detection worked transparently post-`Edit`. **New backlog rows:** `change-type-namespace-preview`. **Existing rows reinforced:** `semantic-edit-with-compile-check-wrapper` (19+ sequences of `Edit` -> `workspace_reload` -> `compile_check` in this session validate the dominant-pattern claim); `find-references-metadataname-parameter-rejected`; `roslyn-mcp-guided-refactor-flow`; `self-edit-bootstrap-mode-mcp-development` (pattern seen here was NOT self-edit — peer codebase — suggesting the "Edit beats preview-ceremony for pinpoint changes" rule is **workload-driven, not bootstrap-driven**); `composite-split-service-di-registration` (F3's "replace N params with 1 record param" is a close cousin); `coupling-metrics-tool`. **Pattern-level observation:** The economics pushing an MCP-friendly agent toward raw `Edit` are cumulative: each preview+apply adds ~1s round-trip vs `Edit`'s <100ms; a 5-edit refactor is 5s of dead time vs a 500ms `Edit` run. `apply_with_verify` fixes this **only if** the agent was already in a preview flow — which is rare for pinpoint changes. This is the single strongest qualitative datapoint for `semantic-edit-with-compile-check-wrapper` landing. **Recovery note:** This row was initially appended, then lost when a concurrent IT-Chat-Bot audit session rewrote the file; re-added at 2026-04-15T21:00Z. |
