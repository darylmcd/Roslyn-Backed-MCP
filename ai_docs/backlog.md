# Next work and backlog

<!-- purpose: Open work only; contract for agents syncing backlog on ship. -->
<!-- scope: in-repo -->

**updated_at:** 2026-05-05T19:30:00Z

## Agent contract

| | |
|---|---|
| **Scope** | This file lists unfinished work only. It is not a changelog. |
| **MUST** | Remove or update backlog rows when work ships; do it in the same PR or an immediate follow-up. |
| **MUST** | End implementation plans with a final todo: `backlog: sync ai_docs/backlog.md`. |
| **MUST** | Use stable, kebab-case `id` values per open row. |
| **MUST** | Every row's `do` cell summarizes the current need + the concrete next deliverable. Include `Anchors:` (specific source file paths) when the row references code, and evidence (audit/retro/CI signal) when one exists. |
| **MUST** | Size every row to a single bounded initiative — ≤4 production files, ≤3 test files, one regression-test shape. Split heroic multi-bug rows into per-bug children before planning against them. |
| **MUST NOT** | Add `Completed`, `Shipped`, `Done`, `History`, or `Changelog` sections. Git is the archive. |
| **MUST NOT** | Leave done items in the open table. |
| **MUST NOT** | Use `### <id>` body sections per item. The table row IS the canonical form. Items needing long-form depth (more than ~10 lines) link to `ai_docs/items/<id>.md` from the `do` cell. |

## Standing rules

- **Reprioritize on each audit pass.** Stale priority order is a finding.
- **Keep rows planner-ready.** A row is ready when an agent can read it cold and start a plan: name the live anchors and the next concrete deliverable or investigation output.
- **Replace stale umbrella rows with concrete follow-ons** before planning against them.
- **Long-form audit evidence belongs in referenced reports**, not in this file. The `do` cell carries a one-line evidence summary plus the report path.
- **Weak-evidence flag.** When a row's signal is thin (single retro session, self-audit only, etc.) say so explicitly in the `do` cell ("Weaker evidence — N until external session reproduces").
- **Priority tiers:** Critical > High > Medium > Low > Defer.
- See `workflow.md` → **Backlog closure** for close-in-PR expectations.

---

## Critical

<!-- Production-breaking or blocking work. Empty section is fine; keep the header. -->

| id | pri | deps | do |
|----|-----|------|-----|

## High

| id | pri | deps | do |
|----|-----|------|-----|
| `tool-output-schema-infrastructure` | High | none | MCP 2025-06-18 introduces `outputSchema` + `structuredContent` for `tools/call`; this server has none — every result is a stringified JSON wrapped in a `TextContentBlock`, blocking client-side validation, native UI rendering, and small-model token efficiency. Build the infrastructure: extend `[McpToolMetadata]` with an `outputSchemaTypeRef`, generate JSON Schema from existing `*Dto` records (System.Text.Json schema-gen), expose schemas through `ServerSurfaceCatalog`, and update `StructuredCallToolFilter` so `_meta` injection survives both the `content[].text` channel and the new `structuredContent` channel (per spec § "tool that returns structured content SHOULD also return the serialized JSON in a TextContent block"). No tool body changes in this row — wiring only. Anchors: `src/RoslynMcp.Host.Stdio/Catalog/ServerSurfaceCatalog.cs`, `src/RoslynMcp.Host.Stdio/Middleware/StructuredCallToolFilter.cs`, `src/RoslynMcp.Core/Models/` (DTO source for schema gen), `src/RoslynMcp.Host.Stdio/RoslynMcp.Host.Stdio.csproj` (potential `JsonSchema.Net` package add). Regression test shape: 1 round-trip fixture verifying a structured-content response survives the filter (`_meta` injected on both channels, schema validates). Evidence: MCP spec 2025-06-18 § Tools / Structured Content; `Grep "structuredContent\|outputSchema" src` returns only the error-envelope filter, confirming zero adoption. Source: 2026-05-05 MCP-best-practices comparison §3 rec A. See `ai_docs/reports/20260505_mcp-best-practices-restored-rows.md` for provenance. |
| `tool-output-schema-batch-1-server-info-workspace` | High | `tool-output-schema-infrastructure` | First concrete batch consuming the output-schema infrastructure: `server_info`, `server_heartbeat`, `workspace_status`, `workspace_list`, `workspace_health`, `workspace_drift_check`. These are the highest-traffic read tools at session start and have well-defined DTOs already. Successive batches (symbols/navigation, validation, references/consumers, refactoring previews, project mutation, scaffolding) will be filed as separate Tier-1 follow-on rows after this batch proves the wiring. Anchors: `src/RoslynMcp.Host.Stdio/Tools/ServerTools.cs`, `src/RoslynMcp.Host.Stdio/Tools/WorkspaceTools.cs`, `src/RoslynMcp.Host.Stdio/Tools/WorkspaceDriftTool.cs`, `src/RoslynMcp.Core/Models/` (existing DTOs for these tools). Regression test shape: 1 integration test asserting `structuredContent` field is present and schema-valid for each tool in the batch. Evidence: retro `ai_docs/reports/20260504T200153Z_*_multisession-retro.md` shows `server_info` / `workspace_*` are most-called at session start (38 of 40 deep-read sessions). Source: 2026-05-05 MCP-best-practices comparison §3 rec A. See `ai_docs/reports/20260505_mcp-best-practices-restored-rows.md` for provenance. |
| `workspace-cache-store-infrastructure` | High | none | OrchardCore 227-project profile (2026-04-26) shows `workspace_load` P95 = 44.85s — the single dominant cold-start cost in the surface. The deferred `workspace-process-pool-or-daemon` row was waiting for a simpler lever; bounded persistence of *cheap-to-cache, expensive-to-recompute* inputs is that lever, with no daemon, no IPC, no second process. Build `IWorkspaceCacheStore` + on-disk format under `~/.roslyn-mcp/cache/<solution-hash>/<sdk-version>/<msbuild-graph-hash>/` covering (1) evaluated MSBuild project graph and (2) per-project MetadataReference path+mtime list. Compilation snapshots and analyzer state are explicitly out of scope for the MVP. Invalidate by solution-mtime / SDK-version / packages-lock-hash mismatch. No `WorkspaceManager` wiring in this row. Anchors: new `src/RoslynMcp.Core/Services/IWorkspaceCacheStore.cs`, new `src/RoslynMcp.Roslyn/Services/WorkspaceCacheStore.cs`, host DI registration via `src/RoslynMcp.Host.Stdio/AddRoslynMcpHostServices.cs`. Regression test shape: 1 round-trip test (write → read → equality), 1 invalidation test (sdk-version bump invalidates the cache). Evidence: `docs/large-solution-profiling-baseline.md` § Recorded run — OrchardCore (2026-04-26); retro §3 #6 (16 of 40 sessions hit first-load timeouts on >100-project solutions). Source: 2026-05-05 MCP-best-practices comparison §3 rec B. See `ai_docs/reports/20260505_mcp-best-practices-restored-rows.md` for provenance. |
| `workspace-load-uses-cache-fast-path` | High | `workspace-cache-store-infrastructure` | Wire `WorkspaceManager.LoadIntoSessionAsync` to consult the cache store before opening MSBuildWorkspace and to write fresh entries after a successful cold load. Cache hits skip MSBuild SDK resolution and use the persisted project-graph / metadata-reference list; semantic models still build on demand. Cache miss falls through to the existing path with no behavior change. Target: cut `workspace_load` P95 from ~45s toward ~5-10s on warm-cache reload of the same solution. Anchors: `src/RoslynMcp.Roslyn/Services/WorkspaceManager.cs` (LoadIntoSessionAsync, LoadAsync paths), `src/RoslynMcp.Roslyn/Services/WorkspaceExecutionGate.cs` (load gate metrics path). Regression test shape: 1 load-then-restart-load timing assertion against the SampleSolution fixture verifying cache-hit path < cold-load. Evidence: see `workspace-cache-store-infrastructure`. Follow-up: re-run `eng/profile-large-solution.ps1` against OrchardCore after merge and append the new row to `docs/large-solution-profiling-baseline.md`. Source: 2026-05-05 MCP-best-practices comparison §3 rec B. See `ai_docs/reports/20260505_mcp-best-practices-restored-rows.md` for provenance. |
| `progress-emit-audit-coverage` | High | none | Server advertises `progress: true` capability and 8 tools currently take `IProgress<ProgressNotificationValue>`, but coverage is uneven — `workspace_load` (40s+ on representative scale), `workspace_warm` (17.32s P95), `build_workspace`, and `test_run` are the long-blocking outliers and should each emit fine-grained stage progress (e.g., `evaluating msbuild → restoring → opening 47/227 projects → done`) so clients can render meaningful UI and agents can branch on the signal mid-flight. Audit each long-running tool: emits progress, or documented as "fast enough not to need it." Add emission points where missing. Anchors: `src/RoslynMcp.Host.Stdio/Tools/ProgressHelper.cs`, `src/RoslynMcp.Host.Stdio/Tools/WorkspaceTools.cs` (workspace_load, workspace_warm), `src/RoslynMcp.Host.Stdio/Tools/ValidationTools.cs` (build_workspace, test_run). Regression test shape: 1 fixture verifying the new emission points fire with the expected stage labels. Evidence: retro §3 #6 (cold-load timeout cluster); `docs/large-solution-profiling-baseline.md` (workspace_load 44.85s, workspace_warm 17.32s on OrchardCore). Source: 2026-05-05 MCP-best-practices comparison §3 rec C. See `ai_docs/reports/20260505_mcp-best-practices-restored-rows.md` for provenance. |

## Medium

| id | pri | deps | do |
|----|-----|------|-----|
| `audit-deep-skill-migration` | Medium | none | Convert the user-global slash-command `~/.claude/commands/audit-deep.md` into a plugin-shipped skill at `skills/audit-deep/`. Today the skill points at `<repo>/ai_docs/prompts/deep-review-and-refactor.md` so each consuming C# repo must own a current copy of an 872-line prompt; that drifts. Move the prompt into the skill so it ships with the plugin. Apply the open skill bugs during migration: (B1) soften the read-only hard rule to "read-only against audited repo's main; Phase 6 mutations confined to a disposable worktree the prompt creates"; (B2) align the `mode` vocabulary with the prompt's `full|promotion-only|read-only` (drop `focused`, or document it as fallback-only); (B3) prune the dead `no-holds-barred-audit.md` resolution branch; (B4/B5) require the Roslyn MCP server (`mcp__roslyn__server_info`) — halt instead of running a generic non-MCP fallback; (S2) delegate Phase 0's drift-detection step to `/surface-audit` when available; (S5) add `scripts/archive-old-reports.ps1` to move `ai_docs/audit-reports/*.md` older than N days into `archive/<YYYY>/`. Anchors: new `skills/audit-deep/SKILL.md`, new `skills/audit-deep/prompts/{full,promotion-only,read-only}.md` (split the 872-line monolith), new `skills/audit-deep/scripts/archive-old-reports.ps1`, plus deprecation note in `~/.claude/commands/audit-deep.md` (consumer-side concern; not edited in this repo). Regression test shape: 1 fixture verifying SKILL.md frontmatter parity + tool-reference validity against the live catalog (matches Phase 16b's per-skill checks). Evidence: 2026-05-05 audit-deep deep-dive (this session); identified 10 guidance gaps fixed in PR #494 via Wave 1 and 6 architectural issues queued here for Wave 2. **Rule 3 structural-unit exemption: 4 units (skill + 3 mode prompts).** |
| `audit-deep-subagent-orchestration` | Medium | `audit-deep-skill-migration` | Refactor the Phase-runner inside `audit-deep` to offload context-heavy phases to subagents, leaving the main agent as orchestrator. Today the 872-line prompt runs inline; a `mode=full` run can take 90–180min and consume large chunks of the orchestrator's context window for raw tool output. Per the prompt's principle #1 ("delegate long-running/log-heavy validation to subagents"), formalize: Phase 1 (broad diagnostics scan), Phase 2 (metrics), Phase 8 (build/test), Phase 8b (concurrency stress) become subagent tasks returning structured summaries to the orchestrator. Phase 6 (refactoring) and the preview/apply chains stay inline (workspace-version-sensitive per principle #3). Anchors: new `.claude/agents/audit-phase-runner.md` for the subagent definition, plus orchestration logic in `skills/audit-deep/SKILL.md` (post-migration). Regression test shape: 1 fixture with a small fixture solution exercising the orchestrator-subagent handoff for a single phase, asserting the orchestrator receives a structured-summary message back, not raw tool output. Evidence: 2026-05-05 audit-deep deep-dive (this session) — context-budget concern flagged as principle #12 in the existing prompt; mature offload pattern not yet implemented. **Depends on `audit-deep-skill-migration` because the subagent invocation needs to live in the post-migration skill structure.** |
| `workspace-cache-prewarm-on-load` | Medium | `workspace-load-uses-cache-fast-path` | Opt-in `prewarm: true` flag on `workspace_load` that, after the initial open, runs `workspace_warm` and persists the resulting compilation hashes / per-project warm artifacts to the cache store from `workspace-cache-store-infrastructure`. Closes the otherwise-separate `workspace-warm-default-above-50-projects` candidate by making warm an explicit, cacheable step. Anchors: `src/RoslynMcp.Roslyn/Services/WorkspaceManager.cs`, `src/RoslynMcp.Roslyn/Services/WorkspaceWarmService.cs`. Regression test shape: 1 test verifying prewarm artifacts present after `workspace_load(prewarm=true)` and reused on the next load. Evidence: retro §4 #6; OrchardCore profile `workspace_warm` P95 = 17.32s. Source: 2026-05-05 MCP-best-practices comparison §3 Tier 1 follow-on. See `ai_docs/reports/20260505_mcp-best-practices-restored-rows.md` for provenance. |
| `elicit-disambiguation-on-multi-symbol-resolve` | Medium | none | When `symbol_search`, `find_references`, `go_to_definition`, or sibling navigation tools resolve a name to multiple symbols (overloads, partial classes, inherited members), use MCP `elicitation/create` (2025-06-18, wrapped by the C# SDK as `McpServer.ElicitAsync`) to ask the user to pick one instead of returning the disambiguation list and forcing a follow-up call. Falls back gracefully to the current disambiguation-list response when the client does not declare the `elicitation` capability — purely additive on capable clients, no breaking change on incapable clients. Anchors: `src/RoslynMcp.Host.Stdio/Tools/SymbolTools.cs`, `src/RoslynMcp.Roslyn/Helpers/SymbolHandleSerializer.cs`, `src/RoslynMcp.Host.Stdio/Middleware/StructuredCallToolFilter.cs` (capability-check helper). Regression test shape: 1 elicit-supported test (mock client returns "accept"), 1 fallback test (no elicitation capability → current list response). Evidence: MCP 2025-06-18 § Elicitation; retro §2a row 4 (29 `NotFound: No symbol` envelopes after locator ambiguity in 17 of 40 sessions). Source: 2026-05-05 MCP-best-practices comparison §3 rec D. See `ai_docs/reports/20260505_mcp-best-practices-restored-rows.md` for provenance. |
| `elicit-workspace-path-on-missing-required-arg` | Medium | none | Add a fallback in `StructuredCallToolFilter` that, on `InvalidArgument: missing 'path'` from `workspace_load`, attempts `elicitation/create` to ask the user for the path before returning the error envelope (clients without elicitation get the current `schemaHint`-augmented envelope). Targets the largest `InvalidArgument` cluster from the retro. Strict scope: only paths the agent has clearly intended to provide qualify; never credentials or sensitive arguments (per MCP spec § Elicitation security: "Servers MUST NOT request sensitive information"). Anchors: `src/RoslynMcp.Host.Stdio/Middleware/StructuredCallToolFilter.cs`, `src/RoslynMcp.Host.Stdio/Tools/WorkspaceTools.cs`. Regression test shape: 1 elicit-supported test, 1 fallback test, 1 sensitive-data-refused test (attempts to elicit a credential field are dropped without sending the request). Evidence: retro §3 #2 (199 `InvalidArgument` matches across 25 of 40 deep-read sessions). Source: 2026-05-05 MCP-best-practices comparison §3 rec D. See `ai_docs/reports/20260505_mcp-best-practices-restored-rows.md` for provenance. |
| `apply-with-verify-diff-not-counts` | Medium | none | `apply_with_verify` rolls back when post-apply error counts increase, but ~5 of 36 rollbacks observed in the 14-day retro window were false positives — a pre-existing diagnostic flipped severity class on the post-apply build path even though the apply itself was innocent. Switch the verify step to diff-based: compare diagnostic identity (id + file + line) pre/post-apply rather than counts. The diff logic already exists for `validate_recent_git_changes`; reuse it. Anchors: `src/RoslynMcp.Host.Stdio/Tools/ApplyWithVerifyTool.cs`, `src/RoslynMcp.Host.Stdio/Tools/ValidationBundleTools.cs` (diff helper to reuse), the underlying `IApplyWithVerifyService` impl in `src/RoslynMcp.Roslyn/Services/`. Regression test shape: 1 false-positive scenario (pre-existing diagnostic, innocent apply, MUST NOT roll back), 1 true-positive scenario (apply introduces new diagnostic, MUST roll back). Evidence: retro §3 #4 (14 of 40 sessions had rollbacks; ~14% false-positive rate). Source: 2026-05-05 MCP-best-practices comparison §3 rec E. See `ai_docs/reports/20260505_mcp-best-practices-restored-rows.md` for provenance. |
| `workspace-warm-default-above-50-projects` | Medium | none — may be obsoleted by `workspace-cache-prewarm-on-load` | Default-on `workspace_warm` after `workspace_load` when the loaded solution exceeds a project-count threshold (e.g., 50), unless the caller passes `warm: false`. Closes the long-tail timeout pattern from the retro for cold-start sessions that don't know to call `workspace_warm` explicitly. If `workspace-cache-prewarm-on-load` ships first, this row collapses to a 1-line default flip on the new `prewarm` flag and should be re-scoped accordingly. Anchors: `src/RoslynMcp.Roslyn/Services/WorkspaceManager.cs`, `src/RoslynMcp.Roslyn/Services/WorkspaceWarmService.cs`. Regression test shape: 1 above-threshold test (warm fires automatically), 1 below-threshold test (warm doesn't fire), 1 explicit `warm: false` opt-out test. Evidence: retro §4 #6; OrchardCore profile cold/warm gap (workspace_load 44.85s vs subsequent symbol_search 1.18s). Source: 2026-05-05 MCP-best-practices comparison §3 rec F. See `ai_docs/reports/20260505_mcp-best-practices-restored-rows.md` for provenance. |
| `mcp-registry-publication` | Medium | none | One-time submission to the public MCP Registry (live since late 2025) for discoverability beyond the GitHub plugin marketplace. Roadmap-aligned ("publish to the MCP Registry for broader discoverability beyond GitHub"). Pure outreach + manifest work, not a code change in `src/`. Anchors: `.claude-plugin/plugin.json`, `.claude-plugin/marketplace.json`, root `README.md` (registry badge / install snippet). Regression test shape: none — submission and link-back only. Evidence: `docs/roadmap.md` § Claude Code Plugin Distribution → "publish to the MCP Registry"; current registry submission process to confirm at submission time (the registry's API and listing format may have evolved since the roadmap entry was written). Source: 2026-05-05 MCP-best-practices comparison §3 rec G. See `ai_docs/reports/20260505_mcp-best-practices-restored-rows.md` for provenance. |

## Low

| id | pri | deps | do |
|----|-----|------|-----|
| `promote-tier-skill` | Low | none | Add a `/roslyn-mcp:promote-tier <tool-or-resource-or-prompt> <stable\|experimental>` skill that automates the tier flip the `/publish-preflight` Step 8 gate currently surfaces as a manual checklist. For a tool: locate the `[McpToolMetadata("category", "experimental", ...)]` annotation on the tool's method (use `mcp__roslyn__symbol_search` to find the method by tool name; the literal lives at parameter index 1 of the attribute), edit the literal, locate the matching entry in `src/RoslynMcp.Host.Stdio/Catalog/ServerSurfaceCatalog.<Category>.cs`, edit that literal, run `dotnet test --filter SurfaceCatalogTests` to confirm parity. For resources: edit `src/RoslynMcp.Host.Stdio/Resources/ServerResources.cs`. For prompts: edit `src/RoslynMcp.Host.Stdio/Prompts/RoslynPrompts.*.cs`. Anchors: new `.claude/skills/promote-tier/SKILL.md` (maintainer-side; this is an internal release-cut helper, not a consumer-facing plugin skill). Regression test shape: 1 fixture exercising a round-trip — promote a known experimental tool to stable, build to confirm `SurfaceCatalogTests` parity passes, then revert via the inverse call (`promote-tier <tool> experimental`) and reconfirm. Evidence: 2026-05-05 audit-deep promotion-gate landed (PRs #494 + #496); `/publish-preflight` Step 8 today emits a manual `Edit: ...` checklist for each `recommendation: "promote"` row — this skill replaces that with one tool call. Pieces A and B (scorecard JSON emission + publish-preflight gate) shipped in PR #496; this row covers only piece C (automation). |
| `sampling-driven-tool-flows-spike` | Low | none | Half-day spike to identify which (if any) existing tools genuinely benefit from server-initiated MCP `sampling/createMessage` (C# SDK: `McpServerExtensions.SampleAsync`) rather than relying on the agent's outer model. Candidates worth prototyping: auto-XML-doc generation from method signature + nearby call graph, refactor-summary text from `workspace_changes`, auto-test-name from method name + test-class context. Output: one-page note in `ai_docs/items/sampling-spike.md` with go/no-go per candidate; per-candidate follow-up rows filed only on go. Defer enabling sampling on any tool until the spike confirms a context-advantage over the agent's outer loop. Anchors: candidate tool sites — `src/RoslynMcp.Host.Stdio/Tools/ScaffoldingTools.cs`, plus an XML-doc tool site to confirm at spike time. Regression test shape: defer to per-tool follow-up. Evidence: MCP 2025-06-18 § Sampling; speculative — no in-window incident driving this. **Weaker evidence — N until at least one candidate shows clear value.** Source: 2026-05-05 MCP-best-practices comparison §3 rec I. See `ai_docs/reports/20260505_mcp-best-practices-restored-rows.md` for provenance. |
| `tool-surface-pagination-or-tool-sets` | Low | none | 167 tools is approaching small-model discovery saturation. MCP `tools/list` already supports cursor pagination per spec; layered on top, named "tool sets" (`navigation`, `refactoring`, `validation`, `analysis`) would let clients enable subsets via `server_info` user-config. Speculative — not yet causing observable friction in the retro window — but the surface continues to grow. Track only; act when (1) a small-model client reports tool-discovery friction, OR (2) surface count crosses a hard threshold (~200 tools). Anchors: `src/RoslynMcp.Host.Stdio/Catalog/ServerSurfaceCatalog.cs`, `src/RoslynMcp.Host.Stdio/Resources/ServerResources.cs`. Regression test shape: defer until acted on. Evidence: surface count drift across recent versions (`server_info.surface.registered.tools` 151 → 167 since v1.27); MCP spec § tools/list pagination. **Weaker evidence — N until small-model discovery friction reported externally.** Source: 2026-05-05 MCP-best-practices comparison §3 rec J. See `ai_docs/reports/20260505_mcp-best-practices-restored-rows.md` for provenance. |

## Defer

<!-- Explicitly parked. Record WHY in the `do` cell. -->

| id | pri | deps | do |
|----|-----|------|-----|
| `validate-locator-preflight-tool` | Defer | re-measurement window after `inv-arg-envelope-schema-hint` (merged 2026-05-05 via PR #483) | Deferred 2026-05-05 by `/backlog-sweep:execute` per the plan-time recommendation in `ai_docs/plans/20260504T203132Z_backlog-sweep/plan.md` §6. After `inv-arg-envelope-schema-hint` shipped, re-measure InvalidArgument-on-locator-tools rate over a 7-day window before deciding whether to ship this tool. If `schemaHint` reduces locator-shape errors ≥80%, mark obsolete; otherwise re-plan and ship. Re-evaluate after 2026-05-12. Anchors: new tool surface in `src/RoslynMcp.Host.Stdio/Tools/SymbolLocatorTool.cs`, reuses existing `src/RoslynMcp.Host.Stdio/Tools/SymbolLocatorFactory.cs` helper from PR #474, catalog registration. Regression test shape: 1 fixture covering valid file/line, valid metadataName, malformed metadataName (parenthesized), unparseable symbolHandle, fully empty locator. Evidence: `ai_docs/reports/20260504T200153Z_roslyn-backed-mcp_roslyn-mcp-multisession-retro.md` §2b row 1 / §4#4 — 6 sessions hit the post-hoc shape error and would have benefited from pre-flight validation. |
| `http-streamable-host-project` | Defer | concrete remote-deployment driver (named users, auth/observability/tenancy plan approved and staffed) | Add `src/RoslynMcp.Host.Http/` as a sibling project that reuses `Core` + `Roslyn`, exposing the same surface over MCP Streamable HTTP transport. Roadmap-aligned but requires multi-week design (auth flows, per-tenant rate limiting, TLS, observability, deployment story). The current local-first product is healthy and the roadmap explicitly punts on these concerns. Re-evaluate when there is a concrete remote-deployment ask with named users / SLA expectations. Anchors: new `src/RoslynMcp.Host.Http/` project; `Core` + `Roslyn` boundaries are already transport-agnostic per `ai_docs/architecture.md`. Evidence: `docs/roadmap.md` § HTTP/SSE Hosting (deferred to a second host project); MCP spec § Streamable HTTP transport (2025-03 stable). Source: 2026-05-05 MCP-best-practices comparison §3 rec H. See `ai_docs/reports/20260505_mcp-best-practices-restored-rows.md` for provenance. |
| `workspace-process-pool-or-daemon` | Defer | future worse-profile evidence | Representative 227-project OrchardCore profile captured on 2026-04-26 did not justify daemon/process-pool implementation: `workspace_load` P95 was 44.85s, `symbol_search` P95 was 1.18s, and `find_references` P95 was 997ms, all below `docs/large-solution-profiling-baseline.md` thresholds. Keep this deferred unless a larger/worse customer-scale profile or daily-use evidence shows `workspace_load` / reload P95 blocking work after `workspace_warm`; then produce a bounded design note comparing daemon, process-pool, and shared-workspace approaches, including lifecycle and failure-isolation hooks. Evidence: `docs/large-solution-profiling-baseline.md` recorded OrchardCore run; local raw artifacts under `artifacts/large-solution-profiling/20260426T212443Z/`. |

## Refs

| Path | Role |
|------|------|
| `ai_docs/planning_index.md` | Planning router and scope boundary |
| `ai_docs/workflow.md` | Branch/PR workflow and backlog-closure rule |
| `ai_docs/prompts/backlog-sweep-plan.md` | Planner prompt; enforces per-initiative Rule 1 (bundle only on shared code path) / Rule 3 (≤4 prod files) / Rule 3b (toolPolicy) / Rule 4 (≤3 test files) / Rule 5 (≤80K context). Each row here is sized for one initiative. |
| `ai_docs/prompts/backlog-sweep-execute.md` | Executor companion; consumes the `state.json` the planner emits and vets each initiative against Rules 3/4/5 before starting work. |
| `ai_docs/bootstrap-read-tool-primer.md` | Self-edit session read-only tool primer (Roslyn-MCP read-side tools to prefer over Bash/Grep). |
| `ai_docs/runtime.md` | Bootstrap scope policy — distinguishes main-checkout self-edit (no `*_apply`) from worktree/parallel-subagent sessions. |
| `docs/large-solution-profiling-baseline.md` | Evidence gate for daemon/process-pool performance work. |
| `ai_docs/procedures/deep-review-backlog-intake.md` | Intake procedure for future audit batches. |
| `review-inbox/` | Staging folder for the NEXT audit batch (flat directory; `/backlog-intake` reads here). |
| `review-inbox/archive/<batch-ts>/` | Processed audit/retro/promotion batches — one subdirectory per successful intake. Keep until every row sourced from a batch is closed or superseded, then delete that subdirectory. |
| `ai_docs/plans/20260428T124405Z_backlog-sweep/plan.md` | Backlog sweep (20260428T124405Z). Shipped 7 initiatives across 7 PRs (#467, #468, #471, #473, #474, #476, #477); closed 7 backlog rows. |
| `ai_docs/plans/20260504T203132Z_backlog-sweep/plan.md` | Backlog sweep (20260504T203132Z). Shipped 5 initiatives across 5 PRs (#483, #485, #486, #488, #490); closed 5 backlog rows. Initiative #6 (`validate-locator-preflight-tool`) deferred pending re-measurement of locator-shape error rate after #1's `schemaHint` lands — re-evaluate after 2026-05-12. |
| `ai_docs/reports/20260505_mcp-best-practices-restored-rows.md` | Provenance + recovery context for the 14 backlog rows added 2026-05-05 from the MCP-best-practices comparison (rec A–J + Tier 1 follow-on). Maps each row id → recommendation letter; lists cross-cutting evidence sources. Read this first when picking up any of the rec-A through rec-J rows. |
