# Post-1.5.0 surface audit — 2026-04-04

Lightweight audit after v1.5.0 release hardening and concurrent implementation of v1.6.0 (tests, promotions, profiling notes). Full 18-phase execution remains in [`../prompts/deep-review-and-refactor.md`](../prompts/deep-review-and-refactor.md).

## Scope

| Area | Status | Notes |
|------|--------|--------|
| Catalog vs registered MCP surface | **PASS** | `SurfaceCatalogTests.ServerSurfaceCatalog_CoversAllRegisteredToolsResourcesAndPrompts` aligns `ServerSurfaceCatalog` with `[McpServerTool]` / resource / prompt attributes. |
| Tier promotions (6 tools → stable) | **PASS** | `compile_check`, `list_analyzers`, `find_consumers`, `get_cohesion_metrics`, `find_shared_members`, `analyze_snippet` updated in `ServerSurfaceCatalog` and `docs/product-contract.md`; semver 1.6.0. |
| Contract docs | **PASS** | `docs/experimental-promotion-analysis.md` aggregate counts refreshed (56 stable / 67 experimental tools). |
| Integration coverage | **PASS** | Added `HighValueCoverageIntegrationTests`, `BoundedStoreEvictionTests`, `ServiceCollectionExtensionsTests` for P1/P2 gaps in `docs/coverage-baseline.md`. |
| Profiling methodology | **PASS** | `docs/large-solution-profiling-baseline.md` includes a sample-solution smoke record; large (50+ project) run still **pending** when a representative solution is available. |
| CI / merge gate | **PASS** | Per `CI_POLICY.md` — run `./eng/verify-release.ps1` before merge. |

## Findings (non-blocking)

1. **Code actions:** Roslyn code-fix/refactoring availability varies by SDK and analyzer load; integration tests use deterministic `CodeActionService` edge cases (unknown file → empty; invalid index → `ArgumentException`) instead of asserting specific fix titles.
2. **Large-solution performance:** No measured P95 on a 50+ project solution yet; defer indexing/caching epic until metrics justify it (`docs/roadmap.md`).

## Follow-up

- Re-run full deep-review prompt after the next major MCP surface change (appendix version in `deep-review-and-refactor.md`).
- When a large customer repo is available, fill the P50/P95 table in `docs/large-solution-profiling-baseline.md` and open `ai_docs/backlog.md` only if thresholds fail.
