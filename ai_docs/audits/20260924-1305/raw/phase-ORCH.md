# Orchestrator raw-harness findings (C1/C4/C6) — installed 4.2.1 + HEAD e24a48a4

Captures: `raw/{installed,head}-*.json`, `raw/installed-roots-pass2.json`, `raw/head-noroots-pass2.json`, `raw/installed-logging-setlevel.json`.

| id | sev | check | tool | anchor | capture | fixedAtHead |
|---|---|---|---|---|---|---|
| O1 sanctioned-roots-unconfigured-error-misattributed | High | C1 | workspace_load (all path-taking tools) | src/RoslynMcp.Host.Stdio/Tools/ClientRootPathValidator.cs:153 ; ToolErrorHandler.cs:646 | server without ROSLYNMCP_SANCTIONED_ROOTS: `workspace_load{path:"<repo>/.../SampleSolution.slnx"}` → `InvalidArgument: Parameter 'path' is invalid. Check that all required parameters are provided and values match the expected types.` Real cause only on stderr: "Filesystem boundary is not configured… fail-closed". MCP roots offered by client are never requested (no roots/list in transcript). Same on HEAD. | false |
| O2 argument-exceptions-without-paramname-render-unknown | Medium | C1 | symbol_search + 16 other ValidatePagination callers | src/RoslynMcp.Host.Stdio/Tools/ParameterValidation.cs:60 (also :63, :66; 18 single-line paramless throws repo-wide) | `symbol_search{limit:-3}` → `Parameter '<unknown>' is invalid. Check that all required parameters…` — server-authored bound ("Limit must be greater than 0") is redacted because it is a plain ArgumentException with no ParamName. | false |
| O3 workspace-close-leaks-test-seams-into-schema | Medium | C4 | workspace_close | src/RoslynMcp.Host.Stdio/Tools/WorkspaceTools.cs:143 (+:146; stale comment :141-142) | tools/list: `getProcessesByName:{"$comment":"Unsupported .NET type","not":true}`, `processDrainTimeout:{TimeSpan pattern}` both undocumented; comment claims "no [Description], so the schema omits it" — false. Client can set processDrainTimeout (e.g. "23:59:59") to extend the bounded drain. | false |
| O4 unknown-arguments-silently-ignored | Medium | C4 | all 175 tools | src/RoslynMcp.Host.Stdio (schema generation; no additionalProperties:false) | `compile_check{workspaceId, severty:"Error"}` → success, filter silently dropped; `workspace_status{…,__bogus:1}` → success. 175/175 inputSchemas lack additionalProperties:false. Typos in optional filters silently widen results. | false |
| O5 prompt-missing-arg-error-omits-arg-name | Low | C3 | prompts/get (all 20) | src/RoslynMcp.Host.Stdio/Middleware/PromptBindingStageAdapter.cs:73 (+:85, :90) | `prompts/get{name:"review_file",arguments:{}}` → -32602 "Invalid parameters for prompt 'review_file'. Provide every required argument using the advertised parameter types." — parameter.Name available but not passed. | false |
| O6 logging-capability-parity | Low | C6 | initialize vs server_info | src/RoslynMcp.Host.Stdio/Tools/ServerTools.cs:150 | initialize advertises `capabilities.logging:{}` and `logging/setLevel` → `{}` success; server_info reports `capabilities.logging:false`; no notifications/message ever emitted. | false |
| O7 nuget-scan-openworld-annotation | Low | C4 | nuget_vulnerability_scan | src/RoslynMcp.Host.Stdio/Tools/SecurityTools.cs:60 | annotation `openWorldHint:false` but tool shells `dotnet list package --vulnerable` against NuGet feeds (network). No tool in the surface sets openWorldHint:true. | false |
| O8 catalog-page-silent-clamp | Low | C2 | resource server_catalog_tools_page / prompts_page | src/RoslynMcp.Host.Stdio/Catalog/ServerSurfaceCatalog.cs:272 | `roslyn://server/catalog/tools/-1/0` → 200 OK offset 0 limit 1 (template doc says limit 1-200); `…/0/999` → limit 200. Invalid slots should be InvalidArgument like the tool-side ValidatePagination. | false |
| O9 output-schema-coverage | Low | C4 | 167/175 tools | src/RoslynMcp.Host.Stdio/Tools/*.cs | only 8 tools (server_*/workspace_* status family) declare outputSchema + structuredContent (all 8 validated clean); the other 167 return JSON as text only. | n/a |
| O10 get-source-text-notfound-generic | Low | C1 | get_source_text (+find_references by location) | src/RoslynMcp.Host.Stdio/Tools/ToolErrorHandler.cs:568 | `get_source_text{filePath:"C:/nonexistent/x.cs"}` → NotFound "The requested item was not found. Ensure the workspace is loaded and the identifier is correct." — doesn't say the file isn't in the workspace. Path traversal `../../Windows/win.ini` → same NotFound (no leak — good). | false |

PASS evidence: annotations match catalog readOnly/destructive for 174/174; catalog counts == server_info.surface (113/61, 9/5, 0/20, parityOk); 8 outputSchema tools' structuredContent validates; symbol_search idempotent; unicode/20k-char queries handled (≤90 ms); empty query returns actionable note; bad catalog-diff pair → actionable InvalidArgument; unknown resource URI → -32002 with URI echoed; all 166 empty-arg probes returned structured InvalidArgument (none crashed, none timed out, max <1 s). HEAD adds tool `apply_composite` (175 vs 174).
